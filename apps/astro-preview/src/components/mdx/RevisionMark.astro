---
import { Milestone } from "lucide-astro";

interface Props {
  class?: string;
  icon?: boolean;
}

const { class: className = "", icon = true } = Astro.props;
---

<span class:list={["cpp-revision-mark", className]}>
  {icon && <Milestone aria-hidden="true" />}
  <span><slot /></span>
</span>
