---
title: "C++ named requirements: SharedTimedMutex (since C++14)"
description: "The SharedTimedMutex requirements extend the TimedMutex requirements to include shared lock ownership mode."
source_url: "https://en.cppreference.com/w/cpp/named_req/SharedTimedMutex"
language: "C++"
---

{/* source:cpp-named-req-sharedtimedmutex:0000 */}
The *SharedTimedMutex* requirements extend the [`TimedMutex`](/docs/cpp/library/named_req/TimedMutex) requirements to include shared lock ownership mode.

### Requirements

{/* source:cpp-named-req-sharedtimedmutex:0001 */}
- [`TimedMutex`](/docs/cpp/library/named_req/TimedMutex)
- [`SharedMutex`](/docs/cpp/library/named_req/SharedMutex)

{/* source:cpp-named-req-sharedtimedmutex:0002 */}
Additionally, an object `m` of *SharedTimedMutex* type supports timed shared operations:

{/* source:cpp-named-req-sharedtimedmutex:0003 */}
- The expression `m.try_lock_shared_for(duration)` has the following properties

{/* source:cpp-named-req-sharedtimedmutex:0004 */}
  - Behaves as an atomic operation.
  - Attempts to obtain shared ownership of the mutex within the duration specified by `duration`. If `duration` is less or equal `duration.zero()`, attempts to obtain the ownership without waiting (as if by `try_lock()`). Otherwise, this function blocks until the mutex is acquired or until the time specified by `duration` passes. It returns within `duration` only if it succeeds, but it allowed to fail to acquire the mutex even if at some point in time during `duration` it was not owned by another thread. In any case, it returns `true` if the mutex was acquired and `false` otherwise.
  - If `try_lock_shared_for(duration)` succeeds, prior `unlock()` operations on the same object *synchronize-with* this operation (equivalent to release-acquire [`std::memory_order`](/docs/cpp/library/atomic/memory_order)).
  - The behavior is undefined if the calling thread already owns the mutex in any mode.
  - An exception may be thrown by clock, time point, or duration during the execution (clocks, time points, and durations provided by the standard library never throw).
  - If an exception is thrown, the shared lock is not acquired.

{/* source:cpp-named-req-sharedtimedmutex:0005 */}
- The expression `m.try_lock_shared_until(time_point)` has the following properties

{/* source:cpp-named-req-sharedtimedmutex:0006 */}
  - Behaves as an atomic operation.
  - Attempts to obtain shared ownership of the mutex within the time left until `time_point`. If `time_point` already passed, attempts to obtain the ownership without locking (as if by `try_lock()`). Otherwise, this function blocks until the mutex is acquired or until the time specified by `time_point` passes. It returns before `time_point` only if it succeeds, but it allowed to fail to acquire the mutex even if at some point in time before `time_point` it was not owned by another thread. In any case, it returns `true` if the mutex was acquired and `false` otherwise.
  - If `try_lock_shared_until(time_point)` succeeds, prior `unlock()` operations on the same object *synchronize-with* this operation (equivalent to release-acquire [`std::memory_order`](/docs/cpp/library/atomic/memory_order)).
  - The behavior is undefined if the calling thread already owns the mutex in any mode.
  - An exception may be thrown by clock, time point, or duration during the execution (clocks, time points, and durations provided by the standard library never throw).
  - If an exception is thrown, the shared lock is not acquired.

### Standard library

{/* source:cpp-named-req-sharedtimedmutex:0007 */}
The following standard library types satisfy *SharedTimedMutex* requirements:

{/* source:cpp-named-req-sharedtimedmutex:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`shared_timed_mutex`](/docs/cpp/library/atomic/shared_timed_mutex) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>provides shared mutual exclusion facility and implements locking with a timeout (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### See also

{/* source:cpp-named-req-sharedtimedmutex:0009 */}
- [`Thread support library`](/docs/cpp/library/atomic)
- [`Mutex`](/docs/cpp/library/named_req/Mutex)
- [`TimedMutex`](/docs/cpp/library/named_req/TimedMutex)
- [`SharedMutex`](/docs/cpp/library/named_req/SharedMutex)
