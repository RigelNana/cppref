---
title: "C++ named requirements: SharedTimedLockable (since C++14)"
description: "The SharedTimedLockable requirements describe the characteristics of types that provide timed shared blocking semantics for execution agents (threads, processes, tasks)."
source_url: "https://en.cppreference.com/w/cpp/named_req/SharedTimedLockable"
language: "C++"
---

{/* source:cpp-named-req-sharedtimedlockable:0000 */}
The *SharedTimedLockable* requirements describe the characteristics of types that provide timed shared blocking semantics for execution agents (threads, processes, tasks).

### Requirements

{/* source:cpp-named-req-sharedtimedlockable:0001 */}
- [`SharedLockable`](/docs/cpp/library/named_req/SharedLockable)

{/* source:cpp-named-req-sharedtimedlockable:0002 */}
For type `L` to be *SharedTimedLockable*, given

{/* source:cpp-named-req-sharedtimedlockable:0003 */}
- `rel_time`, a value of a specialization of [`std::chrono::duration`](/docs/cpp/library/chrono/duration), and
- `abs_time`, a value of a specialization of [`std::chrono::time_point`](/docs/cpp/library/chrono/time_point),

{/* source:cpp-named-req-sharedtimedlockable:0004 */}
the following conditions have to be satisfied for an object `m` of type `L`:

{/* source:cpp-named-req-sharedtimedlockable:0005 */}
| Expression | Requires | Return value |
| --- | --- | --- |
| `m.try_lock_shared_for(rel_time)` | Blocks for the provided duration `rel_time` or until a lock on `m` is acquired. | `true` if the lock was acquired, `false` otherwise. |
| `m.try_lock_shared_until(abs_time)` | Blocks until the provided time point `abs_time` is reached or a lock on `m` is acquired. | `true` if the lock was acquired, `false` otherwise. |

### Notes

{/* source:cpp-named-req-sharedtimedlockable:0006 */}
The `try_lock_shared_for` and `try_lock_shared_until` member functions obtain a shared lock on `m` on success.

### Standard library

{/* source:cpp-named-req-sharedtimedlockable:0007 */}
The following standard library types satisfy *SharedTimedLockable* requirements:

{/* source:cpp-named-req-sharedtimedlockable:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`shared_timed_mutex`](/docs/cpp/library/thread/shared_timed_mutex) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>provides shared mutual exclusion facility and implements locking with a timeout (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### See also

{/* source:cpp-named-req-sharedtimedlockable:0009 */}
- [`Thread support library`](/docs/cpp/library/atomic)
- [`SharedTimedMutex`](/docs/cpp/library/named_req/SharedTimedMutex)
- [`SharedLockable`](/docs/cpp/library/named_req/SharedLockable)
