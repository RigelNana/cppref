---
title: "C++ named requirements: SharedLockable (since C++14)"
description: "The SharedLockable requirements describe the minimal characteristics of types that provide shared blocking semantics for execution agents (i.e. threads)."
source_url: "https://en.cppreference.com/w/cpp/named_req/SharedLockable"
language: "C++"
---

{/* source:cpp-named-req-sharedlockable:0000 */}
The *SharedLockable* requirements describe the minimal characteristics of types that provide shared blocking semantics for execution agents (i.e. threads).

### Requirements

{/* source:cpp-named-req-sharedlockable:0001 */}
For type `L` to be *SharedLockable*, the following conditions have to be satisfied for an object `m` of type `L`:

{/* source:cpp-named-req-sharedlockable:0002 */}
| Expression | Preconditions | Effects | Return value |
| --- | --- | --- | --- |
| `m.lock_shared()` |  | Blocks until a lock can be obtained for the current execution agent (thread, process, task). If an exception is thrown, no lock is obtained. |  |
| `m.try_lock_shared()` |  | Attempts to obtain a lock for the current execution agent (thread, process, task) without blocking. If an exception is thrown, no lock is obtained. | `true` if the lock was obtained, `false` otherwise |
| `m.unlock_shared()` | The current execution agent holds a shared lock on `m`. | Releases the shared lock held by the execution agent.<br />Throws no exceptions. |  |

#### Shared locks

{/* source:cpp-named-req-sharedlockable:0003 */}
A lock on an object is said to be *shared lock* if it is acquired by a call to `lock_shared`, `try_lock_shared`, `try_lock_shared_for`, or `try_lock_shared_until` member function.

### Standard library

{/* source:cpp-named-req-sharedlockable:0004 */}
The following standard library types satisfy *SharedLockable* requirements:

{/* source:cpp-named-req-sharedlockable:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`shared_mutex`](/docs/cpp/library/atomic/shared_mutex) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>provides shared mutual exclusion facility (class)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`shared_timed_mutex`](/docs/cpp/library/atomic/shared_timed_mutex) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>provides shared mutual exclusion facility and implements locking with a timeout (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### See also

{/* source:cpp-named-req-sharedlockable:0006 */}
- [`Thread support library`](/docs/cpp/library/atomic)
- [`SharedTimedLockable`](/docs/cpp/library/named_req/SharedTimedLockable)
- [`SharedMutex`](/docs/cpp/library/named_req/SharedMutex)
