---
title: "C++ named requirements: RandomNumberEngineAdaptor (since C++11)"
description: "A RandomNumberEngineAdaptor is a RandomNumberEngine that transforms the output of another RandomNumberEngine, the latter of which is termed a base engine."
source_url: "https://en.cppreference.com/w/cpp/named_req/RandomNumberEngineAdaptor"
language: "C++"
---

{/* source:cpp-named-req-randomnumberengineadaptor:0000 */}
A RandomNumberEngineAdaptor is a [RandomNumberEngine](/docs/cpp/library/named_req/RandomNumberEngine) that transforms the output of another [RandomNumberEngine](/docs/cpp/library/named_req/RandomNumberEngine), the latter of which is termed a base engine.

## Requirements

{/* source:cpp-named-req-randomnumberengineadaptor:0001 */}
Given

{/* source:cpp-named-req-randomnumberengineadaptor:0002 */}
- `B`, a [RandomNumberEngine](/docs/cpp/library/named_req/RandomNumberEngine)
- `b`, an object of type `B`
- `A`, a RandomNumberEngineAdaptor, adapting `B` as a base engine
- `a`, an object of type `A`

{/* source:cpp-named-req-randomnumberengineadaptor:0003 */}
| function | semantics |
| --- | --- |
| `A::A();` | The base engine is initialized as if by its default constructor. |
| `bool operator==(const A& lhs, const A& rhs);` | Returns whether `lhs`'s base engine is equal to `rhs`'s base engine or not. |
| `A::A(result_type s);` | The base engine is initialized with `s`. |
| `template<class Sseq> A::A(Sseq& q);` | The base engine is initialized with `q`. |
| `void seed();` | Invokes `b.seed()`. |
| `void seed(result_type s);` | Invokes `b.seed(s)`. |
| `template<class Sseq> void seed(Sseq& q);` | Invokes `b.seed(q)`. |

{/* source:cpp-named-req-randomnumberengineadaptor:0004 */}
Furthermore

{/* source:cpp-named-req-randomnumberengineadaptor:0005 */}
- The complexity of each function of `A` does not exceed the complexity of the function of `B`
- The state of `A` includes the state of `B`
- The size of `A`'s state is no less than the size of `b`
- Copying `A`'s state includes copying `B`'s state
- The textual representation of `A` includes the textual representation of `B`

## Standard library

{/* source:cpp-named-req-randomnumberengineadaptor:0006 */}
The following standard library facilities satisfy RandomNumberEngineAdaptor:

{/* source:cpp-named-req-randomnumberengineadaptor:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`discard_block_engine`](/docs/cpp/library/numeric/random/discard_block_engine) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>discards some output of a random number engine (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`independent_bits_engine`](/docs/cpp/library/numeric/random/independent_bits_engine) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>packs the output of a random number engine into blocks of a specified number of bits (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`shuffle_order_engine`](/docs/cpp/library/numeric/random/shuffle_order_engine) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>delivers the output of a random number engine in a different order (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
