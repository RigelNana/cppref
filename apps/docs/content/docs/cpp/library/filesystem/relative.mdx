---
title: "std::filesystem::relative, std::filesystem::proximate"
description: "1) Returns relative(p, current_path(), ec)."
source_url: "https://en.cppreference.com/w/cpp/filesystem/relative"
language: "C++"
---

{/* source:cpp-filesystem-relative:0000 */}
<DeclarationDoc>
Defined in header [`<filesystem>`](/docs/cpp/library/headers/filesystem)

  <Declaration id="1" language="cpp" since="C++17" code={`path relative( const std::filesystem::path& p,
               std::error_code& ec );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`path relative( const std::filesystem::path& p,
               const std::filesystem::path& base = std::filesystem::current_path() );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`path relative( const std::filesystem::path& p,
               const std::filesystem::path& base,

               std::error_code& ec );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`path proximate( const std::filesystem::path& p,
                std::error_code& ec );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`path proximate( const std::filesystem::path& p,
                const std::filesystem::path& base = std::filesystem::current_path() );`} />
  <Declaration id="6" language="cpp" since="C++17" code={`path proximate( const std::filesystem::path& p,
                const std::filesystem::path& base,

                std::error_code& ec );`} />

  <DeclarationDescription>
    {/* source:cpp-filesystem-relative:0001 */}
    1) Returns `relative(p, current_path(), ec)`.

    {/* source:cpp-filesystem-relative:0002 */}
    2,3) Returns `p` made relative to `base`. Resolves symlinks and normalizes both `p` and `base` before other processing. Effectively returns `std::filesystem::weakly_canonical(p).lexically_relative(std::filesystem::weakly_canonical(base))` or `std::filesystem::weakly_canonical(p, ec).lexically_relative(std::filesystem::weakly_canonical(base, ec))`, except the error code form returns `path()` at the first error occurrence, if any.

    {/* source:cpp-filesystem-relative:0003 */}
    4) Returns `proximate(p, current_path(), ec)`.

    {/* source:cpp-filesystem-relative:0004 */}
    5,6) Effectively returns `std::filesystem::weakly_canonical(p).lexically_proximate(std::filesystem::weakly_canonical(base))` or `std::filesystem::weakly_canonical(p, ec).lexically_proximate(std::filesystem::weakly_canonical(base, ec))`, except the error code form returns `path()` at the first error occurrence, if any.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-filesystem-relative:0005 */}
<ParameterList>
  <Parameter name="p">an existing path</Parameter>
  <Parameter name="base">base path, against which `p` will be made relative/proximate</Parameter>
  <Parameter name="ec">error code to store error status to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-filesystem-relative:0006 */}
1) `p` made relative against `current_path()`.

{/* source:cpp-filesystem-relative:0007 */}
2,3) `p` made relative against `base`.

{/* source:cpp-filesystem-relative:0008 */}
4) `p` made proximate against `current_path()`.

{/* source:cpp-filesystem-relative:0009 */}
5,6) `p` made proximate against `base`.

### Exceptions

{/* source:cpp-filesystem-relative:0010 */}
Any overload not marked `noexcept` may throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) if memory allocation fails.

{/* source:cpp-filesystem-relative:0011 */}
2,5) Throws [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error) on underlying OS API errors, constructed with `p` as the first path argument, `base` as the second path argument, and the OS error code as the error code argument.

{/* source:cpp-filesystem-relative:0012 */}
1,3,4,6) Sets a `std::error_code&` parameter to the OS API error code if an OS API call fails, and executes `ec.`[`clear`](/docs/cpp/library/error/error_code/clear)`()` if no errors occur.

### Example

{/* source:cpp-filesystem-relative:0013 */}
```cpp
#include <filesystem>
#include <iostream>
 
void show(std::filesystem::path x, std::filesystem::path y)
{
    std::cout << "x:\t\t " << x << "\ny:\t\t " << y << '\n'
              << "relative(x, y):  "
              << std::filesystem::relative(x, y) << '\n'
              << "proximate(x, y): "
              << std::filesystem::proximate(x, y) << "\n\n";
}
 
int main()
{
    show("/a/b/c", "/a/b");
    show("/a/c", "/a/b");
    show("c", "/a/b");
    show("/a/b", "c");
}
```

Possible output:

```text
x:               "/a/b/c"
y:               "/a/b"
relative(x, y):  "c"
proximate(x, y): "c"
 
x:               "/a/c"
y:               "/a/b"
relative(x, y):  "../c"
proximate(x, y): "../c"
 
x:               "c"
y:               "/a/b"
relative(x, y):  ""
proximate(x, y): "c"
 
x:               "/a/b"
y:               "c"
relative(x, y):  ""
proximate(x, y): "/a/b"
```

### See also

{/* source:cpp-filesystem-relative:0014 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`path`](/docs/cpp/library/filesystem/path)<InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>represents a path <br /> (class)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`absolute`](/docs/cpp/library/filesystem/absolute)<InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>composes an absolute path <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`canonical, weakly_canonical`](/docs/cpp/library/filesystem/canonical)<InlineRevision since="C++17" />, <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>composes a canonical path <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`lexically_normal, lexically_relative, lexically_proximate`](/docs/cpp/library/filesystem/path/lexically_normal)</DescriptionTerm>
    <DescriptionBody>converts path to normal form<br />converts path to relative form<br />converts path to proximate form <br /> (public member function of `std::filesystem::path`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
