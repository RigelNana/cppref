---
title: "std::filesystem::remove, std::filesystem::remove_all"
description: "1,2) The file or empty directory identified by the path p is deleted as if by the POSIX remove. Symlinks are not followed (symlink is removed, not its target)."
source_url: "https://en.cppreference.com/w/cpp/filesystem/remove"
language: "C++"
---

{/* source:cpp-filesystem-remove:0000 */}
<DeclarationDoc>
Defined in header [`<filesystem>`](/docs/cpp/library/headers/filesystem)

  <Declaration id="1" language="cpp" since="C++17" code={`bool remove( const std::filesystem::path& p );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`bool remove( const std::filesystem::path& p, std::error_code& ec ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++17" code={`std::uintmax_t remove_all( const std::filesystem::path& p );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`std::uintmax_t remove_all( const std::filesystem::path& p, std::error_code& ec );`} />

  <DeclarationDescription>
    {/* source:cpp-filesystem-remove:0001 */}
    1,2) The file or empty directory identified by the path `p` is deleted as if by the POSIX [`remove`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/remove.html). Symlinks are not followed (symlink is removed, not its target).

    {/* source:cpp-filesystem-remove:0002 */}
    3,4) Deletes the contents of `p` (if it is a directory) and the contents of all its subdirectories, recursively, then deletes `p` itself as if by repeatedly applying the POSIX [`remove`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/remove.html). Symlinks are not followed (symlink is removed, not its target).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-filesystem-remove:0003 */}
<ParameterList>
  <Parameter name="p">path to delete</Parameter>
  <Parameter name="ec">out-parameter for error reporting in the non-throwing overload.</Parameter>
</ParameterList>

### Return value

{/* source:cpp-filesystem-remove:0004 */}
1,2) `true` if the file was deleted, `false` if it did not exist. The overload that takes `error_code&` argument returns `false` on errors.

{/* source:cpp-filesystem-remove:0005 */}
3,4) Returns the number of files and directories that were deleted (which may be zero if `p` did not exist to begin with). The overload that takes `error_code&` argument returns `static_cast<std::uintmax_t>(-1)` on error.

### Exceptions

{/* source:cpp-filesystem-remove:0006 */}
Any overload not marked `noexcept` may throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) if memory allocation fails.

{/* source:cpp-filesystem-remove:0007 */}
1,3) Throws [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error) on underlying OS API errors, constructed with `p` as the first path argument and the OS error code as the error code argument.

{/* source:cpp-filesystem-remove:0008 */}
2,4) Sets a `std::error_code&` parameter to the OS API error code if an OS API call fails, and executes `ec.`[`clear`](/docs/cpp/library/error/error_code/clear)`()` if no errors occur.

### Notes

{/* source:cpp-filesystem-remove:0009 */}
On POSIX systems, this function typically calls [`unlink`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/unlink.html) and [`rmdir`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/rmdir.html) as needed, on Windows [`DeleteFileW`](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-deletefilew) and [`RemoveDirectoryW`](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-removedirectoryw).

{/* source:cpp-filesystem-remove:0010 */}
If `p` did not exist, this function returns `false` and does not report an error.

### Example

{/* source:cpp-filesystem-remove:0011 */}
```cpp
#include <cstdint>
#include <filesystem>
#include <fstream>
#include <iostream>
 
int main()
{
    namespace fs = std::filesystem;
    std::cout << std::boolalpha;
 
    fs::path tmp{std::filesystem::temp_directory_path()};
 
    const auto O_O{"O_O"};
    std::ofstream{tmp / O_O} << O_O; // creates file containing O_O
    std::cout << "remove(): " << fs::remove(tmp / O_O) << '\n'; // success
    std::cout << "remove(): " << fs::remove(tmp / O_O) << '\n'; // fail
 
    std::filesystem::create_directories(tmp / "abcdef/example");
    const std::uintmax_t n{fs::remove_all(tmp / "abcdef")};
    std::cout << "remove_all(): " << n << " files or directories\n";
}
```

Possible output:

```text
remove(): true
remove(): false
remove_all(): 2 files or directories
```

### Defect reports

{/* source:cpp-filesystem-remove:0012 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-filesystem-remove:0013 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3014} standard="C++17">
    <PublishedBehavior>`error_code` overload of `remove_all` marked `noexcept` but can allocate memory</PublishedBehavior>
    <CorrectedBehavior>`noexcept` removed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-filesystem-remove:0014 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`remove`](/docs/cpp/library/io/c/remove)<InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>erases a file <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
