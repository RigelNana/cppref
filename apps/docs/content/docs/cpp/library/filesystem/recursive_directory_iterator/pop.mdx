---
title: "std::filesystem::recursive_directory_iterator::pop"
description: "Moves the iterator one level up in the directory hierarchy."
source_url: "https://en.cppreference.com/w/cpp/filesystem/recursive_directory_iterator/pop"
language: "C++"
---

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`void pop();`} />
  <Declaration id="2" language="cpp" since="C++17" code={`void pop( std::error_code& ec );`} />

  <DeclarationDescription>
    {/* source:cpp-filesystem-recursive-directory-iterator-pop:0001 */}
    Moves the iterator one level up in the directory hierarchy. Invalidates all copies of the previous value of `*this`.

    {/* source:cpp-filesystem-recursive-directory-iterator-pop:0002 */}
    If the parent directory is outside directory hierarchy that is iterated on (i.e. `depth() == 0`), sets `*this` to an end directory iterator.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0003 */}
<ParameterList>
  <Parameter name="ec">error code to store the error status to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0004 */}
(none)

### Exceptions

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0005 */}
Any overload not marked `noexcept` may throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) if memory allocation fails.

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0006 */}
1) Throws [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error) on underlying OS API errors, constructed with the OS error code as the error code argument.

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0007 */}
2) Sets a `std::error_code&` parameter to the OS API error code if an OS API call fails, and executes `ec`. [`clear`](/docs/cpp/library/error/error_code/clear)() if no errors occur.

### Example

{/* source:cpp-filesystem-recursive-directory-iterator-pop:0008 */}
This section is incomplete Reason: no example
