---
title: "std::filesystem::current_path"
description: "Returns or changes the current path."
source_url: "https://en.cppreference.com/w/cpp/filesystem/current_path"
language: "C++"
---

{/* source:cpp-filesystem-current-path:0000 */}
<DeclarationDoc>
Defined in header [`<filesystem>`](/docs/cpp/library/headers/filesystem)

  <Declaration id="1" language="cpp" since="C++17" code={`path current_path();`} />
  <Declaration id="2" language="cpp" since="C++17" code={`path current_path( std::error_code& ec );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`void current_path( const std::filesystem::path& p );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`void current_path( const std::filesystem::path& p,
                   std::error_code& ec ) noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-filesystem-current-path:0001 */}
    Returns or changes the current path.

    {/* source:cpp-filesystem-current-path:0002 */}
    1,2) Returns the absolute path of the current working directory, obtained as if (in native format) by POSIX [`getcwd`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/getcwd.html). (2) returns `path()` if error occurs.

    {/* source:cpp-filesystem-current-path:0003 */}
    3,4) Changes the current working directory to `p`, as if by POSIX [`chdir`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/chdir.html).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-filesystem-current-path:0004 */}
<ParameterList>
  <Parameter name="p">path to change the current working directory to</Parameter>
  <Parameter name="ec">out-parameter for error reporting in the non-throwing overloads</Parameter>
</ParameterList>

### Return value

{/* source:cpp-filesystem-current-path:0005 */}
1,2) Returns the current working directory.

{/* source:cpp-filesystem-current-path:0006 */}
3,4) (none)

### Exceptions

{/* source:cpp-filesystem-current-path:0007 */}
Any overload not marked `noexcept` may throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) if memory allocation fails.

{/* source:cpp-filesystem-current-path:0008 */}
1) Throws [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error) on underlying OS API errors, constructed with the OS error code as the error code argument.

{/* source:cpp-filesystem-current-path:0009 */}
2) Sets a `std::error_code&` parameter to the OS API error code if an OS API call fails, and executes `ec.`[`clear`](/docs/cpp/library/error/error_code/clear)`()` if no errors occur.

{/* source:cpp-filesystem-current-path:0010 */}
3) Throws [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error) on underlying OS API errors, constructed with `p` as the first path argument and the OS error code as the error code argument.

{/* source:cpp-filesystem-current-path:0011 */}
4) Sets a `std::error_code&` parameter to the OS API error code if an OS API call fails, and executes `ec.`[`clear`](/docs/cpp/library/error/error_code/clear)`()` if no errors occur.

### Notes

{/* source:cpp-filesystem-current-path:0012 */}
The current working directory is the directory, associated with the process, that is used as the starting location in pathname resolution for relative paths.

{/* source:cpp-filesystem-current-path:0013 */}
The current path as returned by many operating systems is a dangerous global variable. It may be changed unexpectedly by third-party or system library functions, or by another thread.

### Example

{/* source:cpp-filesystem-current-path:0014 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    std::cout << "Current path is " << fs::current_path() << '\n'; // (1)
    fs::current_path(fs::temp_directory_path()); // (3)
    std::cout << "Current path is " << fs::current_path() << '\n';
}
```

Possible output:

```text
Current path is "D:/local/ConsoleApplication1"
Current path is "E:/Temp"
```

### See also

{/* source:cpp-filesystem-current-path:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`temp_directory_path`](/docs/cpp/library/filesystem/temp_directory_path)<InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>returns a directory suitable for temporary files <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
