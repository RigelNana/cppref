---
title: "std::filesystem::path::root_directory"
description: "Returns the root directory of the generic-format path. If the path (in generic format) does not include root directory, returns path( )."
source_url: "https://en.cppreference.com/w/cpp/filesystem/path/root_directory"
language: "C++"
---

{/* source:cpp-filesystem-path-root-directory:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`path root_directory() const;`} />
</DeclarationDoc>

{/* source:cpp-filesystem-path-root-directory:0001 */}
Returns the root directory of the generic-format path. If the path (in generic format) does not include root directory, returns `path()`.

### Parameters

{/* source:cpp-filesystem-path-root-directory:0002 */}
(none)

### Return value

{/* source:cpp-filesystem-path-root-directory:0003 */}
The root directory of the path.

### Exceptions

{/* source:cpp-filesystem-path-root-directory:0004 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-filesystem-path-root-directory:0005 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    fs::path p = fs::current_path();
 
    std::cout << "The current path " << p << " decomposes into:\n"
              << "root name " << p.root_name() << '\n'
              << "root directory " << p.root_directory() << '\n'
              << "relative path " << p.relative_path() << '\n';
}
```

Possible output:

```text
The current path "C:\Users\abcdef\Local Settings\temp" decomposes into:
root name "C:"
root directory "\"
relative path "Users\abcdef\Local Settings\temp"
```

### See also

{/* source:cpp-filesystem-path-root-directory:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_name`](/docs/cpp/library/filesystem/path/root_name)</DescriptionTerm>
    <DescriptionBody>returns the root-name of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_path`](/docs/cpp/library/filesystem/path/root_path)</DescriptionTerm>
    <DescriptionBody>returns the root path of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
