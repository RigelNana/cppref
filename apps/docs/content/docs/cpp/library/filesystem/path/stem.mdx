---
title: "std::filesystem::path::stem"
description: "Returns the filename identified by the generic-format path stripped of its extension."
source_url: "https://en.cppreference.com/w/cpp/filesystem/path/stem"
language: "C++"
---

{/* source:cpp-filesystem-path-stem:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`path stem() const;`} />
</DeclarationDoc>

{/* source:cpp-filesystem-path-stem:0001 */}
Returns the filename identified by the generic-format path stripped of its extension.

{/* source:cpp-filesystem-path-stem:0002 */}
Returns the substring from the beginning of [`filename()`](/docs/cpp/library/filesystem/path/filename) up to and not including the last period (`.`) character, with the following exceptions:

{/* source:cpp-filesystem-path-stem:0003 */}
-  If the first character in the filename is a period, that period is ignored (a filename like ".profile" is not treated as an extension).

-  If the filename is one of the special filesystem components *dot* or *dot-dot*, or if it has no periods, the function returns the entire [`filename()`](/docs/cpp/library/filesystem/path/filename).

### Parameters

{/* source:cpp-filesystem-path-stem:0004 */}
(none)

### Return value

{/* source:cpp-filesystem-path-stem:0005 */}
The stem of the filename identified by the path (i.e. the filename without the final extension).

### Exceptions

{/* source:cpp-filesystem-path-stem:0006 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-filesystem-path-stem:0007 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    for (const fs::path p : {"/foo/bar.txt", "/foo/.bar", "foo.bar.baz.tar"})
        std::cout << "path: " << p << ", stem: " << p.stem() << '\n';
 
    std::cout << '\n';
 
    for (fs::path p = "foo.bar.baz.tar"; !p.extension().empty(); p = p.stem())
        std::cout << "path: " << p << ", extension: " << p.extension() << '\n';
}
```

Output:

```text
path: "/foo/bar.txt", stem: "bar"
path: "/foo/.bar", stem: ".bar"
path: "foo.bar.baz.tar", stem: "foo.bar.baz"
 
path: "foo.bar.baz.tar", extension: ".tar"
path: "foo.bar.baz", extension: ".baz"
path: "foo.bar", extension: ".bar"
```

### See also

{/* source:cpp-filesystem-path-stem:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`filename`](/docs/cpp/library/filesystem/path/filename)</DescriptionTerm>
    <DescriptionBody>returns the filename path component <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`extension`](/docs/cpp/library/filesystem/path/extension)</DescriptionTerm>
    <DescriptionBody>returns the file extension path component <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
