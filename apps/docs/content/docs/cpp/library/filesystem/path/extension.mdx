---
title: "std::filesystem::path::extension"
description: "Returns the extension of the filename component of the generic-format view of *this."
source_url: "https://en.cppreference.com/w/cpp/filesystem/path/extension"
language: "C++"
---

{/* source:cpp-filesystem-path-extension:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`path extension() const;`} />
</DeclarationDoc>

{/* source:cpp-filesystem-path-extension:0001 */}
Returns the extension of the filename component of the generic-format view of `*this`.

{/* source:cpp-filesystem-path-extension:0002 */}
If the [`filename()`](/docs/cpp/library/filesystem/path/filename) component of the generic-format path contains a period (`.`), and is not one of the special filesystem elements *dot* or *dot-dot*, then the *extension* is the substring beginning at the rightmost period (including the period) and until the end of the pathname.

{/* source:cpp-filesystem-path-extension:0003 */}
If the first character in the filename is a period, that period is ignored (a filename like ".profile" is not treated as an extension).

{/* source:cpp-filesystem-path-extension:0004 */}
If the pathname is either `.` or `..`, or if [`filename()`](/docs/cpp/library/filesystem/path/filename) does not contain the `'.'` character, then empty path is returned.

{/* source:cpp-filesystem-path-extension:0005 */}
Additional behavior may be defined by the implementations for file systems which append additional elements (such as alternate data streams or partitioned dataset names) to extensions.

### Parameters

{/* source:cpp-filesystem-path-extension:0006 */}
(none)

### Return value

{/* source:cpp-filesystem-path-extension:0007 */}
The extension of the current pathname or an empty path if there's no extension.

### Exceptions

{/* source:cpp-filesystem-path-extension:0008 */}
May throw implementation-defined exceptions.

### Notes

{/* source:cpp-filesystem-path-extension:0009 */}
The extension as returned by this function includes a period to make it possible to distinguish the file that ends with a period (function returns `"."`) from a file with no extension (function returns `""`).

{/* source:cpp-filesystem-path-extension:0010 */}
On a non-POSIX system, it is possible that `p.stem() + p.extension() != p.filename()` even though generic-format versions are the same.

### Example

{/* source:cpp-filesystem-path-extension:0011 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    std::cout << fs::path("/foo/bar.txt").extension() << '\n'
              << fs::path("/foo/bar.").extension() << '\n'
              << fs::path("/foo/bar").extension() << '\n'
              << fs::path("/foo/bar.txt/bar.cc").extension() << '\n'
              << fs::path("/foo/bar.txt/bar.").extension() << '\n'
              << fs::path("/foo/bar.txt/bar").extension() << '\n'
              << fs::path("/foo/.").extension() << '\n'
              << fs::path("/foo/..").extension() << '\n'
              << fs::path("/foo/.hidden").extension() << '\n'
              << fs::path("/foo/..bar").extension() << '\n';
}
```

Output:

```text
".txt"
"."
""
".cc"
"."
""
""
""
""
".bar"
```

### See also

{/* source:cpp-filesystem-path-extension:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`filename`](/docs/cpp/library/filesystem/path/filename)</DescriptionTerm>
    <DescriptionBody>returns the filename path component <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`stem`](/docs/cpp/library/filesystem/path/stem)</DescriptionTerm>
    <DescriptionBody>returns the stem path component (filename without the final extension) <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`replace_extension`](/docs/cpp/library/filesystem/path/replace_extension)</DescriptionTerm>
    <DescriptionBody>replaces the extension <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`has_extension`](/docs/cpp/library/filesystem/path/has_path)</DescriptionTerm>
    <DescriptionBody>checks if the corresponding path element is not empty <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
