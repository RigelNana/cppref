---
title: "std::filesystem::path::root_path"
description: "Returns the root path of the path. If the path does not include root path, returns path( )."
source_url: "https://en.cppreference.com/w/cpp/filesystem/path/root_path"
language: "C++"
---

{/* source:cpp-filesystem-path-root-path:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`path root_path() const;`} />
</DeclarationDoc>

{/* source:cpp-filesystem-path-root-path:0001 */}
Returns the root path of the path. If the path does not include root path, returns `path()`.

{/* source:cpp-filesystem-path-root-path:0002 */}
Effectively returns `root_name() / root_directory()`.

### Parameters

{/* source:cpp-filesystem-path-root-path:0003 */}
(none)

### Return value

{/* source:cpp-filesystem-path-root-path:0004 */}
The root path of the path.

### Exceptions

{/* source:cpp-filesystem-path-root-path:0005 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-filesystem-path-root-path:0006 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    std::cout << "Current root path is: " << fs::current_path().root_path() << '\n';
}
```

Possible output:

```text
Current root path is: "C:\"
```

### See also

{/* source:cpp-filesystem-path-root-path:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_name`](/docs/cpp/library/filesystem/path/root_name)</DescriptionTerm>
    <DescriptionBody>returns the root-name of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_directory`](/docs/cpp/library/filesystem/path/root_directory)</DescriptionTerm>
    <DescriptionBody>returns the root directory of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
