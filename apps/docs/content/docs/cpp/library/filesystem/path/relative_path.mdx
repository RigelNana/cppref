---
title: "std::filesystem::path::relative_path"
description: "Returns path relative to root-path, that is, a pathname composed of every generic-format component of *this after root-path. If *this is an empty path, returns an empty path."
source_url: "https://en.cppreference.com/w/cpp/filesystem/path/relative_path"
language: "C++"
---

{/* source:cpp-filesystem-path-relative-path:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`path relative_path() const;`} />
</DeclarationDoc>

{/* source:cpp-filesystem-path-relative-path:0001 */}
Returns path relative to *root-path*, that is, a pathname composed of every generic-format component of `*this` after *root-path*. If `*this` is an empty path, returns an empty path.

### Parameters

{/* source:cpp-filesystem-path-relative-path:0002 */}
(none)

### Return value

{/* source:cpp-filesystem-path-relative-path:0003 */}
Path relative to the [root path](/docs/cpp/library/filesystem/path/root_path).

### Exceptions

{/* source:cpp-filesystem-path-relative-path:0004 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-filesystem-path-relative-path:0005 */}
```cpp
#include <filesystem>
#include <iostream>
namespace fs = std::filesystem;
 
int main()
{
    fs::path p = fs::current_path();
 
    std::cout << "The current path " << p << " decomposes into:\n"
              << "root-path " << p.root_path() << '\n'
              << "relative path " << p.relative_path() << '\n';
}
```

Possible output:

```text
The current path "C:\Users\abcdef\Local Settings\temp" decomposes into:
root-path "C:\"
relative path "Users\abcdef\Local Settings\temp"
```

### See also

{/* source:cpp-filesystem-path-relative-path:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_name`](/docs/cpp/library/filesystem/path/root_name)</DescriptionTerm>
    <DescriptionBody>returns the root-name of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_directory`](/docs/cpp/library/filesystem/path/root_directory)</DescriptionTerm>
    <DescriptionBody>returns the root directory of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`root_path`](/docs/cpp/library/filesystem/path/root_path)</DescriptionTerm>
    <DescriptionBody>returns the root path of the path, if present <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
