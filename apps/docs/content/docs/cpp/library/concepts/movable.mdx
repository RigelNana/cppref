---
title: "std::movable"
description: "The concept movable<T> specifies that T is an object type that can be moved (that is, it can be move constructed, move assigned, and lvalues of type T can be swapped)."
source_url: "https://en.cppreference.com/w/cpp/concepts/movable"
language: "C++"
---

{/* source:cpp-concepts-movable:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T >
concept movable =
    std::is_object_v<T> &&
    std::move_constructible<T> &&
    std::assignable_from<T&, T> &&

    std::swappable<T>;`} />
</DeclarationDoc>

{/* source:cpp-concepts-movable:0001 */}
The concept `movable<T>` specifies that `T` is an object type that can be moved (that is, it can be move constructed, move assigned, and lvalues of type `T` can be swapped).

### References

{/* source:cpp-concepts-movable:0002 */}
C++23 standard (ISO/IEC 14882:2024): 18.6 Object concepts [concepts.object]

{/* source:cpp-concepts-movable:0003 */}
C++20 standard (ISO/IEC 14882:2020): 18.6 Object concepts [concepts.object]

### See also

{/* source:cpp-concepts-movable:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`copyable`](/docs/cpp/library/concepts/copyable) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be copied, moved, and swapped (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
