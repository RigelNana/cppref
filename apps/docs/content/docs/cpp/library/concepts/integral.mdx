---
title: "std::integral"
description: "The concept integral<T> is satisfied if and only if T is an integral type."
source_url: "https://en.cppreference.com/w/cpp/concepts/integral"
language: "C++"
---

{/* source:cpp-concepts-integral:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T >
concept integral = std::is_integral_v<T>;`} />
</DeclarationDoc>

{/* source:cpp-concepts-integral:0001 */}
The concept `integral<T>` is satisfied if and only if `T` is an integral type.

### Example

{/* source:cpp-concepts-integral:0002 */}
```cpp
#include <concepts>
#include <iostream>
 
void print(std::integral auto i)
{
    std::cout << "Integral: " << i << '\n';
}
 
void print(auto x)
{
    std::cout << "Non-integral: " << x << '\n';
}
 
int main()
{
    std::cout << std::boolalpha;
 
    static_assert(std::integral<bool>);
    print(true);
 
    static_assert(std::integral<char>);
    print('o');
 
    static_assert(std::integral<int>);
    print(007);
 
    static_assert( ! std::integral<double> );
    print(2e2);
 
    static_assert( ! std::integral<decltype("")> );
    print("∫∫∫");
}
```

Output:

```text
Integral: true
Integral: o
Integral: 7
Non-integral: 200
Non-integral: ∫∫∫
```

### References

{/* source:cpp-concepts-integral:0003 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.7 Arithmetic concepts [concepts.arithmetic]

{/* source:cpp-concepts-integral:0004 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.7 Arithmetic concepts [concepts.arithmetic]

### See also

{/* source:cpp-concepts-integral:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_integral`](/docs/cpp/library/meta/is_integral) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an integral type (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
