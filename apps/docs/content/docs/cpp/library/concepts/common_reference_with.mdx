---
title: "std::common_reference_with"
description: "The concept common_reference_with<T, U> specifies that two types T and U share a common reference type (as computed by std::common_reference_t) to which both can be converted."
source_url: "https://en.cppreference.com/w/cpp/concepts/common_reference_with"
language: "C++"
---

{/* source:cpp-concepts-common-reference-with:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T, class U >
concept common_reference_with =
    std::same_as<std::common_reference_t<T, U>, std::common_reference_t<U, T>> &&
    std::convertible_to<T, std::common_reference_t<T, U>> &&

    std::convertible_to<U, std::common_reference_t<T, U>>;`} />
</DeclarationDoc>

{/* source:cpp-concepts-common-reference-with:0001 */}
The concept `common_reference_with<T, U>` specifies that two types `T` and `U` share a common reference type (as computed by `std::common_reference_t`) to which both can be converted.

### Semantic requirements

{/* source:cpp-concepts-common-reference-with:0002 */}
`T` and `U` model `std::common_reference_with<T, U>` only if, given [equality-preserving](/docs/cpp/library/concepts#Equality_preservation) expressions `t1`, `t2`, `u1` and `u2` such that `decltype((t1))` and `decltype((t2))` are both `T` and `decltype((u1))` and `decltype((u2))` are both `U`,

{/* source:cpp-concepts-common-reference-with:0003 */}
- `std::common_reference_t<T, U>(t1)` equals `std::common_reference_t<T, U>(t2)` if and only if `t1` equals `t2`; and
- `std::common_reference_t<T, U>(u1)` equals `std::common_reference_t<T, U>(u2)` if and only if `u1` equals `u2`.

{/* source:cpp-concepts-common-reference-with:0004 */}
In other words, the conversion to the common reference type must [preserve equality](/docs/cpp/library/concepts#Equality_preservation).

### References

{/* source:cpp-concepts-common-reference-with:0005 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.5 Concept `common_reference_with` [concept.commonref]

{/* source:cpp-concepts-common-reference-with:0006 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.5 Concept `common_reference_with` [concept.commonref]

### See also

{/* source:cpp-concepts-common-reference-with:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`common_reference` `basic_common_reference`](/docs/cpp/library/meta/common_reference) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>determines the common reference type of a group of types (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`common_with`](/docs/cpp/library/concepts/common_with) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that two types share a common type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`common_type`](/docs/cpp/library/meta/common_type) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>determines the common type of a group of types (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
