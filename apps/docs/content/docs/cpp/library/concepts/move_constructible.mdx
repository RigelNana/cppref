---
title: "std::move_constructible"
description: "The concept move_constructible is satisfied if T is a reference type, or if it is an object type where an object of that type can be constructed from an rvalue of that type in both direct- and copy-initialization contexts, with the usual semantics."
source_url: "https://en.cppreference.com/w/cpp/concepts/move_constructible"
language: "C++"
---

{/* source:cpp-concepts-move-constructible:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T >
concept move_constructible = std::constructible_from<T, T> && std::convertible_to<T, T>;`} />
</DeclarationDoc>

{/* source:cpp-concepts-move-constructible:0001 */}
The concept `move_constructible` is satisfied if `T` is a reference type, or if it is an object type where an object of that type can be constructed from an rvalue of that type in both direct- and copy-initialization contexts, with the usual semantics.

### Semantic requirements

{/* source:cpp-concepts-move-constructible:0002 */}
If `T` is an object type, then `move_constructible<T>` is modeled only if given

{/* source:cpp-concepts-move-constructible:0003 */}
- `rv`, an rvalue of type `T`, and
- `u2`, a distinct object of type `T` equal to `rv`,

{/* source:cpp-concepts-move-constructible:0004 */}
the following are true:

{/* source:cpp-concepts-move-constructible:0005 */}
- After the definition `T u = rv;`, `u` is equal to `u2`;
- `T(rv)` is equal to `u2`; and
- If `T` is not const-qualified, then `rv`'s resulting state (after the definition/expression is evaluated in either bullets above) is valid but unspecified; otherwise, it is unchanged.

### References

{/* source:cpp-concepts-move-constructible:0006 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.13 Concept `move_constructible` [concept.moveconstructible]

{/* source:cpp-concepts-move-constructible:0007 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.13 Concept `move_constructible` [concept.moveconstructible]

### See also

{/* source:cpp-concepts-move-constructible:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_move_constructible` `is_trivially_move_constructible` `is_nothrow_move_constructible`](/docs/cpp/library/meta/is_move_constructible) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type can be constructed from an rvalue reference (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
