---
title: "std::totally_ordered, std::totally_ordered_with"
description: "The concept std::totally_ordered specifies that the comparison operators ==,!=,<,>,<=,>= on a type yield results consistent with a strict total order on the type."
source_url: "https://en.cppreference.com/w/cpp/concepts/totally_ordered"
language: "C++"
---

{/* source:cpp-concepts-totally-ordered:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration id="1" language="cpp" since="C++20" code={`template< class T >
concept totally_ordered =

    std::equality_comparable<T> && __PartiallyOrderedWith<T, T>;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< class T, class U >
concept totally_ordered_with =
    std::totally_ordered<T> &&
    std::totally_ordered<U> &&
    std::equality_comparable_with<T, U> &&
    std::totally_ordered<
        std::common_reference_t<
            const std::remove_reference_t<T>&,
            const std::remove_reference_t<U>&>> &&

    __PartiallyOrderedWith<T, U>;`} />

Helper concepts

  <Declaration id="3" language="cpp" code={`template< class T, class U >
concept __PartiallyOrderedWith =
    requires(const std::remove_reference_t<T>& t,
             const std::remove_reference_t<U>& u) {
        { t <  u } -> boolean-testable;
        { t >  u } -> boolean-testable;
        { t <= u } -> boolean-testable;
        { t >= u } -> boolean-testable;
        { u <  t } -> boolean-testable;
        { u >  t } -> boolean-testable;
        { u <= t } -> boolean-testable;
        { u >= t } -> boolean-testable;

    };`} />
  (3) (exposition only*)

  <DeclarationDescription>
    {/* source:cpp-concepts-totally-ordered:0001 */}
    1. The concept `std::totally_ordered` specifies that the comparison operators `==`, `!=`, `<`, `>`, `<=`, `>=` on a type yield results consistent with a [strict total order](https://en.wikipedia.org/wiki/Total_order#Strict_and_non-strict_total_orders) on the type.

    {/* source:cpp-concepts-totally-ordered:0002 */}
    2. The concept `std::totally_ordered_with` specifies that the comparison operators `==`, `!=`, `<`, `>`, `<=`, `>=` on (possibly mixed) `T` and `U` operands yield results consistent with a strict total order. Comparing mixed operands yields results equivalent to comparing the operands converted to their common type.

    {/* source:cpp-concepts-totally-ordered:0003 */}
    3. The exposition-only concept `__PartiallyOrderedWith` specifies that a value of type `T` and a value of type `U` can be compared in a partial order with each other (in either order) using `<`, `>`, `<=`, and `>=`, and the results of the comparisons are consistent.
  </DeclarationDescription>
</DeclarationDoc>

### Semantic requirements

{/* source:cpp-concepts-totally-ordered:0004 */}
These concepts are modeled only if they are satisfied and all concepts they subsume are modeled.

{/* source:cpp-concepts-totally-ordered:0005 */}
1. `std::totally_ordered<T>` is modeled only if, given lvalues `a`, `b` and `c` of type `const std::remove_reference_t<T>`:
    - Exactly one of `bool(a < b)`, `bool(a > b)` and `bool(a == b)` is `true`;
    - If `bool(a < b)` and `bool(b < c)` are both `true`, then `bool(a < c)` is `true`;
    - `bool(a > b) == bool(b < a)`
    - `bool(a >= b) == !bool(a < b)`
    - `bool(a <= b) == !bool(b < a)`

{/* source:cpp-concepts-totally-ordered:0006 */}
2. `std::totally_ordered_with<T, U>` is modeled only if, given
    - `t` and `t2`, lvalues denoting distinct equal objects of types `const std::remove_reference_t<T>` and `std::remove_reference_t<T>` respectively, and
    - `u` and `u2`, lvalues denoting distinct equal objects of types `const std::remove_reference_t<U>` and `std::remove_reference_t<U>` respectively,

    let `C` be `std::common_reference_t<const std::remove_reference_t<T>&, const std::remove_reference_t<U>&>`, and, given an expression `E` and a type `C`, let `CONVERT_TO<C>(E)` be:

    <Revision until="C++23">
    - `static_cast<C>(std::as_const(E))`.

    </Revision>

    <Revision since="C++23">
    - `static_cast<const C&>(std::as_const(E))` if that is a valid expression,
    - `static_cast<const C&>(std::move(E))` otherwise.

    </Revision>

    the following are true:
    - `bool(t < u) == bool(CONVERT_TO<C>(t2) < CONVERT_TO<C>(u2))`
    - `bool(t > u) == bool(CONVERT_TO<C>(t2) > CONVERT_TO<C>(u2))`
    - `bool(t <= u) == bool(CONVERT_TO<C>(t2) <= CONVERT_TO<C>(u2))`
    - `bool(t >= u) == bool(CONVERT_TO<C>(t2) >= CONVERT_TO<C>(u2))`
    - `bool(u < t) == bool(CONVERT_TO<C>(u2) < CONVERT_TO<C>(t2))`
    - `bool(u > t) == bool(CONVERT_TO<C>(u2) > CONVERT_TO<C>(t2))`
    - `bool(u <= t) == bool(CONVERT_TO<C>(u2) <= CONVERT_TO<C>(t2))`
    - `bool(u >= t) == bool(CONVERT_TO<C>(u2) >= CONVERT_TO<C>(t2))`

{/* source:cpp-concepts-totally-ordered:0007 */}
3. `__PartiallyOrderedWith<T, U>` is modeled only if given any lvalue `t` of type `const std::remove_reference_t<T>`, and any lvalue `u` of type `const std::remove_reference_t<U>`, the following are true:
    - `t < u`, `t <= u`, `t > u`, `t >= u`, `u < t`, `u <= t`, `u > t`, and `u >= t` have the same domain;
    - `bool(t < u) == bool(u > t)`;
    - `bool(u < t) == bool(t > u)`;
    - `bool(t <= u) == bool(u >= t)`; and
    - `bool(u <= t) == bool(t >= u)`.

### Equality preservation

{/* source:cpp-concepts-totally-ordered:0008 */}
Expressions declared in [requires expressions](/docs/cpp/language/templates/requires) of the standard library concepts are required to be [equality-preserving](/docs/cpp/library/concepts#Equality_preservation) (except where stated otherwise).

### Implicit expression variations

{/* source:cpp-concepts-totally-ordered:0009 */}
A [requires expression](/docs/cpp/language/templates/requires) that uses an expression that is non-modifying for some constant lvalue operand also requires [implicit expression variations](/docs/cpp/library/concepts#Implicit_expression_variations).

### References

{/* source:cpp-concepts-totally-ordered:0010 */}
C++23 standard (ISO/IEC 14882:2024): 18.5.5 Concept `totally_ordered` [concept.totallyordered]

{/* source:cpp-concepts-totally-ordered:0011 */}
C++20 standard (ISO/IEC 14882:2020): 18.5.4 Concept `totally_ordered` [concept.totallyordered]

### See also

{/* source:cpp-concepts-totally-ordered:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`three_way_comparable` `three_way_comparable_with`](/docs/cpp/library/utility/compare/three_way_comparable) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that operator `<=>` produces consistent result on given types (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
