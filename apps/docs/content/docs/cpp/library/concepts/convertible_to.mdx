---
title: "std::convertible_to"
description: "The concept convertible_to<From, To> specifies that an expression of the same type and value category as those of std::declval<From>() can be implicitly and explicitly converted to the type To, and the two forms of conversion produce equal results."
source_url: "https://en.cppreference.com/w/cpp/concepts/convertible_to"
language: "C++"
---

{/* source:cpp-concepts-convertible-to:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class From, class To >
concept convertible_to =
    std::is_convertible_v<From, To> &&
    requires {
        static_cast<To>(std::declval<From>());

    };`} />
</DeclarationDoc>

{/* source:cpp-concepts-convertible-to:0001 */}
The concept `convertible_to<From, To>` specifies that an expression of the same type and value category as those of `std::declval<From>()` can be implicitly and explicitly converted to the type `To`, and the two forms of conversion produce equal results.

### Semantic requirements

{/* source:cpp-concepts-convertible-to:0002 */}
`convertible_to<From, To>` is modeled only if, given a function `fun` of type `std::add_rvalue_reference_t<From>()` such that the expression `fun()` is [equality-preserving](/docs/cpp/library/concepts#Equality_preservation),

{/* source:cpp-concepts-convertible-to:0003 */}
- Either `To` is neither an object type nor a reference-to-object type, or `static_cast<To>(fun())` is equal to `[]() -> To { return fun(); }()`, and

{/* source:cpp-concepts-convertible-to:0004 */}
- One of the following is true:
  - `std::add_rvalue_reference_t<From>` is not a reference-to-object type, or
  - `std::add_rvalue_reference_t<From>` is an rvalue reference to a non-const-qualified type, and the resulting state of the object referenced by `fun()` is valid but unspecified after either expression above; or
  - the object referred to by `fun()` is not modified by either expression above.

### Equality preservation

{/* source:cpp-concepts-convertible-to:0005 */}
Expressions declared in [requires expressions](/docs/cpp/language/templates/requires) of the standard library concepts are required to be [equality-preserving](/docs/cpp/library/concepts#Equality_preservation) (except where stated otherwise).

### References

{/* source:cpp-concepts-convertible-to:0006 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.4 Concept `convertible_to` [concept.convertible]

{/* source:cpp-concepts-convertible-to:0007 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.4 Concept `convertible_to` [concept.convertible]

### See also

{/* source:cpp-concepts-convertible-to:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_convertible`, `is_nothrow_convertible`](/docs/cpp/library/meta/is_convertible) <InlineRevision since="C++11" /> <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>checks if a type can be converted to the other type (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
