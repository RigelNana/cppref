---
title: "std::same_as"
description: "The concept same_as<T, U> is satisfied if and only if T and U denote the same type."
source_url: "https://en.cppreference.com/w/cpp/concepts/same_as"
language: "C++"
---

{/* source:cpp-concepts-same-as:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T, class U >
concept same_as = /* see below */;`} />
</DeclarationDoc>

{/* source:cpp-concepts-same-as:0001 */}
The concept `same_as<T, U>` is satisfied if and only if `T` and `U` denote the same type.

{/* source:cpp-concepts-same-as:0002 */}
`std::same_as<T, U>` [subsumes](/docs/cpp/language/templates/constraints#Partial_ordering_of_constraints) `std::same_as<U, T>` and vice versa.

### Possible implementation

{/* source:cpp-concepts-same-as:0003 */}
```cpp
namespace detail
{
    template< class T, class U >
    concept SameHelper = std::is_same_v<T, U>;
}
 
template< class T, class U >
concept same_as = detail::SameHelper<T, U> && detail::SameHelper<U, T>;
```

### Example

{/* source:cpp-concepts-same-as:0004 */}
```cpp
#include <concepts>
#include <iostream>
 
template<typename T, typename ... U>
concept IsAnyOf = (std::same_as<T, U> || ...);
 
template<typename T>
concept IsPrintable = std::integral<T> || std::floating_point<T> ||
    IsAnyOf<std::remove_cvref_t<std::remove_pointer_t<std::decay_t<T>>>, char, wchar_t>;
 
void println(IsPrintable auto const ... arguments)
{
    (std::wcout << ... << arguments) << '\n';
}
 
int main()
{
    println("Example: ", 3.14, " : ", 42, " : [", 'a', L'-', L"Z]");
}
```

Output:

```text
Example: 3.14 : 42 : [a-Z]
```

### References

{/* source:cpp-concepts-same-as:0005 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.2 Concept `same_as` [concept.same]

{/* source:cpp-concepts-same-as:0006 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.2 Concept `same_as` [concept.same]

### See also

{/* source:cpp-concepts-same-as:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_same`](/docs/cpp/library/meta/is_same) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if two types are the same (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
