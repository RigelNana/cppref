---
title: "std::unsigned_integral"
description: "The concept unsigned_integral<T> is satisfied if and only if T is an integral type and std::is_signed_v<T> is false."
source_url: "https://en.cppreference.com/w/cpp/concepts/unsigned_integral"
language: "C++"
---

{/* source:cpp-concepts-unsigned-integral:0000 */}
<DeclarationDoc>
Defined in header [`<concepts>`](/docs/cpp/library/headers/concepts)

  <Declaration language="cpp" since="C++20" code={`template< class T >
concept unsigned_integral = std::integral<T> && !std::signed_integral<T>;`} />
</DeclarationDoc>

{/* source:cpp-concepts-unsigned-integral:0001 */}
The concept `unsigned_integral<T>` is satisfied if and only if `T` is an integral type and `std::is_signed_v<T>` is `false`.

### Notes

{/* source:cpp-concepts-unsigned-integral:0002 */}
`unsigned_integral<T>` may be satisfied by a type that is not an [unsigned integer type](/docs/cpp/language/basic_concepts/type-id#Type_classification), for example, `bool`.

### Example

{/* source:cpp-concepts-unsigned-integral:0003 */}
```cpp
#include <concepts>
#include <iostream>
#include <string_view>
 
void test(std::signed_integral auto x, std::string_view text = "")
{
    std::cout << text << " (" + (text == "") << x << ") is a signed integral\n";
}
 
void test(std::unsigned_integral auto x, std::string_view text = "")
{
    std::cout << text << " (" + (text == "") << x << ") is an unsigned integral\n";
}
 
void test(auto x, std::string_view text = "")
{
    std::cout << text << " (" + (text == "") << x << ") is non-integral\n";
}
 
int main()
{
    test(42);               // signed
    test(0xFULL, "0xFULL"); // unsigned
    test('A');              // platform-dependent
    test(true, "true");     // unsigned
    test(4e-2, "4e-2");     // non-integral (hex-float)
    test("∫∫");             // non-integral
}
```

Possible output:

```text
(42) is a signed integral
0xFULL (15) is an unsigned integral
(A) is a signed integral
true (1) is an unsigned integral
4e-2 (0.04) is non-integral
(∫∫) is non-integral
```

### References

{/* source:cpp-concepts-unsigned-integral:0004 */}
C++23 standard (ISO/IEC 14882:2024): 18.4.7 Arithmetic concepts [concepts.arithmetic]

{/* source:cpp-concepts-unsigned-integral:0005 */}
C++20 standard (ISO/IEC 14882:2020): 18.4.7 Arithmetic concepts [concepts.arithmetic]

### See also

{/* source:cpp-concepts-unsigned-integral:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_integral`](/docs/cpp/library/meta/is_integral) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an integral type (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_signed`](/docs/cpp/library/meta/is_signed) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a signed arithmetic type (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
