---
title: "boolean-testable"
description: "The exposition-only concept boolean-testable specifies the requirements for expressions that are convertible to bool and for which the logical operators have the usual behavior (including short-circuiting), even for two different boolean-testable types."
source_url: "https://en.cppreference.com/w/cpp/concepts/boolean-testable"
language: "C++"
---

{/* source:cpp-concepts-boolean-testable:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`template< class B >
concept __boolean_testable_impl = std::convertible_to<B, bool>;`} />
  (1) (exposition only*)
  <Declaration id="2" language="cpp" since="C++20" code={`template< class B >
concept boolean-testable =
    __boolean_testable_impl<B> &&
    requires (B&& b) {
        { !std::forward<B>(b) } -> __boolean_testable_impl;

    };`} />
  (2) (exposition only*)
</DeclarationDoc>

{/* source:cpp-concepts-boolean-testable:0001 */}
The exposition-only concept `boolean-testable` specifies the requirements for expressions that are convertible to `bool` and for which the logical operators have the usual behavior (including [short-circuiting](https://en.wikipedia.org/wiki/Short-circuit_evaluation)), even for two different `boolean-testable` types.

{/* source:cpp-concepts-boolean-testable:0002 */}
Formally, to model the exposition-only concept `__boolean_testable_impl`, the type must not define any member `operator&&` and `operator||`, and no viable non-member `operator&&` and `operator||` may be visible by [argument-dependent lookup](/docs/cpp/language/functions/adl). Additionally, given an expression `e` such that `decltype((e))` is `B`, `boolean-testable` is modeled only if `bool(e) == !bool(!e)`.

### Equality preservation

{/* source:cpp-concepts-boolean-testable:0003 */}
Expressions declared in [requires expressions](/docs/cpp/language/templates/requires) of the standard library concepts are required to be [equality-preserving](/docs/cpp/library/concepts#Equality_preservation) (except where stated otherwise).

### Notes

{/* source:cpp-concepts-boolean-testable:0004 */}
Examples of `boolean-testable` types include `bool`, [`std::true_type`](/docs/cpp/library/meta/integral_constant), `std::bitset<N>::`[`reference`](/docs/cpp/library/utility/bitset/reference), and `int*`.

### References

{/* source:cpp-concepts-boolean-testable:0005 */}
C++23 standard (ISO/IEC 14882:2024): 18.5.2 Boolean testability [concept.booleantestable]

{/* source:cpp-concepts-boolean-testable:0006 */}
C++20 standard (ISO/IEC 14882:2020): 18.5.2 Boolean testability [concept.booleantestable]
