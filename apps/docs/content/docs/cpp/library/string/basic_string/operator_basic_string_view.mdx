---
title: "std::basic_string<CharT,Traits,Allocator>::operator basic_string_view"
description: "Returns a std::basic_string_view, constructed as if by std::basic_string_view<CharT, Traits>(data(), size())."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string/operator_basic_string_view"
language: "C++"
---

{/* source:cpp-string-basic-string-operator-basic-string-view:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`operator std::basic_string_view<CharT, Traits>() const noexcept;`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-string-basic-string-operator-basic-string-view:0001 */}
Returns a [`std::basic_string_view`](/docs/cpp/library/string/basic_string_view), constructed as if by `std::basic_string_view<CharT, Traits>(data(), size())`.

{/* source:cpp-string-basic-string-operator-basic-string-view:0002 */}
### Parameters

(none)

{/* source:cpp-string-basic-string-operator-basic-string-view:0003 */}
### Return value

A string view representing the entire contents of the string.

{/* source:cpp-string-basic-string-operator-basic-string-view:0004 */}
### Notes

It is the programmer's responsibility to ensure that the resulting string view does not outlive the string.

{/* source:cpp-string-basic-string-operator-basic-string-view:0005 */}
```cpp
std::string get_string();
int f(std::string_view sv);
 
int x = f(get_string()); // OK
std::string_view sv = get_string(); // Bad: holds a dangling pointer
```

{/* source:cpp-string-basic-string-operator-basic-string-view:0006 */}
### Example

```cpp
#include <iostream>
#include <string>
#include <string_view>
 
void show_wstring_size(std::wstring_view wcstr_v)
{
    std::cout << wcstr_v.size() << " code points\n";
}
 
int main()
{
    std::string cppstr = "ラーメン";   // narrow string
    std::wstring wcstr = L"ラーメン";  // wide string
 
    // Implicit conversion from string to string_view
    // via std::string::operator string_view:
    std::string_view cppstr_v = cppstr;
 
    std::cout << cppstr_v << '\n'
              << cppstr_v.size() << " code units\n";
 
    // Implicit conversion from wstring to wstring_view
    // via std::wstring::operator wstring_view:
    show_wstring_size(wcstr);
}
```

Output:

```text
ラーメン
12 code units
4 code points
```

{/* source:cpp-string-basic-string-operator-basic-string-view:0007 */}
### See also

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/string/basic_string_view/basic_string_view)</DescriptionTerm>
    <DescriptionBody>constructs a `basic_string_view` (public member function of `std::basic_string_view<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
