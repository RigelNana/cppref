---
title: "std::basic_string<CharT,Traits,Allocator>::rend, std::basic_string<CharT,Traits,Allocator>::crend"
description: "Returns a reverse iterator to the character following the last character of the reversed string."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string/rend"
language: "C++"
---

{/* source:cpp-string-basic-string-rend:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`reverse_iterator rend();`} />
  (noexcept since C++11)
  (constexpr since C++20)
  <Declaration id="2" language="cpp" code={`const_reverse_iterator rend() const;`} />
  (noexcept since C++11)
  (constexpr since C++20)
  <Declaration id="3" language="cpp" since="C++11" code={`const_reverse_iterator crend() const noexcept;`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-string-basic-string-rend:0001 */}
Returns a reverse iterator to the character following the last character of the reversed string. It corresponds to the character preceding the first character of the non-reversed string. This character acts as a placeholder, attempting to access it results in undefined behavior.

### Parameters

{/* source:cpp-string-basic-string-rend:0002 */}
(none)

### Return value

{/* source:cpp-string-basic-string-rend:0003 */}
Reverse iterator to the character following the last character.

### Complexity

{/* source:cpp-string-basic-string-rend:0004 */}
Constant.

### Notes

{/* source:cpp-string-basic-string-rend:0005 */}
libc++ backports `crend()` to C++98 mode.

### Example

{/* source:cpp-string-basic-string-rend:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <iterator>
#include <string>
 
int main()
{
    std::string p("[A man, a plan, a canal: Panama]");
    std::string q;
 
    std::copy(p.crbegin(), p.crend(), std::back_inserter(q));
    std::cout << "q = " << q << '\n';
 
    std::copy(q.crbegin(), q.crend(), p.rbegin());
    std::cout << "p = " << p << '\n';
}
```

Output:

```text
q = ]amanaP :lanac a ,nalp a ,nam A[
p = ]amanaP :lanac a ,nalp a ,nam A[
```

### See also

{/* source:cpp-string-basic-string-rend:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/string/basic_string/rbegin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/string/basic_string_view/rend)</DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the end (public member function of `std::basic_string_view<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

