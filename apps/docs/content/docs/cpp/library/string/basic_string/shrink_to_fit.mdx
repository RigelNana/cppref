---
title: "std::basic_string<CharT,Traits,Allocator>::shrink_to_fit"
description: "Requests the removal of unused capacity."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string/shrink_to_fit"
language: "C++"
---

{/* source:cpp-string-basic-string-shrink-to-fit:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`void shrink_to_fit();`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-string-basic-string-shrink-to-fit:0001 */}
Requests the removal of unused capacity.

{/* source:cpp-string-basic-string-shrink-to-fit:0002 */}
It is a non-binding request to reduce [`capacity()`](/docs/cpp/library/string/basic_string/capacity) to [`size()`](/docs/cpp/library/string/basic_string/size). It depends on the implementation if the request is fulfilled.

{/* source:cpp-string-basic-string-shrink-to-fit:0003 */}
If (and only if) reallocation takes place, all pointers, references, and iterators are invalidated.

### Complexity

{/* source:cpp-string-basic-string-shrink-to-fit:0004 */}
Linear in the size of the string.

### Notes

{/* source:cpp-string-basic-string-shrink-to-fit:0005 */}
In libstdc++, `shrink_to_fit()` is [not available](https://gcc.gnu.org/onlinedocs/libstdc++/manual/strings.html#strings.string.shrink) in C++98 mode.

### Example

{/* source:cpp-string-basic-string-shrink-to-fit:0006 */}
```cpp
#include <iostream>
#include <string>
 
int main()
{
    std::string s;
    std::cout << "Size of std::string is " << sizeof s << " bytes\n"
        << "Default-constructed capacity is " << s.capacity() 
        << " and size is " << s.size() << '\n';
 
    for (int i = 0; i < 42; i++)
        s.append(" 42 ");
    std::cout << "Capacity after 42 appends is " << s.capacity() 
        << " and size is " << s.size() << '\n';
 
    s.clear();
    std::cout << "Capacity after clear() is " << s.capacity() 
        << " and size is " << s.size() << '\n';
 
    s.shrink_to_fit();
    std::cout << "Capacity after shrink_to_fit() is " << s.capacity() 
        << " and size is " << s.size() << '\n';
}
```

Possible output:

```text
GCC output:
Size of std::string is 32 bytes
Default-constructed capacity is 15 and size 0
Capacity after 42 appends is 240 and size 168
Capacity after clear() is 240 and size 0
Capacity after shrink_to_fit() is 15 and size 0
 
clang output (with -stdlib=libc++):
Size of std::string is 24 bytes
Default-constructed capacity is 22 and size is 0
Capacity after 42 appends is 191 and size is 168
Capacity after clear() is 191 and size is 0
Capacity after shrink_to_fit() is 22 and size is 0
```

### Defect reports

{/* source:cpp-string-basic-string-shrink-to-fit:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-string-basic-string-shrink-to-fit:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={755} standard="C++98">
    <PublishedBehavior>`std::string` lacked explicit shrink-to-fit operations</PublishedBehavior>
    <CorrectedBehavior>provided</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2223} standard="C++98">
    <PublishedBehavior>1. references, pointers, and iterators were not invalidated<br />2. there was no complexity requirement</PublishedBehavior>
    <CorrectedBehavior>1. they may be invalidated<br />2. required to be linear</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-string-basic-string-shrink-to-fit:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`size`, `length`](/docs/cpp/library/string/basic_string/size)</DescriptionTerm>
    <DescriptionBody>returns the number of characters (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`capacity`](/docs/cpp/library/string/basic_string/capacity)</DescriptionTerm>
    <DescriptionBody>returns the number of characters that can be held in currently allocated storage (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`resize`](/docs/cpp/library/string/basic_string/resize)</DescriptionTerm>
    <DescriptionBody>changes the number of characters stored (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

