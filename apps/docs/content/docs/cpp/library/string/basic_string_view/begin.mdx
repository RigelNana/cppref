---
title: "std::basic_string_view<CharT,Traits>::begin, std::basic_string_view<CharT,Traits>::cbegin"
description: "Returns an iterator to the first character of the view."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string_view/begin"
language: "C++"
---

{/* source:cpp-string-basic-string-view-begin:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`constexpr const_iterator begin() const noexcept;`} />
  <Declaration language="cpp" since="C++17" code={`constexpr const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-string-basic-string-view-begin:0001 */}
Returns an iterator to the first character of the view.

### Parameters

{/* source:cpp-string-basic-string-view-begin:0002 */}
(none)

### Return value

{/* source:cpp-string-basic-string-view-begin:0003 */}
`const_iterator` to the first character.

### Complexity

{/* source:cpp-string-basic-string-view-begin:0004 */}
Constant.

### Example

{/* source:cpp-string-basic-string-view-begin:0005 */}
```cpp
#include <concepts>
#include <string_view>
 
int main()
{
    constexpr std::string_view str_view("abcd");
 
    constexpr auto begin = str_view.begin();
    constexpr auto cbegin = str_view.cbegin();
    static_assert(
        *begin == 'a' and
        *cbegin == 'a' and
        *begin == *cbegin and
        begin == cbegin and
        std::same_as<decltype(begin), decltype(cbegin)>);
}
```

### See also

{/* source:cpp-string-basic-string-view-begin:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end` `cend`](/docs/cpp/library/string/basic_string_view/end)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin` `cbegin`](/docs/cpp/library/string/basic_string/begin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
