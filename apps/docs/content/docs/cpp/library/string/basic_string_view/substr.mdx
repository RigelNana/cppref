---
title: "std::basic_string_view<CharT,Traits>::substr"
description: "Returns a view of the substring [pos, pos + rlen), where rlen is the smaller of count and size() - pos."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string_view/substr"
language: "C++"
---

{/* source:cpp-string-basic-string-view-substr:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`constexpr basic_string_view substr( size_type pos = 0,
                                    size_type count = npos ) const;`} />
</DeclarationDoc>

{/* source:cpp-string-basic-string-view-substr:0001 */}
Returns a view of the substring [`pos`, `pos + rlen`), where `rlen` is the smaller of `count` and `size() - pos`.

### Parameters

{/* source:cpp-string-basic-string-view-substr:0002 */}
<ParameterList>
  <Parameter name="pos">position of the first character</Parameter>
  <Parameter name="count">requested length</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-basic-string-view-substr:0003 */}
View of the substring [`pos`, `pos + rlen`).

### Exceptions

{/* source:cpp-string-basic-string-view-substr:0004 */}
[`std::out_of_range`](/docs/cpp/library/error/out_of_range) if `pos > size()`.

### Complexity

{/* source:cpp-string-basic-string-view-substr:0005 */}
Constant.

### Example

{/* source:cpp-string-basic-string-view-substr:0006 */}
```cpp
#include <cstddef>
#include <iostream>
#include <stdexcept>
#include <string_view>
 
int main()
{
    typedef std::size_t count_t, pos_t;
 
    constexpr std::string_view data{"ABCDEF"};
 
    std::cout << data.substr() << '\n'; // ABCDEF, i.e. data[0, 5] that is [0, 6)
    std::cout << data.substr(pos_t(1)) << '\n'; // BCDEF, i.e. [1, 6)
    std::cout << data.substr(pos_t(2), count_t(3)) << '\n'; // CDE, i.e. [2, 2 + 3)
    std::cout << data.substr(pos_t(4), count_t(42)) << '\n'; // EF, i.e. [4, 6)
 
    try
    {
        [[maybe_unused]]
        auto sub = data.substr(pos_t(666), count_t(1)); // throws: pos > size()
    }
    catch (std::out_of_range const& ex)
    {
        std::cout << ex.what() << '\n';
    }
}
```

Possible output:

```text
ABCDEF
BCDEF
CDE
EF
basic_string_view::substr: __pos (which is 666) > __size (which is 6)
```

### See also

{/* source:cpp-string-basic-string-view-substr:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`copy`](/docs/cpp/library/string/basic_string_view/copy)</DescriptionTerm>
    <DescriptionBody>copies characters (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/string/basic_string_view/find)</DescriptionTerm>
    <DescriptionBody>find characters in the view (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`substr`](/docs/cpp/library/string/basic_string/substr)</DescriptionTerm>
    <DescriptionBody>returns a substring (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
