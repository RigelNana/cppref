---
title: "std::basic_string_view<CharT,Traits>::size, std::basic_string_view<CharT,Traits>::length"
description: "Returns the number of CharT elements in the view, i.e. std::distance(begin(), end())."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string_view/size"
language: "C++"
---

{/* source:cpp-string-basic-string-view-size:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`constexpr size_type size() const noexcept;`} />
  <Declaration language="cpp" since="C++17" code={`constexpr size_type length() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-string-basic-string-view-size:0001 */}
Returns the number of CharT elements in the view, i.e. `std::distance(begin(), end())`.

### Parameters

{/* source:cpp-string-basic-string-view-size:0002 */}
(none)

### Return value

{/* source:cpp-string-basic-string-view-size:0003 */}
The number of CharT elements in the view.

### Complexity

{/* source:cpp-string-basic-string-view-size:0004 */}
Constant.

### Example

{/* source:cpp-string-basic-string-view-size:0005 */}
```cpp
#include <iostream>
#include <string_view>
 
// Print a string surrounded by single quotes, its
// length and whether it is considered empty.
void check_string(std::string_view ref)
{
    std::cout << std::boolalpha
              << "'" << ref << "' has " << ref.size()
              << " character(s); emptiness: " << ref.empty() << '\n';
}
 
int main(int argc, char **argv)
{
    // An empty string
    check_string("");
 
    // Almost always not empty: argv[0]
    if (argc > 0)
        check_string(argv[0]);
}
```

Possible output:

```text
'' has 0 character(s); emptiness: true
'./a.out' has 7 character(s); emptiness: false
```

### See also

{/* source:cpp-string-basic-string-view-size:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/string/basic_string_view/empty)</DescriptionTerm>
    <DescriptionBody>checks whether the view is empty (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max_size`](/docs/cpp/library/string/basic_string_view/max_size)</DescriptionTerm>
    <DescriptionBody>returns the maximum number of characters (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`size` `length`](/docs/cpp/library/string/basic_string/size)</DescriptionTerm>
    <DescriptionBody>returns the number of characters (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
