---
title: "std::basic_string_view<CharT,Traits>::rbegin, std::basic_string_view<CharT,Traits>::crbegin"
description: "Returns a reverse iterator to the first character of the reversed view. It corresponds to the last character of the non-reversed view."
source_url: "https://en.cppreference.com/w/cpp/string/basic_string_view/rbegin"
language: "C++"
---

{/* source:cpp-string-basic-string-view-rbegin:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`constexpr const_reverse_iterator rbegin() const noexcept;`} />
  <Declaration language="cpp" since="C++17" code={`constexpr const_reverse_iterator crbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-string-basic-string-view-rbegin:0001 */}
Returns a reverse iterator to the first character of the reversed view. It corresponds to the last character of the non-reversed view.

### Parameters

{/* source:cpp-string-basic-string-view-rbegin:0002 */}
(none)

### Return value

{/* source:cpp-string-basic-string-view-rbegin:0003 */}
`const_reverse_iterator` to the first character.

### Complexity

{/* source:cpp-string-basic-string-view-rbegin:0004 */}
Constant.

### Example

{/* source:cpp-string-basic-string-view-rbegin:0005 */}
```cpp
#include <algorithm>
#include <iostream>
#include <iterator>
#include <string_view>
 
int main()
{
    std::ostream_iterator<char> out_it(std::cout);
    std::string_view str_view("abcdef");
 
    std::copy(str_view.rbegin(), std::next(str_view.rbegin(), 3), out_it);
    *out_it = '\n';
 
    std::copy(str_view.crbegin(), std::next(str_view.crbegin(), 3), out_it);
    *out_it = '\n';
}
```

Output:

```text
fed
fed
```

### See also

{/* source:cpp-string-basic-string-view-rbegin:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rend` `crend`](/docs/cpp/library/string/basic_string_view/rend)</DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin` `crbegin`](/docs/cpp/library/string/basic_string/rbegin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
