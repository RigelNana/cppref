---
title: "std::atoi, std::atol, std::atoll"
description: "Interprets an integer value in a byte string pointed to by str. The implied radix is always 10."
source_url: "https://en.cppreference.com/w/cpp/string/byte/atoi"
language: "C++"
---

{/* source:cpp-string-byte-atoi:0000 */}
<DeclarationDoc>
Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)
  <Declaration id="1" language="cpp" code={`int       atoi( const char* str );`} />
  <Declaration id="2" language="cpp" code={`long      atol( const char* str );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`long long atoll( const char* str );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-atoi:0001 */}
Interprets an integer value in a byte string pointed to by `str`. The implied radix is always 10.

{/* source:cpp-string-byte-atoi:0002 */}
Discards any whitespace characters until the first non-whitespace character is found, then takes as many characters as possible to form a valid integer number representation and converts them to an integer value. The valid integer value consists of the following parts:

{/* source:cpp-string-byte-atoi:0003 */}
- (optional) plus or minus sign
- numeric digits

{/* source:cpp-string-byte-atoi:0004 */}
If the value of the result cannot be represented, i.e. the converted value falls out of range of the corresponding return type, the behavior is undefined.

### Parameters

{/* source:cpp-string-byte-atoi:0005 */}
<ParameterList>
  <Parameter name="str">pointer to the null-terminated byte string to be interpreted</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-atoi:0006 */}
Integer value corresponding to the contents of `str` on success.

{/* source:cpp-string-byte-atoi:0007 */}
If no conversion can be performed, `0` is returned.

### Possible implementation

{/* source:cpp-string-byte-atoi:0008 */}
```cpp
template<typename T>
T atoi_impl(const char* str)
{
    while (std::isspace(static_cast<unsigned char>(*str)))
        ++str;
 
    bool negative = false;
 
    if (*str == '+')
        ++str;
    else if (*str == '-')
    {
        ++str;
        negative = true;
    }
 
    T result = 0;
    for (; std::isdigit(static_cast<unsigned char>(*str)); ++str)
    {
        int digit = *str - '0';
        result *= 10;
        result -= digit; // calculate in negatives to support INT_MIN, LONG_MIN,..
    }
 
    return negative ? result : -result;
}
 
int atoi(const char* str)
{
    return atoi_impl<int>(str);
}
 
long atol(const char* str)
{
    return atoi_impl<long>(str);
}
 
long long atoll(const char* str)
{
    return atoi_impl<long long>(str);
}
```

{/* source:cpp-string-byte-atoi:0009 */}
Actual C++ library implementations fall back to C library implementations of `atoi`, `atoil`, and `atoll`, which either implement it directly (as in [MUSL libc](https://github.com/bminor/musl/blob/master/src/stdlib/atoi.c)) or delegate to [`strtol`](/docs/cpp/library/text/byte/strtol)/[`strtoll`](/docs/cpp/library/text/byte/strtol) (as in [GNU libc](https://github.com/bminor/glibc/blob/master/stdlib/atoi.c)).

### Example

{/* source:cpp-string-byte-atoi:0010 */}
```cpp
#include <cstdlib>
#include <iostream>
 
int main()
{
    const auto data =
    {
        "42",
        "0x2A", // treated as "0" and junk "x2A", not as hexadecimal
        "3.14159",
        "31337 with words",
        "words and 2",
        "-012345",
        "10000000000" // note: out of int32_t range
    };
 
    for (const char* s : data)
    {
        const int i{std::atoi(s)};
        std::cout << "std::atoi('" << s << "') is " << i << '\n';
        if (const long long ll{std::atoll(s)}; i != ll)
            std::cout << "std::atoll('" << s << "') is " << ll << '\n';
    }
}
```
Possible output:
```text
std::atoi('42') is 42
std::atoi('0x2A') is 0
std::atoi('3.14159') is 3
std::atoi('31337 with words') is 31337
std::atoi('words and 2') is 0
std::atoi('-012345') is -12345
std::atoi('10000000000') is 1410065408
std::atoll('10000000000') is 10000000000
```

### See also

{/* source:cpp-string-byte-atoi:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`stoi`, `stol`, `stoll`](/docs/cpp/library/string/basic_string/stol) <InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a string to a signed integer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`stoul`, `stoull`](/docs/cpp/library/string/basic_string/stoul) <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a string to an unsigned integer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtol`, `strtoll`](/docs/cpp/library/text/byte/strtol) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to an integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtoul`, `strtoull`](/docs/cpp/library/text/byte/strtoul) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to an unsigned integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtoimax`, `strtoumax`](/docs/cpp/library/text/byte/strtoimax) <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to [`std::intmax_t`](/docs/cpp/library/utility/types/integer) or [`std::uintmax_t`](/docs/cpp/library/utility/types/integer) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`from_chars`](/docs/cpp/library/text/from_chars) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts a character sequence to an integer or floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/atoi) for `atoi, atol, atoll`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
