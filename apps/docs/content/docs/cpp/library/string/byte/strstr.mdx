---
title: "std::strstr"
description: "Finds the first occurrence of the byte string needle in the byte string pointed to by haystack. The terminating null characters are not compared."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strstr"
language: "C++"
---

{/* source:cpp-string-byte-strstr:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`const char* strstr( const char* haystack, const char* needle );`} />
  <Declaration language="cpp" code={`      char* strstr(       char* haystack, const char* needle );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strstr:0001 */}
Finds the first occurrence of the byte string `needle` in the byte string pointed to by `haystack`. The terminating null characters are not compared.

### Parameters

{/* source:cpp-string-byte-strstr:0002 */}
<ParameterList>
  <Parameter name="haystack">pointer to the null-terminated byte string to examine</Parameter>
  <Parameter name="needle">pointer to the null-terminated byte string to search for</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strstr:0003 */}
Pointer to the first character of the found substring in `haystack`, or a null pointer if no such character is found. If `needle` points to an empty string, `haystack` is returned.

### Example

{/* source:cpp-string-byte-strstr:0004 */}
```cpp
#include <cstring>
#include <iomanip>
#include <iostream>
 
int main()
{
    const char* str = "Try not. Do, or do not. There is no try.";
    const char* target = "not";
 
    for (const char* result = str; (result = std::strstr(result, target)); ++result)
        std::cout << "Found " << std::quoted(target)
                  << " starting at (" << result - str << "): "
                  << std::quoted(result) << '\n';
}
```
Output:
```text
Found "not" starting at (4): "not. Do, or do not. There is no try."
Found "not" starting at (19): "not. There is no try."
```

### See also

{/* source:cpp-string-byte-strstr:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/string/basic_string/find)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of the given substring <br /> (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcsstr`](/docs/cpp/library/text/wide/wcsstr)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of a wide string within another wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strchr`](/docs/cpp/library/text/byte/strchr)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of a character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strrchr`](/docs/cpp/library/text/byte/strrchr)</DescriptionTerm>
    <DescriptionBody>finds the last occurrence of a character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strstr) for `strstr`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
