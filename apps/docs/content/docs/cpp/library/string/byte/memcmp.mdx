---
title: "std::memcmp"
description: "Reinterprets the objects pointed to by lhs and rhs as arrays of unsigned char and compares the first count bytes of these arrays. The comparison is done lexicographically."
source_url: "https://en.cppreference.com/w/cpp/string/byte/memcmp"
language: "C++"
---

{/* source:cpp-string-byte-memcmp:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`int memcmp( const void* lhs, const void* rhs, std::size_t count );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-memcmp:0001 */}
Reinterprets the objects pointed to by `lhs` and `rhs` as arrays of `unsigned char` and compares the first `count` bytes of these arrays. The comparison is done lexicographically.

{/* source:cpp-string-byte-memcmp:0002 */}
The sign of the result is the sign of the difference between the values of the first pair of bytes (both interpreted as `unsigned char`) that differ in the objects being compared.

### Parameters

{/* source:cpp-string-byte-memcmp:0003 */}
<ParameterList>
  <Parameter name="lhs, rhs">pointers to the memory buffers to compare</Parameter>
  <Parameter name="count">number of bytes to examine</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-memcmp:0004 */}
Negative value if the first differing byte (reinterpreted as `unsigned char`) in `lhs` is less than the corresponding byte in `rhs`.

{/* source:cpp-string-byte-memcmp:0005 */}
`0` if all `count` bytes of `lhs` and `rhs` are equal.

{/* source:cpp-string-byte-memcmp:0006 */}
Positive value if the first differing byte in `lhs` is greater than the corresponding byte in `rhs`.

### Notes

{/* source:cpp-string-byte-memcmp:0007 */}
This function reads [object representations](/docs/cpp/language/basic_concepts/objects#Object_representation_and_value_representation), not the object values, and is typically meaningful for only trivially-copyable objects that have no padding. For example, `memcmp()` between two objects of type [`std::string`](/docs/cpp/library/string/basic_string) or [`std::vector`](/docs/cpp/library/container/vector) will not compare their contents, `memcmp()` between two objects of type `struct { char c; int n; }` will compare the padding bytes whose values may differ when the values of `c` and `n` are the same, and even if there were no padding bytes, the `int` would be compared without taking into account endianness.

### Example

{/* source:cpp-string-byte-memcmp:0008 */}
```cpp
#include <cstring>
#include <iostream>
 
void demo(const char* lhs, const char* rhs, std::size_t sz)
{
    std::cout << std::string(lhs, sz);
    const int rc = std::memcmp(lhs, rhs, sz);
    if (rc < 0)
        std::cout << " precedes ";
    else if (rc > 0)
        std::cout << " follows ";
    else
        std::cout << " compares equal to ";
    std::cout << std::string(rhs, sz) << " in lexicographical order\n";
}
 
int main()
{
    char a1[] = {'a', 'b', 'c'};
    char a2[sizeof a1] = {'a', 'b', 'd'};
 
    demo(a1, a2, sizeof a1);
    demo(a2, a1, sizeof a1);
    demo(a1, a1, sizeof a1);
}
```
Output:
```text
abc precedes abd in lexicographical order
abd follows abc in lexicographical order
abc compares equal to abc in lexicographical order
```

### See also

{/* source:cpp-string-byte-memcmp:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`strcmp`](/docs/cpp/library/text/byte/strcmp)</DescriptionTerm>
    <DescriptionBody>compares two strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strncmp`](/docs/cpp/library/text/byte/strncmp)</DescriptionTerm>
    <DescriptionBody>compares a certain number of characters from two strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/memcmp) for `memcmp`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
