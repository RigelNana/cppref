---
title: "std::wcsncpy"
description: "Copies at most count characters of the wide string pointed to by src (including the terminating null wide character) to wide character array pointed to by dest."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wcsncpy"
language: "C++"
---

{/* source:cpp-string-wide-wcsncpy:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)

  <Declaration language="cpp" code={
`wchar_t* wcsncpy( wchar_t* dest, const wchar_t* src, std::size_t count );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wcsncpy:0001 */}

Copies at most `count` characters of the wide string pointed to by `src` (including the terminating null wide character) to wide character array pointed to by `dest`.

{/* source:cpp-string-wide-wcsncpy:0002 */}

If `count` is reached before the entire string `src` was copied, the resulting wide character array is not null-terminated.

{/* source:cpp-string-wide-wcsncpy:0003 */}

If, after copying the terminating null wide character from `src`, `count` is not reached, additional null wide characters are written to `dest` until the total of `count` characters have been written.

{/* source:cpp-string-wide-wcsncpy:0004 */}

If the strings overlap, the behavior is undefined.

### Parameters

{/* source:cpp-string-wide-wcsncpy:0005 */}

<ParameterList>
  <Parameter name="dest">pointer to the wide character array to copy to</Parameter>
  <Parameter name="src">pointer to the wide string to copy from</Parameter>
  <Parameter name="count">maximum number of wide characters to copy</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wcsncpy:0006 */}

`dest`

### Notes

{/* source:cpp-string-wide-wcsncpy:0007 */}

In typical usage, `count` is the size of the destination array.

### Example

{/* source:cpp-string-wide-wcsncpy:0008 */}

```cpp
#include <cwchar>
#include <iostream>
 
int main()
{
    const wchar_t src[] = L"hi";
    wchar_t dest[6] = {L'a', L'b', L'c', L'd', L'e', L'f'};
 
    std::wcsncpy(dest, src, 5); // this will copy 'hi' and repeat \0 three times
 
    std::wcout << "The contents of dest are: ";
    for (const wchar_t c : dest)
    {
        if (c)
            std::wcout << c << ' ';
        else
            std::wcout << "\\0" << ' ';
    }
    std::wcout << '\n';
}
```

Output:

```text
The contents of dest are: h i \0 \0 \0 f
```

### See also

{/* source:cpp-string-wide-wcsncpy:0009 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`wcscpy`](/docs/cpp/library/text/wide/wcscpy)</DescriptionTerm>
    <DescriptionBody>copies one wide string to another <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wmemcpy`](/docs/cpp/library/text/wide/wmemcpy)</DescriptionTerm>
    <DescriptionBody>copies a certain amount of wide characters between two non-overlapping arrays <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strncpy`](/docs/cpp/library/text/byte/strncpy)</DescriptionTerm>
    <DescriptionBody>copies a certain amount of characters from one string to another <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wcsncpy) for `wcsncpy`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
