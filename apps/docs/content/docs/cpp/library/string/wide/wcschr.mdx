---
title: "std::wcschr"
description: "Finds the first occurrence of the wide character ch in the wide string pointed to by str."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wcschr"
language: "C++"
---

{/* source:cpp-string-wide-wcschr:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)

  <Declaration id="1" language="cpp" code={
`const wchar_t* wcschr( const wchar_t* str, wchar_t ch );`
  } />
  <Declaration id="2" language="cpp" code={
`      wchar_t* wcschr(       wchar_t* str, wchar_t ch );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wcschr:0001 */}

Finds the first occurrence of the wide character `ch` in the wide string pointed to by `str`.

### Parameters

{/* source:cpp-string-wide-wcschr:0002 */}

<ParameterList>
  <Parameter name="str">pointer to the null-terminated wide string to be analyzed</Parameter>
  <Parameter name="ch">wide character to search for</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wcschr:0003 */}

Pointer to the found character in `str`, or a null pointer if no such character is found.

### Example

{/* source:cpp-string-wide-wcschr:0004 */}

```cpp
#include <cwchar>
#include <iostream>
#include <locale>
 
int main()
{
    const wchar_t arr[] = L"白猫 黒猫 кошки";
    const wchar_t* cat = std::wcschr(arr, L'猫');
    const wchar_t* dog = std::wcschr(arr, L'犬');
 
    std::cout.imbue(std::locale("en_US.utf8"));
 
    if (cat)
        std::cout << "The character 猫 found at position " << cat - arr << '\n';
    else
        std::cout << "The character 猫 not found\n";
 
    if (dog)
        std::cout << "The character 犬 found at position " << dog - arr << '\n';
    else
        std::cout << "The character 犬 not found\n";
}
```

Output:

```text
The character 猫 found at position 1
The character 犬 not found
```

### See also

{/* source:cpp-string-wide-wcschr:0005 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/string/basic_string/find)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of the given substring <br /> (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strchr`](/docs/cpp/library/text/byte/strchr)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of a character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcsrchr`](/docs/cpp/library/text/wide/wcsrchr)</DescriptionTerm>
    <DescriptionBody>finds the last occurrence of a wide character in a wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcspbrk`](/docs/cpp/library/text/wide/wcspbrk)</DescriptionTerm>
    <DescriptionBody>finds the first location of any wide character in one wide string, in another wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wcschr) for `wcschr`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
