---
title: "std::towlower"
description: "Converts the given wide character to lowercase, if possible."
source_url: "https://en.cppreference.com/w/cpp/string/wide/towlower"
language: "C++"
---

{/* source:cpp-string-wide-towlower:0000 */}

<DeclarationDoc>
Defined in header [`<cwctype>`](/docs/cpp/library/headers/cwctype)

  <Declaration language="cpp" code={
`std::wint_t towlower( std::wint_t ch );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-towlower:0001 */}

Converts the given wide character to lowercase, if possible.

{/* source:cpp-string-wide-towlower:0002 */}

If the value of `ch` is neither representable as a `wchar_t` nor equal to the value of the macro `WEOF`, the behavior is undefined.

### Parameters

{/* source:cpp-string-wide-towlower:0003 */}

<ParameterList>
  <Parameter name="ch">wide character to be converted</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-towlower:0004 */}

Lowercase version of `ch` or unmodified `ch` if no lowercase version is listed in the current C locale.

### Notes

{/* source:cpp-string-wide-towlower:0005 */}

Only 1:1 character mapping can be performed by this function, e.g. the Greek uppercase letter `'Σ'` has two lowercase forms, depending on the position in a word: `'σ'` and `'ς'`. A call to `std::towlower` cannot be used to obtain the correct lowercase form in this case.

{/* source:cpp-string-wide-towlower:0006 */}

[ISO 30112](https://www.open-std.org/JTC1/SC35/WG5/docs/30112d10.pdf) specifies which pairs of Unicode characters are included in this mapping.

### Example

{/* source:cpp-string-wide-towlower:0007 */}

```cpp
#include <clocale>
#include <cwctype>
#include <iostream>
 
int main()
{
    wchar_t c = L'\u0190'; // Latin capital open E ('Ɛ')
 
    std::cout << std::hex << std::showbase;
    std::cout << "in the default locale, towlower("
              << static_cast<std::wint_t>(c) << ") = "
              << std::towlower(c) << '\n';
    std::setlocale(LC_ALL, "en_US.utf8");
    std::cout << "in Unicode locale, towlower("
              << static_cast<std::wint_t>(c) << ") = "
              << std::towlower(c) << '\n';
}
```

Output:

```text
in the default locale, towlower(0x190) = 0x190
in Unicode locale, towlower(0x190) = 0x25b
```

### See also

{/* source:cpp-string-wide-towlower:0008 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`towupper`](/docs/cpp/library/text/wide/towupper)</DescriptionTerm>
    <DescriptionBody>converts a wide character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tolower(std::locale)`](/docs/cpp/library/text/locale/tolower)</DescriptionTerm>
    <DescriptionBody>converts a character to lowercase using the `ctype` facet of a locale <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tolower`](/docs/cpp/library/text/byte/tolower)</DescriptionTerm>
    <DescriptionBody>converts a character to lowercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/towlower) for `towlower`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
