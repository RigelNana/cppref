---
title: "std::wcslen"
description: "Returns the length of a wide string, that is the number of non-null wide characters that precede the terminating null wide character."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wcslen"
language: "C++"
---

{/* source:cpp-string-wide-wcslen:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)

  <Declaration language="cpp" code={
`std::size_t wcslen( const wchar_t* str );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wcslen:0001 */}

Returns the length of a wide string, that is the number of non-null wide characters that precede the terminating null wide character.

{/* source:cpp-string-wide-wcslen:0002 */}

The behavior is undefined if there is no null character in the wide character array pointed to by `str`.

### Parameters

{/* source:cpp-string-wide-wcslen:0003 */}

<ParameterList>
  <Parameter name="str">pointer to the null-terminated wide string to be examined</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wcslen:0004 */}

The length of the null-terminated wide string `str`.

### Possible implementation

{/* source:cpp-string-wide-wcslen:0005 */}

```cpp
std::size_t wcslen(const wchar_t* start)
{
    // NB: start is not checked for nullptr!
    const wchar_t* end = start;
    while (*end != L'\0')
        ++end;
    return end - start;
}
```

### Example

{/* source:cpp-string-wide-wcslen:0006 */}

```cpp
#include <iostream>
#include <cwchar>
int main()
{
    const wchar_t* str = L"Hello, world!";
    std::wcout << "The length of L\"" << str << "\" is " << std::wcslen(str) << '\n';
}
```

Output:

```text
The length of L"Hello, world!" is 13
```

### See also

{/* source:cpp-string-wide-wcslen:0007 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`strlen`](/docs/cpp/library/text/byte/strlen)</DescriptionTerm>
    <DescriptionBody>returns the length of a given string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`mblen`](/docs/cpp/library/text/multibyte/mblen)</DescriptionTerm>
    <DescriptionBody>returns the number of bytes in the next multibyte character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wcslen) for `wcslen`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
