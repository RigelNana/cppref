---
title: "std::wcscspn"
description: "Returns the length of the maximum initial segment of the wide string pointed to by dest, that consists of only the characters not found in wide string pointed to by src."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wcscspn"
language: "C++"
---

{/* source:cpp-string-wide-wcscspn:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)

  <Declaration language="cpp" code={
`std::size_t wcscspn( const wchar_t* dest, const wchar_t* src );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wcscspn:0001 */}

Returns the length of the maximum initial segment of the wide string pointed to by `dest`, that consists of only the characters *not* found in wide string pointed to by `src`.

### Parameters

{/* source:cpp-string-wide-wcscspn:0002 */}

<ParameterList>
  <Parameter name="dest">pointer to the null-terminated wide string to be analyzed</Parameter>
  <Parameter name="src">pointer to the null-terminated wide string that contains the characters to search for</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wcscspn:0003 */}

The length of the maximum initial segment that contains only characters not found in the character string pointed to by `src`.

### Example

{/* source:cpp-string-wide-wcscspn:0004 */}

The output below was obtained using clang (libc++).
```cpp
#include <cwchar>
#include <iostream>
#include <locale>
 
int main()
{
    wchar_t dest[] = L"白猫 黑狗 甲虫";
    //                      └───┐
    const wchar_t* src = L"甲虫,黑狗";
 
    const std::size_t len = std::wcscspn(dest, src);
    dest[len] = L'\0'; // terminates the segment to print it out
 
    std::wcout.imbue(std::locale("en_US.utf8"));
    std::wcout << L"The length of maximum initial segment is " << len << L".\n";
    std::wcout << L"The segment is \"" << dest << L"\".\n";
}
```

Possible output:

```text
The length of maximum initial segment is 3.
The segment is "白猫 ".
```

### See also

{/* source:cpp-string-wide-wcscspn:0005 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`wcsspn`](/docs/cpp/library/text/wide/wcsspn)</DescriptionTerm>
    <DescriptionBody>returns the length of the maximum initial segment that consists<br />of only the wide characters found in another wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcspbrk`](/docs/cpp/library/text/wide/wcspbrk)</DescriptionTerm>
    <DescriptionBody>finds the first location of any wide character in one wide string, in another wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wcscspn) for `wcscspn`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
