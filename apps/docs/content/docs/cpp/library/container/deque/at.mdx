---
title: "std::deque<T,Allocator>::at"
description: "Returns a reference to the element at specified location pos, with bounds checking."
source_url: "https://en.cppreference.com/w/cpp/container/deque/at"
language: "C++"
---

{/* source:cpp-container-deque-at:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++98" code={`reference at( size_type pos );`} />
  <Declaration id="2" language="cpp" since="C++98" code={`const_reference at( size_type pos ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-deque-at:0001 */}
Returns a reference to the element at specified location `pos`, with bounds checking.

{/* source:cpp-container-deque-at:0002 */}
If `pos` is not within the range of the container, an exception of type [`std::out_of_range`](/docs/cpp/library/error/out_of_range) is thrown.

### Parameters

{/* source:cpp-container-deque-at:0003 */}
<ParameterList>
  <Parameter name="pos">position of the element to return</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-deque-at:0004 */}
Reference to the requested element

### Exceptions

{/* source:cpp-container-deque-at:0005 */}
[`std::out_of_range`](/docs/cpp/library/error/out_of_range) if `pos >= size()`.

### Complexity

{/* source:cpp-container-deque-at:0006 */}
Constant.

### Example

{/* source:cpp-container-deque-at:0007 */}
```cpp
#include <chrono>
#include <cstddef>
#include <iostream>
#include <deque>
#include <stdexcept>

int main()
{
    std::deque<int> data{1, 2, 4, 5, 5, 6};

    // Set element 1
    data.at(1) = 88;

    // Read element 2
    std::cout << "Element at index 2 has value " << data.at(2) << '\n';

    std::cout << "data size = " << data.size() << '\n';

    try
    {
        // Try to set an element at random position >= size()
        auto moon_phase = []
        {
            return std::chrono::system_clock::now().time_since_epoch().count() % 8;
        };
        data.at(data.size() + moon_phase()) = 13;
    }
    catch(const std::out_of_range& ex)
    {
        std::cout << ex.what() << '\n';
    }

    // Print final values
    std::cout << "data:";
    for (int elem : data)
        std::cout << ' ' << elem;
    std::cout << '\n';
}
```

Possible output:

```text
Element at index 2 has value 4
data size = 6
deque::_M_range_check: __n (which is 8) >= this->size() (which is 6)
data: 1 88 4 5 5 6
```

### See also

{/* source:cpp-container-deque-at:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator[]`](/docs/cpp/library/container/deque/operator_at)</DescriptionTerm>
    <DescriptionBody>access specified element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
