---
title: "std::deque<T,Allocator>::rend, std::deque<T,Allocator>::crend"
description: "Returns a reverse iterator to the element following the last element of the reversed deque."
source_url: "https://en.cppreference.com/w/cpp/container/deque/rend"
language: "C++"
---

{/* source:cpp-container-deque-rend:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`reverse_iterator rend();`} />
  <Declaration id="2" language="cpp" code={`const_reverse_iterator rend() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_reverse_iterator crend() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-container-deque-rend:0001 */}
    1. <InlineRevision since="C++11" /> Returns a reverse iterator to the element following the last element of the reversed deque. It corresponds to the element preceding the first element of the non-reversed deque. This element acts as a placeholder, attempting to access it results in undefined behavior.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-deque-rend:0002 */}
(none)

### Return value

{/* source:cpp-container-deque-rend:0003 */}
Reverse iterator to the element following the last element.

### Complexity

{/* source:cpp-container-deque-rend:0004 */}
Constant.

### Notes

{/* source:cpp-container-deque-rend:0005 */}
`libc++` backports `crend()` to C++98 mode.

### Example

{/* source:cpp-container-deque-rend:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <numeric>
#include <string>
#include <deque>

int main()
{
    std::deque<int> nums{1, 2, 4, 8, 16};
    std::deque<std::string> fruits{"orange", "apple", "raspberry"};
    std::deque<char> empty;

    // Print deque.
    std::for_each(nums.rbegin(), nums.rend(), [](const int n) { std::cout << n << ' '; });
    std::cout << '\n';

    // Sums all integers in the deque nums (if any), printing only the result.
    std::cout << "Sum of nums: "
              << std::accumulate(nums.rbegin(), nums.rend(), 0) << '\n';

    // Prints the first fruit in the deque fruits, checking if there is any.
    if (!fruits.empty())
        std::cout << "First fruit: " << *fruits.rbegin() << '\n';

    if (empty.rbegin() == empty.rend())
        std::cout << "deque 'empty' is indeed empty.\n";
}
```

Output:

```text
16 8 4 2 1
Sum of nums: 31
First fruit: raspberry
deque 'empty' is indeed empty.
```

### See also

{/* source:cpp-container-deque-rend:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/container/deque/rbegin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/iterator/rend) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse end iterator for a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
