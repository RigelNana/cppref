---
title: "std::swap(std::deque)"
description: "Specializes the std::swap algorithm for std::deque."
source_url: "https://en.cppreference.com/w/cpp/container/deque/swap2"
language: "C++"
---

{/* source:cpp-container-deque-swap2:0000 */}
<DeclarationDoc>
Defined in header [`<deque>`](/docs/cpp/library/headers/deque)

  <Declaration id="1" language="cpp" until="C++17" code={`template< class T, class Alloc >
void swap( std::deque<T, Alloc>& lhs,

           std::deque<T, Alloc>& rhs );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class T, class Alloc >
void swap( std::deque<T, Alloc>& lhs,
           std::deque<T, Alloc>& rhs )

               noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-container-deque-swap2:0001 */}
Specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm for [`std::deque`](/docs/cpp/library/container/deque). Swaps the contents of `lhs` and `rhs`. Calls `lhs.swap(rhs)`.

### Parameters

{/* source:cpp-container-deque-swap2:0002 */}
<ParameterList>
  <Parameter name="lhs, rhs">containers whose contents to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-deque-swap2:0003 */}
(none)

### Complexity

{/* source:cpp-container-deque-swap2:0004 */}
Constant.

### Exceptions

{/* source:cpp-container-deque-swap2:0005 */}
<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(lhs.swap(rhs)))`
</Revision>

### Example

{/* source:cpp-container-deque-swap2:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <deque>

int main()
{
    std::deque<int> alice{1, 2, 3};
    std::deque<int> bob{7, 8, 9, 10};

    auto print = [](const int& n) { std::cout << ' ' << n; };

    // Print state before swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';

    std::cout << "-- SWAP\n";
    std::swap(alice, bob);

    // Print state after swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';
}
```

Output:

```text
Alice: 1 2 3
Bobby: 7 8 9 10
-- SWAP
Alice: 7 8 9 10
Bobby: 1 2 3
```

### See also

{/* source:cpp-container-deque-swap2:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/container/deque/swap)</DescriptionTerm>
    <DescriptionBody>swaps the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
