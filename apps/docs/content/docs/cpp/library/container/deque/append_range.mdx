---
title: "std::deque<T,Allocator>::append_range"
description: "Inserts copies of elements from the range rg before end(), in non-reversing order."
source_url: "https://en.cppreference.com/w/cpp/container/deque/append_range"
language: "C++"
---

{/* source:cpp-container-deque-append-range:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`template< container-compatible-range<T> R >
void append_range( R&& rg );`} />
</DeclarationDoc>

{/* source:cpp-container-deque-append-range:0001 */}
Inserts copies of elements from the range `rg` before [`end()`](/docs/cpp/library/container/deque/end), in non-reversing order.

{/* source:cpp-container-deque-append-range:0002 */}
All iterators (including the [`end()`](/docs/cpp/library/container/deque/end) iterator) are invalidated. No references are invalidated.

{/* source:cpp-container-deque-append-range:0003 */}
Each iterator in `rg` is dereferenced exactly once.

### Parameters

{/* source:cpp-container-deque-append-range:0004 */}
<ParameterList>
  <Parameter name="rg">a [container compatible range](/docs/cpp/library/ranges/to#container_compatible_range), that is, an [`input_range`](/docs/cpp/library/ranges/input_range) whose elements are convertible to `T`</Parameter>
</ParameterList>

Type requirements:

- `T` must be [`EmplaceConstructible`](/docs/cpp/library/named_req/EmplaceConstructible) into `deque` from `*ranges::begin(rg)`. Otherwise, the behavior is undefined.

### Return value

{/* source:cpp-container-deque-append-range:0005 */}
(none)

### Complexity

{/* source:cpp-container-deque-append-range:0006 */}
Linear in size of `rg`. The number of calls to the constructor of `T` is exactly equal to the `std::ranges::size(rg))`.

### Exceptions

{/* source:cpp-container-deque-append-range:0007 */}
If an exception is thrown other than by the copy constructor, move constructor, assignment operator, or move assignment operator of `T` there are no effects. If an exception is thrown while inserting a single element at either end, there are no effects. Otherwise, if an exception is thrown by the move constructor of a non-[`CopyInsertable`](/docs/cpp/library/named_req/CopyInsertable) `T`, the effects are unspecified.

### Notes

{/* source:cpp-container-deque-append-range:0008 */}
| [`Feature-test`](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_containers_ranges`](/docs/cpp/library/feature_test#cpp_lib_containers_ranges) | [`202202L`](/docs/cpp/library/compiler_support/23#cpp_lib_containers_ranges_202202L) | (C++23) | [Ranges-aware](/docs/cpp/library/ranges/to#container_compatible_range) construction and insertion |

### Example

{/* source:cpp-container-deque-append-range:0009 */}
```cpp
#include <cassert>
#include <deque>
#include <list>

int main()
{
    auto head = std::deque{1, 2, 3, 4};
    const auto tail = std::list{-5, -6, -7};
#ifdef __cpp_lib_containers_ranges
    head.append_range(tail);
#else
    head.insert(head.end(), tail.cbegin(), tail.cend());
#endif
    assert((head == std::deque{1, 2, 3, 4, -5, -6, -7}));
}
```

### See also

{/* source:cpp-container-deque-append-range:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`prepend_range`](/docs/cpp/library/container/deque/prepend_range) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>adds a range of elements to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert_range`](/docs/cpp/library/container/deque/insert_range) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>inserts a range of elements (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`push_back`](/docs/cpp/library/container/deque/push_back)</DescriptionTerm>
    <DescriptionBody>adds an element to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_back`](/docs/cpp/library/container/deque/emplace_back) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs an element in-place at the end (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
