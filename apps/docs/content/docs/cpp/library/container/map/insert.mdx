---
title: "std::map<Key,T,Compare,Allocator>::insert"
description: "Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key."
source_url: "https://en.cppreference.com/w/cpp/container/map/insert"
language: "C++"
---

{/* source:cpp-container-map-insert:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`std::pair<iterator, bool> insert( const value_type& value );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class P >
std::pair<iterator, bool> insert( P&& value );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`std::pair<iterator, bool> insert( value_type&& value );`} />
  <Declaration id="4" language="cpp" until="C++11" code={`iterator insert( iterator pos, const value_type& value );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`iterator insert( const_iterator pos, const value_type& value );`} />
  <Declaration id="5" language="cpp" since="C++11" code={`template< class P >
iterator insert( const_iterator pos, P&& value );`} />
  <Declaration id="6" language="cpp" since="C++17" code={`iterator insert( const_iterator pos, value_type&& value );`} />
  <Declaration id="7" language="cpp" code={`template< class InputIt >
void insert( InputIt first, InputIt last );`} />
  <Declaration id="8" language="cpp" since="C++11" code={`void insert( std::initializer_list<value_type> ilist );`} />
  <Declaration id="9" language="cpp" since="C++17" code={`insert_return_type insert( node_type&& nh );`} />
  <Declaration id="10" language="cpp" since="C++17" code={`iterator insert( const_iterator pos, node_type&& nh );`} />
</DeclarationDoc>

{/* source:cpp-container-map-insert:0001 */}
Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key.

{/* source:cpp-container-map-insert:0002 */}
- 1-3) Inserts `value`.

{/* source:cpp-container-map-insert:0003 */}
- Overload (2) is equivalent to `emplace(std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-map-insert:0004 */}
- 4-6) Inserts `value` in the position as close as possible to the position just prior to `pos`.

{/* source:cpp-container-map-insert:0005 */}
- Overload (5) is equivalent to `emplace_hint(hint, std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-map-insert:0006 */}
- 7) Inserts elements from range `[first, last)`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-map-insert:0007 */}
- 8) Inserts elements from initializer list `ilist`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-map-insert:0008 */}
- 9) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-map-insert:0009 */}
- 10) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing and returns the end iterator. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`, and returns the iterator pointing to the element with key equivalent to `nh.key()` (regardless of whether the insert succeeded or failed). If the insertion succeeds, `nh` is moved from, otherwise it retains ownership of the element. The element is inserted as close as possible to the position just prior to `pos`. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-map-insert:0010 */}
No iterators or references are invalidated. <InlineRevision since="C++17">If the insertion is successful, pointers and references to the element obtained while it is held in the node handle are invalidated, and pointers and references obtained to that element before it was extracted become valid.</InlineRevision>

### Parameters

{/* source:cpp-container-map-insert:0011 */}
<ParameterList>
  <Parameter name="pos">iterator to the position before which the new element will be inserted</Parameter>
  <Parameter name="value">element value to insert</Parameter>
  <Parameter name="first, last">range of elements to insert</Parameter>
  <Parameter name="ilist">initializer list to insert the values from</Parameter>
  <Parameter name="nh">a compatible [node handle](/docs/cpp/library/container/node_handle)</Parameter>
</ParameterList>

Type requirements

- `InputIt` must meet the requirements of [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator).

### Return value

{/* source:cpp-container-map-insert:0012 */}
- 1-3) A pair consisting of an iterator to the inserted element (or to the element that prevented the insertion) and a `bool` value set to `true` if and only if the insertion took place.

{/* source:cpp-container-map-insert:0013 */}
- 4-6) An iterator to the inserted element, or to the element that prevented the insertion.

{/* source:cpp-container-map-insert:0014 */}
- 7,8) (none)

{/* source:cpp-container-map-insert:0015 */}
- 9) An object of [`insert_return_type`](/docs/cpp/library/container/map#Member_types) with the members initialized as follows: If `nh` is empty, `inserted` is `false`, `position` is `end()`, and `node` is empty. Otherwise if the insertion took place, `inserted` is `true`, `position` points to the inserted element, and `node` is empty. If the insertion failed, `inserted` is `false`, `node` has the previous value of `nh`, and `position` points to an element with a key equivalent to `nh.key()`.

{/* source:cpp-container-map-insert:0016 */}
- 10) End iterator if `nh` was empty, iterator pointing to the inserted element if insertion took place, and iterator pointing to an element with a key equivalent to `nh.key()` if it failed.

### Exceptions

{/* source:cpp-container-map-insert:0017 */}
- 1-6) If an exception is thrown by any operation, the insertion has no effect.

{/* source:cpp-container-map-insert:0018 */}
This section is incomplete

Reason: cases 7-10

### Complexity

{/* source:cpp-container-map-insert:0019 */}
- 1-3) Logarithmic in the size of the container, `O(log(size()))`.

{/* source:cpp-container-map-insert:0020 */}
- 4-6) Amortized constant if the insertion happens in the position just <InlineRevision until="C++11">after</InlineRevision><InlineRevision since="C++11">before</InlineRevision> `pos`, logarithmic in the size of the container otherwise.

{/* source:cpp-container-map-insert:0021 */}
- 7,8) `O(N·log(size() + N))`, where N is the number of elements to insert.

{/* source:cpp-container-map-insert:0022 */}
- 9) Logarithmic in the size of the container, `O(log(size()))`.

{/* source:cpp-container-map-insert:0023 */}
- 10) Amortized constant if the insertion happens in the position just before `pos`, logarithmic in the size of the container otherwise.

### Notes

{/* source:cpp-container-map-insert:0024 */}
The hinted insert (4-6) does not return a boolean in order to be signature-compatible with positional insert on sequential containers, such as [`std::vector::insert`](/docs/cpp/library/container/vector/insert). This makes it possible to create generic inserters such as [`std::inserter`](/docs/cpp/library/iterator/inserter). One way to check success of a hinted insert is to compare [`size()`](/docs/cpp/library/container/map/size) before and after.

### Example

{/* source:cpp-container-map-insert:0025 */}
```cpp
#include <iomanip>
#include <iostream>
#include <map>
#include <string>
using namespace std::literals;

template<typename It>
void print_insertion_status(It it, bool success)
{
    std::cout << "Insertion of " << it->first
              << (success ? " succeeded\n" : " failed\n");
}

int main()
{
    std::map<std::string, float> heights;

    // Overload 3: insert from rvalue reference
    const auto [it_hinata, success] = heights.insert({"Hinata"s, 162.8});
    print_insertion_status(it_hinata, success);

    {
        // Overload 1: insert from lvalue reference
        const auto [it, success2] = heights.insert(*it_hinata);
        print_insertion_status(it, success2);
    }
    {
        // Overload 2: insert via forwarding to emplace
        const auto [it, success] = heights.insert(std::pair{"Kageyama", 180.6});
        print_insertion_status(it, success);
    }
    {
        // Overload 6: insert from rvalue reference with positional hint
        const std::size_t n = std::size(heights);
        const auto it = heights.insert(it_hinata, {"Azumane"s, 184.7});
        print_insertion_status(it, std::size(heights) != n);
    }
    {
        // Overload 4: insert from lvalue reference with positional hint
        const std::size_t n = std::size(heights);
        const auto it = heights.insert(it_hinata, *it_hinata);
        print_insertion_status(it, std::size(heights) != n);
    }
    {
        // Overload 5: insert via forwarding to emplace with positional hint
        const std::size_t n = std::size(heights);
        const auto it = heights.insert(it_hinata, std::pair{"Tsukishima", 188.3});
        print_insertion_status(it, std::size(heights) != n);
    }

    auto node_hinata = heights.extract(it_hinata);
    std::map<std::string, float> heights2;

    // Overload 7: insert from iterator range
    heights2.insert(std::begin(heights), std::end(heights));

    // Overload 8: insert from initializer_list
    heights2.insert({{"Kozume"s, 169.2}, {"Kuroo", 187.7}});

    // Overload 9: insert node
    const auto status = heights2.insert(std::move(node_hinata));
    print_insertion_status(status.position, status.inserted);

    node_hinata = heights2.extract(status.position);
    {
        // Overload 10: insert node with positional hint
        const std::size_t n = std::size(heights2);
        const auto it = heights2.insert(std::begin(heights2), std::move(node_hinata));
        print_insertion_status(it, std::size(heights2) != n);
    }

    // Print resulting map
    std::cout << std::left << '\n';
    for (const auto& [name, height] : heights2)
        std::cout << std::setw(10) << name << " | " << height << "cm\n";
}
```

Output:

```text
Insertion of Hinata succeeded
Insertion of Hinata failed
Insertion of Kageyama succeeded
Insertion of Azumane succeeded
Insertion of Hinata failed
Insertion of Tsukishima succeeded
Insertion of Hinata succeeded
Insertion of Hinata succeeded

Azumane    | 184.7cm
Hinata     | 162.8cm
Kageyama   | 180.6cm
Kozume     | 169.2cm
Kuroo      | 187.7cm
Tsukishima | 188.3cm
```

### Defect reports

{/* source:cpp-container-map-insert:0026 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-map-insert:0027 */}
<DefectReportList>
  <DefectReport kind="lwg" id={233} standard="C++98">
    <PublishedBehavior>`pos` was just a hint, it could be totally ignored</PublishedBehavior>
    <CorrectedBehavior>the insertion is required to be as close as possible to the position just prior to `pos`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={264} standard="C++98">
    <PublishedBehavior>the complexity of overload (7) was required to be linear if the range `[first, last)` is sorted according to `Compare`</PublishedBehavior>
    <CorrectedBehavior>removed the linear requirement in this special case</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={316} standard="C++98">
    <PublishedBehavior>in the return value of overload (1), it was not specified which `bool` value indicates a successful insertion</PublishedBehavior>
    <CorrectedBehavior>success is indicated by `true`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2005} standard="C++11">
    <PublishedBehavior>overloads (2,5) were poorly described</PublishedBehavior>
    <CorrectedBehavior>improved the description</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-map-insert:0028 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/container/map/emplace) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs element in-place (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_hint`](/docs/cpp/library/container/map/emplace_hint) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs elements in-place using a hint (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert_or_assign`](/docs/cpp/library/container/map/insert_or_assign) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>inserts an element or assigns to the current element if the key already exists (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`inserter`](/docs/cpp/library/iterator/inserter)</DescriptionTerm>
    <DescriptionBody>creates a [`std::insert_iterator`](/docs/cpp/library/iterator/insert_iterator) of type inferred from the argument (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
