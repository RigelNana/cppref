---
title: "std::map<Key,T,Compare,Allocator>::operator[]"
description: "Returns a reference to the value that is mapped to a key equivalent to key or x respectively, performing an insertion if such key does not already exist."
source_url: "https://en.cppreference.com/w/cpp/container/map/operator_at"
language: "C++"
---

{/* source:cpp-container-map-operator-at:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++98" code={`T& operator[]( const Key& key );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`T& operator[]( Key&& key );`} />
  <Declaration id="3" language="cpp" since="C++26" code={`template< class K >
T& operator[]( K&& x );`} />
</DeclarationDoc>

{/* source:cpp-container-map-operator-at:0001 */}
Returns a reference to the value that is mapped to a key equivalent to `key` or `x` respectively, performing an insertion if such key does not already exist.

{/* source:cpp-container-map-operator-at:0002 */}
<Revision until="C++11">
1. Inserts `value_type(key, T())` if the key does not exist.

- `key_type` must meet the requirements of [`CopyConstructible`](/docs/cpp/library/named_req/CopyConstructible).
- `mapped_type` must meet the requirements of [`CopyConstructible`](/docs/cpp/library/named_req/CopyConstructible) and [`DefaultConstructible`](/docs/cpp/library/named_req/DefaultConstructible).

If an insertion is performed, the mapped value is [value-initialized](/docs/cpp/language/initialization/value_initialization) (default-constructed for class types, zero-initialized otherwise) and a reference to it is returned.
</Revision>

<Revision since="C++11">
1. Inserts a [`value_type`](/docs/cpp/library/container/map#Member_types) object constructed in-place from `std::piecewise_construct, std::forward_as_tuple(key), std::tuple<>()` if the key does not exist.
<InlineRevision since="C++17">Equivalent to `return this->try_emplace(key).first->second;`.</InlineRevision>
When the default allocator is used, this results in the key being copy constructed from `key` and the mapped value being value-initialized.

- `value_type` must be [`EmplaceConstructible`](/docs/cpp/library/named_req/EmplaceConstructible) from `std::piecewise_construct, std::forward_as_tuple(key), std::tuple<>()`.

When the default allocator is used, this means that `key_type` must be [`CopyConstructible`](/docs/cpp/library/named_req/CopyConstructible) and `mapped_type` must be [`DefaultConstructible`](/docs/cpp/library/named_req/DefaultConstructible).

2. Inserts a [`value_type`](/docs/cpp/library/container/map#Member_types) object constructed in-place from `std::piecewise_construct, std::forward_as_tuple(std::move(key)), std::tuple<>()` if the key does not exist.
<InlineRevision since="C++17">Equivalent to `return this->try_emplace(std::move(key)).first->second;`.</InlineRevision>
When the default allocator is used, this results in the key being move constructed from `key` and the mapped value being value-initialized.

- `value_type` must be [`EmplaceConstructible`](/docs/cpp/library/named_req/EmplaceConstructible) from `std::piecewise_construct, std::forward_as_tuple(std::move(key)), std::tuple<>()`.

When the default allocator is used, this means that `key_type` must be [`MoveConstructible`](/docs/cpp/library/named_req/MoveConstructible) and `mapped_type` must be [`DefaultConstructible`](/docs/cpp/library/named_req/DefaultConstructible).
</Revision>

{/* source:cpp-container-map-operator-at:0003 */}
- 3) Inserts a [`value_type`](/docs/cpp/library/container/map#Member_types) object constructed in-place if there is no key that transparently compares equivalent to the value `x`.

{/* source:cpp-container-map-operator-at:0004 */}
- Equivalent to `return this->try_emplace(std::forward<K>(x)).first->second;`. This overload participates in overload resolution only if the qualified-id `Compare::is_transparent` is valid and denotes a type. It allows calling this function without constructing an instance of `Key`.

{/* source:cpp-container-map-operator-at:0005 */}
No iterators or references are invalidated.

## Parameters

{/* source:cpp-container-map-operator-at:0006 */}
<ParameterList>
  <Parameter name="key">the key of the element to find</Parameter>
  <Parameter name="x">a value of any type that can be transparently compared with a key</Parameter>
</ParameterList>

## Return value

{/* source:cpp-container-map-operator-at:0007 */}
- 1,2) A reference to the mapped value of the new element if no element with key `key` existed. Otherwise, a reference to the mapped value of the existing element whose key is equivalent to `key`.

{/* source:cpp-container-map-operator-at:0008 */}
- 3) A reference to the mapped value of the new element if no element with key that compares equivalent to the value `x` existed. Otherwise, a reference to the mapped value of the existing element whose key compares equivalent to `x`.

## Exceptions

{/* source:cpp-container-map-operator-at:0009 */}
If an exception is thrown by any operation, the insertion has no effect.

## Complexity

{/* source:cpp-container-map-operator-at:0010 */}
Logarithmic in the size of the container.

## Notes

{/* source:cpp-container-map-operator-at:0011 */}
In the published C++11 and C++14 standards, this function was specified to require `mapped_type` to be [`DefaultInsertable`](/docs/cpp/library/named_req/DefaultInsertable) and `key_type` to be [`CopyInsertable`](/docs/cpp/library/named_req/CopyInsertable) or [`MoveInsertable`](/docs/cpp/library/named_req/MoveInsertable) into `*this`. This specification was defective and was fixed by [LWG issue 2469](https://cplusplus.github.io/LWG/issue2469), and the description above incorporates the resolution of that issue.

{/* source:cpp-container-map-operator-at:0012 */}
However, one implementation (libc++) is known to construct the `key_type` and `mapped_type` objects via two separate allocator `construct()` calls, as arguably required by the standards as published, rather than emplacing a `value_type` object.

{/* source:cpp-container-map-operator-at:0013 */}
`operator[]` is non-`const` because it inserts the key if it doesn't exist. If this behavior is undesirable or if the container is `const`, [`at`](/docs/cpp/library/container/map/at) may be used.

{/* source:cpp-container-map-operator-at:0014 */}
<Revision since="C++17">
[`insert_or_assign`](/docs/cpp/library/container/map/insert_or_assign) returns more information than `operator[]` and does not require default-constructibility of the mapped type.
</Revision>

{/* source:cpp-container-map-operator-at:0015 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_associative_heterogeneous_insertion`](/docs/cpp/library/feature_test#cpp_lib_associative_heterogeneous_insertion) | [`202311L`](/docs/cpp/library/compiler_support/26#cpp_lib_associative_heterogeneous_insertion_202311L) | (C++26) | Heterogeneous overloads for the remaining member functions in [ordered](/docs/cpp/library/container#Associative_containers) and [unordered](/docs/cpp/library/container#Unordered_associative_containers) associative [containers](/docs/cpp/library/container). ([3](/docs/cpp/library/container/map/operator_at#Version_3)) |

## Example

{/* source:cpp-container-map-operator-at:0016 */}
```cpp
#include <iostream>
#include <string>
#include <map>

void println(auto const comment, auto const& map)
{
    std::cout << comment << '{';
    for (const auto& pair : map)
        std::cout << '{' << pair.first << ": " << pair.second << '}';
    std::cout << "}\n";
}

int main()
{
    std::map<char, int> letter_counts{{'a', 27}, {'b', 3}, {'c', 1}};

    println("letter_counts initially contains: ", letter_counts);

    letter_counts['b'] = 42; // updates an existing value
    letter_counts['x'] = 9;  // inserts a new value

    println("after modifications it contains: ", letter_counts);

    // count the number of occurrences of each word
    // (the first call to operator[] initialized the counter with zero)
    std::map<std::string, int>  word_map;
    for (const auto& w : {"this", "sentence", "is", "not", "a", "sentence",
                          "this", "sentence", "is", "a", "hoax"})
        ++word_map[w];
    word_map["that"]; // just inserts the pair {"that", 0}

    for (const auto& [word, count] : word_map)
        std::cout << count << " occurrence(s) of word '" << word << "'\n";
}
```

Output:

```text
letter_counts initially contains: {{a: 27}{b: 3}{c: 1}}
after modifications it contains: {{a: 27}{b: 42}{c: 1}{x: 9}}
2 occurrence(s) of word 'a'
1 occurrence(s) of word 'hoax'
2 occurrence(s) of word 'is'
1 occurrence(s) of word 'not'
3 occurrence(s) of word 'sentence'
0 occurrence(s) of word 'that'
2 occurrence(s) of word 'this'
```

## Defect reports

{/* source:cpp-container-map-operator-at:0017 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-map-operator-at:0018 */}
<DefectReportList>
  <DefectReport kind="lwg" id={334} standard="C++98">
    <PublishedBehavior>the effect of overload (1) was simply returning `(*((insert(std::make_pair(x, T()))).first)).second`</PublishedBehavior>
    <CorrectedBehavior>provided its own description instead</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

## See also

{/* source:cpp-container-map-operator-at:0019 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`at`](/docs/cpp/library/container/map/at)</DescriptionTerm>
    <DescriptionBody>access specified element with bounds checking (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert_or_assign`](/docs/cpp/library/container/map/insert_or_assign)</DescriptionTerm>
    <DescriptionBody>inserts an element or assigns to the current element if the key already exists (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_emplace`](/docs/cpp/library/container/map/try_emplace) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>inserts in-place if the key does not exist, does nothing if the key exists (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
