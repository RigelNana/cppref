---
title: "std::flat_multiset<Key,Compare,KeyContainer>::begin, std::flat_multiset<Key,Compare,KeyContainer>::cbegin"
description: "Returns an iterator to the first element of the flat_multiset."
source_url: "https://en.cppreference.com/w/cpp/container/flat_multiset/begin"
language: "C++"
---

{/* source:cpp-container-flat-multiset-begin:0000 */}
<DeclarationDoc since="C++23">
  <Declaration id="1" language="cpp" code={`iterator begin() noexcept;`} />
  <Declaration id="2" language="cpp" code={`const_iterator begin() const noexcept;`} />
  <Declaration id="3" language="cpp" code={`const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-flat-multiset-begin:0001 */}
Returns an iterator to the first element of the `flat_multiset`.

{/* source:cpp-container-flat-multiset-begin:0002 */}
If the `flat_multiset` is empty, the returned iterator will be equal to `end()`.

### Parameters

{/* source:cpp-container-flat-multiset-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-flat-multiset-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-flat-multiset-begin:0005 */}
Constant.

### Notes

{/* source:cpp-container-flat-multiset-begin:0006 */}
Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

### Example

{/* source:cpp-container-flat-multiset-begin:0007 */}
```cpp
#include <algorithm>
#include <flat_set>
#include <iostream>

int main()
{
    std::flat_multiset<int> set{3, 1, 4, 1, 5, 9, 2, 6, 5};
    std::for_each(set.cbegin(), set.cend(), [](int x)
    {
        std::cout << x << ' ';
    });
    std::cout << '\n';
}
```

Output:

```text
1 1 2 3 4 5 5 6 9
```

### See also

{/* source:cpp-container-flat-multiset-begin:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end cend`](/docs/cpp/library/container/flat_multiset/end)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
