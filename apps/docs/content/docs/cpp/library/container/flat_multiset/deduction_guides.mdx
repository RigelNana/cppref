---
title: "deduction guides for std::flat_multiset"
description: "These deduction guides are provided for to allow deduction from: a container and a comparator."
source_url: "https://en.cppreference.com/w/cpp/container/flat_multiset/deduction_guides"
language: "C++"
---

{/* source:cpp-container-flat-multiset-deduction-guides:0000 */}
<DeclarationDoc since="C++23">
Defined in header [`<flat_set>`](/docs/cpp/library/headers/flat_set)

  <Declaration id="1" language="cpp" code={`template< class KeyContainer,
          class Compare = std::less<typename KeyContainer::value_type> >
flat_multiset( KeyContainer, Compare = Compare() )

    -> flat_multiset<typename KeyContainer::value_type, Compare, KeyContainer>;`} />
  <Declaration id="2" language="cpp" code={`template< class KeyContainer, class Allocator >
flat_multiset( KeyContainer, Allocator )
    -> flat_multiset<typename KeyContainer::value_type,

                     std::less<typename KeyContainer::value_type>, KeyContainer>;`} />
  <Declaration id="3" language="cpp" code={`template< class KeyContainer, class Compare, class Allocator >
flat_multiset( KeyContainer, Compare, Allocator )

    -> flat_multiset<typename KeyContainer::value_type, Compare, KeyContainer>;`} />
  <Declaration id="4" language="cpp" code={`template< class KeyContainer,
          class Compare = std::less<typename KeyContainer::value_type> >
flat_multiset( std::sorted_equivalent_t, KeyContainer, Compare = Compare() )

    -> flat_multiset<typename KeyContainer::value_type, Compare, KeyContainer>;`} />
  <Declaration id="5" language="cpp" code={`template< class KeyContainer, class Allocator >
flat_multiset( std::sorted_equivalent_t, KeyContainer, Allocator )
    -> flat_multiset<typename KeyContainer::value_type,

                     std::less<typename KeyContainer::value_type>, KeyContainer>;`} />
  <Declaration id="6" language="cpp" code={`template< class KeyContainer, class Compare, class Allocator >
flat_multiset( std::sorted_equivalent_t, KeyContainer, Compare, Allocator )

    -> flat_multiset<typename KeyContainer::value_type, Compare, KeyContainer>;`} />
  <Declaration id="7" language="cpp" code={`template< class InputIter,
          class Compare = std::less</*iter-value-type*/<InputIter>> >
flat_multiset( InputIter, InputIter, Compare = Compare() )

    -> flat_multiset</*iter-value-type*/<InputIter>, Compare>;`} />
  <Declaration id="8" language="cpp" code={`template< class InputIter,
          class Compare = std::less</*iter-value-type*/<InputIter>> >
flat_multiset( std::sorted_equivalent_t, InputIter, InputIter, Compare = Compare() )

    -> flat_multiset</*iter-value-type*/<InputIter>, Compare>;`} />
  <Declaration id="9" language="cpp" code={`template< ranges::input_range R,
          class Compare = std::less<ranges::range_value_t<R>>,
          class Allocator = std::allocator<ranges::range_value_t<R>> >
flat_multiset( std::from_range_t, R&&, Compare = Compare(), Allocator = Allocator() )
    -> flat_multiset<ranges::range_value_t<R>, Compare,
                     std::vector<ranges::range_value_t<R>,

                     /*alloc-rebind*/<Allocator, ranges::range_value_t<R>>>>;`} />
  <Declaration id="10" language="cpp" code={`template< ranges::input_range R, class Allocator >
flat_multiset( std::from_range_t, R&&, Allocator )
    -> flat_multiset<ranges::range_value_t<R>, std::less<ranges::range_value_t<R>>,
                     std::vector<ranges::range_value_t<R>,

                     /*alloc-rebind*/<Allocator, ranges::range_value_t<R>>>>;`} />
  <Declaration id="11" language="cpp" code={`template< class Key, class Compare = std::less<Key> >
flat_multiset( std::initializer_list<Key>, Compare = Compare() )

    -> flat_multiset<Key, Compare>;`} />
  <Declaration id="12" language="cpp" code={`template< class Key, class Compare = std::less<Key> >
flat_multiset( std::sorted_equivalent_t,
               std::initializer_list<Key>, Compare = Compare() )

    -> flat_multiset<Key, Compare>;`} />
</DeclarationDoc>

{/* source:cpp-container-flat-multiset-deduction-guides:0001 */}
These [`deduction guides`](/docs/cpp/language/templates/ctad) are provided for to allow deduction from:

{/* source:cpp-container-flat-multiset-deduction-guides:0002 */}
1. A container and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0003 */}
2. A container and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0004 */}
3. A container, a comparator and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0005 */}
4. The `std::sorted_equivalent_t` tag, a container and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0006 */}
5. The `std::sorted_equivalent_t` tag, a container and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0007 */}
6. The `std::sorted_equivalent_t` tag, a container, a comparator and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0008 */}
7. An iterator range and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0009 */}
8. The `std::sorted_equivalent_t` tag, an iterator range and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0010 */}
9. The `std::from_range_t` tag, an [`input_range`](/docs/cpp/library/ranges/input_range) range, a comparator and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0011 */}
10. The `std::from_range_t` tag, an [`input_range`](/docs/cpp/library/ranges/input_range) range and an allocator.

{/* source:cpp-container-flat-multiset-deduction-guides:0012 */}
11. The [`std::initializer_list`](/docs/cpp/library/utility/initializer_list) and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0013 */}
12. The `std::sorted_equivalent_t` tag, the [`std::initializer_list`](/docs/cpp/library/utility/initializer_list) and a comparator.

{/* source:cpp-container-flat-multiset-deduction-guides:0014 */}
These overloads participate in overload resolution only if `InputIt` satisfies [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator), `Alloc` satisfies [`Allocator`](/docs/cpp/library/named_req/Allocator), and `Comp` does not satisfy [`Allocator`](/docs/cpp/library/named_req/Allocator).

{/* source:cpp-container-flat-multiset-deduction-guides:0015 */}
Note: the extent to which the library determines that a type does not satisfy [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator) is unspecified, except that as a minimum integral types do not qualify as input iterators. Likewise, the extent to which it determines that a type does not satisfy [`Allocator`](/docs/cpp/library/named_req/Allocator) is unspecified, except that as a minimum the member type `Alloc::value_type` must exist and the expression `std::declval<Alloc&>().allocate(std::size_t{})` must be well-formed when treated as an unevaluated operand.

### Example

{/* source:cpp-container-flat-multiset-deduction-guides:0016 */}
This section is incomplete
Reason: no example
