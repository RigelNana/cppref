---
title: "std::swap(std::unordered_multimap)"
description: "Specializes the std::swap algorithm for std::unordered_multimap."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_multimap/swap2"
language: "C++"
---

{/* source:cpp-container-unordered-multimap-swap2:0000 */}
<DeclarationDoc>
Defined in header [`<unordered_map>`](/docs/cpp/library/headers/unordered_map)

  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class Key, class T, class Hash, class KeyEqual, class Alloc >
void swap( std::unordered_multimap<Key, T, Hash, KeyEqual, Alloc>& lhs,

           std::unordered_multimap<Key, T, Hash, KeyEqual, Alloc>& rhs );`} />
  <Declaration id="2" language="cpp" code={`template< class Key, class T, class Hash, class KeyEqual, class Alloc >
void swap( std::unordered_multimap<Key, T, Hash, KeyEqual, Alloc>& lhs,
           std::unordered_multimap<Key, T, Hash, KeyEqual, Alloc>& rhs )

               noexcept(/* see below */);`} />

  <DeclarationDescription>
    {/* source:cpp-container-unordered-multimap-swap2:0001 */}
    1. Specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm for [`std::unordered_multimap`](/docs/cpp/library/container/unordered_multimap). Swaps the contents of `lhs` and `rhs`. Calls `lhs.swap(rhs)`.

    2. <InlineRevision since="C++17" /> Specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm for [`std::unordered_multimap`](/docs/cpp/library/container/unordered_multimap). Swaps the contents of `lhs` and `rhs`. Calls `lhs.swap(rhs)`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-unordered-multimap-swap2:0002 */}
<ParameterList>
  <Parameter name="lhs, rhs">containers whose contents to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-unordered-multimap-swap2:0003 */}
(none)

### Complexity

{/* source:cpp-container-unordered-multimap-swap2:0004 */}
Constant.

### Exceptions

{/* source:cpp-container-unordered-multimap-swap2:0005 */}
<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(lhs.swap(rhs)))`
</Revision>

### Example

{/* source:cpp-container-unordered-multimap-swap2:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <unordered_map>
 
int main()
{
    std::unordered_multimap<int, char> alice{{1, 'a'}, {2, 'b'}, {3, 'c'}};
    std::unordered_multimap<int, char> bob{{7, 'Z'}, {8, 'Y'}, {9, 'X'}, {10, 'W'}};
 
    auto print = [](std::pair<const int, char>& n)
    {
        std::cout << ' ' << n.first << ':' << n.second;
    };
 
    // Print state before swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';
 
    std::cout << "-- SWAP\n";
    std::swap(alice, bob);
 
    // Print state after swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';
}
```

Possible output:

```text
Alice: 1:a 2:b 3:c
Bobby: 7:Z 8:Y 9:X 10:W
-- SWAP
Alice: 7:Z 8:Y 9:X 10:W
Bobby: 1:a 2:b 3:c
```

### See also

{/* source:cpp-container-unordered-multimap-swap2:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/container/unordered_multimap/swap)</DescriptionTerm>
    <DescriptionBody>swaps the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
