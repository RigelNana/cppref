---
title: "std::multimap<Key,T,Compare,Allocator>::contains"
description: "Checks if there is an element with key equivalent to key in the container."
source_url: "https://en.cppreference.com/w/cpp/container/multimap/contains"
language: "C++"
---

{/* source:cpp-container-multimap-contains:0000 */}
<DeclarationDoc>
  <Declaration id="1" since="C++20" language="cpp" code={`bool contains( const Key& key ) const;`} />
  <Declaration id="2" since="C++20" language="cpp" code={`template< class K >
bool contains( const K& x ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-multimap-contains:0001 */}
1. Checks if there is an element with key equivalent to `key` in the container.

{/* source:cpp-container-multimap-contains:0002 */}
2. Checks if there is an element with key that compares equivalent to the value `x`. This overload participates in overload resolution only if the qualified-id `Compare::is_transparent` is valid and denotes a type. It allows calling this function without constructing an instance of `Key`.

### Parameters

{/* source:cpp-container-multimap-contains:0003 */}
<ParameterList>
  <Parameter name="key">key value of the element to search for</Parameter>
  <Parameter name="x">a value of any type that can be transparently compared with a key</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-multimap-contains:0004 */}
`true` if there is such an element, otherwise `false`.

### Complexity

{/* source:cpp-container-multimap-contains:0005 */}
Logarithmic in the size of the container.

### Example

{/* source:cpp-container-multimap-contains:0006 */}
```cpp
#include <iostream>
#include <map>
 
int main()
{
    std::multimap<int, char> example{{1, 'a'}, {2, 'b'}};
 
    for (int x : {2, 5})
        if (example.contains(x))
            std::cout << x << ": Found\n";
        else
            std::cout << x << ": Not found\n";
}
```

Output:

```text
2: Found
5: Not found
```

### See also

{/* source:cpp-container-multimap-contains:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/container/multimap/find)</DescriptionTerm>
    <DescriptionBody>finds element with specific key (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`count`](/docs/cpp/library/container/multimap/count)</DescriptionTerm>
    <DescriptionBody>returns the number of elements matching specific key (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`equal_range`](/docs/cpp/library/container/multimap/equal_range)</DescriptionTerm>
    <DescriptionBody>returns range of elements matching a specific key (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
