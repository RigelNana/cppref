---
title: "std::multimap<Key,T,Compare,Allocator>::insert"
description: "Inserts element(s) into the container."
source_url: "https://en.cppreference.com/w/cpp/container/multimap/insert"
language: "C++"
---

{/* source:cpp-container-multimap-insert:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`iterator insert( const value_type& value );`} />
  <Declaration id="2" since="C++17" language="cpp" code={`iterator insert( value_type&& value );`} />
  <Declaration id="3" since="C++11" language="cpp" code={`template< class P >
iterator insert( P&& value );`} />
  <Declaration id="4" until="C++11" language="cpp" code={`iterator insert( iterator pos, const value_type& value );`} />
  <Declaration id="4" since="C++11" language="cpp" code={`iterator insert( const_iterator pos, const value_type& value );`} />
  <Declaration id="5" since="C++17" language="cpp" code={`iterator insert( const_iterator pos, value_type&& value );`} />
  <Declaration id="6" since="C++11" language="cpp" code={`template< class P >
iterator insert( const_iterator pos, P&& value );`} />
  <Declaration id="7" language="cpp" code={`template< class InputIt >
void insert( InputIt first, InputIt last );`} />
  <Declaration id="8" since="C++11" language="cpp" code={`void insert( std::initializer_list<value_type> ilist );`} />
  <Declaration id="9" since="C++17" language="cpp" code={`iterator insert( node_type&& nh );`} />
  <Declaration id="10" since="C++17" language="cpp" code={`iterator insert( const_iterator pos, node_type&& nh );`} />
</DeclarationDoc>

{/* source:cpp-container-multimap-insert:0001 */}
Inserts element(s) into the container.

{/* source:cpp-container-multimap-insert:0002 */}
- 1-3) Inserts `value`. If the container has elements with equivalent key, inserts at the upper bound of that range.

{/* source:cpp-container-multimap-insert:0003 */}
- Overload (3) is equivalent to `emplace(std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-multimap-insert:0004 */}
- 4-6) Inserts `value` in the position as close as possible to the position just prior to `pos`.

{/* source:cpp-container-multimap-insert:0005 */}
- Overload (6) is equivalent to `emplace_hint(hint, std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-multimap-insert:0006 */}
- 7) Inserts elements from range [`first, last)`.

{/* source:cpp-container-multimap-insert:0007 */}
- 8) Inserts elements from initializer list `ilist`.

{/* source:cpp-container-multimap-insert:0008 */}
- 9) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing. Otherwise, inserts the element owned by `nh` into the container and returns an iterator pointing at the inserted element. If a range containing elements with keys equivalent to `nh.key()` exists in the container, the element is inserted at the end of that range. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-multimap-insert:0009 */}
- 10) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing and returns the end iterator. Otherwise, inserts the element owned by `nh` into the container, and returns the iterator pointing to the element with key equivalent to `nh.key()`. The element is inserted as close as possible to the position just prior to `pos`. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-multimap-insert:0010 */}
No iterators or references are invalidated. <InlineRevision since="C++17">If the insertion is successful, pointers and references to the element obtained while it is held in the node handle are invalidated, and pointers and references obtained to that element before it was extracted become valid.</InlineRevision>

### Parameters

{/* source:cpp-container-multimap-insert:0011 */}
<ParameterList>
  <Parameter name="pos">
    iterator to the position before which the new element will be inserted
  </Parameter>
  <Parameter name="value">
    element value to insert
  </Parameter>
  <Parameter name="first, last">
    range of elements to insert
  </Parameter>
  <Parameter name="ilist">
    initializer list to insert the values from
  </Parameter>
  <Parameter name="nh">
    a compatible [node handle](/docs/cpp/library/container/node_handle)
  </Parameter>
</ParameterList>

Type requirements

- `InputIt` must meet the requirements of [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator).

### Return value

{/* source:cpp-container-multimap-insert:0012 */}
- 1-6) An iterator to the inserted element.

{/* source:cpp-container-multimap-insert:0013 */}
- 7,8) (none)

{/* source:cpp-container-multimap-insert:0014 */}
- 9,10) End iterator if `nh` was empty, iterator pointing to the inserted element otherwise.

### Exceptions

{/* source:cpp-container-multimap-insert:0015 */}
- 1-6) If an exception is thrown by any operation, the insertion has no effect.

{/* source:cpp-container-multimap-insert:0016 */}
- 7,8) No exception safety guarantee.

{/* source:cpp-container-multimap-insert:0017 */}
- 9,10) If an exception is thrown by any operation, the insertion has no effect.

### Complexity

{/* source:cpp-container-multimap-insert:0018 */}
- 1-3) `O(log(size()))`

{/* source:cpp-container-multimap-insert:0019 */}
- 4-6) Amortized constant if the insertion happens in the position just before `pos`, `O(log(size()))` otherwise.

{/* source:cpp-container-multimap-insert:0020 */}
- 7,8) `O(N·log(size() + N))`, where `N` is the number of elements to insert.

{/* source:cpp-container-multimap-insert:0021 */}
- 9) `O(log(size()))`

{/* source:cpp-container-multimap-insert:0022 */}
- 10) Amortized constant if the insertion happens in the position just before `pos`, `O(log(size()))` otherwise.

### Example

{/* source:cpp-container-multimap-insert:0023 */}
```cpp
#include <functional>
#include <iostream>
#include <map>
#include <string>
#include <string_view>
#include <utility>
 
template<class M>
void print(const std::string_view rem, const M& mmap)
{
    std::cout << rem << ' ';
    for (const auto& e : mmap)
        std::cout << '{' << e.first << ',' << e.second << "} ";
    std::cout << '\n';
}
 
int main()
{
    // list-initialize
    std::multimap<int, std::string, std::greater<int>> mmap
        {{2, "foo"}, {2, "bar"}, {3, "baz"}, {1, "abc"}, {5, "def"}};
    print("#1", mmap);
 
    // insert using value_type
    mmap.insert(decltype(mmap)::value_type(5, "pqr"));
    print("#2", mmap);
 
    // insert using pair
    mmap.insert(std::pair{6, "uvw"});
    print("#3", mmap);
 
    mmap.insert({7, "xyz"});
    print("#4", mmap);
 
    // insert using initializer_list
    mmap.insert({{5, "one"}, {5, "two"}});
    print("#5", mmap);
 
    // insert using a pair of iterators
    mmap.clear();
    const auto il = {std::pair{1, "ä"}, {2, "ё"}, {2, "ö"}, {3, "ü"}};
    mmap.insert(il.begin(), il.end());
    print("#6", mmap);
}
```

Output:

```text
#1 {5,def} {3,baz} {2,foo} {2,bar} {1,abc}
#2 {5,def} {5,pqr} {3,baz} {2,foo} {2,bar} {1,abc}
#3 {6,uvw} {5,def} {5,pqr} {3,baz} {2,foo} {2,bar} {1,abc}
#4 {7,xyz} {6,uvw} {5,def} {5,pqr} {3,baz} {2,foo} {2,bar} {1,abc}
#5 {7,xyz} {6,uvw} {5,def} {5,pqr} {5,one} {5,two} {3,baz} {2,foo} {2,bar} {1,abc}
#6 {3,ü} {2,ё} {2,ö} {1,ä}
```

### Defect reports

{/* source:cpp-container-multimap-insert:0024 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-multimap-insert:0025 */}
<DefectReportList>
  <DefectReport kind="lwg" id={233} standard="C++98">
    <PublishedBehavior>`pos` was just a hint, it could be totally ignored</PublishedBehavior>
    <CorrectedBehavior>the insertion is required to be as close as possible to the position just prior to `pos`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={264} standard="C++98">
    <PublishedBehavior>the complexity of overload (5) was required to be linear if the range [`first, last)` is sorted according to `Compare`</PublishedBehavior>
    <CorrectedBehavior>removed the linear requirement in this special case</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={371} standard="C++98">
    <PublishedBehavior>the order of equivalent elements was not guaranteed to be preserved</PublishedBehavior>
    <CorrectedBehavior>required to be preserved</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2005} standard="C++11">
    <PublishedBehavior>overloads (3,6) were poorly described</PublishedBehavior>
    <CorrectedBehavior>improved the description</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-multimap-insert:0026 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/container/multimap/emplace) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs element in-place (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_hint`](/docs/cpp/library/container/multimap/emplace_hint) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs elements in-place using a hint (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`inserter`](/docs/cpp/library/iterator/inserter)</DescriptionTerm>
    <DescriptionBody>creates a [`std::insert_iterator`](/docs/cpp/library/iterator/insert_iterator) of type inferred from the argument (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>