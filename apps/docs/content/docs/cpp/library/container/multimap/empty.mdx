---
title: "std::multimap<Key,T,Compare,Allocator>::empty"
description: "Checks if the container has no elements, i.e. whether begin() == end()."
source_url: "https://en.cppreference.com/w/cpp/container/multimap/empty"
language: "C++"
---

{/* source:cpp-container-multimap-empty:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`bool empty() const;`} />
</DeclarationDoc>

{/* source:cpp-container-multimap-empty:0001 */}
<InlineRevision since="C++11" />
Checks if the container has no elements, i.e. whether `begin() == end()`.

### Parameters

{/* source:cpp-container-multimap-empty:0002 */}
(none)

### Return value

{/* source:cpp-container-multimap-empty:0003 */}
`true` if the container is empty, `false` otherwise.

### Complexity

{/* source:cpp-container-multimap-empty:0004 */}
Constant.

### Example

{/* source:cpp-container-multimap-empty:0005 */}
The following code uses `empty` to check if a `std::multimap<int, int>` contains any elements:

```cpp
#include <iostream>
#include <map>
#include <utility>
 
int main()
{
    std::multimap<int,int> numbers;
    std::cout << std::boolalpha;
    std::cout << "Initially, numbers.empty(): " << numbers.empty() << '\n';
 
    numbers.emplace(42, 13);
    numbers.insert(std::make_pair(13317, 123));
    std::cout << "After adding elements, numbers.empty(): " << numbers.empty() << '\n';
}
```

Output:

```text
Initially, numbers.empty(): true
After adding elements, numbers.empty(): false
```

### See also

{/* source:cpp-container-multimap-empty:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/container/multimap/size)</DescriptionTerm>
    <DescriptionBody>returns the number of elements (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/iterator/empty) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>checks whether the container is empty (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
