---
title: "std::multimap<Key,T,Compare,Allocator>::merge"
description: "Attempts to extract (\"splice\") each element in source and insert it into *this using the comparison object of *this."
source_url: "https://en.cppreference.com/w/cpp/container/multimap/merge"
language: "C++"
---

{/* source:cpp-container-multimap-merge:0000 */}
<DeclarationDoc>
  <Declaration id="1" since="C++17" language="cpp" code={`template< class C2 >
void merge( std::map<Key, T, C2, Allocator>& source );`} />
  <Declaration id="2" since="C++17" language="cpp" code={`template< class C2 >
void merge( std::map<Key, T, C2, Allocator>&& source );`} />
  <Declaration id="3" since="C++17" language="cpp" code={`template< class C2 >
void merge( std::multimap<Key, T, C2, Allocator>& source );`} />
  <Declaration id="4" since="C++17" language="cpp" code={`template< class C2 >
void merge( std::multimap<Key, T, C2, Allocator>&& source );`} />
</DeclarationDoc>

{/* source:cpp-container-multimap-merge:0001 */}
Attempts to extract ("splice") each element in `source` and insert it into `*this` using the comparison object of `*this`.

{/* source:cpp-container-multimap-merge:0002 */}
No elements are copied or moved, only the internal pointers of the container nodes are repointed. All pointers and references to the transferred elements remain valid, but now refer into `*this`, not into `source`.

{/* source:cpp-container-multimap-merge:0003 */}
The behavior is undefined if `get_allocator() != source.get_allocator()`.

### Parameters

{/* source:cpp-container-multimap-merge:0004 */}
<ParameterList>
  <Parameter name="source">compatible container to transfer the nodes from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-multimap-merge:0005 */}
(none)

### Exceptions

{/* source:cpp-container-multimap-merge:0006 */}
Does not throw unless comparison throws.

### Complexity

{/* source:cpp-container-multimap-merge:0007 */}
N * log(size() + N)), where N is `source.size()`.

### Example

{/* source:cpp-container-multimap-merge:0008 */}
```cpp
#include <iostream>
#include <map>
#include <string>
 
int main()
{
    std::multimap<int, std::string> ma{{1, "apple"}, {5, "pear"}, {10, "banana"}};
    std::multimap<int, std::string> mb{{2, "zorro"}, {4, "batman"}, {5, "X"}, {8, "alpaca"}};
    std::multimap<int, std::string> u;
    u.merge(ma);
    std::cout << "ma.size(): " << ma.size() << '\n';
    u.merge(mb);
    std::cout << "mb.size(): " << mb.size() << '\n';
 
    for (auto const& kv : u)
        std::cout << kv.first << ", " << kv.second << '\n';
}
```

Output:

```text
ma.size(): 0
mb.size(): 0
1, apple
2, zorro
4, batman
5, pear
5, X
8, alpaca
10, banana
```

### See also

{/* source:cpp-container-multimap-merge:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`extract`](/docs/cpp/library/container/multimap/extract)</DescriptionTerm>
    <DescriptionBody>extracts nodes from the container (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert`](/docs/cpp/library/container/multimap/insert)</DescriptionTerm>
    <DescriptionBody>inserts elements <InlineRevision since="C++17">or nodes</InlineRevision> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
