---
title: "std::multimap<Key,T,Compare,Allocator>::begin, std::multimap<Key,T,Compare,Allocator>::cbegin"
description: "Returns an iterator to the first element of the multimap."
source_url: "https://en.cppreference.com/w/cpp/container/multimap/begin"
language: "C++"
---

{/* source:cpp-container-multimap-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`iterator begin();`} />
  <Declaration id="2" language="cpp" code={`const_iterator begin() const;`} />
  <Declaration id="3" since="C++11" language="cpp" code={`const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-multimap-begin:0001 */}
<InlineRevision since="C++11" /> <InlineRevision since="C++11" />
Returns an iterator to the first element of the multimap.

{/* source:cpp-container-multimap-begin:0002 */}
If the multimap is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/multimap/end).

### Parameters

{/* source:cpp-container-multimap-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-multimap-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-multimap-begin:0005 */}
Constant.

### Notes

{/* source:cpp-container-multimap-begin:0006 */}
libc++ backports `cbegin()` to C++98 mode.

### Example

{/* source:cpp-container-multimap-begin:0007 */}
```cpp
#include <algorithm>
#include <cassert>
#include <cstddef>
#include <iostream>
#include <map>
#include <string>
 
int main()
{
    auto show_node = [](const auto& node, char ending = '\n')
    {
        std::cout << "{ " << node.first << ", " << node.second << " }" << ending;
    };
 
    std::multimap<std::size_t, std::string> mmap;
    assert(mmap.begin() == mmap.end());   // OK
    assert(mmap.cbegin() == mmap.cend()); // OK
 
    mmap.insert({ sizeof(long), "LONG" });
    show_node(*(mmap.cbegin()));
    assert(mmap.begin() != mmap.end());   // OK
    assert(mmap.cbegin() != mmap.cend()); // OK
    mmap.begin()->second = "long";
    show_node(*(mmap.cbegin()));
 
    mmap.insert({ sizeof(int), "int" });
    show_node(*mmap.cbegin());
 
    mmap.insert({ sizeof(short), "short" });
    show_node(*mmap.cbegin());
 
    mmap.insert({ sizeof(char), "char" });
    show_node(*mmap.cbegin());
 
    mmap.insert({{ sizeof(float), "float" }, { sizeof(double), "double"}});
 
    std::cout << "mmap = { ";
    std::for_each(mmap.cbegin(), mmap.cend(), [&](const auto& n) { show_node(n, ' '); });
    std::cout << "};\n";
}
```

Possible output:

```text
{ 8, LONG }
{ 8, long }
{ 4, int }
{ 2, short }
{ 1, char }
mmap = { { 1, char } { 2, short } { 4, int } { 4, float } { 8, long } { 8, double } };
```

### See also

{/* source:cpp-container-multimap-begin:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/multimap/end) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
