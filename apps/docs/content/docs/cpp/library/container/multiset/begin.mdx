---
title: "std::multiset<Key,Compare,Allocator>::begin, std::multiset<Key,Compare,Allocator>::cbegin"
description: "Returns an iterator to the first element of the multiset."
source_url: "https://en.cppreference.com/w/cpp/container/multiset/begin"
language: "C++"
---

{/* source:cpp-container-multiset-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`iterator begin();`} />
  <Declaration id="2" language="cpp" code={`const_iterator begin() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cbegin() const noexcept;`} />
  <DeclarationDescription>
    {/* source:cpp-container-multiset-begin:0001 */}
    1. <InlineRevision since="C++11" /> Returns an iterator to the first element of the multiset.

    {/* source:cpp-container-multiset-begin:0002 */}
    2. <InlineRevision since="C++11" /> If the multiset is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/multiset/end).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-multiset-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-multiset-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-multiset-begin:0005 */}
Constant.

### Notes

{/* source:cpp-container-multiset-begin:0006 */}
Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

{/* source:cpp-container-multiset-begin:0007 */}
libc++ backports `cbegin()` to C++98 mode.

### Example

{/* source:cpp-container-multiset-begin:0008 */}
```cpp
#include <iostream>
#include <iterator>
#include <set>
#include <string>
 
int main()
{
    const std::multiset<std::string> words =
    {
        "some", "not", "sorted", "words",
        "will", "come", "out", "sorted",
    };
 
    for (auto it = words.begin(); it != words.end(); )
    {
        auto count = words.count(*it);
        std::cout << *it << ":\t" << count << '\n';
        std::advance(it, count); // all count elements have equivalent keys
    }
}
```

Output:

```text
come:	1
not:	1
out:	1
some:	1
sorted:	2
will:	1
words:	1
```

### See also

{/* source:cpp-container-multiset-begin:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/multiset/end) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
