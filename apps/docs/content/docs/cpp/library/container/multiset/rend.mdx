---
title: "std::multiset<Key,Compare,Allocator>::rend, std::multiset<Key,Compare,Allocator>::crend"
description: "Returns a reverse iterator to the element following the last element of the reversed multiset."
source_url: "https://en.cppreference.com/w/cpp/container/multiset/rend"
language: "C++"
---

{/* source:cpp-container-multiset-rend:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`reverse_iterator rend();`} />
  <Declaration id="2" language="cpp" code={`const_reverse_iterator rend() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_reverse_iterator crend() const noexcept;`} />
  <DeclarationDescription>
    {/* source:cpp-container-multiset-rend:0001 */}
    1. <InlineRevision since="C++11" /> Returns a reverse iterator to the element following the last element of the reversed multiset. It corresponds to the element preceding the first element of the non-reversed multiset. This element acts as a placeholder, attempting to access it results in undefined behavior.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-multiset-rend:0002 */}
(none)

### Return value

{/* source:cpp-container-multiset-rend:0003 */}
Reverse iterator to the element following the last element.

### Complexity

{/* source:cpp-container-multiset-rend:0004 */}
Constant.

### Notes

{/* source:cpp-container-multiset-rend:0005 */}
Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

{/* source:cpp-container-multiset-rend:0006 */}
libc++ backports `crend()` to C++98 mode.

### Example

{/* source:cpp-container-multiset-rend:0007 */}
```cpp
#include <iostream>
#include <set>
 
int main()
{
    std::multiset<unsigned> rep{1, 2, 3, 4, 1, 2, 3, 4};
 
    for (auto it = rep.crbegin(); it != rep.crend(); ++it)
    {
        for (auto n = *it; n > 0; --n)
            std::cout << "⏼" << ' ';
        std::cout << '\n';
    }
}
```

Output:

```text
⏼ ⏼ ⏼ ⏼
⏼ ⏼ ⏼ ⏼
⏼ ⏼ ⏼
⏼ ⏼ ⏼
⏼ ⏼
⏼ ⏼
⏼
⏼
```

### See also

{/* source:cpp-container-multiset-rend:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/container/multiset/rbegin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/iterator/rend) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse end iterator for a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
