---
title: "std::swap(std::queue)"
description: "Specializes the std::swap algorithm for std::queue."
source_url: "https://en.cppreference.com/w/cpp/container/queue/swap2"
language: "C++"
---

{/* source:cpp-container-queue-swap2:0000 */}
<DeclarationDoc>
Defined in header [`<queue>`](/docs/cpp/library/headers/queue)

  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class T, class Container >
void swap( std::queue<T, Container>& lhs,

           std::queue<T, Container>& rhs );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class T, class Container >
void swap( std::queue<T, Container>& lhs,
           std::queue<T, Container>& rhs )

           noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-container-queue-swap2:0001 */}
<Revision since="C++17">
This overload participates in overload resolution only if `std::is_swappable_v<Container>` is `true`.
</Revision>

### Parameters

{/* source:cpp-container-queue-swap2:0002 */}
<ParameterList>
  <Parameter name="lhs, rhs">containers whose contents to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-queue-swap2:0003 */}
(none)

### Complexity

{/* source:cpp-container-queue-swap2:0004 */}
Same as swapping the underlying containers.

### Exceptions

{/* source:cpp-container-queue-swap2:0005 */}
<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(lhs.swap(rhs)))`
</Revision>

### Notes

{/* source:cpp-container-queue-swap2:0006 */}
Although the overloads of [`std::swap`](/docs/cpp/library/algorithm/swap) for container adaptors are introduced in C++11, container adaptors can already be swapped by [`std::swap`](/docs/cpp/library/algorithm/swap) in C++98. Such calls to [`std::swap`](/docs/cpp/library/algorithm/swap) usually have linear time complexity, but better complexity may be provided.

### Example

{/* source:cpp-container-queue-swap2:0007 */}
```cpp
#include <algorithm>
#include <iostream>
#include <queue>

int main()
{
    std::queue<int> alice;
    std::queue<int> bob;

    auto print = [](const auto& title, const auto& cont)
    {
        std::cout << title << " size=" << cont.size();
        std::cout << " front=" << cont.front();
        std::cout << " back=" << cont.back() << '\n';
    };

    for (int i = 1; i < 4; ++i)
        alice.push(i);
    for (int i = 7; i < 11; ++i)
        bob.push(i);

    // Print state before swap
    print("Alice:", alice);
    print("Bobby:", bob);

    std::cout << "-- SWAP\n";
    std::swap(alice, bob);

    // Print state after swap
    print("Alice:", alice);
    print("Bobby:", bob);
}
```

Output:

```text
Alice: size=3 front=1 back=3
Bobby: size=4 front=7 back=10
-- SWAP
Alice: size=4 front=7 back=10
Bobby: size=3 front=1 back=3
```

### See also

{/* source:cpp-container-queue-swap2:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/container/queue/swap) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>swaps the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
