---
title: "std::vector<T,Allocator>::begin, std::vector<T,Allocator>::cbegin"
description: "Returns an iterator to the first element of the vector."
source_url: "https://en.cppreference.com/w/cpp/container/vector/begin"
language: "C++"
---

{/* source:cpp-container-vector-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`iterator begin();`} />
  <Declaration id="2" language="cpp" code={`const_iterator begin() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cbegin() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-container-vector-begin:0001 */}
    1. <InlineRevision since="C++11" /> <InlineRevision since="C++20" /> Returns an iterator to the first element of the vector.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-container-vector-begin:0002 */}
If the vector is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/vector/end).

### Parameters

{/* source:cpp-container-vector-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-vector-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-vector-begin:0005 */}
Constant.

### Notes

{/* source:cpp-container-vector-begin:0006 */}
`libc++` backports `cbegin()` to C++98 mode.

### Example

{/* source:cpp-container-vector-begin:0007 */}
```cpp
#include <algorithm>
#include <iostream>
#include <numeric>
#include <string>
#include <vector>

int main()
{
    std::vector<int> nums{1, 2, 4, 8, 16};
    std::vector<std::string> fruits{"orange", "apple", "raspberry"};
    std::vector<char> empty;

    // Print vector.
    std::for_each(nums.begin(), nums.end(), [](const int n) { std::cout << n << ' '; });
    std::cout << '\n';

    // Sums all integers in the vector nums (if any), printing only the result.
    std::cout << "Sum of nums: "
              << std::accumulate(nums.begin(), nums.end(), 0) << '\n';

    // Prints the first fruit in the vector fruits, checking if there is any.
    if (!fruits.empty())
        std::cout << "First fruit: " << *fruits.begin() << '\n';

    if (empty.begin() == empty.end())
        std::cout << "vector 'empty' is indeed empty.\n";
}
```

Output:

```text
1 2 4 8 16
Sum of nums: 31
First fruit: orange
vector 'empty' is indeed empty.
```

### See also

{/* source:cpp-container-vector-begin:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/vector/end) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
