---
title: "std::vector<T,Allocator>::empty"
description: "Checks if the container has no elements, i.e. whether begin() == end()."
source_url: "https://en.cppreference.com/w/cpp/container/vector/empty"
language: "C++"
---

{/* source:cpp-container-vector-empty:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`bool empty() const;`} />

  <DeclarationDescription>
    {/* source:cpp-container-vector-empty:0001 */}
    1. <InlineRevision since="C++11" /> <InlineRevision since="C++20" /> Checks if the container has no elements, i.e. whether `begin() == end()`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-vector-empty:0002 */}
(none)

### Return value

{/* source:cpp-container-vector-empty:0003 */}
`true` if the container is empty, `false` otherwise.

### Complexity

{/* source:cpp-container-vector-empty:0004 */}
Constant.

### Example

{/* source:cpp-container-vector-empty:0005 */}
```cpp
#include <iostream>
#include <vector>

int main()
{
    std::cout << std::boolalpha;

    std::vector<int> numbers;
    std::cout << "Initially, numbers.empty(): " << numbers.empty() << '\n';

    numbers.push_back(42);
    std::cout << "After adding elements, numbers.empty(): " << numbers.empty() << '\n';
}
```

Output:

```text
Initially, numbers.empty(): true
After adding elements, numbers.empty(): false
```

### See also

{/* source:cpp-container-vector-empty:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/container/vector/size)</DescriptionTerm>
    <DescriptionBody>returns the number of elements (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/iterator/empty) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>checks whether the container is empty (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
