---
title: "std::vector<T,Allocator>::~vector"
description: "A destructor. Destructs the vector. The destructors of the elements are called and the used storage is deallocated."
source_url: "https://en.cppreference.com/w/cpp/container/vector/~vector"
language: "C++"
---

{/* source:cpp-container-vector-vector:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++98" code={`~vector();`} />

  <DeclarationDescription>
    {/* source:cpp-container-vector-vector:0001 */}
    1. <InlineRevision since="C++20" /> A [destructor](/docs/cpp/language/classes/destructor). Destructs the vector. The destructors of the elements are called and the used storage is deallocated. Note, that if the elements are pointers, the pointed-to objects are not destroyed.
  </DeclarationDescription>
</DeclarationDoc>

### Complexity

{/* source:cpp-container-vector-vector:0002 */}
Linear in the size of the vector.
