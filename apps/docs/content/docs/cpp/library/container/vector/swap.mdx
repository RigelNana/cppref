---
title: "std::vector<T,Allocator>::swap"
description: "Exchanges the contents and capacity of the container with those of other."
source_url: "https://en.cppreference.com/w/cpp/container/vector/swap"
language: "C++"
---

{/* source:cpp-container-vector-swap:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" until="C++17" code={`void swap( vector& other );`} />
  <Declaration id="2" language="cpp" since="C++17" until="C++20" code={`void swap( vector& other ) noexcept(/* see below */);`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr void swap( vector& other ) noexcept(/* see below */);`} />

  <DeclarationDescription>
    {/* source:cpp-container-vector-swap:0001 */}
    1. Exchanges the contents and capacity of the container with those of `other`. Does not invoke any move, copy, or swap operations on individual elements.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-container-vector-swap:0002 */}
All iterators and references remain valid. The [`end()`](/docs/cpp/library/container/vector/end) iterator is invalidated.

{/* source:cpp-container-vector-swap:0003 */}
<Revision since="C++11">
If `std::allocator_traits<allocator_type>::propagate_on_container_swap::value` is `true`, then the allocators are exchanged using an unqualified call to non-member `swap`. Otherwise, they are not swapped (and if `get_allocator() != other.get_allocator()`, the behavior is undefined).
</Revision>

### Parameters

{/* source:cpp-container-vector-swap:0004 */}
<ParameterList>
  <Parameter name="other">container to exchange the contents with</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-container-vector-swap:0005 */}
<Revision until="C++17">(none)</Revision>

<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(std::allocator_traits<Allocator>::propagate_on_container_swap::value || std::allocator_traits<Allocator>::is_always_equal::value)`
</Revision>

### Complexity

{/* source:cpp-container-vector-swap:0006 */}
Constant.

### Example

{/* source:cpp-container-vector-swap:0007 */}
```cpp
#include <iostream>
#include <vector>

template<class Os, class Co>
Os& operator<<(Os& os, const Co& co)
{
    os << '{';
    for (auto const& i : co)
        os << ' ' << i;
    return os << " } ";
}

int main()
{
    std::vector<int> a1{1, 2, 3}, a2{4, 5};

    auto it1 = std::next(a1.begin());
    auto it2 = std::next(a2.begin());

    int& ref1 = a1.front();
    int& ref2 = a2.front();

    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
    a1.swap(a2);
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';

    // Note that after swap the iterators and references stay associated with their
    // original elements, e.g. it1 that pointed to an element in 'a1' with value 2
    // still points to the same element, though this element was moved into 'a2'.
}
```

Output:

```text
{ 1 2 3 } { 4 5 } 2 5 1 4
{ 4 5 } { 1 2 3 } 2 5 1 4
```

### Defect reports

{/* source:cpp-container-vector-swap:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-vector-swap:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={341} standard="C++98">
    <PublishedBehavior>the capacities of `std::vector` could not be swapped</PublishedBehavior>
    <CorrectedBehavior>they are also swapped</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-vector-swap:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::swap(std::vector)`](/docs/cpp/library/container/vector/swap2)</DescriptionTerm>
    <DescriptionBody>specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
