---
title: "std::vector<T,Allocator>::clear"
description: "Erases all elements from the container."
source_url: "https://en.cppreference.com/w/cpp/container/vector/clear"
language: "C++"
---

{/* source:cpp-container-vector-clear:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`void clear();`} />

  <DeclarationDescription>
    {/* source:cpp-container-vector-clear:0001 */}
    1. <InlineRevision since="C++11" /> <InlineRevision since="C++20" /> Erases all elements from the container. After this call, [`size()`](/docs/cpp/library/container/vector/size) returns zero.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-container-vector-clear:0002 */}
Invalidates any references, pointers, and iterators referring to contained elements. Any past-the-end iterators are also invalidated.

{/* source:cpp-container-vector-clear:0003 */}
Leaves the [`capacity()`](/docs/cpp/library/container/vector/capacity) of the `vector` unchanged (Note: the standard's restriction on the changes to capacity is in the specification of [`reserve()`](/docs/cpp/library/container/vector/reserve), see [SO](https://stackoverflow.com/a/18467916)).

### Parameters

{/* source:cpp-container-vector-clear:0004 */}
(none)

### Return value

{/* source:cpp-container-vector-clear:0005 */}
(none)

### Complexity

{/* source:cpp-container-vector-clear:0006 */}
Linear in the size of the container, i.e., the number of elements.

### Example

{/* source:cpp-container-vector-clear:0007 */}
```cpp
#include <iostream>
#include <string_view>
#include <vector>

void print_info(std::string_view rem, const std::vector<int>& v)
{
    std::cout << rem << "{ ";
    for (const auto& value : v)
        std::cout << value << ' ';
    std::cout << "}\n";
    std::cout << "Size=" << v.size() << ", Capacity=" << v.capacity() << '\n';
}

int main()
{
    std::vector<int> container{1, 2, 3};
    print_info("Before clear: ", container);
    container.clear();
    print_info("After clear: ", container);
}
```

Output:

```text
Before clear: { 1 2 3 }
Size=3, Capacity=3
After clear: { }
Size=0, Capacity=3
```

### Defect reports

{/* source:cpp-container-vector-clear:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-vector-clear:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2231} standard="C++11">
    <PublishedBehavior>complexity guarantee was mistakenly omitted in C++11</PublishedBehavior>
    <CorrectedBehavior>complexity reaffirmed as linear</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-vector-clear:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`erase`](/docs/cpp/library/container/vector/erase)</DescriptionTerm>
    <DescriptionBody>erases elements (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
