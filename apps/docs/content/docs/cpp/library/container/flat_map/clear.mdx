---
title: "std::flat_map<Key,T,Compare,KeyContainer,MappedContainer>::clear"
description: "Erases all elements from the container adaptor."
source_url: "https://en.cppreference.com/w/cpp/container/flat_map/clear"
language: "C++"
---

{/* source:cpp-container-flat-map-clear:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`void clear() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-flat-map-clear:0001 */}
Erases all elements from the container adaptor. After this call, `size()` returns zero.

{/* source:cpp-container-flat-map-clear:0002 */}
Invalidates any references, pointers, and iterators referring to contained elements.

### Parameters

{/* source:cpp-container-flat-map-clear:0003 */}
(none)

### Return value

{/* source:cpp-container-flat-map-clear:0004 */}
(none)

### Complexity

{/* source:cpp-container-flat-map-clear:0005 */}
Linear in the size of the container adaptor, i.e., the number of elements.

### Example

{/* source:cpp-container-flat-map-clear:0006 */}
```cpp
#include <iostream>
#include <string_view>
#include <flat_map>
 
void print_info(std::string_view rem, const std::flat_map<int, char>& v)
{
    std::cout << rem << "{ ";
    for (const auto& [key, value] : v)
        std::cout << '[' << key << "]:" << value << ' ';
    std::cout << "}\n";
    std::cout << "Size=" << v.size() << '\n';
}
 
int main()
{
    std::flat_map<int, char> container{{1, 'x'}, {2, 'y'}, {3, 'z'}};
    print_info("Before clear: ", container);
    container.clear();
    print_info("After clear: ", container);
}
```

Output:

```text
Before clear: { [1]:x [2]:y [3]:z }
Size=3
After clear: { }
Size=0
```

### See also

{/* source:cpp-container-flat-map-clear:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`erase`](/docs/cpp/library/container/flat_map/erase)</DescriptionTerm>
    <DescriptionBody>erases elements (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

