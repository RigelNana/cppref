---
title: "deduction guides for std::flat_map"
description: "These deduction guides are provided for to allow deduction from:"
source_url: "https://en.cppreference.com/w/cpp/container/flat_map/deduction_guides"
language: "C++"
---

{/* source:cpp-container-flat-map-deduction-guides:0000 */}
<DeclarationDoc>
Defined in header [`<flat_map>`](/docs/cpp/library/headers/flat_map)

  <Declaration id="1" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer,
          class Compare = std::less<typename KeyContainer::value_type> >
flat_map( KeyContainer, MappedContainer, Compare = Compare() )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,

                Compare, KeyContainer, MappedContainer>;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer, class Allocator >
flat_map( KeyContainer, MappedContainer, Allocator )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,
                std::less<typename KeyContainer::value_type>,

                KeyContainer, MappedContainer>;`} />
  <Declaration id="3" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer,
          class Compare, class Allocator >
flat_map( KeyContainer, MappedContainer, Compare, Allocator )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,

                Compare, KeyContainer, MappedContainer>;`} />
  <Declaration id="4" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer,
          class Compare = std::less<typename KeyContainer::value_type> >
flat_map( std::sorted_unique_t, KeyContainer, MappedContainer,
          Compare = Compare() )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,

                Compare, KeyContainer, MappedContainer>;`} />
  <Declaration id="5" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer, class Allocator >
flat_map( std::sorted_unique_t, KeyContainer, MappedContainer,
          Allocator )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,
                std::less<typename KeyContainer::value_type>,

                KeyContainer, MappedContainer>;`} />
  <Declaration id="6" language="cpp" since="C++23" code={`template< class KeyContainer, class MappedContainer,
          class Compare, class Allocator>
flat_map( std::sorted_unique_t, KeyContainer, MappedContainer,
          Compare, Allocator )
    -> flat_map<typename KeyContainer::value_type,
                typename MappedContainer::value_type,

                Compare, KeyContainer, MappedContainer>;`} />
  <Declaration id="7" language="cpp" since="C++23" code={`template< class InputIter,
          class Compare = std::less</*iter-key-type*/<InputIter>> >
flat_map( InputIter, InputIter, Compare = Compare() )
    -> flat_map</*iter-key-type*/<InputIter>,

                /*iter-mapped-type*/<InputIter>, Compare>;`} />
  <Declaration id="8" language="cpp" since="C++23" code={`template< class InputIter,
          class Compare = std::less</*iter-key-type*/<InputIter>> >
flat_map( std::sorted_unique_t, InputIter, InputIter,
          Compare = Compare() )
    -> flat_map</*iter-key-type*/<InputIter>,

                /*iter-mapped-type*/<InputIter>, Compare>;`} />
  <Declaration id="9" language="cpp" since="C++23" code={`template< ranges::input_range R,
          class Compare = std::less</*range-key-type*/<R>>,
          class Allocator = allocator<byte> >
flat_map( std::from_range_t, R&&, Compare = Compare(),
          Allocator = Allocator() )
    -> flat_map</*range-key-type*/<R>, /*range-mapped-type*/<R>, Compare,
                std::vector</*range-key-type*/<R>,
                            /*alloc-rebind*/<Allocator,
                                             /*range-key-type*/<R>>>,
                std::vector</*range-mapped-type*/<R>,
                            /*alloc-rebind*/<Allocator,

                                             /*range-mapped-type*/<R>>>>;`} />
  <Declaration id="10" language="cpp" since="C++23" code={`template< ranges::input_range R, class Allocator >
flat_map( std::from_range_t, R&&, Allocator )
    -> flat_map</*range-key-type*/<R>, /*range-mapped-type*/<R>,
                std::less</*range-key-type*/<R>>,
                std::vector</*range-key-type*/<R>,
                            /*alloc-rebind*/<Allocator,
                                             /*range-key-type*/<R>>>,
                std::vector</*range-mapped-type*/<R>,
                            /*alloc-rebind*/<Allocator,

                                             /*range-mapped-type*/<R>>>>;`} />
  <Declaration id="11" language="cpp" since="C++23" code={`template< class Key, class T, class Compare = std::less<Key> >
flat_map( std::initializer_list<pair<Key, T>>, Compare = Compare() )

    -> flat_map<Key, T, Compare>;`} />
  <Declaration id="12" language="cpp" since="C++23" code={`template< class Key, class T, class Compare = std::less<Key> >
flat_map( std::sorted_unique_t, std::initializer_list<pair<Key, T>>,
          Compare = Compare() )

    -> flat_map<Key, T, Compare>;`} />
</DeclarationDoc>

{/* source:cpp-container-flat-map-deduction-guides:0001 */}
These [deduction guides](/docs/cpp/language/templates/ctad) are provided for to allow deduction from:

{/* source:cpp-container-flat-map-deduction-guides:0002 */}
- 1) A key-container, a mapped-container and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0003 */}
- 2) A key-container, a mapped-container and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0004 */}
- 3) A key-container, a mapped-container, a comparator and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0005 */}
- 4) The `std::sorted_unique_t` tag, a key-container, a mapped-container and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0006 */}
- 5) The `std::sorted_unique_t` tag, a key-container, a mapped-container and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0007 */}
- 6) The `std::sorted_unique_t` tag, a key-container, a mapped-container, a comparator and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0008 */}
- 7) An iterator range and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0009 */}
- 8) The `std::sorted_unique_t` tag, an iterator range and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0010 */}
- 9) The `std::from_range_t` tag, an [`input_range`](/docs/cpp/library/ranges/input_range) range, a comparator and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0011 */}
- 10) The `std::from_range_t` tag, an [`input_range`](/docs/cpp/library/ranges/input_range) range and an allocator.

{/* source:cpp-container-flat-map-deduction-guides:0012 */}
- 11) The [`std::initializer_list`](/docs/cpp/library/utility/initializer_list) and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0013 */}
- 12) The `std::sorted_unique_t` tag, the [`std::initializer_list`](/docs/cpp/library/utility/initializer_list) and a comparator.

{/* source:cpp-container-flat-map-deduction-guides:0014 */}
These overloads participate in overload resolution only if `InputIt` satisfies [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator), `Alloc` satisfies [`Allocator`](/docs/cpp/library/named_req/Allocator), and `Comp` does not satisfy [`Allocator`](/docs/cpp/library/named_req/Allocator).

{/* source:cpp-container-flat-map-deduction-guides:0015 */}
Note: the extent to which the library determines that a type does not satisfy [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator) is unspecified, except that as a minimum integral types do not qualify as input iterators. Likewise, the extent to which it determines that a type does not satisfy [`Allocator`](/docs/cpp/library/named_req/Allocator) is unspecified, except that as a minimum the member type `Alloc::value_type` must exist and the expression `std::declval<Alloc&>().allocate(std::size_t{})` must be well-formed when treated as an unevaluated operand.

### Example

{/* source:cpp-container-flat-map-deduction-guides:0016 */}
This section is incomplete

Reason: no example

