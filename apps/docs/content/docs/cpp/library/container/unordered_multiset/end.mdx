---
title: "std::unordered_multiset<Key,Hash,KeyEqual,Allocator>::end, std::unordered_multiset<Key,Hash,KeyEqual,Allocator>::cend"
description: "Returns an iterator to the element following the last element of the unordered_multiset."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_multiset/end"
language: "C++"
---

{/* source:cpp-container-unordered-multiset-end:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`iterator end() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`const_iterator end() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cend() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-multiset-end:0001 */}
Returns an iterator to the element following the last element of the `unordered_multiset`.

{/* source:cpp-container-unordered-multiset-end:0002 */}
This element acts as a placeholder; attempting to access it results in undefined behavior.

### Parameters

{/* source:cpp-container-unordered-multiset-end:0003 */}
(none)

### Return value

{/* source:cpp-container-unordered-multiset-end:0004 */}
Iterator to the element following the last element.

### Complexity

{/* source:cpp-container-unordered-multiset-end:0005 */}
Constant.

### Notes

{/* source:cpp-container-unordered-multiset-end:0006 */}
Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

### Example

{/* source:cpp-container-unordered-multiset-end:0007 */}
```cpp
#include <iostream>
#include <iterator>
#include <string>
#include <unordered_set>

int main()
{
    const std::unordered_multiset<std::string> words =
    {
        "some", "words", "to", "count",
        "count", "these", "words"
    };

    for (auto it = words.begin(); it != words.end(); )
    {
        auto count = words.count(*it);
        std::cout << *it << ":\t" << count << '\n';
        std::advance(it, count); // all count elements have equivalent keys
    }
}
```

Possible output:

```text
some:   1
words:  2
to:     1
count:  2
these:  1
```

### See also

{/* source:cpp-container-unordered-multiset-end:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`begin` `cbegin`](/docs/cpp/library/container/unordered_multiset/begin)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`end` `cend`](/docs/cpp/library/iterator/end) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
