---
title: "std::unordered_multiset<Key,Hash,KeyEqual,Allocator>::count"
description: "Returns the number of elements with key that compares equal to the specified argument key."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_multiset/count"
language: "C++"
---

{/* source:cpp-container-unordered-multiset-count:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`size_type count( const Key& key ) const;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< class K >
size_type count( const K& x ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-multiset-count:0001 */}
1. Returns the number of elements with key that compares equal to the specified argument `key`.

{/* source:cpp-container-unordered-multiset-count:0002 */}
2. Returns the number of elements with key that compares equivalent to the specified argument `x`. This overload participates in overload resolution only if `Hash::is_transparent` and `KeyEqual::is_transparent` are valid and each denotes a type. This assumes that such `Hash` is callable with both `K` and `Key` type, and that the `KeyEqual` is transparent, which, together, allows calling this function without constructing an instance of `Key`.

### Parameters

{/* source:cpp-container-unordered-multiset-count:0003 */}
<ParameterList>
  <Parameter name="key">key value of the elements to count</Parameter>
  <Parameter name="x">a value of any type that can be transparently compared with a key</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-unordered-multiset-count:0004 */}
1. Number of elements with key `key`.

{/* source:cpp-container-unordered-multiset-count:0005 */}
2. Number of elements with key that compares equivalent to `x`.

### Complexity

{/* source:cpp-container-unordered-multiset-count:0006 */}
linear in the number of elements with key `key` on average, worst case linear in the size of the container.

### Notes

{/* source:cpp-container-unordered-multiset-count:0007 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_generic_unordered_lookup`](/docs/cpp/library/feature_test#cpp_lib_generic_unordered_lookup) | [`201811L`](/docs/cpp/library/compiler_support/20#cpp_lib_generic_unordered_lookup_201811L) | (C++20) | Heterogeneous comparison lookup in [unordered associative containers](/docs/cpp/library/container#Unordered_associative_containers), overload (2) |

### Example

{/* source:cpp-container-unordered-multiset-count:0008 */}
```cpp
#include <algorithm>
#include <iostream>
#include <unordered_set>

int main()
{
    std::unordered_multiset set{2, 7, 1, 8, 2, 8, 1, 8, 2, 8};

    std::cout << "The set is:\n";
    for (int e : set)
        std::cout << e << ' ';

    const auto [min, max] = std::ranges::minmax(set);

    std::cout << "\nNumbers [" << min << ".." << max << "] frequency:\n";

    for (int i{min}; i <= max; ++i)
        std::cout << i << ':' << set.count(i) << "; ";
    std::cout << '\n';
}
```

Possible output:

```text
The set is:
8 8 8 8 1 1 7 2 2 2
Numbers [1..8] frequency:
1:2; 2:3; 3:0; 4:0; 5:0; 6:0; 7:1; 8:4;
```

### See also

{/* source:cpp-container-unordered-multiset-count:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/container/unordered_multiset/find)</DescriptionTerm>
    <DescriptionBody>finds element with specific key (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`contains`](/docs/cpp/library/container/unordered_multiset/contains) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>checks if the container contains element with specific key (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`equal_range`](/docs/cpp/library/container/unordered_multiset/equal_range)</DescriptionTerm>
    <DescriptionBody>returns range of elements matching a specific key (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
