---
title: "std::flat_multimap<Key,T,Compare,KeyContainer,MappedContainer>::extract"
description: "Extracts adapted containers c."
source_url: "https://en.cppreference.com/w/cpp/container/flat_multimap/extract"
language: "C++"
---

{/* source:cpp-container-flat-multimap-extract:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`containers extract() &&;`} />
</DeclarationDoc>

{/* source:cpp-container-flat-multimap-extract:0001 */}
Extracts adapted containers [`c`](/docs/cpp/library/container/flat_multimap#Member_objects). Equivalent to `return std::move(c);`.

{/* source:cpp-container-flat-multimap-extract:0002 */}
After this operation `*this` is empty, even if an exception is thrown.

### Parameters

{/* source:cpp-container-flat-multimap-extract:0003 */}
(none)

### Return value

{/* source:cpp-container-flat-multimap-extract:0004 */}
`std::move(c)`.

### Complexity

{/* source:cpp-container-flat-multimap-extract:0005 */}
Constant.

### Example

{/* source:cpp-container-flat-multimap-extract:0006 */}
```cpp
#include <cassert>
#include <flat_map>
#include <print>
#include <type_traits>
#include <vector>
 
int main()
{
    std::flat_multimap<int, double> map{{1, 1.1}, {2, 2.2}, {3, 3.3}};
    const auto size = map.size();
 
    auto c = map.extract();
    assert(c.keys.size() == size);
    assert(c.values.size() == size);
    assert(map.empty());
    assert(map.keys().empty());
    assert(map.values().empty());
 
    // The default keys and values containers are std::vector:
    static_assert(std::is_same_v<decltype(c.keys), std::vector<int>>);
    static_assert(std::is_same_v<decltype(c.value), std::vector<int>>);
 
    std::println("keys: {}", c.keys);
    std::println("values: {}", c.values);
}
```

Output:

```text
keys: [1, 2, 3]
values: [1.1, 2.2, 3.3]
```

### See also

{/* source:cpp-container-flat-multimap-extract:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`replace`](/docs/cpp/library/container/flat_multimap/replace)</DescriptionTerm>
    <DescriptionBody>replaces the underlying containers (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
