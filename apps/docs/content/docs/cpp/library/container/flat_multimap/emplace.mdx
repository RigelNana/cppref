---
title: "std::flat_multimap<Key,T,Compare,KeyContainer,MappedContainer>::emplace"
description: "Inserts a new element into the container constructed in-place with the given args."
source_url: "https://en.cppreference.com/w/cpp/container/flat_multimap/emplace"
language: "C++"
---

{/* source:cpp-container-flat-multimap-emplace:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`template< class... Args >
iterator emplace( Args&&... args );`} />
</DeclarationDoc>

{/* source:cpp-container-flat-multimap-emplace:0001 */}
Inserts a new element into the container constructed in-place with the given `args`.

{/* source:cpp-container-flat-multimap-emplace:0002 */}
Initializes an object `t` of type `std::pair<key_type, mapped_type>` with `std::forward<Args>(args)...`; if the map already contains an element whose key is equivalent to `t.first`, `*this` is unchanged. Otherwise, equivalent to:

{/* source:cpp-container-flat-multimap-emplace:0003 */}
```cpp
auto key_it = ranges::upper_bound(c.keys, t.first, compare);
auto value_it = c.values.begin() + std::distance(c.keys.begin(), key_it);
c.keys.insert(key_it, std::move(t.first));
c.values.insert(value_it, std::move(t.second));
```

{/* source:cpp-container-flat-multimap-emplace:0004 */}
This overload participates in overload resolution only if `std::is_constructible_v<std::pair<key_type, mapped_type>, Args...>` is `true`.

{/* source:cpp-container-flat-multimap-emplace:0005 */}
Careful use of `emplace` allows the new element to be constructed while avoiding unnecessary copy or move operations.

{/* source:cpp-container-flat-multimap-emplace:0006 */}
Information on iterator invalidation is copied from [here](/docs/Template:cpp/container/note-iterator-invalidation).

### Parameters

{/* source:cpp-container-flat-multimap-emplace:0007 */}
<ParameterList>
  <Parameter name="args">arguments to forward to the constructor of the element</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-flat-multimap-emplace:0008 */}
An iterator to the inserted element.

### Exceptions

{/* source:cpp-container-flat-multimap-emplace:0009 */}
If an exception is thrown for any reason, this function has no effect ([strong exception safety guarantee](/docs/cpp/language/exceptions#Exception_safety)).

### Complexity

{/* source:cpp-container-flat-multimap-emplace:0010 */}
Linear in the size of the container

### Example

{/* source:cpp-container-flat-multimap-emplace:0011 */}
```cpp
#include <iostream>
#include <string>
#include <utility>
#include <flat_map>
 
int main()
{
    std::flat_multimap<std::string, std::string> m;
 
    // uses pair's move constructor
    m.emplace(std::make_pair(std::string("a"), std::string("a")));
 
    // uses pair's converting move constructor
    m.emplace(std::make_pair("b", "abcd"));
 
    // uses pair's template constructor
    m.emplace("d", "ddd");
 
    // emplace with duplicate key 
    m.emplace("d", "DDD");
 
    // uses pair's piecewise constructor
    m.emplace(std::piecewise_construct,
              std::forward_as_tuple("c"),
              std::forward_as_tuple(10, 'c'));
 
    for (const auto& p : m)
        std::cout << p.first << " => " << p.second << '\n';
}
```

Output:

```text
a => a
b => abcd
c => cccccccccc
d => ddd
d => DDD
```

### See also

{/* source:cpp-container-flat-multimap-emplace:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_hint`](/docs/cpp/library/container/flat_multimap/emplace_hint)</DescriptionTerm>
    <DescriptionBody>constructs elements in-place using a hint (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_emplace`](https://en.cppreference.com/mwiki/index.php?title=cpp/container/flat_multimap/try_emplace&action=edit&redlink=1)</DescriptionTerm>
    <DescriptionBody>inserts in-place if the key does not exist, does nothing if the key exists (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert`](/docs/cpp/library/container/flat_multimap/insert)</DescriptionTerm>
    <DescriptionBody>inserts elements (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
