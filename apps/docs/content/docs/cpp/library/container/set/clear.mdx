---
title: "std::set<Key,Compare,Allocator>::clear"
description: "Erases all elements from the container. After this call, size() returns zero."
source_url: "https://en.cppreference.com/w/cpp/container/set/clear"
language: "C++"
---


{/* source:cpp-container-set-clear:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`void clear();`} />
</DeclarationDoc>

{/* source:cpp-container-set-clear:0001 */}
<InlineRevision since="C++11" /> Erases all elements from the container. After this call, [`size()`](/docs/cpp/library/container/set/size) returns zero.

{/* source:cpp-container-set-clear:0002 */}
Invalidates any references, pointers, and iterators referring to contained elements. Any past-the-end iterator remains valid.

### Parameters

{/* source:cpp-container-set-clear:0003 */}
(none)

### Return value

{/* source:cpp-container-set-clear:0004 */}
(none)

### Complexity

{/* source:cpp-container-set-clear:0005 */}
Linear in the size of the container, i.e., the number of elements.

### Example

{/* source:cpp-container-set-clear:0006 */}
```cpp
#include <iostream>
#include <string_view>
#include <set>
 
void print_info(std::string_view rem, const std::set<int>& v)
{
    std::cout << rem << "{ ";
    for (const auto& value : v)
        std::cout << value << ' ';
    std::cout << "}\n";
    std::cout << "Size=" << v.size() << '\n';
}
 
int main()
{
    std::set<int> container{1, 2, 3};
    print_info("Before clear: ", container);
    container.clear();
    print_info("After clear: ", container);
}
```

Output:

```text
Before clear: { 1 2 3 }
Size=3
After clear: { }
Size=0
```

### Defect reports

{/* source:cpp-container-set-clear:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-set-clear:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={224} standard="C++98">
    <PublishedBehavior>the complexity was `log(size()) + N`, but `N` was not defined</PublishedBehavior>
    <CorrectedBehavior>corrected to 'linear in `size()`'</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-set-clear:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`erase`](/docs/cpp/library/container/set/erase)</DescriptionTerm>
    <DescriptionBody>erases elements (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
