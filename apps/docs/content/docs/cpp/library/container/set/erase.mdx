---
title: "std::set<Key,Compare,Allocator>::erase"
description: "Removes specified elements from the container."
source_url: "https://en.cppreference.com/w/cpp/container/set/erase"
language: "C++"
---

{/* source:cpp-container-set-erase:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" until="C++23" code={`iterator erase( iterator pos );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`iterator erase( iterator pos )
    requires(!std::same_as<iterator, const_iterator>);`} />
  <Declaration id="2" language="cpp" since="C++11" code={`iterator erase( const_iterator pos );`} />
  <Declaration id="3" language="cpp" until="C++11" code={`iterator erase( iterator first, iterator last );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`iterator erase( const_iterator first, const_iterator last );`} />
  <Declaration id="4" language="cpp" code={`size_type erase( const Key& key );`} />
  <Declaration id="5" language="cpp" since="C++23" code={`template< class K >
size_type erase( K&& x );`} />
</DeclarationDoc>

{/* source:cpp-container-set-erase:0001 */}
Removes specified elements from the container.

{/* source:cpp-container-set-erase:0002 */}
1,2. Removes the element at `pos`. <InlineRevision since="C++11">Only one overload is provided if `iterator` and `const_iterator` are the same type.</InlineRevision>

{/* source:cpp-container-set-erase:0003 */}
3. Removes the elements in the range `[first, last)`, which must be a valid range in `*this`.

{/* source:cpp-container-set-erase:0004 */}
4. Removes the element (if one exists) with the key equivalent to `key`.

{/* source:cpp-container-set-erase:0005 */}
5. Removes all elements with key that compares equivalent to the value `x`. This overload participates in overload resolution only if the qualified-id `Compare::is_transparent` is valid and denotes a type, and neither `iterator` nor `const_iterator` is implicitly convertible from `K`. It allows calling this function without constructing an instance of `Key`.

{/* source:cpp-container-set-erase:0006 */}
References and iterators to the erased elements are invalidated. Other references and iterators are not affected.

{/* source:cpp-container-set-erase:0007 */}
The iterator `pos` must be valid and dereferenceable. Thus the [`end()`](/docs/cpp/library/container/set/end) iterator (which is valid, but is not dereferenceable) cannot be used as a value for `pos`.

## Parameters

{/* source:cpp-container-set-erase:0008 */}
<ParameterList>
  <Parameter name="pos">iterator to the element to remove</Parameter>
  <Parameter name="first, last">range of elements to remove</Parameter>
  <Parameter name="key">key value of the elements to remove</Parameter>
  <Parameter name="x">a value of any type that can be transparently compared with a key denoting the elements to remove</Parameter>
</ParameterList>

## Return value

{/* source:cpp-container-set-erase:0009 */}
1-3. Iterator following the last removed element.

{/* source:cpp-container-set-erase:0010 */}
4. Number of elements removed (0 or 1).

{/* source:cpp-container-set-erase:0011 */}
5. Number of elements removed.

## Exceptions

{/* source:cpp-container-set-erase:0012 */}
1-3. Throws nothing.

{/* source:cpp-container-set-erase:0013 */}
4,5. Any exceptions thrown by the `Compare` object.

## Complexity

{/* source:cpp-container-set-erase:0014 */}
Given an instance `c` of `set`:

{/* source:cpp-container-set-erase:0015 */}
1,2. Amortized constant

{/* source:cpp-container-set-erase:0016 */}
3. `log(c.size()) + std::distance(first, last)`

{/* source:cpp-container-set-erase:0017 */}
4. `log(c.size()) + c.count(key)`

{/* source:cpp-container-set-erase:0018 */}
5. `log(c.size()) + c.count(x)`

## Notes

{/* source:cpp-container-set-erase:0019 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_associative_heterogeneous_erasure`](/docs/cpp/library/feature_test#cpp_lib_associative_heterogeneous_erasure) | [`202110L`](/docs/cpp/library/compiler_support/23#cpp_lib_associative_heterogeneous_erasure_202110L) | (C++23) | Heterogeneous erasure in [`associative containers`](/docs/cpp/library/container#Associative_containers) and [`unordered associative containers`](/docs/cpp/library/container#Unordered_associative_containers); overload ([5](/docs/cpp/library/container/set/erase#Version_5)) |

## Example

{/* source:cpp-container-set-erase:0020 */}
```cpp
#include <set>
#include <iostream>

int main()
{
    std::set<int> c = {1, 2, 3, 4, 1, 2, 3, 4};

    auto print = [&c]
    {
        std::cout << "c = { ";
        for (int n : c)
            std::cout << n << ' ';
        std::cout << "}\n";
    };
    print();

    std::cout << "Erase all odd numbers:\n";
    for (auto it = c.begin(); it != c.end();)
    {
        if (*it % 2 != 0)
            it = c.erase(it);
        else
            ++it;
    }
    print();

    std::cout << "Erase 1, erased count: " << c.erase(1) << '\n';
    std::cout << "Erase 2, erased count: " << c.erase(2) << '\n';
    std::cout << "Erase 2, erased count: " << c.erase(2) << '\n';
    print();
}
```

Output:

```text
c = { 1 2 3 4 }
Erase all odd numbers:
c = { 2 4 }
Erase 1, erased count: 0
Erase 2, erased count: 1
Erase 2, erased count: 0
c = { 4 }
```

## Defect reports

{/* source:cpp-container-set-erase:0021 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-set-erase:0022 */}
<DefectReportList>
  <DefectReport kind="lwg" id={130} standard="C++98"><PublishedBehavior>the return type of overloads (1) and (3) was `void` (it is not consistent with the `erase()` requirement on sequence containers)</PublishedBehavior><CorrectedBehavior>corrected to `iterator`</CorrectedBehavior></DefectReport>
  <DefectReport kind="lwg" id={2059} standard="C++11"><PublishedBehavior>replacing overload (1) with overload (2) introduced new ambiguity</PublishedBehavior><CorrectedBehavior>added overload (1) back</CorrectedBehavior></DefectReport>
</DefectReportList>

## See also

{/* source:cpp-container-set-erase:0023 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`clear`](/docs/cpp/library/container/set/clear)</DescriptionTerm>
    <DescriptionBody>clears the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
