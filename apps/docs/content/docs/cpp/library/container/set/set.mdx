---
title: "std::set<Key,Compare,Allocator>::set"
description: "Constructs new container from a variety of data sources and optionally using user supplied allocator alloc or comparison function object comp."
source_url: "https://en.cppreference.com/w/cpp/container/set/set"
language: "C++"
---

{/* source:cpp-container-set-set:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" until="C++11" code={`set();`} />
  <Declaration id="1" language="cpp" since="C++11" code={`set()
    : set(Compare()) {}`} />
  <Declaration id="2" language="cpp" code={`explicit set( const Compare& comp,
              const Allocator& alloc = Allocator() );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`explicit set( const Allocator& alloc );`} />
  <Declaration id="4" language="cpp" since="C++98" code={`template< class InputIt >
set( InputIt first, InputIt last,
     const Compare& comp = Compare(),

     const Allocator& alloc = Allocator() );`} />
  <Declaration id="5" language="cpp" since="C++14" code={`template< class InputIt >
set( InputIt first, InputIt last,
     const Allocator& alloc )

    : set(first, last, Compare(), alloc) {}`} />
  <Declaration id="6" language="cpp" since="C++98" code={`set( const set& other );`} />
  <Declaration id="7" language="cpp" since="C++11" code={`set( const set& other, const Allocator& alloc );`} />
  <Declaration id="8" language="cpp" since="C++11" code={`set( set&& other );`} />
  <Declaration id="9" language="cpp" since="C++11" code={`set( set&& other, const Allocator& alloc );`} />
  <Declaration id="10" language="cpp" since="C++11" code={`set( std::initializer_list<value_type> init,
     const Compare& comp = Compare(),

     const Allocator& alloc = Allocator() );`} />
  <Declaration id="11" language="cpp" since="C++14" code={`set( std::initializer_list<value_type> init,
     const Allocator& alloc )

    : set(init, Compare(), alloc) {}`} />
  <Declaration id="12" language="cpp" since="C++23" code={`template< container-compatible-range<value_type> R >
set( std::from_range_t, R&& rg,
     const Compare& comp = Compare(),

     const Allocator& alloc = Allocator() );`} />
  <Declaration id="13" language="cpp" since="C++23" code={`template< container-compatible-range<value_type> R >
set( std::from_range_t, R&& rg,
     const Allocator& alloc )

    : set(std::from_range, std::forward<R>(rg), Compare(), alloc) {}`} />
</DeclarationDoc>

{/* source:cpp-container-set-set:0001 */}
Constructs new container from a variety of data sources and optionally using user supplied allocator `alloc` or comparison function object `comp`.

{/* source:cpp-container-set-set:0002 */}
1-3. Constructs an empty container.

{/* source:cpp-container-set-set:0003 */}
4,5. Constructs the container with the contents of the range `[first, last)`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-set-set:0004 */}
If `[first, last)` is not a [valid range](/docs/cpp/library/iterator#Ranges), the behavior is undefined.

{/* source:cpp-container-set-set:0005 */}
6,7. [Copy constructor](/docs/cpp/language/classes/copy_constructor). Constructs the container with the copy of the contents of `other`.

<Revision since="C++11">
If `alloc` is not provided, allocator is obtained by calling `std::allocator_traits<allocator_type>::select_on_container_copy_construction(other.get_allocator())`.
</Revision>

<Revision since="C++23">
During [class template argument deduction](/docs/cpp/language/templates/ctad), only the first argument contributes to the deduction of the container's `Allocator` template parameter.
</Revision>

{/* source:cpp-container-set-set:0006 */}
8,9. [Move constructor](/docs/cpp/language/classes/move_constructor). Constructs the container with the contents of `other` using move semantics. If `alloc` is not provided, allocator is obtained by move-construction from the allocator belonging to `other`.

<Revision since="C++23">
During [class template argument deduction](/docs/cpp/language/templates/ctad), only the first argument contributes to the deduction of the container's `Allocator` template parameter.
</Revision>

{/* source:cpp-container-set-set:0007 */}
10,11. [Initializer-list constructor](/docs/cpp/language/initialization/list_initialization). Constructs the container with the contents of the initializer list `init`.If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-set-set:0008 */}
12,13. Constructs the container with the contents of `rg`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

## Parameters

{/* source:cpp-container-set-set:0009 */}
<ParameterList>
  <Parameter name="alloc">allocator to use for all memory allocations of this container</Parameter>
  <Parameter name="comp">comparison function object to use for all comparisons of keys</Parameter>
  <Parameter name="first, last">the range to copy the elements from</Parameter>
  <Parameter name="other">another container to be used as source to initialize the elements of the container with</Parameter>
  <Parameter name="init">initializer list to initialize the elements of the container with</Parameter>
  <Parameter name="rg">a [`container compatible range`](/docs/cpp/library/ranges/to#container_compatible_range), that is, an [`input_range`](/docs/cpp/library/ranges/input_range) whose elements are convertible to [`value_type`](/docs/cpp/library/container/set#Member_types)</Parameter>
</ParameterList>

#### Type requirements

- `InputIt` must meet the requirements of [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator).
- `Compare` must meet the requirements of [`Compare`](/docs/cpp/library/named_req/Compare).
- `Allocator` must meet the requirements of [`Allocator`](/docs/cpp/library/named_req/Allocator).

## Complexity

{/* source:cpp-container-set-set:0010 */}
1-3. Constant.

{/* source:cpp-container-set-set:0011 */}
4,5. N·log(N) where `N` is `std::distance(first, last)` in general, linear in `N` if `[first, last)` is already sorted by `value_comp()`.

{/* source:cpp-container-set-set:0012 */}
6,7. Linear in size of `other`.

{/* source:cpp-container-set-set:0013 */}
8,9. Constant. If `alloc` is given and `alloc != other.get_allocator()`, then linear.

{/* source:cpp-container-set-set:0014 */}
10,11. N·log(N) where `N` is `init.size()` in general, linear in `N` if `init` is already sorted by `value_comp()`.

{/* source:cpp-container-set-set:0015 */}
12,13. N·log(N) where `N` is `ranges::distance(rg)` in general, linear in `N` if `rg` is already sorted by `value_comp()`.

## Exceptions

{/* source:cpp-container-set-set:0016 */}
Calls to `Allocator::allocate` may throw.

## Notes

{/* source:cpp-container-set-set:0017 */}
After container move construction (overload (8,9)), references, pointers, and iterators (other than the end iterator) to `other` remain valid, but refer to elements that are now in `*this`. The current standard makes this guarantee via the blanket statement in [[container.reqmts]/67](https://eel.is/c++draft/container.reqmts#67), and a more direct guarantee is under consideration via [LWG issue 2321](https://cplusplus.github.io/LWG/issue2321).

{/* source:cpp-container-set-set:0018 */}
Although not formally required until C++23, some implementations has already put the template parameter `Allocator` into [non-deduced contexts](/docs/cpp/language/templates/template_argument_deduction#Non-deduced_contexts) in earlier modes.

{/* source:cpp-container-set-set:0019 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_containers_ranges`](/docs/cpp/library/feature_test#cpp_lib_containers_ranges) | [`202202L`](/docs/cpp/library/compiler_support/23#cpp_lib_containers_ranges_202202L) | (C++23) | [`Ranges-aware`](/docs/cpp/library/ranges/to#container_compatible_range) construction and insertion; overloads ([12,13](/docs/cpp/library/container/set/set#Version_12)) |

## Example

{/* source:cpp-container-set-set:0020 */}
```cpp
#include <cmath>
#include <iostream>
#include <set>
#include <string>

struct Point { double x, y; };

struct PointCmp
{
    bool operator()(const Point& lhs, const Point& rhs) const
    {
        return std::hypot(lhs.x, lhs.y) < std::hypot(rhs.x, rhs.y);
    }
};

std::ostream& operator<<(std::ostream& os, Point pt)
{
    return os << '(' << pt.x << ',' << pt.x << ')';
}

void println(auto rem, const auto& seq)
{
    std::cout << rem << '{';
    for (auto n{seq.size()}; const auto& elm : seq)
        std::cout << elm << (--n ? ", " : "");
    std::cout << "}\n";
}

int main()
{
    // (1) Default constructor
    std::set<std::string> a;
    a.insert("horse");
    a.insert("cat");
    a.insert("dog");
    println("1) a: ", a);

    // (4) Range constructor
    std::set<std::string> b(a.find("dog"), a.end());
    println("2) b: ", b);

    // (6) Copy constructor
    std::set<std::string> c(a);
    c.insert("another horse");
    println("3) c: ", c);

    // (8) Move constructor
    std::set<std::string> d(std::move(a));
    println("4) d: ", d);
    println("5) a: ", a);

    // (10) Initializer list constructor
    std::set<std::string> e{"one", "two", "three", "five", "eight"};
    println("6) e: ", e);

    // Custom comparison
    std::set<Point, PointCmp> f = {{2, 5}, {3, 4}, {1, 1}};
    f.insert({1, -1}); // This fails because the magnitude of (1,-1) equals (1,1)
    println("7) f: ", f);

    // (12) Range constructor
    const auto w = {"Eurybia", "Theia", "Rhea", "Aura", "Mnemosyne", "Mnemosyne"};
#if __cpp_lib_containers_ranges
    std::set<std::string> g(std::from_range, w); // overload (12)
#else
    std::set<std::string> g(w.begin(), w.end()); // fallback to (4)
#endif
    println("8) g: ", g);
}
```

Possible output:

```text
1) a: {cat, dog, horse}
2) b: {dog, horse}
3) c: {another horse, cat, dog, horse}
4) d: {cat, dog, horse}
5) a: {}
6) e: {eight, five, one, three, two}
7) f: {(1,1), (3,3), (2,2)}
8) g: {Aura, Eurybia, Mnemosyne, Rhea, Theia}
```

## Defect reports

{/* source:cpp-container-set-set:0021 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-set-set:0022 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2076} standard="C++11"><PublishedBehavior>overload (4) conditionally required `Key` to be [`CopyInsertable`](/docs/cpp/library/named_req/CopyInsertable) into `*this`</PublishedBehavior><CorrectedBehavior>not required</CorrectedBehavior></DefectReport>
  <DefectReport kind="lwg" id={2193} standard="C++11"><PublishedBehavior>the default constructor was explicit</PublishedBehavior><CorrectedBehavior>made non-explicit</CorrectedBehavior></DefectReport>
</DefectReportList>

## See also

{/* source:cpp-container-set-set:0023 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/container/set/operator%3D)</DescriptionTerm>
    <DescriptionBody>assigns values to the container (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
