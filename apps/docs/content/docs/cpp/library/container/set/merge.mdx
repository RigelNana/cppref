---
title: "std::set<Key,Compare,Allocator>::merge"
description: "Attempts to extract (\"splice\") each element in source and insert it into *this using the comparison object of *this."
source_url: "https://en.cppreference.com/w/cpp/container/set/merge"
language: "C++"
---


{/* source:cpp-container-set-merge:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`template< class C2 >
void merge( std::set<Key, C2, Allocator>& source );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class C2 >
void merge( std::set<Key, C2, Allocator>&& source );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`template< class C2 >
void merge( std::multiset<Key, C2, Allocator>& source );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class C2 >
void merge( std::multiset<Key, C2, Allocator>&& source );`} />
</DeclarationDoc>

{/* source:cpp-container-set-merge:0001 */}
Attempts to extract ("splice") each element in `source` and insert it into `*this` using the comparison object of `*this`. If there is an element in `*this` with key equivalent to the key of an element from `source`, then that element is not extracted from `source`. No elements are copied or moved, only the internal pointers of the container nodes are repointed. All pointers and references to the transferred elements remain valid, but now refer into `*this`, not into `source`.

{/* source:cpp-container-set-merge:0002 */}
The behavior is undefined if `get_allocator() != source.get_allocator()`.

### Parameters

{/* source:cpp-container-set-merge:0003 */}
<ParameterList>
  <Parameter name="source">compatible container to transfer the nodes from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-set-merge:0004 */}
(none)

### Exceptions

{/* source:cpp-container-set-merge:0005 */}
Does not throw unless comparison throws.

### Complexity

{/* source:cpp-container-set-merge:0006 */}
N * log(size() + N)), where N is `source.size()`.

### Example

{/* source:cpp-container-set-merge:0007 */}
```cpp
#include <iostream>
#include <set>
 
// print out a container
template<class Os, class K>
Os& operator<<(Os& os, const std::set<K>& v)
{
    os << '[' << v.size() << "] {";
    bool o{};
    for (const auto& e : v)
        os << (o ? ", " : (o = 1, " ")) << e;
    return os << " }\n";
}
 
int main()
{
    std::set<char>
        p{'C', 'B', 'B', 'A'}, 
        q{'E', 'D', 'E', 'C'};
 
    std::cout << "p: " << p << "q: " << q;
 
    p.merge(q);
 
    std::cout << "p.merge(q);\n" << "p: " << p << "q: " << q;
}
```

Output:

```text
p: [3] { A, B, C }
q: [3] { C, D, E }
p.merge(q);
p: [5] { A, B, C, D, E }
q: [1] { C }
```

### See also

{/* source:cpp-container-set-merge:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`extract`](/docs/cpp/library/container/set/extract) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>extracts nodes from the container (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert`](/docs/cpp/library/container/set/insert)</DescriptionTerm>
    <DescriptionBody>inserts elements <InlineRevision since="C++17">or nodes</InlineRevision> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
