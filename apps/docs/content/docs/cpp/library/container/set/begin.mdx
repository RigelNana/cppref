---
title: "std::set<Key,Compare,Allocator>::begin, std::set<Key,Compare,Allocator>::cbegin"
description: "Returns an iterator to the first element of the set."
source_url: "https://en.cppreference.com/w/cpp/container/set/begin"
language: "C++"
---

{/* source:cpp-container-set-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`iterator begin();`} />
  <Declaration id="2" language="cpp" code={`const_iterator begin() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-set-begin:0001 */}
<InlineRevision since="C++11" /> Returns an iterator to the first element of the `set`.

{/* source:cpp-container-set-begin:0002 */}
If the `set` is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/set/end).

## Parameters

{/* source:cpp-container-set-begin:0003 */}
(none)

## Return value

{/* source:cpp-container-set-begin:0004 */}
Iterator to the first element.

## Complexity

{/* source:cpp-container-set-begin:0005 */}
Constant.

## Notes

{/* source:cpp-container-set-begin:0006 */}
Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

{/* source:cpp-container-set-begin:0007 */}
libc++ backports `cbegin()` to C++98 mode.

## Example

{/* source:cpp-container-set-begin:0008 */}
```cpp
#include <algorithm>
#include <iostream>
#include <set>

int main()
{
    std::set<int> set{3, 1, 4, 1, 5, 9, 2, 6, 5};
    std::for_each(set.cbegin(), set.cend(), [](int x)
    {
        std::cout << x << ' ';
    });
    std::cout << '\n';
}
```

Output:

```text
1 2 3 4 5 6 9
```

## See also

{/* source:cpp-container-set-begin:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/set/end) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
