---
title: "std::stack<T,Container>::top"
description: "Returns reference to the top element in the stack."
source_url: "https://en.cppreference.com/w/cpp/container/stack/top"
language: "C++"
---

{/* source:cpp-container-stack-top:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`reference top();`} />
  <Declaration id="2" language="cpp" code={`const_reference top() const;`} />
</DeclarationDoc>

{/* source:cpp-container-stack-top:0001 */}
Returns reference to the top element in the stack. This is the most recently pushed element. This element will be removed on a call to [`pop()`](/docs/cpp/library/container/stack/pop). Equivalent to: [`c`](/docs/cpp/library/container/stack#Member_objects)`.back()`.

### Parameters

{/* source:cpp-container-stack-top:0002 */}
(none)

### Return value

{/* source:cpp-container-stack-top:0003 */}
Reference to the last element.

### Complexity

{/* source:cpp-container-stack-top:0004 */}
Constant.

### Example

{/* source:cpp-container-stack-top:0005 */}
```cpp
#include <iostream>
#include <stack>

void reportStackSize(const std::stack<int>& s)
{
    std::cout << s.size() << " elements on stack\n";
}

void reportStackTop(const std::stack<int>& s)
{
    // Leaves element on stack
    std::cout << "Top element: " << s.top() << '\n';
}

int main()
{
    std::stack<int> s;
    s.push(2);
    s.push(6);
    s.push(51);

    reportStackSize(s);
    reportStackTop(s);

    reportStackSize(s);
    s.pop();

    reportStackSize(s);
    reportStackTop(s);
}
```

Output:

```text
3 elements on stack
Top element: 51
3 elements on stack
2 elements on stack
Top element: 6
```

### See also

{/* source:cpp-container-stack-top:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`push`](/docs/cpp/library/container/stack/push)</DescriptionTerm>
    <DescriptionBody>inserts element at the top (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`pop`](/docs/cpp/library/container/stack/pop)</DescriptionTerm>
    <DescriptionBody>removes the top element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
