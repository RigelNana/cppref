---
title: "std::stack<T,Container>::emplace"
description: "Pushes a new element on top of the stack."
source_url: "https://en.cppreference.com/w/cpp/container/stack/emplace"
language: "C++"
---

{/* source:cpp-container-stack-emplace:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class... Args >
void emplace( Args&&... args );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class... Args >
decltype(auto) emplace( Args&&... args );`} />
</DeclarationDoc>

{/* source:cpp-container-stack-emplace:0001 */}
Pushes a new element on top of the stack. The element is constructed in-place, i.e. no copy or move operations are performed. The constructor of the element is called with exactly the same arguments as supplied to the function.

{/* source:cpp-container-stack-emplace:0002 */}
Effectively calls `c.emplace_back(std::forward<Args>(args)...);`.

### Parameters

{/* source:cpp-container-stack-emplace:0003 */}
<ParameterList>
  <Parameter name="args">arguments to forward to the constructor of the element</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-stack-emplace:0004 */}
<Revision until="C++17">
(none)
</Revision>
<Revision since="C++17">
The value or reference, if any, returned by the above call to `Container::emplace_back`.
</Revision>

### Complexity

{/* source:cpp-container-stack-emplace:0005 */}
Identical to the complexity of `Container::emplace_back`.

### Example

{/* source:cpp-container-stack-emplace:0006 */}
```cpp
#include <iostream>
#include <stack>

struct S
{
    int id;

    S(int i, double d, std::string s) : id{i}
    {
        std::cout << "S::S(" << i << ", " << d << ", \"" << s << "\");\n";
    }
};

int main()
{
    std::stack<S> stack;
    const S& s = stack.emplace(42, 3.14, "C++"); // for return value C++17 required
    std::cout << "id = " << s.id << '\n';
}
```

Output:

```text
S::S(42, 3.14, "C++")
id = 42
```

### Defect reports

{/* source:cpp-container-stack-emplace:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-stack-emplace:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2783} standard="C++17">
    <PublishedBehavior>`emplace` returned `reference`, breaking compatibility with pre-C++17 containers</PublishedBehavior>
    <CorrectedBehavior>returns `decltype(auto)`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-stack-emplace:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`push`](/docs/cpp/library/container/stack/push)</DescriptionTerm>
    <DescriptionBody>inserts element at the top (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`pop`](/docs/cpp/library/container/stack/pop)</DescriptionTerm>
    <DescriptionBody>removes the top element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
