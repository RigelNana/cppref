---
title: "std::vector<bool,Allocator>::swap"
description: "Swaps the contents of x and y."
source_url: "https://en.cppreference.com/w/cpp/container/vector_bool/swap"
language: "C++"
---
{/* source:cpp-container-vector-bool-swap:0000 */}
<DeclarationDoc>
Defined in header [`<vector>`](/docs/cpp/library/headers/vector)

  <Declaration language="cpp" code={`static void swap( reference x, reference y );`} />
</DeclarationDoc>

{/* source:cpp-container-vector-bool-swap:0001 */}
<InlineRevision since="C++20" /> Swaps the contents of `x` and `y` as if by `bool b = x; x = y; y = b;`.

### Parameters

{/* source:cpp-container-vector-bool-swap:0002 */}
<ParameterList>
  <Parameter name="x">[`std::vector<bool>`](/docs/cpp/library/container/vector_bool)::[`reference`](/docs/cpp/library/container/vector_bool/reference) value to swap with `y`</Parameter>
  <Parameter name="y">[`std::vector<bool>`](/docs/cpp/library/container/vector_bool)::[`reference`](/docs/cpp/library/container/vector_bool/reference) value to swap with `x`</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-vector-bool-swap:0003 */}
(none)

### Complexity

{/* source:cpp-container-vector-bool-swap:0004 */}
Constant.

### Example

{/* source:cpp-container-vector-bool-swap:0005 */}
```cpp
#include <iostream>
#include <vector>
 
void println(std::string_view fmt, std::vector<bool> const& vb = {})
{
    for (std::cout << fmt; bool const e : vb)
        std::cout << e << ' ';
    std::cout << '\n';
}
 
int main()
{
    println("swap elements of the same vector:");
    std::vector<bool> x{1, 0};
    println("before swap, x: ", x);
    x.swap(x[0], x[1]); // same as std::vector<bool>::swap(x[0], x[1]);
    println("after swap,  x: ", x);
 
    println("swap elements of two different vectors:");
    std::vector<bool> y{0, 0, 1};
    println("before swap, x: ", x);
    println("before swap, y: ", y);
    y.swap(x[0], y[2]); // same as std::vector<bool>::swap(x[0], y[2]);
    println("after swap,  x: ", x);
    println("after swap,  y: ", y);
}
```
Output:

```text
swap elements of the same vector:
before swap, x: 1 0 
after swap,  x: 0 1 
swap elements of two different vectors:
before swap, x: 0 1 
before swap, y: 0 0 1 
after swap,  x: 1 1 
after swap,  y: 0 0 0
```
### Defect reports

{/* source:cpp-container-vector-bool-swap:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-vector-bool-swap:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={814} standard="C++98">
    <PublishedBehavior>the description of this member function was missing</PublishedBehavior>
    <CorrectedBehavior>added</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-vector-bool-swap:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`reference`](/docs/cpp/library/container/vector_bool/reference)</DescriptionTerm>
    <DescriptionBody>proxy class representing a reference to a single `bool` (class)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/container/vector/swap)</DescriptionTerm>
    <DescriptionBody>swaps the contents (public member function of `std::vector<T,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::swap(std::vector)`](/docs/cpp/library/container/vector/swap2)</DescriptionTerm>
    <DescriptionBody>specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
