---
title: "std::unordered_set<Key,Hash,KeyEqual,Allocator>::begin, std::unordered_set<Key,Hash,KeyEqual,Allocator>::cbegin"
description: "Returns an iterator to the first element of the unordered_set."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/begin"
language: "C++"
---

{/* source:cpp-container-unordered-set-begin:0000 */}

<DeclarationDoc>
  <Declaration language="cpp" id="1" since="C++11" code={`iterator begin() noexcept;`} />
  <Declaration language="cpp" id="2" since="C++11" code={`const_iterator begin() const noexcept;`} />
  <Declaration language="cpp" id="3" since="C++11" code={`const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-begin:0001 */}

Returns an iterator to the first element of the unordered_set.

{/* source:cpp-container-unordered-set-begin:0002 */}

If the unordered_set is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/unordered_set/end).

### Parameters

{/* source:cpp-container-unordered-set-begin:0003 */}

(none)

### Return value

{/* source:cpp-container-unordered-set-begin:0004 */}

Iterator to the first element.

### Complexity

{/* source:cpp-container-unordered-set-begin:0005 */}

Constant.

### Notes

{/* source:cpp-container-unordered-set-begin:0006 */}

Because both `iterator` and `const_iterator` are constant iterators (and may in fact be the same type), it is not possible to mutate the elements of the container through an iterator returned by any of these member functions.

### Example

{/* source:cpp-container-unordered-set-begin:0007 */}

```cpp
#include <iostream>
#include <unordered_set>
 
struct Point { double x, y; };
 
int main()
{
    Point pts[3] = {{1, 0}, {2, 0}, {3, 0}};
 
    // points is a set containing the addresses of points
    std::unordered_set<Point*> points = { pts, pts + 1, pts + 2 };
 
    // Change each y-coordinate of (i, 0) from 0 into i^2 and print the point
    for (auto iter = points.begin(); iter != points.end(); ++iter)
    {
        (*iter)->y = ((*iter)->x) * ((*iter)->x); // iter is a pointer-to-Point*
        std::cout << "(" << (*iter)->x << ", " << (*iter)->y << ") ";
    }
    std::cout << '\n';
 
    // Now using the range-based for loop, we increase each y-coordinate by 10
    for (Point* i : points)
    {
        i->y += 10;
        std::cout << "(" << i->x << ", " << i->y << ") ";
    }
}
```

Possible output:

```text
(3, 9) (1, 1) (2, 4) 
(3, 19) (1, 11) (2, 14)
```

### See also

{/* source:cpp-container-unordered-set-begin:0008 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/unordered_set/end)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
