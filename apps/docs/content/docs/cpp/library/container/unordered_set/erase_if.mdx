---
title: "std::erase_if (std::unordered_set)"
description: "Erases all elements that satisfy the predicate pred from c."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/erase_if"
language: "C++"
---

{/* source:cpp-container-unordered-set-erase-if:0000 */}

Defined in header [`<unordered_set>`](/docs/cpp/library/headers/unordered_set)

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class Key, class Hash, class KeyEqual, class Alloc,
          class Pred >
std::unordered_set<Key, Hash, KeyEqual, Alloc>::size_type
    erase_if( std::unordered_set<Key, Hash, KeyEqual, Alloc>& c,

              Pred pred );`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-erase-if:0001 */}

Erases all elements that satisfy the predicate `pred` from `c`.

{/* source:cpp-container-unordered-set-erase-if:0002 */}

Equivalent to

{/* source:cpp-container-unordered-set-erase-if:0003 */}

```cpp
auto old_size = c.size();
for (auto first = c.begin(), last = c.end(); first != last;)
{
    if (pred(*first))
        first = c.erase(first);
    else
        ++first;
}
return old_size - c.size();
```

### Parameters

{/* source:cpp-container-unordered-set-erase-if:0004 */}

<ParameterList>
  <Parameter name="c">container from which to erase</Parameter>
  <Parameter name="pred">predicate that returns `true` if the element should be erased</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-unordered-set-erase-if:0005 */}

The number of erased elements.

### Complexity

{/* source:cpp-container-unordered-set-erase-if:0006 */}

Linear.

### Example

{/* source:cpp-container-unordered-set-erase-if:0007 */}

```cpp
#include <iostream>
#include <unordered_set>
 
void println(auto rem, auto const& container)
{
    std::cout << rem << '{';
    for (char sep[]{0, ' ', 0}; const auto& item : container)
        std::cout << sep << item, *sep = ',';
    std::cout << "}\n";
}
 
int main()
{
    std::unordered_set data{3, 3, 4, 5, 5, 6, 6, 7, 2, 1, 0};
    println("Original:\n", data);
 
    auto divisible_by_3 = [](auto const& x) { return (x % 3) == 0; };
 
    const auto count = std::erase_if(data, divisible_by_3);
 
    println("Erase all items divisible by 3:\n", data);
    std::cout << count << " items erased.\n";
}
```

Possible output:

```text
Original:
{0, 1, 2, 7, 6, 5, 4, 3}
Erase all items divisible by 3:
{1, 2, 7, 5, 4}
3 items erased.
```

### See also

{/* source:cpp-container-unordered-set-erase-if:0008 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`remove`, `remove_if`](/docs/cpp/library/algorithm/remove)</DescriptionTerm>
    <DescriptionBody>removes elements satisfying specific criteria (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::remove`, `ranges::remove_if`](/docs/cpp/library/algorithm/ranges/remove) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>removes elements satisfying specific criteria (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
