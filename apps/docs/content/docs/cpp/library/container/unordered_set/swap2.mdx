---
title: "std::swap(std::unordered_set)"
description: "Specializes the std::swap algorithm for std::unordered_set."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/swap2"
language: "C++"
---

{/* source:cpp-container-unordered-set-swap2:0000 */}
Defined in header [`<unordered_set>`](/docs/cpp/library/headers/unordered_set)

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class Key, class Hash, class KeyEqual, class Alloc >
void swap( std::unordered_set<Key, Hash, KeyEqual, Alloc>& lhs,

           std::unordered_set<Key, Hash, KeyEqual, Alloc>& rhs );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class Key, class Hash, class KeyEqual, class Alloc >
void swap( std::unordered_set<Key, Hash, KeyEqual, Alloc>& lhs,
           std::unordered_set<Key, Hash, KeyEqual, Alloc>& rhs )

               noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-swap2:0001 */}
Specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm for [`std::unordered_set`](/docs/cpp/library/container/unordered_set). Swaps the contents of `lhs` and `rhs`. Calls `lhs.swap(rhs)`.

### Parameters

{/* source:cpp-container-unordered-set-swap2:0002 */}
<ParameterList>
  <Parameter name="lhs, rhs">containers whose contents to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-unordered-set-swap2:0003 */}
(none)

### Complexity

{/* source:cpp-container-unordered-set-swap2:0004 */}
Constant.

### Exceptions

{/* source:cpp-container-unordered-set-swap2:0005 */}
<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(lhs.swap(rhs)))`
</Revision>

### Example

{/* source:cpp-container-unordered-set-swap2:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <unordered_set>
 
int main()
{
    std::unordered_set<int> alice{1, 2, 3};
    std::unordered_set<int> bob{7, 8, 9, 10};
 
    auto print = [](const int& n) { std::cout << ' ' << n; };
 
    // Print state before swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';
 
    std::cout << "-- SWAP\n";
    std::swap(alice, bob);
 
    // Print state after swap
    std::cout << "Alice:";
    std::for_each(alice.begin(), alice.end(), print);
    std::cout << "\nBobby:";
    std::for_each(bob.begin(), bob.end(), print);
    std::cout << '\n';
}
```

Possible output:

```text
Alice: 1 2 3
Bobby: 7 8 9 10
-- SWAP
Alice: 7 8 9 10
Bobby: 1 2 3
```

### See also

{/* source:cpp-container-unordered-set-swap2:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/container/unordered_set/swap)</DescriptionTerm>
    <DescriptionBody>swaps the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
