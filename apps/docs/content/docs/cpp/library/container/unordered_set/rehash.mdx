---
title: "std::unordered_set<Key,Hash,KeyEqual,Allocator>::rehash"
description: "Changes the number of buckets to a value n that is not less than count and satisfies n >= size() / max_load_factor()."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/rehash"
language: "C++"
---

{/* source:cpp-container-unordered-set-rehash:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void rehash( size_type count );`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-rehash:0001 */}
Changes the number of buckets to a value `n` that is not less than `count` and satisfies `n >= size() / max_load_factor()`, then rehashes the container, i.e. puts the elements into appropriate buckets considering that total number of buckets has changed.

### Parameters

{/* source:cpp-container-unordered-set-rehash:0002 */}
<ParameterList>
  <Parameter name="count">lower bound for the new number of buckets</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-unordered-set-rehash:0003 */}
(none)

### Complexity

{/* source:cpp-container-unordered-set-rehash:0004 */}
Average case linear in the size of the container, worst case quadratic.

### Notes

{/* source:cpp-container-unordered-set-rehash:0005 */}
`rehash(0)` may be used to force an unconditional rehash, such as after suspension of automatic rehashing by temporarily increasing `max_load_factor()`.

### See also

{/* source:cpp-container-unordered-set-rehash:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`reserve`](/docs/cpp/library/container/unordered_set/reserve)</DescriptionTerm>
    <DescriptionBody>reserves space for at least the specified number of elements and regenerates the hash table (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
