---
title: "std::unordered_set<Key,Hash,KeyEqual,Allocator>::max_bucket_count"
description: "Returns the maximum number of buckets the container is able to hold due to system or library implementation limitations."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/max_bucket_count"
language: "C++"
---

{/* source:cpp-container-unordered-set-max-bucket-count:0000 */}

<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`size_type max_bucket_count() const;`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-max-bucket-count:0001 */}

Returns the maximum number of buckets the container is able to hold due to system or library implementation limitations.

### Parameters

{/* source:cpp-container-unordered-set-max-bucket-count:0002 */}

(none)

### Return value

{/* source:cpp-container-unordered-set-max-bucket-count:0003 */}

Maximum number of buckets.

### Complexity

{/* source:cpp-container-unordered-set-max-bucket-count:0004 */}

Constant.

### Example

{/* source:cpp-container-unordered-set-max-bucket-count:0005 */}

```cpp
#include <iostream>
#include <unordered_set>
 
int main()
{
    struct Ha { std::size_t operator()(long x) const { return std::hash<long>{}(x); }; };
 
    auto c1 = std::unordered_set<char>{};
    auto c2 = std::unordered_set<long>{};
    auto c3 = std::unordered_set<long, std::hash<int>>{};
    auto c4 = std::unordered_set<long, Ha>{};
 
    std::cout
        << "Max bucket count of\n" << std::hex << std::showbase
        << "c1: " << c1.max_bucket_count() << '\n'
        << "c2: " << c2.max_bucket_count() << '\n'
        << "c3: " << c3.max_bucket_count() << '\n'
        << "c4: " << c4.max_bucket_count() << '\n'
        ;
}
```

Possible output:

```text
Max bucket count of
c1: 0xfffffffffffffff
c2: 0xfffffffffffffff
c3: 0xfffffffffffffff
c4: 0xaaaaaaaaaaaaaaa
```

### See also

{/* source:cpp-container-unordered-set-max-bucket-count:0006 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`bucket_count`](/docs/cpp/library/container/unordered_set/bucket_count)</DescriptionTerm>
    <DescriptionBody>returns the number of buckets (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
