---
title: "std::unordered_set<Key,Hash,KeyEqual,Allocator>::insert"
description: "Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_set/insert"
language: "C++"
---

{/* source:cpp-container-unordered-set-insert:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`std::pair<iterator,bool> insert( const value_type& value );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`std::pair<iterator,bool> insert( value_type&& value );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`iterator insert( const_iterator hint, const value_type& value );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`iterator insert( const_iterator hint, value_type&& value );`} />
  <Declaration id="5" language="cpp" since="C++11" code={`template< class InputIt >
void insert( InputIt first, InputIt last );`} />
  <Declaration id="6" language="cpp" since="C++11" code={`void insert( std::initializer_list<value_type> ilist );`} />
  <Declaration id="7" language="cpp" since="C++17" code={`insert_return_type insert( node_type&& nh );`} />
  <Declaration id="8" language="cpp" since="C++17" code={`iterator insert( const_iterator hint, node_type&& nh );`} />
  <Declaration id="9" language="cpp" since="C++23" code={`template< class K >
std::pair<iterator, bool> insert( K&& obj );`} />
  <Declaration id="10" language="cpp" since="C++23" code={`template< class K >
iterator insert( const_iterator hint, K&& obj );`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-set-insert:0001 */}
Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key.

{/* source:cpp-container-unordered-set-insert:0002 */}
1. Inserts `value`.

{/* source:cpp-container-unordered-set-insert:0003 */}
2. Inserts `value`, using `hint` as a non-binding suggestion to where the search should start.

{/* source:cpp-container-unordered-set-insert:0004 */}
3. Inserts elements from range `[first, last)`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [`LWG2844`](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-unordered-set-insert:0005 */}
4. Inserts elements from initializer list `ilist`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [`LWG2844`](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-unordered-set-insert:0006 */}
5. If `nh` is an empty [`node handle`](/docs/cpp/library/container/node_handle), does nothing. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-unordered-set-insert:0007 */}
6. If `nh` is an empty [`node handle`](/docs/cpp/library/container/node_handle), does nothing and returns the end iterator. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`, and returns the iterator pointing to the element with key equivalent to `nh.key()` (regardless of whether the insert succeeded or failed). If the insertion succeeds, `nh` is moved from, otherwise it retains ownership of the element. `hint` is used as a non-binding suggestion to where the search should start. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-unordered-set-insert:0008 */}
7. If `*this` already contains an element which transparently compares equivalent to `obj`, does nothing. Otherwise, constructs an object `u` of [`value_type`](/docs/cpp/library/container/unordered_set#Member_types) with `std::forward<K>(obj)` and then inserts `u` into `*this`. If `equal_range(u) != hash_function()(obj) || contains(u)` is true, the behavior is undefined. The `value_type` must be [`EmplaceConstructible`](/docs/cpp/library/named_req/EmplaceConstructible) into `unordered_set` from `std::forward<K>(obj)`. This overload participates in overload resolution only if `Hash::is_transparent` and `KeyEqual::is_transparent` are valid and each denotes a type. This assumes that such `Hash` is callable with both `K` and `Key` type, and that the `KeyEqual` is transparent, which, together, allows calling this function without constructing an instance of `Key`.

{/* source:cpp-container-unordered-set-insert:0009 */}
8. If `*this` already contains an element which transparently compares equivalent to `obj`, does nothing. Otherwise, constructs an object `u` of `value_type` with `std::forward<K>(obj)` and then inserts `u` into `*this`. [`Template:hint`](https://en.cppreference.com/mwiki/index.php?title=Template:hint&action=edit&redlink=1) is used as a non-binding suggestion to where the search should start. If `equal_range(u) != hash_function()(obj) || contains(u)` is true, the behavior is undefined. The `value_type` must be `EmplaceConstructible` into `unordered_set` from `std::forward<K>(obj)`. This overload participates in overload resolution only if: `std::is_convertible_v<K&&, const_iterator>` and `std::is_convertible_v<K&&, iterator>` are both false, and `Hash::is_transparent` and `KeyEqual::is_transparent` are valid and each denotes a type. This assumes that such `Hash` is callable with both `K` and `Key` type, and that the `KeyEqual` is transparent, which, together, allows calling this function without constructing an instance of `Key`.

{/* source:cpp-container-unordered-set-insert:0010 */}
If after the operation the new number of elements is greater than old [`max_load_factor()`](/docs/cpp/library/container/unordered_set/max_load_factor) * [`bucket_count()`](/docs/cpp/library/container/unordered_set/bucket_count) a rehashing takes place. If rehashing occurs (due to the insertion), all iterators are invalidated. Otherwise (no rehashing), iterators are not invalidated. <InlineRevision since="C++17">If the insertion is successful, pointers and references to the element obtained while it is held in the node handle are invalidated, and pointers and references obtained to that element before it was extracted become valid.</InlineRevision>

### Parameters

{/* source:cpp-container-unordered-set-insert:0011 */}
<ParameterList>
  <Parameter name="hint">iterator, used as a suggestion as to where to insert the content</Parameter>
  <Parameter name="value">element value to insert</Parameter>
  <Parameter name="first, last">range of elements to insert</Parameter>
  <Parameter name="ilist">initializer list to insert the values from</Parameter>
  <Parameter name="nh">a compatible [`node handle`](/docs/cpp/library/container/node_handle)</Parameter>
  <Parameter name="obj">a value of any type that can be transparently compared with a key</Parameter>
</ParameterList>

Type requirements: `InputIt` must meet the requirements of [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator).

### Return value

{/* source:cpp-container-unordered-set-insert:0012 */}
1. A pair consisting of an iterator to the inserted element (or to the element that prevented the insertion) and a `bool` value set to `true` if and only if the insertion took place.

{/* source:cpp-container-unordered-set-insert:0013 */}
2. An iterator to the inserted element, or to the element that prevented the insertion.

{/* source:cpp-container-unordered-set-insert:0014 */}
3. (none)

{/* source:cpp-container-unordered-set-insert:0015 */}
4. An object of `insert_return_type` with the members initialized as follows: If `nh` is empty, `inserted` is `false`, `position` is `end()`, and `node` is empty. Otherwise if the insertion took place, `inserted` is `true`, `position` points to the inserted element, and `node` is empty. If the insertion failed, `inserted` is `false`, `node` has the previous value of `nh`, and `position` points to an element with a key equivalent to `nh.key()`.

{/* source:cpp-container-unordered-set-insert:0016 */}
5. End iterator if `nh` was empty, iterator pointing to the inserted element if insertion took place, and iterator pointing to an element with a key equivalent to `nh.key()` if it failed.

{/* source:cpp-container-unordered-set-insert:0017 */}
6. A pair consisting of an iterator to the inserted element (or to the element that prevented the insertion) and a `bool` value set to `true` if and only if the insertion took place.

{/* source:cpp-container-unordered-set-insert:0018 */}
7. An iterator to the inserted element, or to the element that prevented the insertion.

### Exceptions

{/* source:cpp-container-unordered-set-insert:0019 */}
1. If an exception is thrown by any operation, the insertion has no effect.

{/* source:cpp-container-unordered-set-insert:0020 */}
This section is incomplete
Reason: cases 5-10

### Complexity

{/* source:cpp-container-unordered-set-insert:0021 */}
1. Average case: O(1), worst case O(size()).

{/* source:cpp-container-unordered-set-insert:0022 */}
2. Average case: O(N), where N is the number of elements to insert. Worst case: O(N * size() + N).

{/* source:cpp-container-unordered-set-insert:0023 */}
3. Average case: O(1), worst case O(size()).

### Notes

{/* source:cpp-container-unordered-set-insert:0024 */}
The hinted insert (3,4) does not return a boolean in order to be signature-compatible with positional insert on sequential containers, such as [`std::vector::insert`](/docs/cpp/library/container/vector/insert). This makes it possible to create generic inserters such as [`std::inserter`](/docs/cpp/library/iterator/inserter). One way to check success of a hinted insert is to compare [`size()`](/docs/cpp/library/container/unordered_set/size) before and after.

{/* source:cpp-container-unordered-set-insert:0025 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_associative_heterogeneous_insertion`](/docs/cpp/library/feature_test#cpp_lib_associative_heterogeneous_insertion) | [`202311L`](/docs/cpp/library/compiler_support/26#cpp_lib_associative_heterogeneous_insertion_202311L) | (C++26) | Heterogeneous overloads for the remaining member functions in [`ordered`](/docs/cpp/library/container#Associative_containers) and [`unordered`](/docs/cpp/library/container#Unordered_associative_containers) [`containers`](/docs/cpp/library/container). ([9,10](/docs/cpp/library/container/unordered_set/insert#Version_9)) |

### Example

{/* source:cpp-container-unordered-set-insert:0026 */}
```cpp
#include <array>
#include <iostream>
#include <unordered_set>

std::ostream& operator<<(std::ostream& os, std::unordered_set<int> const& s)
{
    for (os << '[' << s.size() << "] { "; int i : s)
        os << i << ' ';
    return os << "}\n";
}

int main ()
{
    std::unordered_set<int> nums{2, 3, 4};

    std::cout << "1) Initially: " << nums << std::boolalpha;

    auto p = nums.insert(1); // insert element, overload (1)
    std::cout << "2) '1' was inserted: " << p.second << '\n';
    std::cout << "3) After insertion: " << nums;

    nums.insert(p.first, 0); // insert with hint, overload (3)
    std::cout << "4) After insertion: " << nums;

        std::array<int, 4> a = {10, 11, 12, 13};
    nums.insert(a.begin(), a.end()); // insert range, overload (5)
    std::cout << "5) After insertion: " << nums;

    nums.insert({20, 21, 22, 23}); // insert initializer_list, (6)
    std::cout << "6) After insertion: " << nums;

    std::unordered_set<int> other_nums = {42, 43};
    auto node = other_nums.extract(other_nums.find(42));
    nums.insert(std::move(node)); // insert node, overload (7)
    std::cout << "7) After insertion: " << nums;

    node = other_nums.extract(other_nums.find(43));
    nums.insert(nums.begin(), std::move(node)); // insert node with hint, (8)
    std::cout << "8) After insertion: " << nums;
}
```

Possible output:

```text
1) Initially: [3] { 4 3 2 }
2) '1' was inserted: true
3) After insertion: [4] { 1 2 3 4 }
4) After insertion: [5] { 0 1 2 3 4 }
5) After insertion: [9] { 13 12 11 10 4 3 2 1 0 }
6) After insertion: [13] { 23 22 13 12 11 10 21 4 20 3 2 1 0 }
7) After insertion: [14] { 42 23 22 13 12 11 10 21 4 20 3 2 1 0 }
8) After insertion: [15] { 43 42 23 22 13 12 11 10 21 4 20 3 2 1 0 }
```

### See also

{/* source:cpp-container-unordered-set-insert:0027 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/container/unordered_set/emplace)</DescriptionTerm>
    <DescriptionBody>constructs element in-place (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_hint`](/docs/cpp/library/container/unordered_set/emplace_hint)</DescriptionTerm>
    <DescriptionBody>constructs elements in-place using a hint (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`inserter`](/docs/cpp/library/iterator/inserter)</DescriptionTerm>
    <DescriptionBody>creates a [`std::insert_iterator`](/docs/cpp/library/iterator/insert_iterator) of type inferred from the argument (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
