---
title: "std::list<T,Allocator>::emplace"
description: "Inserts a new element into the container directly before pos."
source_url: "https://en.cppreference.com/w/cpp/container/list/emplace"
language: "C++"
---

{/* source:cpp-container-list-emplace:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`template< class... Args >
iterator emplace( const_iterator pos, Args&&... args );`} />
</DeclarationDoc>

{/* source:cpp-container-list-emplace:0001 */}
Inserts a new element into the container directly before `pos`.

{/* source:cpp-container-list-emplace:0002 */}
The element is constructed through [`std::allocator_traits::construct`](/docs/cpp/library/memory/allocator_traits/construct), which uses placement-new to construct the element in-place at a location provided by the container.

{/* source:cpp-container-list-emplace:0003 */}
The arguments `args...` are forwarded to the constructor as `std::forward<Args>(args)...`. `args...` may directly or indirectly refer to a value in the container.

{/* source:cpp-container-list-emplace:0004 */}
No iterators or references are invalidated.

### Parameters

{/* source:cpp-container-list-emplace:0005 */}
<ParameterList>
  <Parameter name="pos">iterator before which the new element will be constructed</Parameter>
  <Parameter name="args">arguments to forward to the constructor of the element</Parameter>
</ParameterList>

Type requirements

- `T (the container's element type)` must meet the requirements of [`EmplaceConstructible`](/docs/cpp/library/named_req/EmplaceConstructible).

### Return value

{/* source:cpp-container-list-emplace:0006 */}
Iterator pointing to the emplaced element.

### Complexity

{/* source:cpp-container-list-emplace:0007 */}
Constant.

### Exceptions

{/* source:cpp-container-list-emplace:0008 */}
If an exception is thrown (e.g. by the constructor), the container is left unmodified, as if this function was never called (strong exception guarantee).

### Example

{/* source:cpp-container-list-emplace:0009 */}
```cpp
#include <iostream>
#include <string>
#include <list>

struct A
{
    std::string s;

    A(std::string str) : s(std::move(str)) { std::cout << " constructed\n"; }

    A(const A& o) : s(o.s) { std::cout << " copy constructed\n"; }

    A(A&& o) : s(std::move(o.s)) { std::cout << " move constructed\n"; }

    A& operator=(const A& other)
    {
        s = other.s;
        std::cout << " copy assigned\n";
        return *this;
    }

    A& operator=(A&& other)
    {
        s = std::move(other.s);
        std::cout << " move assigned\n";
        return *this;
    }
};

int main()
{
    std::list<A> container;

    std::cout << "construct 2 times A:\n";
    A two{"two"};
    A three{"three"};

    std::cout << "emplace:\n";
    container.emplace(container.end(), "one");

    std::cout << "emplace with A&:\n";
    container.emplace(container.end(), two);

    std::cout << "emplace with A&&:\n";
    container.emplace(container.end(), std::move(three));

    std::cout << "content:\n";
    for (const auto& obj : container)
        std::cout << ' ' << obj.s;
    std::cout << '\n';
}
```

Output:

```text
construct 2 times A:
 constructed
 constructed
emplace:
 constructed
emplace with A&:
 copy constructed
emplace with A&&:
 move constructed
content:
 one two three
```

### Defect reports

{/* source:cpp-container-list-emplace:0010 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-list-emplace:0011 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2164} standard="C++11"><PublishedBehavior>it was not clear whether the arguments can refer to the container</PublishedBehavior><CorrectedBehavior>clarified</CorrectedBehavior></DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-list-emplace:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`insert`](/docs/cpp/library/container/list/insert)</DescriptionTerm>
    <DescriptionBody>inserts elements (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_back`](/docs/cpp/library/container/list/emplace_back) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs an element in-place at the end (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
