---
title: "std::list<T,Allocator>::pop_back"
description: "Removes the last element of the container."
source_url: "https://en.cppreference.com/w/cpp/container/list/pop_back"
language: "C++"
---

{/* source:cpp-container-list-pop-back:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`void pop_back();`} />
</DeclarationDoc>

{/* source:cpp-container-list-pop-back:0001 */}
Removes the last element of the container.

{/* source:cpp-container-list-pop-back:0002 */}
Calling `pop_back` on an empty container results in undefined behavior.

{/* source:cpp-container-list-pop-back:0003 */}
References and iterators to the erased element are invalidated.

### Parameters

{/* source:cpp-container-list-pop-back:0004 */}
(none)

### Return value

{/* source:cpp-container-list-pop-back:0005 */}
(none)

### Complexity

{/* source:cpp-container-list-pop-back:0006 */}
Constant.

### Exceptions

{/* source:cpp-container-list-pop-back:0007 */}
Throws nothing.

### Example

{/* source:cpp-container-list-pop-back:0008 */}
```cpp
#include <list>
#include <iostream>

namespace stq {
template<typename T>
void println(auto, const T& xz)
{
    std::cout << '[';
    bool first{true};
    for (auto const& x : xz)
        std::cout << (first ? first = false, "" : ", ") << x;
    std::cout << "]\n";
}
}

int main()
{
    std::list<int> numbers{1, 2, 3};
    stq::println("{}", numbers);
    while (not numbers.empty())
    {
        numbers.pop_back();
        stq::println("{}", numbers);
    }
}
```

Output:

```text
[1, 2, 3]
[1, 2]
[1]
[]
```

### See also

{/* source:cpp-container-list-pop-back:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`pop_front`](/docs/cpp/library/container/list/pop_front)</DescriptionTerm>
    <DescriptionBody>removes the first element (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`push_back`](/docs/cpp/library/container/list/push_back)</DescriptionTerm>
    <DescriptionBody>adds an element to the end (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
