---
title: "std::list<T,Allocator>::emplace_back"
description: "Appends a new element to the end of the container."
source_url: "https://en.cppreference.com/w/cpp/container/list/emplace_back"
language: "C++"
---

{/* source:cpp-container-list-emplace-back:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class... Args >
void emplace_back( Args&&... args );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class... Args >
reference emplace_back( Args&&... args );`} />

  <DeclarationDescription>
    {/* source:cpp-container-list-emplace-back:0001 */}
    Appends a new element to the end of the container. The element is constructed through [`std::allocator_traits::construct`](/docs/cpp/library/memory/allocator_traits/construct), which typically uses placement-new to construct the element in-place at the location provided by the container. The arguments `args...` are forwarded to the constructor as `std::forward<Args>(args)...`.

    {/* source:cpp-container-list-emplace-back:0002 */}
    No iterators or references are invalidated.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-list-emplace-back:0003 */}
<ParameterList>
  <Parameter name="args">arguments to forward to the constructor of the element</Parameter>
</ParameterList>

**Type requirements**

- `T` (the container's element type) must meet the requirements of [EmplaceConstructible](/docs/cpp/library/named_req/EmplaceConstructible).

### Return value

{/* source:cpp-container-list-emplace-back:0004 */}
<Revision until="C++17">(none)</Revision>

<Revision since="C++17">A reference to the inserted element.</Revision>

### Complexity

{/* source:cpp-container-list-emplace-back:0005 */}
Constant.

### Exceptions

{/* source:cpp-container-list-emplace-back:0006 */}
If an exception is thrown for any reason, this function has no effect ([strong exception safety guarantee](/docs/cpp/language/exceptions#Exception_safety)).

### Example

{/* source:cpp-container-list-emplace-back:0007 */}
The following code uses `emplace_back` to append an object of type `President` to a [`std::list`](/docs/cpp/library/container/list). It demonstrates how `emplace_back` forwards parameters to the `President` constructor and shows how using `emplace_back` avoids the extra copy or move operation required when using [`push_back`](/docs/cpp/library/container/list/push_back).

```cpp
#include <list>
#include <cassert>
#include <iostream>
#include <string>

struct President
{
    std::string name;
    std::string country;
    int year;

    President(std::string p_name, std::string p_country, int p_year)
        : name(std::move(p_name)), country(std::move(p_country)), year(p_year)
    {
        std::cout << "I am being constructed.\n";
    }

    President(President&& other)
        : name(std::move(other.name)), country(std::move(other.country)), year(other.year)
    {
        std::cout << "I am being moved.\n";
    }

    President& operator=(const President& other) = default;
};

int main()
{
    std::list<President> elections;
    std::cout << "emplace_back:\n";
    auto& ref = elections.emplace_back("Nelson Mandela", "South Africa", 1994);
    assert(ref.year == 1994 && "uses a reference to the created object (C++17)");

    std::list<President> reElections;
    std::cout << "\npush_back:\n";
    reElections.push_back(President("Franklin Delano Roosevelt", "the USA", 1936));

    std::cout << "\nContents:\n";
    for (President const& president: elections)
        std::cout << president.name << " was elected president of "
                  << president.country << " in " << president.year << ".\n";

    for (President const& president: reElections)
        std::cout << president.name << " was re-elected president of "
                  << president.country << " in " << president.year << ".\n";
}
```

Output:

```text
emplace_back:
I am being constructed.

push_back:
I am being constructed.
I am being moved.

Contents:
Nelson Mandela was elected president of South Africa in 1994.
Franklin Delano Roosevelt was re-elected president of the USA in 1936.
```

### See also

{/* source:cpp-container-list-emplace-back:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`push_back`](/docs/cpp/library/container/list/push_back)</DescriptionTerm>
    <DescriptionBody>adds an element to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/container/list/emplace) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>constructs element in-place (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
