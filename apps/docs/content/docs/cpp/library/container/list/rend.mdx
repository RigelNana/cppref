---
title: "std::list<T,Allocator>::rend, std::list<T,Allocator>::crend"
description: "Returns a reverse iterator to the element following the last element of the reversed list."
source_url: "https://en.cppreference.com/w/cpp/container/list/rend"
language: "C++"
---

{/* source:cpp-container-list-rend:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`reverse_iterator rend();`} />
  <Declaration id="2" language="cpp" code={`const_reverse_iterator rend() const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_reverse_iterator crend() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-container-list-rend:0001 */}
    1. <InlineRevision since="C++11" /> Returns a reverse iterator to the element following the last element of the reversed list. It corresponds to the element preceding the first element of the non-reversed list. This element acts as a placeholder, attempting to access it results in undefined behavior.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-list-rend:0002 */}
(none)

### Return value

{/* source:cpp-container-list-rend:0003 */}
Reverse iterator to the element following the last element.

### Complexity

{/* source:cpp-container-list-rend:0004 */}
Constant.

### Notes

{/* source:cpp-container-list-rend:0005 */}
`libc++` backports `crend()` to C++98 mode.

### Example

{/* source:cpp-container-list-rend:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <numeric>
#include <string>
#include <list>

int main()
{
    std::list<int> nums{1, 2, 4, 8, 16};
    std::list<std::string> fruits{"orange", "apple", "raspberry"};
    std::list<char> empty;

    // Print list.
    std::for_each(nums.rbegin(), nums.rend(), [](const int n) { std::cout << n << ' '; });
    std::cout << '\n';

    // Sums all integers in the list nums (if any), printing only the result.
    std::cout << "Sum of nums: "
              << std::accumulate(nums.rbegin(), nums.rend(), 0) << '\n';

    // Prints the first fruit in the list fruits, checking if there is any.
    if (!fruits.empty())
        std::cout << "First fruit: " << *fruits.rbegin() << '\n';

    if (empty.rbegin() == empty.rend())
        std::cout << "list 'empty' is indeed empty.\n";
}
```

Output:

```text
16 8 4 2 1
Sum of nums: 31
First fruit: raspberry
list 'empty' is indeed empty.
```

### See also

{/* source:cpp-container-list-rend:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/container/list/rbegin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/iterator/rend) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse end iterator for a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
