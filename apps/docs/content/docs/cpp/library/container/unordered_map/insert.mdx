---
title: "std::unordered_map<Key,T,Hash,KeyEqual,Allocator>::insert"
description: "Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key."
source_url: "https://en.cppreference.com/w/cpp/container/unordered_map/insert"
language: "C++"
---

{/* source:cpp-container-unordered-map-insert:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`std::pair<iterator, bool> insert( const value_type& value );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`std::pair<iterator, bool> insert( value_type&& value );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< class P >
std::pair<iterator, bool> insert( P&& value );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`iterator insert( const_iterator hint, const value_type& value );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`iterator insert( const_iterator hint, value_type&& value );`} />
  <Declaration id="6" language="cpp" since="C++11" code={`template< class P >
iterator insert( const_iterator hint, P&& value );`} />
  <Declaration id="7" language="cpp" since="C++11" code={`template< class InputIt >
void insert( InputIt first, InputIt last );`} />
  <Declaration id="8" language="cpp" since="C++11" code={`void insert( std::initializer_list<value_type> ilist );`} />
  <Declaration id="9" language="cpp" since="C++17" code={`insert_return_type insert( node_type&& nh );`} />
  <Declaration id="10" language="cpp" since="C++17" code={`iterator insert( const_iterator hint, node_type&& nh );`} />
</DeclarationDoc>

{/* source:cpp-container-unordered-map-insert:0001 */}
Inserts element(s) into the container, if the container doesn't already contain an element with an equivalent key.

{/* source:cpp-container-unordered-map-insert:0002 */}
- 1-3) Inserts `value`.

{/* source:cpp-container-unordered-map-insert:0003 */}
- Overload (3) is equivalent to `emplace(std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-unordered-map-insert:0004 */}
- 4-6) Inserts `value`, using `hint` as a non-binding suggestion to where the search should start.

{/* source:cpp-container-unordered-map-insert:0005 */}
- Overload (6) is equivalent to `emplace_hint(hint, std::forward<P>(value))` and only participates in overload resolution if `std::is_constructible<value_type, P&&>::value == true`.

{/* source:cpp-container-unordered-map-insert:0006 */}
- 7) Inserts elements from range `[first, last)`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-unordered-map-insert:0007 */}
- If `[first, last)` is not a [valid range](/docs/cpp/library/iterator#Ranges), or `first` and/or `last` are iterators into `*this`, the behavior is undefined.

{/* source:cpp-container-unordered-map-insert:0008 */}
- 8) Inserts elements from initializer list `ilist`. If multiple elements in the range have keys that compare equivalent, it is unspecified which element is inserted (pending [LWG2844](https://cplusplus.github.io/LWG/issue2844)).

{/* source:cpp-container-unordered-map-insert:0009 */}
- 9) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-unordered-map-insert:0010 */}
- 10) If `nh` is an empty [node handle](/docs/cpp/library/container/node_handle), does nothing and returns the end iterator. Otherwise, inserts the element owned by `nh` into the container, if the container doesn't already contain an element with a key equivalent to `nh.key()`, and returns the iterator pointing to the element with key equivalent to `nh.key()` (regardless of whether the insert succeeded or failed). If the insertion succeeds, `nh` is moved from, otherwise it retains ownership of the element. `hint` is used as a non-binding suggestion to where the search should start. The behavior is undefined if `nh` is not empty and `get_allocator() != nh.get_allocator()`.

{/* source:cpp-container-unordered-map-insert:0011 */}
If after the operation the new number of elements is greater than old [`max_load_factor()`](/docs/cpp/library/container/unordered_map/max_load_factor) `*` [`bucket_count()`](/docs/cpp/library/container/unordered_map/bucket_count) a rehashing takes place.
If rehashing occurs (due to the insertion), all iterators are invalidated. Otherwise (no rehashing), iterators are not invalidated. <InlineRevision since="C++17">If the insertion is successful, pointers and references to the element obtained while it is held in the node handle are invalidated, and pointers and references obtained to that element before it was extracted become valid.</InlineRevision>

## Parameters

{/* source:cpp-container-unordered-map-insert:0012 */}
<ParameterList>
  <Parameter name="hint">iterator, used as a suggestion as to where to insert the content</Parameter>
  <Parameter name="value">element value to insert</Parameter>
  <Parameter name="first, last">range of elements to insert</Parameter>
  <Parameter name="ilist">initializer list to insert the values from</Parameter>
  <Parameter name="nh">a compatible [node handle](/docs/cpp/library/container/node_handle)</Parameter>
</ParameterList>

Type requirements

- `InputIt` must meet the requirements of [`LegacyInputIterator`](/docs/cpp/library/named_req/InputIterator).

## Return value

{/* source:cpp-container-unordered-map-insert:0013 */}
- 1-3) A pair consisting of an iterator to the inserted element (or to the element that prevented the insertion) and a `bool` value set to `true` if and only if the insertion took place.

{/* source:cpp-container-unordered-map-insert:0014 */}
- 4-6) An iterator to the inserted element, or to the element that prevented the insertion.

{/* source:cpp-container-unordered-map-insert:0015 */}
- 7,8) (none)

{/* source:cpp-container-unordered-map-insert:0016 */}
- 9) An object of [`insert_return_type`](/docs/cpp/library/container/unordered_map#Member_types) with the members initialized as follows:
  - If `nh` is empty, `inserted` is `false`, `position` is `end()`, and `node` is empty.
  - Otherwise if the insertion took place, `inserted` is `true`, `position` points to the inserted element, and `node` is empty.
  - If the insertion failed, `inserted` is `false`, `node` has the previous value of `nh`, and `position` points to an element with a key equivalent to `nh.key()`.

{/* source:cpp-container-unordered-map-insert:0017 */}
- 10) End iterator if `nh` was empty, iterator pointing to the inserted element if insertion took place, and iterator pointing to an element with a key equivalent to `nh.key()` if it failed.

## Exceptions

{/* source:cpp-container-unordered-map-insert:0018 */}
- 1-6) If an exception is thrown for any reason, these functions have no effect ([strong exception safety guarantee](/docs/cpp/language/exceptions#Exception_safety)).

{/* source:cpp-container-unordered-map-insert:0019 */}
- 7,8) No exception safety guarantee.

{/* source:cpp-container-unordered-map-insert:0020 */}
- 9,10) If an exception is thrown for any reason, these functions have no effect ([strong exception safety guarantee](/docs/cpp/language/exceptions#Exception_safety)).

## Complexity

{/* source:cpp-container-unordered-map-insert:0021 */}
- 1-6) Average case: `O(1)`, worst case `O(size())`.

{/* source:cpp-container-unordered-map-insert:0022 */}
- 7,8) Average case: `O(N)`, where N is the number of elements to insert. Worst case: `O(N * size() + N)`.

{/* source:cpp-container-unordered-map-insert:0023 */}
- 9,10) Average case: `O(1)`, worst case `O(size())`.

## Notes

{/* source:cpp-container-unordered-map-insert:0024 */}
The hinted insert (4-6) does not return a boolean in order to be signature-compatible with positional insert on sequential containers, such as [`std::vector::insert`](/docs/cpp/library/container/vector/insert). This makes it possible to create generic inserters such as [`std::inserter`](/docs/cpp/library/iterator/inserter). One way to check success of a hinted insert is to compare [`size()`](/docs/cpp/library/container/unordered_map/size) before and after.

## Example

{/* source:cpp-container-unordered-map-insert:0025 */}
```cpp
#include <iostream>
#include <string>
#include <unordered_map>

int main ()
{
    std::unordered_map<int, std::string> dict = {{1, "one"}, {2, "two"}};
    dict.insert({3, "three"});
    dict.insert(std::make_pair(4, "four"));
    dict.insert({{4, "another four"}, {5, "five"}});

    const bool ok = dict.insert({1, "another one"}).second;
    std::cout << "inserting 1 => \"another one\" "
              << (ok ? "succeeded" : "failed") << '\n';

    std::cout << "contents:\n";
    for (auto& p : dict)
        std::cout << ' ' << p.first << " => " << p.second << '\n';
}
```

Possible output:

```text
inserting 1 => "another one" failed
contents:
 5 => five
 1 => one
 2 => two
 3 => three
 4 => four
```

## Defect reports

{/* source:cpp-container-unordered-map-insert:0026 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-unordered-map-insert:0027 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2005} standard="C++11">
    <PublishedBehavior>overloads (3,6) would only participate in overload resolution if `P` is implicitly convertible to `value_type`</PublishedBehavior>
    <CorrectedBehavior>only participates if `value_type` is constructible from `P&&`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

## See also

{/* source:cpp-container-unordered-map-insert:0028 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/container/unordered_map/emplace)</DescriptionTerm>
    <DescriptionBody>constructs element in-place (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`emplace_hint`](/docs/cpp/library/container/unordered_map/emplace_hint)</DescriptionTerm>
    <DescriptionBody>constructs elements in-place using a hint (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`insert_or_assign`](/docs/cpp/library/container/unordered_map/insert_or_assign) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>inserts an element or assigns to the current element if the key already exists (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`inserter`](/docs/cpp/library/iterator/inserter)</DescriptionTerm>
    <DescriptionBody>creates a [`std::insert_iterator`](/docs/cpp/library/iterator/insert_iterator) of type inferred from the argument (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
