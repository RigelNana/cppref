---
title: "std::inplace_vector<T,N>::clear"
description: "Erases all elements from the container."
source_url: "https://en.cppreference.com/w/cpp/container/inplace_vector/clear"
language: "C++"
---

{/* source:cpp-container-inplace-vector-clear:0000 */}
<DeclarationDoc since="C++26">
  <Declaration language="cpp" code={`constexpr void clear() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-inplace-vector-clear:0001 */}
Erases all elements from the container. After this call, `size()` returns zero.

{/* source:cpp-container-inplace-vector-clear:0002 */}
Invalidates any references, pointers, and iterators referring to contained elements. Any past-the-end iterators are also invalidated.

### Parameters

{/* source:cpp-container-inplace-vector-clear:0003 */}
(none)

### Return value

{/* source:cpp-container-inplace-vector-clear:0004 */}
(none)

### Complexity

{/* source:cpp-container-inplace-vector-clear:0005 */}
Linear in the size of the container, i.e., the number of elements.

### Example

{/* source:cpp-container-inplace-vector-clear:0006 */}
```cpp
#include <iostream>
#include <string_view>
#include <inplace_vector>

void print_info(std::string_view rem, const std::inplace_vector<int, 3>& v)
{
    std::cout << rem << "{ ";
    for (const auto& value : v)
        std::cout << value << ' ';
    std::cout << "}\n";
    std::cout << "Size=" << v.size() << '\n';
}

int main()
{
    std::inplace_vector<int, 3> container{1, 2, 3};
    print_info("Before clear: ", container);
    container.clear();
    print_info("After clear: ", container);
}
```

Output:

```text
Before clear: { 1 2 3 }
Size=3
After clear: { }
Size=0
```

### See also

{/* source:cpp-container-inplace-vector-clear:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`erase`](/docs/cpp/library/container/inplace_vector/erase)</DescriptionTerm>
    <DescriptionBody>erases elements (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
