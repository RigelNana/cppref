---
title: "std::inplace_vector<T,N>::rend, std::inplace_vector<T,N>::crend"
description: "Returns a reverse iterator to the element following the last element of the reversed inplace_vector."
source_url: "https://en.cppreference.com/w/cpp/container/inplace_vector/rend"
language: "C++"
---

{/* source:cpp-container-inplace-vector-rend:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++26" code={`constexpr reverse_iterator rend() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++26" code={`constexpr const_reverse_iterator rend() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++26" code={`constexpr const_reverse_iterator crend() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-inplace-vector-rend:0001 */}
Returns a reverse iterator to the element following the last element of the reversed `inplace_vector`. It corresponds to the element preceding the first element of the non-reversed `inplace_vector`. This element acts as a placeholder, attempting to access it results in undefined behavior.

### Parameters

{/* source:cpp-container-inplace-vector-rend:0002 */}
(none)

### Return value

{/* source:cpp-container-inplace-vector-rend:0003 */}
Reverse iterator to the element following the last element.

### Complexity

{/* source:cpp-container-inplace-vector-rend:0004 */}
Constant.

### Example

{/* source:cpp-container-inplace-vector-rend:0005 */}
```cpp
#include <algorithm>
#include <iostream>
#include <inplace_vector>
 
int main()
{
    std::inplace_vector<int, 11> a{1, 11, 11, 35, 0, 12, 79, 76, 76, 69, 40};
 
    // Print elements of container in reverse order using const_reverse_iterator's.
    std::for_each(a.crbegin(), a.crend(), [](int e){ std::cout << e << ' '; });
    std::cout << '\n';
 
    // Modify each element of container using non-const reverse_iterator's.
    std::for_each(a.rbegin(), a.rend(), [](int& e){ e += 32; });
 
    // Print elements as chars in reverse order using const_reverse_iterator's.
    std::for_each(a.crbegin(), a.crend(), [](char e){ std::cout << e; });
    std::cout << '\n';
}
```

Output:

```text
40 69 76 76 79 12 0 35 11 11 1
Hello, C++!
```

### See also

{/* source:cpp-container-inplace-vector-rend:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/container/inplace_vector/rbegin)</DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/iterator/rend) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse end iterator for a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
