---
title: "std::inplace_vector<T,N>::~inplace_vector"
description: "A destructor. Also, a trivial destructor if std::is_trivially_destructible_v<T> is true."
source_url: "https://en.cppreference.com/w/cpp/container/inplace_vector/~inplace_vector"
language: "C++"
---

{/* source:cpp-container-inplace-vector-inplace-vector:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++26" code={`constexpr ~inplace_vector();`} />
</DeclarationDoc>

{/* source:cpp-container-inplace-vector-inplace-vector:0001 */}
A [destructor](/docs/cpp/language/classes/destructor). Also, a [trivial destructor](/docs/cpp/language/classes/destructor#Trivial_destructor) if `std::is_trivially_destructible_v<T>` is `true`. Destructs the `inplace_vector`. The destructors of the elements are called and the used storage is deallocated. Note, that if the elements are pointers, the pointed-to objects are not destroyed.

### Complexity

{/* source:cpp-container-inplace-vector-inplace-vector:0002 */}
Linear in the size of the `inplace_vector`.
