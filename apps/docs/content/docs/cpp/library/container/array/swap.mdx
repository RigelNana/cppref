---
title: "std::array<T,N>::swap"
description: "Exchanges the contents of the container with those of other."
source_url: "https://en.cppreference.com/w/cpp/container/array/swap"
language: "C++"
---

{/* source:cpp-container-array-swap:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void swap( array& other ) noexcept(/* see below */);`} />

  <DeclarationDescription>
    {/* source:cpp-container-array-swap:0001 */}
    1. <InlineRevision since="C++20" /> Exchanges the contents of the container with those of `other`. Does not cause iterators and references to associate with the other container.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-container-array-swap:0002 */}
<ParameterList>
  <Parameter name="other">container to exchange the contents with</Parameter>
</ParameterList>

### Return value

{/* source:cpp-container-array-swap:0003 */}
(none)

### Exceptions

{/* source:cpp-container-array-swap:0004 */}
<Revision until="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(swap(std::declval<T&>(), std::declval<T&>())))`

In the expression above, the identifier `swap` is looked up in the same manner as the one used by the C++17 [`std::is_nothrow_swappable`](/docs/cpp/library/meta/is_swappable) trait.
</Revision>

<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(std::is_nothrow_swappable_v<T>)`
</Revision>

{/* source:cpp-container-array-swap:0005 */}
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept`

### Complexity

{/* source:cpp-container-array-swap:0006 */}
Linear in size of the container.

### Example

{/* source:cpp-container-array-swap:0007 */}
```cpp
#include <array>
#include <iostream>

template<class Os, class V> Os& operator<<(Os& os, const V& v)
{
    os << '{';
    for (auto i : v)
        os << ' ' << i;
    return os << " } ";
}

int main()
{
    std::array<int, 3> a1{1, 2, 3}, a2{4, 5, 6};

    auto it1 = a1.begin();
    auto it2 = a2.begin();
    int& ref1 = a1[1];
    int& ref2 = a2[1];

    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
    a1.swap(a2);
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';

    // Note that after swap iterators and references stay associated with their original
    // array, e.g. `it1` still points to element a1[0], `ref1` still refers to a1[1].
}
```

Output:

```text
{ 1 2 3 } { 4 5 6 } 1 4 2 5
{ 4 5 6 } { 1 2 3 } 4 1 5 2
```

### Defect reports

{/* source:cpp-container-array-swap:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-array-swap:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2456} standard="C++11">
    <PublishedBehavior>the `noexcept` specification is ill-formed</PublishedBehavior>
    <CorrectedBehavior>made to work</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-array-swap:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::swap(std::array)`](/docs/cpp/library/container/array/swap2) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
