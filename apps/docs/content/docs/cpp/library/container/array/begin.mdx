---
title: "std::array<T,N>::begin, std::array<T,N>::cbegin"
description: "Returns an iterator to the first element of the array."
source_url: "https://en.cppreference.com/w/cpp/container/array/begin"
language: "C++"
---

{/* source:cpp-container-array-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`iterator begin() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`const_iterator begin() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cbegin() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-container-array-begin:0001 */}
    1. <InlineRevision since="C++17" /> Returns an iterator to the first element of the array.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-container-array-begin:0002 */}
If the array is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/array/end).

### Parameters

{/* source:cpp-container-array-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-array-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-array-begin:0005 */}
Constant.

### Example

{/* source:cpp-container-array-begin:0006 */}
```cpp
#include <algorithm>
#include <array>
#include <iomanip>
#include <iostream>
 
int main()
{
    std::cout << std::boolalpha;
 
    std::array<int, 0> empty;
    std::cout << "1) "
              << (empty.begin() == empty.end()) << ' '     // true
              << (empty.cbegin() == empty.cend()) << '\n'; // true
    // *(empty.begin()) = 42; // => undefined behavior at run-time
 
 
    std::array<int, 4> numbers{5, 2, 3, 4};
    std::cout << "2) "
              << (numbers.begin() == numbers.end()) << ' '    // false
              << (numbers.cbegin() == numbers.cend()) << '\n' // false
              << "3) "
              << *(numbers.begin()) << ' '    // 5
              << *(numbers.cbegin()) << '\n'; // 5
 
    *numbers.begin() = 1;
    std::cout << "4) " << *(numbers.begin()) << '\n'; // 1
    // *(numbers.cbegin()) = 42; // compile-time error: 
                                 // read-only variable is not assignable
 
    // print out all elements
    std::cout << "5) ";
    std::for_each(numbers.cbegin(), numbers.cend(), [](int x)
    {
        std::cout << x << ' ';
    });
    std::cout << '\n';
 
    constexpr std::array constants{'A', 'B', 'C'};
    static_assert(constants.begin() != constants.end());   // OK
    static_assert(constants.cbegin() != constants.cend()); // OK
    static_assert(*constants.begin() == 'A');              // OK
    static_assert(*constants.cbegin() == 'A');             // OK
    // *constants.begin() = 'Z'; // compile-time error: 
                                 // read-only variable is not assignable
}
```

Output:

```text
1) true true
2) false false
3) 5 5
4) 1
5) 1 2 3 4
```

### See also

{/* source:cpp-container-array-begin:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end cend`](/docs/cpp/library/container/array/end)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
