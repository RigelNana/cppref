---
title: "std::forward_list<T,Allocator>::swap"
description: "Exchanges the contents of the container with those of other."
source_url: "https://en.cppreference.com/w/cpp/container/forward_list/swap"
language: "C++"
---

{/* source:cpp-container-forward-list-swap:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`void swap( forward_list& other );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`void swap( forward_list& other ) noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-container-forward-list-swap:0001 */}
Exchanges the contents of the container with those of `other`. Does not invoke any move, copy, or swap operations on individual elements.

{/* source:cpp-container-forward-list-swap:0002 */}
All iterators and references remain valid. It is unspecified whether an iterator holding the [`end()`](/docs/cpp/library/container/forward_list/end) value in this container will refer to this or the other container after the operation.

{/* source:cpp-container-forward-list-swap:0003 */}
<Revision since="C++11">
If `std::allocator_traits<allocator_type>::propagate_on_container_swap::value` is `true`, then the allocators are exchanged using an unqualified call to non-member `swap`. Otherwise, they are not swapped (and if `get_allocator() != other.get_allocator()`, the behavior is undefined).
</Revision>

### Parameters

{/* source:cpp-container-forward-list-swap:0004 */}
<ParameterList>
  <Parameter name="other">container to exchange the contents with</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-container-forward-list-swap:0005 */}
<Revision until="C++17">
(none)
</Revision>
<Revision since="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(std::allocator_traits<Allocator>::is_always_equal::value)`
</Revision>

### Complexity

{/* source:cpp-container-forward-list-swap:0006 */}
Constant.

### Example

{/* source:cpp-container-forward-list-swap:0007 */}
```cpp
#include <iostream>
#include <forward_list>
 
template<class Os, class Co>
Os& operator<<(Os& os, const Co& co)
{
    os << '{';
    for (auto const& i : co)
        os << ' ' << i;
    return os << " } ";
}
 
int main()
{
    std::forward_list<int> a1{1, 2, 3}, a2{4, 5};
 
    auto it1 = std::next(a1.begin());
    auto it2 = std::next(a2.begin());
 
    int& ref1 = a1.front();
    int& ref2 = a2.front();
 
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
    a1.swap(a2);
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
 
    // Note that after swap the iterators and references stay associated with their
    // original elements, e.g. it1 that pointed to an element in 'a1' with value 2
    // still points to the same element, though this element was moved into 'a2'.
}
```

Output:

```text
{ 1 2 3 } { 4 5 } 2 5 1 4
{ 4 5 } { 1 2 3 } 2 5 1 4
```

### See also

{/* source:cpp-container-forward-list-swap:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::swap(std::forward_list)`](/docs/cpp/library/container/forward_list/swap2) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
