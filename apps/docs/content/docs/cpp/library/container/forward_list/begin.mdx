---
title: "std::forward_list<T,Allocator>::begin, std::forward_list<T,Allocator>::cbegin"
description: "Returns an iterator to the first element of the forward_list."
source_url: "https://en.cppreference.com/w/cpp/container/forward_list/begin"
language: "C++"
---

{/* source:cpp-container-forward-list-begin:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`iterator begin() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`const_iterator begin() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`const_iterator cbegin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-forward-list-begin:0001 */}
Returns an iterator to the first element of the `forward_list`.

{/* source:cpp-container-forward-list-begin:0002 */}
If the `forward_list` is empty, the returned iterator will be equal to [`end()`](/docs/cpp/library/container/forward_list/end).

### Parameters

{/* source:cpp-container-forward-list-begin:0003 */}
(none)

### Return value

{/* source:cpp-container-forward-list-begin:0004 */}
Iterator to the first element.

### Complexity

{/* source:cpp-container-forward-list-begin:0005 */}
Constant.

### Example

{/* source:cpp-container-forward-list-begin:0006 */}
```cpp
#include <algorithm>
#include <iostream>
#include <numeric>
#include <string>
#include <forward_list>

int main()
{
    std::forward_list<int> nums{1, 2, 4, 8, 16};
    std::forward_list<std::string> fruits{"orange", "apple", "raspberry"};
    std::forward_list<char> empty;

    // Print forward_list.
    std::for_each(nums.begin(), nums.end(), [](const int n) { std::cout << n << ' '; });
    std::cout << '\n';

    // Sums all integers in the forward_list nums (if any), printing only the result.
    std::cout << "Sum of nums: "
              << std::accumulate(nums.begin(), nums.end(), 0) << '\n';

    // Prints the first fruit in the forward_list fruits, checking if there is any.
    if (!fruits.empty())
        std::cout << "First fruit: " << *fruits.begin() << '\n';

    if (empty.begin() == empty.end())
        std::cout << "forward_list 'empty' is indeed empty.\n";
}
```

Output:

```text
1 2 4 8 16
Sum of nums: 31
First fruit: orange
forward_list 'empty' is indeed empty.
```

### See also

{/* source:cpp-container-forward-list-begin:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`, `cend`](/docs/cpp/library/container/forward_list/end)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`, `cbegin`](/docs/cpp/library/iterator/begin) <InlineRevision since="C++11" /> <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
