---
title: "std::forward_list<T,Allocator>::clear"
description: "Erases all elements from the container."
source_url: "https://en.cppreference.com/w/cpp/container/forward_list/clear"
language: "C++"
---

{/* source:cpp-container-forward-list-clear:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void clear() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-container-forward-list-clear:0001 */}
Erases all elements from the container.

{/* source:cpp-container-forward-list-clear:0002 */}
Invalidates any references, pointers, and iterators referring to contained elements. Any past-the-end iterator remains valid.

### Parameters

{/* source:cpp-container-forward-list-clear:0003 */}
(none)

### Return value

{/* source:cpp-container-forward-list-clear:0004 */}
(none)

### Complexity

{/* source:cpp-container-forward-list-clear:0005 */}
Linear in the size of the container, i.e., the number of elements.

### Example

{/* source:cpp-container-forward-list-clear:0006 */}
```cpp
#include <iostream>
#include <string_view>
#include <forward_list>
 
void print_info(std::string_view rem, const std::forward_list<int>& v)
{
    std::cout << rem << "{ ";
    for (const auto& value : v)
        std::cout << value << ' ';
    std::cout << "}\n";
}
 
int main()
{
    std::forward_list<int> container{1, 2, 3};
    print_info("Before clear: ", container);
    container.clear();
    print_info("After clear: ", container);
}
```

Output:

```text
Before clear: { 1 2 3 }
After clear: { }
```

### Defect reports

{/* source:cpp-container-forward-list-clear:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-forward-list-clear:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2231} standard="C++11">
    <PublishedBehavior>complexity guarantee was mistakenly omitted in C++11</PublishedBehavior>
    <CorrectedBehavior>complexity reaffirmed as linear</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-container-forward-list-clear:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`erase_after`](/docs/cpp/library/container/forward_list/erase_after)</DescriptionTerm>
    <DescriptionBody>erases an element after an element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
