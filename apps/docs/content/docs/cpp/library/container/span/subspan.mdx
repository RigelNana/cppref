---
title: "std::span<T,Extent>::subspan"
description: "Obtains a span that is a view over the Count elements of this span starting at offset Offset."
source_url: "https://en.cppreference.com/w/cpp/container/span/subspan"
language: "C++"
---

{/* source:cpp-container-span-subspan:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`template< std::size_t Offset,
          std::size_t Count = std::dynamic_extent >
constexpr std::span<element_type, E /* see below */>

    subspan() const;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::span<element_type, std::dynamic_extent>
    subspan( size_type Offset,

              size_type Count = std::dynamic_extent ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-span-subspan:0001 */}
Obtains a span that is a view over the `Count` elements of this span starting at offset `Offset`. If `Count` is `std::dynamic_extent`, the number of elements in the subspan is `size() - offset` (i.e., it ends at the end of `*this`).

{/* source:cpp-container-span-subspan:0002 */}
1) Is ill-formed if

- `Offset` is greater than `Extent`, or
- `Count` is not `std::dynamic_extent` and `Count` is greater than `Extent - Offset`.

{/* source:cpp-container-span-subspan:0003 */}
The behavior is undefined if either `Offset` or `Count` is out of range. This happens if

{/* source:cpp-container-span-subspan:0004 */}
- `Offset` is greater than `size()`, or
- `Count` is not `std::dynamic_extent` and `Count` is greater than `size() - Offset`.

{/* source:cpp-container-span-subspan:0005 */}
The extent `E` of the span returned by (1) is determined as follows:

{/* source:cpp-container-span-subspan:0006 */}
- If `Count` is not `std::dynamic_extent`, `Count`;
- Otherwise, if `Extent` is not `std::dynamic_extent`, `Extent - Offset`;
- Otherwise, `std::dynamic_extent`.

## Return value

{/* source:cpp-container-span-subspan:0007 */}
The requested subspan `r`, such that `r.data() == this->data() + Offset`. If `Count` is `std::dynamic_extent`, `r.size() == this->size() - Offset`; otherwise `r.size() == Count`.

## Example

{/* source:cpp-container-span-subspan:0008 */}
```cpp
#include <algorithm>
#include <cstdio>
#include <numeric>
#include <ranges>
#include <span>

void display(std::span<const char> abc)
{
    const auto columns{20U};
    const auto rows{abc.size() - columns + 1};

    for (auto offset{0U}; offset < rows; ++offset)
    {
        std::ranges::for_each(abc.subspan(offset, columns), std::putchar);
        std::putchar('\n');
    }
}

int main()
{
    char abc[26];
    std::iota(std::begin(abc), std::end(abc), 'A');
    display(abc);
}
```

Output:

```text
ABCDEFGHIJKLMNOPQRST
BCDEFGHIJKLMNOPQRSTU
CDEFGHIJKLMNOPQRSTUV
DEFGHIJKLMNOPQRSTUVW
EFGHIJKLMNOPQRSTUVWX
FGHIJKLMNOPQRSTUVWXY
GHIJKLMNOPQRSTUVWXYZ
```

## See also

{/* source:cpp-container-span-subspan:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`first`](/docs/cpp/library/container/span/first)</DescriptionTerm>
    <DescriptionBody>obtains a subspan consisting of the first `N` elements of the sequence (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`last`](/docs/cpp/library/container/span/last)</DescriptionTerm>
    <DescriptionBody>obtains a subspan consisting of the last `N` elements of the sequence (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
