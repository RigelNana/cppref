---
title: "std::span<T,Extent>::first"
description: "Obtains a span that is a view over the first Count elements of this span."
source_url: "https://en.cppreference.com/w/cpp/container/span/first"
language: "C++"
---

{/* source:cpp-container-span-first:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< std::size_t Count >
constexpr std::span<element_type, Count> first() const;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::span<element_type, std::dynamic_extent> first( size_type Count ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-span-first:0001 */}
Obtains a span that is a view over the first `Count` elements of this span. The program is ill-formed if `Count > Extent`. The behavior is undefined if `Count > size()`.

## Return value

{/* source:cpp-container-span-first:0002 */}
A span `r` that is a view over the first `Count` elements of `*this`, such that `r.data() == this->data() && r.size() == Count`.

## Example

{/* source:cpp-container-span-first:0003 */}
```cpp
#include <iostream>
#include <ranges>
#include <span>
#include <string_view>

void print(std::string_view const title,
           std::ranges::forward_range auto const& container)
{
    auto size{std::size(container)};
    std::cout << title << '[' << size << "]{";
    for (auto const& elem : container)
        std::cout << elem << (--size ? ", " : "");
    std::cout << "};\n";
}

void run_game(std::span<const int> span)
{
    print("span: ", span);

    std::span<const int, 5> span_first = span.first<5>();
    print("span.first<5>(): ", span_first);

    std::span<const int, std::dynamic_extent> span_first_dynamic = span.first(4);
    print("span.first(4): ", span_first_dynamic);
}

int main()
{
    int a[8]{1, 2, 3, 4, 5, 6, 7, 8};
    print("int a", a);
    run_game(a);
}
```

Output:

```text
int a[8]{1, 2, 3, 4, 5, 6, 7, 8};
span: [8]{1, 2, 3, 4, 5, 6, 7, 8};
span.first<5>(): [5]{1, 2, 3, 4, 5};
span.first(4): [4]{1, 2, 3, 4};
```

## See also

{/* source:cpp-container-span-first:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`last`](/docs/cpp/library/container/span/last)</DescriptionTerm>
    <DescriptionBody>obtains a subspan consisting of the last `N` elements of the sequence (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`subspan`](/docs/cpp/library/container/span/subspan)</DescriptionTerm>
    <DescriptionBody>obtains a subspan (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
