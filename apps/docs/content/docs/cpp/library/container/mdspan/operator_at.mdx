---
title: "std::mdspan<T,Extents,LayoutPolicy,AccessorPolicy>::operator[]"
description: "Returns a reference to the indices-th element of the mdspan."
source_url: "https://en.cppreference.com/w/cpp/container/mdspan/operator_at"
language: "C++"
---

{/* source:cpp-container-mdspan-operator-at:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`template< class... OtherIndexTypes >
constexpr reference operator[]( OtherIndexTypes... indices ) const;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`template< class OtherIndexType >
constexpr reference operator[]

    ( std::span<OtherIndexType, rank()> indices ) const;`} />
  <Declaration id="3" language="cpp" since="C++23" code={`template< class OtherIndexType >
constexpr reference operator[]

    ( const std::array<OtherIndexType, rank()>& indices ) const;`} />
</DeclarationDoc>

{/* source:cpp-container-mdspan-operator-at:0001 */}
Returns a reference to the `indices`-th element of the `mdspan`.

{/* source:cpp-container-mdspan-operator-at:0002 */}
1. Equivalent to `return acc_.access(ptr_, map_(static_cast<index_type>(std::move(indices))...));`.

{/* source:cpp-container-mdspan-operator-at:0003 */}
This overload participates in overload resolution only if:

- `(std::is_convertible_v<OtherIndexTypes, index_type> && ...)` is `true`,
- `(std::is_nothrow_constructible_v<index_type, OtherIndexTypes> && ...)` is `true`, and
- `sizeof...(OtherIndexTypes) == rank()` is `true`.

{/* source:cpp-container-mdspan-operator-at:0004 */}
Let `I` be `extents_type::`*`index-cast`*`(std::move(indices))`. Then the behavior is undefined if `I` is not a multidimensional index in `extents()`, i.e., `map_(I) < map_.required_span_size()` is `false`.

{/* source:cpp-container-mdspan-operator-at:0005 */}
2,3. Let `P` be a parameter pack such that `std::is_same_v<make_index_sequence<rank()>, index_sequence<P...>>` is `true`, then the operator is equivalent to `return operator[](extents_type::`*`index-cast`*`(std::as_const(indices[P]))...);`.

{/* source:cpp-container-mdspan-operator-at:0006 */}
This overload participates in overload resolution only if:

- `std::is_convertible_v<const OtherIndexType&, index_type>` is `true`, and
- `std::is_nothrow_constructible_v<index_type, const OtherIndexType&>` is `true`.

## Parameters

{/* source:cpp-container-mdspan-operator-at:0007 */}
<ParameterList>
  <Parameter name="indices">the indices of the element to access</Parameter>
</ParameterList>

## Return value

{/* source:cpp-container-mdspan-operator-at:0008 */}
A reference to the element.

## Example

{/* source:cpp-container-mdspan-operator-at:0009 */}
This section is incomplete

Reason: no example

## Defect reports

{/* source:cpp-container-mdspan-operator-at:0010 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-container-mdspan-operator-at:0011 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3974} standard="C++23">
    <PublishedBehavior>overloads (2,3) did not apply `extents_type::`*`index-cast`*</PublishedBehavior>
    <CorrectedBehavior>applies</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

## See also

{/* source:cpp-container-mdspan-operator-at:0012 */}
This section is incomplete
