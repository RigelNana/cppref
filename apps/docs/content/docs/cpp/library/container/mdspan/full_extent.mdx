---
title: "std::full_extent, std::full_extent_t"
description: "The class std::full_extent_t is a slice specifier type that can be used in std::submdspan."
source_url: "https://en.cppreference.com/w/cpp/container/mdspan/full_extent"
language: "C++"
---

{/* source:cpp-container-mdspan-full-extent:0000 */}
<DeclarationDoc>
Defined in header [`<mdspan>`](/docs/cpp/library/headers/mdspan)

  <Declaration id="1" language="cpp" since="C++26" code={`struct full_extent_t { explicit full_extent_t() = default; };`} />
  <Declaration id="2" language="cpp" since="C++26" code={`inline constexpr std::full_extent_t full_extent {};`} />
</DeclarationDoc>

{/* source:cpp-container-mdspan-full-extent:0001 */}
1. The class `std::full_extent_t` is a slice specifier type that can be used in `std::submdspan`.

{/* source:cpp-container-mdspan-full-extent:0002 */}
2. The corresponding `std::full_extent` instance of (1) is a slice specifier to indicate full range of indices in the specified extent in `std::submdspan`.

## Example

{/* source:cpp-container-mdspan-full-extent:0003 */}
```cpp
#include <mdspan>
#include <print>
 
void print(auto view)
{
    static_assert(view.rank() <= 2);
 
    if constexpr (view.rank() == 2)
    {
        for (std::size_t i = 0; i != view.extent(0); ++i)
        {
            for (std::size_t j = 0; j != view.extent(1); ++j)
                std::print("{} ", view[i, j]);
            std::println();
        }
    }
    else if constexpr (view.rank() == 1)
    {
        for (std::size_t i = 0; i != view.extent(0); ++i)
            std::print("{} ", view[i]);
        std::println();
    }
    else
        std::println("{}", view[]);
 
    std::println();
}
 
int main()
{
    const char letters []{'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I'};
    const std::mdspan view(letters, 3, 3);
 
    print(view);
    print(std::submdspan(view, std::full_extent, std::full_extent));
    print(std::submdspan(view, std::full_extent, 1));
    print(std::submdspan(view, 1, std::full_extent));
    print(std::submdspan(view, 2, 1));
}
```

Possible output:

```text
A B C
D E F
G H I
 
A B C
D E F
G H I
 
B E H
 
D E F
 
H
```

## See also

{/* source:cpp-container-mdspan-full-extent:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`submdspan`](https://en.cppreference.com/mwiki/index.php?title=cpp/container/mdspan/submdspan&action=edit&redlink=1) <InlineRevision since="C++26" /></DescriptionTerm>
    <DescriptionBody>returns a view of a subset of an existing `mdspan` (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
