---
title: "std::runtime_error"
description: "Defines a type of object to be thrown as exception. It reports errors that are due to events beyond the scope of the program and cannot be easily predicted."
source_url: "https://en.cppreference.com/w/cpp/error/runtime_error"
language: "C++"
---

{/* source:cpp-error-runtime-error:0000 */}
<DeclarationDoc>
Defined in header [`<stdexcept>`](/docs/cpp/library/headers/stdexcept)

  <Declaration language="cpp" code={`class runtime_error;`} />
</DeclarationDoc>

{/* source:cpp-error-runtime-error:0001 */}
Defines a type of object to be thrown as exception. It reports errors that are due to events beyond the scope of the program and cannot be easily predicted.

{/* source:cpp-error-runtime-error:0002 */}
Inheritance diagram

{/* source:cpp-error-runtime-error:0003 */}
Exceptions of type `std::runtime_error` are thrown by the following standard library components:

{/* source:cpp-error-runtime-error:0004 */}
- [`std::locale::locale`](/docs/cpp/library/text/locale/locale/locale)
- [`std::locale::combine`](/docs/cpp/library/text/locale/locale/combine)

{/* source:cpp-error-runtime-error:0005 */}
<Revision since="C++20">
- `std::chrono::current_zone`
- `std::chrono::get_tzdb_list`
- `std::chrono::get_tzdb`
- `std::chrono::locate_zone`
- `std::chrono::reload_tzdb`
- `std::chrono::remote_version`
- `std::chrono::tzdb::locate_zone`
</Revision>

{/* source:cpp-error-runtime-error:0006 */}
In addition, the following standard exception types are derived from `std::runtime_error`:

{/* source:cpp-error-runtime-error:0007 */}
- [`std::range_error`](/docs/cpp/library/error/range_error)
- [`std::overflow_error`](/docs/cpp/library/error/overflow_error)
- [`std::underflow_error`](/docs/cpp/library/error/underflow_error)

{/* source:cpp-error-runtime-error:0008 */}
<Revision since="C++11">
- [`std::ios_base::failure`](/docs/cpp/library/io/ios_base/failure)
- [`std::regex_error`](/docs/cpp/library/text/regex/regex_error)
- [`std::system_error`](/docs/cpp/library/error/system_error)
</Revision>

{/* source:cpp-error-runtime-error:0009 */}
<Revision since="C++17">
- [`std::filesystem::filesystem_error`](/docs/cpp/library/filesystem/filesystem_error)
</Revision>

{/* source:cpp-error-runtime-error:0010 */}
<Revision since="C++20">
- `std::chrono::ambiguous_local_time`
- `std::chrono::nonexistent_local_time`
- `std::format_error`
</Revision>

### Member functions

{/* source:cpp-error-runtime-error:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`(constructor)`</DescriptionTerm>
    <DescriptionBody>constructs a new `runtime_error` object with the given message <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator=`</DescriptionTerm>
    <DescriptionBody>replaces the `runtime_error` object <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-error-runtime-error:0012 */}
#### `std::runtime_error::runtime_error`

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`runtime_error( const std::string& what_arg );`} />
  <Declaration id="2" language="cpp" code={`runtime_error( const char* what_arg );`} />
  <Declaration id="3" language="cpp" code={`runtime_error( const runtime_error& other );`} /> (noexcept since C++11)

  <DeclarationDescription>
    1. Constructs the exception object with `what_arg` as explanatory string. After construction, `std::strcmp(what(), what_arg.c_str()) == 0`.
    2. Constructs the exception object with `what_arg` as explanatory string. After construction, `std::strcmp(what(), what_arg) == 0`.
    3. Copy constructor. If `*this` and `other` both have dynamic type `std::runtime_error` then `std::strcmp(what(), other.what()) == 0`. No exception can be thrown from the copy constructor.
  </DeclarationDescription>
</DeclarationDoc>

##### Parameters

<ParameterList>
  <Parameter name="what_arg">explanatory string</Parameter>
  <Parameter name="other">another exception object to copy</Parameter>
</ParameterList>

##### Exceptions

1,2) May throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc).

##### Notes

Because copying `std::runtime_error` is not permitted to throw exceptions, this message is typically stored internally as a separately-allocated reference-counted string. This is also why there is no constructor taking `std::string&&`: it would have to copy the content anyway.

Before the resolution of [LWG issue 254](https://cplusplus.github.io/LWG/issue254), the non-copy constructor can only accept [`std::string`](/docs/cpp/library/string/basic_string). It makes dynamic allocation mandatory in order to construct a [`std::string`](/docs/cpp/library/string/basic_string) object.

After the resolution of [LWG issue 471](https://cplusplus.github.io/LWG/issue471), a derived standard exception class must have a publicly accessible copy constructor. It can be implicitly defined as long as the explanatory strings obtained by `what()` are the same for the original object and the copied object.

{/* source:cpp-error-runtime-error:0013 */}
#### `std::runtime_error::operator=`

<DeclarationDoc>
  <Declaration language="cpp" code={`runtime_error& operator=( const runtime_error& other );`} /> (noexcept since C++11)
</DeclarationDoc>

Assigns the contents with those of `other`. If `*this` and `other` both have dynamic type `std::runtime_error` then `std::strcmp(what(), other.what()) == 0` after assignment. No exception can be thrown from the copy assignment operator.

##### Parameters

<ParameterList>
  <Parameter name="other">another exception object to assign with</Parameter>
</ParameterList>

##### Return value

`*this`

##### Notes

After the resolution of [LWG issue 471](https://cplusplus.github.io/LWG/issue471), a derived standard exception class must have a publicly accessible copy assignment operator. It can be implicitly defined as long as the explanatory strings obtained by `what()` are the same for the original object and the copied object.

{/* source:cpp-error-runtime-error:0014 */}
## Inherited from [`std::exception`](/docs/cpp/library/error/exception)

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(destructor)`](/docs/cpp/library/error/exception/~exception) [virtual]</DescriptionTerm>
    <DescriptionBody>destroys the exception object <br /> (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`what`](/docs/cpp/library/error/exception/what) [virtual]</DescriptionTerm>
    <DescriptionBody>returns an explanatory string <br /> (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Defect reports

{/* source:cpp-error-runtime-error:0015 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-error-runtime-error:0016 */}
<DefectReportList>
  <DefectReport kind="lwg" id={254} standard="C++98">
    <PublishedBehavior>the constructor accepting `const char*` was missing</PublishedBehavior>
    <CorrectedBehavior>added</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={471} standard="C++98">
    <PublishedBehavior>the explanatory strings of `std::runtime_error`'s<br />copies were implementation-defined</PublishedBehavior>
    <CorrectedBehavior>they are the same as that of the<br />original `std::runtime_error` object</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
