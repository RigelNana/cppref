---
title: "std::ratio_add"
description: "The alias template std::ratio_add denotes the result of adding two exact rational fractions represented by the std::ratio specializations R1 and R2."
source_url: "https://en.cppreference.com/w/cpp/numeric/ratio/ratio_add"
language: "C++"
---

{/* source:cpp-numeric-ratio-ratio-add:0000 */}
<DeclarationDoc>
Defined in header [`<ratio>`](/docs/cpp/library/headers/ratio)

  <Declaration language="cpp" since="C++11" code={`template< class R1, class R2 >
using ratio_add = /* see below */;`} />
</DeclarationDoc>

{/* source:cpp-numeric-ratio-ratio-add:0001 */}
The alias template `std::ratio_add` denotes the result of adding two exact rational fractions represented by the [`std::ratio`](/docs/cpp/library/meta/ratio/ratio) specializations `R1` and `R2`.

{/* source:cpp-numeric-ratio-ratio-add:0002 */}
The result is a [`std::ratio`](/docs/cpp/library/meta/ratio/ratio) specialization `std::ratio<U, V>`, such that given `Num == R1::num * R2::den + R2::num * R1::den` and `Denom == R1::den * R2::den` (computed without arithmetic overflow), `U` is `std::ratio<Num, Denom>::num` and `V` is `std::ratio<Num, Denom>::den`.

### Notes

{/* source:cpp-numeric-ratio-ratio-add:0003 */}
If `U` or `V` is not representable in [`std::intmax_t`](/docs/cpp/library/utility/types/integer), the program is ill-formed. If `Num` or `Denom` is not representable in [`std::intmax_t`](/docs/cpp/library/utility/types/integer), the program is ill-formed unless the implementation yields correct values for `U` and `V`.

{/* source:cpp-numeric-ratio-ratio-add:0004 */}
The above definition requires that the result of `std::ratio_add<R1, R2>` be already reduced to lowest terms; for example, `std::ratio_add<std::ratio<1, 3>, std::ratio<1, 6>>` is the same type as `std::ratio<1, 2>`.

### Example

{/* source:cpp-numeric-ratio-ratio-add:0005 */}
```cpp
#include <iostream>
#include <ratio>

int main()
{
    using two_third = std::ratio<2, 3>;
    using one_sixth = std::ratio<1, 6>;
    using sum = std::ratio_add<two_third, one_sixth>;

    std::cout << "2/3 + 1/6 = " << sum::num << '/' << sum::den << '\n';
}
```

Output:

```text
2/3 + 1/6 = 5/6
```

### See also

{/* source:cpp-numeric-ratio-ratio-add:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ratio_subtract`](/docs/cpp/library/meta/ratio/ratio_subtract)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>subtracts two `ratio` objects at compile-time (alias template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
