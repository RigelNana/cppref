---
title: "std::is_empty"
description: "std::is_empty is a UnaryTypeTrait."
source_url: "https://en.cppreference.com/w/cpp/types/is_empty"
language: "C++"
---

{/* source:cpp-types-is-empty:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct is_empty;`} />
</DeclarationDoc>

{/* source:cpp-types-is-empty:0001 */}
`std::is_empty` is a [UnaryTypeTrait](/docs/cpp/library/named_req/UnaryTypeTrait).

{/* source:cpp-types-is-empty:0002 */}
If `T` is an empty type (that is, a non-union class type with no non-static data members other than bit-fields of size 0, no virtual functions, no virtual base classes, and no non-empty base classes), provides the member constant `value` equal to `true`. For any other type, `value` is `false`.

{/* source:cpp-types-is-empty:0003 */}
If `T` is an incomplete non-union class type, the behavior is undefined.

{/* source:cpp-types-is-empty:0004 */}
If the program adds specializations for `std::is_empty` or `std::is_empty_v`, the behavior is undefined.

### Template parameters

{/* source:cpp-types-is-empty:0005 */}
<ParameterList>
  <Parameter name="T">a type to check</Parameter>
</ParameterList>

### Helper variable template

{/* source:cpp-types-is-empty:0006 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
constexpr bool is_empty_v = is_empty<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-empty:0007 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is an empty class type, `false` otherwise <br /> (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>Type</DescriptionTerm>
    <DescriptionBody>Definition</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Notes

{/* source:cpp-types-is-empty:0008 */}
Inheriting from empty base classes usually does not increase the size of a class due to [empty base optimization](/docs/cpp/language/classes/ebo).

{/* source:cpp-types-is-empty:0009 */}
`std::is_empty<T>` and all other type traits are empty classes.

### Example

{/* source:cpp-types-is-empty:0010 */}
```cpp
#include <iostream>
#include <type_traits>

struct A {};
static_assert(std::is_empty_v<A> == true);

struct B { int m; };
static_assert(std::is_empty_v<B> == false);

struct C { static int m; };
static_assert(std::is_empty_v<C> == true);

struct D { virtual ~D(); };
static_assert(std::is_empty_v<D> == false);

union E {};
static_assert(std::is_empty_v<E> == false);

struct F { [[no_unique_address]] E e; };

struct G
{
    int:0;
    // C++ standard allow "as a special case, an unnamed bit-field with a width of zero
    // specifies alignment of the next bit-field at an allocation unit boundary.
    // Only when declaring an unnamed bit-field may the width be zero."
};
static_assert(std::is_empty_v<G>); // holds only unnamed bit-fields of zero width

int main()
{
    std::cout << std::boolalpha;
    std::cout << "F: " << std::is_empty_v<F> << '\n'; // the result is ABI-dependent
}
```

Possible output:

```text
F: true
```

### Defect reports

{/* source:cpp-types-is-empty:0011 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-is-empty:0012 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2015} standard="C++11">
    <PublishedBehavior>the behavior was undefined if `T` is an incomplete union type</PublishedBehavior>
    <CorrectedBehavior>the base characteristic is [`std::false_type`](/docs/cpp/library/meta/integral_constant) in this case</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-is-empty:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_class`](/docs/cpp/library/meta/is_class)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a non-union class type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
