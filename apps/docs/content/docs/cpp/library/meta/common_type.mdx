---
title: "std::common_type"
description: "Determines the common type among all types T..., that is the type all T... can be implicitly converted to."
source_url: "https://en.cppreference.com/w/cpp/types/common_type"
language: "C++"
---

{/* source:cpp-types-common-type:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class... T >
struct common_type;`} />

  <DeclarationDescription>
    {/* source:cpp-types-common-type:0001 */}
    Determines the common type among all types `T...`, that is the type all `T...` can be implicitly converted to. If such a type exists (as determined according to the rules below), the member type names that type. Otherwise, there is no member type.

    {/* source:cpp-types-common-type:0002 */}
    - If `sizeof...(T)` is zero, there is no member type.
    - If `sizeof...(T)` is one (i.e., `T...` contains only one type `T0`), the member type names the same type as `std::common_type<T0, T0>::type` if it exists; otherwise there is no member type.
    - If `sizeof...(T)` is two (i.e., `T...` contains exactly two types `T1` and `T2`),

    {/* source:cpp-types-common-type:0003 */}
    - If applying [`std::decay`](/docs/cpp/library/meta/decay) to at least one of `T1` and `T2` produces a different type, the member type names the same type as `std::common_type<std::decay<T1>::type, std::decay<T2>::type>::type`, if it exists; if not, there is no member type;
    - Otherwise, if there is a user specialization for `std::common_type<T1, T2>`, that specialization is used;
    - Otherwise, if `std::decay<decltype(false ? std::declval<T1>() : std::declval<T2>())>::type` is a valid type, the member type denotes that type, see [the conditional operator](/docs/cpp/language/expressions/operator_other#Conditional_operator);

    {/* source:cpp-types-common-type:0004 */}
    <Revision since="C++20">
    - Otherwise, if `std::decay<decltype(false ? std::declval<CR1>() : std::declval<CR2>())>::type` is a valid type, where `CR1` and `CR2` are `const std::remove_reference_t<T1>&` and `const std::remove_reference_t<T2>&` respectively, the member type denotes that type;
    </Revision>

    {/* source:cpp-types-common-type:0005 */}
    - Otherwise, there is no member type.

    {/* source:cpp-types-common-type:0006 */}
    - If `sizeof...(T)` is greater than two (i.e., `T...` consists of the types `T1, T2, R...`), then if `std::common_type<T1, T2>::type` exists, the member type denotes `std::common_type<typename std::common_type<T1, T2>::type, R...>::type` if such a type exists.
    - In all other cases, there is no member type.

    {/* source:cpp-types-common-type:0007 */}
    If any type in the parameter pack `T` is not a complete type, (possibly cv-qualified) `void`, or an array of unknown bound, the behavior is undefined.

    {/* source:cpp-types-common-type:0008 */}
    If an instantiation of a template above depends, directly or indirectly, on an incomplete type, and that instantiation could yield a different result if that type were hypothetically completed, the behavior is undefined.
  </DeclarationDescription>
</DeclarationDoc>

### Nested types

{/* source:cpp-types-common-type:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>the common type for all `T`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-types-common-type:0010 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++14" code={`template< class... T >
using common_type_t = typename common_type<T...>::type;`} />
  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

### Specializations

{/* source:cpp-types-common-type:0011 */}
Users may specialize `common_type` for types `T1` and `T2` if

{/* source:cpp-types-common-type:0012 */}
- At least one of `T1` and `T2` depends on a user-defined type, and [`std::decay`](/docs/cpp/library/meta/decay) is an identity transformation for both `T1` and `T2`.

{/* source:cpp-types-common-type:0013 */}
If such a specialization has a member named `type`, it must be a public and unambiguous member that names a cv-unqualified non-reference type to which both `T1` and `T2` are explicitly convertible. Additionally, `std::common_type<T1, T2>::type` and `std::common_type<T2, T1>::type` must denote the same type.

{/* source:cpp-types-common-type:0014 */}
A program that adds `common_type` specializations in violation of these rules has undefined behavior.

{/* source:cpp-types-common-type:0015 */}
Note that the behavior of a program that adds a specialization to any other template <InlineRevision since="C++20">(except for [`std::basic_common_reference`](/docs/cpp/library/meta/common_reference))</InlineRevision> from `<type_traits>` is undefined.

{/* source:cpp-types-common-type:0016 */}
The following specializations are already provided by the standard library:

{/* source:cpp-types-common-type:0017 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::common_type<std::chrono::duration>`](/docs/cpp/library/chrono/duration/common_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specializes the `std::common_type` trait <br /> (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::common_type<std::chrono::time_point>`](/docs/cpp/library/chrono/time_point/common_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specializes the `std::common_type` trait <br /> (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::common_type<std::pair>`](/docs/cpp/library/utility/pair/common_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>determines the common type of two pairs <br /> (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::common_type<tuple-like>`](/docs/cpp/library/utility/tuple/common_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>determines the common type of a tuple and a tuple-like type <br /> (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::common_type<std::basic_const_iterator>`](/docs/cpp/library/iterator/basic_const_iterator/common_type)<InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>determines the common type of an iterator and an adapted `basic_const_iterator` type <br /> (class template specialization)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-common-type:0018 */}
```cpp
// primary template (used for zero types)
template<class...>
struct common_type {};
 
// one type
template<class T>
struct common_type<T> : common_type<T, T> {};
 
namespace detail
{
    template<class...>
    using void_t = void;
 
    template<class T1, class T2>
    using conditional_result_t = decltype(false ? std::declval<T1>() : std::declval<T2>());
 
    template<class, class, class = void>
    struct decay_conditional_result {};
    template<class T1, class T2>
    struct decay_conditional_result<T1, T2, void_t<conditional_result_t<T1, T2>>>
        : std::decay<conditional_result_t<T1, T2>> {};
 
    template<class T1, class T2, class = void>
    struct common_type_2_impl : decay_conditional_result<const T1&, const T2&> {};
 
    // C++11 implementation:
    // template<class, class, class = void>
    // struct common_type_2_impl {};
 
    template<class T1, class T2>
    struct common_type_2_impl<T1, T2, void_t<conditional_result_t<T1, T2>>>
        : decay_conditional_result<T1, T2> {};
}
 
// two types
template<class T1, class T2>
struct common_type<T1, T2> 
    : std::conditional<std::is_same<T1, typename std::decay<T1>::type>::value &&
                       std::is_same<T2, typename std::decay<T2>::type>::value,
                       detail::common_type_2_impl<T1, T2>,
                       common_type<typename std::decay<T1>::type,
                                   typename std::decay<T2>::type>>::type {};
 
// 3+ types
namespace detail
{
    template<class AlwaysVoid, class T1, class T2, class... R>
    struct common_type_multi_impl {};
    template<class T1, class T2, class...R>
    struct common_type_multi_impl<void_t<typename common_type<T1, T2>::type>, T1, T2, R...>
        : common_type<typename common_type<T1, T2>::type, R...> {};
}
 
template<class T1, class T2, class... R>
struct common_type<T1, T2, R...>
    : detail::common_type_multi_impl<void, T1, T2, R...> {};
```

### Notes

{/* source:cpp-types-common-type:0019 */}
For arithmetic types not subject to promotion, the common type may be viewed as the type of the (possibly mixed-mode) arithmetic expression such as `T0() + T1() + ... + Tn()`.

### Examples

{/* source:cpp-types-common-type:0020 */}
Demonstrates mixed-mode arithmetic on a program-defined class:

```cpp
#include <iostream>
#include <type_traits>
 
template<class T>
struct Number { T n; };
 
template<class T, class U>
constexpr Number<std::common_type_t<T, U>>
    operator+(const Number<T>& lhs, const Number<U>& rhs)
{
    return {lhs.n + rhs.n};
}
 
void describe(const char* expr, const Number<int>& x)
{
    std::cout << expr << "  is  Number<int>{" << x.n << "}\n";
}
 
void describe(const char* expr, const Number<double>& x)
{
    std::cout << expr << "  is  Number<double>{" << x.n << "}\n";
}
 
int main()
{
    Number<int> i1 = {1}, i2 = {2};
    Number<double> d1 = {2.3}, d2 = {3.5};
    describe("i1 + i2", i1 + i2);
    describe("i1 + d2", i1 + d2);
    describe("d1 + i2", d1 + i2);
    describe("d1 + d2", d1 + d2);
}
```

Output:

```text
i1 + i2  is  Number<int>{3}
i1 + d2  is  Number<double>{4.5}
d1 + i2  is  Number<double>{4.3}
d1 + d2  is  Number<double>{5.8}
```

### Defect reports

{/* source:cpp-types-common-type:0021 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-common-type:0022 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2141} standard="C++11">
    <PublishedBehavior>the result type of the conditional operator was not decayed</PublishedBehavior>
    <CorrectedBehavior>decayed the result type</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2408} standard="C++11">
    <PublishedBehavior>`common_type` was not SFINAE-friendly</PublishedBehavior>
    <CorrectedBehavior>made SFINAE-friendly</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2460} standard="C++11">
    <PublishedBehavior>`common_type` specializations were nearly impossible to write</PublishedBehavior>
    <CorrectedBehavior>reduced the number of <br /> specializations needed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-common-type:0023 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`common_with`](/docs/cpp/library/concepts/common_with)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that two types share a common type <br /> (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
