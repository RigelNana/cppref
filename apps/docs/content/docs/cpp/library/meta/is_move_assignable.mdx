---
title: "std::is_move_assignable, std::is_trivially_move_assignable, std::is_nothrow_move_assignable"
description: "The value of the member constant value is std::is_assignable<T&, T&&>::value if T is a referenceable type, and false if T is not a referenceable type."
source_url: "https://en.cppreference.com/w/cpp/types/is_move_assignable"
language: "C++"
---

{/* source:cpp-types-is-move-assignable:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration id="1" language="cpp" since="C++11" code={`template< class T >
struct is_move_assignable;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class T >
struct is_trivially_move_assignable;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< class T >
struct is_nothrow_move_assignable;`} />
</DeclarationDoc>

{/* source:cpp-types-is-move-assignable:0001 */}
| Type trait | The value of the member constant `value` | |
| --- | --- | --- |
| | `T` is a [referenceable type](/docs/cpp/library/meta#Definitions) | `T` is not a referenceable type |
| (1) | `std::is_assignable<T&, T&&>::value` | `false` |
| (2) | `std::is_trivially_assignable<T&, T&&>::value` | `false` |
| (3) | `std::is_nothrow_assignable<T&, T&&>::value` | `false` |

{/* source:cpp-types-is-move-assignable:0002 */}
If `T` is not a complete type, (possibly cv-qualified) `void`, or an array of unknown bound, the behavior is undefined.

{/* source:cpp-types-is-move-assignable:0003 */}
If an instantiation of a template above depends, directly or indirectly, on an incomplete type, and that instantiation could yield a different result if that type were hypothetically completed, the behavior is undefined.

{/* source:cpp-types-is-move-assignable:0004 */}
If the program adds specializations for any of the templates described on this page, the behavior is undefined.

### Helper variable templates

{/* source:cpp-types-is-move-assignable:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
inline constexpr bool is_move_assignable_v =
    is_move_assignable<T>::value;`} />
  <Declaration language="cpp" since="C++17" code={`template< class T >
inline constexpr bool is_trivially_move_assignable_v =
    is_trivially_move_assignable<T>::value;`} />
  <Declaration language="cpp" since="C++17" code={`template< class T >
inline constexpr bool is_nothrow_move_assignable_v =
    is_nothrow_move_assignable<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-move-assignable:0006 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is move-assignable, `false` otherwise (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-is-move-assignable:0007 */}
```cpp
template<class T>
struct is_move_assignable
    : std::is_assignable<typename std::add_lvalue_reference<T>::type,
                         typename std::add_rvalue_reference<T>::type> {};

template<class T>
struct is_trivially_move_assignable
    : std::is_trivially_assignable<typename std::add_lvalue_reference<T>::type,
                                   typename std::add_rvalue_reference<T>::type> {};

template<class T>
struct is_nothrow_move_assignable
    : std::is_nothrow_assignable<typename std::add_lvalue_reference<T>::type,
                                 typename std::add_rvalue_reference<T>::type> {};
```

### Notes

{/* source:cpp-types-is-move-assignable:0008 */}
The trait `std::is_move_assignable` is less strict than [MoveAssignable](/docs/cpp/library/named_req/MoveAssignable) because it does not check the type of the result of the assignment (which, for a [MoveAssignable](/docs/cpp/library/named_req/MoveAssignable) type, must be `T&`), nor the semantic requirement that the target's value after the assignment is equivalent to the source's value before the assignment.

{/* source:cpp-types-is-move-assignable:0009 */}
The type does not have to implement a [move assignment operator](/docs/cpp/language/classes/move_operator) in order to satisfy this trait; see [MoveAssignable](/docs/cpp/library/named_req/MoveAssignable) for details.

### Example

{/* source:cpp-types-is-move-assignable:0010 */}
```cpp
#include <iostream>
#include <string>
#include <type_traits>

struct Foo { int n; };

struct NoMove
{
    // prevents implicit declaration of default move assignment operator
    // however, the class is still move-assignable because its
    // copy assignment operator can bind to an rvalue argument
    NoMove& operator=(const NoMove&) { return *this; }
};

int main()
{
    std::cout << std::boolalpha
              << "std::string is nothrow move-assignable? "
              << std::is_nothrow_move_assignable<std::string>::value << '\n'
              << "int[2] is move-assignable? "
              << std::is_move_assignable<int[2]>::value << '\n'
              << "Foo is trivially move-assignable? "
              << std::is_trivially_move_assignable<Foo>::value << '\n'
              << "NoMove is move-assignable? "
              << std::is_move_assignable<NoMove>::value << '\n'
              << "NoMove is nothrow move-assignable? "
              << std::is_nothrow_move_assignable<NoMove>::value << '\n';
}
```

Output:

```text
std::string is nothrow move-assignable? true
int[2] is move-assignable? false
Foo is trivially move-assignable? true
NoMove is move-assignable? true
NoMove is nothrow move-assignable? false
```

### Defect reports

{/* source:cpp-types-is-move-assignable:0011 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-is-move-assignable:0012 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2196} standard="C++11">
    <PublishedBehavior>the behavior was unclear if `T&&` cannot be formed</PublishedBehavior>
    <CorrectedBehavior>the value produced is `false` in this case</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-is-move-assignable:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_assignable`, `is_trivially_assignable`, `is_nothrow_assignable`](/docs/cpp/library/meta/is_assignable)<InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type has an assignment operator for a specific argument (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_copy_assignable`, `is_trivially_copy_assignable`, `is_nothrow_copy_assignable`](/docs/cpp/library/meta/is_copy_assignable)<InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type has a copy assignment operator (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
