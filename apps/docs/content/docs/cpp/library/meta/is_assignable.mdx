---
title: "std::is_assignable, std::is_trivially_assignable, std::is_nothrow_assignable"
description: "If the expression std::declval<T>() = std::declval<U>() is well-formed in unevaluated context, provides the member constant value equal to true. Otherwise, value is false."
source_url: "https://en.cppreference.com/w/cpp/types/is_assignable"
language: "C++"
---

{/* source:cpp-types-is-assignable:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration id="1" language="cpp" since="C++11" code={`template< class T, class U >
struct is_assignable;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class T, class U >
struct is_trivially_assignable;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< class T, class U >
struct is_nothrow_assignable;`} />

  <DeclarationDescription>
    {/* source:cpp-types-is-assignable:0001 */}
    1. If the expression `std::declval<T>() = std::declval<U>()` is well-formed in unevaluated context, provides the member constant `value` equal to `true`. Otherwise, `value` is `false`. [Access checks](/docs/cpp/language/classes/access) are performed as if from a context unrelated to either type.

    {/* source:cpp-types-is-assignable:0002 */}
    2. Same as (1), but the evaluation of the assignment expression will not call any operation that is not trivial. For the purposes of this check, a call to [`std::declval`](/docs/cpp/library/utility/declval) is considered trivial and not considered an [odr-use](/docs/cpp/language/basic_concepts/definition#ODR-use) of [`std::declval`](/docs/cpp/library/utility/declval).

    {/* source:cpp-types-is-assignable:0003 */}
    3. Same as (1), but the evaluation of the assignment expression will not call any operation that is not noexcept.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-types-is-assignable:0004 */}
If `T` or `U` is not a complete type, (possibly cv-qualified) `void`, or an array of unknown bound, the behavior is undefined.

{/* source:cpp-types-is-assignable:0005 */}
If an instantiation of a template above depends, directly or indirectly, on an incomplete type, and that instantiation could yield a different result if that type were hypothetically completed, the behavior is undefined.

{/* source:cpp-types-is-assignable:0006 */}
If the program adds specializations for any of the templates described on this page, the behavior is undefined.

### Helper variable templates

{/* source:cpp-types-is-assignable:0007 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool is_assignable_v = is_assignable<T, U>::value;`} />
  <Declaration language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool is_trivially_assignable_v = is_trivially_assignable<T, U>::value;`} />
  <Declaration language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool is_nothrow_assignable_v = is_nothrow_assignable<T, U>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-assignable:0008 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is assignable from `U`, `false` otherwise (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Notes

{/* source:cpp-types-is-assignable:0009 */}
This trait does not check anything outside the immediate context of the assignment expression: if the use of `T` or `U` would trigger template specializations, generation of implicitly-defined special member functions etc, and those have errors, the actual assignment may not compile even if `std::is_assignable<T,U>::value` compiles and evaluates to `true`.

### Example

{/* source:cpp-types-is-assignable:0010 */}
```cpp
#include <iostream>
#include <string>
#include <type_traits>
 
struct Ex1 { int n; };
 
int main()
{
    std::cout << std::boolalpha
              << "int is assignable from int? "
              << std::is_assignable<int, int>::value << '\n' // 1 = 1; wouldn't compile
              << "int& is assignable from int? "
              << std::is_assignable<int&, int>::value << '\n' // int a; a = 1; works
              << "int is assignable from double? "
              << std::is_assignable<int, double>::value << '\n'
              << "int& is nothrow assignable from double? "
              << std::is_nothrow_assignable<int&, double>::value << '\n'
              << "string is assignable from double? "
              << std::is_assignable<std::string, double>::value << '\n'
              << "Ex1& is trivially assignable from const Ex1&? "
              << std::is_trivially_assignable<Ex1&, const Ex1&>::value << '\n';
}
```

Output:

```text
int is assignable from int? false
int& is assignable from int? true
int is assignable from double? false
int& is nothrow assignable from double? true
string is assignable from double? true
Ex1& is trivially assignable from const Ex1&? true
```

### See also

{/* source:cpp-types-is-assignable:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_copy_assignable`, `is_trivially_copy_assignable`, `is_nothrow_copy_assignable`](/docs/cpp/library/meta/is_copy_assignable)<InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type has a copy assignment operator (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_move_assignable`, `is_trivially_move_assignable`, `is_nothrow_move_assignable`](/docs/cpp/library/meta/is_move_assignable)<InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type has a move assignment operator (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`assignable_from`](/docs/cpp/library/concepts/assignable_from)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is assignable from another type (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
