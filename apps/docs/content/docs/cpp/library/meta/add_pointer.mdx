---
title: "std::add_pointer"
description: "If T is a referenceable type or (possibly cv-qualified) void, the member typedef type provided is typename std::remove_reference<T>::type*."
source_url: "https://en.cppreference.com/w/cpp/types/add_pointer"
language: "C++"
---

{/* source:cpp-types-add-pointer:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct add_pointer;`} />

  <DeclarationDescription>
    {/* source:cpp-types-add-pointer:0001 */}
    If `T` is a [referenceable type](/docs/cpp/library/meta#Definitions) or (possibly cv-qualified) `void`, the member typedef `type` provided is `typename std::remove_reference<T>::type*`.

    {/* source:cpp-types-add-pointer:0002 */}
    Otherwise, the member typedef `type` provided is `T`.

    {/* source:cpp-types-add-pointer:0003 */}
    If the program adds specializations for `std::add_pointer`, the behavior is undefined.
  </DeclarationDescription>
</DeclarationDoc>

### Nested types

{/* source:cpp-types-add-pointer:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>determined as above</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-types-add-pointer:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++14" code={`template< class T >
using add_pointer_t = typename add_pointer<T>::type;`} />
  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

### Possible implementation

{/* source:cpp-types-add-pointer:0006 */}
```cpp
namespace detail
{
    template<class T>
    struct type_identity { using type = T; }; // or use std::type_identity (since C++20)
 
    template<class T>
    auto try_add_pointer(int)
      -> type_identity<typename std::remove_reference<T>::type*>; // usual case
 
    template<class T>
    auto try_add_pointer(...)
      -> type_identity<T>; // unusual case (cannot form std::remove_reference<T>::type*)
} // namespace detail
 
template<class T>
struct add_pointer : decltype(detail::try_add_pointer<T>(0)) {};
```

### Example

{/* source:cpp-types-add-pointer:0007 */}
```cpp
#include <iostream>
#include <type_traits>
 
template<typename F, typename Class>
void ptr_to_member_func_cvref_test(F Class::*)
{
    // F is an “abominable function type”
    using FF = std::add_pointer_t<F>;
    static_assert(std::is_same_v<F, FF>, "FF should be precisely F");
}
 
struct S
{
    void f_ref() & {}
    void f_const() const {}
};
 
int main()
{
    int i = 123;
    int& ri = i;
    typedef std::add_pointer<decltype(i)>::type IntPtr;
    typedef std::add_pointer<decltype(ri)>::type IntPtr2;
    IntPtr pi = &i;
    std::cout << "i = " << i << '\n';
    std::cout << "*pi = " << *pi << '\n';
 
    static_assert(std::is_pointer_v<IntPtr>, "IntPtr should be a pointer");
    static_assert(std::is_same_v<IntPtr, int*>, "IntPtr should be a pointer to int");
    static_assert(std::is_same_v<IntPtr2, IntPtr>, "IntPtr2 should be equal to IntPtr");

    typedef std::remove_pointer<IntPtr>::type IntAgain;
    IntAgain j = i;
    std::cout << "j = " << j << '\n';

    static_assert(!std::is_pointer_v<IntAgain>, "IntAgain should not be a pointer");
    static_assert(std::is_same_v<IntAgain, int>, "IntAgain should be equal to int");

    ptr_to_member_func_cvref_test(&S::f_ref);
    ptr_to_member_func_cvref_test(&S::f_const);
}
```

Output:

```text
i = 123
*pi = 123
j = 123
```

### Defect reports

{/* source:cpp-types-add-pointer:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-add-pointer:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2101} standard="C++11">
    <PublishedBehavior>the program was ill-formed if `T` is a [function type](/docs/cpp/language/functions/function#Function_type) with *cv* or *ref*</PublishedBehavior>
    <CorrectedBehavior>the type produced is `T` in this case</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-add-pointer:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_pointer`](/docs/cpp/library/meta/is_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a pointer type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`remove_pointer`](/docs/cpp/library/meta/remove_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>removes a pointer from the given type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
