---
title: "std::extent"
description: "If T is an array type, provides the member constant value equal to the number of elements along the Nth dimension of the array, if N is in [0, std::rank<T>::value)."
source_url: "https://en.cppreference.com/w/cpp/types/extent"
language: "C++"
---

{/* source:cpp-types-extent:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T, unsigned N = 0 >
struct extent;`} />

  <DeclarationDescription>
    {/* source:cpp-types-extent:0001 */}
    If `T` is an array type, provides the member constant `value` equal to the number of elements along the `N`th dimension of the array, if `N` is in [`0`, `std::rank<T>::value`). For any other type, or if `T` is an array of unknown bound along its first dimension and `N` is `0`, `value` is `0`.

    {/* source:cpp-types-extent:0002 */}
    If the program adds specializations for `std::extent`<InlineRevision since="C++17"> or `std::extent_v`</InlineRevision>, the behavior is undefined.
  </DeclarationDescription>
</DeclarationDoc>

### Helper variable template

{/* source:cpp-types-extent:0003 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T, unsigned N = 0 >
constexpr std::size_t extent_v = extent<T, N>::value;`} />
  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-types-extent:0004 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value`[static]</DescriptionTerm>
    <DescriptionBody>the number of elements along the `N`th dimension of `T` <br /> (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator std::size_t`</DescriptionTerm>
    <DescriptionBody>converts the object to `std::size_t`, returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`std::size_t`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<std::size_t, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-extent:0005 */}
```cpp
template<class T, unsigned N = 0>
struct extent : std::integral_constant<std::size_t, 0> {};
 
template<class T>
struct extent<T[], 0> : std::integral_constant<std::size_t, 0> {};
 
template<class T, unsigned N>
struct extent<T[], N> : std::extent<T, N - 1> {};
 
template<class T, std::size_t I>
struct extent<T[I], 0> : std::integral_constant<std::size_t, I> {};
 
template<class T, std::size_t I, unsigned N>
struct extent<T[I], N> : std::extent<T, N - 1> {};
```

### Example

{/* source:cpp-types-extent:0006 */}
```cpp
#include <type_traits>
 
static_assert(
    std::extent_v<int[3]> == 3 && // default dimension is 0
    std::extent_v<int[3], 0> == 3 && // the same as above
    std::extent_v<int[3][4], 0> == 3 &&
    std::extent_v<int[3][4], 1> == 4 &&
    std::extent_v<int[3][4], 2> == 0 &&
    std::extent_v<int[]> == 0
);
 
int main()
{
    const auto ext = std::extent<int['*']>{};
    static_assert(ext == 42); // with implicit conversion to std::size_t
 
    const int ints[]{1, 2, 3, 4};
    static_assert(std::extent_v<decltype(ints)> == 4); // array size
 
    [[maybe_unused]] int ary[][3] = {{1, 2, 3}};
 
    // ary[0] is of type reference to 'int[3]', so, the extent
    // cannot be calculated correctly and it returns 0
    static_assert(std::is_same_v<decltype(ary[0]), int(&)[3]>);
    static_assert(std::extent_v<decltype(ary[0])> == 0);
 
    // removing reference gives correct extent value 3
    static_assert(std::extent_v<std::remove_cvref_t<decltype(ary[0])>> == 3);
}
```

### See also

{/* source:cpp-types-extent:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_array`](/docs/cpp/library/meta/is_array)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an array type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rank`](/docs/cpp/library/meta/rank)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the number of dimensions of an array type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`remove_extent`](/docs/cpp/library/meta/remove_extent)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>removes one extent from the given array type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`remove_all_extents`](/docs/cpp/library/meta/remove_all_extents)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>removes all extents from the given array type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`extents`](/docs/cpp/library/container/mdspan/extents)<InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>a descriptor of a multidimensional index space of some rank <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
