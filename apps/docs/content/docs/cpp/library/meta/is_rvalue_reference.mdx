---
title: "std::is_rvalue_reference"
description: "Checks whether T is an rvalue reference type. Provides the member constant value which is equal to true, if T is an rvalue reference type. Otherwise, value is equal to false."
source_url: "https://en.cppreference.com/w/cpp/types/is_rvalue_reference"
language: "C++"
---

{/* source:cpp-types-is-rvalue-reference:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration id="1" language="cpp" since="C++11" code={`template< class T >
struct is_rvalue_reference;`} />
</DeclarationDoc>

{/* source:cpp-types-is-rvalue-reference:0001 */}
`std::is_rvalue_reference` is a [UnaryTypeTrait](/docs/cpp/library/named_req/UnaryTypeTrait).

{/* source:cpp-types-is-rvalue-reference:0002 */}
Checks whether `T` is an rvalue reference type. Provides the member constant `value` which is equal to `true`, if `T` is an rvalue reference type. Otherwise, `value` is equal to `false`.

{/* source:cpp-types-is-rvalue-reference:0003 */}
If the program adds specializations for `std::is_rvalue_reference` or `std::is_rvalue_reference_v`, the behavior is undefined.

### Template parameters

{/* source:cpp-types-is-rvalue-reference:0004 */}
<ParameterList>
  <Parameter name="T">a type to check</Parameter>
</ParameterList>

### Helper variable template

{/* source:cpp-types-is-rvalue-reference:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
constexpr bool is_rvalue_reference_v = is_rvalue_reference<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-rvalue-reference:0006 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is an rvalue reference type, `false` otherwise (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-is-rvalue-reference:0007 */}
```cpp
template<class T> struct is_rvalue_reference : std::false_type {};
template<class T> struct is_rvalue_reference<T&&> : std::true_type {};
```

### Example

{/* source:cpp-types-is-rvalue-reference:0008 */}
```cpp
#include <iostream>
#include <type_traits>

class A {};

static_assert
(
    std::is_rvalue_reference_v<A> == false and
    std::is_rvalue_reference_v<A&> == false and
    std::is_rvalue_reference_v<A&&> != false and
    std::is_rvalue_reference_v<char> == false and
    std::is_rvalue_reference_v<char&> == false and
    std::is_rvalue_reference_v<char&&> != false
);

template <typename T>
void test(T&& x)
{
    static_assert(std::is_same_v<T&&, decltype(x)>);
    std::cout << "T\t" << std::is_rvalue_reference<T>::value << '\n';
    std::cout << "T&&\t" << std::is_rvalue_reference<T&&>::value << '\n';
    std::cout << "decltype(x)\t" << std::is_rvalue_reference<decltype(x)>::value << '\n';
}

int main()
{
    std::cout << std::boolalpha;
    std::cout << "A\t" << std::is_rvalue_reference<A>::value << '\n';
    std::cout << "A&\t" << std::is_rvalue_reference<A&>::value << '\n';
    std::cout << "A&&\t" << std::is_rvalue_reference<A&&>::value << '\n';
    std::cout << "char\t" << std::is_rvalue_reference<char>::value << '\n';
    std::cout << "char&\t" << std::is_rvalue_reference<char&>::value << '\n';
    std::cout << "char&&\t" << std::is_rvalue_reference<char&&>::value << '\n';

    std::cout << "\ntest(42)\n";
    test(42);

    std::cout << "\ntest(x)\n";
    int x = 42;
    test(x);
}
```

Output:

```text
A	false
A&	false
A&&	true
char	false
char&	false
char&&	true

test(42)
T	false
T&&	true
decltype(x)	true

test(x)
T	false
T&&	false
decltype(x)	false
```

### See also

{/* source:cpp-types-is-rvalue-reference:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_lvalue_reference`](/docs/cpp/library/meta/is_lvalue_reference)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an lvalue reference (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_reference`](/docs/cpp/library/meta/is_reference)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is either an lvalue reference or rvalue reference (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
