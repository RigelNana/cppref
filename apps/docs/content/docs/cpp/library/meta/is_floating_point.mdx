---
title: "std::is_floating_point"
description: "std::is_floating_point is a UnaryTypeTrait."
source_url: "https://en.cppreference.com/w/cpp/types/is_floating_point"
language: "C++"
---

{/* source:cpp-types-is-floating-point:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct is_floating_point;`} />
</DeclarationDoc>

{/* source:cpp-types-is-floating-point:0001 */}
`std::is_floating_point` is a [UnaryTypeTrait](/docs/cpp/library/named_req/UnaryTypeTrait).

{/* source:cpp-types-is-floating-point:0002 */}
Checks whether `T` is a floating-point type. Provides the member constant `value` which is equal to `true`, if `T` is the type `float`, `double`, `long double`<InlineRevision since="C++23">, or any extended floating-point types (`std::float16_t`, `std::float32_t`, `std::float64_t`, `std::float128_t`, or `std::bfloat16_t`)</InlineRevision>, including any cv-qualified variants. Otherwise, `value` is equal to `false`.

{/* source:cpp-types-is-floating-point:0003 */}
If the program adds specializations for `std::is_floating_point` or `std::is_floating_point_v`, the behavior is undefined.

### Template parameters

{/* source:cpp-types-is-floating-point:0004 */}
<ParameterList>
  <Parameter name="T">a type to check</Parameter>
</ParameterList>

### Helper variable template

{/* source:cpp-types-is-floating-point:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
constexpr bool is_floating_point_v = is_floating_point<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-floating-point:0006 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is a floating-point type (possibly cv-qualified), `false` otherwise <br /> (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>Type</DescriptionTerm>
    <DescriptionBody>Definition</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-is-floating-point:0007 */}
```cpp
template<class T>
struct is_floating_point
     : std::integral_constant<
         bool,
         // Note: standard floating-point types
         std::is_same<float, typename std::remove_cv<T>::type>::value
         || std::is_same<double, typename std::remove_cv<T>::type>::value
         || std::is_same<long double, typename std::remove_cv<T>::type>::value
         // Note: extended floating-point types (C++23, if supported)
         || std::is_same<std::float16_t, typename std::remove_cv<T>::type>::value
         || std::is_same<std::float32_t, typename std::remove_cv<T>::type>::value
         || std::is_same<std::float64_t, typename std::remove_cv<T>::type>::value
         || std::is_same<std::float128_t, typename std::remove_cv<T>::type>::value
         || std::is_same<std::bfloat16_t, typename std::remove_cv<T>::type>::value
     > {};
```

### Example

{/* source:cpp-types-is-floating-point:0008 */}
```cpp
#include <type_traits>

class A {};
static_assert(!std::is_floating_point_v<A>);

static_assert(std::is_floating_point_v<float>);
static_assert(!std::is_floating_point_v<float&>);
static_assert(std::is_floating_point_v<double>);
static_assert(!std::is_floating_point_v<double&>);
static_assert(!std::is_floating_point_v<int>);

int main() {}
```

### See also

{/* source:cpp-types-is-floating-point:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_iec559`](/docs/cpp/library/utility/types/numeric_limits/is_iec559)[static]</DescriptionTerm>
    <DescriptionBody>identifies the IEC 559/IEEE 754 floating-point types <br /> (public static member constant of `std::numeric_limits<T>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_integral`](/docs/cpp/library/meta/is_integral)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an integral type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_arithmetic`](/docs/cpp/library/meta/is_arithmetic)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an arithmetic type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`floating_point`](/docs/cpp/library/concepts/floating_point)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is a floating-point type <br /> (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
