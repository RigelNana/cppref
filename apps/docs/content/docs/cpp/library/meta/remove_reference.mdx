---
title: "std::remove_reference"
description: "If the type T is a reference type, provides the member typedef type which is the type referred to by T. Otherwise type is T."
source_url: "https://en.cppreference.com/w/cpp/types/remove_reference"
language: "C++"
---

{/* source:cpp-types-remove-reference:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct remove_reference;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-types-remove-reference:0001 */}
If the type `T` is a reference type, provides the member typedef `type` which is the type referred to by `T`. Otherwise `type` is `T`.

{/* source:cpp-types-remove-reference:0002 */}
If the program adds specializations for `std::remove_reference`, the behavior is undefined.

### Member types

{/* source:cpp-types-remove-reference:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>the type referred by `T` or `T` if it is not a reference</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-types-remove-reference:0004 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++14" code={`template< class T >
using remove_reference_t = typename remove_reference<T>::type;`} />
</DeclarationDoc>

### Possible implementation

{/* source:cpp-types-remove-reference:0005 */}
```cpp
template<class T> struct remove_reference { typedef T type; };
template<class T> struct remove_reference<T&> { typedef T type; };
template<class T> struct remove_reference<T&&> { typedef T type; };
```

### Example

{/* source:cpp-types-remove-reference:0006 */}
```cpp
#include <iostream>
#include <type_traits>
 
int main()
{
    std::cout << std::boolalpha;
 
    std::cout << "std::remove_reference<int>::type is int? "
              << std::is_same<int, std::remove_reference<int>::type>::value << '\n';
    std::cout << "std::remove_reference<int&>::type is int? "
              << std::is_same<int, std::remove_reference<int&>::type>::value << '\n';
    std::cout << "std::remove_reference<int&&>::type is int? "
              << std::is_same<int, std::remove_reference<int&&>::type>::value << '\n';
    std::cout << "std::remove_reference<const int&>::type is const int? "
              << std::is_same<const int,
                              std::remove_reference<const int&>::type>::value << '\n';
}
```

Output:

```text
std::remove_reference<int>::type is int? true
std::remove_reference<int&>::type is int? true
std::remove_reference<int&&>::type is int? true
std::remove_reference<const int&>::type is const int? true
```

### See also

{/* source:cpp-types-remove-reference:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_reference`](/docs/cpp/library/meta/is_reference)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is either an *lvalue reference* or *rvalue reference* <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`add_lvalue_reference`, `add_rvalue_reference`](/docs/cpp/library/meta/add_reference)<InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>adds an *lvalue* or *rvalue* reference to the given type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`remove_cvref`](/docs/cpp/library/meta/remove_cvref)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>combines [`std::remove_cv`](/docs/cpp/library/meta/remove_cv) and `std::remove_reference` <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
