---
title: "std::is_pointer"
description: "Checks whether T is a pointer to object or function (including pointer to void, but excluding pointer to member) or a cv-qualified version thereof. Provides the member constant value which is equal to true, if T is an object/function pointer type. Otherwise, value is equal to false."
source_url: "https://en.cppreference.com/w/cpp/types/is_pointer"
language: "C++"
---

{/* source:cpp-types-is-pointer:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration id="1" language="cpp" since="C++11" code={`template< class T >
struct is_pointer;`} />
</DeclarationDoc>

{/* source:cpp-types-is-pointer:0001 */}
`std::is_pointer` is a [UnaryTypeTrait](/docs/cpp/library/named_req/UnaryTypeTrait).

{/* source:cpp-types-is-pointer:0002 */}
Checks whether `T` is a [pointer to object or function](/docs/cpp/language/declarations/pointer#Pointers) (including pointer to `void`, but excluding pointer to member) or a cv-qualified version thereof. Provides the member constant `value` which is equal to `true`, if `T` is an object/function pointer type. Otherwise, `value` is equal to `false`.

{/* source:cpp-types-is-pointer:0003 */}
If the program adds specializations for `std::is_pointer` or `std::is_pointer_v`, the behavior is undefined.

### Template parameters

{/* source:cpp-types-is-pointer:0004 */}
<ParameterList>
  <Parameter name="T">a type to check</Parameter>
</ParameterList>

### Helper variable template

{/* source:cpp-types-is-pointer:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
constexpr bool is_pointer_v = is_pointer<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-pointer:0006 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is a pointer type, `false` otherwise (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-is-pointer:0007 */}
```cpp
template<class T>
struct is_pointer : std::false_type {};

template<class T>
struct is_pointer<T*> : std::true_type {};

template<class T>
struct is_pointer<T* const> : std::true_type {};

template<class T>
struct is_pointer<T* volatile> : std::true_type {};

template<class T>
struct is_pointer<T* const volatile> : std::true_type {};
```

### Example

{/* source:cpp-types-is-pointer:0008 */}
```cpp
#include <type_traits>

int main()
{
    struct A
    {
        int m;
        void f() {}
    };

    int A::*mem_data_ptr = &A::m;     // a pointer to member data
    void (A::*mem_fun_ptr)() = &A::f; // a pointer to member function

    static_assert(
           ! std::is_pointer<A>::value
        && ! std::is_pointer_v<A>    // same thing as above, but in C++17!
        && ! std::is_pointer<A>()    // same as above, using inherited operator bool
        && ! std::is_pointer<A>{}    // ditto
        && ! std::is_pointer<A>()()  // same as above, using inherited operator()
        && ! std::is_pointer<A>{}()  // ditto
        &&   std::is_pointer_v<A*>
        &&   std::is_pointer_v<A const* volatile>
        && ! std::is_pointer_v<A&>
        && ! std::is_pointer_v<decltype(mem_data_ptr)>
        && ! std::is_pointer_v<decltype(mem_fun_ptr)>
        &&   std::is_pointer_v<void*>
        && ! std::is_pointer_v<int>
        &&   std::is_pointer_v<int*>
        &&   std::is_pointer_v<int**>
        && ! std::is_pointer_v<int[10]>
        && ! std::is_pointer_v<std::nullptr_t>
        &&   std::is_pointer_v<void (*)()>
    );
}
```

### See also

{/* source:cpp-types-is-pointer:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_member_pointer`](/docs/cpp/library/meta/is_member_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a pointer to a non-static member function or object (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_member_object_pointer`](/docs/cpp/library/meta/is_member_object_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a non-static member object pointer (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_member_function_pointer`](/docs/cpp/library/meta/is_member_function_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a non-static member function pointer (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_array`](/docs/cpp/library/meta/is_array)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an array type (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_scalar`](/docs/cpp/library/meta/is_scalar)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a scalar type (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
