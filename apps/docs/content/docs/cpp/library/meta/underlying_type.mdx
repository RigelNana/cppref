---
title: "std::underlying_type"
description: "If T is a complete enumeration (enum) type, provides a member typedef type that names the underlying type of T."
source_url: "https://en.cppreference.com/w/cpp/types/underlying_type"
language: "C++"
---

{/* source:cpp-types-underlying-type:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct underlying_type;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-types-underlying-type:0001 */}
If `T` is a complete enumeration (`enum`) type, provides a member typedef `type` that names the underlying type of `T`.

{/* source:cpp-types-underlying-type:0002 */}
<Revision until="C++20">
Otherwise, the behavior is undefined.
</Revision>
<Revision since="C++20">
Otherwise, if `T` is not an enumeration type, there is no member type. Otherwise (`T` is an incomplete enumeration type), the program is ill-formed.
</Revision>

{/* source:cpp-types-underlying-type:0003 */}
If the program adds specializations for `std::underlying_type`, the behavior is undefined.

### Member types

{/* source:cpp-types-underlying-type:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>the underlying type of `T`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-types-underlying-type:0005 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++14" code={`template< class T >
using underlying_type_t = typename underlying_type<T>::type;`} />
</DeclarationDoc>

### Notes

{/* source:cpp-types-underlying-type:0006 */}
Each [enumeration type](/docs/cpp/language/declarations/enum) has an *underlying type*, which can be

{/* source:cpp-types-underlying-type:0007 */}
1. Specified explicitly (both scoped and unscoped enumerations);
2. Omitted, in which case it is `int` for scoped enumerations or an implementation-defined integral type capable of representing all values of the enum (for unscoped enumerations).

### Example

{/* source:cpp-types-underlying-type:0008 */}
```cpp
#include <iostream>
#include <type_traits>
 
enum e1 {};
enum class e2 {};
enum class e3 : unsigned {};
enum class e4 : int {};
 
int main()
{
    constexpr bool e1_t = std::is_same_v<std::underlying_type_t<e1>, int>;
    constexpr bool e2_t = std::is_same_v<std::underlying_type_t<e2>, int>;
    constexpr bool e3_t = std::is_same_v<std::underlying_type_t<e3>, int>;
    constexpr bool e4_t = std::is_same_v<std::underlying_type_t<e4>, int>;
 
    std::cout
        << "underlying type for 'e1' is " << (e1_t ? "int" : "non-int") << '\n'
        << "underlying type for 'e2' is " << (e2_t ? "int" : "non-int") << '\n'
        << "underlying type for 'e3' is " << (e3_t ? "int" : "non-int") << '\n'
        << "underlying type for 'e4' is " << (e4_t ? "int" : "non-int") << '\n';
}
```

Possible output:

```text
underlying type for 'e1' is non-int
underlying type for 'e2' is int
underlying type for 'e3' is non-int
underlying type for 'e4' is int
```

### Defect reports

{/* source:cpp-types-underlying-type:0009 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-underlying-type:0010 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2396} standard="C++11">
    <PublishedBehavior>incomplete enumeration types were allowed</PublishedBehavior>
    <CorrectedBehavior>complete enumeration type required</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-underlying-type:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_enum`](/docs/cpp/library/meta/is_enum)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an enumeration type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_scoped_enum`](/docs/cpp/library/meta/is_scoped_enum)<InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a scoped enumeration type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_underlying`](/docs/cpp/library/utility/to_underlying)<InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>converts an enumeration to its underlying type <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
