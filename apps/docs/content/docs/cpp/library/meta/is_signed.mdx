---
title: "std::is_signed"
description: "std::is_signed is a UnaryTypeTrait."
source_url: "https://en.cppreference.com/w/cpp/types/is_signed"
language: "C++"
---

{/* source:cpp-types-is-signed:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct is_signed;`} />
</DeclarationDoc>

{/* source:cpp-types-is-signed:0001 */}
`std::is_signed` is a [UnaryTypeTrait](/docs/cpp/library/named_req/UnaryTypeTrait).

{/* source:cpp-types-is-signed:0002 */}
Checks whether `T` is a signed arithmetic type.

{/* source:cpp-types-is-signed:0003 */}
- If [`std::is_arithmetic`](/docs/cpp/library/meta/is_arithmetic)`<T>::value` is `true`, provides the member constant `value` equal to `T(-1) < T(0)`.
- Otherwise, provides the member constant `value` equal to `false`.

{/* source:cpp-types-is-signed:0004 */}
If the program adds specializations for `std::is_signed` or `std::is_signed_v`, the behavior is undefined.

### Template parameters

{/* source:cpp-types-is-signed:0005 */}
<ParameterList>
  <Parameter name="T">a type to check</Parameter>
</ParameterList>

### Helper variable template

{/* source:cpp-types-is-signed:0006 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class T >
constexpr bool is_signed_v = is_signed<T>::value;`} />
</DeclarationDoc>

{/* source:cpp-types-is-signed:0007 */}
## Inherited from [`std::integral_constant`](/docs/cpp/library/meta/integral_constant)

### Member constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value` [static]</DescriptionTerm>
    <DescriptionBody>`true` if `T` is a signed arithmetic type, `false` otherwise <br /> (public static member constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator bool`</DescriptionTerm>
    <DescriptionBody>converts the object to `bool`, returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator()`<InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns `value` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member types

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`bool`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>`std::integral_constant<bool, value>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Possible implementation

{/* source:cpp-types-is-signed:0008 */}
```cpp
namespace detail
{
    template<typename T, bool = std::is_arithmetic<T>::value>
    struct is_signed : std::integral_constant<bool, T(-1) < T(0)> {};

    template<typename T>
    struct is_signed<T, false> : std::false_type {};
}

template<typename T>
struct is_signed : detail::is_signed<T>::type {};
```

### Example

{/* source:cpp-types-is-signed:0009 */}
```cpp
#include <iostream>
#include <type_traits>

class A {};
static_assert(std::is_signed_v<A> == false);

class B { int i; };
static_assert(std::is_signed_v<B> == false);

enum C : int {};
static_assert(std::is_signed_v<C> == false);

enum class D : int {};
static_assert(std::is_signed_v<D> == false);

static_assert
(
    std::is_signed<signed int>::value == true and // C++11
    std::is_signed<signed int>() == true and      // C++11
    std::is_signed<signed int>{} == true and      // C++11
    std::is_signed_v<signed int> == true and      // C++17
    std::is_signed_v<unsigned int> == false and
    std::is_signed_v<float> == true and
    std::is_signed_v<bool> == false and
    std::is_signed_v<signed char> == true and
    std::is_signed_v<unsigned char> == false
);

int main()
{
    // signedness of char is implementation-defined:
    std::cout << std::boolalpha << std::is_signed_v<char> << '\n';
}
```

Possible output:

```text
true
```

### Defect reports

{/* source:cpp-types-is-signed:0010 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-types-is-signed:0011 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2197} standard="C++11">
    <PublishedBehavior>`value` could be `true` even if `T` is not an arithmetic type</PublishedBehavior>
    <CorrectedBehavior>can only be `false` in this case</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-types-is-signed:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_unsigned`](/docs/cpp/library/meta/is_unsigned)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an unsigned arithmetic type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_signed`](/docs/cpp/library/utility/types/numeric_limits/is_signed)[static]</DescriptionTerm>
    <DescriptionBody>identifies signed types <br /> (public static member constant of `std::numeric_limits<T>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_arithmetic`](/docs/cpp/library/meta/is_arithmetic)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is an arithmetic type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`make_signed`](/docs/cpp/library/meta/make_signed)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the corresponding signed type for the given integral type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`make_unsigned`](/docs/cpp/library/meta/make_unsigned)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the corresponding signed type for the given integral type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
