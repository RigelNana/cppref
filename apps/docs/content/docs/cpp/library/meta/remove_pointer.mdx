---
title: "std::remove_pointer"
description: "Provides the member typedef type which is the type pointed to by T, or, if T is not a pointer, then type is the same as T."
source_url: "https://en.cppreference.com/w/cpp/types/remove_pointer"
language: "C++"
---

{/* source:cpp-types-remove-pointer:0000 */}
<DeclarationDoc>
Defined in header [`<type_traits>`](/docs/cpp/library/headers/type_traits)

  <Declaration language="cpp" since="C++11" code={`template< class T >
struct remove_pointer;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-types-remove-pointer:0001 */}
Provides the member typedef `type` which is the type pointed to by `T`, or, if `T` is not a pointer, then `type` is the same as `T`.

{/* source:cpp-types-remove-pointer:0002 */}
If the program adds specializations for `std::remove_pointer`, the behavior is undefined.

### Member types

{/* source:cpp-types-remove-pointer:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>the type pointed to by `T` or `T` if it's not a pointer</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-types-remove-pointer:0004 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++14" code={`template< class T >
using remove_pointer_t = typename remove_pointer<T>::type;`} />
</DeclarationDoc>

### Possible implementation

{/* source:cpp-types-remove-pointer:0005 */}
```cpp
template<class T> struct remove_pointer { typedef T type; };
template<class T> struct remove_pointer<T*> { typedef T type; };
template<class T> struct remove_pointer<T* const> { typedef T type; };
template<class T> struct remove_pointer<T* volatile> { typedef T type; };
template<class T> struct remove_pointer<T* const volatile> { typedef T type; };
```

### Example

{/* source:cpp-types-remove-pointer:0006 */}
```cpp
#include <type_traits>
 
static_assert
(
    std::is_same_v<int, int> == true &&
    std::is_same_v<int, int*> == false &&
    std::is_same_v<int, int**> == false &&
    std::is_same_v<int, std::remove_pointer_t<int>> == true &&
    std::is_same_v<int, std::remove_pointer_t<int*>> == true &&
    std::is_same_v<int, std::remove_pointer_t<int**>> == false &&
    std::is_same_v<int, std::remove_pointer_t<int* const>> == true &&
    std::is_same_v<int, std::remove_pointer_t<int* volatile>> == true &&
    std::is_same_v<int, std::remove_pointer_t<int* const volatile>> == true
);
 
int main() {}
```

### See also

{/* source:cpp-types-remove-pointer:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`is_pointer`](/docs/cpp/library/meta/is_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a type is a pointer type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`add_pointer`](/docs/cpp/library/meta/add_pointer)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>adds a pointer to the given type <br /> (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
