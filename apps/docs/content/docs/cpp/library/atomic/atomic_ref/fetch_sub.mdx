---
title: "std::atomic_ref<T>::fetch_sub"
description: "Atomically replaces the current value of the referenced object with the result of arithmetic subtraction of the value and arg. This operation is a read-modify-write operation. Memory is affected according to the value of order."
source_url: "https://en.cppreference.com/w/cpp/atomic/atomic_ref/fetch_sub"
language: "C++"
---

{/* source:cpp-atomic-atomic-ref-fetch-sub:0000 */}
<DeclarationDoc>
Provided only when `T` is an arithmetic type other than *cv* `bool` or a pointer-to-object type
  <Declaration language="cpp" code={`value_type fetch_sub( difference_type arg,
                      std::memory_order order =
                          std::memory_order_seq_cst ) const noexcept;`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-atomic-atomic-ref-fetch-sub:0001 */}
    Atomically replaces the current value of the referenced object with the result of arithmetic subtraction of the value and `arg`. This operation is a read-modify-write operation. Memory is affected according to the value of `order`.

    {/* source:cpp-atomic-atomic-ref-fetch-sub:0002 */}
    - For signed integral types, arithmetic is defined to use two’s complement representation. There are no undefined results.
    - For floating-point types, the [floating-point environment](/docs/cpp/library/numeric/fenv) in effect may be different from the calling thread's floating-point environment. The operation need not be conform to the corresponding [`std::numeric_limits`](/docs/cpp/library/utility/types/numeric_limits) traits but is encouraged to do so. If the result is not a representable value for its type, the result is unspecified but the operation otherwise has no undefined behavior.
    - For pointer types, the result may be an undefined address, but the operation otherwise has no undefined behavior.
      - If `std::remove_pointer_t<T>` is not a complete object type, the program is ill-formed.

    {/* source:cpp-atomic-atomic-ref-fetch-sub:0003 */}
    This overload participates in overload resolution only if `std::is_const_v<T>` is `false`.
  </DeclarationDescription>
</DeclarationDoc>


### Parameters


{/* source:cpp-atomic-atomic-ref-fetch-sub:0004 */}
<ParameterList>
  <Parameter name="arg">the other argument of arithmetic subtraction</Parameter>
  <Parameter name="order">memory order constraints to enforce</Parameter>
</ParameterList>


### Return value


{/* source:cpp-atomic-atomic-ref-fetch-sub:0005 */}
The value referenced by `*`[`ptr`](/docs/cpp/library/atomic/atomic_ref#ptr), immediately preceding the effects of this function.


### Example


{/* source:cpp-atomic-atomic-ref-fetch-sub:0006 */}
This section is incomplete Reason: no example


### Defect reports


{/* source:cpp-atomic-atomic-ref-fetch-sub:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-atomic-atomic-ref-fetch-sub:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3508} standard="C++20">
    <PublishedBehavior>`fetch_sub` was meaningless for `const T`</PublishedBehavior>
    <CorrectedBehavior>constrained to accept only non-const `T`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
