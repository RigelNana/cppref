---
title: "ATOMIC_VAR_INIT"
description: "Expands to an expression which can be used to initialize an std::atomic object that can be initialized from value. If the atomic object has static storage duration, this initialization is constant initialization."
source_url: "https://en.cppreference.com/w/cpp/atomic/ATOMIC_VAR_INIT"
language: "C++"
---

{/* source:cpp-atomic-atomic-var-init:0000 */}
<DeclarationDoc>
Defined in header [`<atomic>`](/docs/cpp/library/headers/atomic)

  <Declaration language="cpp" since="C++11" code={`#define ATOMIC_VAR_INIT(value) /* implementation-defined */`} />(deprecated in C++20)
</DeclarationDoc>

{/* source:cpp-atomic-atomic-var-init:0001 */}
Expands to an expression which can be used to initialize an [`std::atomic`](/docs/cpp/library/atomic/atomic) object that can be initialized from `value`. If the atomic object has static storage duration, this initialization is [constant initialization](/docs/cpp/language/initialization/constant_initialization).

### Notes

{/* source:cpp-atomic-atomic-var-init:0002 */}
Accessing the variable during initialization from another thread, even through an atomic operation, is a data race (it may happen if the address is immediately passed to another thread with a [`std::memory_order_relaxed`](/docs/cpp/library/atomic/memory_order) operation)

{/* source:cpp-atomic-atomic-var-init:0003 */}
This macro is primarily provided for compatibility with C; it behaves the same as the constructor of [`std::atomic`](/docs/cpp/library/atomic/atomic).

### Example

{/* source:cpp-atomic-atomic-var-init:0004 */}
```cpp
#include <atomic>
#include <iostream>

int main()
{
    std::atomic<int> a = ATOMIC_VAR_INIT(1);
    // std::atomic<int> a(1);   // C++-only alternative
    std::cout << "Initialized std::atomic<int> as: " << a << '\n';
}
```

Output:

```text
Initialized std::atomic<int> as: 1
```

### See also

{/* source:cpp-atomic-atomic-var-init:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`atomic_init`](/docs/cpp/library/atomic/atomic_init) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>non-atomic initialization of a default-constructed atomic object <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/atomic/atomic/atomic)</DescriptionTerm>
    <DescriptionBody>constructs an atomic object <br /> (public member function of `std::atomic<T>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/thread/ATOMIC_VAR_INIT) for `ATOMIC_VAR_INIT`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
