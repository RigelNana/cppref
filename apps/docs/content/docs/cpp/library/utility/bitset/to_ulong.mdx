---
title: "std::bitset<N>::to_ulong"
description: "Converts the contents of the bitset to an unsigned long integer."
source_url: "https://en.cppreference.com/w/cpp/utility/bitset/to_ulong"
language: "C++"
---
{/* source:cpp-utility-bitset-to-ulong:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`unsigned long to_ulong() const`} />
  (constexpr since C++23)
</DeclarationDoc>

{/* source:cpp-utility-bitset-to-ulong:0001 */}
Converts the contents of the bitset to an `unsigned long` integer.

{/* source:cpp-utility-bitset-to-ulong:0002 */}
The first bit of the bitset corresponds to the least significant digit of the number and the last bit corresponds to the most significant digit.

### Parameters

{/* source:cpp-utility-bitset-to-ulong:0003 */}
(none)

### Return value

{/* source:cpp-utility-bitset-to-ulong:0004 */}
The converted integer.

### Exceptions

{/* source:cpp-utility-bitset-to-ulong:0005 */}
Throws [`std::overflow_error`](/docs/cpp/library/error/overflow_error) if the value can not be represented in `unsigned long`.

### Example

{/* source:cpp-utility-bitset-to-ulong:0006 */}
```cpp
#include <bitset>
#include <iostream>
#include <stdexcept>
 
int main()
{
    for (unsigned long i = 0; i < 10; ++i)
    {
        std::bitset<5> b(i);
        std::bitset<5> b_inverted = ~b;
        std::cout << i << '\t' << b << '\t' << b_inverted << '\t'
                  << b_inverted.to_ulong() << '\n';
    }
 
    std::cout << std::bitset<32>().to_string('-') << '\n';
 
    try
    {
        std::bitset<128> x(42);
        std::cout << x.to_ulong() << '\n';
        x.flip();
        std::cout << x.to_ulong() << '\n'; // throws
    }
    catch (const std::overflow_error& ex)
    {
        std::cout << "ex: " << ex.what() << '\n';
    }
}
```

Possible output:

```text
0   00000   11111   31
1   00001   11110   30
2   00010   11101   29
3   00011   11100   28
4   00100   11011   27
5   00101   11010   26
6   00110   11001   25
7   00111   11000   24
8   01000   10111   23
9   01001   10110   22
--------------------------------
42
ex: bitset to_ulong overflow error
```

### See also

{/* source:cpp-utility-bitset-to-ulong:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`to_string`](/docs/cpp/library/utility/bitset/to_string)</DescriptionTerm>
    <DescriptionBody>returns a string representation of the data (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_ullong`](/docs/cpp/library/utility/bitset/to_ullong) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns an `unsigned long long` integer representation of the data (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
