---
title: "operator&,|,^(std::bitset)"
description: "Performs binary AND, OR, and XOR between two bitsets."
source_url: "https://en.cppreference.com/w/cpp/utility/bitset/operator_logic2"
language: "C++"
---

{/* source:cpp-utility-bitset-operator-logic2:0000 */}
Defined in header [`<bitset>`](/docs/cpp/library/headers/bitset)

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`template< std::size_t N >
std::bitset<N> operator&( const std::bitset<N>& lhs,

                          const std::bitset<N>& rhs );`} />
  (noexcept since C++11)
  (constexpr since C++23)

  <Declaration id="2" language="cpp" code={`template< std::size_t N >
std::bitset<N> operator|( const std::bitset<N>& lhs,

                          const std::bitset<N>& rhs );`} />
  (noexcept since C++11)
  (constexpr since C++23)

  <Declaration id="3" language="cpp" code={`template< std::size_t N >
std::bitset<N> operator^( const std::bitset<N>& lhs,

                          const std::bitset<N>& rhs );`} />
  (noexcept since C++11)
  (constexpr since C++23)
</DeclarationDoc>

{/* source:cpp-utility-bitset-operator-logic2:0001 */}
Performs binary AND, OR, and XOR between two bitsets, `lhs` and `rhs`.

{/* source:cpp-utility-bitset-operator-logic2:0002 */}
1. 1) Returns a `std::bitset<N>` containing the result of binary AND on corresponding pairs of bits of `lhs` and `rhs`.

{/* source:cpp-utility-bitset-operator-logic2:0003 */}
2. 2) Returns a `std::bitset<N>` containing the result of binary OR on corresponding pairs of bits of `lhs` and `rhs`.

{/* source:cpp-utility-bitset-operator-logic2:0004 */}
3. 3) Returns a `std::bitset<N>` containing the result of binary XOR on corresponding pairs of bits of `lhs` and `rhs`.

### Parameters

{/* source:cpp-utility-bitset-operator-logic2:0005 */}
<ParameterList>
  <Parameter name="lhs">the bitset on the left-hand side of the operator</Parameter>
  <Parameter name="rhs">the bitset on the right-hand side of the operator</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-bitset-operator-logic2:0006 */}
1. 1) `std::bitset<N>(lhs) &= rhs`

{/* source:cpp-utility-bitset-operator-logic2:0007 */}
2. 2) `std::bitset<N>(lhs) |= rhs`

{/* source:cpp-utility-bitset-operator-logic2:0008 */}
3. 3) `std::bitset<N>(lhs) ^= rhs`

### Example

{/* source:cpp-utility-bitset-operator-logic2:0009 */}
```cpp
#include <bitset>
#include <iostream>
 
int main()
{
    std::bitset<4> b1("0110");
    std::bitset<4> b2("0011");
 
    std::cout << "b1 & b2: " << (b1 & b2) << '\n';
    std::cout << "b1 | b2: " << (b1 | b2) << '\n';
    std::cout << "b1 ^ b2: " << (b1 ^ b2) << '\n';
}
```

Output:

```text
b1 & b2: 0010
b1 | b2: 0111
b1 ^ b2: 0101
```

### See also

{/* source:cpp-utility-bitset-operator-logic2:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator&=` `operator|=` `operator^=` `operator~`](/docs/cpp/library/utility/bitset/operator_logic)</DescriptionTerm>
    <DescriptionBody>performs binary AND, OR, XOR and NOT (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
