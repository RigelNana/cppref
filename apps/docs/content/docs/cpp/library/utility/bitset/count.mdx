---
title: "std::bitset<N>::count"
description: "std::bitset<N>::count returns the number of bits that are set to true."
source_url: "https://en.cppreference.com/w/cpp/utility/bitset/count"
language: "C++"
---

{/* source:cpp-utility-bitset-count:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`std::size_t count() const;`} />
  (noexcept since C++11)
  (constexpr since C++23)
</DeclarationDoc>

{/* source:cpp-utility-bitset-count:0001 */}
Returns the number of bits that are set to `true`.

### Return value

{/* source:cpp-utility-bitset-count:0002 */}
Number of bits that are set to `true`.

### Example

{/* source:cpp-utility-bitset-count:0003 */}
```cpp
#include <bitset>
#include <iostream>
 
constexpr auto popcount(unsigned x) noexcept
{
    unsigned num{};
    for (; x; ++num, x &= (x - 1));
    return num;
};
static_assert(popcount(0b101010) == std::bitset<8>{0b101010}.count());
 
int main()
{
    std::bitset<8> b("00010010");
    std::cout << "Initial value: " << b << '\n';
 
    // Find the first unset bit
    std::size_t idx = 0;
    while (idx < b.size() && b.test(idx))
        ++idx;
 
    // Continue setting bits until half the bitset is filled
    while (idx < b.size() && b.count() < b.size() / 2)
    {
        b.set(idx);
        std::cout << "Setting bit " << idx << ": " << b << '\n';
        while (idx < b.size() && b.test(idx))
            ++idx;
    }
}
```

Output:

```text
Initial value: 00010010
Setting bit 0: 00010011
Setting bit 2: 00010111
```

### See also

{/* source:cpp-utility-bitset-count:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/utility/bitset/size)</DescriptionTerm>
    <DescriptionBody>returns the number of bits that the bitset holds (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`popcount`](/docs/cpp/library/utility/bit/popcount) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>counts the number of `1` bits in an unsigned integer (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
