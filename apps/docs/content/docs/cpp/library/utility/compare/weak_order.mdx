---
title: "std::weak_order"
description: "Compares two values using 3-way comparison and produces a result of type std::weak_ordering."
source_url: "https://en.cppreference.com/w/cpp/utility/compare/weak_order"
language: "C++"
---

{/* source:cpp-utility-compare-weak-order:0000 */}
<DeclarationDoc>
Defined in header [`<compare>`](/docs/cpp/library/headers/compare)

  <Declaration language="cpp" since="C++20" code={`inline namespace /* unspecified */ {
     inline constexpr /* unspecified */ weak_order = /* unspecified */;

}`} />

Call signature

  <Declaration language="cpp" code={`template< class T, class U >
     requires /* see below */

constexpr std::weak_ordering weak_order(T&& t, U&& u) noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-utility-compare-weak-order:0001 */}
Compares two values using 3-way comparison and produces a result of type [`std::weak_ordering`](/docs/cpp/library/utility/compare/weak_ordering).

{/* source:cpp-utility-compare-weak-order:0002 */}
Let `t` and `u` be expressions and `T` and `U` denote `decltype((t))` and `decltype((u))` respectively, `std::weak_order(t, u)` is [expression-equivalent](/docs/cpp/language/expressions#Expression-equivalence) to:

{/* source:cpp-utility-compare-weak-order:0003 */}
- If `std::is_same_v<std::decay_t<T>, std::decay_t<U>>` is `true`:
    - `std::weak_ordering(weak_order(t, u))`, if it is a well-formed expression with overload resolution performed in a context that does not include a declaration of `std::weak_order`,
    - otherwise, if `T` is a floating-point type:
        - if `std::numeric_limits<T>::is_iec559` is true, performs the weak ordering comparison of floating-point values (see below) and returns that result as a value of type [`std::weak_ordering`](/docs/cpp/library/utility/compare/weak_ordering),
        - otherwise, yields a value of type [`std::weak_ordering`](/docs/cpp/library/utility/compare/weak_ordering) that is consistent with the ordering observed by `T`'s comparison operators,
    - otherwise, `std::weak_ordering(std::compare_three_way()(t, u))`, if it is well-formed,
    - otherwise, `std::weak_ordering(std::strong_order(t, u))`, if it is well-formed.
- In all other cases, the expression is ill-formed, which can result in [substitution failure](/docs/cpp/language/templates/sfinae) when it appears in the immediate context of a template instantiation.

### Customization point objects

{/* source:cpp-utility-compare-weak-order:0004 */}
The name `std::weak_order` denotes a customization point object, which is a const [function object](/docs/cpp/library/named_req/FunctionObject) of a [literal](/docs/cpp/library/named_req/LiteralType) [`semiregular`](/docs/cpp/library/concepts/semiregular) class type. For exposition purposes, the cv-unqualified version of its type is denoted as `__weak_order_fn`.

{/* source:cpp-utility-compare-weak-order:0005 */}
All instances of `__weak_order_fn` are equal. The effects of invoking different instances of type `__weak_order_fn` on the same arguments are equivalent, regardless of whether the expression denoting the instance is an lvalue or rvalue, and is const-qualified or not (however, a volatile-qualified instance is not required to be invocable). Thus, `std::weak_order` can be copied freely and its copies can be used interchangeably.

{/* source:cpp-utility-compare-weak-order:0006 */}
Given a set of types `Args...`, if `std::declval<Args>()...` meet the requirements for arguments to `std::weak_order` above, `__weak_order_fn` models

{/* source:cpp-utility-compare-weak-order:0007 */}
- `std::invocable<__weak_order_fn, Args...>`,
- `std::invocable<const __weak_order_fn, Args...>`,
- `std::invocable<__weak_order_fn&, Args...>`, and
- `std::invocable<const __weak_order_fn&, Args...>`.

{/* source:cpp-utility-compare-weak-order:0008 */}
Otherwise, no function call operator of `__weak_order_fn` participates in overload resolution.

#### Strict weak order of IEEE floating-point types

{/* source:cpp-utility-compare-weak-order:0009 */}
Let `x` and `y` be values of same IEEE floating-point type, and `weak_order_less(x, y)` be the boolean result indicating if `x` precedes `y` in the strict weak order defined by the C++ standard.

{/* source:cpp-utility-compare-weak-order:0010 */}
- If neither `x` nor `y` is NaN, then `weak_order_less(x, y) == true` if and only if `x < y`, i.e. all representations of equal floating-point value are equivalent;
- If `x` is negative NaN and `y` is not negative NaN, then `weak_order_less(x, y) == true`;
- If `x` is not positive NaN and `y` is positive NaN, then `weak_order_less(x, y) == true`;
- If both `x` and `y` are NaNs with the same sign, then `(weak_order_less(x, y) || weak_order_less(y, x)) == false`, i.e. all NaNs with the same sign are equivalent.

### Example

{/* source:cpp-utility-compare-weak-order:0011 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-utility-compare-weak-order:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`weak_ordering`](/docs/cpp/library/utility/compare/weak_ordering) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>the result type of 3-way comparison that supports all 6 operators and is not substitutable (class)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strong_order`](/docs/cpp/library/utility/compare/strong_order) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs 3-way comparison and produces a result of type `std::strong_ordering` (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`partial_order`](/docs/cpp/library/utility/compare/partial_order) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs 3-way comparison and produces a result of type `std::partial_ordering` (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`compare_weak_order_fallback`](/docs/cpp/library/utility/compare/compare_weak_order_fallback) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs 3-way comparison and produces a result of type `std::weak_ordering`, even if `operator<=>` is unavailable (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
