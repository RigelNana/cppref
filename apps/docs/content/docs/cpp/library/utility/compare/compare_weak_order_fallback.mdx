---
title: "std::compare_weak_order_fallback"
description: "Performs three-way comparison on subexpressions t and u and produces a result of type std::weak_ordering, even if the operator <=> is unavailable."
source_url: "https://en.cppreference.com/w/cpp/utility/compare/compare_weak_order_fallback"
language: "C++"
---
{/* source:cpp-utility-compare-compare-weak-order-fallback:0000 */}
<DeclarationDoc>
Defined in header [`<compare>`](/docs/cpp/library/headers/compare)

  <Declaration language="cpp" since="C++20" code={`inline namespace /* unspecified */ {
    inline constexpr /* unspecified */
        compare_weak_order_fallback = /* unspecified */;

}`} />

Call signature

  <Declaration language="cpp" since="C++20" code={`template< class T, class U >
    requires /* see below */
constexpr std::weak_ordering

    compare_weak_order_fallback( T&& t, U&& u ) noexcept(/* see below */);`} />
</DeclarationDoc>

{/* source:cpp-utility-compare-compare-weak-order-fallback:0001 */}
Performs three-way comparison on [subexpressions](/docs/cpp/language/expressions#Full-expressions) `t` and `u` and produces a result of type [`std::weak_ordering`](/docs/cpp/library/utility/compare/weak_ordering), even if the operator `<=>` is unavailable.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0002 */}
If `std::decay_t<T>` and `std::decay_t<U>` are the same type, `std::compare_weak_order_fallback(t, u)` is [expression-equivalent](/docs/cpp/language/expressions#Expression-equivalence) to:

{/* source:cpp-utility-compare-compare-weak-order-fallback:0003 */}
- `std::weak_order(t, u)`, if it is a well-formed expression;
- otherwise, `t == u ? std::weak_ordering::equivalent :`<br />
  `t <  u ? std::weak_ordering::less :`<br />
  `         std::weak_ordering::greater`<br />
  if the expressions `t == u` and `t < u` are both well-formed and each of `decltype(t == u)` and `decltype(t < u)` models [*boolean-testable*](/docs/cpp/library/concepts/boolean-testable), except that `t` and `u` are evaluated only once.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0004 */}
In all other cases, `std::compare_weak_order_fallback(t, u)` is ill-formed, which can result in [substitution failure](/docs/cpp/language/templates/sfinae) when it appears in the immediate context of a template instantiation.

### Customization point objects

{/* source:cpp-utility-compare-compare-weak-order-fallback:0005 */}
The name `std::compare_weak_order_fallback` denotes a *customization point object*, which is a const [function object](/docs/cpp/library/named_req/FunctionObject) of a [literal](/docs/cpp/library/named_req/LiteralType) [`semiregular`](/docs/cpp/library/concepts/semiregular) class type. For exposition purposes, the cv-unqualified version of its type is denoted as `__compare_weak_order_fallback_fn`.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0006 */}
All instances of `__compare_weak_order_fallback_fn` are equal. The effects of invoking different instances of type `__compare_weak_order_fallback_fn` on the same arguments are equivalent, regardless of whether the expression denoting the instance is an lvalue or rvalue, and is const-qualified or not (however, a volatile-qualified instance is not required to be invocable). Thus, `std::compare_weak_order_fallback` can be copied freely and its copies can be used interchangeably.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0007 */}
Given a set of types `Args...`, if `std::declval<Args>()...` meet the requirements for arguments to `std::compare_weak_order_fallback` above, `__compare_weak_order_fallback_fn` models

{/* source:cpp-utility-compare-compare-weak-order-fallback:0008 */}
- `std::invocable<__compare_weak_order_fallback_fn, Args...>`,
- `std::invocable<const __compare_weak_order_fallback_fn, Args...>`,
- `std::invocable<__compare_weak_order_fallback_fn&, Args...>`, and
- `std::invocable<const __compare_weak_order_fallback_fn&, Args...>`.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0009 */}
Otherwise, no function call operator of `__compare_weak_order_fallback_fn` participates in overload resolution.

### Example

{/* source:cpp-utility-compare-compare-weak-order-fallback:0010 */}
```cpp
#include <compare>
#include <iostream>
 
// does not support <=>
struct Rational_1
{
    int num;
    int den; // > 0
};
 
inline constexpr bool operator<(Rational_1 lhs, Rational_1 rhs)
{
    return lhs.num * rhs.den < rhs.num * lhs.den;
}
 
inline constexpr bool operator==(Rational_1 lhs, Rational_1 rhs)
{
    return lhs.num * rhs.den == rhs.num * lhs.den;
}
 
// supports <=>
struct Rational_2
{
    int num;
    int den; // > 0
};
 
inline constexpr std::weak_ordering operator<=>(Rational_2 lhs, Rational_2 rhs)
{
    return lhs.num * rhs.den <=> rhs.num * lhs.den;
}
 
inline constexpr bool operator==(Rational_2 lhs, Rational_2 rhs)
{
    return lhs <=> rhs == 0;
}
 
void print(int id, std::weak_ordering value)
{
    std::cout << id << ") ";
    if (value == 0)
        std::cout << "equal\n";
    else if (value < 0)
        std::cout << "less\n";
    else
        std::cout << "greater\n";
}
 
int main()
{
    Rational_1 a{1, 2}, b{3, 4};
//  print(0, a <=> b); // does not work
    print(1, std::compare_weak_order_fallback(a, b)); // works, defaults to < and ==
 
    Rational_2 c{6, 5}, d{8, 7};
    print(2, c <=> d); // works
    print(3, std::compare_weak_order_fallback(c, d)); // works
 
    Rational_2 e{2, 3}, f{4, 6};
    print(4, e <=> f); // works
    print(5, std::compare_weak_order_fallback(e, f)); // works
}
```

Output:

```text
1) less
2) greater
3) greater
4) equal
5) equal
```

### Defect reports

{/* source:cpp-utility-compare-compare-weak-order-fallback:0011 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-compare-compare-weak-order-fallback:0012 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2114} standard="C++20">
    <PublishedBehavior>the fallback mechanism only required<br />return types to be convertible to `bool` ([P2167R3](https://wg21.link/P2167R3))</PublishedBehavior>
    <CorrectedBehavior>constraints strengthened</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-compare-compare-weak-order-fallback:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`weak_order`](/docs/cpp/library/utility/compare/weak_order) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs 3-way comparison and produces a result of type `std::weak_ordering` (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
