---
title: "std::tuple_element"
description: "Provides compile-time indexed access to the types of the elements of a tuple-like type."
source_url: "https://en.cppreference.com/w/cpp/utility/tuple_element"
language: "C++"
---

{/* source:cpp-utility-tuple-element:0000 */}
<DeclarationDoc>
Defined in header [`<tuple>`](/docs/cpp/library/headers/tuple)

Defined in header [`<array>`](/docs/cpp/library/headers/array)

Defined in header [`<utility>`](/docs/cpp/library/headers/utility)

Defined in header [`<ranges>`](/docs/cpp/library/headers/ranges) <InlineRevision since="C++20" />

Defined in header [`<complex>`](/docs/cpp/library/headers/complex) <InlineRevision since="C++26" />

  <Declaration id="1" language="cpp" since="C++11" code={`template< std::size_t I, class T >
struct tuple_element; // not defined`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< std::size_t I, class T >
struct tuple_element< I, const T > {
    using type = typename
        std::add_const<typename std::tuple_element<I, T>::type>::type;

};`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< std::size_t I, class T >
struct tuple_element< I, volatile T > {
    using type = typename
        std::add_volatile<typename std::tuple_element<I, T>::type>::type;

};`} />
  (deprecated in C++20)
  <Declaration id="4" language="cpp" since="C++11" code={`template< std::size_t I, class T >
struct tuple_element< I, const volatile T > {
    using type = typename
        std::add_cv<typename std::tuple_element<I, T>::type>::type;

};`} />
  (deprecated in C++20)
</DeclarationDoc>

{/* source:cpp-utility-tuple-element:0001 */}
Provides compile-time indexed access to the types of the elements of a [*tuple-like*](/docs/cpp/library/utility/tuple/tuple-like) type.

{/* source:cpp-utility-tuple-element:0002 */}
1. 1) The primary template is not defined. An explicit (full) or partial specialization is required to make a type *tuple-like*.

{/* source:cpp-utility-tuple-element:0003 */}
2. 2-4) Specializations for cv-qualified types simply add corresponding cv-qualifiers by default.

{/* source:cpp-utility-tuple-element:0004 */}
<Revision since="C++17">
`std::tuple_element` interacts with the core language: it can provide [structured binding](/docs/cpp/language/declarations/structured_binding) support in the tuple-like case.
</Revision>

### Specializations

{/* source:cpp-utility-tuple-element:0005 */}
The standard library provides following specializations for standard library types:

{/* source:cpp-utility-tuple-element:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::tuple_element<std::tuple>`](/docs/cpp/library/utility/tuple/tuple_element) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the type of the specified element (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::tuple_element<std::pair>`](/docs/cpp/library/utility/pair/tuple_element) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the type of the elements of `pair` (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::tuple_element<std::array>`](/docs/cpp/library/container/array/tuple_element) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the type of the elements of `array` (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::tuple_element<std::ranges::subrange>`](/docs/cpp/library/ranges/subrange/tuple_element) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>obtains the type of the iterator or the sentinel of a [`std::ranges::subrange`](/docs/cpp/library/ranges/subrange) (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::tuple_element<std::complex>`](/docs/cpp/library/numeric/complex/tuple_element) <InlineRevision since="C++26" /></DescriptionTerm>
    <DescriptionBody>obtains the underlying real and imaginary number type of a [`std::complex`](/docs/cpp/library/numeric/complex) (class template specialization)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-utility-tuple-element:0007 */}
Users may specialize `std::tuple_element` for program-defined types to make them *tuple-like*.

{/* source:cpp-utility-tuple-element:0008 */}
In normal cases where the get functions returns reference members or reference to subobjects, only specializations for cv-unqualified types are needed to be customized.

### Member types

{/* source:cpp-utility-tuple-element:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`type`</DescriptionTerm>
    <DescriptionBody>for a standard specialization, the type of `I` th element of the [*tuple-like*](/docs/cpp/library/utility/tuple/tuple-like) type `T`, where `I` is in [`0`, `std::tuple_size<T>::value`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Helper types

{/* source:cpp-utility-tuple-element:0010 */}
<DeclarationDoc>
Defined in header [`<tuple>`](/docs/cpp/library/headers/tuple)

  <Declaration language="cpp" since="C++14" code={`template< std::size_t I, class T >
using tuple_element_t = typename tuple_element<I, T>::type;`} />
</DeclarationDoc>

### Notes

{/* source:cpp-utility-tuple-element:0011 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_tuple_element_t`](/docs/cpp/library/feature_test#cpp_lib_tuple_element_t) | [`201402L`](/docs/cpp/library/compiler_support/14#cpp_lib_tuple_element_t_201402L) | (C++14) | `std::tuple_element_t` |

### Example

{/* source:cpp-utility-tuple-element:0012 */}
```cpp
#include <array>
#include <cstddef>
#include <iostream>
#include <ranges>
#include <tuple>
#include <type_traits>
#include <utility>
 
template<typename T1, typename T2, typename T3>
struct Triple
{
    T1 t1;
    T2 t2;
    T3 t3;
};
 
// A specialization of std::tuple_element for program-defined type Triple:
template<std::size_t I, typename T1, typename T2, typename T3>
    struct std::tuple_element<I, Triple<T1, T2, T3>>
    { static_assert(false, "Invalid index"); }; 
template<typename T1, typename T2, typename T3>
    struct std::tuple_element<0, Triple<T1, T2, T3>> { using type = T1; };
template<typename T1, typename T2, typename T3>
    struct std::tuple_element<1, Triple<T1, T2, T3>> { using type = T2; };
template<typename T1, typename T2, typename T3>
    struct std::tuple_element<2, Triple<T1, T2, T3>> { using type = T3; };
 
 
template<typename... Args> struct TripleTypes
{
    static_assert(3 == sizeof...(Args), "Expected exactly 3 type names");
    template<std::size_t N>
    using type = typename std::tuple_element_t<N, Triple<Args...>>;
};
 
int main()
{
    TripleTypes<char, int, float>::type<1> i{42};
    std::cout << i << '\n';
 
    using Tri = Triple<int, char, short>; //< Program-defined type
    static_assert(std::is_same_v<std::tuple_element_t<0, Tri>, int> &&
                  std::is_same_v<std::tuple_element_t<1, Tri>, char> &&
                  std::is_same_v<std::tuple_element_t<2, Tri>, short>);
 
    using Tuple = std::tuple<int, char, short>;
    static_assert(std::is_same_v<std::tuple_element_t<0, Tuple>, int> &&
                  std::is_same_v<std::tuple_element_t<1, Tuple>, char> &&
                  std::is_same_v<std::tuple_element_t<2, Tuple>, short>);
 
    using Array3 = std::array<int, 3>;
    static_assert(std::is_same_v<std::tuple_element_t<0, Array3>, int> &&
                  std::is_same_v<std::tuple_element_t<1, Array3>, int> &&
                  std::is_same_v<std::tuple_element_t<2, Array3>, int>);
 
    using Pair = std::pair<Tuple, Tri>;
    static_assert(std::is_same_v<std::tuple_element_t<0, Pair>, Tuple> &&
                  std::is_same_v<std::tuple_element_t<1, Pair>, Tri>);
 
    using Sub = std::ranges::subrange<int*, int*>;
    static_assert(std::is_same_v<std::tuple_element_t<0, Sub>, int*> &&
                  std::is_same_v<std::tuple_element_t<1, Sub>, int*>);
}
```

Output:

```text
42
```

### Defect reports

{/* source:cpp-utility-tuple-element:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-tuple-element:0014 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2212} standard="C++11">
    <PublishedBehavior>specializations for cv types were not required in some headers, which led to ambiguity</PublishedBehavior>
    <CorrectedBehavior>required</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-tuple-element:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[Structured binding](/docs/cpp/language/declarations/structured_binding) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>binds the specified names to sub-objects or tuple elements of the initializer</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tuple_size`](/docs/cpp/library/utility/tuple_size) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>obtains the number of elements of a tuple-like type (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tuple_cat`](/docs/cpp/library/utility/tuple/tuple_cat) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>creates a `tuple` by concatenating any number of tuples (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
