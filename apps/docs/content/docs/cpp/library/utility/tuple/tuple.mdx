---
title: "std::tuple<Types...>::tuple"
description: "Constructs a new tuple."
source_url: "https://en.cppreference.com/w/cpp/utility/tuple/tuple"
language: "C++"
---

{/* source:cpp-utility-tuple-tuple:0000 */}
<DeclarationDoc>
Defined in header [`<tuple>`](/docs/cpp/library/headers/tuple)

  <Declaration id="1" language="cpp" since="C++11" code={`constexpr tuple();`} />
  (conditionally explicit)
  <Declaration id="2" language="cpp" since="C++11" code={`tuple( const Types&... args );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="3" language="cpp" since="C++11" code={`template< class... UTypes >
tuple( UTypes&&... args );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="4" language="cpp" since="C++23" code={`template< class... UTypes >
constexpr tuple( tuple<UTypes...>& other );`} />
  (conditionally explicit)
  <Declaration id="5" language="cpp" since="C++11" code={`template< class... UTypes >
tuple( const tuple<UTypes...>& other );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="6" language="cpp" since="C++11" code={`template< class... UTypes >
tuple( tuple<UTypes...>&& other );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="7" language="cpp" since="C++23" code={`template< class... UTypes >
constexpr tuple( const tuple<UTypes...>&& other );`} />
  (conditionally explicit)
  <Declaration id="8" language="cpp" since="C++23" code={`template< class U1, class U2 >
constexpr tuple( std::pair<U1, U2>& p );`} />
  (conditionally explicit)
  <Declaration id="9" language="cpp" since="C++11" code={`template< class U1, class U2 >
tuple( const std::pair<U1, U2>& p );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="10" language="cpp" since="C++11" code={`template< class U1, class U2 >
tuple( std::pair<U1, U2>&& p );`} />
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="11" language="cpp" since="C++23" code={`template< class U1, class U2 >
constexpr tuple( const std::pair<U1, U2>&& p );`} />
  (conditionally explicit)
  <Declaration id="12" language="cpp" since="C++23" code={`template< tuple-like UTuple >
constexpr tuple( UTuple&& u );`} />
  (conditionally explicit)
  <Declaration id="13" language="cpp" since="C++11" code={`tuple( const tuple& other ) = default;`} />
  <Declaration id="14" language="cpp" since="C++11" code={`tuple( tuple&& other ) = default;`} />

Allocator-extended constructors

  <Declaration id="15" language="cpp" since="C++11" code={`template< class Alloc >
tuple( std::allocator_arg_t, const Alloc& a );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="16" language="cpp" since="C++11" code={`template< class Alloc >
tuple( std::allocator_arg_t, const Alloc& a,

       const Types&... args );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="17" language="cpp" since="C++11" code={`template< class Alloc, class... UTypes >
tuple( std::allocator_arg_t, const Alloc& a,

       UTypes&&... args );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="18" language="cpp" since="C++23" code={`template< class Alloc, class... UTypes >
constexpr tuple( std::allocator_arg_t, const Alloc& a,

                 tuple<UTypes...>& other );`} />
  (conditionally explicit)
  <Declaration id="19" language="cpp" since="C++11" code={`template< class Alloc, class... UTypes >
tuple( std::allocator_arg_t, const Alloc& a,

       const tuple<UTypes...>& other );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="20" language="cpp" since="C++11" code={`template< class Alloc, class... UTypes >
tuple( std::allocator_arg_t, const Alloc& a,

       tuple<UTypes...>&& other );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="21" language="cpp" since="C++23" code={`template< class Alloc, class... UTypes >
constexpr tuple( std::allocator_arg_t, const Alloc& a,

                 const tuple<UTypes...>&& other );`} />
  (conditionally explicit)
  <Declaration id="22" language="cpp" since="C++23" code={`template< class Alloc, class U1, class U2 >
constexpr tuple( std::allocator_arg_t, const Alloc& a,

                 std::pair<U1, U2>& p );`} />
  (conditionally explicit)
  <Declaration id="23" language="cpp" since="C++11" code={`template< class Alloc, class U1, class U2 >
tuple( std::allocator_arg_t, const Alloc& a,

       const std::pair<U1, U2>& p );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="24" language="cpp" since="C++11" code={`template< class Alloc, class U1, class U2 >
tuple( std::allocator_arg_t, const Alloc& a,

       std::pair<U1, U2>&& p );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="25" language="cpp" since="C++23" code={`template< class Alloc, class U1, class U2 >
constexpr tuple( std::allocator_arg_t, const Alloc& a,

                 const std::pair<U1, U2>&& p );`} />
  (conditionally explicit)
  <Declaration id="26" language="cpp" since="C++23" code={`template< class Alloc, tuple-like UTuple >
constexpr tuple( std::allocator_arg_t, const Alloc& a, UTuple&& u );`} />
  (conditionally explicit)
  <Declaration id="27" language="cpp" since="C++11" code={`template< class Alloc >
tuple( std::allocator_arg_t, const Alloc& a,

       const tuple& other );`} />
  (constexpr since C++20)
  <Declaration id="28" language="cpp" since="C++11" code={`template< class Alloc >
tuple( std::allocator_arg_t, const Alloc& a,

       tuple&& other );`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-utility-tuple-tuple:0001 */}
Constructs a new tuple.

{/* source:cpp-utility-tuple-tuple:0002 */}
In the descriptions that follow, let

{/* source:cpp-utility-tuple-tuple:0003 */}
`i` be in the range [`0`, `sizeof...(Types)`) in order, `Ti` be the ith type in `Types`, and `Ui` be the ith type in a template parameter pack named `UTypes`,

{/* source:cpp-utility-tuple-tuple:0004 */}
where indexing is zero-based.

{/* source:cpp-utility-tuple-tuple:0005 */}
1. 1) Default constructor. [Value-initializes](/docs/cpp/language/initialization/value_initialization) all elements, if any. The default constructor is trivial if `sizeof...(Types) == 0`. This overload participates in overload resolution only if `std::is_default_constructible<Ti>::value` is `true` for all `i`. The constructor is `explicit` if and only if `Ti` is not copy-list-initializable from `{}` for at least one `i`.

{/* source:cpp-utility-tuple-tuple:0006 */}
2. 2) Direct constructor. Initializes each element of the tuple with the corresponding parameter. This overload participates in overload resolution only if `sizeof...(Types) >= 1` and `std::is_copy_constructible<Ti>::value` is `true` for all `i`. This constructor is `explicit` if and only if `std::is_convertible<const Ti&, Ti>::value` is `false` for at least one `i`.

{/* source:cpp-utility-tuple-tuple:0007 */}
3. 3) Converting constructor. Initializes each element of the tuple with the corresponding value in `std::forward<UTypes>(args)`. This overload participates in overload resolution only if

   - `sizeof...(Types) == sizeof...(UTypes)`,
   - `sizeof...(Types) >= 1`,
   - `std::is_constructible<Ti, Ui>::value` is `true` for all `i`, and
   - let `D` be <InlineRevision until="C++20">[`std::decay<U0>::type`](/docs/cpp/library/meta/decay)</InlineRevision> <InlineRevision since="C++20">[`std::remove_cvref_t<U0>`](/docs/cpp/library/meta/remove_cvref)</InlineRevision>, if `sizeof...(Types) == 1`, then `D` is not `std::tuple`, otherwise, if `sizeof...(Types) == 2` or `sizeof...(Types) == 3`, then either `D` is not [`std::allocator_arg_t`](/docs/cpp/library/memory/allocator_arg_t), or `T0` is `std::allocator_arg_t`.
   The constructor is `explicit` if and only if `std::is_convertible<Ui, Ti>::value` is `false` for at least one `i`.

   <Revision since="C++23">
   This constructor is defined as deleted if the initialization of any element that is a reference would [bind it to a temporary object](/docs/cpp/language/initialization/reference_initialization#Lifetime_of_a_temporary).
   </Revision>

{/* source:cpp-utility-tuple-tuple:0008 */}
4. 4-7) Converting constructor. Initializes each element of the tuple with the corresponding element of `other`. Formally, let `FWD(other)` be `std::forward<decltype(other)>(other)`, for all `i`, initializes ith element of the tuple with `std::get<i>(FWD(other))`. This overload participates in overload resolution only if `sizeof...(Types) == sizeof...(UTypes)`, `std::is_constructible_v<Ti, decltype(std::get<i>(FWD(other)))>` is `true` for all `i`, and either `sizeof...(Types)` is not `1`, or (when `Types...` expands to `T` and `UTypes...` expands to `U`) `std::is_convertible_v<decltype(other), T>`, `std::is_constructible_v<T, decltype(other)>`, and `std::is_same_v<T, U>` are all `false`. These constructors are `explicit` if and only if `std::is_convertible_v<decltype(std::get<i>(FWD(other))), Ti>` is `false` for at least one `i`.

   <Revision since="C++23">
   These constructors are defined as deleted if the initialization of any element that is a reference would bind it to a temporary object.
   </Revision>

{/* source:cpp-utility-tuple-tuple:0009 */}
5. 8-11) Pair constructor. Constructs a 2-element tuple with each element constructed from the corresponding element of `p`. Formally, let `FWD(p)` be `std::forward<decltype(p)>(p)`, initializes the first element with `std::get<0>(FWD(p))` and the second element with `std::get<1>(FWD(p))`. This overload participates in overload resolution only if `sizeof...(Types) == 2`, `std::is_constructible_v<T0, decltype(std::get<0>(FWD(p)))>` is `true`, and `std::is_constructible_v<T1, decltype(std::get<1>(FWD(p)))>` is `true`. The constructor is `explicit` if and only if `std::is_convertible_v<decltype(std::get<0>(FWD(p))), T0>` or `std::is_convertible_v<decltype(std::get<1>(FWD(p))), T1>` is `false`.

   <Revision since="C++23">
   These constructors are defined as deleted if the initialization of any element that is a reference would bind it to a temporary object.
   </Revision>

{/* source:cpp-utility-tuple-tuple:0010 */}
6. 12) [*tuple-like*](/docs/cpp/library/utility/tuple/tuple-like) constructor. Constructs a tuple with each element constructed from the corresponding element of `u`. Formally, for all `i`, initializes ith element of the tuple with `std::get<i>(std::forward<UTuple>(u))`. This overload participates in overload resolution only if `std::same_as<std::remove_cvref_t<UTuple>, std::tuple>` is `false`, `std::remove_cvref_t<UTuple>` is not a specialization of [`std::ranges::subrange`](/docs/cpp/library/ranges/subrange), `sizeof...(Types)` equals `std::tuple_size_v<std::remove_cvref_t<UTuple>>`, `std::is_constructible_v<Ti, decltype(std::get<i>(std::forward<UTuple>(u)))>` is `true` for all `i`, and either `sizeof...(Types)` is not `1`, or (when `Types...` expands to `T`) `std::is_convertible_v<UTuple, T>` and `std::is_constructible_v<T, UTuple>` are both `false`. This constructor is defined as deleted if the initialization of any element that is a reference would bind it to a temporary object.

{/* source:cpp-utility-tuple-tuple:0011 */}
7. 13) Implicitly-defined copy constructor. Initializes each element of the tuple with the corresponding element of `other`. This constructor is `constexpr` if every operation it performs is `constexpr`. For the empty tuple `std::tuple<>`, it is `constexpr`. `std::is_copy_constructible<Ti>::value` must be `true` for all `i`, otherwise <InlineRevision until="C++20">the behavior is undefined</InlineRevision> <InlineRevision since="C++20">the program is ill-formed</InlineRevision>.

{/* source:cpp-utility-tuple-tuple:0012 */}
8. 14) Implicitly-defined move constructor. For all `i`, initializes the ith element of the tuple with `std::forward<Ui>(std::get<i>(other))`. This constructor is `constexpr` if every operation it performs is `constexpr`. For the empty tuple `std::tuple<>`, it is `constexpr`. `std::is_move_constructible<Ti>::value` must be `true` for all `i`, otherwise <InlineRevision until="C++20">the behavior is undefined</InlineRevision> <InlineRevision since="C++20">this overload does not participate in overload resolution</InlineRevision>.

{/* source:cpp-utility-tuple-tuple:0013 */}
9. 15-28) Identical to (1-14) except each element is created by [uses-allocator construction](/docs/cpp/library/memory/uses_allocator#Uses-allocator_construction), that is, the Allocator object `a` is passed as an additional argument to the constructor of each element for which `std::uses_allocator<Ui, Alloc>::value` is `true`.

### Parameters

{/* source:cpp-utility-tuple-tuple:0014 */}
<ParameterList>
  <Parameter name="args">values used to initialize each element of the tuple</Parameter>
  <Parameter name="other">the tuple of values used to initialize each element of the tuple</Parameter>
  <Parameter name="p">the pair of values used to initialize both elements of the 2-tuple</Parameter>
  <Parameter name="u">the [*tuple-like*](/docs/cpp/library/utility/tuple/tuple-like) object of values used to initialize each element of the tuple</Parameter>
  <Parameter name="a">the allocator to use in uses-allocator construction</Parameter>
</ParameterList>

### Notes

{/* source:cpp-utility-tuple-tuple:0015 */}
Conditionally-explicit constructors make it possible to construct a tuple in copy-initialization context using list-initialization syntax:

{/* source:cpp-utility-tuple-tuple:0016 */}
```cpp
std::tuple<int, int> foo_tuple() 
{
    // return {1, -1};             // Error before N4387
    return std::make_tuple(1, -1); // Always works
}
```

{/* source:cpp-utility-tuple-tuple:0017 */}
Note that if some element of the list is not implicitly convertible to the corresponding element of the target tuple, the constructors become explicit:

{/* source:cpp-utility-tuple-tuple:0018 */}
```cpp
using namespace std::chrono;
void launch_rocket_at(std::tuple<hours, minutes, seconds>);
 
launch_rocket_at({hours(1), minutes(2), seconds(3)}); // OK
launch_rocket_at({1, 2, 3}); // Error: int is not implicitly convertible to duration
launch_rocket_at(std::tuple<hours, minutes, seconds>{1, 2, 3}); // OK
```

### Example

{/* source:cpp-utility-tuple-tuple:0019 */}
```cpp
#include <iomanip>
#include <iostream>
#include <memory>
#include <string>
#include <string_view>
#include <tuple>
#include <type_traits>
#include <vector>
 
// helper function to print a vector to a stream
template<class Os, class T>
Os& operator<<(Os& os, std::vector<T> const& v)
{
    os << '{';
    for (auto i{v.size()}; const T& e : v)
        os << e << (--i ? "," : "");
    return os << '}';
}
 
template<class T>
void print_single(T const& v)
{
    if constexpr (std::is_same_v<T, std::decay_t<std::string>>)
        std::cout << std::quoted(v);
    else if constexpr (std::is_same_v<std::decay_t<T>, char>)
        std::cout << "'" << v << "'";
    else
        std::cout << v;
}
 
// helper function to print a tuple of any size
template<class Tuple, std::size_t N>
struct TuplePrinter
{
    static void print(const Tuple& t)
    {
        TuplePrinter<Tuple, N - 1>::print(t);
        std::cout << ", ";
        print_single(std::get<N - 1>(t));
    }
};
 
template<class Tuple>
struct TuplePrinter<Tuple, 1>
{
    static void print(const Tuple& t)
    {
        print_single(std::get<0>(t));
    }
};
 
template<class... Args>
void print(std::string_view message, const std::tuple<Args...>& t)
{
    std::cout << message << " (";
    TuplePrinter<decltype(t), sizeof...(Args)>::print(t);
    std::cout << ")\n";
}
// end helper function
 
int main()
{
    std::tuple<int, std::string, double> t1;
    print("Value-initialized, t1:", t1);
 
    std::tuple<int, std::string, double> t2{42, "Test", -3.14};
    print("Initialized with values, t2:", t2);
 
    std::tuple<char, std::string, int> t3{t2};
    print("Implicitly converted, t3:", t3);
 
    std::tuple<int, double> t4{std::make_pair(42, 3.14)};
    print("Constructed from a pair, t4:", t4);
 
    // given Allocator my_alloc with a single-argument constructor
    // my_alloc(int); use my_alloc(1) to allocate 5 ints in a vector
    using my_alloc = std::allocator<int>;
    std::vector<int, my_alloc> v{5, 1, my_alloc{/* 1 */}};
 
    // use my_alloc(2) to allocate 5 ints in a vector in a tuple
    std::tuple<int, std::vector<int, my_alloc>, double> t5
        {std::allocator_arg, my_alloc{/* 2 */}, 42, v, -3.14};
    print("Constructed with allocator, t5:", t5);
}
```

Possible output:

```text
Value-initialized, t1: (0, "", 0)
Initialized with values, t2: (42, "Test", -3.14)
Implicitly converted, t3: ('*', "Test", -3)
Constructed from a pair, t4: (42, 3.14)
Constructed with allocator, t5: (42, {1,1,1,1,1}, -3.14)
```

### Defect reports

{/* source:cpp-utility-tuple-tuple:0020 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-tuple-tuple:0021 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2510} standard="C++11">
    <PublishedBehavior>default constructor was implicit</PublishedBehavior>
    <CorrectedBehavior>made conditionally-explicit</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3121} standard="C++11">
    <PublishedBehavior>constructor of 1-tuple might recursively check the constraints;<br />`allocator_arg_t` argument brought ambiguity</PublishedBehavior>
    <CorrectedBehavior>furtherly constrained<br />the constructor</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3158} standard="C++11">
    <PublishedBehavior>the uses-allocator constructor corresponding<br />to default constructor was implicit</PublishedBehavior>
    <CorrectedBehavior>made conditionally-explicit</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3211} standard="C++11">
    <PublishedBehavior>whether the default constructor of<br />`tuple<>` is trivial was unspecified</PublishedBehavior>
    <CorrectedBehavior>require to be trivial</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={4045} standard="C++23">
    <PublishedBehavior>[*tuple-like*](/docs/cpp/library/utility/tuple/tuple-like) constructor may potentially create dangling references</PublishedBehavior>
    <CorrectedBehavior>made defined as deleted</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="N4387" standard="C++11">
    <PublishedBehavior>some constructors were explicit, preventing useful behavior</PublishedBehavior>
    <CorrectedBehavior>most constructors made<br />conditionally-explicit</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-tuple-tuple:0022 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/utility/tuple/operator_assignment)</DescriptionTerm>
    <DescriptionBody>assigns the contents of one `tuple` to another (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`make_tuple`](/docs/cpp/library/utility/tuple/make_tuple) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>creates a `tuple` object of the type defined by the argument types (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tie`](/docs/cpp/library/utility/tuple/tie) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>creates a [`tuple`](/docs/cpp/library/utility/tuple) of lvalue references or unpacks a tuple into individual objects (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`forward_as_tuple`](/docs/cpp/library/utility/tuple/forward_as_tuple) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>creates a `tuple` of [forwarding references](/docs/cpp/language/declarations/reference#Forwarding_references) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/utility/pair/pair)</DescriptionTerm>
    <DescriptionBody>constructs new `pair` (public member function of `std::pair<T1,T2>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
