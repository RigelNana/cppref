---
title: "std::swap(std::optional)"
description: "Overloads the std::swap algorithm for std::optional. Exchanges the state of lhs with that of rhs. Effectively calls lhs.swap(rhs)."
source_url: "https://en.cppreference.com/w/cpp/utility/optional/swap2"
language: "C++"
---

{/* source:cpp-utility-optional-swap2:0000 */}
<DeclarationDoc>
Defined in header [`<optional>`](/docs/cpp/library/headers/optional)

  <Declaration language="cpp" since="C++17" code={`template< class T >
void swap( std::optional<T>& lhs,

           std::optional<T>& rhs ) noexcept(/* see below */);`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-utility-optional-swap2:0001 */}
Overloads the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm for [`std::optional`](/docs/cpp/library/utility/optional). Exchanges the state of `lhs` with that of `rhs`. Effectively calls `lhs.swap(rhs)`.

{/* source:cpp-utility-optional-swap2:0002 */}
This overload participates in overload resolution only if `std::is_move_constructible_v<T>` and `std::is_swappable_v<T>` are both `true`.

### Parameters

{/* source:cpp-utility-optional-swap2:0003 */}
<ParameterList>
  <Parameter name="lhs, rhs">`optional` objects whose states to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-optional-swap2:0004 */}
(none)

### Exceptions

{/* source:cpp-utility-optional-swap2:0005 */}
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(lhs.swap(rhs)))`

### Notes

{/* source:cpp-utility-optional-swap2:0006 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_optional`](/docs/cpp/library/feature_test#cpp_lib_optional) | [`202106L`](/docs/cpp/library/compiler_support/20#cpp_lib_optional_202106L) | (C++20) (DR20) | Fully `constexpr` |

### Example

{/* source:cpp-utility-optional-swap2:0007 */}
```cpp
#include <iostream>
#include <optional>
#include <string>
 
int main()
{
    std::optional<std::string> a{"██████"}, b{"▒▒▒▒▒▒"};
 
    auto print = [&](auto const& s)
    {
        std::cout << s << "\t"
                     "a = " << a.value_or("(null)") << "  "
                     "b = " << b.value_or("(null)") << '\n';
    };
 
    print("Initially:");
    std::swap(a, b);
    print("swap(a, b):");
    a.reset();
    print("\n""a.reset():");
    std::swap(a, b);
    print("swap(a, b):");
}
```

Output:

```text
Initially:   a = ██████  b = ▒▒▒▒▒▒
swap(a, b):  a = ▒▒▒▒▒▒  b = ██████
 
a.reset():   a = (null)  b = ██████
swap(a, b):  a = ██████  b = (null)
```

### Defect reports

{/* source:cpp-utility-optional-swap2:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-optional-swap2:0009 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2231R1" standard="C++20">
    <PublishedBehavior>`swap` was not `constexpr` while the required operations can be `constexpr` in C++20</PublishedBehavior>
    <CorrectedBehavior>made `constexpr`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-optional-swap2:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/utility/optional/swap)</DescriptionTerm>
    <DescriptionBody>exchanges the contents (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
