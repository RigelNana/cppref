---
title: "std::optional<T>::optional"
description: "Constructs a new optional object."
source_url: "https://en.cppreference.com/w/cpp/utility/optional/optional"
language: "C++"
---

{/* source:cpp-utility-optional-optional:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`constexpr optional() noexcept;
constexpr optional( std::nullopt_t ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++17" code={`constexpr optional( const optional& other );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`constexpr optional( optional&& other ) noexcept(/* see below */);`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template < class U >
optional( const optional<U>& other );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="5" language="cpp" since="C++17" code={`template < class U >
optional( optional<U>&& other );`} />
  (constexpr since C++20)
  (conditionally explicit)
  <Declaration id="6" language="cpp" since="C++17" code={`template< class... Args >
constexpr explicit optional( std::in_place_t, Args&&... args );`} />
  <Declaration id="7" language="cpp" since="C++17" code={`template< class U, class... Args >
constexpr explicit optional( std::in_place_t,
                             std::initializer_list<U> ilist,

                             Args&&... args );`} />
  <Declaration id="8" language="cpp" since="C++17" code={`template < class U = T >
constexpr optional( U&& value );`} />
  (conditionally explicit)
</DeclarationDoc>

{/* source:cpp-utility-optional-optional:0001 */}
Constructs a new optional object.

{/* source:cpp-utility-optional-optional:0002 */}
1. 1) Constructs an object that *does not contain* a value.

{/* source:cpp-utility-optional-optional:0003 */}
2. 2) Copy constructor: If `other` contains a value, initializes the contained value as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` with the expression `*other`. If `other` does not contain a value, constructs an object that *does not contain* a value.

   - This constructor is defined as deleted if `std::is_copy_constructible_v<T>` is `false`.
   - It is a trivial constructor if `std::is_trivially_copy_constructible_v<T>` is `true`.

{/* source:cpp-utility-optional-optional:0004 */}
3. 3) Move constructor: If `other` contains a value, initializes the contained value as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` with the expression `std::move(*other)` and *does not* make `other` empty: a moved-from `std::optional` still *contains* a value, but the value itself is moved from. If `other` does not contain a value, constructs an object that *does not contain* a value.

   - This constructor does not participate in overload resolution unless `std::is_move_constructible_v<T>` is `true`.
   - It is a trivial constructor if `std::is_trivially_move_constructible_v<T>` is `true`.

{/* source:cpp-utility-optional-optional:0005 */}
4. 4) Converting copy constructor: If `other` does not contain a value, constructs an optional object that *does not contain* a value. Otherwise, constructs an optional object that *contains* a value, initialized as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` with the expression `*other`.

   - This constructor does not participate in overload resolution unless the following conditions are met:
     - `std::is_constructible_v<T, const U&>` is `true`.
     - If `T` is not (possibly cv-qualified) `bool`, `T` is not constructible or convertible from any expression of type (possibly `const`) `std::optional<U>`, i.e., the following 8 values are all `false`:
       - `std::is_constructible_v<T, std::optional<U>&>`
       - `std::is_constructible_v<T, const std::optional<U>&>`
       - `std::is_constructible_v<T, std::optional<U>&&>`
       - `std::is_constructible_v<T, const std::optional<U>&&>`
       - `std::is_convertible_v<std::optional<U>&, T>`
       - `std::is_convertible_v<const std::optional<U>&, T>`
       - `std::is_convertible_v<std::optional<U>&&, T>`
       - `std::is_convertible_v<const std::optional<U>&&, T>`
   - This constructor is `explicit` if and only if `std::is_convertible_v<const U&, T>` is `false`.

{/* source:cpp-utility-optional-optional:0006 */}
5. 5) Converting move constructor: If `other` does not contain a value, constructs an optional object that *does not contain* a value. Otherwise, constructs an optional object that *contains* a value, initialized as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` with the expression `std::move(*other)`.

   - This constructor does not participate in overload resolution unless the following conditions are met:
     - `std::is_constructible_v<T, U&&>` is `true`.
     - If `T` is not (possibly cv-qualified) `bool`, `T` is not constructible or convertible from any expression of type (possibly `const`) `std::optional<U>`, i.e., the following 8 values are all `false`:
       - `std::is_constructible_v<T, std::optional<U>&>`
       - `std::is_constructible_v<T, const std::optional<U>&>`
       - `std::is_constructible_v<T, std::optional<U>&&>`
       - `std::is_constructible_v<T, const std::optional<U>&&>`
       - `std::is_convertible_v<std::optional<U>&, T>`
       - `std::is_convertible_v<const std::optional<U>&, T>`
       - `std::is_convertible_v<std::optional<U>&&, T>`
       - `std::is_convertible_v<const std::optional<U>&&, T>`
   - This constructor is `explicit` if and only if `std::is_convertible_v<U&&, T>` is `false`.

{/* source:cpp-utility-optional-optional:0007 */}
6. 6) Constructs an optional object that *contains* a value, initialized as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` from the arguments `std::forward<Args>(args)...`.

   - If the selected constructor of `T` is a `constexpr` constructor, this constructor is a `constexpr` constructor.
   - The function does not participate in the overload resolution unless `std::is_constructible_v<T, Args...>` is `true`.

{/* source:cpp-utility-optional-optional:0008 */}
7. 7) Constructs an optional object that *contains* a value, initialized as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` from the arguments `ilist, std::forward<Args>(args)...`.

   - If the selected constructor of `T` is a `constexpr` constructor, this constructor is a `constexpr` constructor.
   - The function does not participate in the overload resolution unless `std::is_constructible_v<T, std::initializer_list<U>&, Args...>` is `true`.

{/* source:cpp-utility-optional-optional:0009 */}
8. 8) Constructs an optional object that *contains* a value, initialized as if [direct-initializing](/docs/cpp/language/initialization/direct_initialization) (but not direct-list-initializing) an object of type `T` with the expression `std::forward<U>(value)`.

   - If the selected constructor of `T` is a `constexpr` constructor, this constructor is a `constexpr` constructor.
   - This constructor does not participate in overload resolution unless the following conditions are met:
     - `std::is_constructible_v<T, U&&>` is `true`.
     - <InlineRevision until="C++20">[`std::decay_t<U>`](/docs/cpp/library/meta/decay)</InlineRevision> <InlineRevision since="C++20">[`std::remove_cvref_t<U>`](/docs/cpp/library/meta/remove_cvref)</InlineRevision> is neither [`std::in_place_t`](/docs/cpp/library/utility/in_place) nor `std::optional<T>`.
     - If `T` is (possibly cv-qualified) `bool`, <InlineRevision until="C++20">[`std::decay_t<U>`](/docs/cpp/library/meta/decay)</InlineRevision> <InlineRevision since="C++20">[`std::remove_cvref_t<U>`](/docs/cpp/library/meta/remove_cvref)</InlineRevision> is not a specialization of `std::optional`.
   - This constructor is `explicit` if and only if `std::is_convertible_v<U&&, T>` is `false`.

### Parameters

{/* source:cpp-utility-optional-optional:0010 */}
<ParameterList>
  <Parameter name="other">another optional object whose contained value is copied</Parameter>
  <Parameter name="value">value with which to initialize the contained value</Parameter>
  <Parameter name="args...">arguments with which to initialize the contained value</Parameter>
  <Parameter name="ilist">initializer list with which to initialize the contained value</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-utility-optional-optional:0011 */}
2) Throws any exception thrown by the constructor of `T`.

{/* source:cpp-utility-optional-optional:0012 */}
3) Throws any exception thrown by the constructor of `T`. Has the following [`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(std::is_nothrow_move_constructible<T>::value)`

{/* source:cpp-utility-optional-optional:0013 */}
4-8) Throws any exception thrown by the constructor of `T`.

### [Deduction guides](/docs/cpp/library/utility/optional/deduction_guides)

<InlineRevision since="C++17" />

### Notes

{/* source:cpp-utility-optional-optional:0014 */}
Before the resolution of [LWG issue 3836](https://cplusplus.github.io/LWG/issue3836), constructing an `std::optional<bool>` from `std::optional<U>` would select overload ([8](/docs/cpp/library/utility/optional/optional#Version_8)) instead of overloads ([4,5](/docs/cpp/library/utility/optional/optional#Version_4)) if `U` is not `bool`. This is because overloads ([4,5](/docs/cpp/library/utility/optional/optional#Version_4)) did not participate in overload resolution if `T` (`bool` in this case) can be constructed or converted from `std::optional<U>`, but [`std::optional::operator bool`](/docs/cpp/library/utility/optional/operator_bool) makes the conversion possible for any `U`.

{/* source:cpp-utility-optional-optional:0015 */}
As a result, the constructed `std::optional<bool>` always contains a value. That value is determined by whether the provided `std::optional<U>` object contains a value, rather than the `bool` value direct-initialized from the contained value:

{/* source:cpp-utility-optional-optional:0016 */}
```cpp
std::optional<bool> op_false(false);
std::optional<int> op_zero(0);
 
std::optional<int> from_bool(op_false); // OK: contains 0 (initialized from false)
std::optional<bool> from_int(op_zero);  // DEFECT (LWG 3836): contains true because
                                        // op_zero contains a value, even if initializing
                                        // bool from that value gives false
```

{/* source:cpp-utility-optional-optional:0017 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_optional`](/docs/cpp/library/feature_test#cpp_lib_optional) | [`202106L`](/docs/cpp/library/compiler_support/20#cpp_lib_optional_202106L) | (C++20) (DR20) | Fully `constexpr` ([4,5](/docs/cpp/library/utility/optional/optional#Version_4)) |

### Example

{/* source:cpp-utility-optional-optional:0018 */}
```cpp
#include <iostream>
#include <optional>
#include <string>
 
int main()
{
    std::optional<int> o1, // empty
                       o2 = 1, // init from rvalue
                       o3 = o2; // copy-constructor
 
    // calls std::string( initializer_list<CharT> ) constructor
    std::optional<std::string> o4(std::in_place, {'a', 'b', 'c'});
 
    // calls std::string( size_type count, CharT ch ) constructor
    std::optional<std::string> o5(std::in_place, 3, 'A');
 
    // Move-constructed from std::string using deduction guide to pick the type
 
    std::optional o6(std::string{"deduction"});
 
    std::cout << *o2 << ' ' << *o3 << ' ' << *o4 << ' ' << *o5  << ' ' << *o6 << '\n';
}
```

Output:

```text
1 1 abc AAA deduction
```

### Defect reports

{/* source:cpp-utility-optional-optional:0019 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-optional-optional:0020 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3836} standard="C++17">
    <PublishedBehavior>when constructing an `std::optional<bool>`<br />from `std::optional<U>`, the overload resolution<br />would select overload ([8](/docs/cpp/library/utility/optional/optional#Version_8)) if `U` is not `bool`</PublishedBehavior>
    <CorrectedBehavior>always selects the<br />converting copy/move<br />constructor in this case</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P0602R4" standard="C++17">
    <PublishedBehavior>copy/move constructors might not be trivial<br />even if underlying constructor is trivial</PublishedBehavior>
    <CorrectedBehavior>required to<br />propagate triviality</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P2231R1" standard="C++20">
    <PublishedBehavior>converting constructors ([4,5](/docs/cpp/library/utility/optional/optional#Version_4)) from another `std::optional` was<br />not `constexpr` while the required operations can be in C++20</PublishedBehavior>
    <CorrectedBehavior>made `constexpr`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-optional-optional:0021 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`make_optional`](/docs/cpp/library/utility/optional/make_optional) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>creates an `optional` object (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
