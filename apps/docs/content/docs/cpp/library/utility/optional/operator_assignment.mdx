---
title: "std::optional<T>::operator="
description: "Replaces contents of *this with the contents of other."
source_url: "https://en.cppreference.com/w/cpp/utility/optional/operator_assignment"
language: "C++"
---

{/* source:cpp-utility-optional-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`optional& operator=( std::nullopt_t ) noexcept;`} />
  (constexpr since C++20)
  <Declaration id="2" language="cpp" since="C++17" code={`constexpr optional& operator=( const optional& other );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`constexpr optional& operator=( optional&& other ) noexcept(/* see below */);`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class U = T >
optional& operator=( U&& value );`} />
  (constexpr since C++20)
  <Declaration id="5" language="cpp" since="C++17" code={`template< class U >
optional& operator=( const optional<U>& other );`} />
  (constexpr since C++20)
  <Declaration id="6" language="cpp" since="C++17" code={`template< class U >
optional& operator=( optional<U>&& other );`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-utility-optional-operator:0001 */}
Replaces contents of `*this` with the contents of `other`.

{/* source:cpp-utility-optional-operator:0002 */}
1. 1) If `*this` contains a value before the call, the contained value is destroyed by calling its destructor as if by `value().T::~T()`. `*this` does not contain a value after this call.

{/* source:cpp-utility-optional-operator:0003 */}
2. 2,3) Assigns the state of `other`.

   - If both `*this` and `other` do not contain a value, the function has no effect.
   - If `*this` contains a value, but `other` does not, then the contained value is destroyed by calling its destructor. `*this` does not contain a value after the call.
   - If `other` contains a value, then depending on whether `*this` contains a value, the contained value is either [direct-initialized](/docs/cpp/language/initialization/direct_initialization) or assigned from `*other` (2) or `std::move(*other)` (3). Note that a moved-from optional still *contains a value*.
   - Overload (2) is deleted when either `std::is_copy_constructible_v<T>` or `std::is_copy_assignable_v<T>` is `false`. It is trivial if `std::is_trivially_copy_constructible_v<T>`, `std::is_trivially_copy_assignable_v<T>` and `std::is_trivially_destructible_v<T>` are all `true`.
   - Overload (3) does not participate in overload resolution when either `std::is_move_constructible_v<T>` or `std::is_move_assignable_v<T>` is `false`. It is trivial if `std::is_trivially_move_constructible_v<T>`, `std::is_trivially_move_assignable_v<T>` and `std::is_trivially_destructible_v<T>` are all `true`.

{/* source:cpp-utility-optional-operator:0004 */}
3. 4) Perfect-forwarded assignment: depending on whether `*this` contains a value before the call, the contained value is either direct-initialized from `std::forward<U>(value)` or assigned from `std::forward<U>(value)`. The function does not participate in overload resolution unless <InlineRevision until="C++20">[`std::decay_t<U>`](/docs/cpp/library/meta/decay)</InlineRevision> <InlineRevision since="C++20">[`std::remove_cvref_t<U>`](/docs/cpp/library/meta/remove_cvref)</InlineRevision> is not `std::optional<T>`, `std::is_constructible_v<T, U>` is `true`, `std::is_assignable_v<T&, U>` is `true`, and at least one of the following is true:

   - `T` is not a [scalar type](/docs/cpp/language/basic_concepts/type-id);
   - `std::decay_t<U>` is not `T`.

{/* source:cpp-utility-optional-operator:0005 */}
4. 5,6) Assigns the state of `other`.

   - If both `*this` and `other` do not contain a value, the function has no effect.
   - If `*this` contains a value, but `other` does not, then the contained value is destroyed by calling its destructor. `*this` does not contain a value after the call.
   - If `other` contains a value, then depending on whether `*this` contains a value, the contained value is either [direct-initialized](/docs/cpp/language/initialization/direct_initialization) or assigned from `*other` (5) or `std::move(*other)` (6). Note that a moved-from optional still *contains a value*.
   - These overloads do not participate in overload resolution unless the following conditions are met:
     - `T` is not constructible, convertible, or assignable from any expression of type (possibly `const`) `std::optional<U>`, i.e., the following 12 type traits are all `false`:
       - `std::is_constructible_v<T, std::optional<U>&>`
       - `std::is_constructible_v<T, const std::optional<U>&>`
       - `std::is_constructible_v<T, std::optional<U>&&>`
       - `std::is_constructible_v<T, const std::optional<U>&&>`
       - `std::is_convertible_v<std::optional<U>&, T>`
       - `std::is_convertible_v<const std::optional<U>&, T>`
       - `std::is_convertible_v<std::optional<U>&&, T>`
       - `std::is_convertible_v<const std::optional<U>&&, T>`
       - `std::is_assignable_v<T&, std::optional<U>&>`
       - `std::is_assignable_v<T&, const std::optional<U>&>`
       - `std::is_assignable_v<T&, std::optional<U>&&>`
       - `std::is_assignable_v<T&, const std::optional<U>&&>`
     - For overload (5), `std::is_constructible_v<T, const U&>` and `std::is_assignable_v<T&, const U&>` are both `true`.
     - For overload (6), `std::is_constructible_v<T, U>` and `std::is_assignable_v<T&, U>` are both `true`.

### Parameters

{/* source:cpp-utility-optional-operator:0006 */}
<ParameterList>
  <Parameter name="other">another `optional` object whose contained value to assign</Parameter>
  <Parameter name="value">value to assign to the contained value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-optional-operator:0007 */}
`*this`

### Exceptions

{/* source:cpp-utility-optional-operator:0008 */}
2-6) Throws any exception thrown by the constructor or assignment operator of `T`. If an exception is thrown, the initialization state of `*this` (and of `other` in case of ([2,3](/docs/cpp/library/utility/optional/operator_assignment#Version_2)) and ([5,6](/docs/cpp/library/utility/optional/operator_assignment#Version_5))) is unchanged, i.e. if the object contained a value, it still contains a value, and the other way round. The contents of `value` and the contained values of `*this` and `other` depend on the exception safety guarantees of the operation from which the exception originates (copy-constructor, move-assignment, etc.).

{/* source:cpp-utility-optional-operator:0009 */}
3) Has following [`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(std::is_nothrow_move_assignable_v<T> && std::is_nothrow_move_constructible_v<T>)`

### Notes

{/* source:cpp-utility-optional-operator:0010 */}
An optional object `op` may be turned into an empty optional with both `op = {};` and `op = nullopt;`. The first expression constructs an empty `optional` object with `{}` and assigns it to `op`.

{/* source:cpp-utility-optional-operator:0011 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_optional`](/docs/cpp/library/feature_test#cpp_lib_optional) | [`202106L`](/docs/cpp/library/compiler_support/20#cpp_lib_optional_202106L) | (C++20) (DR20) | Fully `constexpr` ([1](/docs/cpp/library/utility/optional/operator_assignment#Version_1)), ([4-6](/docs/cpp/library/utility/optional/operator_assignment#Version_4)) |

### Example

{/* source:cpp-utility-optional-operator:0012 */}
```cpp
#include <iostream>
#include <optional>
 
int main()
{
    std::optional<const char*> s1 = "abc", s2; // constructor
    s2 = s1; // assignment
    s1 = "def"; // decaying assignment (U = char[4], T = const char*)
    std::cout << *s2 << ' ' << *s1 << '\n';
}
```

Output:

```text
abc def
```

### Defect reports

{/* source:cpp-utility-optional-operator:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-optional-operator:0014 */}
<DefectReportList>
  <DefectReport kind="paper" id="P0602R4" standard="C++17">
    <PublishedBehavior>copy/move assignment operator may not be trivial<br />even if underlying operations are trivial</PublishedBehavior>
    <CorrectedBehavior>required to propagate triviality</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P2231R1" standard="C++20">
    <PublishedBehavior>converting assignment operators ([1](/docs/cpp/library/utility/optional/operator_assignment#Version_1)) and ([4-6](/docs/cpp/library/utility/optional/operator_assignment#Version_4)) were not `constexpr`<br />while the required operations can be in C++20</PublishedBehavior>
    <CorrectedBehavior>made `constexpr`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-optional-operator:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`emplace`](/docs/cpp/library/utility/optional/emplace)</DescriptionTerm>
    <DescriptionBody>constructs the contained value in-place (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
