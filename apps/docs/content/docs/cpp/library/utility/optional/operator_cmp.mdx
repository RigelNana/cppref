---
title: "operator==, !=, <, <=, >, >=, <=>(std::optional)"
description: "Performs comparison operations on optional objects."
source_url: "https://en.cppreference.com/w/cpp/utility/optional/operator_cmp"
language: "C++"
---

{/* source:cpp-utility-optional-operator-cmp:0000 */}
<DeclarationDoc>
Defined in header [`<optional>`](/docs/cpp/library/headers/optional)

Compare two `optional` objects

  <Declaration id="1" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator==( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator!=( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator<( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator<=( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator>( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="6" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator>=( const optional<T>& lhs, const optional<U>& rhs );`} />
  <Declaration id="7" language="cpp" since="C++20" code={`template< class T, std::three_way_comparable_with<T> U >
constexpr std::compare_three_way_result_t<T, U>

    operator<=>( const optional<T>& lhs, const optional<U>& rhs );`} />

Compare an `optional` object with a `nullopt`

  <Declaration id="8" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator==( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="9" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator==( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="10" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator!=( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="11" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator!=( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="12" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator<( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="13" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator<( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="14" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator<=( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="15" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator<=( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="16" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator>( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="17" language="cpp" since="C++17" until="C++20" code={`template< class T >
constexpr bool operator>( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="18" language="cpp" since="C++17" until="C++20" code={`template< class T > 
constexpr bool operator>=( const optional<T>& opt, std::nullopt_t ) noexcept;`} />
  <Declaration id="19" language="cpp" since="C++17" until="C++20" code={`template< class T > 
constexpr bool operator>=( std::nullopt_t, const optional<T>& opt ) noexcept;`} />
  <Declaration id="20" language="cpp" since="C++20" code={`template< class T >
constexpr std::strong_ordering

    operator<=>( const optional<T>& opt, std::nullopt_t ) noexcept;`} />

Compare an `optional` object with a value

  <Declaration id="21" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator==( const optional<T>& opt, const U& value );`} />
  <Declaration id="22" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator==( const U& value, const optional<T>& opt );`} />
  <Declaration id="23" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator!=( const optional<T>& opt, const U& value );`} />
  <Declaration id="24" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator!=( const U& value, const optional<T>& opt );`} />
  <Declaration id="25" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator<( const optional<T>& opt, const U& value );`} />
  <Declaration id="26" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator<( const U& value, const optional<T>& opt );`} />
  <Declaration id="27" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator<=( const optional<T>& opt, const U& value );`} />
  <Declaration id="28" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator<=( const U& value, const optional<T>& opt );`} />
  <Declaration id="29" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator>( const optional<T>& opt, const U& value );`} />
  <Declaration id="30" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator>( const U& value, const optional<T>& opt );`} />
  <Declaration id="31" language="cpp" since="C++17" code={`template< class T, class U >
constexpr bool operator>=( const optional<T>& opt, const U& value );`} />
  <Declaration id="32" language="cpp" since="C++17" code={`template< class U, class T >
constexpr bool operator>=( const U& value, const optional<T>& opt );`} />
  <Declaration id="33" language="cpp" since="C++20" code={`template< class T, std::three_way_comparable_with<T> U >
constexpr std::compare_three_way_result_t<T, U>

    operator<=>( const optional<T>& opt, const U& value );`} />
</DeclarationDoc>

{/* source:cpp-utility-optional-operator-cmp:0001 */}
Performs comparison operations on `optional` objects.

{/* source:cpp-utility-optional-operator-cmp:0002 */}
1. 1-7) Compares two `optional` objects, `lhs` and `rhs`. The contained values are compared (using the corresponding operator of `T`) only if both `lhs` and `rhs` contain values. Otherwise,

   - `lhs` is considered *equal to* `rhs` if, and only if, both `lhs` and `rhs` do not contain a value.
   - `lhs` is considered *less than* `rhs` if, and only if, `rhs` contains a value and `lhs` does not.

{/* source:cpp-utility-optional-operator-cmp:0003 */}
2. 1-6) Let `@` denote the corresponding comparison operator, for each of these functions:

   <Revision until="C++26">
   If the corresponding expression `*lhs @ *rhs` is ill-formed or its result is not convertible to `bool`, the program is ill-formed.
   </Revision>

   <Revision since="C++26">
   This overload participates in overload resolution only if the corresponding expression `*lhs @ *rhs` is well-formed and its result is convertible to `bool`.
   </Revision>

{/* source:cpp-utility-optional-operator-cmp:0004 */}
3. 8-20) Compares `opt` with a `nullopt`. Equivalent to (1-6) when comparing to an `optional` that does not contain a value.

   <Revision since="C++20">
   The `<`, `<=`, `>`, `>=`, and `!=` operators are [synthesized](/docs/cpp/language/expressions/operators#Comparison_operators) from `operator<=>` and `operator==` respectively.
   </Revision>

{/* source:cpp-utility-optional-operator-cmp:0005 */}
4. 21-33) Compares `opt` with a `value`. The values are compared (using the corresponding operator of `T`) only if `opt` contains a value. Otherwise, `opt` is considered *less than* `value`.

{/* source:cpp-utility-optional-operator-cmp:0006 */}
5. 21-32) Let `@` denote the corresponding comparison operator, for each of these functions:

   <Revision until="C++26">
   If the corresponding expression `*opt @ value` or `value @ *opt` (depending on the positions of the operands) is ill-formed or its result is not convertible to `bool`, the program is ill-formed.
   </Revision>

   <Revision since="C++26">
   This overload participates in overload resolution only if all following conditions are satisfied:

   - `U` is not a specialization of [`std::optional`](/docs/cpp/library/utility/optional).
   - The corresponding expression `*opt @ value` or `value @ *opt` (depending on the positions of the operands) is well-formed and its result is convertible to `bool`.
   </Revision>

### Parameters

{/* source:cpp-utility-optional-operator-cmp:0007 */}
<ParameterList>
  <Parameter name="lhs, rhs, opt">an `optional` object to compare</Parameter>
  <Parameter name="value">value to compare to the contained value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-optional-operator-cmp:0008 */}
1. 1) `lhs.has_value() != rhs.has_value() ? false :`<br />`   (lhs.has_value() == false ? true : *lhs == *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0009 */}
2. 2) `lhs.has_value() != rhs.has_value() ? true :`<br />`   (lhs.has_value() == false ? false : *lhs != *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0010 */}
3. 3) `!rhs ? false : (!lhs ? true : *lhs < *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0011 */}
4. 4) `!lhs ? false : (!rhs ? true : *lhs > *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0012 */}
5. 5) `!lhs ? true : (!rhs ? false : *lhs <= *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0013 */}
6. 6) `!rhs ? true : (!lhs ? false : *lhs >= *rhs)`

{/* source:cpp-utility-optional-operator-cmp:0014 */}
7. 7) `lhs && rhs ? *lhs <=> *rhs : lhs.has_value() <=> rhs.has_value()`

{/* source:cpp-utility-optional-operator-cmp:0015 */}
8. 8,9) `!opt`

{/* source:cpp-utility-optional-operator-cmp:0016 */}
9. 10,11) `opt.has_value()`

{/* source:cpp-utility-optional-operator-cmp:0017 */}
10. 12) `false`

{/* source:cpp-utility-optional-operator-cmp:0018 */}
11. 13) `opt.has_value()`

{/* source:cpp-utility-optional-operator-cmp:0019 */}
12. 14) `!opt`

{/* source:cpp-utility-optional-operator-cmp:0020 */}
13. 15) `true`

{/* source:cpp-utility-optional-operator-cmp:0021 */}
14. 16) `opt.has_value()`

{/* source:cpp-utility-optional-operator-cmp:0022 */}
15. 17) `false`

{/* source:cpp-utility-optional-operator-cmp:0023 */}
16. 18) `true`

{/* source:cpp-utility-optional-operator-cmp:0024 */}
17. 19) `!opt`

{/* source:cpp-utility-optional-operator-cmp:0025 */}
18. 20) `opt.has_value() <=> false`

{/* source:cpp-utility-optional-operator-cmp:0026 */}
19. 21) `opt.has_value() ? *opt == value : false`

{/* source:cpp-utility-optional-operator-cmp:0027 */}
20. 22) `opt.has_value() ? value == *opt : false`

{/* source:cpp-utility-optional-operator-cmp:0028 */}
21. 23) `opt.has_value() ? *opt != value : true`

{/* source:cpp-utility-optional-operator-cmp:0029 */}
22. 24) `opt.has_value() ? value != *opt : true`

{/* source:cpp-utility-optional-operator-cmp:0030 */}
23. 25) `opt.has_value() ? *opt < value  : true`

{/* source:cpp-utility-optional-operator-cmp:0031 */}
24. 26) `opt.has_value() ? value < *opt  : false`

{/* source:cpp-utility-optional-operator-cmp:0032 */}
25. 27) `opt.has_value() ? *opt <= value : true`

{/* source:cpp-utility-optional-operator-cmp:0033 */}
26. 28) `opt.has_value() ? value <= *opt : false`

{/* source:cpp-utility-optional-operator-cmp:0034 */}
27. 29) `opt.has_value() ? *opt > value  : false`

{/* source:cpp-utility-optional-operator-cmp:0035 */}
28. 30) `opt.has_value() ? value > *opt  : true`

{/* source:cpp-utility-optional-operator-cmp:0036 */}
29. 31) `opt.has_value() ? *opt >= value : false`

{/* source:cpp-utility-optional-operator-cmp:0037 */}
30. 32) `opt.has_value() ? value >= *opt : true`

{/* source:cpp-utility-optional-operator-cmp:0038 */}
31. 33) `opt.has_value() ? *opt <=> value : std::strong_ordering::less`

### Exceptions

{/* source:cpp-utility-optional-operator-cmp:0039 */}
1-7) May throw implementation-defined exceptions.

{/* source:cpp-utility-optional-operator-cmp:0040 */}
21-33) Throws when and what the comparison throws.

### Notes

{/* source:cpp-utility-optional-operator-cmp:0041 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_constrained_equality`](/docs/cpp/library/feature_test#cpp_lib_constrained_equality) | [`202403L`](/docs/cpp/library/compiler_support/26#cpp_lib_constrained_equality_202403L) | (C++26) | constrained comparison operators for [`std::optional`](/docs/cpp/library/utility/optional) |

### Defect reports

{/* source:cpp-utility-optional-operator-cmp:0042 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-optional-operator-cmp:0043 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2945} standard="C++17">
    <PublishedBehavior>order of template parameters inconsistent for compare-with-T cases</PublishedBehavior>
    <CorrectedBehavior>made consistent</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
