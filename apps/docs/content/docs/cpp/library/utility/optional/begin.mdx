---
title: "std::optional<T>::begin"
description: "If *this contains a value, returns an iterator to the contained value. Otherwise, a past-the-end iterator value."
source_url: "https://en.cppreference.com/w/cpp/utility/optional/begin"
language: "C++"
---

{/* source:cpp-utility-optional-begin:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++26" code={`constexpr iterator begin() noexcept;`} />
  <Declaration language="cpp" since="C++26" code={`constexpr const_iterator begin() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-utility-optional-begin:0001 */}
If `*this` contains a value, returns an iterator to the contained value. Otherwise, a past-the-end iterator value.

### Parameters

{/* source:cpp-utility-optional-begin:0002 */}
(none)

### Return value

{/* source:cpp-utility-optional-begin:0003 */}
Iterator to the contained value if `has_value()` is `true`. Otherwise, a past-the-end iterator.

### Complexity

{/* source:cpp-utility-optional-begin:0004 */}
Constant.

### Notes

{/* source:cpp-utility-optional-begin:0005 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_optional_range_support`](/docs/cpp/library/feature_test#cpp_lib_optional_range_support) | [`202406L`](/docs/cpp/library/compiler_support/26#cpp_lib_optional_range_support_202406L) | (C++26) | Range support for `std::optional` |

### Example

{/* source:cpp-utility-optional-begin:0006 */}
```cpp
#include <optional>
#include <print>
#include <vector>
 
int main()
{
    constexpr std::optional<int> none = std::nullopt;
    constexpr std::optional<int> some = 42;
 
    static_assert(none.begin() == none.end());
    static_assert(some.begin() != some.end());
 
    // ranged-for loop support
    for (int i : none)
        std::println("'none' has a value of {}", i);
 
    for (int i : some)
        std::println("'some' has a value of {}", i);
 
    std::optional<std::vector<int>> many({0, 1, 2});
    for (const auto& v : many)
        std::println("'many' has a value of {}", v);
}
```

Output:

```text
'some' has a value of 42
'many' has a value of [0, 1, 2]
```

### See also

{/* source:cpp-utility-optional-begin:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`end`](/docs/cpp/library/utility/optional/end) <InlineRevision since="C++26" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the end (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
