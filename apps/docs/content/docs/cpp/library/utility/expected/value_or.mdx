---
title: "std::expected<T,E>::value_or"
description: "Returns the expected value if it exists, otherwise returns default_value."
source_url: "https://en.cppreference.com/w/cpp/utility/expected/value_or"
language: "C++"
---

{/* source:cpp-utility-expected-value-or:0000 */}
<DeclarationDoc>
Primary template

  <Declaration id="1" language="cpp" since="C++23" code={`template< class U >
constexpr T value_or( U&& default_value ) const&;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`template< class U >
constexpr T value_or( U&& default_value ) &&;`} />
</DeclarationDoc>

{/* source:cpp-utility-expected-value-or:0001 */}
Returns the expected value if it exists, otherwise returns `default_value`.

{/* source:cpp-utility-expected-value-or:0002 */}
The `void` partial specialization does not have these member functions.

{/* source:cpp-utility-expected-value-or:0003 */}
1) If `std::is_copy_constructible_v<T>` or `std::is_convertible_v<U, T>` is `false`, the program is ill-formed.

{/* source:cpp-utility-expected-value-or:0004 */}
2) If `std::is_move_constructible_v<T>` or `std::is_convertible_v<U, T>` is `false`, the program is ill-formed.

### Parameters

{/* source:cpp-utility-expected-value-or:0005 */}
<ParameterList>
  <Parameter name="default_value">the value to use in case `*this` does not contain an expected value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-expected-value-or:0006 */}
1) `has_value() ? **this : static_cast<T>(std::forward<U>(default_value))`

{/* source:cpp-utility-expected-value-or:0007 */}
2) `has_value() ? std::move(**this) : static_cast<T>(std::forward<U>(default_value))`

### Example

{/* source:cpp-utility-expected-value-or:0008 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-utility-expected-value-or:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`value`](/docs/cpp/library/utility/expected/value)</DescriptionTerm>
    <DescriptionBody>returns the expected value (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
