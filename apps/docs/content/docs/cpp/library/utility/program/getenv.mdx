---
title: "std::getenv"
description: "Searches the environment list provided by the host environment (the OS), for a string that matches the C string pointed to by env_var and returns a pointer to the C string that is associated with the matched environment list member."
source_url: "https://en.cppreference.com/w/cpp/utility/program/getenv"
language: "C++"
---
{/* source:cpp-utility-program-getenv:0000 */}
<DeclarationDoc>
Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)

    <Declaration language="cpp" code={`char* getenv( const char* env_var );`} />
</DeclarationDoc>

{/* source:cpp-utility-program-getenv:0001 */}
Searches the *environment list* provided by the host environment (the OS), for a string that matches the C string pointed to by `env_var` and returns a pointer to the C string that is associated with the matched environment list member.

{/* source:cpp-utility-program-getenv:0002 */}
<Revision until="C++11">
This function is not required to be thread-safe. Another call to `getenv`, as well as a call to the POSIX functions [`setenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/setenv.html), [`unsetenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/unsetenv.html), and [`putenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/putenv.html) may invalidate the pointer returned by a previous call or modify the string obtained from a previous call.
</Revision>

<Revision since="C++11">
This function is thread-safe (calling it from multiple threads does not introduce a data race) as long as no other function modifies the host environment. In particular, the POSIX functions [`setenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/setenv.html), [`unsetenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/unsetenv.html), and [`putenv()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/putenv.html) would introduce a data race if called without synchronization.
</Revision>

{/* source:cpp-utility-program-getenv:0003 */}
Modifying the string returned by `getenv` invokes undefined behavior.

### Parameters

{/* source:cpp-utility-program-getenv:0004 */}
<ParameterList>
  <Parameter name="env_var">null-terminated character string identifying the name of the environmental variable to look for</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-program-getenv:0005 */}
Character string identifying the value of the environmental variable or null pointer if such variable is not found.

### Notes

{/* source:cpp-utility-program-getenv:0006 */}
On POSIX systems, the [environment variables](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap08.html#tag_08) are also accessible through the global variable `environ`, declared as `extern char** environ;` in `<unistd.h>`, and through the optional third argument, `envp`, of [the main function](/docs/cpp/language/basic_concepts/main_function).

### Example

{/* source:cpp-utility-program-getenv:0007 */}
```cpp
#include <cstdlib>
#include <iostream>
 
int main()
{
    if (const char* env_p = std::getenv("PATH"))
        std::cout << "Your PATH is: " << env_p << '\n';
}
```

Possible output:

```text
Your PATH is: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games
```

### See also

{/* source:cpp-utility-program-getenv:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/program/getenv)</DescriptionTerm>
    <DescriptionBody>for `getenv`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
