---
title: "std::unreachable"
description: "Invokes undefined behavior at a given point."
source_url: "https://en.cppreference.com/w/cpp/utility/unreachable"
language: "C++"
---

{/* source:cpp-utility-unreachable:0000 */}
<DeclarationDoc>
Defined in header [`<utility>`](/docs/cpp/library/headers/utility)

  <Declaration language="cpp" since="C++23" code={`[[noreturn]] void unreachable();`} />
</DeclarationDoc>

{/* source:cpp-utility-unreachable:0001 */}
Invokes [undefined behavior](/docs/cpp/language/basic_concepts/ub) at a given point.

{/* source:cpp-utility-unreachable:0002 */}
An implementation may use this to optimize impossible code branches away (typically, in optimized builds) or to trap them to prevent further execution (typically, in debug builds).

### Notes

{/* source:cpp-utility-unreachable:0003 */}
| [`Feature-test`](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_unreachable`](/docs/cpp/library/feature_test#cpp_lib_unreachable) | [`202202L`](/docs/cpp/library/compiler_support/23#cpp_lib_unreachable_202202L) | (C++23) | `std::unreachable` |

### Possible implementation

{/* source:cpp-utility-unreachable:0004 */}
```cpp
[[noreturn]] inline void unreachable()
{
    // Uses compiler specific extensions if possible.
    // Even if no extension is used, undefined behavior is still raised by
    // an empty function body and the noreturn attribute.
#if defined(_MSC_VER) && !defined(__clang__) // MSVC
    __assume(false);
#else // GCC, Clang
    __builtin_unreachable();
#endif
}
```

### Example

{/* source:cpp-utility-unreachable:0005 */}
```cpp
#include <cassert>
#include <cstddef>
#include <cstdint>
#include <utility>
#include <vector>
 
struct Color { std::uint8_t r, g, b, a; };
 
// Assume that only restricted set of texture caps is supported.
void generate_texture(std::vector<Color>& tex, std::size_t xy)
{
    switch (xy)
    {
    case 128: [[fallthrough]];
    case 256: [[fallthrough]];
    case 512: /* ... */
        tex.clear();
        tex.resize(xy * xy, Color{0, 0, 0, 0});
        break;
    default:
        std::unreachable();
    }
}
 
int main()
{
    std::vector<Color> tex;
    generate_texture(tex, 128); // OK
    assert(tex.size() == 128 * 128);
    generate_texture(tex, 32);  // Results in undefined behavior
}
```

Possible output:

```text
Segmentation fault
```

### See also

{/* source:cpp-utility-unreachable:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[[`assume`](/docs/cpp/language/declarations/attributes/assume)(expression)]] <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>specifies that the *expression* will always evaluate to `true` at a given point (attribute specifier)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`assume_aligned`](/docs/cpp/library/memory/assume_aligned) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>informs the compiler that a pointer is aligned (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/program/unreachable) for `unreachable`

### External Links

{/* source:cpp-utility-unreachable:0007 */}
1. [GCC docs: `__builtin_unreachable`](https://gcc.gnu.org/onlinedocs/gcc/Other-Builtins.html#index-_005f_005fbuiltin_005funreachable)
2. [Clang docs: `__builtin_unreachable`](https://clang.llvm.org/docs/LanguageExtensions.html#builtin-unreachable)
3. [MSVC docs: `__assume`](https://docs.microsoft.com/en-us/cpp/intrinsics/assume)
