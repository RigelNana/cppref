---
title: "std::exit"
description: "Causes normal program termination to occur."
source_url: "https://en.cppreference.com/w/cpp/utility/program/exit"
language: "C++"
---
{/* source:cpp-utility-program-exit:0000 */}
<DeclarationDoc>
Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)

    <Declaration language="cpp" until="C++11" code={`             void exit( int exit_code );`} />
    <Declaration language="cpp" since="C++11" code={`[[noreturn]] void exit( int exit_code );`} />
</DeclarationDoc>

{/* source:cpp-utility-program-exit:0001 */}
Causes normal program termination to occur.

{/* source:cpp-utility-program-exit:0002 */}
Several cleanup steps are performed:

{/* source:cpp-utility-program-exit:0003 */}
<Revision until="C++11">
1) Objects with static storage duration are destroyed and functions registered by calling [`std::atexit`](/docs/cpp/library/utility/program/atexit) are called:
a) Non-local objects with static storage duration are destroyed in the reverse order of the completion of their constructor.
b) Functions registered with [`std::atexit`](/docs/cpp/library/utility/program/atexit) are called in the reverse order of their registration, except that a function is called after any previously registered functions that had already been called at the time it was registered.
c) For each function `f` registered with [`std::atexit`](/docs/cpp/library/utility/program/atexit) and each non-local object `obj` of static storage duration,
- if `f` is registered before the initialization of `obj`, `f` will only be called after the destruction of `obj`;
- if `f` is registered after the initialization of `obj`, `f` will only be called before the destruction of `obj`.
d) For each local object `obj` with static storage duration, `obj` is destroyed as if a function calling the destructor of `obj` were registered with [`std::atexit`](/docs/cpp/library/utility/program/atexit) at the completion of the constructor of `obj`.
</Revision>

<Revision since="C++11">
1) The destructors of objects with thread local [storage duration](/docs/cpp/language/declarations/storage_duration) that are associated with the current thread, the destructors of objects with static storage duration, and the functions registered with [`std::atexit`](/docs/cpp/library/utility/program/atexit) are executed concurrently, while maintaining the following guarantees:
a) The last destructor for thread-local objects is [sequenced-before](/docs/cpp/language/expressions/eval_order) the first destructor for a static object.
b) If the completion of the constructor or [dynamic initialization](/docs/cpp/language/initialization#Dynamic_initialization) for thread-local or static object A was sequenced-before thread-local or static object B, the completion of the destruction of B is sequenced-before the start of the destruction of A.
c) If the completion of the initialization of a static object A was sequenced-before the call to [`std::atexit`](/docs/cpp/library/utility/program/atexit) for some function F, the call to F during termination is sequenced-before the start of the destruction of A.
d) If the call to [`std::atexit`](/docs/cpp/library/utility/program/atexit) for some function F was sequenced-before the completion of initialization of a static object A, the start of the destruction of A is sequenced-before the call to F during termination.
e) If a call to [`std::atexit`](/docs/cpp/library/utility/program/atexit) for some function F1 was sequenced-before the call to [`std::atexit`](/docs/cpp/library/utility/program/atexit) for some function F2, then the call to F2 during termination is sequenced-before the call to F1.
</Revision>

{/* source:cpp-utility-program-exit:0004 */}
- In the above,
  - If any function registered with `atexit` or any destructor of static/thread-local object throws an exception, [`std::terminate`](/docs/cpp/library/error/terminate) is called.
  - If the compiler opted to lift dynamic initialization of an object to the static initialization phase of [non-local initialization](/docs/cpp/language/initialization), the sequencing of destruction honors its would-be dynamic initialization.
  - If a function-local (block-scope) static object was destroyed and then that function is called from the destructor of another static object and the control flow passes through the definition of that object (or if it is used indirectly, via pointer or reference), the behavior is undefined.
  - If a function-local (block-scope) static object was initialized during construction of a subobject of a class or array, it is only destroyed after all subobjects of that class or all elements of that array were destroyed.

{/* source:cpp-utility-program-exit:0005 */}
2) All C streams are flushed and closed.

{/* source:cpp-utility-program-exit:0006 */}
3) Files created by [`std::tmpfile`](/docs/cpp/library/io/c/tmpfile) are removed.

{/* source:cpp-utility-program-exit:0007 */}
4) Control is returned to the host environment. If `exit_code` is 0 or [`EXIT_SUCCESS`](/docs/cpp/library/utility/program/EXIT_status), an implementation-defined status indicating successful termination is returned. If `exit_code` is [`EXIT_FAILURE`](/docs/cpp/library/utility/program/EXIT_status), an implementation-defined status indicating unsuccessful termination is returned. In other cases implementation-defined status value is returned.

{/* source:cpp-utility-program-exit:0008 */}
Stack is not unwound: destructors of variables with automatic [storage duration](/docs/cpp/language/declarations/storage_duration) are not called.

### Relationship with the main function

{/* source:cpp-utility-program-exit:0009 */}
Returning from the [main function](/docs/cpp/language/basic_concepts/main_function), either by a return statement or by reaching the end of the function performs the normal function termination (calls the destructors of the variables with automatic [storage durations](/docs/cpp/language/declarations/storage_duration)) and then executes `std::exit`, passing the argument of the return statement (or `0` if implicit return was used) as `exit_code`.

### Parameters

{/* source:cpp-utility-program-exit:0010 */}
<ParameterList>
  <Parameter name="exit_code">exit status of the program</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-program-exit:0011 */}
(none)

### Example

{/* source:cpp-utility-program-exit:0012 */}
```cpp
#include <cstdlib>
#include <iostream>
 
struct Static
{
    ~Static() 
    {
        std::cout << "Static destructor\n";
    }
};
 
struct Local
{
    ~Local() 
    {
        std::cout << "Local destructor\n";
    }
};
 
Static static_variable; // Destructor of this object *will* be called
 
void atexit_handler()
{
    std::cout << "atexit handler\n";
}
 
int main()
{
    Local local_variable; // Destructor of this object will *not* be called
    const int result = std::atexit(atexit_handler); // Handler will be called
 
    if (result != 0)
    {
        std::cerr << "atexit registration failed\n";
        return EXIT_FAILURE;
    }
 
    std::cout << "test\n";
    std::exit(EXIT_FAILURE);
 
    std::cout << "this line will *not* be executed\n";
}
```

Output:

```text
test
atexit handler
Static destructor
```

### Defect reports

{/* source:cpp-utility-program-exit:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-program-exit:0014 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3} standard="C++98">
    <PublishedBehavior>during cleanup, the behavior was unclear when (1) a function is registered with [`std::atexit`](/docs/cpp/library/utility/program/atexit) or (2) a static local object is initialized</PublishedBehavior>
    <CorrectedBehavior>made clear</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-program-exit:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`abort`](/docs/cpp/library/utility/program/abort)</DescriptionTerm>
    <DescriptionBody>causes abnormal program termination (without cleaning up) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atexit`](/docs/cpp/library/utility/program/atexit)</DescriptionTerm>
    <DescriptionBody>registers a function to be called on `std::exit()` invocation (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`quick_exit`](/docs/cpp/library/utility/program/quick_exit) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>causes quick program termination without completely cleaning up (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`at_quick_exit`](/docs/cpp/library/utility/program/at_quick_exit) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>registers a function to be called on [`std::quick_exit`](/docs/cpp/library/utility/program/quick_exit) invocation (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/program/exit)</DescriptionTerm>
    <DescriptionBody>for `exit`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
