---
title: "operator==, !=, <, <=, >, >=, <=>(std::variant)"
description: "Performs comparison operations on std::variant objects."
source_url: "https://en.cppreference.com/w/cpp/utility/variant/operator_cmp"
language: "C++"
---

{/* source:cpp-utility-variant-operator-cmp:0000 */}
<DeclarationDoc>
Defined in header [`<variant>`](/docs/cpp/library/headers/variant)

  <Declaration id="1" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator==( const std::variant<Types...>& lhs,

                           const std::variant<Types...>& rhs );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator!=( const std::variant<Types...>& lhs,

                           const std::variant<Types...>& rhs );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator<( const std::variant<Types...>& lhs,

                          const std::variant<Types...>& rhs );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator>( const std::variant<Types...>& lhs,

                          const std::variant<Types...>& rhs );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator<=( const std::variant<Types...>& lhs,

                           const std::variant<Types...>& rhs );`} />
  <Declaration id="6" language="cpp" since="C++17" code={`template< class... Types >
constexpr bool operator>=( const std::variant<Types...>& lhs,

                           const std::variant<Types...>& rhs );`} />
  <Declaration id="7" language="cpp" since="C++20" code={`template< class... Types >
constexpr std::common_comparison_category_t
              <std::compare_three_way_result_t<Types>...>
    operator<=>( const std::variant<Types...>& lhs,

                 const std::variant<Types...>& rhs );`} />

Helper function template

  <Declaration id="8" language="cpp" code={`template< std::size_t I, class... Types >
constexpr const std::variant_alternative_t<I, std::variant<Types...>>&

    GET( const variant<Types...>& v );`} />
  (exposition only*)
</DeclarationDoc>

{/* source:cpp-utility-variant-operator-cmp:0001 */}
Performs comparison operations on [`std::variant`](/docs/cpp/library/utility/variant) objects.

{/* source:cpp-utility-variant-operator-cmp:0002 */}
1-7) Compares two `std::variant` objects `lhs` and `rhs`. The contained values are compared (using the corresponding operator of `T`) only if both `lhs` and `rhs` contain values corresponding to the same index. Otherwise, `lhs` is considered equal to `rhs` if, and only if, both `lhs` and `rhs` do not contain a value. `lhs` is considered less than `rhs` if, and only if, either `rhs` contains a value and `lhs` does not, or `lhs.index()` is less than `rhs.index()`.

{/* source:cpp-utility-variant-operator-cmp:0003 */}
1-6) Let `@` denote the corresponding comparison operator, for each of these functions:

<Revision until="C++26">
If, for some values of `I`, the corresponding expression `GET<I>(lhs) @ GET<I>(rhs)` is ill-formed or its result is not convertible to `bool`, the program is ill-formed.
</Revision>

<Revision since="C++26">
This overload participates in overload resolution only if for all values of `I`, the corresponding expression `GET<I>(lhs) @ GET<I>(rhs)` is well-formed and its result is convertible to `bool`.
</Revision>

{/* source:cpp-utility-variant-operator-cmp:0004 */}
8) The exposition-only function template `GET` behaves like [`std::get(std::variant)`](/docs/cpp/library/utility/variant/get), except that [`std::bad_variant_access`](/docs/cpp/library/utility/variant/bad_variant_access) is never thrown.

{/* source:cpp-utility-variant-operator-cmp:0005 */}
If `I < sizeof...(Types)` is `false`, the program is ill-formed.

{/* source:cpp-utility-variant-operator-cmp:0006 */}
If `I == v.index()` is `false`, the behavior is undefined.

### Parameters

{/* source:cpp-utility-variant-operator-cmp:0007 */}
<ParameterList>
  <Parameter name="lhs,rhs">variants to compare</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-variant-operator-cmp:0008 */}
| Operator | Both operands contains a value<br />(let `I` be `lhs.index()` and `J` be `rhs.index()`) | | `lhs` or `rhs` is valueless<br />(let `lhs_empty` be `lhs.valueless_by_exception()` and `rhs_empty` be `rhs.valueless_by_exception()`) |
| --- | --- | --- | --- |
| | `I` and `J` are equal | `I` and `J` are unequal | |
| `==` | `GET<I>(lhs) == GET<I>(rhs)` | `false` | `lhs_empty && rhs_empty` |
| `!=` | `GET<I>(lhs) != GET<I>(rhs)` | `true` | `lhs_empty != rhs_empty` |
| `<` | `GET<I>(lhs) < GET<I>(rhs)` | `lhs.index() < rhs.index()` | `lhs_empty && !rhs_empty` |
| `>` | `GET<I>(lhs) > GET<I>(rhs)` | `lhs.index() > rhs.index()` | `!lhs_empty && rhs_empty` |
| `<=` | `GET<I>(lhs) <= GET<I>(rhs)` | `lhs.index() < rhs.index()` | `lhs_empty` |
| `>=` | `GET<I>(lhs) >= GET<I>(rhs)` | `lhs.index() > rhs.index()` | `rhs_empty` |
| `<=>` | `GET<I>(lhs) <=> GET<I>(rhs)` | `lhs.index() <=> rhs.index()` | see below |

{/* source:cpp-utility-variant-operator-cmp:0009 */}
For `operator<=>`:

{/* source:cpp-utility-variant-operator-cmp:0010 */}
- If only `lhs` is valueless, returns [`std::strong_ordering::less`](/docs/cpp/library/utility/compare/strong_ordering).
- If only `rhs` is valueless, returns [`std::strong_ordering::greater`](/docs/cpp/library/utility/compare/strong_ordering).
- If both `lhs` and `rhs` are valueless, returns [`std::strong_ordering::equal`](/docs/cpp/library/utility/compare/strong_ordering).

### Notes

{/* source:cpp-utility-variant-operator-cmp:0011 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_constrained_equality`](/docs/cpp/library/feature_test#cpp_lib_constrained_equality) | [`202403L`](/docs/cpp/library/compiler_support/26#cpp_lib_constrained_equality_202403L) | (C++26) | constrained comparison operators for `std::variant` |

### Example

{/* source:cpp-utility-variant-operator-cmp:0012 */}
```cpp
#include <iostream>
#include <string>
#include <variant>
 
int main()
{
    std::cout << std::boolalpha;
    std::string cmp;
    bool result;
 
    auto print2 = [&cmp, &result](const auto& lhs, const auto& rhs)
    {
        std::cout << lhs << ' ' << cmp << ' ' << rhs << " : " << result << '\n';
    };
 
    std::variant<int, std::string> v1, v2;
 
    std::cout << "operator==\n";
    {
        cmp = "==";
 
        // by default v1 = 0, v2 = 0;
        result = v1 == v2; // true
        std::visit(print2, v1, v2);
 
        v1 = v2 = 1;
        result = v1 == v2; // true
        std::visit(print2, v1, v2);
 
        v2 = 2;
        result = v1 == v2; // false
        std::visit(print2, v1, v2);
 
        v1 = "A";
        result = v1 == v2; // false: v1.index == 1, v2.index == 0
        std::visit(print2, v1, v2);
 
        v2 = "B";
        result = v1 == v2; // false
        std::visit(print2, v1, v2);
 
        v2 = "A";
        result = v1 == v2; // true
        std::visit(print2, v1, v2);
    }
 
    std::cout << "operator<\n";
    {
        cmp = "<";
 
        v1 = v2 = 1;
        result = v1 < v2; // false
        std::visit(print2, v1, v2);
 
        v2 = 2;
        result = v1 < v2; // true
        std::visit(print2, v1, v2);
 
        v1 = 3;
        result = v1 < v2; // false
        std::visit(print2, v1, v2);
 
        v1 = "A"; v2 = 1;
        result = v1 < v2; // false: v1.index == 1, v2.index == 0
        std::visit(print2, v1, v2);
 
        v1 = 1; v2 = "A";
        result = v1 < v2; // true: v1.index == 0, v2.index == 1
        std::visit(print2, v1, v2);
 
        v1 = v2 = "A";
        result = v1 < v2; // false
        std::visit(print2, v1, v2);
 
        v2 = "B";
        result = v1 < v2; // true
        std::visit(print2, v1, v2);
 
        v1 = "C";
        result = v1 < v2; // false
        std::visit(print2, v1, v2);
    }
 
    {
        std::variant<int, std::string> v1;
        std::variant<std::string, int> v2;
    //  v1 == v2; // Compilation error: no known conversion
    }
 
    // TODO: C++20 three-way comparison operator <=> for variants
}
```
Output:

```text
operator==
0 == 0 : true
1 == 1 : true
1 == 2 : false
A == 2 : false
A == B : false
A == A : true
operator<
1 < 1 : false
1 < 2 : true
3 < 2 : false
A < 1 : false
1 < A : true
A < A : false
A < B : true
C < B : false
```

### See also

{/* source:cpp-utility-variant-operator-cmp:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator==` `operator!=` `operator<` `operator<=` `operator>` `operator>=` `operator<=>`](/docs/cpp/library/utility/optional/operator_cmp) <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>compares `optional` objects (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
