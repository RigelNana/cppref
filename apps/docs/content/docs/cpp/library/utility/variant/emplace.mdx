---
title: "std::variant<Types...>::emplace"
description: "Creates a new value in-place, in an existing variant object."
source_url: "https://en.cppreference.com/w/cpp/utility/variant/emplace"
language: "C++"
---

{/* source:cpp-utility-variant-emplace:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`template< class T, class... Args >
T& emplace( Args&&... args );`} />
  (constexpr since C++20)
  <Declaration id="2" language="cpp" since="C++17" code={`template< class T, class U, class... Args >
T& emplace( std::initializer_list<U> il, Args&&... args );`} />
  (constexpr since C++20)
  <Declaration id="3" language="cpp" since="C++17" code={`template< std::size_t I, class... Args >
std::variant_alternative_t<I, variant>& emplace( Args&&... args );`} />
  (constexpr since C++20)
  <Declaration id="4" language="cpp" since="C++17" code={`template< std::size_t I, class U, class... Args >
std::variant_alternative_t<I, variant>&

    emplace( std::initializer_list<U> il, Args&&... args );`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-utility-variant-emplace:0001 */}
Creates a new value in-place, in an existing `variant` object

{/* source:cpp-utility-variant-emplace:0002 */}
1) Equivalent to `emplace<I>(std::forward<Args>(args)...)`, where `I` is the zero-based index of `T` in `Types...`. This overload participates in overload resolution only if `std::is_constructible_v<T, Args...>` is `true`, and `T` occurs exactly once in `Types...`.

{/* source:cpp-utility-variant-emplace:0003 */}
2) Equivalent to `emplace<I>(il, std::forward<Args>(args)...)`, where `I` is the zero-based index of `T` in `Types...`. This overload participates in overload resolution only if `std::is_constructible_v<T, std::initializer_list<U>&, Args...>` is `true`, and `T` occurs exactly once in `Types...`.

{/* source:cpp-utility-variant-emplace:0004 */}
3) First, destroys the currently contained value (if any). Then [direct-initializes](/docs/cpp/language/initialization/direct_initialization) the contained value as if constructing a value of type `T_I` with the arguments `std::forward<Args>(args)...`. If an exception is thrown, `*this` may become [valueless_by_exception](/docs/cpp/library/utility/variant/valueless_by_exception). This overload participates in overload resolution only if `std::is_constructible_v<T_I, Args...>` is `true`. It is a compile-time error if `I` is not less than `sizeof...(Types)`.

{/* source:cpp-utility-variant-emplace:0005 */}
4) First, destroys the currently contained value (if any). Then [direct-initializes](/docs/cpp/language/initialization/direct_initialization) the contained value as if constructing a value of type `T_I` with the arguments `il, std::forward<Args>(args)...`. If an exception is thrown, `*this` may become [valueless_by_exception](/docs/cpp/library/utility/variant/valueless_by_exception). This overload participates in overload resolution only if `std::is_constructible_v<T_I, std::initializer_list<U>&, Args...>` is `true`. It is a compile-time error if `I` is not less than `sizeof...(Types)`.

### Parameters

{/* source:cpp-utility-variant-emplace:0006 */}
<ParameterList>
  <Parameter name="args">constructor arguments to use when constructing the new value</Parameter>
  <Parameter name="il">`initializer_list` argument to use when constructing the new value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-variant-emplace:0007 */}
A reference to the new contained value.

### Exceptions

{/* source:cpp-utility-variant-emplace:0008 */}
1-4) Any exception thrown during the initialization of the contained value.

### Notes

{/* source:cpp-utility-variant-emplace:0009 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_variant`](/docs/cpp/library/feature_test#cpp_lib_variant) | [`202106L`](/docs/cpp/library/compiler_support/20#cpp_lib_variant_202106L) | (C++20) (DR) | Fully `constexpr` `std::variant` ([1-4](/docs/cpp/library/utility/variant/emplace#Version_1)) |

### Example

{/* source:cpp-utility-variant-emplace:0010 */}
```cpp
#include <iostream>
#include <string>
#include <variant>
 
int main()
{
    std::variant<std::string> v1;
    v1.emplace<0>("abc"); // OK
    std::cout << std::get<0>(v1) << '\n';
    v1.emplace<std::string>("def"); // OK
    std::cout << std::get<0>(v1) << '\n';
 
    std::variant<std::string, std::string> v2;
    v2.emplace<1>("ghi"); // OK
    std::cout << std::get<1>(v2) << '\n';
    // v2.emplace<std::string>("abc"); -> Error
}
```
Output:

```text
abc
def
ghi
```

### Defect reports

{/* source:cpp-utility-variant-emplace:0011 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-variant-emplace:0012 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2231R1" standard="C++20">
    <PublishedBehavior>`emplace` was not `constexpr` while the required operations can be `constexpr` in C++20</PublishedBehavior>
    <CorrectedBehavior>made `constexpr`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-variant-emplace:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/utility/variant/operator_assignment)</DescriptionTerm>
    <DescriptionBody>assigns a `variant` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
