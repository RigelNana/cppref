---
title: "std::pair<T1,T2>::swap"
description: "Swaps first with other.first and second with other.second, as if by using std::swap."
source_url: "https://en.cppreference.com/w/cpp/utility/pair/swap"
language: "C++"
---

{/* source:cpp-utility-pair-swap:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`void swap( pair& other ) noexcept(/* see below */);`} />
  (since C++11)
  (until C++20)
  <Declaration id="1" language="cpp" code={`constexpr void swap( pair& other ) noexcept(/* see below */);`} />
  (since C++20)
  <Declaration id="2" language="cpp" code={`constexpr void swap( const pair& other ) const noexcept(/* see below */);`} />
  (since C++23)
</DeclarationDoc>

{/* source:cpp-utility-pair-swap:0001 */}
Swaps `first` with `other.first` and `second` with `other.second`, as if by using `std::swap`; `swap(first, other.first); swap(second, other.second);`.

{/* source:cpp-utility-pair-swap:0002 */}
<Revision until="C++23">
If either selected `swap` function call is ill-formed or does not swap the value of the member, the behavior is undefined.
</Revision>

<Revision since="C++23">
1) The program is ill-formed if either `std::is_swappable_v<T1>` or `std::is_swappable_v<T2>` is not `true`.

2) The program is ill-formed if either `std::is_swappable_v<const T1>` or `std::is_swappable_v<const T2>` is not `true`.

If either selected `swap` function call does not swap the value of the member, the behavior is undefined.
</Revision>

### Parameters

{/* source:cpp-utility-pair-swap:0003 */}
<ParameterList>
  <Parameter name="other">pair of values to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-pair-swap:0004 */}
(none)

### Exceptions

{/* source:cpp-utility-pair-swap:0005 */}
<Revision until="C++17">
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification:

```cpp
noexcept(
     noexcept(swap(first, other.first)) &&
     noexcept(swap(second, other.second))

)
```

In the expression above, the identifier `swap` is looked up in the same manner as the one used by the C++17 [`std::is_nothrow_swappable`](/docs/cpp/library/meta/is_swappable) trait.
</Revision>

<Revision since="C++17">
1) [`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification:

```cpp
noexcept(
     std::is_nothrow_swappable_v<first_type> &&
     std::is_nothrow_swappable_v<second_type>

)
```

2) [`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification:

```cpp
noexcept(
     std::is_nothrow_swappable_v<const first_type> &&
     std::is_nothrow_swappable_v<const second_type>

)
```
</Revision>

### Example

{/* source:cpp-utility-pair-swap:0006 */}
```cpp
#include <iostream>
#include <utility>
#include <string>
int main()
{
    std::pair<int, std::string> p1(10, "test"), p2;
    p2.swap(p1);
    std::cout << "(" << p2.first << ", " << p2.second << ")\n";
 
#if __cpp_lib_ranges_zip >= 202110L
    // Using the C++23 const qualified swap overload
    // (swap is no longer propagating pair constness)
    int i1 = 10, i2{};
    std::string s1("test"), s2;
    const std::pair<int&, std::string&> r1(i1, s1), r2(i2, s2);
    r2.swap(r1);
    std::cout << "(" << i2 << ", " << s2 << ")\n";
#endif
}
```

Possible output:

```text
(10, test)
(10, test)
```

### Defect reports

{/* source:cpp-utility-pair-swap:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-pair-swap:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2456} standard="C++11">
    <PublishedBehavior>the noexcept specification is ill-formed</PublishedBehavior>
    <CorrectedBehavior>made to work</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-pair-swap:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/algorithm/swap)</DescriptionTerm>
    <DescriptionBody>swaps the values of two objects (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/utility/tuple/swap)</DescriptionTerm>
    <DescriptionBody>swaps the contents of two tuples (public member function of `std::tuple<Types...>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
