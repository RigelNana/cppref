---
title: "std::swap(std::pair)"
description: "Swaps the contents of x and y. Equivalent to x.swap(y)."
source_url: "https://en.cppreference.com/w/cpp/utility/pair/swap2"
language: "C++"
---

{/* source:cpp-utility-pair-swap2:0000 */}
<DeclarationDoc>
Defined in header [`<utility>`](/docs/cpp/library/headers/utility)

  <Declaration id="1" language="cpp" code={`template< class T1, class T2 >
void swap( std::pair<T1,T2>& x, std::pair<T1,T2>& y )

    noexcept(/* see below */);`} />
  (since C++11)
  (until C++20)
  <Declaration id="1" language="cpp" code={`template< class T1, class T2 >
constexpr void swap( std::pair<T1,T2>& x, std::pair<T1,T2>& y )

    noexcept(/* see below */);`} />
  (since C++20)
  <Declaration id="2" language="cpp" code={`template< class T1, class T2 >
constexpr void swap( const std::pair<T1,T2>& x, const std::pair<T1,T2>& y )

    noexcept(/* see below */);`} />
  (since C++23)
</DeclarationDoc>

{/* source:cpp-utility-pair-swap2:0001 */}
Swaps the contents of `x` and `y`. Equivalent to `x.swap(y)`.

{/* source:cpp-utility-pair-swap2:0002 */}
<Revision since="C++17">
1) This overload participates in overload resolution only if `std::is_swappable_v<first_type> && std::is_swappable_v<second_type>` is `true`.

2) This overload participates in overload resolution only if `std::is_swappable_v<const first_type> && std::is_swappable_v<const second_type>` is `true`.
</Revision>

### Parameters

{/* source:cpp-utility-pair-swap2:0003 */}
<ParameterList>
  <Parameter name="x, y">pairs whose contents to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-pair-swap2:0004 */}
(none)

### Exceptions

{/* source:cpp-utility-pair-swap2:0005 */}
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification: `noexcept(noexcept(x.swap(y)))`

### Example

{/* source:cpp-utility-pair-swap2:0006 */}
```cpp
#include <iostream>
#include <utility>
 
int main()
{
    auto p1 = std::make_pair(10, 3.14);
    auto p2 = std::pair(12, 1.23); // CTAD, since C++17
 
    auto print_p1_p2 = [&](auto msg) {
        std::cout << msg
                  << "p1 = {" << std::get<0>(p1)
                  << ", "     << std::get<1>(p1) << "}, "
                  << "p2 = {" << std::get<0>(p2)
                  << ", "     << std::get<1>(p2) << "}\n";
    };
 
    print_p1_p2("Before p1.swap(p2): ");
    p1.swap(p2);
    print_p1_p2("After  p1.swap(p2): ");
    std::swap(p1, p2);
    print_p1_p2("After swap(p1, p2): ");
}
```

Output:

```text
Before p1.swap(p2): p1 = {10, 3.14}, p2 = {12, 1.23}
After  p1.swap(p2): p1 = {12, 1.23}, p2 = {10, 3.14}
After swap(p1, p2): p1 = {10, 3.14}, p2 = {12, 1.23}
```

### See also

{/* source:cpp-utility-pair-swap2:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`swap`](/docs/cpp/library/algorithm/swap)</DescriptionTerm>
    <DescriptionBody>swaps the values of two objects (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::swap(std::tuple)`](/docs/cpp/library/utility/tuple/swap2) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specializes the [`std::swap`](/docs/cpp/library/algorithm/swap) algorithm (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
