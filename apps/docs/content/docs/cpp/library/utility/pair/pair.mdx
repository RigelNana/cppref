---
title: "std::pair<T1,T2>::pair"
description: "Constructs a new pair."
source_url: "https://en.cppreference.com/w/cpp/utility/pair/pair"
language: "C++"
---

{/* source:cpp-utility-pair-pair:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`pair();`} />
  (constexpr since C++11)
  (conditionally explicit since C++11)
  <Declaration id="2" language="cpp" code={`pair( const T1& x, const T2& y );`} />
  (conditionally explicit since C++11)
  (constexpr since C++14)
  <Declaration id="3" language="cpp" code={`template< class U1, class U2 >
pair( U1&& x, U2&& y );`} />
  (since C++11)
  (until C++23)
  (constexpr since C++14)
  (conditionally explicit)
  <Declaration id="3" language="cpp" code={`template< class U1 = T1, class U2 = T2 >
constexpr pair( U1&& x, U2&& y );`} />
  (since C++23)
  (conditionally explicit)
  <Declaration id="4" language="cpp" code={`template< class U1, class U2 >
constexpr pair( pair<U1, U2>& p );`} />
  (since C++23)
  (conditionally explicit)
  <Declaration id="5" language="cpp" code={`template< class U1, class U2 >
pair( const pair<U1, U2>& p );`} />
  (conditionally explicit since C++11)
  (constexpr since C++14)
  <Declaration id="6" language="cpp" code={`template< class U1, class U2 >
pair( pair<U1, U2>&& p );`} />
  (constexpr since C++14)
  (conditionally explicit since C++11)
  <Declaration id="7" language="cpp" code={`template< class U1, class U2 >
constexpr pair( const pair<U1, U2>&& p );`} />
  (since C++23)
  (conditionally explicit)
  <Declaration id="8" language="cpp" code={`template< pair-like P >
constexpr pair ( P&& u );`} />
  (since C++23)
  (conditionally explicit)
  <Declaration id="9" language="cpp" code={`template< class... Args1, class... Args2 >
pair( std::piecewise_construct_t,
      std::tuple<Args1...> first_args,

      std::tuple<Args2...> second_args );`} />
  (since C++11)
  (constexpr since C++20)
  <Declaration id="10" language="cpp" code={`pair( const pair& p ) = default;`} />
  <Declaration id="11" language="cpp" code={`pair( pair&& p ) = default;`} />
  (since C++11)

  <DeclarationDescription>
    {/* source:cpp-utility-pair-pair:0001 */}
    Constructs a new pair.

    {/* source:cpp-utility-pair-pair:0002 */}
    1) Default constructor. Value-initializes both elements of the pair, `first` and `second`.

    <Revision since="C++11">
    This constructor participates in overload resolution if and only if `std::is_default_constructible_v<T1>` and `std::is_default_constructible_v<T2>` are both `true`.

    This constructor is `explicit` if and only if either `T1` or `T2` is not implicitly default-constructible.
    </Revision>

    {/* source:cpp-utility-pair-pair:0003 */}
    2) Initializes `first` with `x` and `second` with `y`.

    <Revision since="C++11">
    This constructor participates in overload resolution if and only if `std::is_copy_constructible_v<T1>` and `std::is_copy_constructible_v<T2>` are both `true`.

    This constructor is `explicit` if and only if `std::is_convertible_v<const T1&, T1>` is `false` or `std::is_convertible_v<const T2&, T2>` is `false`.
    </Revision>

    {/* source:cpp-utility-pair-pair:0004 */}
    3) Initializes `first` with `std::forward<U1>(x)` and `second` with `std::forward<U2>(y)`.

    {/* source:cpp-utility-pair-pair:0005 */}
    This constructor participates in overload resolution if and only if `std::is_constructible_v<T1, U1>` and `std::is_constructible_v<T2, U2>` are both `true`.

    {/* source:cpp-utility-pair-pair:0006 */}
    This constructor is `explicit` if and only if `std::is_convertible_v<U1, T1>` is `false` or `std::is_convertible_v<U2, T2>` is `false`.

    <Revision since="C++23">
    This constructor is defined as deleted if the initialization of `first` or `second` would [bind a reference to temporary object](/docs/cpp/language/initialization/reference_initialization#Lifetime_of_a_temporary_object).
    </Revision>

    {/* source:cpp-utility-pair-pair:0007 */}
    4) Initializes `first` with `p.first` and `second` with `p.second`.

    {/* source:cpp-utility-pair-pair:0008 */}
    This constructor participates in overload resolution if and only if `std::is_constructible_v<T1, U1&>` and `std::is_constructible_v<T2, U2&>` are both `true`.

    {/* source:cpp-utility-pair-pair:0009 */}
    This constructor is `explicit` if and only if `std::is_convertible_v<U1&, T1>` is `false` or `std::is_convertible_v<U2&, T2>` is `false`.

    {/* source:cpp-utility-pair-pair:0010 */}
    This constructor is defined as deleted if the initialization of `first` or `second` would bind a reference to temporary object.

    {/* source:cpp-utility-pair-pair:0011 */}
    5) Initializes `first` with `p.first` and `second` with `p.second`.

    <Revision since="C++11">
    This constructor participates in overload resolution if and only if `std::is_constructible_v<T1, const U1&>` and `std::is_constructible_v<T2, const U2&>` are both `true`.

    This constructor is `explicit` if and only if `std::is_convertible_v<const U1&, T1>` is `false` or `std::is_convertible_v<const U2&, T2>` is `false`.
    </Revision>

    <Revision since="C++23">
    This constructor is defined as deleted if the initialization of `first` or `second` would bind a reference to temporary object.
    </Revision>

    {/* source:cpp-utility-pair-pair:0012 */}
    6) Initializes `first` with `std::forward<U1>(p.first)` and `second` with `std::forward<U2>(p.second)`.

    {/* source:cpp-utility-pair-pair:0013 */}
    This constructor participates in overload resolution if and only if `std::is_constructible_v<T1, U1>` and `std::is_constructible_v<T2, U2>` are both `true`.

    {/* source:cpp-utility-pair-pair:0014 */}
    This constructor is `explicit` if and only if `std::is_convertible_v<U1, T1>` is `false` or `std::is_convertible_v<U2, T2>` is `false`.

    <Revision since="C++23">
    This constructor is defined as deleted if the initialization of `first` or `second` would bind a reference to temporary object.
    </Revision>

    {/* source:cpp-utility-pair-pair:0015 */}
    7) Initializes `first` with `std::forward<const U1>(p.first)` and `second` with `std::forward<const U2>(p.second)`.

    {/* source:cpp-utility-pair-pair:0016 */}
    This constructor participates in overload resolution if and only if `std::is_constructible_v<T1, U1>` and `std::is_constructible_v<T2, U2>` are both `true`.

    {/* source:cpp-utility-pair-pair:0017 */}
    This constructor is `explicit` if and only if `std::is_convertible_v<const U1, T1>` is `false` or `std::is_convertible_v<const U2, T2>` is `false`.

    {/* source:cpp-utility-pair-pair:0018 */}
    This constructor is defined as deleted if the initialization of `first` or `second` would bind a reference to temporary object.

    {/* source:cpp-utility-pair-pair:0019 */}
    8) Given `u1` as `std::get<0>(std::forward(u))` and `u2` as `std::get<1>(std::forward(u))`, denote their types as `U1` and `U2` respectively. Initializes `first` with `u1` and `second` with `u2`.

    {/* source:cpp-utility-pair-pair:0020 */}
    This constructor participates in overload resolution if and only if `std::remove_cvref(P)` is not a specialization of [`std::ranges::subrange`](/docs/cpp/library/ranges/subrange), `std::is_constructible_v<T1, U1>` is `true`, and `std::is_constructible_v<T2, U2` is `true`.

    {/* source:cpp-utility-pair-pair:0021 */}
    This constructor is `explicit` if and only if `std::is_convertible_v<U1, T1>` is `false` or `std::is_convertible_v<U2, T2>` is `false`.

    {/* source:cpp-utility-pair-pair:0022 */}
    This constructor is defined as deleted if the initialization of `first` or `second` would bind a reference to temporary object.

    {/* source:cpp-utility-pair-pair:0023 */}
    9) Forwards the elements of `first_args` to the constructor of `first` and forwards the elements of `second_args` to the constructor of `second`. This is the only non-default constructor that can be used to create a pair of non-copyable non-movable types. The program is ill-formed if `first` or `second` is a reference and bound to a temporary object.

    {/* source:cpp-utility-pair-pair:0024 */}
    10) Copy constructor is <InlineRevision until="C++11">implicitly declared</InlineRevision><InlineRevision since="C++11">defaulted, and is `constexpr` if copying of both elements satisfies the requirements on constexpr functions</InlineRevision>.

    {/* source:cpp-utility-pair-pair:0025 */}
    11) Move constructor is defaulted, and is `constexpr` if moving of both elements satisfies the requirements on constexpr functions.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-utility-pair-pair:0026 */}
<ParameterList>
  <Parameter name="x">value to initialize the first element of this pair</Parameter>
  <Parameter name="y">value to initialize the second element of this pair</Parameter>
  <Parameter name="p">pair of values used to initialize both elements of this pair</Parameter>
  <Parameter name="u">[pair-like](/docs/cpp/library/utility/tuple/tuple-like) object of values used to initialize both elements of this pair</Parameter>
  <Parameter name="first_args">tuple of constructor arguments to initialize the first element of this pair</Parameter>
  <Parameter name="second_args">tuple of constructor arguments to initialize the second element of this pair</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-utility-pair-pair:0027 */}
Does not throw exceptions unless one of the specified operations (e.g. constructor of an element) throws.

### Example

{/* source:cpp-utility-pair-pair:0028 */}
```cpp
#include <complex>
#include <iostream>
#include <string>
#include <tuple>
#include <utility>
 
int main()
{
    auto print = [](auto rem, auto const& pair)
    {
        std::cout << rem << "(" << pair.first << ", " << pair.second << ")\n";
    };
 
    std::pair<int, float> p1;
    print("(1) Value-initialized: ", p1);
 
    std::pair<int, double> p2{42, 3.1415};
    print("(2) Initialized with two values: ", p2);
 
    std::pair<char, int> p4{p2};
    print("(4) Implicitly converted: ", p4);
 
    std::pair<std::complex<double>, std::string> p6
        {std::piecewise_construct, std::forward_as_tuple(0.123, 7.7),
            std::forward_as_tuple(10, 'a')};
    print("(8) Piecewise constructed: ", p6);
}
```

Possible output:

```text
(1) Value-initialized: (0, 0)
(2) Initialized with two values: (42, 3.1415)
(4) Implicitly converted: (*, 3)
(8) Piecewise constructed: ((0.123,7.7), aaaaaaaaaa)
```

### Defect reports

{/* source:cpp-utility-pair-pair:0029 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-pair-pair:0030 */}
<DefectReportList>
  <DefectReport kind="lwg" id={265} standard="C++98">
    <PublishedBehavior>the default constructor copy-initialized `first` and `second` with `T1()` and `T2()` respectively (thus required `T1` and `T2` to be [CopyConstructible](/docs/cpp/library/named_req/CopyConstructible))</PublishedBehavior>
    <CorrectedBehavior>`first` and `second` are value-initialized</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2510} standard="C++11">
    <PublishedBehavior>the default constructor was implicit</PublishedBehavior>
    <CorrectedBehavior>made conditionally-explicit</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="N4387" standard="C++11">
    <PublishedBehavior>some constructors were implicit-only, preventing some uses</PublishedBehavior>
    <CorrectedBehavior>constructors made conditionally-explicit</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-pair-pair:0031 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`make_pair`](/docs/cpp/library/utility/pair/make_pair)</DescriptionTerm>
    <DescriptionBody>creates a pair object of type, determined by the argument types (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/utility/tuple/tuple)</DescriptionTerm>
    <DescriptionBody>constructs a new tuple (public member function of `std::tuple<Types...>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
