---
title: "std::any::swap"
description: "Swaps the content of two any objects."
source_url: "https://en.cppreference.com/w/cpp/utility/any/swap"
language: "C++"
---

{/* source:cpp-utility-any-swap:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`void swap( any& other ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-utility-any-swap:0001 */}
Swaps the content of two `any` objects.

### Parameters

{/* source:cpp-utility-any-swap:0002 */}
<ParameterList>
  <Parameter name="other">object to swap with</Parameter>
</ParameterList>

### Example

{/* source:cpp-utility-any-swap:0003 */}
```cpp
#include <any>
#include <print>
#include <string>
#include <string_view>
 
int main()
{
    std::any a = std::string{"King"};
    std::any b = std::string_view{"Queen"};
    std::println("a = {}", std::any_cast<std::string&>(a));
    std::println("b = {}", std::any_cast<std::string_view&>(b));
    std::println("swap(a, b)");
    a.swap(b);
    std::println("a = {}", std::any_cast<std::string_view&>(a));
    std::println("b = {}", std::any_cast<std::string&>(b));
}
```

Output:

```text
a = King
b = Queen
swap(a, b)
a = Queen
b = King
```
