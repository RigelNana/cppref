---
title: "std::any::has_value"
description: "Checks whether the object contains a value."
source_url: "https://en.cppreference.com/w/cpp/utility/any/has_value"
language: "C++"
---

{/* source:cpp-utility-any-has-value:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`bool has_value() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-utility-any-has-value:0001 */}
Checks whether the object contains a value.

### Parameters

{/* source:cpp-utility-any-has-value:0002 */}
(none)

### Return value

{/* source:cpp-utility-any-has-value:0003 */}
`true` if and only if the instance contains a value.

### Example

{/* source:cpp-utility-any-has-value:0004 */}
```cpp
#include <any>
#include <cassert>
#include <string>
 
int main()
{
    std::any a0;
    assert(a0.has_value() == false);
 
    std::any a1 = 42;
    assert(a1.has_value() == true);
    assert(std::any_cast<int>(a1) == 42);
    a1.reset();
    assert(a1.has_value() == false);
 
    auto a2 = std::make_any<std::string>("Andromeda");
    assert(a2.has_value() == true);
    assert(std::any_cast<std::string&>(a2) == "Andromeda");
    a2.reset();
    assert(a2.has_value() == false);
}
```

### See also

{/* source:cpp-utility-any-has-value:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`reset`](/docs/cpp/library/utility/any/reset)</DescriptionTerm>
    <DescriptionBody>destroys contained object (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
