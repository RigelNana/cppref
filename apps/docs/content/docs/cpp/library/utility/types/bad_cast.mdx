---
title: "std::bad_cast"
description: "An exception of this type is thrown when a dynamic_cast to a reference type fails the run-time check (e.g. because the types are not related by inheritance), and also from std::use_facet if the requested facet does not exist in the locale."
source_url: "https://en.cppreference.com/w/cpp/types/bad_cast"
language: "C++"
---

{/* source:cpp-types-bad-cast:0000 */}
<DeclarationDoc>
Defined in header [`<typeinfo>`](/docs/cpp/library/headers/typeinfo)
<Declaration language="cpp" code={`class bad_cast : public std::exception;`} />
</DeclarationDoc>

{/* source:cpp-types-bad-cast:0001 */}
An exception of this type is thrown when a [`dynamic_cast`](/docs/cpp/language/expressions/dynamic_cast) to a reference type fails the run-time check (e.g. because the types are not related by inheritance), and also from [`std::use_facet`](/docs/cpp/library/text/locale/use_facet) if the requested facet does not exist in the locale.

{/* source:cpp-types-bad-cast:0002 */}
Inheritance diagram

### Member functions

{/* source:cpp-types-bad-cast:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>(constructor)</DescriptionTerm>
    <DescriptionBody>constructs a new `bad_cast` object <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>operator=</DescriptionTerm>
    <DescriptionBody>replaces the `bad_cast` object <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>what</DescriptionTerm>
    <DescriptionBody>returns the explanatory string <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-types-bad-cast:0004 */}
#### `std::bad_cast::bad_cast`

<DeclarationDoc>

  <Declaration language="cpp" id="1" until="C++11" code={`bad_cast() throw();`} />
  <Declaration language="cpp" id="1" since="C++11" code={`bad_cast() noexcept;`} />
  <Declaration language="cpp" id="2" until="C++11" code={`bad_cast( const bad_cast& other ) throw();`} />
  <Declaration language="cpp" id="2" since="C++11" code={`bad_cast( const bad_cast& other ) noexcept;`} />

  <DeclarationDescription>
    Constructs a new `bad_cast` object with an implementation-defined null-terminated byte string which is accessible through [`what()`](/docs/cpp/library/error/exception/what).

    1. Default constructor.

    2. Copy constructor. <InlineRevision since="C++11">If `*this` and `other` both have dynamic type `std::bad_cast` then `std::strcmp(what(), other.what()) == 0`.</InlineRevision>
  </DeclarationDescription>

</DeclarationDoc>

##### Parameters

<ParameterList>
  <Parameter name="other">another exception object to copy</Parameter>
</ParameterList>

{/* source:cpp-types-bad-cast:0005 */}
#### `std::bad_cast::operator=`

<DeclarationDoc>

  <Declaration language="cpp" until="C++11" code={`bad_cast& operator=( const bad_cast& other ) throw();`} />
  <Declaration language="cpp" since="C++11" code={`bad_cast& operator=( const bad_cast& other ) noexcept;`} />

  <DeclarationDescription>
    Assigns the contents with those of `other`.<InlineRevision since="C++11"> If `*this` and `other` both have dynamic type `std::bad_cast` then `std::strcmp(what(), other.what()) == 0` after assignment.</InlineRevision>
  </DeclarationDescription>

</DeclarationDoc>

##### Parameters

<ParameterList>
  <Parameter name="other">another exception object to assign with</Parameter>
</ParameterList>

##### Return value

`*this`

{/* source:cpp-types-bad-cast:0006 */}
#### `std::bad_cast::what`

<DeclarationDoc>

  <Declaration language="cpp" until="C++11" code={`virtual const char* what() const throw();`} />
  <Declaration language="cpp" since="C++11" code={`virtual const char* what() const noexcept;`} />
  <InlineRevision since="C++26">constexpr</InlineRevision>

  <DeclarationDescription>
    Returns the explanatory string.
  </DeclarationDescription>

</DeclarationDoc>

##### Return value

Pointer to an implementation-defined null-terminated string with explanatory information. The string is suitable for conversion and display as a [`std::wstring`](/docs/cpp/library/string/basic_string). The pointer is guaranteed to be valid at least until the exception object from which it is obtained is destroyed, or until a non-const member function (e.g. copy assignment operator) on the exception object is called.

<Revision since="C++26">
The returned string is encoded with the ordinary literal encoding during constant evaluation.
</Revision>

##### Notes

Implementations are allowed but not required to override `what()`.

{/* source:cpp-types-bad-cast:0007 */}
#### Inherited from [`std::exception`](/docs/cpp/library/error/exception)

##### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(destructor)`](/docs/cpp/library/error/exception/~exception) [virtual]</DescriptionTerm>
    <DescriptionBody>destroys the exception object <br /> (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[what](/docs/cpp/library/error/exception/what) [virtual]</DescriptionTerm>
    <DescriptionBody>returns an explanatory string <br /> (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Notes

{/* source:cpp-types-bad-cast:0008 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_constexpr_exceptions`](/docs/cpp/library/feature_test#cpp_lib_constexpr_exceptions) | [`202411L`](/docs/cpp/library/compiler_support/26#cpp_lib_constexpr_exceptions_202411L) | (C++26) | `constexpr` for exception types |

### Example

{/* source:cpp-types-bad-cast:0009 */}
```cpp
#include <iostream>
#include <typeinfo>
 
struct Foo { virtual ~Foo() {} };
struct Bar { virtual ~Bar() { std::cout << "~Bar\n"; } };
struct Pub : Bar { ~Pub() override { std::cout << "~Pub\n"; } };
 
int main()
{
    Pub pub;
    try
    {
        [[maybe_unused]]
        Bar& r1 = dynamic_cast<Bar&>(pub); // OK, upcast
 
        [[maybe_unused]]
        Foo& r2 = dynamic_cast<Foo&>(pub); // throws
    }
    catch (const std::bad_cast& e)
    {
        std::cout << "e.what(): " << e.what() << '\n';
    }
}
```

Possible output:



```text
e.what(): std::bad_cast
~Pub
~Bar
```
