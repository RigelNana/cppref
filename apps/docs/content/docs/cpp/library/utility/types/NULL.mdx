---
title: "NULL"
description: "The macro NULL is an implementation-defined null pointer constant."
source_url: "https://en.cppreference.com/w/cpp/types/NULL"
language: "C++"
---

{/* source:cpp-types-null:0000 */}
<DeclarationDoc>
Defined in header [`<clocale>`](/docs/cpp/library/headers/clocale)
Defined in header [`<cstddef>`](/docs/cpp/library/headers/cstddef)
Defined in header [`<cstdio>`](/docs/cpp/library/headers/cstdio)
Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
Defined in header [`<ctime>`](/docs/cpp/library/headers/ctime)
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)
<Declaration language="cpp" code={`#define NULL /* implementation-defined */`} />
</DeclarationDoc>

{/* source:cpp-types-null:0001 */}
The macro `NULL` is an implementation-defined [null pointer constant](/docs/cpp/language/declarations/pointer#Null_pointers).

### Possible implementation

{/* source:cpp-types-null:0002 */}
```cpp
#define NULL 0
// since C++11
#define NULL nullptr
```

### Notes

{/* source:cpp-types-null:0003 */}
In C, the macro `NULL` may have the type `void*`, but that is not allowed in C++ because null pointer constants cannot have that type.

### Example

{/* source:cpp-types-null:0004 */}
```cpp
#include <cstddef>
#include <iostream>
#include <type_traits>
#include <typeinfo>
 
class S;
 
int main()
{
    int* p = NULL;
    int* p2 = static_cast<std::nullptr_t>(NULL);
    void(*f)(int) = NULL;
    int S::*mp = NULL;
    void(S::*mfp)(int) = NULL;
    auto nullvar = NULL; // may trigger a warning when compiling with gcc/clang
 
    std::cout << "The type of nullvar is " << typeid(nullvar).name() << '\n';
 
    if constexpr(std::is_same_v<decltype(NULL), std::nullptr_t>)
        std::cout << "NULL implemented with type std::nullptr_t\n";
    else
        std::cout << "NULL implemented using an integral type\n";
 
    [](...){}(p, p2, f, mp, mfp); // < suppresses "unused variable" warnings
}
```

Possible output:



```text
The type of nullvar is long
NULL implemented using an integral type
```

### See also

{/* source:cpp-types-null:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`nullptr`](/docs/cpp/language/expressions/nullptr) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>the pointer literal which specifies a null pointer value</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[nullptr_t](/docs/cpp/library/utility/types/nullptr_t) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>the type of the null pointer literal [`nullptr`](/docs/cpp/language/expressions/nullptr) <br /> (typedef)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

**[C documentation](/docs/c/library/types/NULL) for `NULL`**
