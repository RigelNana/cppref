---
title: "std::type_info::before"
description: "Returns true if the type of this type_info precedes the type of rhs in the implementation's collation order. No guarantees are given; in particular, the collation order can change between the invocations of the same program."
source_url: "https://en.cppreference.com/w/cpp/types/type_info/before"
language: "C++"
---

{/* source:cpp-types-type-info-before:0000 */}
<DeclarationDoc>
<Declaration language="cpp" code={`bool before( const type_info& rhs ) const;`} />
(noexcept since C++11)
</DeclarationDoc>

{/* source:cpp-types-type-info-before:0001 */}
Returns `true` if the type of this `type_info` precedes the type of `rhs` in the implementation's collation order. No guarantees are given; in particular, the collation order can change between the invocations of the same program.

### Parameters

{/* source:cpp-types-type-info-before:0002 */}
<ParameterList>
  <Parameter name="rhs">another type information object to compare to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-types-type-info-before:0003 */}
`true` if the type of this `type_info` precedes the type of `rhs` in the implementation's collation order.

### Example

{/* source:cpp-types-type-info-before:0004 */}
```cpp
#include <iostream>
#include <typeinfo>
 
int main()
{
    if (typeid(int).before(typeid(char)))
        std::cout << "int goes before char in this implementation.\n";
    else
        std::cout << "char goes before int in this implementation.\n";
}
```

Possible output:



```text
char goes before int in this implementation.
```

### See also

{/* source:cpp-types-type-info-before:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator== operator!=`](/docs/cpp/library/utility/types/type_info/operator_cmp) (removed in C++20)</DescriptionTerm>
    <DescriptionBody>checks whether the objects refer to the same type <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
