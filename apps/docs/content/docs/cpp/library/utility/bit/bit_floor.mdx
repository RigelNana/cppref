---
title: "std::bit_floor"
description: "If x is not zero, calculates the largest integral power of two that is not greater than x. If x is zero, returns zero."
source_url: "https://en.cppreference.com/w/cpp/numeric/bit_floor"
language: "C++"
---

{/* source:cpp-numeric-bit-floor:0000 */}
<DeclarationDoc>
Defined in header [`<bit>`](/docs/cpp/library/headers/bit)

  <Declaration language="cpp" since="C++20" code={`template< class T >
constexpr T bit_floor( T x ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-numeric-bit-floor:0001 */}
If `x` is not zero, calculates the largest integral power of two that is not greater than `x`. If `x` is zero, returns zero.

{/* source:cpp-numeric-bit-floor:0002 */}
This overload participates in overload resolution only if `T` is an unsigned integer type (that is, `unsigned char`, `unsigned short`, `unsigned int`, `unsigned long`, `unsigned long long`, or an extended unsigned integer type).

### Parameters

{/* source:cpp-numeric-bit-floor:0003 */}
<ParameterList>
  <Parameter name="x">unsigned integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-bit-floor:0004 */}
Zero if `x` is zero; otherwise, the largest integral power of two that is not greater than `x`.

### Notes

{/* source:cpp-numeric-bit-floor:0005 */}
Prior to [P1956R1](https://wg21.link/P1956R1), the proposed name for this function template was `floor2`.

{/* source:cpp-numeric-bit-floor:0006 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_int_pow2`](/docs/cpp/library/feature_test#cpp_lib_int_pow2) | [`202002L`](/docs/cpp/library/compiler_support/20#cpp_lib_int_pow2_202002L) | (C++20) | [Integral power-of-2 operations](/docs/cpp/library/numeric#Bit_manipulation_.28since_C.2B.2B20.29) |

### Possible implementation

{/* source:cpp-numeric-bit-floor:0007 */}
```cpp
template<std::unsigned_integral T>
    requires !std::same_as<T, bool> && !std::same_as<T, char> &&
             !std::same_as<T, char8_t> && !std::same_as<T, char16_t> &&
             !std::same_as<T, char32_t> && !std::same_as<T, wchar_t>
constexpr T bit_floor(T x) noexcept
{
    if (x != 0)
        return T{1} << (std::bit_width(x) - 1);
    return 0;
}
```

### Example

{/* source:cpp-numeric-bit-floor:0008 */}
```cpp
#include <bit>
#include <bitset>
#include <iostream>
 
int main()
{
    using bin = std::bitset<8>;
    for (unsigned x{}; x != 012; ++x)
        std::cout << "bit_floor( " << bin(x) << " ) = "
                  << bin(std::bit_floor(x)) << '\n';
}
```

Output:

```text
bit_floor( 00000000 ) = 00000000
bit_floor( 00000001 ) = 00000001
bit_floor( 00000010 ) = 00000010
bit_floor( 00000011 ) = 00000010
bit_floor( 00000100 ) = 00000100
bit_floor( 00000101 ) = 00000100
bit_floor( 00000110 ) = 00000100
bit_floor( 00000111 ) = 00000100
bit_floor( 00001000 ) = 00001000
bit_floor( 00001001 ) = 00001000
```

### See also

{/* source:cpp-numeric-bit-floor:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`bit_ceil`](/docs/cpp/library/utility/bit/bit_ceil) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>finds the smallest integral power of 2 not less than the given value (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rotr`](/docs/cpp/library/utility/bit/rotr) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the result of bitwise right-rotation (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`bit_width`](/docs/cpp/library/utility/bit/bit_width) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>finds the smallest number of bits needed to represent the given value (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`has_single_bit`](/docs/cpp/library/utility/bit/has_single_bit) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>checks if a number is an integral power of 2 (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
