---
title: "std::bit_width"
description: "If x is not zero, calculates the number of bits needed to store the value x, that is, 1 + floor(log2(x)). If x is zero, returns zero."
source_url: "https://en.cppreference.com/w/cpp/numeric/bit_width"
language: "C++"
---

{/* source:cpp-numeric-bit-width:0000 */}
<DeclarationDoc>
Defined in header [`<bit>`](/docs/cpp/library/headers/bit)

  <Declaration language="cpp" since="C++20" code={`template< class T >
constexpr int bit_width( T x ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-numeric-bit-width:0001 */}
If `x` is not zero, calculates the number of bits needed to store the value `x`, that is, `1 + floor(log2(x))`. If `x` is zero, returns zero.

{/* source:cpp-numeric-bit-width:0002 */}
This overload participates in overload resolution only if `T` is an unsigned integer type (that is, `unsigned char`, `unsigned short`, `unsigned int`, `unsigned long`, `unsigned long long`, or an extended unsigned integer type).

### Parameters

{/* source:cpp-numeric-bit-width:0003 */}
<ParameterList>
  <Parameter name="x">unsigned integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-bit-width:0004 */}
Zero if `x` is zero; otherwise, one plus the base-2 logarithm of `x`, with any fractional part discarded.

### Notes

{/* source:cpp-numeric-bit-width:0005 */}
This function is equivalent to `return std::numeric_limits<T>::digits - std::countl_zero(x);`.

{/* source:cpp-numeric-bit-width:0006 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_int_pow2`](/docs/cpp/library/feature_test#cpp_lib_int_pow2) | [`202002L`](/docs/cpp/library/compiler_support/20#cpp_lib_int_pow2_202002L) | (C++20) | [Integral power-of-2 operations](/docs/cpp/library/numeric#Bit_manipulation_.28since_C.2B.2B20.29) |

### Example

{/* source:cpp-numeric-bit-width:0007 */}
```cpp
#include <bit>
#include <bitset>
#include <iostream>
 
int main()
{
    for (unsigned x{}; x != 010; ++x)
        std::cout << "bit_width( "
                  << std::bitset<4>{x} << " ) = "
                  << std::bit_width(x) << '\n';
}
```

Output:

```text
bit_width( 0000 ) = 0
bit_width( 0001 ) = 1
bit_width( 0010 ) = 2
bit_width( 0011 ) = 2
bit_width( 0100 ) = 3
bit_width( 0101 ) = 3
bit_width( 0110 ) = 3
bit_width( 0111 ) = 3
```

### Defect reports

{/* source:cpp-numeric-bit-width:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-numeric-bit-width:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3656} standard="C++20">
    <PublishedBehavior>the return type of `bit_width` is the same as the type of its function argument</PublishedBehavior>
    <CorrectedBehavior>made it `int`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-numeric-bit-width:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`countl_zero`](/docs/cpp/library/utility/bit/countl_zero) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>counts the number of consecutive `0` bits, starting from the most significant bit (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
