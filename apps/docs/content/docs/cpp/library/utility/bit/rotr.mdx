---
title: "std::rotr"
description: "Computes the result of bitwise right-rotating the value of x by s positions. This operation is also known as a right circular shift."
source_url: "https://en.cppreference.com/w/cpp/numeric/rotr"
language: "C++"
---

{/* source:cpp-numeric-rotr:0000 */}
<DeclarationDoc>
Defined in header [`<bit>`](/docs/cpp/library/headers/bit)

  <Declaration language="cpp" since="C++20" code={`template< class T >
constexpr T rotr( T x, int s ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-numeric-rotr:0001 */}
Computes the result of bitwise right-rotating the value of `x` by `s` positions. This operation is also known as a right [circular shift](https://en.wikipedia.org/wiki/Circular_shift).

{/* source:cpp-numeric-rotr:0002 */}
Formally, let `N` be `std::numeric_limits<T>::digits` and `r` be `s % N`.

{/* source:cpp-numeric-rotr:0003 */}
- If `r` is `0`, returns `x`;
- if `r` is positive, returns `(x >> r) | (x << (N - r))`;
- if `r` is negative, returns `std::rotl(x, -r)`.

{/* source:cpp-numeric-rotr:0004 */}
This overload participates in overload resolution only if `T` is an unsigned integer type (that is, `unsigned char`, `unsigned short`, `unsigned int`, `unsigned long`, `unsigned long long`, or an extended unsigned integer type).

### Parameters

{/* source:cpp-numeric-rotr:0005 */}
<ParameterList>
  <Parameter name="x">value of unsigned integer type</Parameter>
  <Parameter name="s">number of positions to shift</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-rotr:0006 */}
The result of bitwise right-rotating `x` by `s` positions.

### Notes

{/* source:cpp-numeric-rotr:0007 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_bitops`](/docs/cpp/library/feature_test#cpp_lib_bitops) | [`201907L`](/docs/cpp/library/compiler_support/20#cpp_lib_bitops_201907L) | (C++20) | [Bit operations](/docs/cpp/library/numeric#Bit_manipulation_.28since_C.2B.2B20.29) |

### Example

{/* source:cpp-numeric-rotr:0008 */}
```cpp
#include <bit>
#include <bitset>
#include <cstdint>
#include <iostream>
 
int main()
{
    using bin = std::bitset<8>;
    const std::uint8_t x{0b00011101};
    std::cout << bin(x) << " <- x\n";
    for (const int s : {0, 1, 9, -1, 2})
        std::cout << bin(std::rotr(x, s)) << " <- rotr(x, " << s << ")\n";
}
```

Output:

```text
00011101 <- x
00011101 <- rotr(x, 0)
10001110 <- rotr(x, 1)
10001110 <- rotr(x, 9)
00111010 <- rotr(x, -1)
01000111 <- rotr(x, 2)
```

### See also

{/* source:cpp-numeric-rotr:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rotl`](/docs/cpp/library/utility/bit/rotl) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the result of bitwise left-rotation (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<=` `operator>>=` `operator<<` `operator>>`](/docs/cpp/library/utility/bitset/operator_ltltgtgt)</DescriptionTerm>
    <DescriptionBody>performs binary shift left and shift right (public member function of `std::bitset<N>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
