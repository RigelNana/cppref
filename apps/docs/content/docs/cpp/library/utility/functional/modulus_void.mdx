---
title: "std::modulus<void>"
description: "std::modulus<void> is a specialization of std::modulus with parameter and return type deduced."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/modulus_void"
language: "C++"
---

{/* source:cpp-utility-functional-modulus-void:0000 */}
<DeclarationDoc>
Defined in header [`<functional>`](/docs/cpp/library/headers/functional)

  <Declaration language="cpp" since="C++14" code={`template<>
class modulus<void>;`} />
</DeclarationDoc>

{/* source:cpp-utility-functional-modulus-void:0001 */}
`std::modulus<void>` is a specialization of [`std::modulus`](/docs/cpp/library/utility/functional/modulus) with parameter and return type deduced.

### Nested types

{/* source:cpp-utility-functional-modulus-void:0002 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`is_transparent`</DescriptionTerm>
    <DescriptionBody>[`unspecified`](/docs/cpp/library/utility/functional#Transparent_function_objects)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-utility-functional-modulus-void:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator()`</DescriptionTerm>
    <DescriptionBody>returns the modulus of two arguments (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-utility-functional-modulus-void:0004 */}
## std::modulus\<void\>::operator()

<DeclarationDoc>
  <Declaration language="cpp" code={`template< class T, class U >
constexpr auto operator()( T&& lhs, U&& rhs ) const

    -> decltype(std::forward<T>(lhs) % std::forward<U>(rhs));`} />
</DeclarationDoc>

Returns the remainder of the division of `lhs` by `rhs`.

### Parameters

<ParameterList>
  <Parameter name="lhs, rhs">values to divide</Parameter>
</ParameterList>

### Return value

`std::forward<T>(lhs) % std::forward<U>(rhs)`.

### Example

{/* source:cpp-utility-functional-modulus-void:0005 */}
```cpp
#include <functional>
#include <iostream>
 
struct M
{
    M(int x) { std::cout << "M(" << x << ");\n"; }
    M() {}
};
 
auto operator%(M, M) { std::cout << "operator%(M, M);\n"; return M{}; }
auto operator%(M, int) { std::cout << "operator%(M, int);\n"; return M{}; }
auto operator%(int, M) { std::cout << "operator%(int, M);\n"; return M{}; }
 
int main()
{
    M m;
 
    // 42 is converted into a temporary object M{42}
    std::modulus<M>{}(m, 42);    // calls operator%(M, M)
 
    // no temporary object
    std::modulus<void>{}(m, 42); // calls operator%(M, int)
    std::modulus<void>{}(42, m); // calls operator%(int, M)
}
```

Output:

```text
M(42);
operator%(M, M);
operator%(M, int);
operator%(int, M);
```
