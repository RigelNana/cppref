---
title: "std::reference_wrapper<T>::get, std::reference_wrapper<T>::operator T&"
description: "Returns the stored reference."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/reference_wrapper/get"
language: "C++"
---

{/* source:cpp-utility-functional-reference-wrapper-get:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`operator T& () const noexcept;`} />
  (constexpr since C++20)
  <Declaration id="2" language="cpp" since="C++11" code={`T& get() const noexcept;`} />
  (constexpr since C++20)
</DeclarationDoc>

{/* source:cpp-utility-functional-reference-wrapper-get:0001 */}
Returns the stored reference.

### Parameters

{/* source:cpp-utility-functional-reference-wrapper-get:0002 */}
(none)

### Return value

{/* source:cpp-utility-functional-reference-wrapper-get:0003 */}
The stored reference.

### Example

{/* source:cpp-utility-functional-reference-wrapper-get:0004 */}
```cpp
#include <cassert>
#include <functional>
#include <map>
#include <optional>
#include <string_view>
 
using Map = std::map<std::string_view, int>;
using Opt = std::optional<std::reference_wrapper<Map::value_type>>;
 
Opt find(Map& m, std::string_view s)
{
    auto it = m.find(s);
    return it == m.end() ? Opt{} : Opt{*it};
}
 
int main()
{
    Map m{{"A", 1}, {"B", 2}, {"C", 3}};
 
    if (auto opt = find(m, "C"); opt)
        opt->get().second = 42;
        // std::optional::operator->() returns reference to std::reference_wrapper, then
        // reference_wrapper::get() returns reference to map::value_type, i.e. std::pair
 
    assert(m["C"] == 42);
}
```

### See also

{/* source:cpp-utility-functional-reference-wrapper-get:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator()`](/docs/cpp/library/utility/functional/reference_wrapper/operator())</DescriptionTerm>
    <DescriptionBody>calls the stored function (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
