---
title: "std::bind1st, std::bind2nd"
description: "Binds a given argument x to a first or second parameter of the given binary function object f."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/bind12"
language: "C++"
---

{/* source:cpp-utility-functional-bind12:0000 */}
<DeclarationDoc>
Defined in header [`<functional>`](/docs/cpp/library/headers/functional)

  <Declaration id="1" language="cpp" removed="C++17" code={`template< class F, class T >
std::binder1st<F> bind1st( const F& f, const T& x );`} />
  (deprecated in C++11)
  <Declaration id="2" language="cpp" removed="C++17" code={`template< class F, class T >
std::binder2nd<F> bind2nd( const F& f, const T& x );`} />
  (deprecated in C++11)
</DeclarationDoc>

{/* source:cpp-utility-functional-bind12:0001 */}
Binds a given argument `x` to a first or second parameter of the given binary function object `f`. That is, stores `x` within the resulting wrapper, which, if called, passes `x` as the first or the second parameter of `f`.

{/* source:cpp-utility-functional-bind12:0002 */}
1) Binds the first argument of `f` to `x`. Effectively calls `std::binder1st<F>(f, typename F::first_argument_type(x))`.

{/* source:cpp-utility-functional-bind12:0003 */}
2) Binds the second argument of `f` to `x`. Effectively calls `std::binder2nd<F>(f, typename F::second_argument_type(x))`.

### Parameters

{/* source:cpp-utility-functional-bind12:0004 */}
<ParameterList>
  <Parameter name="f">pointer to a function to bind an argument to</Parameter>
  <Parameter name="x">argument to bind to `f`</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-functional-bind12:0005 */}
A function object wrapping `f` and `x`.

### Exceptions

{/* source:cpp-utility-functional-bind12:0006 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-utility-functional-bind12:0007 */}
```cpp
#include <algorithm>
#include <cmath>
#include <cstddef>
#include <functional>
#include <iomanip>
#include <iostream>
#include <vector>
 
int main()
{
    std::vector<double> a = {0, 30, 45, 60, 90, 180};
    std::vector<double> r(a.size());
    const double pi = std::acos(-1); // since C++20 use std::numbers::pi
 
    std::transform(a.begin(), a.end(), r.begin(),
        std::bind1st(std::multiplies<double>(), pi / 180.0));
//  an equivalent lambda is: [pi](double a) { return a * pi / 180.0; });
 
    for (std::size_t n = 0; n < a.size(); ++n)
        std::cout << std::setw(3) << a[n] << "° = " << std::fixed << r[n]
                  << " rad\n" << std::defaultfloat;
}
```

Output:

```text
  0° = 0.000000 rad
 30° = 0.523599 rad
 45° = 0.785398 rad
 60° = 1.047198 rad
 90° = 1.570796 rad
180° = 3.141593 rad
```

### See also

{/* source:cpp-utility-functional-bind12:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`binder1st`, `binder2nd`](/docs/cpp/library/utility/functional/binder12) (deprecated in C++11) <InlineRevision removed="C++17" /></DescriptionTerm>
    <DescriptionBody>function object holding a binary function and one of its arguments (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`bind_front`, `bind_back`](/docs/cpp/library/utility/functional/bind_front) <InlineRevision since="C++20" /> <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>bind a variable number of arguments, in order, to a function object (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
