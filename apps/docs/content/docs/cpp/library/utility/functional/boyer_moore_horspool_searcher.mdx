---
title: "std::boyer_moore_horspool_searcher"
description: "A searcher suitable for use with the Searcher overload of std::search that implements the Boyer-Moore-Horspool string searching algorithm."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/boyer_moore_horspool_searcher"
language: "C++"
---

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0000 */}
<DeclarationDoc>
Defined in header [`<functional>`](/docs/cpp/library/headers/functional)

  <Declaration language="cpp" since="C++17" code={`template< class RandomIt1,
          class Hash = std::hash<typename std::iterator_traits<RandomIt1>::value_type>,
          class BinaryPredicate = std::equal_to<> >

class boyer_moore_horspool_searcher;`} />
</DeclarationDoc>

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0001 */}
A searcher suitable for use with the [Searcher](https://en.cppreference.com/mwiki/index.php?title=cpp/named_req/Searcher&action=edit&redlink=1) overload of [`std::search`](/docs/cpp/library/algorithm/search) that implements the [Boyer-Moore-Horspool string searching algorithm](https://en.wikipedia.org/wiki/Boyer%E2%80%93Moore%E2%80%93Horspool_algorithm).

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0002 */}
`std::boyer_moore_horspool_searcher` is [CopyConstructible](/docs/cpp/library/named_req/CopyConstructible) and [CopyAssignable](/docs/cpp/library/named_req/CopyAssignable).

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0003 */}
`RandomIt1` must meet the requirements of [LegacyRandomAccessIterator](/docs/cpp/library/named_req/RandomAccessIterator).

### Member functions

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0004 */}
## std::boyer_moore_horspool_searcher::boyer_moore_horspool_searcher

<DeclarationDoc>
  <Declaration language="cpp" code={`boyer_moore_horspool_searcher( RandomIt1 pat_first,
                               RandomIt1 pat_last,
                               Hash hf = Hash(),

                               BinaryPredicate pred = BinaryPredicate() );`} />
</DeclarationDoc>

Constructs a `std::boyer_moore_horspool_searcher` by storing copies of `pat_first`, `pat_last`, `hf`, and `pred`, setting up any necessary internal data structures.

The value type of `RandomIt1` must be [DefaultConstructible](/docs/cpp/library/named_req/DefaultConstructible), [CopyConstructible](/docs/cpp/library/named_req/CopyConstructible) and [CopyAssignable](/docs/cpp/library/named_req/CopyAssignable).

For any two values `A` and `B` of the type `std::iterator_traits<RandomIt1>::value_type`, if `pred(A, B) == true`, then `hf(A) == hf(B)` shall be true.

### Parameters

<ParameterList>
  <Parameter name="pat_first, pat_last">a pair of iterators designating the string to be searched for</Parameter>
  <Parameter name="hf">a callable object used to hash the elements of the string</Parameter>
  <Parameter name="pred">a callable object used to determine equality</Parameter>
</ParameterList>

### Exceptions

Any exceptions thrown by

- the copy constructor of `RandomIt1`;
- the default constructor, copy constructor, or copy assignment operator of the value type of `RandomIt1`; or
- the copy constructor or function call operator of `BinaryPredicate` or `Hash`.

May also throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) if additional memory required for internal data structures cannot be allocated.

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0005 */}
## std::boyer_moore_horspool_searcher::operator()

<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class RandomIt2 >
std::pair<RandomIt2, RandomIt2> operator()( RandomIt2 first, RandomIt2 last ) const;`} />
</DeclarationDoc>

The member function called by the Searcher overload of [`std::search`](/docs/cpp/library/algorithm/search) to perform a search with this searcher. `RandomIt2` must meet the requirements of [LegacyRandomAccessIterator](/docs/cpp/library/named_req/RandomAccessIterator).

`RandomIt1` and `RandomIt2` must have the same value type.

### Parameters

<ParameterList>
  <Parameter name="first, last">a pair of iterators designating the string to be examined</Parameter>
</ParameterList>

### Return value

If the pattern [`pat_first`, `pat_last`) is empty, returns `std::make_pair(first, first)`.

Otherwise, returns a pair of iterators to the first and one past last positions in [`first`, `last`) where a subsequence that compares equal to [`pat_first`, `pat_last`) as defined by `pred` is located, or `std::make_pair(last, last)` otherwise.

### Notes

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0006 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_boyer_moore_searcher`](/docs/cpp/library/feature_test#cpp_lib_boyer_moore_searcher) | [`201603L`](/docs/cpp/library/compiler_support/17#cpp_lib_boyer_moore_searcher_201603L) | (C++17) | [searchers](/docs/cpp/library/utility/functional#Searchers) |

### Example

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0007 */}
```cpp
#include <algorithm>
#include <functional>
#include <iomanip>
#include <iostream>
#include <string_view>
 
int main()
{
    constexpr std::string_view in =
        "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed "
        "do eiusmod tempor incididunt ut labore et dolore magna aliqua";
 
    const std::string_view needle{"pisci"};
 
    auto it = std::search(in.begin(), in.end(),
                  std::boyer_moore_horspool_searcher(
                      needle.begin(), needle.end()));
    if (it != in.end())
        std::cout << "The string " << std::quoted(needle) << " found at offset "
                  << it - in.begin() << '\n';
    else
        std::cout << "The string " << std::quoted(needle) << " not found\n";
}
```

Output:

```text
The string "pisci" found at offset 43
```

### See also

{/* source:cpp-utility-functional-boyer-moore-horspool-searcher:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`search`](/docs/cpp/library/algorithm/search)</DescriptionTerm>
    <DescriptionBody>searches for the first occurrence of a range of elements (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`default_searcher`](/docs/cpp/library/utility/functional/default_searcher) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>standard C++ library search algorithm implementation (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`boyer_moore_searcher`](/docs/cpp/library/utility/functional/boyer_moore_searcher) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>Boyer-Moore search algorithm implementation (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
