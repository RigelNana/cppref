---
title: "std::default_searcher"
description: "A class suitable for use with Searcher overload of std::search that delegates the search operation to the pre-C++17 standard library's std::search."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/default_searcher"
language: "C++"
---

{/* source:cpp-utility-functional-default-searcher:0000 */}
<DeclarationDoc>
Defined in header [`<functional>`](/docs/cpp/library/headers/functional)

  <Declaration language="cpp" since="C++17" code={`template< class ForwardIt, class BinaryPredicate = std::equal_to<> >
class default_searcher;`} />
</DeclarationDoc>

{/* source:cpp-utility-functional-default-searcher:0001 */}
A class suitable for use with [Searcher](https://en.cppreference.com/mwiki/index.php?title=cpp/named_req/Searcher&action=edit&redlink=1) overload of [`std::search`](/docs/cpp/library/algorithm/search) that delegates the search operation to the pre-C++17 standard library's [`std::search`](/docs/cpp/library/algorithm/search).

{/* source:cpp-utility-functional-default-searcher:0002 */}
`std::default_searcher` is [CopyConstructible](/docs/cpp/library/named_req/CopyConstructible) and [CopyAssignable](/docs/cpp/library/named_req/CopyAssignable).

### Member functions

{/* source:cpp-utility-functional-default-searcher:0003 */}
## std::default_searcher::default_searcher

<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`default_searcher( ForwardIt pat_first,
                  ForwardIt pat_last,

                  BinaryPredicate pred = BinaryPredicate() );`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

Constructs a `std::default_searcher` by storing copies of `pat_first`, `pat_last`, and `pred`.

### Parameters

<ParameterList>
  <Parameter name="pat_first, pat_last">a pair of iterators designating the string to be searched for</Parameter>
  <Parameter name="pred">a callable object used to determine equality</Parameter>
</ParameterList>

### Exceptions

Any exceptions thrown by the copy constructors of `BinaryPredicate` or `ForwardIt`.

{/* source:cpp-utility-functional-default-searcher:0004 */}
## std::default_searcher::operator()

<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`template< class ForwardIt2 >
std::pair<ForwardIt2, ForwardIt2>

    operator()( ForwardIt2 first, ForwardIt2 last ) const;`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

The member function called by the Searcher overload of [`std::search`](/docs/cpp/library/algorithm/search) to perform a search with this searcher.

Returns a pair of iterators `i, j`, where `i` is `std::search(first, last, pat_first, pat_last, pred)` and `j` is `std::next(i, std::distance(pat_first, pat_last))` unless `std::search` returned `last` (no match), in which case `j` equals `last` as well.

### Parameters

<ParameterList>
  <Parameter name="first, last">a pair of iterators designating the string to be examined</Parameter>
</ParameterList>

### Return value

A pair of iterators to the first and one past last positions in `[first, last)` where a subsequence that compares equal to `[pat_first, pat_last)` as defined by `pred` is located, or a pair of copies of `last` otherwise.

### Example

{/* source:cpp-utility-functional-default-searcher:0005 */}
```cpp
#include <algorithm>
#include <functional>
#include <iomanip>
#include <iostream>
#include <string_view>
 
int main()
{
    constexpr std::string_view in =
        "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed "
        "do eiusmod tempor incididunt ut labore et dolore magna aliqua";
 
    const std::string_view needle{"pisci"};
 
    auto it = std::search(in.begin(), in.end(),
                  std::default_searcher(
                      needle.begin(), needle.end()));
    if (it != in.end())
        std::cout << "The string " << std::quoted(needle) << " found at offset "
                  << it - in.begin() << '\n';
    else
        std::cout << "The string " << std::quoted(needle) << " not found\n";
}
```

Output:

```text
The string "pisci" found at offset 43
```

### See also

{/* source:cpp-utility-functional-default-searcher:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`search`](/docs/cpp/library/algorithm/search)</DescriptionTerm>
    <DescriptionBody>searches for the first occurrence of a range of elements (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`boyer_moore_searcher`](/docs/cpp/library/utility/functional/boyer_moore_searcher) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>Boyer-Moore search algorithm implementation (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`boyer_moore_horspool_searcher`](/docs/cpp/library/utility/functional/boyer_moore_horspool_searcher) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>Boyer-Moore-Horspool search algorithm implementation (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
