---
title: "std::greater<void>"
description: "std::greater<void> is a specialization of std::greater with parameter and return type deduced."
source_url: "https://en.cppreference.com/w/cpp/utility/functional/greater_void"
language: "C++"
---
{/* source:cpp-utility-functional-greater-void:0000 */}
<DeclarationDoc>
Defined in header [`<functional>`](/docs/cpp/library/headers/functional)

  <Declaration language="cpp" since="C++14" code={`template<>
class greater<void>;`} />
</DeclarationDoc>

{/* source:cpp-utility-functional-greater-void:0001 */}
`std::greater<void>` is a specialization of [`std::greater`](/docs/cpp/library/utility/functional/greater) with parameter and return type deduced.

### Nested types

{/* source:cpp-utility-functional-greater-void:0002 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`is_transparent`</DescriptionTerm>
    <DescriptionBody>[`unspecified`](/docs/cpp/library/utility/functional#Transparent_function_objects)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-utility-functional-greater-void:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator()`</DescriptionTerm>
    <DescriptionBody>tests if `lhs` compares greater than `rhs` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-utility-functional-greater-void:0004 */}
## std::greater\<void\>::operator()

<DeclarationDoc>
  <Declaration language="cpp" code={`template< class T, class U >
constexpr auto operator()( T&& lhs, U&& rhs ) const

    -> decltype(std::forward<T>(lhs) > std::forward<U>(rhs));`} />
</DeclarationDoc>

Returns the result of `std::forward<T>(lhs) > std::forward<U>(rhs)`.

### Parameters

<ParameterList>
  <Parameter name="lhs, rhs">values to compare</Parameter>
</ParameterList>

### Return value

`std::forward<T>(lhs) > std::forward<U>(rhs)`.

If a built-in operator comparing pointers is called, the result is consistent with the [implementation-defined strict total order over pointers](/docs/cpp/language/expressions/operator_comparison#Pointer_total_order).

### Exceptions

May throw implementation-defined exceptions.

### Example

{/* source:cpp-utility-functional-greater-void:0005 */}
```cpp
#include <algorithm>
#include <cstdint>
#include <functional>
 
constexpr bool strictly_positive(int lhs)
{
    return std::greater<>()(lhs, 0);
}
 
int main()
{
    constexpr std::int64_t low = 0B11;
    constexpr std::uint16_t high = 0X11;
    std::greater<> greater{};
    static_assert(greater(high, low));
 
    constexpr static auto arr = {0, 1, 2, 3, 4, 5};
    static_assert(!std::all_of(arr.begin(), arr.end(), strictly_positive));
    static_assert(std::all_of(arr.begin() + 1, arr.end(), strictly_positive));
}
```

### Defect reports

{/* source:cpp-utility-functional-greater-void:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-functional-greater-void:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2562} standard="C++98">
    <PublishedBehavior>the pointer total order might be inconsistent</PublishedBehavior>
    <CorrectedBehavior>guaranteed to be consistent</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
