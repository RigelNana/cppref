---
title: "std::ranges::subrange<I,S,K>::prev"
description: "Returns a copy of the subrange whose iterator is decremented by a given distance."
source_url: "https://en.cppreference.com/w/cpp/ranges/subrange/prev"
language: "C++"
---

{/* source:cpp-ranges-subrange-prev:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr subrange prev( std::iter_difference_t<I> n = 1 ) const
    requires std::bidirectional_iterator<I>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-subrange-prev:0001 */}
Returns a copy of `*this` whose [`begin_`](/docs/cpp/library/ranges/subrange#begin) is decremented (or incremented if `n` is negative). The actual decrement (or increment) operation is performed by [`advance()`](/docs/cpp/library/ranges/subrange/advance).

{/* source:cpp-ranges-subrange-prev:0002 */}
Equivalent to: `auto tmp = *this;` `tmp.advance(-n);` `return tmp;`

### Parameters

{/* source:cpp-ranges-subrange-prev:0003 */}
<ParameterList>
  <Parameter name="n">number of decrements of the iterator</Parameter>
</ParameterList>

### Return value

{/* source:cpp-ranges-subrange-prev:0004 */}
As described above.

### Notes

{/* source:cpp-ranges-subrange-prev:0005 */}
The difference between this function and [`advance()`](/docs/cpp/library/ranges/subrange/advance) is that the latter performs the decrement (or increment) in place.

### Example

{/* source:cpp-ranges-subrange-prev:0006 */}
```cpp
#include <iterator>
#include <list>
#include <print>
#include <ranges>

int main()
{
    std::list list{1, 2, 3, 4, 5};
    std::ranges::subrange sub{std::next(list.begin(), 2), std::prev(list.end(), 2)};
    std::println("{} {} {}", sub, sub.prev(), sub.prev(2));
}
```

Output:

```text
[3] [2, 3] [1, 2, 3]
```

### See also

{/* source:cpp-ranges-subrange-prev:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`next`](/docs/cpp/library/ranges/subrange/next)</DescriptionTerm>
    <DescriptionBody>obtains a copy of the `subrange` with its iterator advanced by a given distance (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`advance`](/docs/cpp/library/ranges/subrange/advance)</DescriptionTerm>
    <DescriptionBody>advances the iterator by given distance (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`prev`](/docs/cpp/library/iterator/prev) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>decrement an iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::prev`](/docs/cpp/library/iterator/ranges/prev) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>decrement an iterator by a given distance or to a bound (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
