---
title: "std::ranges::concat_view<Views...>::iterator<Const>::iterator"
description: "Constructs an iterator."
source_url: "https://en.cppreference.com/w/cpp/ranges/concat_view/iterator/iterator"
language: "C++"
---

{/* source:cpp-ranges-concat-view-iterator-iterator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++26" code={`/*iterator*/() = default;`} />
  <Declaration id="2" language="cpp" since="C++26" code={`constexpr /*iterator*/( /*iterator*/<!Const> it )
    requires Const &&
             (std::convertible_to<ranges::iterator_t<Views>,

                                  ranges::iterator_t<const Views>> && ...);`} />
  <Declaration id="3" language="cpp" since="C++26" code={`template< class... Args >
constexpr explicit /*iterator*/
    ( /*maybe-const*/<Const, concat_view>* parent, Args&&... args )

    requires std::constructible_from</*base-iter*/, Args&&...>;`} />
  (exposition only*)
</DeclarationDoc>

{/* source:cpp-ranges-concat-view-iterator-iterator:0001 */}
Constructs an iterator.

{/* source:cpp-ranges-concat-view-iterator-iterator:0002 */}
For the definition of `/*maybe-const*/`, see [`maybe-const`](/docs/cpp/library/ranges#maybe-const).

{/* source:cpp-ranges-concat-view-iterator-iterator:0003 */}
| Overload | [`Data members`](/docs/cpp/library/ranges/concat_view/iterator#Data_members) | |
| --- | --- | --- |
| | `parent_` | `it_` |
| (1) | initialized with `nullptr` | [default-initialized](/docs/cpp/language/initialization/default_initialization) |
| (2) | initialized with `it.`[`parent_`](/docs/cpp/library/ranges/concat_view/iterator#parent) | initialized with [`base-iter`](/docs/cpp/library/ranges/concat_view/iterator#base-iter)`(std::in_place_index<I>, std::get<I>(std::move(it.`[`it_`](/docs/cpp/library/ranges/concat_view/iterator#it)`)))` (where `I` is `it.`[`it_`](/docs/cpp/library/ranges/concat_view/iterator#it)`.index()`) |
| (3) | initialized with `parent` | initialized with `std::forward<Args>(args)...` |

{/* source:cpp-ranges-concat-view-iterator-iterator:0004 */}
2) If `it.`[`it_`](/docs/cpp/library/ranges/concat_view/iterator#it)`.valueless_by_exception()` is `true`, the behavior is undefined.

### Parameters

{/* source:cpp-ranges-concat-view-iterator-iterator:0005 */}
<ParameterList>
  <Parameter name="it">a mutable iterator</Parameter>
  <Parameter name="parent">a pointer to `ranges::concat_view`</Parameter>
  <Parameter name="args">the arguments to initialize [`it_`](/docs/cpp/library/ranges/concat_view/iterator#it)</Parameter>
</ParameterList>

### Example

{/* source:cpp-ranges-concat-view-iterator-iterator:0006 */}
This section is incomplete

Reason: no example
