---
title: "std::ranges::zip_transform_view<F,Views...>::iterator<Const>::operator[]"
description: "Returns the element at specified relative location, after transformation."
source_url: "https://en.cppreference.com/w/cpp/ranges/zip_transform_view/iterator/operator_at"
language: "C++"
---

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`constexpr decltype(auto) operator[]( difference_type n ) const
    requires ranges::random_access_range<Base>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0001 */}
Returns the element at specified relative location, after transformation.

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0002 */}
Equivalent to

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0003 */}
```cpp
return
    std::apply
    (
        [&]<class... Is>(const Is&... iters) -> decltype(auto)
        {
            return std::invoke(*parent_->fun_, iters[std::iter_difference_t<Is>(n)]...);
        },
        inner_.current_
    );
```

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0004 */}
where `*parent_->fun_` is the transformation function of type `F` stored in the parent `ranges::zip_transform_view`, and `current_` is the underlying tuple of iterators into `Views...`.

### Parameters

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0005 */}
<ParameterList>
  <Parameter name="n">position relative to current location.</Parameter>
</ParameterList>

### Return value

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0006 */}
The element which is the result of transformation (mapping).

### Notes

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0007 */}
The behavior is undefined if the `parent_` pointer to parent `ranges::zip_transform_view` is null (e.g. if `*this` is default constructed).

### Example

{/* source:cpp-ranges-zip-transform-view-iterator-operator-at:0008 */}
This section is incomplete

Reason: no example
