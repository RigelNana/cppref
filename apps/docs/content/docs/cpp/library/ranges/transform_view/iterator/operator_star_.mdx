---
title: "std::ranges::transform_view<V,F>::iterator<Const>::operator*"
description: "Returns the transformed element."
source_url: "https://en.cppreference.com/w/cpp/ranges/transform_view/iterator/operator_star_"
language: "C++"
---

{/* source:cpp-ranges-transform-view-iterator-operator-star:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr decltype(auto) operator*() const;`} />
</DeclarationDoc>

{/* source:cpp-ranges-transform-view-iterator-operator-star:0001 */}
Returns the transformed element.

{/* source:cpp-ranges-transform-view-iterator-operator-star:0002 */}
Effectively returns `std::invoke(*parent_->fun_, *current_)`, where `*parent_->fun_` is the transformation function stored in the parent `transform_view`, and `current_` is the underlying iterator into `V`.

### Parameters

{/* source:cpp-ranges-transform-view-iterator-operator-star:0003 */}
(none)

### Return value

{/* source:cpp-ranges-transform-view-iterator-operator-star:0004 */}
The transformed element.

### Notes

{/* source:cpp-ranges-transform-view-iterator-operator-star:0005 */}
`operator->` is not provided.

{/* source:cpp-ranges-transform-view-iterator-operator-star:0006 */}
The behavior is undefined if the pointer to parent `transform_view` is null (e.g. if `*this` is default constructed).

{/* source:cpp-ranges-transform-view-iterator-operator-star:0007 */}
If `*current_` is a prvalue, its lifetime ends before this function returns. If the transformation function returns a reference or pointer to it, the result would dangle.

### Example

{/* source:cpp-ranges-transform-view-iterator-operator-star:0008 */}
This section is incomplete

Reason: no example
