---
title: "std::ranges::split_view<V,Pattern>::iterator"
description: "The return type of split_view::begin. This is a forward_iterator, so it is expected that V models at least forward_range."
source_url: "https://en.cppreference.com/w/cpp/ranges/split_view/iterator"
language: "C++"
---

{/* source:cpp-ranges-split-view-iterator:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`class /*iterator*/;`} />

  (exposition only*)
</DeclarationDoc>

{/* source:cpp-ranges-split-view-iterator:0001 */}
The return type of [`split_view::begin`](/docs/cpp/library/ranges/split_view). This is a [`forward_iterator`](/docs/cpp/library/iterator/forward_iterator), so it is expected that `V` models at least [`forward_range`](/docs/cpp/library/ranges/forward_range).

### Member types

{/* source:cpp-ranges-split-view-iterator:0002 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`iterator_concept`</DescriptionTerm>
    <DescriptionBody>[`std::forward_iterator_tag`](/docs/cpp/library/iterator/iterator_tags)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`iterator_category`</DescriptionTerm>
    <DescriptionBody>[`std::input_iterator_tag`](/docs/cpp/library/iterator/iterator_tags)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`ranges::subrange<ranges::iterator_t<V>>`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`difference_type`</DescriptionTerm>
    <DescriptionBody>`ranges::range_difference_t<V>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Data members

{/* source:cpp-ranges-split-view-iterator:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`ranges::split_view<V, Pattern>*` `parent_` (private)</DescriptionTerm>
    <DescriptionBody>a pointer to the parent [`split_view`](/docs/cpp/library/ranges/split_view) object (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`ranges::iterator_t<V>` `cur_` (private)</DescriptionTerm>
    <DescriptionBody>an iterator into the underlying [`view`](/docs/cpp/library/ranges/view) that points to the begin of a current subrange (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`ranges::subrange<ranges::iterator_t<V>>` `next_` (private)</DescriptionTerm>
    <DescriptionBody>a subrange to the position of the pattern next to the current subrange (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`bool` `trailing_empty_` (private)</DescriptionTerm>
    <DescriptionBody>a flag that indicates whether an empty trailing subrange (if any) was reached (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-ranges-split-view-iterator:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`(constructor)`</DescriptionTerm>
    <DescriptionBody>constructs an iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`base`</DescriptionTerm>
    <DescriptionBody>returns the underlying iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator*`</DescriptionTerm>
    <DescriptionBody>returns the current subrange (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator++`, `operator++(int)`</DescriptionTerm>
    <DescriptionBody>advances the iterator (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### `std::ranges::split_view::iterator::iterator`

{/* source:cpp-ranges-split-view-iterator:0005 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`/*iterator*/() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr /*iterator*/( split_view& parent, ranges::iterator_t<V> current,
                        ranges::subrange<ranges::iterator_t<V>> next );`} />
</DeclarationDoc>

1. 1) Value-initializes non-static data members with their default member initializers, that is

    - `ranges::split_view* parent_ = nullptr;`,
    - `ranges::iterator_t<V> cur_ = ranges::iterator_t<V>();`,
    - `ranges::subrange<ranges::iterator_t<V>> next_ = ranges::subrange<ranges::iterator_t<V>>();`, and
    - `bool trailing_empty_ = false;`.

2. 2) Initializes non-static data members:

    - `ranges::split_view* parent_ = std::addressof(parent);`,
    - `ranges::iterator_t<V> cur_ = std::move(current);`,
    - `ranges::subrange<ranges::iterator_t<V>> next_ = std::move(next);`, and
    - `bool trailing_empty_ = false;`.

#### `std::ranges::split_view::iterator::base`

{/* source:cpp-ranges-split-view-iterator:0006 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr const ranges::iterator_t<V> base() const;`} />
</DeclarationDoc>

Equivalent to `return cur_;`.

#### `std::ranges::split_view::iterator::operator*`

{/* source:cpp-ranges-split-view-iterator:0007 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr value_type operator*() const;`} />
</DeclarationDoc>

Equivalent to `return {cur_, next_.begin()};`.

#### `std::ranges::split_view::iterator::operator++`

{/* source:cpp-ranges-split-view-iterator:0008 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr /*iterator*/& operator++();`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr void operator++( int );`} />
</DeclarationDoc>

1. 1) Equivalent to

```cpp
cur_ = next_.begin();
if (cur_ != ranges::end(parent_->base_))
{
    if (cur_ = next_.end(); cur_ == ranges::end(parent_->base_))
    {
        trailing_empty_ = true;
        next_ = {cur_, cur_};
    }
    else
        next_ = parent_->find_next(cur_);
}
else
    trailing_empty_ = false;


return *this;
```

2. 2) Equivalent to `auto tmp = *this; ++*this; return tmp;`.

### Non-member functions

{/* source:cpp-ranges-split-view-iterator:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator==`</DescriptionTerm>
    <DescriptionBody>compares the underlying iterators (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### `operator==(std::ranges::split_view::iterator, std::ranges::split_view::iterator)`

{/* source:cpp-ranges-split-view-iterator:0010 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`friend constexpr bool operator==( const /*iterator*/& x, const /*iterator*/& y );`} />
</DeclarationDoc>

Equivalent to `return x.cur_ == y.cur_ and x.trailing_empty_ == y.trailing_empty_;`.

The `!=` operator is [synthesized](/docs/cpp/language/expressions/default_comparisons#Other_defaulted_comparison_operators) from `operator==`.

This function is not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `std::ranges::split_view::iterator` is an associated class of the arguments.
