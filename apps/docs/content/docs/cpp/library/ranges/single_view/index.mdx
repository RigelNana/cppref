---
title: "std::ranges::views::single, std::ranges::single_view"
description: "std::ranges::single_view is a view that contains exactly one element of a specified value."
source_url: "https://en.cppreference.com/w/cpp/ranges/single_view"
language: "C++"
---

{/* source:cpp-ranges-single-view:0000 */}
<DeclarationDoc>
Defined in header [`<ranges>`](/docs/cpp/library/headers/ranges)

  <Declaration id="1" language="cpp" since="C++20" until="C++23" code={`template< std::copy_constructible T >
    requires std::is_object_v<T>
class single_view

    : public ranges::view_interface<single_view<T>>`} />
  <Declaration id="1" language="cpp" since="C++23" code={`template< std::move_constructible T >
    requires std::is_object_v<T>
class single_view

    : public ranges::view_interface<single_view<T>>`} />
  <Declaration id="2" language="cpp" since="C++20" code={`namespace views {
    inline constexpr /* unspecified */ single = /* unspecified */;

}`} />
  Call signature

  <Declaration language="cpp" since="C++20" code={`template< class T >
    requires /* see below */

constexpr /* see below */ single( T&& t );`} />

  <DeclarationDescription>
    {/* source:cpp-ranges-single-view:0001 */}
    1) Produces a [`view`](/docs/cpp/library/ranges/view) that contains exactly one element of a specified value.

    {/* source:cpp-ranges-single-view:0002 */}
    2) The expression `views::single(e)` is [expression-equivalent](/docs/cpp/language/expressions#Expression-equivalence) to `single_view<std::decay_t<decltype((e))>>(e)` for any suitable subexpression `e`.

    {/* source:cpp-ranges-single-view:0003 */}
    The lifetime of the element is bound to the parent `single_view`. Copying `single_view` makes a copy of the element.
  </DeclarationDescription>
</DeclarationDoc>

### Customization point objects

{/* source:cpp-ranges-single-view:0004 */}
The name `views::single` denotes a *customization point object*, which is a const [function object](/docs/cpp/library/named_req/FunctionObject) of a [literal](/docs/cpp/library/named_req/LiteralType) [`semiregular`](/docs/cpp/library/concepts/semiregular) class type. For exposition purposes, the cv-unqualified version of its type is denoted as `__single_fn`.

{/* source:cpp-ranges-single-view:0005 */}
All instances of `__single_fn` are equal. The effects of invoking different instances of type `__single_fn` on the same arguments are equivalent, regardless of whether the expression denoting the instance is an lvalue or rvalue, and is const-qualified or not (however, a volatile-qualified instance is not required to be invocable). Thus, `views::single` can be copied freely and its copies can be used interchangeably.

{/* source:cpp-ranges-single-view:0006 */}
Given a set of types `Args...`, if `std::declval<Args>()...` meet the requirements for arguments to `views::single` above, `__single_fn` models

{/* source:cpp-ranges-single-view:0007 */}
- `std::invocable<__single_fn, Args...>`,
- `std::invocable<const __single_fn, Args...>`,
- `std::invocable<__single_fn&, Args...>`, and
- `std::invocable<const __single_fn&, Args...>`.

{/* source:cpp-ranges-single-view:0008 */}
Otherwise, no function call operator of `__single_fn` participates in overload resolution.

### Data members

{/* source:cpp-ranges-single-view:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`copyable-box`](/docs/cpp/library/ranges/copyable_wrapper)`<T>` `value_` <InlineRevision until="C++23" /></DescriptionTerm>
    <DescriptionBody>the single element of the view (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`movable-box`](/docs/cpp/library/ranges/copyable_wrapper)`<T>` `value_` <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>the single element of the view (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-ranges-single-view:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/ranges/single_view#ctor)</DescriptionTerm>
    <DescriptionBody>constructs a `single_view` (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`](/docs/cpp/library/ranges/single_view#begin)</DescriptionTerm>
    <DescriptionBody>returns a pointer to the element (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`end`](/docs/cpp/library/ranges/single_view#end)</DescriptionTerm>
    <DescriptionBody>returns a pointer past the element (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/ranges/single_view#empty) [static]</DescriptionTerm>
    <DescriptionBody>returns `false` (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/ranges/single_view#size) [static]</DescriptionTerm>
    <DescriptionBody>returns `1` (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`data`](/docs/cpp/library/ranges/single_view#data)</DescriptionTerm>
    <DescriptionBody>returns a pointer to the element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Inherited from [`std::ranges::view_interface`](/docs/cpp/library/ranges/view_interface)

<DescriptionList>
<DescriptionItem>
    <DescriptionTerm>[`cbegin`](/docs/cpp/library/ranges/view_interface/cbegin) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a constant iterator to the beginning of the range. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cend`](/docs/cpp/library/ranges/view_interface/cend) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a sentinel for the constant iterator of the range. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator bool`](/docs/cpp/library/ranges/view_interface/operator_bool)</DescriptionTerm>
    <DescriptionBody>returns whether the derived view is not empty. Provided if [`ranges::empty`](/docs/cpp/library/ranges/empty) is applicable to it. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`front`](/docs/cpp/library/ranges/view_interface/front)</DescriptionTerm>
    <DescriptionBody>returns the first element in the derived view. Provided if it satisfies [`forward_range`](/docs/cpp/library/ranges/forward_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`back`](/docs/cpp/library/ranges/view_interface/back)</DescriptionTerm>
    <DescriptionBody>returns the last element in the derived view. Provided if it satisfies [`bidirectional_range`](/docs/cpp/library/ranges/bidirectional_range) and [`common_range`](/docs/cpp/library/ranges/common_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator[]`](/docs/cpp/library/ranges/view_interface/operator_at)</DescriptionTerm>
    <DescriptionBody>returns the `n`th element in the derived view. Provided if it satisfies [`random_access_range`](/docs/cpp/library/ranges/random_access_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-ranges-single-view:0011 */}
#### `std::ranges::single_view::single_view`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`single_view() requires std::default_initializable<T> = default;`} />
  <Declaration id="2" language="cpp" since="C++20" until="C++23" code={`constexpr explicit single_view( const T& t );`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr explicit single_view( const T& t )
    requires std::copy_constructible<T>;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr explicit single_view( T&& t );`} />
  <Declaration id="4" language="cpp" since="C++20" code={`template< class... Args >
    requires std::constructible_from<T, Args...>

constexpr explicit single_view( std::in_place_t, Args&&... args );`} />
</DeclarationDoc>

Constructs a `single_view`.

1. Default initializes `value_`, which value-initializes its contained value.
2. Initializes `value_` with `t`.
3. Initializes `value_` with `std::move(t)`.
4. Initializes `value_` as if by `value_{std::in_place, std::forward<Args>(args)...}`.

{/* source:cpp-ranges-single-view:0012 */}
#### `std::ranges::single_view::begin`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr T* begin() noexcept;
constexpr const T* begin() const noexcept;`} />
</DeclarationDoc>

Equivalent to `return data();`.

{/* source:cpp-ranges-single-view:0013 */}
#### `std::ranges::single_view::end`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr T* end() noexcept;
constexpr const T* end() const noexcept;`} />
</DeclarationDoc>

Equivalent to `return data() + 1;`.

{/* source:cpp-ranges-single-view:0014 */}
#### `std::ranges::single_view::empty`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`static constexpr bool empty() noexcept;`} />
</DeclarationDoc>

Equivalent to `return false;`.

{/* source:cpp-ranges-single-view:0015 */}
#### `std::ranges::single_view::size`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`static constexpr std::size_t size() noexcept;`} />
</DeclarationDoc>

Equivalent to `return 1;`. Makes `single_view` model `/*tiny-range*/` as required by [`split_view`](/docs/cpp/library/ranges/split_view).

{/* source:cpp-ranges-single-view:0016 */}
#### `std::ranges::single_view::data`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr T* data() noexcept;
constexpr const T* data() const noexcept;`} />
</DeclarationDoc>

Returns a pointer to the contained value of `value_`. The behavior is undefined if `value_` does not contains a value.

### Deduction guides

{/* source:cpp-ranges-single-view:0017 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class T >
single_view( T ) -> single_view<T>;`} />
</DeclarationDoc>

### Notes

{/* source:cpp-ranges-single-view:0018 */}
For a [`single_view`](/docs/cpp/library/ranges/single_view#top), the inherited `empty` member function always returns `false`, and the inherited `operator bool` conversion function always returns `true`.

### Example

{/* source:cpp-ranges-single-view:0019 */}
```cpp
#include <iomanip>
#include <iostream>
#include <ranges>
#include <string>
#include <tuple>
 
int main()
{
    constexpr std::ranges::single_view sv1{3.1415}; // uses (const T&) constructor
    static_assert(sv1);
    static_assert(not sv1.empty());
 
    std::cout << "1) *sv1.data(): " << *sv1.data() << '\n'
              << "2) *sv1.begin(): " << *sv1.begin() << '\n'
              << "3)  sv1.size(): " << sv1.size() << '\n'
              << "4)  distance: " << std::distance(sv1.begin(), sv1.end()) << '\n';
 
    std::string str{"C++20"};
    std::cout << "5)  str = " << std::quoted(str) << '\n';
    std::ranges::single_view sv2{std::move(str)}; // uses (T&&) constructor
    std::cout << "6) *sv2.data(): " << std::quoted(*sv2.data()) << '\n'
              << "7)  str = " << std::quoted(str) << '\n';
 
    std::ranges::single_view<std::tuple<int, double, std::string>>
        sv3{std::in_place, 42, 3.14, "😄"}; // uses (std::in_place_t, Args&&... args)
 
    std::cout << "8)  sv3 holds a tuple: { "
              << std::get<0>(sv3[0]) << ", "
              << std::get<1>(sv3[0]) << ", "
              << std::get<2>(sv3[0]) << " }\n";
}
```

Output:

```text
1) *sv1.data(): 3.1415
2) *sv1.begin(): 3.1415
3)  sv1.size(): 1
4)  distance: 1
5)  str = "C++20"
6) *sv2.data(): "C++20"
7)  str = ""
8)  sv3 holds a tuple: { 42, 3.14, 😄 }
```

### Defect reports

{/* source:cpp-ranges-single-view:0020 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-ranges-single-view:0021 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3428} standard="C++20">
    <PublishedBehavior>`single_view` was convertible from [`std::in_place_t`](/docs/cpp/library/utility/in_place)</PublishedBehavior>
    <CorrectedBehavior>the constructor is made explicit</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={4035} standard="C++20">
    <PublishedBehavior>`single_view` did not provide the member function `empty()`</PublishedBehavior>
    <CorrectedBehavior>provides `empty()`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P2367R0" standard="C++20">
    <PublishedBehavior>deduction guides for `single_view` failed to decay the argument; `views::single` copied but not wrapped a `single_view`</PublishedBehavior>
    <CorrectedBehavior>a decaying guide provided; made always wrapping</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-ranges-single-view:0022 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`optional`](/docs/cpp/library/utility/optional) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>a wrapper that may or may not hold an object (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::empty_view views::empty`](/docs/cpp/library/ranges/empty_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>an empty [`view`](/docs/cpp/library/ranges/view) with no elements (class template) (variable template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::split_view views::split`](/docs/cpp/library/ranges/split_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a [`view`](/docs/cpp/library/ranges/view) over the subranges obtained from splitting another [`view`](/docs/cpp/library/ranges/view) using a delimiter (class template) (range adaptor object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
