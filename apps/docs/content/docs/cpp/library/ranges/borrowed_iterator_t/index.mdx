---
title: "std::ranges::borrowed_iterator_t, std::ranges::borrowed_subrange_t"
description: "Obtains the iterator or subrange type of a range that models borrowed_range, otherwise the placeholder type dangling."
source_url: "https://en.cppreference.com/w/cpp/ranges/borrowed_iterator_t"
language: "C++"
---

{/* source:cpp-ranges-borrowed-iterator-t:0000 */}
<DeclarationDoc>
Defined in header [`<ranges>`](/docs/cpp/library/headers/ranges)

  <Declaration id="1" language="cpp" since="C++20" code={`template< ranges::range R >
using borrowed_iterator_t = /* see below */;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< ranges::range R >
using borrowed_subrange_t = /* see below */;`} />
</DeclarationDoc>

{/* source:cpp-ranges-borrowed-iterator-t:0001 */}
1. 1) `std::ranges::iterator_t<R>` if `R` models [`borrowed_range`](/docs/cpp/library/ranges/borrowed_range), [`std::ranges::dangling`](/docs/cpp/library/ranges/dangling) otherwise.

{/* source:cpp-ranges-borrowed-iterator-t:0002 */}
2. 2) `std::ranges::subrange<std::ranges::iterator_t<R>>` if `R` models [`borrowed_range`](/docs/cpp/library/ranges/borrowed_range), [`std::ranges::dangling`](/docs/cpp/library/ranges/dangling) otherwise.

{/* source:cpp-ranges-borrowed-iterator-t:0003 */}
These two alias templates are used by some [constrained algorithms](/docs/cpp/library/algorithm/ranges) to avoid returning potentially dangling iterators or views.

### Possible implementation

{/* source:cpp-ranges-borrowed-iterator-t:0004 */}
[`borrowed_iterator_t`](/docs/cpp/library/ranges/borrowed_iterator_t#Version_1)

```cpp
template< std::ranges::range R >
using borrowed_iterator_t = std::conditional_t<std::ranges::borrowed_range<R>,
    std::ranges::iterator_t<R>, std::ranges::dangling>;
```

[`borrowed_subrange_t`](/docs/cpp/library/ranges/borrowed_iterator_t#Version_2)

```cpp
template< std::ranges::range R >
using borrowed_subrange_t = std::conditional_t<std::ranges::borrowed_range<R>,
    std::ranges::subrange<std::ranges::iterator_t<R>>, std::ranges::dangling>;
```

### See also

{/* source:cpp-ranges-borrowed-iterator-t:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::dangling`](/docs/cpp/library/ranges/dangling) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a placeholder type indicating that an iterator or a `subrange` should not be returned since it would be dangling (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
