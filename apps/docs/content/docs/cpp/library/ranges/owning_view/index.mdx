---
title: "std::ranges::owning_view"
description: "std::ranges::owning_view is a view that has unique ownership of a range; it is move-only and stores that range within it."
source_url: "https://en.cppreference.com/w/cpp/ranges/owning_view"
language: "C++"
---

{/* source:cpp-ranges-owning-view:0000 */}
<DeclarationDoc>
Defined in header [`<ranges>`](/docs/cpp/library/headers/ranges)

  <Declaration language="cpp" since="C++20" code={`template< ranges::range R >
    requires std::movable<R> && (!/*is-initializer-list*/<R>)
class owning_view

    : public ranges::view_interface<owning_view<R>>`} />
</DeclarationDoc>

{/* source:cpp-ranges-owning-view:0001 */}
`owning_view` is a [`view`](/docs/cpp/library/ranges/view) that has unique ownership of a [`range`](/docs/cpp/library/ranges/range). It is move-only and stores that `range` within it.

{/* source:cpp-ranges-owning-view:0002 */}
The constant `/*is-initializer-list*/<R>` in the requires-clause is `true` if and only if `std::remove_cvref_t<R>` is a specialization of [`std::initializer_list`](/docs/cpp/library/utility/initializer_list).

### Data members

{/* source:cpp-ranges-owning-view:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`r_` (private)</DescriptionTerm>
    <DescriptionBody>The underlying range of type `R`. (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-ranges-owning-view:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/ranges/owning_view#ctor)</DescriptionTerm>
    <DescriptionBody>constructs an `owning_view` by value-initializing or move-constructing the stored range (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/ranges/owning_view#assign)</DescriptionTerm>
    <DescriptionBody>move-assigns the stored range (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`base`](/docs/cpp/library/ranges/owning_view#base)</DescriptionTerm>
    <DescriptionBody>returns a reference to the stored range (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`begin`](/docs/cpp/library/ranges/owning_view#begin)</DescriptionTerm>
    <DescriptionBody>returns the beginning iterator of the stored range (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`end`](/docs/cpp/library/ranges/owning_view#end)</DescriptionTerm>
    <DescriptionBody>returns the sentinel of the stored range (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/ranges/owning_view#empty)</DescriptionTerm>
    <DescriptionBody>checks whether the stored range is empty (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/ranges/owning_view#size)</DescriptionTerm>
    <DescriptionBody>returns the size of the stored [`sized_range`](/docs/cpp/library/ranges/sized_range) (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`data`](/docs/cpp/library/ranges/owning_view#data)</DescriptionTerm>
    <DescriptionBody>returns the pointer to the beginning of the stored [`contiguous_range`](/docs/cpp/library/ranges/contiguous_range) (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Inherited from [`std::ranges::view_interface`](/docs/cpp/library/ranges/view_interface)

<DescriptionList>
<DescriptionItem>
    <DescriptionTerm>[`cbegin`](/docs/cpp/library/ranges/view_interface/cbegin) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a constant iterator to the beginning of the range. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cend`](/docs/cpp/library/ranges/view_interface/cend) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a sentinel for the constant iterator of the range. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator bool`](/docs/cpp/library/ranges/view_interface/operator_bool)</DescriptionTerm>
    <DescriptionBody>returns whether the derived view is not empty. Provided if [`ranges::empty`](/docs/cpp/library/ranges/empty) is applicable to it. (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`front`](/docs/cpp/library/ranges/view_interface/front)</DescriptionTerm>
    <DescriptionBody>returns the first element in the derived view. Provided if it satisfies [`forward_range`](/docs/cpp/library/ranges/forward_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`back`](/docs/cpp/library/ranges/view_interface/back)</DescriptionTerm>
    <DescriptionBody>returns the last element in the derived view. Provided if it satisfies [`bidirectional_range`](/docs/cpp/library/ranges/bidirectional_range) and [`common_range`](/docs/cpp/library/ranges/common_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator[]`](/docs/cpp/library/ranges/view_interface/operator_at)</DescriptionTerm>
    <DescriptionBody>returns the `n`th element in the derived view. Provided if it satisfies [`random_access_range`](/docs/cpp/library/ranges/random_access_range). (public member function of `std::ranges::view_interface<D>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-ranges-owning-view:0005 */}
#### `std::ranges::owning_view::owning_view`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`owning_view() requires std::default_initializable<R> = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`owning_view( owning_view&& other ) = default;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr owning_view( R&& t );`} />
  <Declaration id="4" language="cpp" since="C++20" code={`owning_view( const owning_view& ) = delete;`} />
</DeclarationDoc>

1. Default constructor. Value-initializes the stored range by its default member initializer (`= R()`).
2. Move constructor. Move constructs the stored range from that of `other`.
3. Move constructs the stored range from `t`.
4. Copy constructor is deleted. `owning_view` is move-only.

##### Parameters

<ParameterList>
  <Parameter name="other">another `owning_view` to move from</Parameter>
  <Parameter name="t">range to move from</Parameter>
</ParameterList>

{/* source:cpp-ranges-owning-view:0006 */}
#### `std::ranges::owning_view::operator=`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`owning_view& operator=( owning_view&& other ) = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`owning_view& operator=( const owning_view& ) = delete;`} />
</DeclarationDoc>

1. Move assignment operator. Move assigns the stored range from that of `other`.
2. Copy assignment operator is deleted. `owning_view` is move-only.

##### Parameters

<ParameterList>
  <Parameter name="other">another `owning_view` to move from</Parameter>
</ParameterList>

##### Return value

`*this`

{/* source:cpp-ranges-owning-view:0007 */}
#### `std::ranges::owning_view::base`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr R& base() & noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr const R& base() const & noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr R&& base() && noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr const R&& base() const && noexcept;`} />
</DeclarationDoc>

Returns a reference to the stored range, keeping value category and const-qualification.

1,2) Equivalent to `return r_;`.

3,4) Equivalent to `return std::move(r_);`.

{/* source:cpp-ranges-owning-view:0008 */}
#### `std::ranges::owning_view::begin`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr ranges::iterator_t<R> begin();`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr auto begin() const requires ranges::range<const R>;`} />
</DeclarationDoc>

Equivalent to `return ranges::begin(r_);`.

{/* source:cpp-ranges-owning-view:0009 */}
#### `std::ranges::owning_view::end`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr ranges::sentinel_t<R> end();`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr auto end() const requires ranges::range<const R>;`} />
</DeclarationDoc>

Equivalent to `return ranges::end(r_);`.

{/* source:cpp-ranges-owning-view:0010 */}
#### `std::ranges::owning_view::empty`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr bool empty() requires requires { ranges::empty(r_); };`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr bool empty() const requires requires { ranges::empty(r_); };`} />
</DeclarationDoc>

Equivalent to `return ranges::empty(r_);`.

{/* source:cpp-ranges-owning-view:0011 */}
#### `std::ranges::owning_view::size`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr auto size() requires ranges::sized_range<R>;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr auto size() const requires ranges::sized_range<const R>;`} />
</DeclarationDoc>

Equivalent to `return ranges::size(r_);`.

{/* source:cpp-ranges-owning-view:0012 */}
#### `std::ranges::owning_view::data`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr auto data() requires ranges::contiguous_range<R>;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr auto data() const requires ranges::contiguous_range<const R>;`} />
</DeclarationDoc>

Equivalent to `return ranges::data(r_);`.

### Helper templates

{/* source:cpp-ranges-owning-view:0013 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class T >
constexpr bool enable_borrowed_range<std::ranges::owning_view<T>> =

    ranges::enable_borrowed_range<T>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-owning-view:0014 */}
This specialization of `ranges::enable_borrowed_range` makes `owning_view` satisfy [`borrowed_range`](/docs/cpp/library/ranges/borrowed_range) when the underlying range satisfies it.

### Example

{/* source:cpp-ranges-owning-view:0015 */}
```cpp
#include <cassert>
#include <iostream>
#include <ranges>
#include <string>
 
int main()
{
    using namespace std::literals;
    std::ranges::owning_view ov{"cosmos"s}; // the deduced type of R is std::string;
                                            // `ov` is the only owner of this string
    assert(
        ov.empty() == false &&
        ov.size() == 6 &&
        ov.size() == ov.base().size() &&
        ov.front() == 'c' &&
        ov.front() == *ov.begin() &&
        ov.back() == 's' &&
        ov.back() == *(ov.end() - 1) &&
        ov.data() == ov.base()
    );
 
    std::cout << "sizeof(ov): " << sizeof ov << '\n' // typically equal to sizeof(R)
              << "range-for: ";
    for (const char ch : ov)
        std::cout << ch;
    std::cout << '\n';
 
    std::ranges::owning_view<std::string> ov2;
    assert(ov2.empty());
//  ov2 = ov; // compile-time error: copy assignment operator is deleted
    ov2 = std::move(ov); // OK
    assert(ov2.size() == 6);
}
```

Possible output:

```text
sizeof(ov): 32
range-for: cosmos
```

### See also

{/* source:cpp-ranges-owning-view:0016 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::ref_view`](/docs/cpp/library/ranges/ref_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a [`view`](/docs/cpp/library/ranges/view) of the elements of some other [`range`](/docs/cpp/library/ranges/range) (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`views::all_t views::all`](/docs/cpp/library/ranges/all_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a [`view`](/docs/cpp/library/ranges/view) that includes all elements of a [`range`](/docs/cpp/library/ranges/range) (alias template) (range adaptor object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
