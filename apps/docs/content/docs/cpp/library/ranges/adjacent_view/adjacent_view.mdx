---
title: "std::ranges::adjacent_view<V,N>::adjacent_view"
description: "Constructs an adjacent_view."
source_url: "https://en.cppreference.com/w/cpp/ranges/adjacent_view/adjacent_view"
language: "C++"
---

{/* source:cpp-ranges-adjacent-view-adjacent-view:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`adjacent_view()
    requires std::default_initializable<V> = default;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr explicit adjacent_view( V base );`} />
</DeclarationDoc>

{/* source:cpp-ranges-adjacent-view-adjacent-view:0001 */}
Constructs an [`adjacent_view`](/docs/cpp/library/ranges/adjacent_view).

{/* source:cpp-ranges-adjacent-view-adjacent-view:0002 */}
1) Default constructor. [Value-initializes](/docs/cpp/language/initialization/value_initialization) the underlying view.

{/* source:cpp-ranges-adjacent-view-adjacent-view:0003 */}
2) Initializes the underlying view [`base_`](/docs/cpp/library/ranges/adjacent_view#Data_members) with `std::move(base)`.

### Parameters

{/* source:cpp-ranges-adjacent-view-adjacent-view:0004 */}
<ParameterList>
  <Parameter name="base">the underlying view</Parameter>
</ParameterList>

### Example

{/* source:cpp-ranges-adjacent-view-adjacent-view:0005 */}
```cpp
#include <iostream>
#include <ranges>
#include <string>
#include <tuple>
 
template<class... Ts>
void print(std::tuple<Ts...> const& tuple)
{
    std::apply([&](auto&& arg, auto&&... args)
    {
        std::cout << arg;
        ((std::cout << args), ...);
    }, tuple);
    std::cout << '\n';
}
 
int main()
{
    const std::string v{"ABCDEF"};
    constexpr int window_size{4};
 
    std::cout << "v: " << v << '\n';
 
    auto view = std::views::adjacent<window_size>(v); // overload (2)
 
    for (auto const& tuple : view)
        print(tuple);
}
```

Output:

```text
v: ABCDEF
ABCD
BCDE
CDEF
```
