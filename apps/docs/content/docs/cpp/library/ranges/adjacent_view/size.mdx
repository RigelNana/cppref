---
title: "std::ranges::adjacent_view<V,N>::size"
description: "Returns the number of elements."
source_url: "https://en.cppreference.com/w/cpp/ranges/adjacent_view/size"
language: "C++"
---

{/* source:cpp-ranges-adjacent-view-size:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr auto size()
    requires ranges::sized_range<V>;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr auto size() const
    requires ranges::sized_range<const V>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-adjacent-view-size:0001 */}
Returns the number of elements.

{/* source:cpp-ranges-adjacent-view-size:0002 */}
Let [`base_`](/docs/cpp/library/ranges/adjacent_view#Data_members) be the underlying view. Equivalent to:

{/* source:cpp-ranges-adjacent-view-size:0003 */}
```cpp
using SizeType = decltype(ranges::size(base_));
using CommonType = ranges::common_type_t<SizeType, std::size_t>;
auto size = static_cast<CommonType>(ranges::size(base_));
size -= std::min<CommonType>(size, N - 1);
return static_cast<SizeType>(size);
```

### Parameters

{/* source:cpp-ranges-adjacent-view-size:0004 */}
(none)

### Return value

{/* source:cpp-ranges-adjacent-view-size:0005 */}
The number of elements, may be `0` if `ranges::size(base_)` is less than `N`.

### Example

{/* source:cpp-ranges-adjacent-view-size:0006 */}
```cpp
#include <ranges>
 
int main()
{
    constexpr static auto v = {1, 2, 3, 4, 5, 6};
 
    constexpr int width1 {4};
    constexpr auto view1 {std::views::adjacent<width1>(v)};
    static_assert(view1.size() == 3);
    static_assert(view1.size() == (v.size() - width1 + 1));
 
    constexpr int width2 {8};
    constexpr auto view2 {std::views::adjacent<width2>(v)};
    // window is too wide, so view2 has no elements:
    static_assert(view2.size() == 0);
}
```

### See also

{/* source:cpp-ranges-adjacent-view-size:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::size`](/docs/cpp/library/ranges/size) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns an integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::ssize`](/docs/cpp/library/ranges/ssize) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns a signed integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
