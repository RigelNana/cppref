---
title: "std::ranges::cartesian_product_view<First, Vs...>::size"
description: "Returns the number of elements. The return type is an implementation-defined /*unsigned-integer-like*/ type U."
source_url: "https://en.cppreference.com/w/cpp/ranges/cartesian_product_view/size"
language: "C++"
---

{/* source:cpp-ranges-cartesian-product-view-size:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /* see description */ size()
    requires /*cartesian-product-is-sized*/<First, Vs...>;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr /* see description */ size() const
    requires /*cartesian-product-is-sized*/<const First, const Vs...>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-cartesian-product-view-size:0001 */}
Returns the number of elements. The return type is an implementation-defined `/*unsigned-integer-like*/` type `U`.

{/* source:cpp-ranges-cartesian-product-view-size:0002 */}
Let *[`bases_`](/docs/cpp/library/ranges/cartesian_product_view#Data_members)* be the underlying tuple of views, and `prod` be the product of the sizes of all the ranges in `bases_`.

{/* source:cpp-ranges-cartesian-product-view-size:0003 */}
1,2) Returns `prod`. The behavior is undefined if `prod` cannot be represented by the return type `U`.

Equivalent to:

```cpp
return [&]<std::size_t... Is>(std::index_sequence<Is...>)
{
    auto prod = static_cast<U>(1);
    prod = (static_cast<U>(ranges::size(std::get<Is>(bases_))) * ...);
    return prod;
}
(std::make_index_sequence<1U + sizeof...(Vs)>{});
```

### Parameters

{/* source:cpp-ranges-cartesian-product-view-size:0004 */}
(none)

### Return value

{/* source:cpp-ranges-cartesian-product-view-size:0005 */}
The number of elements, that is, the product of the sizes of all the underlying ranges.

### Notes

{/* source:cpp-ranges-cartesian-product-view-size:0006 */}
The return type is the smallest `/*unsigned-integer-like*/` type that is sufficiently wide to store the product of the maximum sizes of all the underlying ranges, if such a type exists.

### Example

{/* source:cpp-ranges-cartesian-product-view-size:0007 */}
```cpp
#include <ranges>
 
int main()
{
    constexpr static auto w = {1};
    constexpr static auto x = {2, 3};
    constexpr static auto y = {4, 5, 6};
    constexpr static auto z = {7, 8, 9, 10, 11, 12, 13};
    constexpr auto v = std::ranges::cartesian_product_view(w, x, y, z);
    static_assert(v.size() == w.size() * x.size() * y.size() * z.size() and v.size() == 42);
}
```

### See also

{/* source:cpp-ranges-cartesian-product-view-size:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::size`](/docs/cpp/library/ranges/size) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns an integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::ssize`](/docs/cpp/library/ranges/ssize) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns a signed integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
