---
title: "std::ranges::join_with_view<V,Pattern>::iterator<Const>::operator++,--"
description: "Increments or decrements the iterator."
source_url: "https://en.cppreference.com/w/cpp/ranges/join_with_view/iterator/operator_arith"
language: "C++"
---

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /*iterator*/& operator++();`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr void operator++( int );`} />
  <Declaration id="3" language="cpp" since="C++23" code={`constexpr /*iterator*/ operator++( int )
    requires std::is_reference_v</*InnerBase*/> &&
             ranges::forward_range</*Base*/> &&

             ranges::forward_range</*InnerBase*/>;`} />
  <Declaration id="4" language="cpp" since="C++23" code={`constexpr /*iterator*/& operator--()
    requires std::is_reference_v</*InnerBase*/> &&
             ranges::bidirectional_range</*Base*/> &&
             ranges::bidirectional_range</*InnerBase*/> &&
             ranges::common_range</*InnerBase*/> &&
             ranges::bidirectional_range</*PatternBase*/> &&

             ranges::common_range</*PatternBase*/>;`} />
  <Declaration id="5" language="cpp" since="C++23" code={`constexpr /*iterator*/ operator--( int )
    requires std::is_reference_v</*InnerBase*/> &&
             ranges::bidirectional_range</*Base*/> &&
             ranges::bidirectional_range</*InnerBase*/> &&
             ranges::common_range</*InnerBase*/> &&
             ranges::bidirectional_range</*PatternBase*/> &&

             ranges::common_range</*PatternBase*/>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0001 */}
Increments or decrements the iterator.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0002 */}
1) Increments the [inner iterator](/docs/cpp/library/ranges/join_with_view/iterator) as if by `std::visit([](auto& it){ ++it; }, `[`inner_it_`](/docs/cpp/library/ranges/join_with_view/iterator#inner_it)`);`.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0003 */}
After that, adjusts the inner and outer iterators as follows:

- If the incremented inner iterator is the past-the-end iterator of the pattern range, it is set to an iterator to the beginning of the next inner range.
- If the incremented inner iterator is the past-the-end iterator of an inner range, the outer iterator is incremented. Then:

  - If the incremented outer iterator is not the past-the-end iterator of the outer range, the inner iterator is set to an iterator to the beginning of the pattern range.
  - Otherwise, if `std::is_reference_v<`[`InnerBase`](/docs/cpp/library/ranges/join_with_view/iterator#InnerBase)`>` is `true`, the inner iterator is set to a pattern iterator holding a [singular value](/docs/cpp/library/iterator#Dereferenceability_and_validity).

- Repeats the operations above until either the inner iterator is not a past-the-end iterator, or the outer iterator is a past-the-end iterator.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0004 */}
2) Equivalent to `++*this;`.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0005 */}
3) Equivalent to `auto tmp = *this; ++*this; return tmp;`.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0006 */}
4) If the outer iterator is the past-the-end iterator of the outer range, decrements it and sets the inner iterator to the past-the-end iterator of the last inner range, otherwise do nothing.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0007 */}
After that, adjusts the inner and outer iterators as follows:

- If the inner iterator refers to the beginning of an inner range, it is set to the past-the-end iterator of the pattern range.
- If the inner iterator refers to the beginning of the pattern range, the outer iterator is decremented, and the inner iterator is set to the past-the-end iterator of the previous inner range.
- Repeats the operations above until the inner iterator does not refer to the beginning of any range.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0008 */}
Finally, decrements the inner iterator as if by `std::visit([](auto& it){ --it; }, `[`inner_it_`](/docs/cpp/library/ranges/join_with_view/iterator#inner_it)`);`.

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0009 */}
5) Equivalent to `auto tmp = *this; --*this; return tmp;`.

### Return value

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0010 */}
1,4) `*this`

{/* source:cpp-ranges-join-with-view-iterator-operator-arith:0011 */}
3,5) A copy of `*this` that was made before the change.
