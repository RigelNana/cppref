---
title: "std::ranges::lazy_split_view<V, Pattern>::inner_iterator"
description: "The return type of lazy_split_view::outer_iterator::value_type::begin()."
source_url: "https://en.cppreference.com/w/cpp/ranges/lazy_split_view/inner_iterator"
language: "C++"
---

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< bool Const >
struct /*inner_iterator*/;`} />
  (exposition only*)
</DeclarationDoc>

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0001 */}
The return type of [`lazy_split_view::`](/docs/cpp/library/ranges/lazy_split_view/value_type)[`outer_iterator`](/docs/cpp/library/ranges/lazy_split_view/outer_iterator)`::value_type::begin()`.

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0002 */}
`Const` matches the template argument of [`outer_iterator`](/docs/cpp/library/ranges/lazy_split_view/outer_iterator).

### Member types

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>*`Base`*</DescriptionTerm>
    <DescriptionBody>[`maybe-const`](/docs/cpp/library/ranges#maybe-const)`<Const, V>` (exposition-only member type*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`iterator_concept`</DescriptionTerm>
    <DescriptionBody>

    - [`outer_iterator`](/docs/cpp/library/ranges/lazy_split_view/outer_iterator)`<Const>::iterator_concept`, that is [`std::forward_iterator_tag`](/docs/cpp/library/iterator/iterator_tags), if `Base` models [`forward_range`](/docs/cpp/library/ranges/forward_range).
    - [`std::input_iterator_tag`](/docs/cpp/library/iterator/iterator_tags), otherwise.

</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`iterator_category` (conditionally present)</DescriptionTerm>
    <DescriptionBody>

    Present only if `Base` models [`forward_range`](/docs/cpp/library/ranges/forward_range).

    - [`std::forward_iterator_tag`](/docs/cpp/library/iterator/iterator_tags) if `std::iterator_traits<ranges::iterator_t<Base>>::iterator_category` models `std::derived_from<std::forward_iterator_tag>`.
    - `std::iterator_traits<ranges::iterator_t<Base>>::iterator_category` otherwise.

</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`ranges::range_value_t<Base>`</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`difference_type`</DescriptionTerm>
    <DescriptionBody>`ranges::range_difference_t<Base>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Data members

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`outer_iterator`](/docs/cpp/library/ranges/lazy_split_view/outer_iterator)`<Const>` *`i_`* (private)</DescriptionTerm>
    <DescriptionBody>an iterator into the underlying [`view`](/docs/cpp/library/ranges/view) of the parent object [`lazy_split_view`](/docs/cpp/library/ranges/lazy_split_view/lazy_split_view) (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`bool` *`incremented_`* (private)</DescriptionTerm>
    <DescriptionBody>a flag that indicates whether the `operator++` was invoked on this object at least once (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`(constructor)` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>constructs an iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`base` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the underlying iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator*` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the current element (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator++` `operator++(int)` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>advances the iterator (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0006 */}
## std::ranges::lazy_split_view::inner_iterator::inner_iterator

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`/*inner_iterator*/() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr explicit /*inner_iterator*/( /*outer_iterator*/<Const> i );`} />

  <DeclarationDescription>
    1. 1) Value initializes data member [`i_`](/docs/cpp/library/ranges/lazy_split_view/inner_iterator#i) via its default member initializer (`= /*outer_iterator*/<Const>()`).
    2. 2) Initializes [`i_`](/docs/cpp/library/ranges/lazy_split_view/inner_iterator#i) with `std::move(i)`. The data member [`incremented_`](/docs/cpp/library/ranges/lazy_split_view/inner_iterator#incremented) is initialized with its default member initializer to `false`.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0007 */}
## std::ranges::lazy_split_view::inner_iterator::base

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr const ranges::iterator_t<Base>& base() const & noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr ranges::iterator_t<Base> base() &&
    requires ranges::forward_range<V>;`} />

  Returns a copy of the underlying iterator.

  <DeclarationDescription>
    1. 1) Copy constructs the result from the underlying iterator. Equivalent to `return i_./*cur*/();`.
    2. 2) Move constructs the result from the underlying iterator. Equivalent to `return std::move(i_./*cur*/());`.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0008 */}
## std::ranges::lazy_split_view::inner_iterator::operator*

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr decltype(auto) operator*() const;`} />

  Returns the element the underlying iterator points to.

  Equivalent to `return *i_./*cur*/();`.
</DeclarationDoc>

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0009 */}
## std::ranges::lazy_split_view::inner_iterator::operator++

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr /*inner_iterator*/& operator++();`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr decltype(auto) operator++(int);`} />
</DeclarationDoc>

1. 1) The function body is equivalent to:

```cpp
incremented_ = true;
if constexpr (!ranges::forward_range<Base>)
{
    if constexpr (Pattern::size() == 0)
        return *this;
}
++i_./*cur*/();

return *this;
```

2. 2) Equivalent to:

```cpp
if constexpr (ranges::forward_range<Base>)
{
    auto tmp = *this;
    ++*this;
    return tmp;
}
else
    ++*this; // no return statement
```

### Non-member functions

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator==` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>compares the iterators or the iterator and [`std::default_sentinel`](/docs/cpp/library/iterator/default_sentinel) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`iter_move` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>casts the result of dereferencing the underlying iterator to its associated rvalue reference type (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`iter_swap` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>swaps the objects pointed to by two underlying iterators (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0011 */}
## operator==(std::ranges::split_view::inner_iterator)

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`friend constexpr bool operator==( const /*inner_iterator*/& x,
                                  const /*inner_iterator*/& y )

      requires forward_range<Base>;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`friend constexpr bool operator==( const /*inner_iterator*/& x,
                                  std::default_sentinel_t );`} />
</DeclarationDoc>

1. 1) Equivalent to `return x.i_./*cur*/() == y.i_./*cur*/();`.
2. 2) The function body is equivalent to:

```cpp
auto [pcur, pend] = ranges::subrange{x.i_.parent_->pattern_};
auto end = ranges::end(x.i_.parent_->base_);
if constexpr (/*tiny_range*/<Pattern>)
{
    const auto& cur = x.i_./*cur*/();
    if (cur == end)
        return true;
    if (pcur == pend)
        return x.incremented_;
    return *cur == *pcur;
}
else
{
    auto cur = x.i_./*cur*/();
    if (cur == end)
        return true;
    if (pcur == pend)
        return x.incremented_;
    do
    {
        if (*cur != *pcur)
            return false;
        if (++pcur == pend)
            return true;
    }
    while (++cur != end);
    return false;
}
```

The `!=` operator is [synthesized](/docs/cpp/language/expressions/default_comparisons#Other_defaulted_comparison_operators) from `operator==`.

These functions are not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `std::ranges::split_view::inner_iterator` is an associated class of the arguments.

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0012 */}
## iter_move(std::ranges::split_view::inner_iterator)

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`friend constexpr decltype(auto) iter_move( const /*inner_iterator*/& i )
    noexcept(noexcept(ranges::iter_move(i.i_./*cur*/())));`} />
</DeclarationDoc>

Equivalent to `return ranges::iter_move(i.i_./*cur*/());`.

This function is not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `std::ranges::split_view::inner_iterator` is an associated class of the arguments.

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0013 */}
## iter_swap(std::ranges::split_view::inner_iterator)

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`friend constexpr void iter_swap( const /*inner_iterator*/& x,
                                 const /*inner_iterator*/& y )
    noexcept(noexcept(ranges::iter_swap(x.i_.current, y.i_.current)))

    requires std::indirectly_swappable<ranges::iterator_t<Base>>;`} />
</DeclarationDoc>

Equivalent to `ranges::iter_swap(x.i_./*cur*/(), y.i_./*cur*/())`.

This function is not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `std::ranges::split_view::inner_iterator` is an associated class of the arguments.

### Defect reports

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0014 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-ranges-lazy-split-view-inner-iterator:0015 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3591} standard="C++20">
    <PublishedBehavior>the `&&` overload of `base` might invalidate outer iterators</PublishedBehavior>
    <CorrectedBehavior>constraints added</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3593} standard="C++20">
    <PublishedBehavior>the `const&` overload of `base` returns a reference but might not be noexcept</PublishedBehavior>
    <CorrectedBehavior>made noexcept</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
