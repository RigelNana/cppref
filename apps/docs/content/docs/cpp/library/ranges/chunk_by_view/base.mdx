---
title: "std::ranges::chunk_by_view<V,Pred>::base"
description: "Returns a copy of the underlying view."
source_url: "https://en.cppreference.com/w/cpp/ranges/chunk_by_view/base"
language: "C++"
---
{/* source:cpp-ranges-chunk-by-view-base:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr V base() const& requires std::copy_constructible<V>;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`constexpr V base() &&;`} />
</DeclarationDoc>

{/* source:cpp-ranges-chunk-by-view-base:0001 */}
Returns a copy of the underlying view [`base_`](/docs/cpp/library/ranges/chunk_by_view#base).

{/* source:cpp-ranges-chunk-by-view-base:0002 */}
1. 1) Copy constructs the result from the underlying view. Equivalent to `return base_;`

{/* source:cpp-ranges-chunk-by-view-base:0003 */}
2. 2) Move constructs the result from the underlying view. Equivalent to `return std::`[`move`](/docs/cpp/library/utility/move)`(base_);`

### Parameters

{/* source:cpp-ranges-chunk-by-view-base:0004 */}
(none)

### Return value

{/* source:cpp-ranges-chunk-by-view-base:0005 */}
A copy of the underlying view.

### Example

{/* source:cpp-ranges-chunk-by-view-base:0006 */}
```cpp
#include <algorithm>
#include <functional>
#include <ranges>
 
int main()
{
    static constexpr auto v = {1, 1, 2, 2, 3, 3, 3};
    constexpr auto chunks = v | std::views::chunk_by(std::equal_to{});
    static_assert(std::ranges::equal(v, chunks.base()));
}
```
