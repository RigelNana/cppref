---
title: "std::ranges::enumerate_view<V>::size"
description: "Returns the number of elements."
source_url: "https://en.cppreference.com/w/cpp/ranges/enumerate_view/size"
language: "C++"
---

{/* source:cpp-ranges-enumerate-view-size:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr auto size() requires ranges::sized_range<V>;`} />
  (1)

  <Declaration id="2" language="cpp" since="C++23" code={`constexpr auto size() const requires ranges::sized_range<const V>;`} />
  (2)
</DeclarationDoc>

{/* source:cpp-ranges-enumerate-view-size:0001 */}
Returns the number of elements. Equivalent to `return ranges::size(base_);`, where [`base_`](/docs/cpp/library/ranges/enumerate_view#Data_members) is the underlying view.

### Parameters

{/* source:cpp-ranges-enumerate-view-size:0002 */}
(none)

### Return value

{/* source:cpp-ranges-enumerate-view-size:0003 */}
The number of elements.

### Example

{/* source:cpp-ranges-enumerate-view-size:0004 */}
```cpp
#include <cassert>
#include <forward_list>
#include <ranges>
#include <string_view>
 
int main()
{
    constexpr static auto v1 = {1, 2, 3, 4, 5};
    constexpr auto ev1{v1 | std::views::enumerate};
    static_assert(ev1.size() == 5);
    static_assert(std::ranges::sized_range<decltype(v1)>);
 
    auto v2 = std::forward_list{1, 2, 3, 4, 5};
    auto ev2 {v2 | std::views::enumerate};
    static_assert(not std::ranges::sized_range<decltype(v2)>);
    // auto size = ev2.size(); // Error: v2 is not a sized range
    assert(std::ranges::distance(v2) == 5); // OK, distance does not require sized
                                            // range, but has O(N) complexity here
}
```

### See also

{/* source:cpp-ranges-enumerate-view-size:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::size`](/docs/cpp/library/ranges/size) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns an integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::ssize`](/docs/cpp/library/ranges/ssize) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns a signed integer equal to the size of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
