---
title: "std::ranges::common_view<V>::end"
description: "Returns an iterator representing the end of the common_view."
source_url: "https://en.cppreference.com/w/cpp/ranges/common_view/end"
language: "C++"
---

{/* source:cpp-ranges-common-view-end:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr auto end() requires (!/*simple-view*/<V>);`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr auto end() const requires ranges::range<const V>;`} />
</DeclarationDoc>

{/* source:cpp-ranges-common-view-end:0001 */}
1) Returns an iterator representing the end of the `common_view`, that is: `ranges::begin(base_) + ranges::distance(base_)`, if both `ranges::random_access_range<V>` and `ranges::sized_range<V>` are satisfied, `std::common_iterator<ranges::iterator_t<V>, ranges::sentinel_t<V>>(ranges::end(base_))` otherwise. Here `base_` (the name is for exposition purposes only) is the underlying view.

{/* source:cpp-ranges-common-view-end:0002 */}
2) Same as (1), but `V` is const-qualified.

### Parameters

{/* source:cpp-ranges-common-view-end:0003 */}
(none)

### Return value

{/* source:cpp-ranges-common-view-end:0004 */}
An iterator representing the end of the underlying view.

### Example

{/* source:cpp-ranges-common-view-end:0005 */}
```cpp
#include <iostream>
#include <numeric>
#include <ranges>
 
int main()
{
    constexpr int n{4};
 
    constexpr auto v1 = std::views::iota(1)
                      | std::views::take(n)
                      | std::views::common
                      ;
    constexpr auto v2 = std::views::iota(2)
                      | std::views::take(n)
                      ;
    const int product = std::inner_product(v1.begin(), v1.end(),
                                           v2.begin(),
                                           0);
    std::cout << product << '\n';
}
```

Output:

```text
40
```

### Defect reports

{/* source:cpp-ranges-common-view-end:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-ranges-common-view-end:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={4012} standard="C++20">
    <PublishedBehavior>non-const overload missed simple-view check</PublishedBehavior>
    <CorrectedBehavior>added</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-ranges-common-view-end:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`begin`](/docs/cpp/library/ranges/common_view/begin)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::begin`](/docs/cpp/library/ranges/begin) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns an iterator to the beginning of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::end`](/docs/cpp/library/ranges/end) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns a sentinel indicating the end of a range (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
