---
title: "std::ranges::view_interface"
description: "std::ranges::view_interface is a helper class template for defining a view interface."
source_url: "https://en.cppreference.com/w/cpp/ranges/view_interface"
language: "C++"
---

{/* source:cpp-ranges-view-interface:0000 */}
<DeclarationDoc>
Defined in header [`<ranges>`](/docs/cpp/library/headers/ranges)

  <Declaration language="cpp" since="C++20" code={`template< class D >
    requires std::is_class_v<D> && std::same_as<D, std::remove_cv_t<D>>

class view_interface;`} />
</DeclarationDoc>

{/* source:cpp-ranges-view-interface:0001 */}
`std::ranges::view_interface` is a helper class template for defining a view interface.

{/* source:cpp-ranges-view-interface:0002 */}
`view_interface` is typically used with [CRTP](/docs/cpp/language/crtp):

{/* source:cpp-ranges-view-interface:0003 */}
```cpp
class my_view : public std::ranges::view_interface<my_view>
{
public:
    auto begin() const { /*...*/ }
    auto end() const { /*...*/ }
    // empty() is provided if begin() returns a forward iterator
    // and end() returns a sentinel for it.
};
```

### Member functions

{/* source:cpp-ranges-view-interface:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`empty`](/docs/cpp/library/ranges/view_interface/empty)</DescriptionTerm>
    <DescriptionBody>returns whether the derived view is empty. Provided if it satisfies [`sized_range`](/docs/cpp/library/ranges/sized_range) or [`forward_range`](/docs/cpp/library/ranges/forward_range). (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cbegin`](/docs/cpp/library/ranges/view_interface/cbegin) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a constant iterator to the beginning of the range. (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cend`](/docs/cpp/library/ranges/view_interface/cend) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>returns a sentinel for the constant iterator of the range. (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator bool`](/docs/cpp/library/ranges/view_interface/operator_bool)</DescriptionTerm>
    <DescriptionBody>returns whether the derived view is not empty. Provided if [`ranges::empty`](/docs/cpp/library/ranges/empty) is applicable to it. (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`data`](/docs/cpp/library/ranges/view_interface/data)</DescriptionTerm>
    <DescriptionBody>gets the address of derived view's data. Provided if its iterator type satisfies [`contiguous_iterator`](/docs/cpp/library/iterator/contiguous_iterator). (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`size`](/docs/cpp/library/ranges/view_interface/size)</DescriptionTerm>
    <DescriptionBody>returns the number of elements in the derived view. Provided if it satisfies [`forward_range`](/docs/cpp/library/ranges/forward_range) and its sentinel and iterator type satisfy [`sized_sentinel_for`](/docs/cpp/library/iterator/sized_sentinel_for). (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`front`](/docs/cpp/library/ranges/view_interface/front)</DescriptionTerm>
    <DescriptionBody>returns the first element in the derived view. Provided if it satisfies [`forward_range`](/docs/cpp/library/ranges/forward_range). (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`back`](/docs/cpp/library/ranges/view_interface/back)</DescriptionTerm>
    <DescriptionBody>returns the last element in the derived view. Provided if it satisfies [`bidirectional_range`](/docs/cpp/library/ranges/bidirectional_range) and [`common_range`](/docs/cpp/library/ranges/common_range). (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator[]`](/docs/cpp/library/ranges/view_interface/operator_at)</DescriptionTerm>
    <DescriptionBody>returns the `n`th element in the derived view. Provided if it satisfies [`random_access_range`](/docs/cpp/library/ranges/random_access_range). (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Example

{/* source:cpp-ranges-view-interface:0005 */}
```cpp
#include <iostream>
#include <ranges>
#include <vector>
 
template<class T, class A>
class VectorView : public std::ranges::view_interface<VectorView<T, A>>
{
public:
    VectorView() = default;
 
    VectorView(const std::vector<T, A>& vec) :
        m_begin(vec.cbegin()), m_end(vec.cend())
    {}
 
    auto begin() const { return m_begin; }
 
    auto end() const { return m_end; }
 
private:
    typename std::vector<T, A>::const_iterator m_begin{}, m_end{};
};
 
int main()
{
    std::vector<int> v = {1, 4, 9, 16};
 
    VectorView view_over_v{v};
 
    // We can iterate with begin() and end().
    for (int n : view_over_v)
        std::cout << n << ' ';
    std::cout << '\n';
 
    // We get operator[] for free when inheriting from view_interface
    // since we satisfy the random_access_range concept.
    for (std::ptrdiff_t i = 0; i != view_over_v.size(); ++i)
        std::cout << "v[" << i << "] = " << view_over_v[i] << '\n';
}
```

Output:

```text
1 4 9 16
v[0] = 1
v[1] = 4
v[2] = 9
v[3] = 16
```

### Defect reports

{/* source:cpp-ranges-view-interface:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-ranges-view-interface:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3549} standard="C++20">
    <PublishedBehavior>`view_interface` was required to be derived from `view_base`, which sometimes required multiple `view_base` subobjects in a view</PublishedBehavior>
    <CorrectedBehavior>inheritance removed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-ranges-view-interface:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::subrange`](/docs/cpp/library/ranges/subrange) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>combines an iterator-sentinel pair into a [`view`](/docs/cpp/library/ranges/view) (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
