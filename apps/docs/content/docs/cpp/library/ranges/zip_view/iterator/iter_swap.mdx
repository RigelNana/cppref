---
title: "iter_swap(ranges::zip_view::iterator)"
description: "Swaps underlying pointed-to elements."
source_url: "https://en.cppreference.com/w/cpp/ranges/zip_view/iterator/iter_swap"
language: "C++"
---

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`friend constexpr void iter_swap( const /*iterator*/& x, const /*iterator*/& y )
    noexcept(/* see below */)
        requires (std::indirectly_swappable<ranges::iterator_t<

            /*maybe-const*/<Const, Views>>> && ...);`} />
</DeclarationDoc>

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0001 */}
Performs `ranges::iter_swap(std::get<i>(x.current_), std::get<i>(y.current_))` for every integer `i` in `[0, sizeof...(Views))`, where `current_` denotes the underlying tuple-like object that holds iterators to elements of adapted views.

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0002 */}
This function is not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `zip_view::iterator<Const>` is an associated class of the arguments.

### Parameters

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0003 */}
<ParameterList>
  <Parameter name="x, y">iterators to the elements to swap</Parameter>
</ParameterList>

### Return value

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0004 */}
(none)

### Exceptions

{/* source:cpp-ranges-zip-view-iterator-iter-swap:0005 */}
[`noexcept`](/docs/cpp/language/exceptions/noexcept_spec) specification:

```cpp
noexcept(
  (noexcept(ranges::iter_swap(
    declval<const ranges::iterator_t</*maybe-const*/<Const, Views>>&>(),

    declval<const ranges::iterator_t</*maybe-const*/<Const, Views>>&>())) &&...))
```
