---
title: "std::strncmp"
description: "Compares at most count characters of two possibly null-terminated arrays. The comparison is done lexicographically. Characters following the null character are not compared."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strncmp"
language: "C++"
---

{/* source:cpp-string-byte-strncmp:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`int strncmp( const char* lhs, const char* rhs, std::size_t count );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strncmp:0001 */}
Compares at most `count` characters of two possibly null-terminated arrays. The comparison is done lexicographically. Characters following the null character are not compared.

{/* source:cpp-string-byte-strncmp:0002 */}
The sign of the result is the sign of the difference between the values of the first pair of characters (both interpreted as `unsigned char`) that differ in the arrays being compared.

{/* source:cpp-string-byte-strncmp:0003 */}
The behavior is undefined when access occurs past the end of either array `lhs` or `rhs`. The behavior is undefined when either `lhs` or `rhs` is the null pointer.

### Parameters

{/* source:cpp-string-byte-strncmp:0004 */}
<ParameterList>
  <Parameter name="lhs, rhs">pointers to the possibly null-terminated arrays to compare</Parameter>
  <Parameter name="count">maximum number of characters to compare</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strncmp:0005 */}
Negative value if `lhs` appears before `rhs` in lexicographical order.

{/* source:cpp-string-byte-strncmp:0006 */}
Zero if `lhs` and `rhs` compare equal, or if `count` is zero.

{/* source:cpp-string-byte-strncmp:0007 */}
Positive value if `lhs` appears after `rhs` in lexicographical order.

### Notes

{/* source:cpp-string-byte-strncmp:0008 */}
This function is not locale-sensitive, unlike [`std::strcoll`](/docs/cpp/library/text/byte/strcoll) and [`std::strxfrm`](/docs/cpp/library/text/byte/strxfrm).

### Example

{/* source:cpp-string-byte-strncmp:0009 */}
```cpp
#include <cstring>
#include <iostream>
 
void demo(const char* lhs, const char* rhs, int sz)
{
    const int rc = std::strncmp(lhs, rhs, sz);
    if (rc < 0)
        std::cout << "First " << sz << " chars of ["
                  << lhs << "] precede [" << rhs << "]\n";
    else if (rc > 0)
        std::cout << "First " << sz << " chars of ["
                  << lhs << "] follow [" << rhs << "]\n";
    else
        std::cout << "First " << sz << " chars of ["
                  << lhs << "] equal [" << rhs << "]\n";
}
 
int main()
{
    demo("Hello, world!", "Hello, everybody!", 13);
    demo("Hello, everybody!", "Hello, world!", 13);
    demo("Hello, everybody!", "Hello, world!", 7);
    demo("Hello, everybody!" + 12, "Hello, somebody!" + 11, 5);
}
```
Output:
```text
First 13 chars of [Hello, world!] follow [Hello, everybody!]
First 13 chars of [Hello, everybody!] precede [Hello, world!]
First 7 chars of [Hello, everybody!] equal [Hello, world!]
First 5 chars of [body!] equal [body!]
```

### See also

{/* source:cpp-string-byte-strncmp:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`strcmp`](/docs/cpp/library/text/byte/strcmp)</DescriptionTerm>
    <DescriptionBody>compares two strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcsncmp`](/docs/cpp/library/text/wide/wcsncmp)</DescriptionTerm>
    <DescriptionBody>compares a certain amount of characters from two wide strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`memcmp`](/docs/cpp/library/text/byte/memcmp)</DescriptionTerm>
    <DescriptionBody>compares two buffers <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strcoll`](/docs/cpp/library/text/byte/strcoll)</DescriptionTerm>
    <DescriptionBody>compares two strings in accordance to the current locale <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strncmp) for `strncmp`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
