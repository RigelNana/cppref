---
title: "std::strncpy"
description: "Copies at most count characters of the byte string pointed to by src (including the terminating null character) to character array pointed to by dest."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strncpy"
language: "C++"
---

{/* source:cpp-string-byte-strncpy:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`char* strncpy( char* dest, const char* src, std::size_t count );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strncpy:0001 */}
Copies at most `count` characters of the byte string pointed to by `src` (including the terminating null character) to character array pointed to by `dest`.

{/* source:cpp-string-byte-strncpy:0002 */}
If `count` is reached before the entire string `src` was copied, the resulting character array is not null-terminated.

{/* source:cpp-string-byte-strncpy:0003 */}
If, after copying the terminating null character from `src`, `count` is not reached, additional null characters are written to `dest` until the total of `count` characters have been written.

{/* source:cpp-string-byte-strncpy:0004 */}
If the strings overlap, the behavior is undefined.

### Parameters

{/* source:cpp-string-byte-strncpy:0005 */}
<ParameterList>
  <Parameter name="dest">pointer to the character array to copy to</Parameter>
  <Parameter name="src">pointer to the byte string to copy from</Parameter>
  <Parameter name="count">maximum number of characters to copy</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strncpy:0006 */}
`dest`

### Example

{/* source:cpp-string-byte-strncpy:0007 */}
```cpp
#include <cstring>
#include <iostream>
 
int main()
{
    const char* src = "hi";
    char dest[6] = {'a', 'b', 'c', 'd', 'e', 'f'};
    std::strncpy(dest, src, 5);
 
    std::cout << "The contents of dest are: ";
    for (char c : dest)
    {
        if (c)
            std::cout << c << ' ';
        else
            std::cout << "\\0" << ' ';
    }
    std::cout << '\n';
}
```
Output:
```text
The contents of dest are: h i \0 \0 \0 f
```

### See also

{/* source:cpp-string-byte-strncpy:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`strcpy`](/docs/cpp/library/text/byte/strcpy)</DescriptionTerm>
    <DescriptionBody>copies one string to another <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`memcpy`](/docs/cpp/library/text/byte/memcpy)</DescriptionTerm>
    <DescriptionBody>copies one buffer to another <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strncpy) for `strncpy`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
