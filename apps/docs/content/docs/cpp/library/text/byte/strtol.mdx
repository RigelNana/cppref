---
title: "std::strtol, std::strtoll"
description: "Interprets an integer value in a byte string pointed to by str."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strtol"
language: "C++"
---

{/* source:cpp-string-byte-strtol:0000 */}
<DeclarationDoc>
Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)
  <Declaration id="1" language="cpp" code={`long      strtol( const char* str, char** str_end, int base );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`long long strtoll( const char* str, char** str_end, int base );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strtol:0001 */}
Interprets an integer value in a byte string pointed to by `str`.

{/* source:cpp-string-byte-strtol:0002 */}
Discards any whitespace characters (as identified by calling [`std::isspace`](/docs/cpp/library/text/byte/isspace)) until the first non-whitespace character is found, then takes as many characters as possible to form a valid *base-n* (where n=`base`)  integer number representation and converts them to an  integer value. The valid   integer value consists of the following parts:

{/* source:cpp-string-byte-strtol:0003 */}
- (optional) plus or minus sign
- (optional) prefix (`0`) indicating octal base (applies only when the base is `8` or `0`)
- (optional) prefix (`0x` or `0X`) indicating hexadecimal base (applies only when the base is `16` or `0`)
- a sequence of digits

{/* source:cpp-string-byte-strtol:0004 */}
The set of valid values for base is `{0, 2, 3, ..., 36}`. The set of valid digits for base-`2` integers is `{0, 1}`, for base-`3` integers is `{0, 1, 2}`, and so on. For bases larger than `10`, valid digits include alphabetic characters, starting from `Aa` for base-`11` integer, to `Zz` for base-`36` integer. The case of the characters is ignored.

{/* source:cpp-string-byte-strtol:0005 */}
Additional numeric formats may be accepted by the currently installed C [locale](/docs/cpp/library/text/locale/setlocale).

{/* source:cpp-string-byte-strtol:0006 */}
If the value of `base` is `0`, the numeric base is auto-detected: if the prefix is `0`, the base is octal, if the prefix is `0x` or `0X`, the base is hexadecimal, otherwise the base is decimal.

{/* source:cpp-string-byte-strtol:0007 */}
If the minus sign was part of the input sequence, the numeric value calculated from the sequence of digits is negated as if by [unary minus](/docs/cpp/language/expressions/operator_arithmetic#Unary_arithmetic_operators) in the result type.

{/* source:cpp-string-byte-strtol:0008 */}
The function sets the pointer pointed to by `str_end` to point to the character past the last character interpreted. If `str_end` is a null pointer, it is ignored.

{/* source:cpp-string-byte-strtol:0009 */}
If the `str` is empty or does not have the expected form, no conversion is performed, and (if `str_end` is not a null pointer) the value of `str` is stored in the object pointed to by `str_end`.

### Parameters

{/* source:cpp-string-byte-strtol:0010 */}
<ParameterList>
  <Parameter name="str">pointer to the null-terminated byte string to be interpreted</Parameter>
  <Parameter name="str_end">pointer to a pointer to character</Parameter>
  <Parameter name="base">*base* of the interpreted integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strtol:0011 */}
- If successful, an integer value corresponding to the contents of `str` is returned.
- If the converted value falls out of range of corresponding return type, a range error occurs (setting [`errno`](/docs/cpp/library/error/errno) to [`ERANGE`](/docs/cpp/library/error/errno_macros)) and [`LONG_MAX`](/docs/cpp/library/utility/types/climits), [`LONG_MIN`](/docs/cpp/library/utility/types/climits), [`LLONG_MAX`](/docs/cpp/library/utility/types/climits) or [`LLONG_MIN`](/docs/cpp/library/utility/types/climits) is returned.
- If no conversion can be performed, `0` is returned.

### Example

{/* source:cpp-string-byte-strtol:0012 */}
```cpp
#include <cerrno>
#include <cstdlib>
#include <iomanip>
#include <iostream>
#include <string>
 
int main()
{
    const char* p = "10 200000000000000000000000000000 30 -40";
    std::cout << "Parsing " << std::quoted(p) << ":\n";
 
    for (;;)
    {
        // errno can be set to any non-zero value by a library function call
        // regardless of whether there was an error, so it needs to be cleared
        // in order to check the error set by strtol
        errno = 0;
        char* p_end{};
        const long i = std::strtol(p, &p_end, 10);
        if (p == p_end)
            break;
 
        const bool range_error = errno == ERANGE;
        const std::string extracted(p, p_end - p);
        p = p_end;
 
        std::cout << "Extracted " << std::quoted(extracted)
                  << ", strtol returned " << i << '.';
        if (range_error)
            std::cout << "\n  Range error occurred.";
 
        std::cout << '\n';
    }
}
```
Possible output:
```text
Parsing "10 200000000000000000000000000000 30 -40":
Extracted "10", strtol returned 10.
Extracted " 200000000000000000000000000000", strtol returned 9223372036854775807.
  Range error occurred.
Extracted " 30", strtol returned 30.
Extracted " -40", strtol returned -40.
```

### See also

{/* source:cpp-string-byte-strtol:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`stoi`, `stol`, `stoll`](/docs/cpp/library/string/basic_string/stol) <InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a string to a signed integer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtoul`, `strtoull`](/docs/cpp/library/text/byte/strtoul) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to an unsigned integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtoimax`, `strtoumax`](/docs/cpp/library/text/byte/strtoimax) <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to [`std::intmax_t`](/docs/cpp/library/utility/types/integer) or [`std::uintmax_t`](/docs/cpp/library/utility/types/integer) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcstol`, `wcstoll`](/docs/cpp/library/text/wide/wcstol)</DescriptionTerm>
    <DescriptionBody>converts a wide string to an integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtof`, `strtod`, `strtold`](/docs/cpp/library/text/byte/strtof)</DescriptionTerm>
    <DescriptionBody>converts a byte string to a floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`from_chars`](/docs/cpp/library/text/from_chars) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts a character sequence to an integer or floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atoi`, `atol`, `atoll`](/docs/cpp/library/text/byte/atoi) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to an integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strtol) for `strtol, strtoll`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
