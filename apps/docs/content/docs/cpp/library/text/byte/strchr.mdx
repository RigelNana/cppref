---
title: "std::strchr"
description: "Finds the first occurrence of the character static_cast < char > (ch) in the byte string pointed to by str."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strchr"
language: "C++"
---

{/* source:cpp-string-byte-strchr:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`const char* strchr( const char* str, int ch );`} />
  <Declaration language="cpp" code={`      char* strchr(       char* str, int ch );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strchr:0001 */}
Finds the first occurrence of the character `static_cast<char>(ch)` in the byte string pointed to by `str`.

{/* source:cpp-string-byte-strchr:0002 */}
The terminating null character is considered to be a part of the string and can be found if searching for `'\0'`.

### Parameters

{/* source:cpp-string-byte-strchr:0003 */}
<ParameterList>
  <Parameter name="str">pointer to the null-terminated byte string to be analyzed</Parameter>
  <Parameter name="ch">character to search for</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strchr:0004 */}
Pointer to the found character in `str`, or a null pointer if no such character is found.

### Example

{/* source:cpp-string-byte-strchr:0005 */}
```cpp
#include <cstring>
#include <iostream>
 
int main()
{
    const char* str = "Try not. Do, or do not. There is no try.";
    char target = 'T';
    const char* result = str;
 
    while ((result = std::strchr(result, target)) != nullptr)
    {
        std::cout << "Found '" << target
                  << "' starting at '" << result << "'\n";
 
        // Increment result, otherwise we'll find target at the same location
        ++result;
    }
}
```
Output:
```text
Found 'T' starting at 'Try not. Do, or do not. There is no try.'
Found 'T' starting at 'There is no try.'
```

### See also

{/* source:cpp-string-byte-strchr:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`memchr`](/docs/cpp/library/text/byte/memchr)</DescriptionTerm>
    <DescriptionBody>searches an array for the first occurrence of a character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`find`](/docs/cpp/library/string/basic_string/find)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of the given substring <br /> (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcschr`](/docs/cpp/library/text/wide/wcschr)</DescriptionTerm>
    <DescriptionBody>finds the first occurrence of a wide character in a wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strrchr`](/docs/cpp/library/text/byte/strrchr)</DescriptionTerm>
    <DescriptionBody>finds the last occurrence of a character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strpbrk`](/docs/cpp/library/text/byte/strpbrk)</DescriptionTerm>
    <DescriptionBody>finds the first location of any character from a set of separators <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strchr) for `strchr`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
