---
title: "std::strlen"
description: "Returns the length of the given byte string, that is, the number of characters in a character array whose first element is pointed to by str up to and not including the first null character. The behavior is undefined if there is no null character in the character array pointed to by str."
source_url: "https://en.cppreference.com/w/cpp/string/byte/strlen"
language: "C++"
---

{/* source:cpp-string-byte-strlen:0000 */}
<DeclarationDoc>
Defined in header [`<cstring>`](/docs/cpp/library/headers/cstring)
  <Declaration language="cpp" code={`std::size_t strlen( const char* str );`} />
</DeclarationDoc>

{/* source:cpp-string-byte-strlen:0001 */}
Returns the length of the given byte string, that is, the number of characters in a character array whose first element is pointed to by `str` up to and not including the first null character. The behavior is undefined if there is no null character in the character array pointed to by `str`.

### Parameters

{/* source:cpp-string-byte-strlen:0002 */}
<ParameterList>
  <Parameter name="str">pointer to the null-terminated byte string to be examined</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-byte-strlen:0003 */}
The length of the null-terminated string `str`.

### Possible implementation

{/* source:cpp-string-byte-strlen:0004 */}
```cpp
std::size_t strlen(const char* start)
{
    // NB: start is not checked for nullptr!
    const char* end = start;
    while (*end != '\0')
        ++end;
    return end - start;
}
```

### Example

{/* source:cpp-string-byte-strlen:0005 */}
```cpp
#include <cstring>
#include <iostream>
 
int main()
{
    const char str[] = "dog cat\0mouse";
 
    std::cout << "without null character: " << std::strlen(str) << '\n'
              << "with null character: " << sizeof str << '\n';
}
```
Output:
```text
without null character: 7
with null character: 14
```

### See also

{/* source:cpp-string-byte-strlen:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`wcslen`](/docs/cpp/library/text/wide/wcslen)</DescriptionTerm>
    <DescriptionBody>returns the length of a wide string <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`mblen`](/docs/cpp/library/text/multibyte/mblen)</DescriptionTerm>
    <DescriptionBody>returns the number of bytes in the next multibyte character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/byte/strlen) for `strlen`</DescriptionTerm>
    <DescriptionBody></DescriptionBody>
  </DescriptionItem>
</DescriptionList>
