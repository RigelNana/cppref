---
title: "std::ispunct(std::locale)"
description: "Checks if the given character is classified as a punctuation character by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/ispunct"
language: "C++"
---

{/* source:cpp-locale-ispunct:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`template< class CharT >
bool ispunct( CharT ch, const locale& loc );`} />
</DeclarationDoc>

{/* source:cpp-locale-ispunct:0001 */}
Checks if the given character is classified as a punctuation character by the given locale's [std::ctype](/docs/cpp/library/text/locale/ctype) facet.

### Parameters

{/* source:cpp-locale-ispunct:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-ispunct:0003 */}
Returns `true` if the character is classified as punctuation, `false` otherwise.

### Possible implementation

{/* source:cpp-locale-ispunct:0004 */}
```cpp
template<class CharT>
bool ispunct(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).is(std::ctype_base::punct, ch);
}
```

### Example

{/* source:cpp-locale-ispunct:0005 */}
Demonstrates the use of `std::ispunct()` with different locales (OS-specific).
```cpp
#include <iostream>
#include <locale>
 
int main()
{
    const wchar_t c = L'\u214b'; // upside-down ampersand
 
    std::locale loc1("C");
    std::cout << "ispunct('⅋', C locale) returned "
              << std::boolalpha << std::ispunct(c, loc1) << '\n';
 
    std::locale loc2("en_US.UTF-8");
    std::cout << "ispunct('⅋', Unicode locale) returned "
              << std::boolalpha << std::ispunct(c, loc2) << '\n';
}
```
Possible output:
```text
isalpha('⅋', C locale) returned false
isalpha('⅋', Unicode locale) returned true
```

### See also

{/* source:cpp-locale-ispunct:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ispunct`](/docs/cpp/library/text/byte/ispunct)</DescriptionTerm>
    <DescriptionBody>checks if a character is a punctuation character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iswpunct`](/docs/cpp/library/text/wide/iswpunct)</DescriptionTerm>
    <DescriptionBody>checks if a wide character is a punctuation character <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
