---
title: "std::moneypunct<CharT,International>::frac_digits, do_frac_digits"
description: "Public member function, calls the member function do_frac_digits of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/moneypunct/frac_digits"
language: "C++"
---

{/* source:cpp-locale-moneypunct-frac-digits:0000 */}

<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
int frac_digits() const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual int do_frac_digits() const;`} />
  <DeclarationDescription>
    {/* source:cpp-locale-moneypunct-frac-digits:0001 */}
    1. Public member function, calls the member function `do_frac_digits` of the most derived class.
    {/* source:cpp-locale-moneypunct-frac-digits:0002 */}
    2. Returns the number of digits to be displayed after the decimal point when printing monetary values.
  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-moneypunct-frac-digits:0003 */}

The number of digits to be displayed after the decimal point. In common U.S. locales, this is the value `2`.

### Example

{/* source:cpp-locale-moneypunct-frac-digits:0004 */}

```cpp
#include <iomanip>
#include <iostream>
#include <iterator>
#include <locale>
 
struct space_out : std::moneypunct<char>
{
    pattern do_pos_format() const { return {value, none, none, none}; }
    int do_frac_digits() const { return 0; }
    char_type do_thousands_sep() const { return ' '; }
    string_type do_grouping() const { return "\002"; }
};
 
int main()
{
    std::cout.imbue(std::locale("en_US.UTF-8"));
    std::cout << "american locale: " << std::showbase
              << std::put_money(12345678.0) << '\n';
 
    std::cout.imbue(std::locale(std::cout.getloc(), new space_out));
    std::cout << "locale with modified moneypunct: "
              << std::put_money(12345678.0) << '\n';
}
```

Output:

```text
american locale: $123,456.78
locale with modified moneypunct: 12 34 56 78
```

### See also

{/* source:cpp-locale-moneypunct-frac-digits:0005 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_thousands_sep`](/docs/cpp/library/text/locale/moneypunct/thousands_sep)[virtual]</DescriptionTerm>
    <DescriptionBody>provides the character to use as thousands separator <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_decimal_point`](/docs/cpp/library/text/locale/moneypunct/decimal_point)[virtual]</DescriptionTerm>
    <DescriptionBody>provides the character to use as decimal point <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
