---
title: "std::messages<CharT>::open, std::messages<CharT>::do_open"
description: "Public member function, calls the protected virtual member function do_open of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/messages/open"
language: "C++"
---

{/* source:cpp-locale-messages-open:0000 */}

<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
catalog open( const std::string& name, const std::locale& loc ) const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual catalog do_open( const std::string& name, const std::locale& loc ) const;`} />
  <DeclarationDescription>
    {/* source:cpp-locale-messages-open:0001 */}
    1. Public member function, calls the protected virtual member function `do_open` of the most derived class.
    {/* source:cpp-locale-messages-open:0002 */}
    2. Obtains a value of type `catalog` (inherited from [`std::messages_base`](/docs/cpp/library/text/locale/messages_base)), which can be passed to [`get()`](/docs/cpp/library/text/locale/messages/get) to retrieve messages from the message catalog named by `name`. This value is usable until passed to [`close()`](/docs/cpp/library/text/locale/messages/close).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-messages-open:0003 */}

<ParameterList>
  <Parameter name="name">name of the message catalog to open</Parameter>
  <Parameter name="loc">a locale object that provides additional facets that may be required to read messages from the catalog, such as [`std::codecvt`](/docs/cpp/library/text/locale/codecvt) to perform wide/multibyte conversions</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-messages-open:0004 */}

The non-negative value of type `catalog` that can be used with [`get()`](/docs/cpp/library/text/locale/messages/get) and [`close()`](/docs/cpp/library/text/locale/messages/close). Returns a negative value if the catalog could not be opened.

### Notes

{/* source:cpp-locale-messages-open:0005 */}

On POSIX systems, this function call usually translates to a call to [`catopen()`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/catopen.html). In GNU libstdc++, it calls [`textdomain`](https://gcc.gnu.org/onlinedocs/libstdc++/manual/facets.html).

{/* source:cpp-locale-messages-open:0006 */}

The actual catalog location is implementation-defined: for the catalog `"sed"` (message catalogs installed with the Unix utility `'sed'`) in German locale, for example, the file opened by this function call may be `/usr/lib/nls/msg/de_DE/sed.cat`, `/usr/lib/locale/de_DE/LC_MESSAGES/sed.cat`, or `/usr/share/locale/de/LC_MESSAGES/sed.mo`.

### Example

{/* source:cpp-locale-messages-open:0007 */}

The following example demonstrated retrieval of messages: on a typical GNU/Linux system it reads from `/usr/share/locale/de/LC_MESSAGES/sed.mo`.

```cpp
#include <iostream>
#include <locale>
 
int main()
{
    std::locale loc("de_DE.utf8");
    std::cout.imbue(loc);
    auto& facet = std::use_facet<std::messages<char>>(loc);
    auto cat = facet.open("sed", loc);
    if (cat < 0)
        std::cout << "Could not open german \"sed\" message catalog\n";
    else
        std::cout << "\"No match\" in German: "
                  << facet.get(cat, 0, 0, "No match") << '\n'
                  << "\"Memory exhausted\" in German: "
                  << facet.get(cat, 0, 0, "Memory exhausted") << '\n';
    facet.close(cat);
}
```

Possible output:

```text
"No match" in German: Keine Übereinstimmung
"Memory exhausted" in German: Speicher erschöpft
```

### See also

{/* source:cpp-locale-messages-open:0008 */}
