---
title: "std::ctype<char>::is"
description: "1) Checks if the character c is classified by the mask m according to the classification table returned by the member function table(). Effectively calculates table( ) [ ( unsigned char )c] & m."
source_url: "https://en.cppreference.com/w/cpp/locale/ctype_char/is"
language: "C++"
---

{/* source:cpp-locale-ctype-char-is:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" id="1" code={`bool is( mask m, char c ) const;`} />
  <Declaration language="cpp" id="2" code={`const char* is( const char* low, const char* high, mask* vec ) const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-ctype-char-is:0001 */}
    1) Checks if the character `c` is classified by the mask `m` according to the classification table returned by the member function `table()`. Effectively calculates `table()[(unsigned char)c] & m`.

    {/* source:cpp-locale-ctype-char-is:0002 */}
    2) For every character in the character array `[low, high)`, reads its full classification mask from the classification table returned by the member function `table()` (that is, evaluates `table()[(unsigned char)*p]` and stores it in the corresponding element of the array pointed to by `vec`.

  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-locale-ctype-char-is:0003 */}
If `(unsigned char)c >= std::ctype<char>::table_size`, then an implementation-defined value is substituted instead of `table()[(unsigned char)c]`, possibly different for different values of `c`.

### Parameters

{/* source:cpp-locale-ctype-char-is:0004 */}
<ParameterList>
  <Parameter name="c">character to classify</Parameter>
  <Parameter name="m">mask to use for classifying a single character</Parameter>
  <Parameter name="low">pointer to the first character in an array of characters to classify</Parameter>
  <Parameter name="high">one past the end pointer for the array of characters to classify</Parameter>
  <Parameter name="vec">pointer to the first element of the array of masks to fill</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-ctype-char-is:0005 */}
1) `true` if `c` is classified by `m` in `table()`, `false` otherwise.

{/* source:cpp-locale-ctype-char-is:0006 */}
2) `high`

### Notes

{/* source:cpp-locale-ctype-char-is:0007 */}
Unlike the primary template `std::ctype`, this specialization does not perform a virtual function call when classifying characters. To customize the behavior, a derived class may provide a non-default classification table to the base class constructor.

### Example

{/* source:cpp-locale-ctype-char-is:0008 */}
This section is incomplete Reason: no example

### Defect reports

{/* source:cpp-locale-ctype-char-is:0009 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-locale-ctype-char-is:0010 */}
<DefectReportList>
  <DefectReport kind="lwg" id={28} standard="C++98">
    <PublishedBehavior>overload (2) copied the values from `vec` into `table()`,<br />which is the reverse of the intended behavior</PublishedBehavior>
    <CorrectedBehavior>corrected</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-locale-ctype-char-is:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_is`](/docs/cpp/library/text/locale/ctype/is)[virtual]</DescriptionTerm>
    <DescriptionBody>classifies a character or a character sequence <br /> (virtual protected member function of `std::ctype<CharT>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

