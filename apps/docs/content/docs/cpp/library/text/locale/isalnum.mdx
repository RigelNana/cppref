---
title: "std::isalnum(std::locale)"
description: "Checks if the given character is classified as an alphanumeric character by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/isalnum"
language: "C++"
---

{/* source:cpp-locale-isalnum:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`template< class CharT >
bool isalnum( CharT ch, const locale& loc );`} />
</DeclarationDoc>

{/* source:cpp-locale-isalnum:0001 */}
Checks if the given character is classified as an alphanumeric character by the given locale's [std::ctype](/docs/cpp/library/text/locale/ctype) facet.

### Parameters

{/* source:cpp-locale-isalnum:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-isalnum:0003 */}
Returns `true` if the character is classified as alphanumeric, `false` otherwise.

### Possible implementation

{/* source:cpp-locale-isalnum:0004 */}
```cpp
template<class CharT>
bool isalnum(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).is(std::ctype_base::alnum, ch);
}
```

### Example

{/* source:cpp-locale-isalnum:0005 */}
Demonstrates the use of `isalnum()` with different locales (OS-specific).
```cpp
#include <iostream>
#include <locale>
 
int main()
{
    const wchar_t c = L'\u2135'; // mathematical symbol aleph
 
    std::locale loc1("C");
    std::cout << "isalnum('ℵ', C locale) returned "
              << std::boolalpha << std::isalnum(c, loc1) << '\n';
 
    std::locale loc2("en_US.UTF-8");
    std::cout << "isalnum('ℵ', Unicode locale) returned "
              << std::boolalpha << std::isalnum(c, loc2) << '\n';
}
```
Possible output:
```text
isalnum('ℵ', C locale) returned false
isalnum('ℵ', Unicode locale) returned true
```

### See also

{/* source:cpp-locale-isalnum:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`isalnum`](/docs/cpp/library/text/byte/isalnum)</DescriptionTerm>
    <DescriptionBody>checks if a character is alphanumeric <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iswalnum`](/docs/cpp/library/text/wide/iswalnum)</DescriptionTerm>
    <DescriptionBody>checks if a wide character is alphanumeric <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
