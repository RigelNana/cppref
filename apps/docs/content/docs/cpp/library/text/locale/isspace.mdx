---
title: "std::isspace(std::locale)"
description: "Checks if the given character is classified as a whitespace character by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/isspace"
language: "C++"
---

{/* source:cpp-locale-isspace:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`template< class CharT >
bool isspace( CharT ch, const locale& loc );`} />
</DeclarationDoc>

{/* source:cpp-locale-isspace:0001 */}
Checks if the given character is classified as a whitespace character by the given locale's [std::ctype](/docs/cpp/library/text/locale/ctype) facet.

### Parameters

{/* source:cpp-locale-isspace:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-isspace:0003 */}
Returns `true` if the character is classified as a whitespace character, `false` otherwise.

### Possible implementation

{/* source:cpp-locale-isspace:0004 */}
```cpp
template<class CharT>
bool isspace(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).is(std::ctype_base::space, ch);
}
```

### Example

{/* source:cpp-locale-isspace:0005 */}
Demonstrates the use of `std::isspace()` with different locales (OS-specific).
```cpp
#include <iostream>
#include <locale>
 
void try_with(wchar_t c, const char* loc)
{
    std::wcout << "isspace('" << c << "', locale(\"" << loc << "\")) returned "
               << std::boolalpha << std::isspace(c, std::locale(loc)) << '\n';
}
 
int main()
{
    const wchar_t EM_SPACE = L'\u2003'; // Unicode character 'EM SPACE'
    try_with(EM_SPACE, "C");
    try_with(EM_SPACE, "en_US.UTF8");
}
```
Possible output:
```text
isspace(' ', locale("C")) returned false
isspace(' ', locale("en_US.UTF8")) returned true
```

### See also

{/* source:cpp-locale-isspace:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`isspace`](/docs/cpp/library/text/byte/isspace)</DescriptionTerm>
    <DescriptionBody>checks if a character is a space character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iswspace`](/docs/cpp/library/text/wide/iswspace)</DescriptionTerm>
    <DescriptionBody>checks if a wide character is a space character <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
