---
title: "std::isblank(std::locale)"
description: "Checks if the given character is classified as a blank character by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/isblank"
language: "C++"
---

{/* source:cpp-locale-isblank:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" since="C++11" code={`template< class CharT >
bool isblank( CharT ch, const locale& loc );`} />
</DeclarationDoc>

{/* source:cpp-locale-isblank:0001 */}
Checks if the given character is classified as a blank character by the given locale's [std::ctype](/docs/cpp/library/text/locale/ctype) facet.

### Parameters

{/* source:cpp-locale-isblank:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-isblank:0003 */}
Returns `true` if the character is classified as a blank character, `false` otherwise.

### Possible implementation

{/* source:cpp-locale-isblank:0004 */}
```cpp
template<class CharT>
bool isblank(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).is(std::ctype_base::blank, ch);
}
```

### Example

{/* source:cpp-locale-isblank:0005 */}
Demonstrates the use of `isblank()` with different locales (OS-specific).
```cpp
#include <iostream>
#include <locale>
 
void try_with(wchar_t c, const char* loc)
{
    std::wcout << "isblank('" << c << "', locale(\"" << loc << "\")) returned "
               << std::boolalpha
               << std::isblank(c, std::locale(loc)) << '\n';
}
 
int main()
{
    const wchar_t IDEO_SPACE = L'\u3000'; // Unicode character 'IDEOGRAPHIC SPACE'
    try_with(IDEO_SPACE, "C");
    try_with(IDEO_SPACE, "en_US.UTF-8");
}
```
Possible output:
```text
isblank(' ', locale("C")) returned false
isblank(' ', locale("en_US.UTF-8")) returned true
```

### See also

{/* source:cpp-locale-isblank:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`isblank`](/docs/cpp/library/text/byte/isblank)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a character is a blank character <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iswblank`](/docs/cpp/library/text/wide/iswblank)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if a wide character is a blank character <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
