---
title: "std::locale::name"
description: "Returns the name of the locale, which is the name by which it is known to the operating system, such as \"POSIX\" or \"en_US.UTF8\" or \"English_United States.1252\". If the locale is not a copy of a system-supplied locale, the string \"*\" is returned."
source_url: "https://en.cppreference.com/w/cpp/locale/locale/name"
language: "C++"
---

{/* source:cpp-locale-locale-name:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`std::string name() const;`} />
</DeclarationDoc>

{/* source:cpp-locale-locale-name:0001 */}
Returns the name of the locale, which is the name by which it is known to the operating system, such as `"POSIX"` or `"en_US.UTF8"` or `"English_United States.1252"`. If the locale is not a copy of a system-supplied locale, the string `"*"` is returned.

### Return value

{/* source:cpp-locale-locale-name:0002 */}
The name of the locale or `"*"` if unnamed.

### Example

{/* source:cpp-locale-locale-name:0003 */}
```cpp
#include <iostream>
#include <locale>
#include <string>
 
int main()
{
    std::locale loc(std::locale(), new std::ctype<char>);
    std::cout << "The default locale is " << std::locale().name() << '\n'
              << "The user's locale is " << std::locale("").name() << '\n'
              << "A nameless locale is " << loc.name() << '\n';
}
```
Possible output:
```text
The default locale is C
The user's locale is en_US.UTF8
A nameless locale is *
```

### See also

{/* source:cpp-locale-locale-name:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/text/locale/locale/locale)</DescriptionTerm>
    <DescriptionBody>constructs a new locale <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
