---
title: "std::wbuffer_convert<Codecvt,Elem,Tr>::wbuffer_convert"
description: "2) If pcvt is a null pointer, the behavior is undefined."
source_url: "https://en.cppreference.com/w/cpp/locale/wbuffer_convert/wbuffer_convert"
language: "C++"
---

{/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`wbuffer_convert() : wbuffer_convert(nullptr) {}`} />
  <Declaration id="2" language="cpp" code={`explicit wbuffer_convert( std::streambuf* bytebuf,
                          Codecvt* pcvt = new Codecvt,
                          state_type state = state_type() );`} />
  <Declaration id="3" language="cpp" since="C++14" code={`wbuffer_convert( const std::wbuffer_convert& ) = delete;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0001 */}
    | Overload | [Data members](/docs/cpp/library/text/locale/wbuffer_convert#Data_members) |   |   |
| Overload | `bufptr` | `cvtptr` | `cvtstate` |
| --- | --- | --- | --- |
| (1) | `nullptr` | `new Codecvt` | `state_type()` |
| (2) | `bytebuf` | `pcvt` | `state` |

    {/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0002 */}
    2) If `pcvt` is a null pointer, the behavior is undefined.

    {/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0003 */}
    3) The copy constructor is deleted, `wbuffer_convert` is not [`CopyConstructible`](/docs/cpp/library/named_req/CopyConstructible).

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0004 */}
<ParameterList>
  <Parameter name="bytebuf">pointer to the underlying buffer</Parameter>
  <Parameter name="pcvt">pointer to a standalone (not managed by a locale) facet</Parameter>
  <Parameter name="state">the initial value of the character conversion state</Parameter>
</ParameterList>

### Example

{/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0005 */}
```cpp
#include <codecvt>
#include <iostream>
#include <locale>
#include <sstream>
 
int main()
{
    // Wrap a UTF-8 string stream in a UCS4 wbuffer_convert
    std::stringbuf utf8buf("z\u00df\u6c34\U0001f34c");
                       // or "\x7a\xc3\x9f\xe6\xb0\xb4\xf0\x9f\x8d\x8c"
                       // or u8"zß水🍌"
    std::wbuffer_convert<std::codecvt_utf8<wchar_t>> conv_in(&utf8buf);
    std::wistream ucsbuf(&conv_in);
    std::cout << "Reading from a UTF-8 stringbuf via wbuffer_convert: "
              << std::hex << std::showbase;
    for (wchar_t c; ucsbuf.get(c);)
        std::cout << static_cast<std::wint_t>(c) << ' ';
 
    // Wrap a UTF-8 aware std::cout in a UCS4 wbuffer_convert to output UCS4
    std::wbuffer_convert<std::codecvt_utf8<wchar_t>> conv_out(std::cout.rdbuf());
    std::wostream out(&conv_out);
    std::cout << "\nSending UCS4 data to std::cout via wbuffer_convert: ";
    out << L"z\u00df\u6c34\U0001f34c\n";
}
```
Output:
```text
Reading from a UTF-8 stringbuf via wbuffer_convert: 0x7a 0xdf 0x6c34 0x1f34c 
Sending UCS4 data to std::cout via wbuffer_convert: zß水🍌
```

### Defect reports

{/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-locale-wbuffer-convert-wbuffer-convert:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2175} standard="C++11">
    <PublishedBehavior>`pcvt` could be a null pointer</PublishedBehavior>
    <CorrectedBehavior>the behavior is undefined in this case</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2176} standard="C++11">
    <PublishedBehavior>constructors accepting single argument were implicit</PublishedBehavior>
    <CorrectedBehavior>made explicit</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P0935R0" standard="C++11">
    <PublishedBehavior>the default constructor was explicit</PublishedBehavior>
    <CorrectedBehavior>made implicit</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
