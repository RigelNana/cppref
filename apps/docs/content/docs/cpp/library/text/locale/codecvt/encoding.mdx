---
title: "std::codecvt<InternT,ExternT,StateT>::encoding, do_encoding"
description: "1) Public member function, calls the member function do_encoding of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/codecvt/encoding"
language: "C++"
---

{/* source:cpp-locale-codecvt-encoding:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" id="1" until="C++11" code={`public:
int encoding() const throw();`} />
  <Declaration language="cpp" id="1" since="C++11" code={`public:
int encoding() const noexcept;`} />
  <Declaration language="cpp" id="2" until="C++11" code={`protected:
virtual int do_encoding() const throw();`} />
  <Declaration language="cpp" id="2" since="C++11" code={`protected:
virtual int do_encoding() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-codecvt-encoding:0001 */}
    1) Public member function, calls the member function `do_encoding` of the most derived class.

    {/* source:cpp-locale-codecvt-encoding:0002 */}
    2) If the encoding represented by this codecvt facet maps each internal character to the same, constant number of external characters, returns that number. If the encoding is variable-length (e.g. UTF-8 or UTF-16), returns `0`. If the encoding is state-dependent, returns `-1`.

  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-codecvt-encoding:0003 */}
The exact number of `externT` characters that correspond to one `internT` character, if constant. `0` if the number varies, `-1` if the encoding is state-dependent.

{/* source:cpp-locale-codecvt-encoding:0004 */}
The non-converting specialization `std::codecvt<char, char, std::mbstate_t>` returns `1`.

### Example

{/* source:cpp-locale-codecvt-encoding:0005 */}
```cpp
#include <iostream>
#include <locale>
 
int main()
{
    std::cout << "en_US.utf8 is a variable-length encoding, encoding() returns "
              << std::use_facet<std::codecvt<wchar_t, char, std::mbstate_t>>(
                     std::locale("en_US.utf8")
                 ).encoding() << '\n';
 
    std::cout << "zh_CN.gb18030 is also variable-length, encoding() == "
              << std::use_facet<std::codecvt<wchar_t, char, std::mbstate_t>>(
                     std::locale("zh_CN.gb18030")
                 ).encoding() << '\n';
 
    std::cout << "ru_RU.koi8r is a single-byte encoding, encoding() == "
              << std::use_facet<std::codecvt<wchar_t, char, std::mbstate_t>>(
                     std::locale("ru_RU.koi8r")
                 ).encoding() << '\n';
}
```
Output:
```text
en_US.utf8 is a variable-length encoding, encoding() returns 0
zh_CN.gb18030 is also variable-length, encoding() == 0
ru_RU.koi8r is a single-byte encoding, encoding() == 1
```

### See also

{/* source:cpp-locale-codecvt-encoding:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>MB_CUR_MAX</DescriptionTerm>
    <DescriptionBody>maximum number of bytes in a multibyte character in the current C locale<br />(macro variable)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_max_length`](/docs/cpp/library/text/locale/codecvt/max_length)[virtual]</DescriptionTerm>
    <DescriptionBody>returns the maximum number of `ExternT` characters that could be converted into a single `InternT` character <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

