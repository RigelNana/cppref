---
title: "std::tolower(std::locale)"
description: "Converts the character ch to lowercase if possible, using the conversion rules specified by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/tolower"
language: "C++"
---

{/* source:cpp-locale-tolower:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`template< class CharT >
CharT tolower( CharT ch, const locale& loc );`} />

  <DeclarationDescription>
    {/* source:cpp-locale-tolower:0001 */}
    Converts the character `ch` to lowercase if possible, using the conversion rules specified by the given locale's [`std::ctype`](/docs/cpp/library/text/locale/ctype) facet.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-tolower:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-tolower:0003 */}
Returns the lowercase form of `ch` if one is listed in the locale, otherwise return `ch` unchanged.

### Notes

{/* source:cpp-locale-tolower:0004 */}
Only 1:1 character mapping can be performed by this function, e.g. the Greek uppercase letter 'Σ' has two lowercase forms, depending on the position in a word: 'σ' and 'ς'. A call to `std::tolower` cannot be used to obtain the correct lowercase form in this case.

### Possible implementation

{/* source:cpp-locale-tolower:0005 */}
```cpp
template<class CharT>
CharT tolower(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).tolower(ch);
}
```

### Example

{/* source:cpp-locale-tolower:0006 */}
```cpp
#include <cwctype>
#include <iostream>
#include <locale>
 
int main()
{
    wchar_t c = L'\u0190'; // Latin capital open E ('Ɛ')
 
    std::cout << std::hex << std::showbase;
 
    std::cout << "in the default locale, tolower(" << (std::wint_t)c << ") = "
              << (std::wint_t)std::tolower(c, std::locale()) << '\n';
 
    std::cout << "in Unicode locale, tolower(" << (std::wint_t)c << ") = "
              << (std::wint_t)std::tolower(c, std::locale("en_US.utf8")) << '\n';
}
```
Possible output:
```text
in the default locale, tolower(0x190) = 0x190
in Unicode locale, tolower(0x190) = 0x25b
```

### See also

{/* source:cpp-locale-tolower:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`toupper(std::locale)`](/docs/cpp/library/text/locale/toupper)</DescriptionTerm>
    <DescriptionBody>converts a character to uppercase using the ctype facet of a locale <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tolower`](/docs/cpp/library/text/byte/tolower)</DescriptionTerm>
    <DescriptionBody>converts a character to lowercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`towlower`](/docs/cpp/library/text/wide/towlower)</DescriptionTerm>
    <DescriptionBody>converts a wide character to lowercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
