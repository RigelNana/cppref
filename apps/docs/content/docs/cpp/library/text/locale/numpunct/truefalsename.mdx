---
title: "std::numpunct<CharT>::truename, do_truename, falsename, do_falsename"
description: "1,2) Public member function, calls the member function do_truename and do_falsename of the most derived class respectively."
source_url: "https://en.cppreference.com/w/cpp/locale/numpunct/truefalsename"
language: "C++"
---

{/* source:cpp-locale-numpunct-truefalsename:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
string_type truename() const;`} />
  <Declaration id="2" language="cpp" code={`public:
string_type falsename() const;`} />
  <Declaration id="3" language="cpp" code={`protected:
virtual string_type do_truename() const;`} />
  <Declaration id="4" language="cpp" code={`protected:
virtual string_type do_falsename() const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-numpunct-truefalsename:0001 */}
    1,2) Public member function, calls the member function `do_truename` and `do_falsename` of the most derived class respectively.

    {/* source:cpp-locale-numpunct-truefalsename:0002 */}
    3) Returns the string to be used as the representation of the boolean value `true`.

    {/* source:cpp-locale-numpunct-truefalsename:0003 */}
    4) Returns the string to be used as the representation of the boolean value `false`.

  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-numpunct-truefalsename:0004 */}
1,3) The object of type `string_type` to use as the representation of `true`. The standard specializations of `std::numpunct` return `"true"` and `L"true"`.

{/* source:cpp-locale-numpunct-truefalsename:0005 */}
2,4) The object of type `string_type` to use as the representation of `false`. The standard specializations of `std::numpunct` return `"false"` and `L"false"`.

### Example

{/* source:cpp-locale-numpunct-truefalsename:0006 */}
```cpp
#include <iomanip>
#include <iostream>
#include <locale>
 
struct custom_tf : std::numpunct<char>
{
    std::string do_truename()  const { return {'t'}; }
    std::string do_falsename() const { return {'f'}; }
};
 
int main()
{
    std::cout << std::boolalpha;
 
    // default boolalpha output
    std::cout << "Default locale,\n"
                 "  boolalpha  true: " << true << "\n"
                 "  boolalpha false: " << false << "\n\n";
 
    // with custom_tf applied to locale
    std::cout.imbue(std::locale(std::cout.getloc(), new custom_tf));
    std::cout << "Locale with modified numpunct,\n"
                 "  boolalpha  true: " << true << "\n"
                 "  boolalpha false: " << false << '\n';
}
```
Output:
```text
Default locale,
  boolalpha  true: true
  boolalpha false: false
 
Locale with modified numpunct,
  boolalpha  true: t
  boolalpha false: f
```
