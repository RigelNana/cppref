---
title: "std::numpunct<CharT>::decimal_point, do_decimal_point"
description: "Public member function, calls the member function do_decimal_point of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/numpunct/decimal_point"
language: "C++"
---

{/* source:cpp-locale-numpunct-decimal-point:0000 */}

<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
char_type decimal_point() const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual char_type do_decimal_point() const;`} />
  <DeclarationDescription>
    {/* source:cpp-locale-numpunct-decimal-point:0001 */}
    1. Public member function, calls the member function `do_decimal_point` of the most derived class.
    {/* source:cpp-locale-numpunct-decimal-point:0002 */}
    2. Returns the character to be used as the decimal separator between integer and fractional parts.
  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-numpunct-decimal-point:0003 */}

The value of type `char_type` to use as the decimal separator. The standard specializations of `std::numpunct` return `'.'` and `L'.'`.

### Example

{/* source:cpp-locale-numpunct-decimal-point:0004 */}

```cpp
#include <iostream>
#include <locale>
 
struct slash : std::numpunct<char>
{
    char do_decimal_point() const { return '/'; }  // separate with slash
};
 
int main()
{
    std::cout.precision(10);
    std::cout << "default locale: " << 1234.5678 << '\n';
    std::cout.imbue(std::locale(std::cout.getloc(), new slash));
    std::cout << "locale with modified numpunct: " << 1234.5678 << '\n';
}
```

Output:

```text
default locale: 1234.5678
locale with modified numpunct: 1234/5678
```
