---
title: "std::localeconv"
description: "The localeconv function obtains a pointer to a static object of type std::lconv, which represents numeric and monetary formatting rules of the current C locale."
source_url: "https://en.cppreference.com/w/cpp/locale/localeconv"
language: "C++"
---

{/* source:cpp-locale-localeconv:0000 */}
<DeclarationDoc>
Defined in header [`<clocale>`](/docs/cpp/library/headers/clocale)
  <Declaration language="cpp" code={`std::lconv* localeconv();`} />
</DeclarationDoc>

{/* source:cpp-locale-localeconv:0001 */}
The `localeconv` function obtains a pointer to a static object of type [std::lconv](/docs/cpp/library/text/locale/lconv), which represents numeric and monetary formatting rules of the current C locale.

### Parameters

{/* source:cpp-locale-localeconv:0002 */}
(none)

### Return value

{/* source:cpp-locale-localeconv:0003 */}
Pointer to the current [std::lconv](/docs/cpp/library/text/locale/lconv) object.

### Notes

{/* source:cpp-locale-localeconv:0004 */}
Modifying the object references through the returned pointer is undefined behavior.

{/* source:cpp-locale-localeconv:0005 */}
`std::localeconv` modifies a static object, calling it from different threads without synchronization is undefined behavior.

### Example

{/* source:cpp-locale-localeconv:0006 */}
```cpp
#include <clocale>
#include <iostream>
 
int main()
{
    std::setlocale(LC_ALL, "ja_JP.UTF-8");
    std::lconv* lc = std::localeconv();
    std::cout << "Japanese currency symbol: " << lc->currency_symbol
              << '(' << lc->int_curr_symbol << ")\n";
}
```
Output:
```text
Japanese currency symbol: ￥(JPY )
```

### See also

{/* source:cpp-locale-localeconv:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`setlocale`](/docs/cpp/library/text/locale/setlocale)</DescriptionTerm>
    <DescriptionBody>gets and sets the current C locale <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`lconv`](/docs/cpp/library/text/locale/lconv)</DescriptionTerm>
    <DescriptionBody>formatting details, returned by std::localeconv <br /> (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/locale/localeconv) for localeconv
