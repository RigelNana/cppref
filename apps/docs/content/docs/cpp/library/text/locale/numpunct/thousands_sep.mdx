---
title: "std::numpunct<CharT>::thousands_sep, do_thousands_sep"
description: "1) Public member function, calls the member function do_thousands_sep of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/numpunct/thousands_sep"
language: "C++"
---

{/* source:cpp-locale-numpunct-thousands-sep:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
char_type thousands_sep() const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual char_type do_thousands_sep() const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-numpunct-thousands-sep:0001 */}
    1) Public member function, calls the member function `do_thousands_sep` of the most derived class.

    {/* source:cpp-locale-numpunct-thousands-sep:0002 */}
    2) Returns the character to be used as the separator between digit groups when parsing or formatting integers and integral parts of floating-point values.

  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-numpunct-thousands-sep:0003 */}
The object of type `char_type` to use as the thousands separator. The standard specializations of `std::numpunct` return `','` and `L','`.

### Example

{/* source:cpp-locale-numpunct-thousands-sep:0004 */}
```cpp
#include <iostream>
#include <locale>
 
struct space_out : std::numpunct<char>
{
    char do_thousands_sep()   const { return ' '; }  // separate with spaces
    std::string do_grouping() const { return "\1"; } // groups of 1 digit
};
 
int main()
{
    std::cout << "default locale: " << 12345678 << '\n';
    std::cout.imbue(std::locale(std::cout.getloc(), new space_out));
    std::cout << "locale with modified numpunct: " << 12345678 << '\n';
}
```
Output:
```text
default locale: 12345678
locale with modified numpunct: 1 2 3 4 5 6 7 8
```

### Defect reports

{/* source:cpp-locale-numpunct-thousands-sep:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-locale-numpunct-thousands-sep:0006 */}
<DefectReportList>
  <DefectReport kind="lwg" id={20} standard="C++98">
    <PublishedBehavior>the return type was `string_type`</PublishedBehavior>
    <CorrectedBehavior>changed to `char_type`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-locale-numpunct-thousands-sep:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_grouping`](/docs/cpp/library/text/locale/numpunct/grouping)[virtual]</DescriptionTerm>
    <DescriptionBody>provides the numbers of digits between each pair of thousands separators <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
