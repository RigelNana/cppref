---
title: "std::num_get<CharT,InputIt>::get, std::num_get<CharT,InputIt>::do_get"
description: "Public member function, calls the member function do_get of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/num_get/get"
language: "C++"
---

{/* source:cpp-locale-num-get-get:0000 */}

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`public:
iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, bool& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, long& v ) const;`} />
  <Declaration id="1" since="C++11" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, long long& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, unsigned short& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, unsigned int& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, unsigned long& v ) const;`} />
  <Declaration id="1" since="C++11" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, unsigned long long& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, float& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, double& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, long double& v ) const;`} />
  <Declaration id="1" language="cpp" code={`iter_type get( iter_type in, iter_type end, std::ios_base& str, 
               std::ios_base::iostate& err, void*& v ) const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, bool& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, long& v ) const;`} />
  <Declaration id="2" since="C++11" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, long long& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, unsigned short& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, unsigned int& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, unsigned long& v ) const;`} />
  <Declaration id="2" since="C++11" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err,
                          unsigned long long& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, float& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, double& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, long double& v ) const;`} />
  <Declaration id="2" language="cpp" code={`virtual iter_type do_get( iter_type in, iter_type end, std::ios_base& str, 
                          std::ios_base::iostate& err, void*& v ) const;`} />
  <DeclarationDescription>
    {/* source:cpp-locale-num-get-get:0001 */}
    1. Public member function, calls the member function `do_get` of the most derived class.
    {/* source:cpp-locale-num-get-get:0002 */}
    2. Reads characters from the input iterator `in` and generates the value of the type of `v`, taking into account I/O stream formatting flags from `str.flags()`, character classification rules from `std::use_facet<std::ctype<CharT>>(str.getloc())`, and numeric punctuation characters from `std::use_facet<std::numpunct<CharT>>(str.getloc())`. This function is called by all formatted input stream operators such as `std::cin >> n;`.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-locale-num-get-get:0003 */}

Conversion occurs in three stages:

#### Stage 1: conversion specifier selection

{/* source:cpp-locale-num-get-get:0004 */}

- I/O format flags are obtained, as if by

{/* source:cpp-locale-num-get-get:0005 */}

`fmtflags basefield = (str.flags() & std::ios_base::basefield);`

`fmtflags boolalpha = (str.flags() & std::ios_base::boolalpha);`

{/* source:cpp-locale-num-get-get:0006 */}

- If the type of `v` is an integer type, the first applicable choice of the following five is selected:

{/* source:cpp-locale-num-get-get:0007 */}

If `basefield == oct`, will use conversion specifier `%o`

If `basefield == hex`, will use conversion specifier `%X`

If `basefield == 0`, will use conversion specifier `%i`

If the type of `v` is signed, will use conversion specifier `%d`

If the type of `v` is unsigned, will use conversion specifier `%u`

{/* source:cpp-locale-num-get-get:0008 */}

- For integer types, length modifier is added to the conversion specification if necessary: `h` for `short` and `unsigned short`, `l` for `long` and `unsigned long`<InlineRevision since="C++11">, `ll` for `long long` and `unsigned long long`</InlineRevision>
- If the type of `v` is `float`, will use conversion specifier `%g`
- If the type of `v` is `double`, will use conversion specifier `%lg`
- If the type of `v` is `long double`, will use conversion specifier `%Lg`
- If the type of `v` is `void*`, will use conversion specifier `%p`
- If the type of `v` is `bool` and `boolalpha == 0`, proceeds as if the type of `v` is `long`, except for the value to be stored in `v` in stage 3.
- If the type of `v` is `bool` and `boolalpha != 0`, the following replaces stages 2 and 3:
  - Successive characters obtained from the input iterator `in` are matched against the character sequences obtained from `std::use_facet<std::numpunct<CharT>>(str.getloc()).falsename()` and `std::use_facet<std::numpunct<CharT>>(str.getloc()).truename()` only as necessary as to identify the unique match. The input iterator `in` is compared to `end` only when necessary to obtain a character.
  - If the target sequence is uniquely matched, `v` is set to the corresponding `bool` value. Otherwise `false` is stored in `v` and [`std::ios_base::failbit`](/docs/cpp/library/io/ios_base/iostate) is assigned to `err`. If unique match could not be found before the input ended (`in == end`), `err |= std::ios_base::eofbit` is executed.

#### Stage 2: character extraction

{/* source:cpp-locale-num-get-get:0009 */}

- If `in == end`, stage 2 is terminated immediately, no further characters are extracted.
- The next character is extracted from `in` as if by `char_type ct = *in;`:
  - If the character matches one of <InlineRevision until="C++11">`"0123456789abcdefxABCDEFX+-"`</InlineRevision><InlineRevision since="C++11">`"0123456789abcdefpxABCDEFPX+-"`</InlineRevision>, widened to the locale's char_type as if by `std::use_facet<std::ctype<CharT>>(str.getloc()).widen()`, it is converted to the corresponding `char`.
  - If the character matches the decimal point separator (`std::use_facet<std::numpunct<CharT>>(str.getloc()).decimal_point())`), it is replaced by `'.'`.
  - If the character matches the thousands separator (`std::use_facet<std::numpunct<CharT>>(str.getloc()).thousands_sep()`) and the thousands separation is in use (as determined by `std::use_facet<std::numpunct<CharT>>(str.getloc()).grouping().length() != 0`), then if the decimal point `'.'` has not yet been accumulated, the position of the character is remembered, but the character is otherwise ignored. If the decimal point has already been accumulated, the character is discarded and stage 2 terminates.
  - In any case, the check is made whether the `char` obtained from the previous steps is allowed in the input field that would be parsed by [`std::scanf`](/docs/cpp/library/io/c/scanf) given the conversion specifier selected in stage 1. If it is allowed, it is accumulated in a temporary buffer and stage 2 repeats. If it is not allowed, stage 2 terminates.

#### Stage 3: conversion and storage

{/* source:cpp-locale-num-get-get:0010 */}

- The sequence of `chars` accumulated in stage 2 is converted to a numeric value:

{/* source:cpp-locale-num-get-get:0011 */}

<Revision until="C++11">

The input is parsed according to the rules of [`std::scanf`](/docs/cpp/library/io/c/scanf).

</Revision>

<Revision since="C++11">

The input is parsed as if by

- [`std::strtoll`](/docs/cpp/library/text/byte/strtol) for signed integer `v`,
- [`std::strtoull`](/docs/cpp/library/text/byte/strtoul) for unsigned integer `v`,
- [`std::strtof`](/docs/cpp/library/text/byte/strtof) for `float` `v`,
- [`std::strtod`](/docs/cpp/library/text/byte/strtof) for `double` `v`, or
- [`std::strtold`](/docs/cpp/library/text/byte/strtof) for `long double` `v`.

</Revision>

{/* source:cpp-locale-num-get-get:0012 */}

- If the conversion function fails to convert the entire field, the value `0` is stored in `v`.
- If the type of `v` is a signed integer type and the conversion function results in a positive or negative value too large to fit in it, the most positive or negative representable value is stored in `v`, respectively.
- If the type of `v` is an unsigned integer type and the conversion function results in a value that does not fit in it, the most positive representable value is stored in `v`.
- In any case, if the conversion function fails [`std::ios_base::failbit`](/docs/cpp/library/io/ios_base/iostate) is assigned to `err`.
- Otherwise, the numeric result of the conversion is stored in `v`.
  - If the type of `v` is `bool` and boolalpha is not set, then if the value to be stored is `0`, `false` is stored, if the value to be stored is `1`, `true` is stored, for any other value [`std::ios_base::failbit`](/docs/cpp/library/io/ios_base/iostate) is assigned to `err` and `true` is stored.
- After this, digit grouping is checked. if the position of any of the thousands separators discarded in stage 2 does not match the grouping provided by `std::use_facet<std::numpunct<CharT>>(str.getloc()).grouping()`, [`std::ios_base::failbit`](/docs/cpp/library/io/ios_base/iostate) is assigned to `err`.
- If stage 2 was terminated by the test `in == end`, `err |= std::ios_base::eofbit` is executed to set the eof bit.

### Return value

{/* source:cpp-locale-num-get-get:0013 */}

`in`

### Notes

{/* source:cpp-locale-num-get-get:0014 */}

Before the resolutions of [LWG issue 23](https://cplusplus.github.io/LWG/issue23) and [LWG issue 696](https://cplusplus.github.io/LWG/issue696), `v` was left unchanged if an error occurs.

{/* source:cpp-locale-num-get-get:0015 */}

Before the resolution of [LWG issue 221](https://cplusplus.github.io/LWG/issue221), strings representing hexadecimal integers (e.g. `"0xA0"`) were rejected by `do_get(int)` even if they are valid input to [`strtol`](/docs/cpp/library/text/byte/strtol) because stage 2 filters out characters `'X'` and `'x'`.

{/* source:cpp-locale-num-get-get:0016 */}

Before the resolution of [LWG issue 1169](https://cplusplus.github.io/LWG/issue1169), converting a negative number string into an unsigned integer might produce zero (since the value represented by the string is smaller than what the target type can represent).

{/* source:cpp-locale-num-get-get:0017 */}

<Revision since="C++11">

Before the resolution of [LWG issue 2381](https://cplusplus.github.io/LWG/issue2381), strings representing hexadecimal floating-point numbers with exponents (e.g. `"0x1.23p-10"`) were rejected by `do_get(double)` even if they are valid input to [`strtod`](/docs/cpp/library/text/byte/strtof) because stage 2 filters out characters `'P'` and `'p'`.

The strings representing infinity or not-a-number (e.g. `"NaN"` and `"inf"`) are rejected by `do_get(double)` even if they are valid input to `strtod` because stage 2 filters out characters such as `'N'` or `'i'`.

</Revision>

### Example

{/* source:cpp-locale-num-get-get:0018 */}

An implementation of `operator>>` for a user-defined type.

```cpp
#include <iostream>
#include <iterator>
#include <locale>
 
struct base { long x; };
 
template<class CharT, class Traits>
std::basic_istream<CharT, Traits>&
    operator >>(std::basic_istream<CharT, Traits>& is, base& b)
{
    std::ios_base::iostate err = std::ios_base::goodbit;
 
    try // setting err could throw
    {
        typename std::basic_istream<CharT, Traits>::sentry s(is);
 
        if (s) // if stream is ready for input
            std::use_facet<std::num_get<CharT>>(is.getloc()).get(is, {}, is, err, b.x);
    }
    catch (std::ios_base::failure& error)
    {
        // handle the exception
    }
 
    return is;
}
 
int main()
{
    base b;
    std::cin >> b;
}
```

### Defect reports

{/* source:cpp-locale-num-get-get:0019 */}

The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-locale-num-get-get:0020 */}

<DefectReportList>
  <DefectReport kind="lwg" id={17} standard="C++98">
    <PublishedBehavior>the process of parsing text boolean values was errornous</PublishedBehavior>
    <CorrectedBehavior>corrected</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={18} standard="C++98">
    <PublishedBehavior>the overload of `get` taking `bool&` value was missing</PublishedBehavior>
    <CorrectedBehavior>added</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={23} standard="C++98">
    <PublishedBehavior>overflowing input resulted in undefined behavior</PublishedBehavior>
    <CorrectedBehavior>overflow handled</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={154} standard="C++98">
    <PublishedBehavior>the conversion specifier for `double` was `%g` (same as `float`)</PublishedBehavior>
    <CorrectedBehavior>changed to `%lg`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={221} standard="C++98">
    <PublishedBehavior>`do_get` did not parse `'x'` and `'X'` while [`strtol`](/docs/cpp/library/text/byte/strtol) parsed them</PublishedBehavior>
    <CorrectedBehavior>made `'x'` and `'X'` parsed</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={275} standard="C++98">
    <PublishedBehavior>`get` had an overload taking `short&` value instead of `float&`</PublishedBehavior>
    <CorrectedBehavior>corrected</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={358} standard="C++98">
    <PublishedBehavior>thousand separators after the decimal point were ignored</PublishedBehavior>
    <CorrectedBehavior>stage 2 is terminated if encountered</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={696} standard="C++98">
    <PublishedBehavior>the result was unchanged on conversion failure</PublishedBehavior>
    <CorrectedBehavior>set to zero</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={1169} standard="C++98">
    <PublishedBehavior>overflow handling was inconsistent between floating-point types</PublishedBehavior>
    <CorrectedBehavior>made consistent<br />with `strtof`/`strtod`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2381} standard="C++11">
    <PublishedBehavior>`do_get` did not parse `'p'` and `'P'` while [`strtod`](/docs/cpp/library/text/byte/strtof) parsed them</PublishedBehavior>
    <CorrectedBehavior>made `'p'` and `'P'` parsed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-locale-num-get-get:0021 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator>>`](/docs/cpp/library/io/basic_istream/operator_gtgt)</DescriptionTerm>
    <DescriptionBody>extracts formatted data <br /> (public member function of `std::basic_istream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
