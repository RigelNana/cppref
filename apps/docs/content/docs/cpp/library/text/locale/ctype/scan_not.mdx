---
title: "std::ctype<CharT>::scan_not, std::ctype<CharT>::do_scan_not"
description: "1) Public member function, calls the protected virtual member function do_scan_not of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/ctype/scan_not"
language: "C++"
---

{/* source:cpp-locale-ctype-scan-not:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" id="1" code={`public:
const CharT* scan_not( mask m, const CharT* beg, const CharT* end ) const;`} />
  <Declaration language="cpp" id="2" code={`protected:
virtual const CharT* do_scan_not( mask m, const CharT* beg, const CharT* end ) const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-ctype-scan-not:0001 */}
    1) Public member function, calls the protected virtual member function `do_scan_not` of the most derived class.

    {/* source:cpp-locale-ctype-scan-not:0002 */}
    2) Locates the first character in the character array `[beg, end)` that does not satisfy the classification mask `m`, that is, the first character `c` such that `is(m, c)` would return `false`.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-ctype-scan-not:0003 */}
<ParameterList>
  <Parameter name="m">mask to search for</Parameter>
  <Parameter name="beg">pointer to the first character in an array of characters to search</Parameter>
  <Parameter name="end">one past the end pointer for the array of characters to search</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-ctype-scan-not:0004 */}
Pointer to the first character in `[beg, end)` that doesn't satisfy the mask, or `end` if no such character was found.

### Example

{/* source:cpp-locale-ctype-scan-not:0005 */}
```cpp
#include <clocale>
#include <iostream>
#include <iterator>
#include <locale>
 
int main()
{
    std::setlocale(LC_ALL, "en_US.utf8");
    std::wcout.imbue(std::locale("en_US.utf8"));
    auto& f = std::use_facet<std::ctype<wchar_t>>(std::wcout.getloc());
 
    // skip leading whitespace
    wchar_t s1[] = L"      \t\t\n  Кошка";
    const wchar_t* p1 = f.scan_not(std::ctype_base::space, std::begin(s1), std::end(s1));
    std::wcout << '\'' << p1 << "'\n";
 
    // skip leading digits
    wchar_t s2[] = L"123456789ネプネプ";
    const wchar_t* p2 = f.scan_not(std::ctype_base::digit, std::begin(s2), std::end(s2));
    std::wcout << '\'' << p2 << "'\n";
}
```
Output:
```text
'Кошка'
'ネプネプ'
```

### See also

{/* source:cpp-locale-ctype-scan-not:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`scan_not`](/docs/cpp/library/text/locale/ctype/../ctype_char/scan_not)</DescriptionTerm>
    <DescriptionBody>locates the first character in a sequence that fails given classification, using the classification table <br /> (public member function of `std::ctype<char>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_scan_is`](/docs/cpp/library/text/locale/ctype/scan_is)[virtual]</DescriptionTerm>
    <DescriptionBody>locates the first character in a sequence that conforms to given classification <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

