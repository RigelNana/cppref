---
title: "std::moneypunct<CharT,International>::thousands_sep, do_thousands_sep"
description: "Public member function, calls the member function do_thousands_sep of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/moneypunct/thousands_sep"
language: "C++"
---

{/* source:cpp-locale-moneypunct-thousands-sep:0000 */}

<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
char_type thousands_sep() const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual char_type do_thousands_sep() const;`} />
  <DeclarationDescription>
    {/* source:cpp-locale-moneypunct-thousands-sep:0001 */}
    1. Public member function, calls the member function `do_thousands_sep` of the most derived class.
    {/* source:cpp-locale-moneypunct-thousands-sep:0002 */}
    2. Returns the character to be used as the separator between digit groups when parsing or formatting the integral parts of monetary values.
  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-moneypunct-thousands-sep:0003 */}

The object of type `char_type` to use as the thousands separator. In common U.S. locales, this is `','` or `L','`.

### Example

{/* source:cpp-locale-moneypunct-thousands-sep:0004 */}

```cpp
#include <iomanip>
#include <iostream>
#include <iterator>
#include <locale>
 
struct space_out : std::moneypunct<char>
{
    pattern do_pos_format() const { return {value, none, none, none}; }
    int do_frac_digits() const { return 0; }
    char_type do_thousands_sep() const { return ' '; }
    string_type do_grouping() const { return "\002"; }
};
 
int main()
{
    std::cout.imbue(std::locale("en_US.UTF-8"));
    std::cout << "american locale: " << std::showbase
              << std::put_money(12345678.0) << '\n';
 
    std::cout.imbue(std::locale(std::cout.getloc(), new space_out));
    std::cout << "locale with modified moneypunct: "
              << std::put_money(12345678.0) << '\n';
}
```

Output:

```text
american locale: $123,456.78
locale with modified moneypunct: 12 34 56 78
```

### See also

{/* source:cpp-locale-moneypunct-thousands-sep:0005 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_grouping`](/docs/cpp/library/text/locale/moneypunct/grouping)[virtual]</DescriptionTerm>
    <DescriptionBody>provides the numbers of digits between each pair of thousands separators <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
