---
title: "std::ctype<CharT>::toupper, std::ctype<CharT>::do_toupper"
description: "1,2) Public member function, calls the protected virtual member function do_toupper of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/ctype/toupper"
language: "C++"
---

{/* source:cpp-locale-ctype-toupper:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
CharT toupper( CharT c ) const;`} />
  <Declaration id="2" language="cpp" code={`public:
const CharT* toupper( CharT* beg, const CharT* end ) const;`} />
  <Declaration id="3" language="cpp" code={`protected:
virtual CharT do_toupper( CharT c ) const;`} />
  <Declaration id="4" language="cpp" code={`protected:
virtual const CharT* do_toupper( CharT* beg, const CharT* end ) const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-ctype-toupper:0001 */}
    1,2) Public member function, calls the protected virtual member function `do_toupper` of the most derived class.

    {/* source:cpp-locale-ctype-toupper:0002 */}
    3) Converts the character `c` to upper case if an upper case form is defined by this locale.

    {/* source:cpp-locale-ctype-toupper:0003 */}
    4) For every character in the character array [`beg`, `end`), for which an upper case form exists, replaces the character with that upper case form.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-ctype-toupper:0004 */}
<ParameterList>
  <Parameter name="c">character to convert</Parameter>
  <Parameter name="beg">pointer to the first character in an array of characters to convert</Parameter>
  <Parameter name="end">one past the end pointer for the array of characters to convert</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-ctype-toupper:0005 */}
1,3) Upper case character or `c` if no upper case form is listed by this locale.

{/* source:cpp-locale-ctype-toupper:0006 */}
2,4) `end`

### Notes

{/* source:cpp-locale-ctype-toupper:0007 */}
Only 1:1 character mapping can be performed by this function, e.g. the uppercase form of 'ß' is the two-character string "SS" (with some exceptions - see [«Capital ẞ»](https://en.wikipedia.org/wiki/Capital_%E1%BA%9E)), which cannot be obtained by `do_toupper`.

### Example

{/* source:cpp-locale-ctype-toupper:0008 */}
```cpp
#include <iostream>
#include <locale>
 
void try_upper(const std::ctype<wchar_t>& f, wchar_t c)
{
    wchar_t up = f.toupper(c);
    if (up != c)
        std::wcout << "Upper case form of \'" << c << "' is " << up << '\n';
    else
        std::wcout << '\'' << c << "' has no upper case form\n";
}
 
int main()
{
    std::locale::global(std::locale("en_US.utf8"));
    std::wcout.imbue(std::locale());
    std::wcout << "In US English UTF-8 locale:\n";
    auto& f = std::use_facet<std::ctype<wchar_t>>(std::locale());
    try_upper(f, L's');
    try_upper(f, L'ſ');
    try_upper(f, L'δ');
    try_upper(f, L'ö');
    try_upper(f, L'ß');
 
    std::wstring str = L"Hello, World!";
    std::wcout << "Uppercase form of the string '" << str << "' is ";
    f.toupper(&str[0], &str[0] + str.size());
    std::wcout << '\'' << str << "'\n";
}
```
Output:
```text
In US English UTF-8 locale:
Upper case form of 's' is S
Upper case form of 'ſ' is S
Upper case form of 'δ' is Δ
Upper case form of 'ö' is Ö
'ß' has no upper case form
Uppercase form of the string 'Hello, World!' is 'HELLO, WORLD!'
```

### See also

{/* source:cpp-locale-ctype-toupper:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`tolower`](/docs/cpp/library/text/locale/ctype/tolower)</DescriptionTerm>
    <DescriptionBody>invokes `do_tolower` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`toupper`](/docs/cpp/library/text/byte/toupper)</DescriptionTerm>
    <DescriptionBody>converts a character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`towupper`](/docs/cpp/library/text/wide/towupper)</DescriptionTerm>
    <DescriptionBody>converts a wide character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
