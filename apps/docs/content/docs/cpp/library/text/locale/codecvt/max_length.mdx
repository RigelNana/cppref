---
title: "std::codecvt<InternT,ExternT,StateT>::max_length, do_max_length"
description: "1) Public member function, calls the member function do_max_length of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/codecvt/max_length"
language: "C++"
---

{/* source:cpp-locale-codecvt-max-length:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" id="1" until="C++11" code={`public:
int max_length() const throw();`} />
  <Declaration language="cpp" id="1" since="C++11" code={`public:
int max_length() const noexcept;`} />
  <Declaration language="cpp" id="2" until="C++11" code={`protected:
virtual int do_max_length() const throw();`} />
  <Declaration language="cpp" id="2" since="C++11" code={`protected:
virtual int do_max_length() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-codecvt-max-length:0001 */}
    1) Public member function, calls the member function `do_max_length` of the most derived class.

    {/* source:cpp-locale-codecvt-max-length:0002 */}
    2) Returns the maximum value that `do_length(state, from, from_end, 1)` can return for any valid range `[from, from_end)` and any valid `state`.

  </DeclarationDescription>
</DeclarationDoc>

### Return value

{/* source:cpp-locale-codecvt-max-length:0003 */}
The maximum number of `ExternT` characters that could be consumed if converted by [`in()`](/docs/cpp/library/text/locale/codecvt/in) to produce one `InternT` character.

{/* source:cpp-locale-codecvt-max-length:0004 */}
The non-converting specialization `std::codecvt<char, char, std::mbstate_t>` returns `1`.

### Notes

{/* source:cpp-locale-codecvt-max-length:0005 */}
If the encoding is state-dependent (`encoding() == -1`), then more than `max_length()` external characters may be consumed to produce one internal character.

### Example

{/* source:cpp-locale-codecvt-max-length:0006 */}
```cpp
#include <codecvt>
#include <iostream>
#include <locale>
 
int main()
{
    std::cout << "In codecvt_utf8, the longest multibyte character is "
              << std::codecvt_utf8<wchar_t>().max_length() << " bytes long\n";
 
    std::cout << "In header-consuming codecvt_utf8, the longest multibyte character is "
              << std::codecvt_utf8<wchar_t,
                                   0x10ffff,
                                   std::consume_header>().max_length() << " bytes long\n";
 
    std::cout << "In this system's en_US.utf8, the longest multibyte character is "
              << std::use_facet<std::codecvt<wchar_t, char, std::mbstate_t>>(
                     std::locale("en_US.utf8")
                 ).max_length() << " bytes long\n";
 
    std::cout << "In this system's zh_CN.gb18030, the longest multibyte character is "
              << std::use_facet<std::codecvt<wchar_t, char, std::mbstate_t>>(
                     std::locale("zh_CN.gb18030")
                 ).max_length() << " bytes long\n";
}
```
Output:
```text
In codecvt_utf8, the longest multibyte character is 4 bytes long
In header-consuming codecvt_utf8, the longest multibyte character is 7 bytes long
In this system's en_US.utf8, the longest multibyte character is 6 bytes long
In this system's zh_CN.gb18030, the longest multibyte character is 4 bytes long
```

### See also

{/* source:cpp-locale-codecvt-max-length:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>MB_CUR_MAX</DescriptionTerm>
    <DescriptionBody>maximum number of bytes in a multibyte character in the current C locale<br />(macro variable)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_encoding`](/docs/cpp/library/text/locale/codecvt/encoding)[virtual]</DescriptionTerm>
    <DescriptionBody>returns the number of `ExternT` characters necessary to produce one `InternT` character, if constant <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

