---
title: "std::ctype<CharT>::widen, do_widen"
description: "1,2) Public member function, calls the corresponding protected virtual member function do_widen overload of the most derived class. Overload (1) calls do_widen(c), overload (2) calls do_widen(beg, end, dst)."
source_url: "https://en.cppreference.com/w/cpp/locale/ctype/widen"
language: "C++"
---

{/* source:cpp-locale-ctype-widen:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
CharT widen( char c ) const;`} />
  <Declaration id="2" language="cpp" code={`public:
const char* widen( const char* beg, const char* end, CharT* dst ) const;`} />
  <Declaration id="3" language="cpp" code={`protected:
virtual CharT do_widen( char c ) const;`} />
  <Declaration id="4" language="cpp" code={`protected:
virtual const char* do_widen( const char* beg, const char* end, CharT* dst ) const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-ctype-widen:0001 */}
    1,2) Public member function, calls the corresponding protected virtual member function `do_widen` overload of the most derived class. Overload (1) calls `do_widen(c)`, overload (2) calls `do_widen(beg, end, dst)`.

    {/* source:cpp-locale-ctype-widen:0002 */}
    3) Converts the single-byte character `c` to the corresponding wide character representation using the simplest reasonable transformation. Typically, this applies only to the characters whose multibyte encoding is a single byte (e.g. `U+0000`-`U+007F` in UTF-8).

    {/* source:cpp-locale-ctype-widen:0003 */}
    4) For every character in the character array [`beg`, `end`), writes the corresponding widened character to the successive locations in the character array pointed to by `dst`.
  </DeclarationDescription>
</DeclarationDoc>

### Notes

{/* source:cpp-locale-ctype-widen:0004 */}
Widening always returns a wide character, but only the characters from the <InlineRevision until="C++23">[basic source character set](/docs/cpp/language/basic_concepts/charset#Basic_source_character_set)</InlineRevision> <InlineRevision since="C++23">[basic character set](/docs/cpp/language/basic_concepts/charset#Basic_character_set)</InlineRevision> are guaranteed to have a unique, well-defined, widening transformation, which is also guaranteed to be reversible (by `narrow()`). In practice, all characters whose multibyte representation is a single byte are usually widened to their wide character counterparts, and the rest of the possible single-byte values are usually mapped into the same placeholder value, typically `CharT(-1)`.

{/* source:cpp-locale-ctype-widen:0005 */}
Widening, if successful, preserves all character classification categories known to [`is()`](/docs/cpp/library/text/locale/ctype/is).

### Parameters

{/* source:cpp-locale-ctype-widen:0006 */}
<ParameterList>
  <Parameter name="c">character to convert</Parameter>
  <Parameter name="dflt">default value to produce if the conversion fails</Parameter>
  <Parameter name="beg">pointer to the first character in an array of characters to convert</Parameter>
  <Parameter name="end">one past the end pointer for the array of characters to convert</Parameter>
  <Parameter name="dst">pointer to the first element of the array of characters to fill</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-ctype-widen:0007 */}
1,3) Widened character.

{/* source:cpp-locale-ctype-widen:0008 */}
2,4) `end`

### Example

{/* source:cpp-locale-ctype-widen:0009 */}
```cpp
#include <iostream>
#include <locale>
 
void try_widen(const std::ctype<wchar_t>& f, char c)
{
    wchar_t w = f.widen(c);
    std::cout << "The single-byte character " << +(unsigned char)c
              << " widens to " << +w << '\n';
}
 
int main()
{
    std::locale::global(std::locale("cs_CZ.iso88592"));
    auto& f = std::use_facet<std::ctype<wchar_t>>(std::locale());
    std::cout << std::hex << std::showbase << "In Czech ISO-8859-2 locale:\n";
    try_widen(f, 'a');
    try_widen(f, '\xdf'); // German letter ß (U+00df) in ISO-8859-2
    try_widen(f, '\xec'); // Czech letter ě (U+011b) in ISO-8859-2
 
    std::locale::global(std::locale("cs_CZ.utf8"));
    auto& f2 = std::use_facet<std::ctype<wchar_t>>(std::locale());
    std::cout << "In Czech UTF-8 locale:\n";
    try_widen(f2, 'a');
    try_widen(f2, '\xdf'); 
    try_widen(f2, '\xec'); 
}
```
Possible output:
```text
In Czech ISO-8859-2 locale:
The single-byte character 0x61 widens to 0x61
The single-byte character 0xdf widens to 0xdf
The single-byte character 0xec widens to 0x11b
In Czech UTF-8 locale:
The single-byte character 0x61 widens to 0x61
The single-byte character 0xdf widens to 0xffffffff
The single-byte character 0xec widens to 0xffffffff
```

### Defect reports

{/* source:cpp-locale-ctype-widen:0010 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-locale-ctype-widen:0011 */}
<DefectReportList>
  <DefectReport kind="lwg" id={153} standard="C++98">
    <PublishedBehavior>`widen` always called overload (4)</PublishedBehavior>
    <CorrectedBehavior>calls the corresponding overload</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-locale-ctype-widen:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`narrow`](/docs/cpp/library/text/locale/ctype/narrow)</DescriptionTerm>
    <DescriptionBody>invokes `do_narrow` <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`widen`](/docs/cpp/library/io/basic_ios/widen)</DescriptionTerm>
    <DescriptionBody>widens characters <br /> (public member function of `std::basic_ios<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`btowc`](/docs/cpp/library/text/multibyte/btowc)</DescriptionTerm>
    <DescriptionBody>widens a single-byte narrow character to wide character, if possible <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
