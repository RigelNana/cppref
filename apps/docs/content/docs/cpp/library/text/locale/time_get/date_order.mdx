---
title: "std::time_get<CharT,InputIt>::date_order, std::time_get<CharT,InputIt>::do_date_order"
description: "1) Public member function, calls the protected virtual member function do_date_order of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/locale/time_get/date_order"
language: "C++"
---

{/* source:cpp-locale-time-get-date-order:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration id="1" language="cpp" code={`public:
dateorder date_order() const;`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual dateorder do_date_order() const;`} />

  <DeclarationDescription>
    {/* source:cpp-locale-time-get-date-order:0001 */}
    1) Public member function, calls the protected virtual member function `do_date_order` of the most derived class.

    {/* source:cpp-locale-time-get-date-order:0002 */}
    2) Returns a value of type [`std::time_base::dateorder`](/docs/cpp/library/text/locale/time_base), which describes the default date format used by this locale (expected by [`get_date()`](/docs/cpp/library/text/locale/time_get/get_date) and produced by [`std::strftime()`](/docs/cpp/library/chrono/c/strftime) with format specifier `'%x'`).

    {/* source:cpp-locale-time-get-date-order:0003 */}
    The valid values (inherited from [`std::time_base`](/docs/cpp/library/text/locale/time_base)) are:

  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-locale-time-get-date-order:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`no_order`</DescriptionTerm>
    <DescriptionBody>the format contains variable items (week day, Julian day, etc), or this function is not implemented</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`dmy`</DescriptionTerm>
    <DescriptionBody>day, month, year (European locales)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`mdy`</DescriptionTerm>
    <DescriptionBody>month, day, year (American locales)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`ymd`</DescriptionTerm>
    <DescriptionBody>year, month, day (Asian locales)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`ydm`</DescriptionTerm>
    <DescriptionBody>year, day, month (rare)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Parameters

{/* source:cpp-locale-time-get-date-order:0005 */}
(none)

### Return value

{/* source:cpp-locale-time-get-date-order:0006 */}
A value of type `dateorder`.

### Notes

{/* source:cpp-locale-time-get-date-order:0007 */}
This function is optional, it may return `no_order` in every case.

### Example

{/* source:cpp-locale-time-get-date-order:0008 */}
The output below was obtained using clang (libc++).
```cpp
#include <iostream>
#include <locale>
 
void show_date_order()
{
    std::time_base::dateorder d = std::use_facet<std::time_get<char>>(
                                      std::locale()
                                  ).date_order();
    switch (d)
    {
        case std::time_base::no_order:
            std::cout << "no_order\n";
            break;
        case std::time_base::dmy:
            std::cout << "day, month, year\n";
            break;
        case std::time_base::mdy:
            std::cout << "month, day, year\n";
            break;
        case std::time_base::ymd:
            std::cout << "year, month, day\n";
            break;
        case std::time_base::ydm:
            std::cout << "year, day, month\n";
            break;
    }
}
 
int main()
{
    std::locale::global(std::locale("en_US.utf8"));
    std::cout << "In U.S. locale, the default date order is: ";
    show_date_order();
 
    std::locale::global(std::locale("ja_JP.utf8"));
    std::cout << "In Japanese locale, the default date order is: ";
    show_date_order();
 
    std::locale::global(std::locale("de_DE.utf8"));
    std::cout << "In German locale, the default date order is: ";
    show_date_order();
}
```
Possible output:
```text
In U.S. locale, the default date order is: month, day, year
In Japanese locale, the default date order is: year, month, day
In German locale, the default date order is: day, month, year
```

### See also

{/* source:cpp-locale-time-get-date-order:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_get_date`](/docs/cpp/library/text/locale/time_get/get_date)[virtual]</DescriptionTerm>
    <DescriptionBody>extracts month, day, and year from input stream <br /> (virtual protected member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`time_base`](/docs/cpp/library/text/locale/time_base)</DescriptionTerm>
    <DescriptionBody>defines date format constants <br /> (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
