---
title: "std::toupper(std::locale)"
description: "Converts the character ch to uppercase if possible, using the conversion rules specified by the given locale's std::ctype facet."
source_url: "https://en.cppreference.com/w/cpp/locale/toupper"
language: "C++"
---

{/* source:cpp-locale-toupper:0000 */}
<DeclarationDoc>
Defined in header [`<locale>`](/docs/cpp/library/headers/locale)
  <Declaration language="cpp" code={`template< class CharT >
CharT toupper( CharT ch, const locale& loc );`} />

  <DeclarationDescription>
    {/* source:cpp-locale-toupper:0001 */}
    Converts the character `ch` to uppercase if possible, using the conversion rules specified by the given locale's [`std::ctype`](/docs/cpp/library/text/locale/ctype) facet.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-locale-toupper:0002 */}
<ParameterList>
  <Parameter name="ch">character</Parameter>
  <Parameter name="loc">locale</Parameter>
</ParameterList>

### Return value

{/* source:cpp-locale-toupper:0003 */}
Returns the uppercase form of `ch` if one is listed in the locale, otherwise returns `ch` unchanged.

### Notes

{/* source:cpp-locale-toupper:0004 */}
Only 1:1 character mapping can be performed by this function, e.g. the uppercase form of 'ß' is (with some exceptions) the two-character string "SS", which cannot be obtained by `std::toupper`.

### Possible implementation

{/* source:cpp-locale-toupper:0005 */}
```cpp
template<class CharT>
CharT toupper(CharT ch, const std::locale& loc)
{
    return std::use_facet<std::ctype<CharT>>(loc).toupper(ch);
}
```

### Example

{/* source:cpp-locale-toupper:0006 */}
```cpp
#include <cwctype>
#include <iostream>
#include <locale>
 
int main()
{
    wchar_t c = L'\u017f'; // Latin small letter Long S ('ſ')
 
    std::cout << std::hex << std::showbase;
 
    std::cout << "in the default locale, toupper(" << (std::wint_t)c << ") = "
              << (std::wint_t)std::toupper(c, std::locale()) << '\n';
 
    std::cout << "in Unicode locale, toupper(" << (std::wint_t)c << ") = "
              << (std::wint_t)std::toupper(c, std::locale("en_US.utf8")) << '\n';
}
```
Possible output:
```text
in the default locale, toupper(0x17f) = 0x17f
in Unicode locale, toupper(0x17f) = 0x53
```

### See also

{/* source:cpp-locale-toupper:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`tolower(std::locale)`](/docs/cpp/library/text/locale/tolower)</DescriptionTerm>
    <DescriptionBody>converts a character to lowercase using the `ctype` facet of a locale <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`toupper`](/docs/cpp/library/text/byte/toupper)</DescriptionTerm>
    <DescriptionBody>converts a character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`towupper`](/docs/cpp/library/text/wide/towupper)</DescriptionTerm>
    <DescriptionBody>converts a wide character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
