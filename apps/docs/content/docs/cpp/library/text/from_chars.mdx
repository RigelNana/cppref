---
title: "std::from_chars"
description: "Analyzes the character sequence [first, last) for a pattern described below. If no characters match the pattern or if the value obtained by parsing the matched characters is not representable in the type of value, value is unmodified, otherwise the characters matching the pattern are interpreted as a text representation of an arithmetic value, which is stored in value."
source_url: "https://en.cppreference.com/w/cpp/utility/from_chars"
language: "C++"
---

{/* source:cpp-utility-from-chars:0000 */}
<DeclarationDoc>
Defined in header [`<charconv>`](/docs/cpp/library/headers/charconv)

  <Declaration id="1" language="cpp" since="C++17" code={`std::from_chars_result
    from_chars( const char* first, const char* last,

                /* integer-type */& value, int base = 10 );`} />
(constexpr since C++23)
  <Declaration id="2" language="cpp" since="C++17" code={`std::from_chars_result
    from_chars( const char* first, const char* last,
                /* floating-point-type */& value,

                std::chars_format fmt = std::chars_format::general );`} />
(constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-utility-from-chars:0001 */}
    Analyzes the character sequence `[first, last)` for a pattern described below. If no characters match the pattern or if the value obtained by parsing the matched characters is not representable in the type of `value`, `value` is unmodified, otherwise the characters matching the pattern are interpreted as a text representation of an arithmetic value, which is stored in `value`.

    {/* source:cpp-utility-from-chars:0002 */}
    1. Integer parsers: Expects the pattern identical to the one used by [`std::strtol`](/docs/cpp/library/text/byte/strtol) in the default (`"C"`) locale and the given non-zero numeric base, except that
       - `"0x"` or `"0X"` prefixes are not recognized if `base` is 16
       - only the minus sign is recognized (not the plus sign), and only for signed integer types of `value`
       - leading whitespace is not ignored.

       The library provides overloads for all <InlineRevision since="C++23">cv-unqualified</InlineRevision> signed and unsigned integer types and `char` as the referenced type of the parameter `value`.

    {/* source:cpp-utility-from-chars:0003 */}
    2. Floating-point parsers: Expects the pattern identical to the one used by [`std::strtod`](/docs/cpp/library/text/byte/strtof) in the default (`"C"`) locale, except that
       - the plus sign is not recognized outside of the exponent (only the minus sign is permitted at the beginning)
       - if `fmt` has [`std::chars_format::scientific`](/docs/cpp/library/text/chars_format) set but not [`std::chars_format::fixed`](/docs/cpp/library/text/chars_format), the exponent part is required (otherwise it is optional)
       - if `fmt` has [`std::chars_format::fixed`](/docs/cpp/library/text/chars_format) set but not [`std::chars_format::scientific`](/docs/cpp/library/text/chars_format), the optional exponent is not permitted
       - if `fmt` is [`std::chars_format::hex`](/docs/cpp/library/text/chars_format), the prefix `"0x"` or `"0X"` is not permitted (the string `"0x123"` parses as the value `"0"` with unparsed remainder `"x123"`)
       - leading whitespace is not ignored.

    {/* source:cpp-utility-from-chars:0004 */}
    In any case, the resulting value is one of at most two floating-point values closest to the value of the string matching the pattern, after rounding according to [`std::round_to_nearest`](/docs/cpp/library/utility/types/numeric_limits/float_round_style).

    {/* source:cpp-utility-from-chars:0005 */}
    The library provides overloads for all cv-unqualified <InlineRevision until="C++23">standard</InlineRevision> floating-point types as the referenced type of the parameter `value`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-utility-from-chars:0006 */}
<ParameterList>
  <Parameter name="first, last">valid character range to parse</Parameter>
  <Parameter name="value">the out-parameter where the parsed value is stored if successful</Parameter>
  <Parameter name="base">integer base to use: a value between 2 and 36 (inclusive).</Parameter>
  <Parameter name="fmt">floating-point formatting to use, a bitmask of type [`std::chars_format`](/docs/cpp/library/text/chars_format)</Parameter>
</ParameterList>

### Return value

{/* source:cpp-utility-from-chars:0007 */}
On success, returns a value of type [`std::from_chars_result`](/docs/cpp/library/text/from_chars_result) such that `ptr` points at the first character not matching the pattern, or has the value equal to `last` if all characters match and `ec` is value-initialized.

{/* source:cpp-utility-from-chars:0008 */}
If there is no pattern match, returns a value of type [`std::from_chars_result`](/docs/cpp/library/text/from_chars_result) such that `ptr` equals `first` and `ec` equals [`std::errc::invalid_argument`](/docs/cpp/library/error/errc). `value` is unmodified.

{/* source:cpp-utility-from-chars:0009 */}
If the pattern was matched, but the parsed value is not in the range representable by the type of `value`, returns value of type [`std::from_chars_result`](/docs/cpp/library/text/from_chars_result) such that `ec` equals [`std::errc::result_out_of_range`](/docs/cpp/library/error/errc) and `ptr` points at the first character not matching the pattern. `value` is unmodified.

### Exceptions

{/* source:cpp-utility-from-chars:0010 */}
Throws nothing.

### Notes

{/* source:cpp-utility-from-chars:0011 */}
Unlike other parsing functions in C++ and C libraries, `std::from_chars` is locale-independent, non-allocating, and non-throwing. Only a small subset of parsing policies used by other libraries (such as [`std::sscanf`](/docs/cpp/library/io/c/scanf)) is provided. This is intended to allow the fastest possible implementation that is useful in common high-throughput contexts such as text-based interchange ([JSON](https://en.wikipedia.org/wiki/JSON) or [XML](https://en.wikipedia.org/wiki/XML)).

{/* source:cpp-utility-from-chars:0012 */}
The guarantee that `std::from_chars` can recover every floating-point value formatted by [`std::to_chars`](/docs/cpp/library/text/to_chars) exactly is only provided if both functions are from the same implementation.

{/* source:cpp-utility-from-chars:0013 */}
A pattern consisting of a sign with no digits following it is treated as pattern that did not match anything.

{/* source:cpp-utility-from-chars:0014 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_to_chars`](/docs/cpp/library/feature_test#cpp_lib_to_chars) | [`201611L`](/docs/cpp/library/compiler_support/17#cpp_lib_to_chars_201611L) | (C++17) | Elementary string conversions ([`std::from_chars`](/docs/cpp/library/text/from_chars), [`std::to_chars`](/docs/cpp/library/text/to_chars)) |
| | [`202306L`](/docs/cpp/library/compiler_support/26#cpp_lib_to_chars_202306L) | (C++26) | Testing for success or failure of [`<charconv>`](/docs/cpp/library/headers/charconv) functions |
| [`__cpp_lib_constexpr_charconv`](/docs/cpp/library/feature_test#cpp_lib_constexpr_charconv) | [`202207L`](/docs/cpp/library/compiler_support/23#cpp_lib_constexpr_charconv_202207L) | (C++23) | Add `constexpr` modifiers to `std::from_chars` and `std::to_chars` overloads for integral types |

### Example

{/* source:cpp-utility-from-chars:0015 */}
```cpp
#include <cassert>
#include <charconv>
#include <iomanip>
#include <iostream>
#include <optional>
#include <string_view>
#include <system_error>
 
int main()
{
    for (std::string_view const str : {"1234", "15 foo", "bar", " 42", "5000000000"})
    {
        std::cout << "String: " << std::quoted(str) << ". ";
        int result{};
        auto [ptr, ec] = std::from_chars(str.data(), str.data() + str.size(), result);
 
        if (ec == std::errc())
            std::cout << "Result: " << result << ", ptr -> " << std::quoted(ptr) << '\n';
        else if (ec == std::errc::invalid_argument)
            std::cout << "This is not a number.\n";
        else if (ec == std::errc::result_out_of_range)
            std::cout << "This number is larger than an int.\n";
    }
 
    // C++23's constexpr from_char demo / C++26's operator bool() demo:
    auto to_int = [](std::string_view s) -> std::optional<int>
    {
        int value{};
#if __cpp_lib_to_chars >= 202306L
        if (std::from_chars(s.data(), s.data() + s.size(), value))
#else
        if (std::from_chars(s.data(), s.data() + s.size(), value).ec == std::errc{})
#endif
            return value;
        else
            return std::nullopt;
    };
 
    assert(to_int("42") == 42);
    assert(to_int("foo") == std::nullopt);
#if __cpp_lib_constexpr_charconv and __cpp_lib_optional >= 202106
    static_assert(to_int("42") == 42);
    static_assert(to_int("foo") == std::nullopt);
#endif
}
```
Output:
```text
String: "1234". Result: 1234, ptr -> ""
String: "15 foo". Result: 15, ptr -> " foo"
String: "bar". This is not a number.
String: " 42". This is not a number.
String: "5000000000". This number is larger than an int.
```

### Defect reports

{/* source:cpp-utility-from-chars:0016 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-utility-from-chars:0017 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2955} standard="C++17">
    <PublishedBehavior>this function was in [`<utility>`](/docs/cpp/library/headers/utility) and used [`std::error_code`](/docs/cpp/library/error/error_code)</PublishedBehavior>
    <CorrectedBehavior>moved to [`<charconv>`](/docs/cpp/library/headers/charconv) and uses [`std::errc`](/docs/cpp/library/error/errc)</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3373} standard="C++17">
    <PublishedBehavior>`std::from_chars_result` might have additional members</PublishedBehavior>
    <CorrectedBehavior>additional members are prohibited</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-utility-from-chars:0018 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`from_chars_result`](/docs/cpp/library/text/from_chars_result) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>the return type of `std::from_chars` <br /> (class)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_chars`](/docs/cpp/library/text/to_chars) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts an integer or floating-point value to a character sequence <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`stoi`, `stol`, `stoll`](/docs/cpp/library/string/basic_string/stol) <InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a string to a signed integer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`stof`, `stod`, `stold`](/docs/cpp/library/string/basic_string/stof) <InlineRevision since="C++11" />, <InlineRevision since="C++11" />, <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a string to a floating point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtol`, `strtoll`](/docs/cpp/library/text/byte/strtol) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to an integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strtof`, `strtod`, `strtold`](/docs/cpp/library/text/byte/strtof)</DescriptionTerm>
    <DescriptionBody>converts a byte string to a floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`scanf`, `fscanf`, `sscanf`](/docs/cpp/library/io/c/scanf)</DescriptionTerm>
    <DescriptionBody>reads formatted input from [`stdin`](/docs/cpp/library/io/c/std_streams), a file stream or a buffer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator>>`](/docs/cpp/library/io/basic_istream/operator_gtgt)</DescriptionTerm>
    <DescriptionBody>extracts formatted data <br /> (public member function of `std::basic_istream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
