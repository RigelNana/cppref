---
title: "std::wmemcmp"
description: "Compares the first count wide characters of the wide character arrays pointed to by lhs and rhs. The comparison is done lexicographically."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wmemcmp"
language: "C++"
---

{/* source:cpp-string-wide-wmemcmp:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)

  <Declaration language="cpp" code={
`int wmemcmp( const wchar_t* lhs, const wchar_t* rhs, std::size_t count );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wmemcmp:0001 */}

Compares the first `count` wide characters of the wide character arrays pointed to by `lhs` and `rhs`. The comparison is done lexicographically.

{/* source:cpp-string-wide-wmemcmp:0002 */}

The sign of the result is the sign of the difference between the values of the first pair of wide characters that differ in the arrays being compared.

{/* source:cpp-string-wide-wmemcmp:0003 */}

If `count` is zero, the function does nothing.

### Parameters

{/* source:cpp-string-wide-wmemcmp:0004 */}

<ParameterList>
  <Parameter name="lhs, rhs">pointers to the wide character arrays to compare</Parameter>
  <Parameter name="count">number of wide characters to examine</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wmemcmp:0005 */}

Negative value if the value of the first differing wide character in `lhs` is less than the value of the corresponding wide character in `rhs`: `lhs` precedes `rhs` in lexicographical order.

{/* source:cpp-string-wide-wmemcmp:0006 */}

`0` if all `count` wide characters of `lhs` and `rhs` are equal.

{/* source:cpp-string-wide-wmemcmp:0007 */}

Positive value if the value of the first differing wide character in `lhs` is greater than the value of the corresponding wide character in `rhs`: `rhs` precedes `lhs` in lexicographical order.

### Notes

{/* source:cpp-string-wide-wmemcmp:0008 */}

This function is not locale-sensitive and pays no attention to the values of the `wchar_t` objects it examines: nulls as well as invalid wide characters are compared too.

### Example

{/* source:cpp-string-wide-wmemcmp:0009 */}

```cpp
#include <clocale>
#include <cwchar>
#include <iostream>
#include <locale>
#include <string>
 
void demo(const wchar_t* lhs, const wchar_t* rhs, std::size_t sz)
{
    std::wcout << std::wstring(lhs, sz);
    int rc = std::wmemcmp(lhs, rhs, sz);
    if (rc == 0)
        std::wcout << " compares equal to ";
    else if (rc < 0)
        std::wcout << " precedes ";
    else if (rc > 0)
        std::wcout << " follows ";
    std::wcout << std::wstring(rhs, sz) << " in lexicographical order\n";
}
 
int main()
{
    std::setlocale(LC_ALL, "en_US.utf8");
    std::wcout.imbue(std::locale("en_US.utf8"));
 
    wchar_t a1[] = {L'α',L'β',L'γ'};
    constexpr std::size_t sz = sizeof a1 / sizeof *a1;
    wchar_t a2[sz] = {L'α',L'β',L'δ'};
 
    demo(a1, a2, sz);
    demo(a2, a1, sz);
    demo(a1, a1, sz);
}
```

Possible output:

```text
αβγ precedes αβδ in lexicographical order
αβδ follows αβγ in lexicographical order
αβγ compares equal to αβγ in lexicographical order
```

### See also

{/* source:cpp-string-wide-wmemcmp:0010 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`wcscmp`](/docs/cpp/library/text/wide/wcscmp)</DescriptionTerm>
    <DescriptionBody>compares two wide strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`memcmp`](/docs/cpp/library/text/byte/memcmp)</DescriptionTerm>
    <DescriptionBody>compares two buffers <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcsncmp`](/docs/cpp/library/text/wide/wcsncmp)</DescriptionTerm>
    <DescriptionBody>compares a certain amount of characters from two wide strings <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wmemcmp) for `wmemcmp`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
