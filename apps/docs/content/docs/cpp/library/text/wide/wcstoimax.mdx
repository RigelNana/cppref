---
title: "std::wcstoimax, std::wcstoumax"
description: "Interprets an unsigned integer value in a wide string pointed to by nptr."
source_url: "https://en.cppreference.com/w/cpp/string/wide/wcstoimax"
language: "C++"
---

{/* source:cpp-string-wide-wcstoimax:0000 */}

<DeclarationDoc>
Defined in header [`<cinttypes>`](/docs/cpp/library/headers/cinttypes)

  <Declaration id="1" since="C++11" language="cpp" code={
`std::intmax_t wcstoimax( const wchar_t* nptr, wchar_t** endptr, int base );`
  } />
  <Declaration id="2" since="C++11" language="cpp" code={
`std::uintmax_t wcstoumax( const wchar_t* nptr, wchar_t** endptr, int base );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-wcstoimax:0001 */}

Interprets an unsigned integer value in a wide string pointed to by `nptr`.

{/* source:cpp-string-wide-wcstoimax:0002 */}

Discards any whitespace characters (as identified by calling [`std::iswspace`](/docs/cpp/library/text/wide/iswspace)) until the first non-whitespace character is found, then takes as many characters as possible to form a valid *base-n* (where n=`base`) unsigned integer number representation and converts them to an integer value. The valid unsigned integer value consists of the following parts:

{/* source:cpp-string-wide-wcstoimax:0003 */}

- (optional) plus or minus sign
- (optional) prefix (`0`) indicating octal base (applies only when the base is `8` or `0`)
- (optional) prefix (`0x` or `0X`) indicating hexadecimal base (applies only when the base is `16` or `0`)
- a sequence of digits

{/* source:cpp-string-wide-wcstoimax:0004 */}

The set of valid values for base is `{0, 2, 3, ..., 36}`. The set of valid digits for base-`2` integers is `{0, 1}`, for base-`3` integers is `{0, 1, 2}`, and so on. For bases larger than `10`, valid digits include alphabetic characters, starting from `Aa` for base-`11` integer, to `Zz` for base-`36` integer. The case of the characters is ignored.

{/* source:cpp-string-wide-wcstoimax:0005 */}

Additional numeric formats may be accepted by the currently installed C [locale](/docs/cpp/library/text/locale/setlocale).

{/* source:cpp-string-wide-wcstoimax:0006 */}

If the value of `base` is `0`, the numeric base is auto-detected: if the prefix is `0`, the base is octal, if the prefix is `0x` or `0X`, the base is hexadecimal, otherwise the base is decimal.

{/* source:cpp-string-wide-wcstoimax:0007 */}

If the minus sign was part of the input sequence, the numeric value calculated from the sequence of digits is negated as if by [unary minus](/docs/cpp/language/expressions/operator_arithmetic#Unary_arithmetic_operators) in the result type, which applies unsigned integer wraparound rules.

{/* source:cpp-string-wide-wcstoimax:0008 */}

The functions sets the pointer pointed to by `endptr` to point to the wide character past the last character interpreted. If `endptr` is a null pointer, it is ignored.

### Parameters

{/* source:cpp-string-wide-wcstoimax:0009 */}

<ParameterList>
  <Parameter name="nptr">pointer to the null-terminated wide string to be interpreted</Parameter>
  <Parameter name="endptr">pointer to a pointer to a wide character</Parameter>
  <Parameter name="base">*base* of the interpreted integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-wcstoimax:0010 */}

Integer value corresponding to the contents of `str` on success. If the converted value falls out of range of corresponding return type, range error occurs and [`INTMAX_MAX`](/docs/cpp/library/utility/types/integer), [`INTMAX_MIN`](/docs/cpp/library/utility/types/integer), [`UINTMAX_MAX`](/docs/cpp/library/utility/types/integer), or `0` is returned, as appropriate. If no conversion can be performed, `0` is returned.

### Example

{/* source:cpp-string-wide-wcstoimax:0011 */}

```cpp
#include <cinttypes>
#include <iostream>
#include <string>
 
int main()
{
    std::wstring str = L"helloworld";
    std::intmax_t val = std::wcstoimax(str.c_str(), nullptr, 36);
    std::wcout << str << " in base 36 is " << val << " in base 10\n";
 
    wchar_t* nptr;
    val = std::wcstoimax(str.c_str(), &nptr, 30);
    if (nptr != &str[0] + str.size())
        std::wcout << str << " in base 30 is invalid."
                   << " The first invalid digit is " << *nptr << '\n';
}
```

Output:

```text
helloworld in base 36 is 1767707668033969 in base 10
helloworld in base 30 is invalid. The first invalid digit is w
```

### See also

{/* source:cpp-string-wide-wcstoimax:0012 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`strtoimax strtoumax`](/docs/cpp/library/text/byte/strtoimax) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a byte string to [`std::intmax_t`](/docs/cpp/library/utility/types/integer) or [`std::uintmax_t`](/docs/cpp/library/utility/types/integer) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcstol wcstoll`](/docs/cpp/library/text/wide/wcstol)</DescriptionTerm>
    <DescriptionBody>converts a wide string to an integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcstoul wcstoull`](/docs/cpp/library/text/wide/wcstoul)</DescriptionTerm>
    <DescriptionBody>converts a wide string to an unsigned integer value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/wcstoimax) for `wcstoimax, wcstoumax`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
