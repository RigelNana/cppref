---
title: "std::towupper"
description: "Converts the given wide character to uppercase, if possible."
source_url: "https://en.cppreference.com/w/cpp/string/wide/towupper"
language: "C++"
---

{/* source:cpp-string-wide-towupper:0000 */}

<DeclarationDoc>
Defined in header [`<cwctype>`](/docs/cpp/library/headers/cwctype)

  <Declaration language="cpp" code={
`std::wint_t towupper( std::wint_t ch );`
  } />
</DeclarationDoc>

{/* source:cpp-string-wide-towupper:0001 */}

Converts the given wide character to uppercase, if possible.

{/* source:cpp-string-wide-towupper:0002 */}

If the value of `ch` is neither representable as a `wchar_t` nor equal to the value of the macro `WEOF`, the behavior is undefined.

### Parameters

{/* source:cpp-string-wide-towupper:0003 */}

<ParameterList>
  <Parameter name="ch">wide character to be converted</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-wide-towupper:0004 */}

Uppercase version of `ch` or unmodified `ch` if no uppercase version is listed in the current C locale.

### Notes

{/* source:cpp-string-wide-towupper:0005 */}

Only 1:1 character mapping can be performed by this function, e.g. the uppercase form of `'ß'` is (with some exceptions) the two-character string `"SS"`, which cannot be obtained by `std::towupper`.

{/* source:cpp-string-wide-towupper:0006 */}

[ISO 30112](https://www.open-std.org/JTC1/SC35/WG5/docs/30112d10.pdf) specifies which pairs of Unicode characters are included in this mapping.

### Example

{/* source:cpp-string-wide-towupper:0007 */}

The latin [letter 'ſ' (U+017F)](https://en.wikipedia.org/wiki/Long_s) is the alternative lowercase form of `'S'` (U+0053).
```cpp
#include <clocale>
#include <cwctype>
#include <iostream>
 
int main()
{
    wchar_t c = L'\u017f'; // Latin small letter Long S ('ſ')
 
    std::cout << std::hex << std::showbase;
    std::cout << "in the default locale, towupper("
              << static_cast<std::wint_t>(c) << ") = "
              << std::towupper(c) << '\n';
 
    std::setlocale(LC_ALL, "en_US.utf8");
    std::cout << "in Unicode locale, towupper("
              << static_cast<std::wint_t>(c) << ") = "
              << std::towupper(c) << '\n';
}
```

Output:

```text
in the default locale, towupper(0x17f) = 0x17f
in Unicode locale, towupper(0x17f) = 0x53
```

### See also

{/* source:cpp-string-wide-towupper:0008 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`towlower`](/docs/cpp/library/text/wide/towlower)</DescriptionTerm>
    <DescriptionBody>converts a wide character to lowercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`toupper(std::locale)`](/docs/cpp/library/text/locale/toupper)</DescriptionTerm>
    <DescriptionBody>converts a character to uppercase using the ctype facet of a locale <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`toupper`](/docs/cpp/library/text/byte/toupper)</DescriptionTerm>
    <DescriptionBody>converts a character to uppercase <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/wide/towupper) for `towupper`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
