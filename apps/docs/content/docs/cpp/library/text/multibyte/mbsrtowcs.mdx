---
title: "std::mbsrtowcs"
description: "Converts a null-terminated multibyte character sequence, which begins in the conversion state described by *ps, from the array whose first element is pointed to by *src to its wide character representation. If dst is not null, converted characters are stored in the successive elements of the wchar_t array pointed to by dst. No more than len wide characters are written to the destination array."
source_url: "https://en.cppreference.com/w/cpp/string/multibyte/mbsrtowcs"
language: "C++"
---

{/* source:cpp-string-multibyte-mbsrtowcs:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)
  <Declaration language="cpp" code={`std::size_t mbsrtowcs( wchar_t* dst,
                       const char** src,
                       std::size_t len,

                       std::mbstate_t* ps );`} />
</DeclarationDoc>

{/* source:cpp-string-multibyte-mbsrtowcs:0001 */}

Converts a null-terminated multibyte character sequence, which begins in the conversion state described by `*ps`, from the array whose first element is pointed to by `*src` to its wide character representation. If `dst` is not null, converted characters are stored in the successive elements of the wchar_t array pointed to by `dst`. No more than `len` wide characters are written to the destination array.


{/* source:cpp-string-multibyte-mbsrtowcs:0002 */}

Each multibyte character is converted as if by a call to [`std::mbrtowc`](/docs/cpp/library/text/multibyte/mbrtowc). The conversion stops if:


{/* source:cpp-string-multibyte-mbsrtowcs:0003 */}

- The multibyte null character was converted and stored. `src` is set to a null pointer and `*ps` represents the initial shift state.
- An invalid multibyte character (according to the current C locale) was encountered. `src` is set to point at the beginning  of the first unconverted multibyte character.
- The next wide character to be stored would exceed `len`. `src` is set to point at the beginning of the first unconverted multibyte character. This condition is not checked if `dst` is a null pointer.

### Parameters

{/* source:cpp-string-multibyte-mbsrtowcs:0004 */}

<ParameterList>
  <Parameter name="dst">pointer to wide character array where the results will be stored</Parameter>
  <Parameter name="src">pointer to pointer to the first element of a null-terminated multibyte string</Parameter>
  <Parameter name="len">number of wide characters available in the array pointed to by dst</Parameter>
  <Parameter name="ps">pointer to the conversion state object</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-multibyte-mbsrtowcs:0005 */}

On success, returns the number of wide characters, excluding the terminating `L'\0'`, written to the character array. If `dst` is a null pointer, returns the number of wide characters that would have been written given unlimited length.


{/* source:cpp-string-multibyte-mbsrtowcs:0006 */}

On conversion error (if invalid multibyte character was encountered), returns `static_cast<std::size_t>(-1)`, stores [`EILSEQ`](/docs/cpp/library/error/errno_macros) in [`errno`](/docs/cpp/library/error/errno), and leaves `*ps` in unspecified state.


### Notes

{/* source:cpp-string-multibyte-mbsrtowcs:0007 */}

This function moves the `src` pointer to the end of the converted multibyte string. This doesn't happen if `dst` is a null pointer.


### Example

{/* source:cpp-string-multibyte-mbsrtowcs:0008 */}

```cpp
#include <clocale>
#include <cwchar>
#include <iostream>
#include <vector>
 
void print_as_wide(const char* mbstr)
{
    std::mbstate_t state = std::mbstate_t();
    std::size_t len = 1 + std::mbsrtowcs(nullptr, &mbstr, 0, &state);
    std::vector<wchar_t> wstr(len);
    std::mbsrtowcs(&wstr[0], &mbstr, wstr.size(), &state);
    std::wcout << "Wide string: " << &wstr[0] << '\n'
               << "The length, including '\\0': " << wstr.size() << '\n';
}
 
int main()
{
    std::setlocale(LC_ALL, "en_US.utf8");
    const char* mbstr = "z\u00df\u6c34\U0001f34c"; // or u8"zß水🍌"
    print_as_wide(mbstr);
}
```

Output:

```text
Wide string: zß水🍌
The length, including '\0': 5
```

### See also

{/* source:cpp-string-multibyte-mbsrtowcs:0009 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`mbrtowc`](/docs/cpp/library/text/multibyte/mbrtowc)</DescriptionTerm>
    <DescriptionBody>converts the next multibyte character to wide character, given state <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wcsrtombs`](/docs/cpp/library/text/multibyte/wcsrtombs)</DescriptionTerm>
    <DescriptionBody>converts a wide string to narrow multibyte character string, given state <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_in`](/docs/cpp/library/text/locale/codecvt/in) [virtual]</DescriptionTerm>
    <DescriptionBody>converts a string from `ExternT` to `InternT`, such as when reading from file <br /> (virtual protected member function of `std::codecvt<InternT,ExternT,StateT>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/multibyte/mbsrtowcs) for `mbsrtowcs`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
