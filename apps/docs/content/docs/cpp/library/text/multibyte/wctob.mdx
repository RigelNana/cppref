---
title: "std::wctob"
description: "Narrows a wide character c if its multibyte character equivalent in the initial shift state is a single byte."
source_url: "https://en.cppreference.com/w/cpp/string/multibyte/wctob"
language: "C++"
---

{/* source:cpp-string-multibyte-wctob:0000 */}

<DeclarationDoc>
Defined in header [`<cwchar>`](/docs/cpp/library/headers/cwchar)
  <Declaration language="cpp" code={`int wctob( std::wint_t c );`} />
</DeclarationDoc>

{/* source:cpp-string-multibyte-wctob:0001 */}

Narrows a wide character `c` if its multibyte character equivalent in the initial shift state is a single byte.


{/* source:cpp-string-multibyte-wctob:0002 */}

This is typically possible for the characters from the ASCII character set, since most multibyte encodings (such as UTF-8) use single bytes to encode those characters.


### Parameters

{/* source:cpp-string-multibyte-wctob:0003 */}

<ParameterList>
  <Parameter name="c">wide character to narrow</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-multibyte-wctob:0004 */}

[`EOF`](/docs/cpp/library/io/c) if `c` does not represent a multibyte character with length `1` in initial shift state.


{/* source:cpp-string-multibyte-wctob:0005 */}

Otherwise, the single-byte representation of `c` as `unsigned char` converted to `int`.


### Example

{/* source:cpp-string-multibyte-wctob:0006 */}

```cpp
#include <clocale>
#include <cwchar>
#include <iostream>
 
void try_narrowing(wchar_t c)
{
    int cn = std::wctob(c);
    if (cn != EOF)
        std::cout << '\'' << int(c) << "' narrowed to " << +cn << '\n';
    else
        std::cout << '\'' << int(c) << "' could not be narrowed\n";
}
 
int main()
{
    std::setlocale(LC_ALL, "th_TH.utf8");
    std::cout << std::hex << std::showbase << "In Thai UTF-8 locale:\n";
    try_narrowing(L'a');
    try_narrowing(L'๛');
 
    std::setlocale(LC_ALL, "th_TH.tis620");
    std::cout << "In Thai TIS-620 locale:\n";
    try_narrowing(L'a');
    try_narrowing(L'๛');
}
```

Output:

```text
In Thai UTF-8 locale:
'0x61' narrowed to 0x61
'0xe5b' could not be narrowed
In Thai TIS-620 locale:
'0x61' narrowed to 0x61
'0xe5b' narrowed to 0xfb
```

### See also

{/* source:cpp-string-multibyte-wctob:0007 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`btowc`](/docs/cpp/library/text/multibyte/btowc)</DescriptionTerm>
    <DescriptionBody>widens a single-byte narrow character to wide character, if possible <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`narrow`](/docs/cpp/library/io/basic_ios/narrow)</DescriptionTerm>
    <DescriptionBody>narrows characters <br /> (public member function of `std::basic_ios<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`narrow`](/docs/cpp/library/text/locale/ctype/narrow)</DescriptionTerm>
    <DescriptionBody>invokes `do_narrow` <br /> (public member function of `std::ctype<CharT>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/multibyte/wctob) for `wctob`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
