---
title: "std::c8rtomb"
description: "Converts a single code point from UTF-8 to a narrow multibyte character representation."
source_url: "https://en.cppreference.com/w/cpp/string/multibyte/c8rtomb"
language: "C++"
---

{/* source:cpp-string-multibyte-c8rtomb:0000 */}

<DeclarationDoc>
Defined in header [`<cuchar>`](/docs/cpp/library/headers/cuchar)
  <Declaration language="cpp" since="C++20" code={`std::size_t c8rtomb( char* s, char8_t c8, std::mbstate_t* ps );`} />
</DeclarationDoc>

{/* source:cpp-string-multibyte-c8rtomb:0001 */}

Converts a single code point from UTF-8 to a narrow multibyte character representation.


{/* source:cpp-string-multibyte-c8rtomb:0002 */}

If `s` is not a null pointer and `c8` is the last code unit in a valid UTF-8 encoding of a code point, the function determines the number of bytes necessary to store the multibyte character representation of that code point (including any shift sequences, and taking into account the current multibyte conversion state `*ps`), and stores the multibyte character representation in the character array whose first element is pointed to by `s`, updating `*ps` as necessary. At most `MB_CUR_MAX` bytes can be written by this function.


{/* source:cpp-string-multibyte-c8rtomb:0003 */}

If `c8` is not the final UTF-8 code unit in a representation of a code point, the function does not write to the array pointed to by `s`, only `*ps` is updated.


{/* source:cpp-string-multibyte-c8rtomb:0004 */}

If `s` is a null pointer, the call is equivalent to `std::c8rtomb(buf, u8'\0', ps)` for some internal buffer `buf`.


{/* source:cpp-string-multibyte-c8rtomb:0005 */}

If `c8` is the null character `u8'\0'`, a null byte is stored, preceded by any shift sequence necessary to restore the initial shift state and the conversion state parameter `*ps` is updated to represent the initial shift state.


{/* source:cpp-string-multibyte-c8rtomb:0006 */}

The multibyte encoding used by this function is specified by the currently active C locale.


### Parameters

{/* source:cpp-string-multibyte-c8rtomb:0007 */}

<ParameterList>
  <Parameter name="s">pointer to narrow character array where the multibyte character will be stored</Parameter>
  <Parameter name="c8">the UTF-8 code unit to convert</Parameter>
  <Parameter name="ps">pointer to the conversion state object used when interpreting the multibyte string</Parameter>
</ParameterList>

### Return value

{/* source:cpp-string-multibyte-c8rtomb:0008 */}

The number of bytes stored in the array object (including any shift sequences). This may be zero when `c8` is not the final code unit in the UTF-8 representation of a code point.


{/* source:cpp-string-multibyte-c8rtomb:0009 */}

If `c8` is invalid (does not contribute to a sequence of char8_t corresponding to a valid multibyte character), the value of the macro [`EILSEQ`](/docs/cpp/library/error/errno_macros) is stored in [`errno`](/docs/cpp/library/error/errno), `static_cast<std::size_t>(-1)` is returned, and the conversion state is unspecified.


### Notes

{/* source:cpp-string-multibyte-c8rtomb:0010 */}

Calls to `c8rtomb` with a null pointer argument for `s` may introduce a data race with other calls to `c8rtomb` with a null pointer argument for `s`.


### Example

{/* source:cpp-string-multibyte-c8rtomb:0011 */}

This section is incomplete Reason: no example

### See also

{/* source:cpp-string-multibyte-c8rtomb:0012 */}

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`mbrtoc8`](/docs/cpp/library/text/multibyte/mbrtoc8) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>converts a narrow multibyte character to UTF-8 encoding <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`c16rtomb`](/docs/cpp/library/text/multibyte/c16rtomb) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a UTF-16 character to narrow multibyte encoding <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/string/multibyte/c8rtomb) for `c8rtomb`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
