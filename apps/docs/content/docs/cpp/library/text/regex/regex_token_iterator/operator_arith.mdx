---
title: "std::regex_token_iterator<BidirIt,CharT,Traits>::operator++, operator++(int)"
description: "Advances the iterator on the next sub match."
source_url: "https://en.cppreference.com/w/cpp/regex/regex_token_iterator/operator_arith"
language: "C++"
---

{/* source:cpp-regex-regex-token-iterator-operator-arith:0000 */}
<DeclarationDoc>

  <Declaration language="cpp" since="C++11" code={`regex_token_iterator& operator++();`} />
  <Declaration language="cpp" since="C++11" code={`regex_token_iterator operator++( int );`} />
</DeclarationDoc>

{/* source:cpp-regex-regex-token-iterator-operator-arith:0001 */}
Advances the iterator on the next sub match.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0002 */}
This section is incomplete Reason: Explain better. For example, subs is an exposition-only vector of matched sub-expressions.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0003 */}
If `*this` is a suffix iterator, sets `*this` to an end-of-sequence iterator.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0004 */}
Otherwise, if `N + 1 < subs.size()`, increments N and sets result to the address of the current match.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0005 */}
Otherwise, sets `N` to `​0​` and increments `position`. If `position` is not an end-of-sequence iterator the operator sets result to the address of the current match.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0006 */}
Otherwise, if any of the values stored in subs is equal to `-1` and `prev->suffix().length()` is not `​0​` the operator sets `*this` to a suffix iterator that points to the range [`prev->suffix().first`, `prev->suffix().second`).

{/* source:cpp-regex-regex-token-iterator-operator-arith:0007 */}
Otherwise, sets `*this` to an end-of-sequence iterator.

{/* source:cpp-regex-regex-token-iterator-operator-arith:0008 */}
The behavior is undefined if the iterator is end-of-sequence iterator.

### Parameters

{/* source:cpp-regex-regex-token-iterator-operator-arith:0009 */}
(none)

### Return value

{/* source:cpp-regex-regex-token-iterator-operator-arith:0010 */}
1. 1) `*this`

{/* source:cpp-regex-regex-token-iterator-operator-arith:0011 */}
2. 2) The previous value of the iterator.
