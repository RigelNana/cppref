---
title: "Regular expressions library (since C++11)"
description: "The regular expressions library provides a class that represents regular expressions, which are a kind of mini-language used to perform pattern matching within strings. Almost all operations with regexes can be characterized by operating on several of the following objects:"
source_url: "https://en.cppreference.com/w/cpp/regex"
language: "C++"
---

{/* source:cpp-regex:0000 */}
The regular expressions library provides a class that represents [regular expressions](https://en.wikipedia.org/wiki/Regular_expression), which are a kind of mini-language used to perform pattern matching within strings. Almost all operations with regexes can be characterized by operating on several of the following objects:

{/* source:cpp-regex:0001 */}
- Target sequence. The character sequence that is searched for a pattern. This may be a range specified by two iterators, a null-terminated character string or a [`std::string`](/docs/cpp/library/string/basic_string).

{/* source:cpp-regex:0002 */}
- Pattern. This is the regular expression itself. It determines what constitutes a match. It is an object of type [`std::basic_regex`](/docs/cpp/library/text/regex/basic_regex), constructed from a string with special [`grammar`](/docs/cpp/library/text/regex#Regular_expression_grammars).

{/* source:cpp-regex:0003 */}
- Matched array. The information about matches may be retrieved as an object of type [`std::match_results`](/docs/cpp/library/text/regex/match_results).

{/* source:cpp-regex:0004 */}
- Replacement string. This is a string that determines how to replace the matches.

### Regular expression grammars

{/* source:cpp-regex:0005 */}
Patterns and replacement strings support the following regular expression grammars:

{/* source:cpp-regex:0006 */}
- [Modified ECMAScript regular expression grammar](/docs/cpp/library/text/regex/ecmascript). This is the default grammar.
- [Basic POSIX regular expression grammar](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap09.html#tag_09_03).
- [Extended POSIX regular expression grammar](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap09.html#tag_09_04).
- The regular expression grammar used by the [awk](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/awk.html#tag_20_06_13_04) utility in POSIX.
- The regular expression grammar used by the `grep` utility in POSIX. This is effectively the same as the basic POSIX regular expression grammar, with the addition of newline `'\n'` as an alternation separator.
- The regular expression grammar used by the `grep` utility, with the `-E` option, in POSIX. This is effectively the same as the extended POSIX regular expression grammar, with the addition of newline `'\n'` as an alternation separator in addition to '|'.

[`grep`](/docs/https://pubs.opengroup.org/onlinepubs/9699919799/utilities/grep.html)

{/* source:cpp-regex:0007 */}
Some grammar variations (such as case-insensitive matching) are also avaliable, see [this page](/docs/cpp/library/text/regex/basic_regex/constants) for details.

### Main classes

{/* source:cpp-regex:0008 */}
These classes encapsulate a regular expression and the results of matching a regular expression within a target sequence of characters.

{/* source:cpp-regex:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`basic_regex`](/docs/cpp/library/text/regex/basic_regex)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>regular expression object  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`sub_match`](/docs/cpp/library/text/regex/sub_match)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>identifies the sequence of characters matched by a sub-expression  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`match_results`](/docs/cpp/library/text/regex/match_results)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>identifies one regular expression match, including all sub-expression matches  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Algorithms

{/* source:cpp-regex:0010 */}
These functions are used to apply the regular expression encapsulated in a regex to a target sequence of characters.

{/* source:cpp-regex:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`regex_match`](/docs/cpp/library/text/regex/regex_match)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>attempts to match a regular expression to an entire character sequence  <br />  (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`regex_search`](/docs/cpp/library/text/regex/regex_search)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>attempts to match a regular expression to any part of a character sequence  <br />  (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`regex_replace`](/docs/cpp/library/text/regex/regex_replace)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>replaces occurrences of a regular expression with formatted replacement text  <br />  (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Iterators

{/* source:cpp-regex:0012 */}
The regex iterators are used to traverse the entire set of regular expression matches found within a sequence.

{/* source:cpp-regex:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`regex_iterator`](/docs/cpp/library/text/regex/regex_iterator)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>iterates through all regex matches within a character sequence  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`regex_token_iterator`](/docs/cpp/library/text/regex/regex_token_iterator)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>iterates through the specified sub-expressions within all regex matches in a given string or through unmatched substrings  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Exceptions

{/* source:cpp-regex:0014 */}
This class defines the type of objects thrown as exceptions to report errors from the regular expressions library.

{/* source:cpp-regex:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`regex_error`](/docs/cpp/library/text/regex/regex_error)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>reports errors generated by the regular expressions library  <br />  (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Traits

{/* source:cpp-regex:0016 */}
The regex traits class is used to encapsulate the localizable aspects of a regex.

{/* source:cpp-regex:0017 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`regex_traits`](/docs/cpp/library/text/regex/regex_traits)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>provides metainformation about a character type, required by the regex library  <br />  (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Constants

{/* source:cpp-regex:0018 */}
Defined in namespace `std::regex_constants`

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`syntax_option_type`](/docs/cpp/library/text/regex/syntax_option_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>general options controlling regex behavior  <br />  (typedef)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`match_flag_type`](/docs/cpp/library/text/regex/match_flag_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>options specific to matching  <br />  (typedef)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`error_type`](/docs/cpp/library/text/regex/error_type)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>describes different types of matching errors  <br />  (typedef)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Example

{/* source:cpp-regex:0019 */}
```cpp
#include <iostream>
#include <iterator>
#include <regex>
#include <string>
 
int main()
{
    std::string s = "Some people, when confronted with a problem, think "
        "\"I know, I'll use regular expressions.\" "
        "Now they have two problems.";
 
    std::regex self_regex("REGULAR EXPRESSIONS",
        std::regex_constants::ECMAScript | std::regex_constants::icase);
    if (std::regex_search(s, self_regex))
        std::cout << "Text contains the phrase 'regular expressions'\n";
 
    std::regex word_regex("(\\w+)");
    auto words_begin = 
        std::sregex_iterator(s.begin(), s.end(), word_regex);
    auto words_end = std::sregex_iterator();
 
    std::cout << "Found "
              << std::distance(words_begin, words_end)
              << " words\n";
 
    const int N = 6;
    std::cout << "Words longer than " << N << " characters:\n";
    for (std::sregex_iterator i = words_begin; i != words_end; ++i)
    {
        std::smatch match = *i;
        std::string match_str = match.str();
        if (match_str.size() > N)
            std::cout << "  " << match_str << '\n';
    }
 
    std::regex long_word_regex("(\\w{7,})");
    std::string new_s = std::regex_replace(s, long_word_regex, "[$&]");
    std::cout << new_s << '\n';
}
```
Output:
```text
Text contains the phrase 'regular expressions'
Found 20 words
Words longer than 6 characters:
  confronted
  problem
  regular
  expressions
  problems
Some people, when [confronted] with a [problem], think 
"I know, I'll use [regular] [expressions]." Now they have two [problems].
```
