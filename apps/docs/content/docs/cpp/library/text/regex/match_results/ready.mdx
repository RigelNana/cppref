---
title: "std::match_results<BidirIt,Alloc>::ready"
description: "Indicates if the match results are ready (valid) or not."
source_url: "https://en.cppreference.com/w/cpp/regex/match_results/ready"
language: "C++"
---

{/* source:cpp-regex-match-results-ready:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`bool ready() const;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-regex-match-results-ready:0001 */}
Indicates if the match results are ready (valid) or not.

{/* source:cpp-regex-match-results-ready:0002 */}
A default-constructed match result has no result state (is not ready), and can only be made ready by one of the regex algorithms. The ready state implies that all match results have been fully established.

{/* source:cpp-regex-match-results-ready:0003 */}
The result of calling most member functions of the `match_results` object that is not ready is undefined.

### Return value

{/* source:cpp-regex-match-results-ready:0004 */}
`true` if the match results are ready, `false` otherwise.

### Example

{/* source:cpp-regex-match-results-ready:0005 */}
```cpp
#include <iostream>
#include <regex>
#include <string>
 
int main()
{
    std::string target("big-red-cat");
    std::smatch sm;
    std::cout << "Default constructed smatch is "
              << (sm.ready() ? "ready.\n" : "not ready.\n");
 
    std::regex re1(".*-red-.*");
    std::regex_search(target, sm, re1);
 
    std::cout << "After search, smatch is "
              << (sm.ready() ? "ready.\n" : "not ready.\n");
}
```
Output:
```text
Default constructed smatch is not ready.
After search, smatch is ready.
```
