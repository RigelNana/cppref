---
title: "std::match_results<BidirIt,Alloc>::suffix"
description: "Obtains a reference to the std::sub_match object representing the target sequence between the end of the entire match of the regular expression and the end of the target sequence."
source_url: "https://en.cppreference.com/w/cpp/regex/match_results/suffix"
language: "C++"
---

{/* source:cpp-regex-match-results-suffix:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`const_reference suffix() const;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-regex-match-results-suffix:0001 */}
Obtains a reference to the [`std::sub_match`](/docs/cpp/library/text/regex/sub_match) object representing the target sequence between the end of the entire match of the regular expression and the end of the target sequence.

{/* source:cpp-regex-match-results-suffix:0002 */}
[`ready()`](/docs/cpp/library/text/regex/match_results/ready) must be `true`. Otherwise, the behavior is undefined.

### Parameters

{/* source:cpp-regex-match-results-suffix:0003 */}
(none)

### Return value

{/* source:cpp-regex-match-results-suffix:0004 */}
Reference to the unmatched suffix.

### Example

{/* source:cpp-regex-match-results-suffix:0005 */}
```cpp
#include <iostream>
#include <regex>
#include <string>
 
int main()
{
    std::regex re("a(a)*by");
    std::string target("baaaby123");
    std::smatch sm;
 
    std::regex_search(target, sm, re);
    std::cout << sm.suffix() << '\n';
}
```
Output:
```text
123
```

### See also

{/* source:cpp-regex-match-results-suffix:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`prefix`](/docs/cpp/library/text/regex/match_results/prefix)</DescriptionTerm>
    <DescriptionBody>returns sub-sequence between the beginning of the target sequence and the beginning of the full match  <br />  (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
