---
title: "std::regex_iterator<BidirIt,CharT,Traits>::regex_iterator"
description: "Constructs a new regex_iterator:"
source_url: "https://en.cppreference.com/w/cpp/regex/regex_iterator/regex_iterator"
language: "C++"
---

{/* source:cpp-regex-regex-iterator-regex-iterator:0000 */}
<DeclarationDoc>

  <Declaration id="1" language="cpp" since="C++11" code={`regex_iterator();`} />
  <Declaration id="2" language="cpp" since="C++11" code={`regex_iterator( BidirIt a, BidirIt b,
                const regex_type& re,
                std::regex_constants::match_flag_type m =

                    std::regex_constants::match_default );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`regex_iterator( const regex_iterator& );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`regex_iterator( BidirIt, BidirIt,
                const regex_type&&,
                std::regex_constants::match_flag_type =

                    std::regex_constants::match_default ) = delete;`} />
</DeclarationDoc>

{/* source:cpp-regex-regex-iterator-regex-iterator:0001 */}
Constructs a new `regex_iterator`:

{/* source:cpp-regex-regex-iterator-regex-iterator:0002 */}
1. 1) Default constructor. Constructs an end-of-sequence iterator.

{/* source:cpp-regex-regex-iterator-regex-iterator:0003 */}
2. 2) Constructs a `regex_iterator` from the sequence of characters `[``a``, ``b``)`, the regular expression `re`, and a flag `m` that governs matching behavior. This constructor performs an initial call to [`std::regex_search`](/docs/cpp/library/text/regex/regex_search) with this data. If the result of this initial call is `false`, `*this` is set to an end-of-sequence iterator.

{/* source:cpp-regex-regex-iterator-regex-iterator:0004 */}
3. 3) Copies a `regex_iterator`.

{/* source:cpp-regex-regex-iterator-regex-iterator:0005 */}
4. 4) The overload (2) is not allowed to be called with a temporary regex, since the returned iterator would be immediately invalidated.

### Parameters

{/* source:cpp-regex-regex-iterator-regex-iterator:0006 */}
<ParameterList>
  <Parameter name="a">[*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator) to the beginning of the target character sequence</Parameter>
  <Parameter name="b">[*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator) to the end of the target character sequence</Parameter>
  <Parameter name="re">regular expression used to search the target character sequence</Parameter>
  <Parameter name="m">flags that govern the behavior of `re`</Parameter>
</ParameterList>

### Example

{/* source:cpp-regex-regex-iterator-regex-iterator:0007 */}
```cpp
#include <iostream>
#include <regex>
#include <string_view>
 
int main()
{
    constexpr std::string_view str{R"(
        #ONE: *p = &Mass;
        #Two: MOV %rd, 42
    )"};
    const std::regex re("[a-w]");
 
    // create regex_iterator, overload (2)
    auto it = std::regex_iterator<std::string_view::iterator>
    {
        str.cbegin(), str.cend(),
        re // re is lvalue; if an immediate expression was used
           // instead, e.g. std::regex{"[a-z]"}, this would
           // produce an error since overload (4) is deleted
    };
 
    for (decltype(it) last /* overload (1) */; it != last; ++it)
        std::cout << (*it).str();
    std::cout << '\n';
}
```
Output:
```text
password
```

### Defect reports

{/* source:cpp-regex-regex-iterator-regex-iterator:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-regex-regex-iterator-regex-iterator:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2332} standard="C++11">
    <PublishedBehavior>a `regex_iterator` constructed from a temporary `basic_regex` became invalid immediately</PublishedBehavior>
    <CorrectedBehavior>such construction is disallowed via a deleted overload</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
