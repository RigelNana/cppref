---
title: "std::regex_token_iterator<BidirIt,CharT,Traits>::regex_token_iterator"
description: "Constructs a new regex_token_iterator:"
source_url: "https://en.cppreference.com/w/cpp/regex/regex_token_iterator/regex_token_iterator"
language: "C++"
---

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0000 */}
<DeclarationDoc>

  <Declaration id="1" language="cpp" since="C++11" code={`regex_token_iterator();`} />
  <Declaration id="2" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type& re,
                      int submatch = 0,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type& re,
                      const std::vector<int>& submatches,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type& re,
                      std::initializer_list<int> submatches,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default );`} />
  <Declaration id="5" language="cpp" since="C++11" code={`template< std::size_t N >
regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type& re,
                      const int (&submatches)[N],
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default );`} />
  <Declaration id="6" language="cpp" since="C++11" code={`regex_token_iterator( const regex_token_iterator& other );`} />
  <Declaration id="7" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type&& re,
                      int submatch = 0,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default ) = delete;`} />
  <Declaration id="8" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type&& re,
                      const std::vector<int>& submatches,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default ) = delete;`} />
  <Declaration id="9" language="cpp" since="C++11" code={`regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type&& re,
                      std::initializer_list<int> submatches,
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default ) = delete;`} />
  <Declaration id="10" language="cpp" since="C++11" code={`template< std::size_t N >
regex_token_iterator( BidirIt a, BidirIt b,
                      const regex_type&& re,
                      const int (&submatches)[N],
                      std::regex_constants::match_flag_type m =

                          std::regex_constants::match_default ) = delete;`} />
</DeclarationDoc>

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0001 */}
Constructs a new `regex_token_iterator`:

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0002 */}
1. 1) Default constructor. Constructs the end-of-sequence iterator.

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0003 */}
2. 2-5) First, copies the list of the requested submatch out of the `submatches` or `submatch` argument into the member list stored in the iterator and constructs the member [`std::regex_iterator`](/docs/cpp/library/text/regex/regex_iterator) by passing `a`, `b`, `re`, and `m` to its four-argument constructor (that constructor performs the initial call to [`std::regex_search`](/docs/cpp/library/text/regex/regex_search)) and sets the internal counter of `submatches` to zero.
   -  If, after construction, the member `regex_iterator` is not an end-of-sequence iterator, sets the member pointer to the address of the current [`std::sub_match`](/docs/cpp/library/text/regex/sub_match).
   
   -  Otherwise (if the member `regex_iterator` is an end-of-sequence iterator), but the value `-1` is one of the values in `submatches`/`submatch`, turns `*this` into a *suffix iterator* pointing at the range `[``a``, ``b``)` (the entire string is the non-matched suffix).
   
   -  Otherwise (if `-1` is not in the list of `submatches`), turns `*this` into the end-of-sequence iterator.

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0004 */}
The behavior is undefined if any value in `submatches` is less than `-1`.

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0005 */}
1. 6) Copy constructor: performs member-wise copy (including making a copy of the member `regex_iterator` and the member pointer to current `sub_match`).

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0006 */}
2. 7-10) The overloads (2-5) are prohibited from being called with a temporary regex since otherwise the returned iterator would be immediately invalidated.

### Parameters

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0007 */}
<ParameterList>
  <Parameter name="a">[*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator) to the beginning of the target character sequence</Parameter>
  <Parameter name="b">[*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator) to the end of the target character sequence</Parameter>
  <Parameter name="re">regular expression used to search the target character sequence</Parameter>
  <Parameter name="submatch">the index of the submatch that should be returned. "0" represents the entire match, and "-1" represents the parts that are not matched (e.g, the stuff between matches)</Parameter>
  <Parameter name="submatches">the sequence of submatch indices that should be iterated over within each match, may include the special value `-1` for the non-matched fragments</Parameter>
  <Parameter name="m">flags that govern the behavior of `re`</Parameter>
</ParameterList>

### Example

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0008 */}
This section is incomplete Reason: no example

### Defect reports

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0009 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-regex-regex-token-iterator-regex-token-iterator:0010 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2332} standard="C++11">
    <PublishedBehavior>a `regex_token_iterator` constructed from a temporary `basic_regex` became invalid immediately</PublishedBehavior>
    <CorrectedBehavior>such construction is disallowed via deleted overloads</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
