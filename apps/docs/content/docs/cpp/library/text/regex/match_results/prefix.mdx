---
title: "std::match_results<BidirIt,Alloc>::prefix"
description: "Obtains a reference to the std::sub_match object representing the target sequence between the start of the beginning of the target sequence and the start of the entire match of the regular expression."
source_url: "https://en.cppreference.com/w/cpp/regex/match_results/prefix"
language: "C++"
---

{/* source:cpp-regex-match-results-prefix:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`const_reference prefix() const;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-regex-match-results-prefix:0001 */}
Obtains a reference to the [`std::sub_match`](/docs/cpp/library/text/regex/sub_match) object representing the target sequence between the start of the beginning of the target sequence and the start of the entire match of the regular expression.

{/* source:cpp-regex-match-results-prefix:0002 */}
[`ready()`](/docs/cpp/library/text/regex/match_results/ready) must be `true`. Otherwise, the behavior is undefined.

### Return value

{/* source:cpp-regex-match-results-prefix:0003 */}
Reference to the unmatched prefix.

### Example

{/* source:cpp-regex-match-results-prefix:0004 */}
```cpp
#include <iostream>
#include <regex>
#include <string>
 
int main()
{
    std::regex re("a(a)*b");
    std::string target("baaaby");
    std::smatch sm;
 
    std::regex_search(target, sm, re);
    std::cout << sm.prefix().str() << '\n';
}
```
Output:
```text
b
```

### See also

{/* source:cpp-regex-match-results-prefix:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`suffix`](/docs/cpp/library/text/regex/match_results/suffix)</DescriptionTerm>
    <DescriptionBody>returns sub-sequence between the end of the full match and the end of the target sequence  <br />  (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
