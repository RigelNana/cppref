---
title: "std::sub_match<BidirIt>::compare"
description: "A value less than zero if this sub_match is less than the other character sequence, zero if the both underlying character sequences are equal, greater than zero if this sub_match is greater than the other character sequence."
source_url: "https://en.cppreference.com/w/cpp/regex/sub_match/compare"
language: "C++"
---

{/* source:cpp-regex-sub-match-compare:0000 */}
<DeclarationDoc>

  <Declaration id="1" language="cpp" since="C++11" code={`int compare( const sub_match& m ) const;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`int compare( const string_type& s ) const;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`int compare( const value_type* c ) const;`} />

  <DeclarationDescription>
    {/* source:cpp-regex-sub-match-compare:0001 */}
    1. 1) Compares two `sub_matches` directly by comparing their underlying character sequences. Equivalent to `str().compare(m.str())`.

    {/* source:cpp-regex-sub-match-compare:0002 */}
    2. 2) Compares a `sub_match` with a [`std::basic_string`](/docs/cpp/library/string/basic_string). Equivalent to `str().compare(s)`.

    {/* source:cpp-regex-sub-match-compare:0003 */}
    3. 3) Compares a `sub_match` with a null-terminated sequence of the underlying character type pointed to by `s`. Equivalent to `str().compare(c)`.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-regex-sub-match-compare:0004 */}
<ParameterList>
  <Parameter name="m">a reference to another sub_match</Parameter>
  <Parameter name="s">a reference to a string to compare to</Parameter>
  <Parameter name="c">a pointer to a null-terminated character sequence of the underlying `value_type` to compare to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-regex-sub-match-compare:0005 */}
A value less than zero if this `sub_match` is *less* than the other character sequence, zero if the both underlying character sequences are equal, greater than zero if this `sub_match` is *greater* than the other character sequence.

### Notes

{/* source:cpp-regex-sub-match-compare:0006 */}
This function is infrequently used directly by application code. Instead, one of the non-member comparison operators is used.

### Example

{/* source:cpp-regex-sub-match-compare:0007 */}
This section is incomplete Reason: no example

### See also

{/* source:cpp-regex-sub-match-compare:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`compare`](/docs/cpp/library/string/basic_string/compare)</DescriptionTerm>
    <DescriptionBody>compares two strings <br /> (public member function of `std::basic_string<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`str operator string_type`](/docs/cpp/library/text/regex/sub_match/str)</DescriptionTerm>
    <DescriptionBody>converts to the underlying string type <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator== operator!= operator< operator<= operator> operator>= operator<=>`](/docs/cpp/library/text/regex/sub_match/operator_cmp)` `<InlineRevision until="C++20" />` `<InlineRevision until="C++20" />` `<InlineRevision until="C++20" />` `<InlineRevision until="C++20" />` `<InlineRevision until="C++20" />` `<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>compares a `sub_match` with another `sub_match`, a string, or a character <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
