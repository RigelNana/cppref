---
title: "std::regex_traits<CharT>::imbue"
description: "Replaces the current locale with a copy of loc. If loc is different than the current locale, then all cached data is invalidated."
source_url: "https://en.cppreference.com/w/cpp/regex/regex_traits/imbue"
language: "C++"
---

{/* source:cpp-regex-regex-traits-imbue:0000 */}
<DeclarationDoc>

  <Declaration language="cpp" since="C++11" code={`locale_type imbue( locale_type loc );`} />
</DeclarationDoc>

{/* source:cpp-regex-regex-traits-imbue:0001 */}
Replaces the current locale with a copy of `loc`. If `loc` is different than the current locale, then all cached data is invalidated.

{/* source:cpp-regex-regex-traits-imbue:0002 */}
After the call `getloc() == loc`.

### Parameters

{/* source:cpp-regex-regex-traits-imbue:0003 */}
<ParameterList>
  <Parameter name="loc">the locale to imbue</Parameter>
</ParameterList>

### Return value

{/* source:cpp-regex-regex-traits-imbue:0004 */}
The current locale of the traits object.

{/* source:cpp-regex-regex-traits-imbue:0005 */}
If `imbue()` has been never called for this object, then the global locale at the time of the call is returned. Otherwise, the locale passed to the last call to `imbue()` is returned.

### Example

{/* source:cpp-regex-regex-traits-imbue:0006 */}
This section is incomplete Reason: no example

### See also

{/* source:cpp-regex-regex-traits-imbue:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`getloc`](/docs/cpp/library/text/regex/regex_traits/getloc)</DescriptionTerm>
    <DescriptionBody>gets the locale <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
