---
title: "std::pmr::monotonic_buffer_resource::do_allocate"
description: "Allocates storage."
source_url: "https://en.cppreference.com/w/cpp/memory/monotonic_buffer_resource/do_allocate"
language: "C++"
---

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`virtual void* do_allocate( std::size_t bytes, std::size_t alignment );`} />
</DeclarationDoc>

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0001 */}
Allocates storage.

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0002 */}
If the current buffer has sufficient unused space to fit a block with the specified size and alignment, allocates the return block from the current buffer.

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0003 */}
Otherwise, this function allocates a new buffer by calling `upstream_resource()->allocate(n, m)`, where `n` is not less than the greater of `bytes` and the next buffer size and `m` is not less than `alignment`. It sets the new buffer as the current buffer, increases the next buffer size by an implementation-defined growth factor (which is not necessarily integral), and then allocates the return block from the newly allocated buffer.

### Return value

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0004 */}
A pointer to allocated storage of at least `bytes` bytes in size, aligned to the specified `alignment` if such alignment is supported, and to `alignof(std::max_align_t)` otherwise.

### Exceptions

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0005 */}
Throws nothing unless calling `allocate()` on the upstream memory resource throws.

### See also

{/* source:cpp-memory-monotonic-buffer-resource-do-allocate:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`allocate`](/docs/cpp/library/memory/memory_resource/allocate)</DescriptionTerm>
    <DescriptionBody>allocates memory (public member function of `std::pmr::memory_resource`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_allocate`](/docs/cpp/library/memory/memory_resource/do_allocate) [virtual]</DescriptionTerm>
    <DescriptionBody>allocates memory (virtual private member function of `std::pmr::memory_resource`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
