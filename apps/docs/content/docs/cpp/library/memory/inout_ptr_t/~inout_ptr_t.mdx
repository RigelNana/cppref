---
title: "std::inout_ptr_t<Smart,Pointer,Args...>::~inout_ptr_t"
description: "Resets the adapted Smart object by the value of modified Pointer object (or the void* object if operator void**() has been called) and the captured arguments."
source_url: "https://en.cppreference.com/w/cpp/memory/inout_ptr_t/~inout_ptr_t"
language: "C++"
---

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`~inout_ptr_t();`} />
</DeclarationDoc>

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0001 */}
Resets the adapted `Smart` object by the value of modified `Pointer` object (or the `void*` object if `operator void**()` has been called) and the captured arguments. `release()` may be called on the adapted `Smart` object if it is not called by the constructor.

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0002 */}
Let

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0003 */}
- `s` denotes the adapted `Smart` object,
- `args...` denotes the captured arguments,
- `p` denotes the value of stored `Pointer`, or `static_cast<Pointer>(*operator void**())` if `operator void**` has been called,
- `SP` be
  - `Smart::pointer`, if it is valid and denotes a type, otherwise,
  - `Smart::element_type*`, if `Smart::element_type` is valid and denotes a type, otherwise,
  - `std::pointer_traits<Smart>::element_type*`, if `std::pointer_traits<Smart>::element_type` is valid and denotes a type, otherwise,
  - `Pointer`,
- `/*do-release*/` denotes `s.release()` if the [constructor](/docs/cpp/library/memory/inout_ptr_t/inout_ptr_t) does not call `release()`, empty otherwise.

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0004 */}
If `Smart` is a pointer type, the destructor performs

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0005 */}
`s = static_cast<Smart>(p);`, and the program is ill-formed if `sizeof...(Args) > 0`;

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0006 */}
otherwise, if `s.reset(static_cast<SP>(p), std::forward<Args>(args)...)` is well-formed, the destructor performs

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0007 */}
```cpp
/*do-release*/; if (p) { s.reset(static_cast<SP>(p), std::forward<Args>(args)...); }
```

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0008 */}
otherwise, if `std::is_constructible_v<Smart, SP, Args...>` is `true`, the destructor performs

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0009 */}
```cpp
/*do-release*/; if (p) { s = Smart(static_cast<SP>(p), std::forward<Args>(args)...); }
```

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0010 */}
otherwise, the program is ill-formed.

### Notes

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0011 */}
The implementation may allocate the storage for the data structure needed for `Smart` (e.g. a control block) on construction, in order to leave non-throwing works to the destructor.

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0012 */}
Arguments captured by value are destroyed after resetting.

### Defect reports

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-inout-ptr-t-inout-ptr-t:0014 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3897} standard="C++23">
    <PublishedBehavior>the destructor did not update a raw pointer to the null value</PublishedBehavior>
    <CorrectedBehavior>it does</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
