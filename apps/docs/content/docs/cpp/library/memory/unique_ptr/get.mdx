---
title: "std::unique_ptr<T,Deleter>::get"
description: "Returns a pointer to the managed object or nullptr if no object is owned."
source_url: "https://en.cppreference.com/w/cpp/memory/unique_ptr/get"
language: "C++"
---

{/* source:cpp-memory-unique-ptr-get:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`pointer get() const noexcept;`} />
  <InlineRevision since="C++23">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-memory-unique-ptr-get:0001 */}
Returns a pointer to the managed object or `nullptr` if no object is owned.

### Parameters

{/* source:cpp-memory-unique-ptr-get:0002 */}
(none)

### Return value

{/* source:cpp-memory-unique-ptr-get:0003 */}
Pointer to the managed object or `nullptr` if no object is owned.

### Example

{/* source:cpp-memory-unique-ptr-get:0004 */}
```cpp
#include <iomanip>
#include <iostream>
#include <memory>
#include <string>
#include <utility>
 
class Res
{
    std::string s;
 
public:
    Res(std::string arg) : s{std::move(arg)}
    {
        std::cout << "Res::Res(" << std::quoted(s) << ");\n";
    }
 
    ~Res()
    {
        std::cout << "Res::~Res();\n";
    }
 
private:
    friend std::ostream& operator<<(std::ostream& os, Res const& r)
    {
        return os << "Res { s = " << std::quoted(r.s) << "; }";
    }
};
 
int main()
{
    std::unique_ptr<Res> up(new Res{"Hello, world!"});
    Res* res = up.get();
    std::cout << *res << '\n';
}
```

Output:

```text
Res::Res("Hello, world!");
Res { s = "Hello, world!"; }
Res::~Res();
```

### See also

{/* source:cpp-memory-unique-ptr-get:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`release`](/docs/cpp/library/memory/unique_ptr/release)</DescriptionTerm>
    <DescriptionBody>returns a pointer to the managed object and releases the ownership (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
