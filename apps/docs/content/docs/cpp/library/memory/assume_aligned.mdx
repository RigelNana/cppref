---
title: "std::assume_aligned"
description: "Informs the implementation that the object ptr points to is aligned to at least N."
source_url: "https://en.cppreference.com/w/cpp/memory/assume_aligned"
language: "C++"
---

{/* source:cpp-memory-assume-aligned:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

  <Declaration language="cpp" since="C++20" code={`template< std::size_t N, class T >
constexpr T* assume_aligned( T* ptr );`} />
</DeclarationDoc>

{/* source:cpp-memory-assume-aligned:0001 */}
Informs the implementation that the object `ptr` points to is aligned to at least `N`. The implementation may use this information to generate more efficient code, but it might only make this assumption if the object is accessed via the return value of `assume_aligned`.

{/* source:cpp-memory-assume-aligned:0002 */}
`N` must be a power of 2. The behavior is undefined if `ptr` does not point to an object of type `T` (ignoring cv-qualification at every level), or if the object's alignment is not at least `N`.

### Return value

{/* source:cpp-memory-assume-aligned:0003 */}
`ptr`.

### Exceptions

{/* source:cpp-memory-assume-aligned:0004 */}
Throws nothing.

### Notes

{/* source:cpp-memory-assume-aligned:0005 */}
To ensure that the program benefits from the optimizations enabled by `assume_aligned`, it is important to access the object via its return value:

{/* source:cpp-memory-assume-aligned:0006 */}
```cpp
void f(int* p)
{
    int* p1 = std::assume_aligned<256>(p);
    // Use p1, not p, to ensure benefit from the alignment assumption.
    // However, the program has undefined behavior if p is not aligned
    // regardless of whether p1 is used.
}
```

{/* source:cpp-memory-assume-aligned:0007 */}
It is up to the program to ensure that the alignment assumption actually holds. A call to `assume_aligned` does not cause the compiler to verify or enforce this.

{/* source:cpp-memory-assume-aligned:0008 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_assume_aligned`](/docs/cpp/library/feature_test#cpp_lib_assume_aligned) | [`201811L`](/docs/cpp/library/compiler_support/20#cpp_lib_assume_aligned_201811L) | (C++20) | `std::assume_aligned` |

### Example

{/* source:cpp-memory-assume-aligned:0009 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-memory-assume-aligned:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`alignof`](/docs/cpp/language/expressions/alignof) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>queries alignment requirements of a type (operator)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`alignas`](/docs/cpp/language/declarations/alignas) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>specifies that the storage for the variable should be aligned by specific amount (specifier)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`aligned_storage`](/docs/cpp/library/meta/aligned_storage) <InlineRevision since="C++11" /> (deprecated in C++23)</DescriptionTerm>
    <DescriptionBody>defines the type suitable for use as uninitialized storage for types of given size (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`align`](/docs/cpp/library/memory/align) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>aligns a pointer in a buffer (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[[`assume`](/docs/cpp/language/declarations/attributes/assume)(*expression*)]] <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>specifies that the *expression* will always evaluate to `true` at a given point (attribute specifier)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
