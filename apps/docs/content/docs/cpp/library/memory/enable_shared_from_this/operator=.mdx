---
title: "std::enable_shared_from_this<T>::operator="
description: "Does nothing; returns *this."
source_url: "https://en.cppreference.com/w/cpp/memory/enable_shared_from_this/operator="
language: "C++"
---

{/* source:cpp-memory-enable-shared-from-this-operator:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`enable_shared_from_this& operator=( const enable_shared_from_this &rhs ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-memory-enable-shared-from-this-operator:0001 */}
Does nothing; returns `*this`.

### Parameters

{/* source:cpp-memory-enable-shared-from-this-operator:0002 */}
<ParameterList>
  <Parameter name="rhs">another `enable_shared_from_this` to assign to `*this`</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-enable-shared-from-this-operator:0003 */}
`*this`

### Notes

{/* source:cpp-memory-enable-shared-from-this-operator:0004 */}
[`weak_this`](/docs/cpp/library/memory/enable_shared_from_this#weak_this) is not affected by the assignment.

### Example

{/* source:cpp-memory-enable-shared-from-this-operator:0005 */}
Note: `enable_shared_from_this::operator=` is defined as protected in order to prevent accidental slicing but allow derived classes to have default assignment operators.

{/* source:cpp-memory-enable-shared-from-this-operator:0006 */}
```cpp
#include <iostream>
#include <memory>
 
class SharedInt : public std::enable_shared_from_this<SharedInt>
{
public:
    explicit SharedInt(int n) : mNumber(n) {}
    SharedInt(const SharedInt&) = default;
    SharedInt(SharedInt&&) = default;
    ~SharedInt() = default;
 
    // Both assignment operators use enable_shared_from_this::operator=
    SharedInt& operator=(const SharedInt&) = default;
    SharedInt& operator=(SharedInt&&) = default;
 
    int number() const { return mNumber; }
 
private:
    int mNumber;
};
 
int main()
{
    std::shared_ptr<SharedInt> a = std::make_shared<SharedInt>(2);
    std::shared_ptr<SharedInt> b = std::make_shared<SharedInt>(4);
    *a = *b;
 
    std::cout << a->number() << '\n';
}
```

Output:

```text
4
```

### See also

{/* source:cpp-memory-enable-shared-from-this-operator:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`shared_ptr`](/docs/cpp/library/memory/shared_ptr) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>smart pointer with shared object ownership semantics (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
