---
title: "std::auto_ptr<T>::auto_ptr"
description: "Constructs the auto_ptr from a pointer that refers to the object to manage."
source_url: "https://en.cppreference.com/w/cpp/memory/auto_ptr/auto_ptr"
language: "C++"
---

{/* source:cpp-memory-auto-ptr-auto-ptr:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" removed="C++17" code={`explicit auto_ptr( X* p = 0 ) throw();`} /> (deprecated in C++11)
  <Declaration id="2" language="cpp" removed="C++17" code={`auto_ptr( auto_ptr& r ) throw();`} /> (deprecated in C++11)
  <Declaration id="3" language="cpp" removed="C++17" code={`template< class Y >
auto_ptr( auto_ptr<Y>& r ) throw();`} /> (deprecated in C++11)
  <Declaration id="4" language="cpp" removed="C++17" code={`auto_ptr( auto_ptr_ref<X> m ) throw();`} /> (deprecated in C++11)
</DeclarationDoc>

{/* source:cpp-memory-auto-ptr-auto-ptr:0001 */}
Constructs the `auto_ptr` from a pointer that refers to the object to manage.

{/* source:cpp-memory-auto-ptr-auto-ptr:0002 */}
1. 1) Constructs the `auto_ptr` with pointer `p`.

{/* source:cpp-memory-auto-ptr-auto-ptr:0003 */}
2. 2) Constructs the `auto_ptr` with the pointer held in `r`. `r.release()` is called to acquire the ownership of the object.

{/* source:cpp-memory-auto-ptr-auto-ptr:0004 */}
3. 3) Same as (2). `Y*` must be implicitly convertible to `T*`.

{/* source:cpp-memory-auto-ptr-auto-ptr:0005 */}
4. 4) Constructs the `auto_ptr` with the pointer held in the `auto_ptr` instance referred to by `m`. `p.release()` is called for the `auto_ptr` `p` that `m` holds to acquire the ownership of the object.

{/* source:cpp-memory-auto-ptr-auto-ptr:0006 */}
`auto_ptr_ref` is an implementation-defined type that holds a reference to `auto_ptr`. [`std::auto_ptr`](/docs/cpp/library/memory/auto_ptr) is implicitly [convertible to](/docs/cpp/library/memory/auto_ptr/operator_auto_ptr) and [assignable from](/docs/cpp/library/memory/auto_ptr/operator_assignment) this type. The implementation is allowed to provide the template with a different name or implement equivalent functionality in other ways.

### Parameters

{/* source:cpp-memory-auto-ptr-auto-ptr:0007 */}
<ParameterList>
  <Parameter name="p">a pointer to an object to manage</Parameter>
  <Parameter name="r">another `auto_ptr` to transfer the ownership of the object from</Parameter>
  <Parameter name="m">an implementation-defined type that holds a reference to `auto_ptr`</Parameter>
</ParameterList>

### Notes

{/* source:cpp-memory-auto-ptr-auto-ptr:0008 */}
The constructor and the copy assignment operator from `auto_ptr_ref` is provided to allow copy-constructing and assigning `std::auto_ptr` from nameless temporaries. Since its copy constructor and copy assignment operator take the argument as non-const reference, they cannot bind rvalue arguments directly. However, a [user-defined conversion](/docs/cpp/library/memory/auto_ptr/operator_auto_ptr) can be executed (which releases the original `auto_ptr`), followed by a call to the constructor or copy-assignment operator that take `auto_ptr_ref` by value. This is an early implementation of [move semantics](/docs/cpp/library/utility/move).
