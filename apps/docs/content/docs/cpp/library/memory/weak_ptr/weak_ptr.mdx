---
title: "std::weak_ptr<T>::weak_ptr"
description: "Constructs new weak_ptr that potentially shares an object with r."
source_url: "https://en.cppreference.com/w/cpp/memory/weak_ptr/weak_ptr"
language: "C++"
---

{/* source:cpp-memory-weak-ptr-weak-ptr:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`constexpr weak_ptr() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`weak_ptr( const weak_ptr& r ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class Y > 
weak_ptr( const weak_ptr<Y>& r ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class Y > 
weak_ptr( const std::shared_ptr<Y>& r ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`weak_ptr( weak_ptr&& r ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< class Y > 
weak_ptr( weak_ptr<Y>&& r ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-memory-weak-ptr-weak-ptr:0001 */}
Constructs new `weak_ptr` that potentially shares an object with `r`.

{/* source:cpp-memory-weak-ptr-weak-ptr:0002 */}
1. 1) Default constructor. Constructs empty `weak_ptr`.

{/* source:cpp-memory-weak-ptr-weak-ptr:0003 */}
2. 2) Constructs new `weak_ptr` which shares an object managed by `r`. If `r` manages no object, `*this` manages no object too. The templated overloads don't participate in the overload resolution unless `Y*` is implicitly convertible to `T*`<InlineRevision since="C++17">, or `Y` is the type "array of `N` `U`" for some type `U` and some number `N`, and `T` is the type "array of unknown bound of (possibly cv-qualified) `U`"</InlineRevision>.

{/* source:cpp-memory-weak-ptr-weak-ptr:0004 */}
3. 3) Move constructors. Moves a `weak_ptr` instance from `r` into `*this`. After this, `r` is empty and `r.use_count() == 0`. The templated overload doesn't participate in the overload resolution unless `Y*` is implicitly convertible to `T*`.

### Parameters

{/* source:cpp-memory-weak-ptr-weak-ptr:0005 */}
<ParameterList>
  <Parameter name="r">a [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr) or [`std::weak_ptr`](/docs/cpp/library/memory/weak_ptr) that will be viewed by this [`std::weak_ptr`](/docs/cpp/library/memory/weak_ptr)</Parameter>
</ParameterList>

### Notes

{/* source:cpp-memory-weak-ptr-weak-ptr:0006 */}
Because the default constructor is `constexpr`, static [`std::weak_ptrs`](/docs/cpp/library/memory/weak_ptr) are initialized as part of [static non-local initialization](/docs/cpp/language/initialization#Non-local_variables), before any dynamic non-local initialization begins. This makes it safe to use a [`std::weak_ptr`](/docs/cpp/library/memory/weak_ptr) in a constructor of any static object.

### Example

{/* source:cpp-memory-weak-ptr-weak-ptr:0007 */}
```cpp
#include <iostream>
#include <memory>
 
struct Foo {};
 
int main()
{
    std::weak_ptr<Foo> w_ptr;
 
    {
        auto ptr = std::make_shared<Foo>();
        w_ptr = ptr;
        std::cout << "w_ptr.use_count() inside scope: " << w_ptr.use_count() << '\n';
    }
 
    std::cout << "w_ptr.use_count() out of scope: " << w_ptr.use_count() << '\n';
    std::cout << "w_ptr.expired() out of scope: "
              << std::boolalpha << w_ptr.expired() << '\n';
}
```

Output:

```text
w_ptr.use_count() inside scope: 1
w_ptr.use_count() out of scope: 0
w_ptr.expired() out of scope: true
```

### Defect reports

{/* source:cpp-memory-weak-ptr-weak-ptr:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-weak-ptr-weak-ptr:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2315} standard="C++11">
    <PublishedBehavior>move semantic was not enabled for `weak_ptr`</PublishedBehavior>
    <CorrectedBehavior>enabled</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-memory-weak-ptr-weak-ptr:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/memory/weak_ptr/operator_assignment)</DescriptionTerm>
    <DescriptionBody>assigns the `weak_ptr` (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
