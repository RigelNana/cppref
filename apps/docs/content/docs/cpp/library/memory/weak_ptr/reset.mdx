---
title: "std::weak_ptr<T>::reset"
description: "Releases the reference to the managed object."
source_url: "https://en.cppreference.com/w/cpp/memory/weak_ptr/reset"
language: "C++"
---

{/* source:cpp-memory-weak-ptr-reset:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void reset() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-memory-weak-ptr-reset:0001 */}
Releases the reference to the managed object. After the call `*this` manages no object.

### Parameters

{/* source:cpp-memory-weak-ptr-reset:0002 */}
(none)

### Return value

{/* source:cpp-memory-weak-ptr-reset:0003 */}
(none)

### Example

{/* source:cpp-memory-weak-ptr-reset:0004 */}
```cpp
#include <iostream>
#include <memory>
 
int main()
{
    auto shared = std::make_shared<int>(), shared2 = shared, shared3 = shared2;
 
    auto weak = std::weak_ptr<int>{shared};
 
    std::cout << std::boolalpha 
              << "shared.use_count(): " << shared.use_count() << '\n'
              << "weak.use_count(): " << weak.use_count() << '\n'
              << "weak.expired(): " << weak.expired() << '\n';
 
    weak.reset();
 
    std::cout << "weak.reset();\n"
              << "shared.use_count(): " << shared.use_count() << '\n'
              << "weak.use_count(): " << weak.use_count() << '\n'
              << "weak.expired(): " << weak.expired() << '\n';
}
```

Output:

```text
shared.use_count(): 3
weak.use_count(): 3
weak.expired(): false
weak.reset();
shared.use_count(): 3
weak.use_count(): 0
weak.expired(): true
```

### See also

{/* source:cpp-memory-weak-ptr-reset:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`expired`](/docs/cpp/library/memory/weak_ptr/expired)</DescriptionTerm>
    <DescriptionBody>checks whether the referenced object was already deleted (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
