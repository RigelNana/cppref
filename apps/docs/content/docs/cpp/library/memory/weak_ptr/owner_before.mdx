---
title: "std::weak_ptr<T>::owner_before"
description: "Checks whether this weak_ptr precedes other in implementation defined owner-based (as opposed to value-based) order."
source_url: "https://en.cppreference.com/w/cpp/memory/weak_ptr/owner_before"
language: "C++"
---

{/* source:cpp-memory-weak-ptr-owner-before:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`template< class Y > 
bool owner_before( const weak_ptr<Y>& other ) const noexcept;`} />
  <Declaration language="cpp" code={`template< class Y >
bool owner_before( const std::shared_ptr<Y>& other ) const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-memory-weak-ptr-owner-before:0001 */}
Checks whether this `weak_ptr` precedes `other` in implementation defined owner-based (as opposed to value-based) order. The order is such that two smart pointers compare equivalent only if they are both empty or if they both own the same object, even if the values of the pointers obtained by `get()` are different (e.g. because they point at different subobjects within the same object).

{/* source:cpp-memory-weak-ptr-owner-before:0002 */}
This ordering is used to make shared and weak pointers usable as keys in associative containers, typically through [`std::owner_less`](/docs/cpp/library/memory/owner_less).

### Parameters

{/* source:cpp-memory-weak-ptr-owner-before:0003 */}
<ParameterList>
  <Parameter name="other">the [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr) or [`std::weak_ptr`](/docs/cpp/library/memory/weak_ptr) to be compared</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-weak-ptr-owner-before:0004 */}
`true` if `*this` precedes `other`, `false` otherwise. Common implementations compare the addresses of the control blocks.

### Example

{/* source:cpp-memory-weak-ptr-owner-before:0005 */}
```cpp
#include <iostream>
#include <memory>
 
struct Foo
{
    int n1;
    int n2; 
    Foo(int a, int b) : n1(a), n2(b) {}
};
 
int main()
{   
    auto p1 = std::make_shared<Foo>(1, 2);
    std::shared_ptr<int> p2(p1, &p1->n1);
    std::shared_ptr<int> p3(p1, &p1->n2);
 
    std::cout << std::boolalpha
              << "p2 < p3 " << (p2 < p3) << '\n'
              << "p3 < p2 " << (p3 < p2) << '\n'
              << "p2.owner_before(p3) " << p2.owner_before(p3) << '\n'
              << "p3.owner_before(p2) " << p3.owner_before(p2) << '\n';
 
    std::weak_ptr<int> w2(p2);
    std::weak_ptr<int> w3(p3);
    std::cout 
//            << "w2 < w3 " << (w2 < w3) << '\n' // won't compile 
//            << "w3 < w2 " << (w3 < w2) << '\n' // won't compile
              << "w2.owner_before(w3) " << w2.owner_before(w3) << '\n'
              << "w3.owner_before(w2) " << w3.owner_before(w2) << '\n';
}
```

Output:

```text
p2 < p3 true
p3 < p2 false
p2.owner_before(p3) false
p3.owner_before(p2) false
w2.owner_before(w3) false
w3.owner_before(w2) false
```

### Defect reports

{/* source:cpp-memory-weak-ptr-owner-before:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-weak-ptr-owner-before:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2083} standard="C++11">
    <PublishedBehavior>`owner_before` was not declared `const`</PublishedBehavior>
    <CorrectedBehavior>declared `const`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2942} standard="C++11">
    <PublishedBehavior>`owner_before` was not declared `noexcept`</PublishedBehavior>
    <CorrectedBehavior>declared `noexcept`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-memory-weak-ptr-owner-before:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`owner_less`](/docs/cpp/library/memory/owner_less) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>provides mixed-type owner-based ordering of shared and weak pointers (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
