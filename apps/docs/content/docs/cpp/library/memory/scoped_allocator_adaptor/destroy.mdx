---
title: "std::scoped_allocator_adaptor<OuterAlloc,InnerAlloc...>::destroy"
description: "Uses the outer allocator to call the destructor of the object pointed to by p, by calling std::allocator_traits<OUTERMOST>::destroy(OUTERMOST(*this), p)."
source_url: "https://en.cppreference.com/w/cpp/memory/scoped_allocator_adaptor/destroy"
language: "C++"
---

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0000 */}
<DeclarationDoc>
Defined in header [`<scoped_allocator>`](/docs/cpp/library/headers/scoped_allocator)

  <Declaration language="cpp" since="C++11" code={`template< class T >
void destroy( T* p );`} />
</DeclarationDoc>

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0001 */}
Uses the outer allocator to call the destructor of the object pointed to by `p`, by calling

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0002 */}
`std::allocator_traits<OUTERMOST>::destroy(OUTERMOST(*this), p)`

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0003 */}
where `OUTERMOST` is the type that would be returned by calling `this->outer_allocator()`, and then calling the `outer_allocator()` member function recursively on the result of this call until reaching the type that has no such member function.

## Parameters

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0004 */}
<ParameterList>
  <Parameter name="p">pointer to the object that is going to be destroyed</Parameter>
</ParameterList>

## Return value

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0005 */}
(none)

## See also

{/* source:cpp-memory-scoped-allocator-adaptor-destroy:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`destroy`](/docs/cpp/library/memory/allocator_traits/destroy)</DescriptionTerm>
    <DescriptionBody>destructs an object stored in the allocated storage (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`destroy`](/docs/cpp/library/memory/allocator/destroy) <InlineRevision until="C++20" /></DescriptionTerm>
    <DescriptionBody>destructs an object in allocated storage (public member function of `std::allocator<T>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
