---
title: "std::pmr::unsynchronized_pool_resource::unsynchronized_pool_resource"
description: "Constructs an unsynchronized_pool_resource."
source_url: "https://en.cppreference.com/w/cpp/memory/unsynchronized_pool_resource/unsynchronized_pool_resource"
language: "C++"
---

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`unsynchronized_pool_resource();`} />
  <Declaration id="2" language="cpp" since="C++17" code={`explicit unsynchronized_pool_resource( std::pmr::memory_resource* upstream );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`explicit unsynchronized_pool_resource( const std::pmr::pool_options& opts );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`unsynchronized_pool_resource( const std::pmr::pool_options& opts,
                              std::pmr::memory_resource* upstream );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`unsynchronized_pool_resource( const unsynchronized_pool_resource& ) = delete;`} />
</DeclarationDoc>

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0001 */}
Constructs an `unsynchronized_pool_resource`.

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0002 */}
1. 1-4) Constructs an `unsynchronized_pool_resource` using the specified upstream memory resource and tuned according to the specified options. The resulting object holds a copy of `upstream` but does not own the resource to which `upstream` points.

   The overloads not taking `opts` as a parameter uses a default constructed instance of [`pool_options`](/docs/cpp/library/memory/pool_options) as the options. The overloads not taking `upstream` as a parameter use the return value of `std::pmr::get_default_resource()` as the upstream memory resource.

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0003 */}
2. 5) Copy constructor is deleted.

### Parameters

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0004 */}
<ParameterList>
  <Parameter name="opts">a [`std::pmr::pool_options`](/docs/cpp/library/memory/pool_options) struct containing the constructor options</Parameter>
  <Parameter name="upstream">the upstream memory resource to use</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-memory-unsynchronized-pool-resource-unsynchronized-pool-resource:0005 */}
1. 1-4) Throws only if a call to the `allocate()` function of the upstream resource throws. It is unspecified if or under what conditions such a call takes place.
