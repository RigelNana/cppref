---
title: "std::pmr::unsynchronized_pool_resource::do_allocate"
description: "Allocates storage."
source_url: "https://en.cppreference.com/w/cpp/memory/unsynchronized_pool_resource/do_allocate"
language: "C++"
---

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`virtual void* do_allocate( std::size_t bytes, std::size_t alignment );`} />
</DeclarationDoc>

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0001 */}
Allocates storage.

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0002 */}
If the pool selected for a block of size `bytes` is unable to satisfy the request from its internal data structures, calls `allocate()` on the upstream memory resource to obtain memory.

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0003 */}
If the size requested is larger than what the largest pool can handle, memory is allocated by calling `allocate()` on the upstream memory resource.

### Return value

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0004 */}
A pointer to allocated storage of at least `bytes` bytes in size, aligned to the specified `alignment` if such alignment is supported, and to `alignof(std::max_align_t)` otherwise.

### Exceptions

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0005 */}
Throws nothing unless calling `allocate()` on the upstream memory resource throws.

### See also

{/* source:cpp-memory-unsynchronized-pool-resource-do-allocate:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`allocate`](/docs/cpp/library/memory/memory_resource/allocate)</DescriptionTerm>
    <DescriptionBody>allocates memory (public member function of `std::pmr::memory_resource`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`do_allocate`](/docs/cpp/library/memory/memory_resource/do_allocate) [virtual]</DescriptionTerm>
    <DescriptionBody>allocates memory (virtual private member function of `std::pmr::memory_resource`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
