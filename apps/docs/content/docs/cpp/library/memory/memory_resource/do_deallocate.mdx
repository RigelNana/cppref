---
title: "std::pmr::memory_resource::do_deallocate"
description: "Deallocates the storage pointed to by p. p must have been returned by a prior call to allocate(bytes, alignment) on a memory_resource that compares equal to *this, and the storage it points to must not yet have been deallocated, otherwise the behavior is undefined."
source_url: "https://en.cppreference.com/w/cpp/memory/memory_resource/do_deallocate"
language: "C++"
---

{/* source:cpp-memory-memory-resource-do-deallocate:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`virtual void do_deallocate( void* p, std::size_t bytes, std::size_t alignment ) = 0;`} />
</DeclarationDoc>

{/* source:cpp-memory-memory-resource-do-deallocate:0001 */}
Deallocates the storage pointed to by `p`.

{/* source:cpp-memory-memory-resource-do-deallocate:0002 */}
`p` must have been returned by a prior call to `allocate(bytes, alignment)` on a `memory_resource` that compares equal to `*this`, and the storage it points to must not yet have been deallocated, otherwise the behavior is undefined.

### Exceptions

{/* source:cpp-memory-memory-resource-do-deallocate:0003 */}
Throws nothing.

### See also

{/* source:cpp-memory-memory-resource-do-deallocate:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`deallocate`](/docs/cpp/library/memory/memory_resource/deallocate)</DescriptionTerm>
    <DescriptionBody>deallocates memory (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
