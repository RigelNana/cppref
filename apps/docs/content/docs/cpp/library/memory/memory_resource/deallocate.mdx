---
title: "std::pmr::memory_resource::deallocate"
description: "Deallocates the storage pointed to by p. p shall have been returned by a prior call to allocate(bytes, alignment) on a memory_resource that compares equal to *this, and the storage it points to shall not yet have been deallocated."
source_url: "https://en.cppreference.com/w/cpp/memory/memory_resource/deallocate"
language: "C++"
---

{/* source:cpp-memory-memory-resource-deallocate:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`void deallocate( void* p,
                 std::size_t bytes,

                 std::size_t alignment = alignof(std::max_align_t) );`} />
</DeclarationDoc>

{/* source:cpp-memory-memory-resource-deallocate:0001 */}
Deallocates the storage pointed to by `p`. `p` shall have been returned by a prior call to `allocate(bytes, alignment)` on a `memory_resource` that compares equal to `*this`, and the storage it points to shall not yet have been deallocated.

{/* source:cpp-memory-memory-resource-deallocate:0002 */}
Equivalent to `do_deallocate(p, bytes, alignment);`.

### Exceptions

{/* source:cpp-memory-memory-resource-deallocate:0003 */}
Throws nothing.

### See also

{/* source:cpp-memory-memory-resource-deallocate:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`do_deallocate`](/docs/cpp/library/memory/memory_resource/do_deallocate) [virtual]</DescriptionTerm>
    <DescriptionBody>deallocates memory (virtual private member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
