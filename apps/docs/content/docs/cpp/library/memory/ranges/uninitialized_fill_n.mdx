---
title: "std::ranges::uninitialized_fill_n"
description: "Copies value to an uninitialized memory area first + [0, count)."
source_url: "https://en.cppreference.com/w/cpp/memory/ranges/uninitialized_fill_n"
language: "C++"
---
{/* source:cpp-memory-ranges-uninitialized-fill-n:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< no-throw-forward-range I, class T >
    requires std::constructible_from<std::iter_value_t<I>, const T&>
I uninitialized_fill_n( I first, std::iter_difference_t<I> count,

                        const T& value );`} />
  <InlineRevision since="C++26">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-memory-ranges-uninitialized-fill-n:0001 */}
Copies `value` to an uninitialized memory area `first + [`​0​`, `count`)` as if by

`return ranges::uninitialized_fill(std::counted_iterator(first, count),`<br />`                                  std::default_sentinel, value).base();`

{/* source:cpp-memory-ranges-uninitialized-fill-n:0002 */}
If an exception is thrown during the initialization, the objects already constructed are destroyed in an unspecified order.

{/* source:cpp-memory-ranges-uninitialized-fill-n:0003 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-memory-ranges-uninitialized-fill-n:0004 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-memory-ranges-uninitialized-fill-n:0005 */}
<ParameterList>
  <Parameter name="first">the beginning of the range of the elements to initialize</Parameter>
  <Parameter name="count">number of elements to construct</Parameter>
  <Parameter name="value">the value to construct the elements with</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-ranges-uninitialized-fill-n:0006 */}
As described above.

### Complexity

{/* source:cpp-memory-ranges-uninitialized-fill-n:0007 */}
Linear in `count`.

### Exceptions

{/* source:cpp-memory-ranges-uninitialized-fill-n:0008 */}
Any exception thrown on construction of the elements in the destination range.

### Notes

{/* source:cpp-memory-ranges-uninitialized-fill-n:0009 */}
An implementation may improve the efficiency of the `ranges::uninitialized_fill_n`, e.g. by using [`ranges::fill_n`](/docs/cpp/library/algorithm/ranges/fill_n), if the value type of the output range is [*TrivialType*](/docs/cpp/library/named_req/TrivialType).

### Notes

{/* source:cpp-memory-ranges-uninitialized-fill-n:0010 */}
| [`Feature-test`](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_raw_memory_algorithms`](/docs/cpp/library/feature_test#cpp_lib_raw_memory_algorithms) | [`202411L`](/docs/cpp/library/compiler_support/26#cpp_lib_raw_memory_algorithms_202411L) | (C++26) | `constexpr` for [specialized memory algorithms](/docs/cpp/library/memory#Uninitialized_memory_algorithms) |

### Possible implementation

{/* source:cpp-memory-ranges-uninitialized-fill-n:0011 */}
```cpp
struct uninitialized_fill_n_fn
{
    template<no-throw-forward-range I, class T>
    requires std::constructible_from<std::iter_value_t<I>, const T&>
    I operator()(I first, std::iter_difference_t<I> n, const T& x) const
    {
        I rollback{first};
        try
        {
            for (; n-- > 0; ++first)
                ranges::construct_at(std::addressof(*first), x);
            return first;
        }
        catch (...) // rollback: destroy constructed elements
        {
            for (; rollback != first; ++rollback)
                ranges::destroy_at(std::addressof(*rollback));
            throw;
        }
    }
};
 
inline constexpr uninitialized_fill_n_fn uninitialized_fill_n{};
```

### Example

{/* source:cpp-memory-ranges-uninitialized-fill-n:0012 */}
```cpp
#include <iostream>
#include <memory>
#include <string>
 
int main()
{
    constexpr int n{3};
    alignas(alignof(std::string)) char out[n * sizeof(std::string)];
 
    try
    {
        auto first{reinterpret_cast<std::string*>(out)};
        auto last = std::ranges::uninitialized_fill_n(first, n, "cppreference");
 
        for (auto it{first}; it != last; ++it)
            std::cout << *it << '\n';
 
        std::ranges::destroy(first, last);
    }
    catch (...)
    {
        std::cout << "Exception!\n";
    }
}
```

Output:

```text
cppreference
cppreference
cppreference
```

### Defect reports

{/* source:cpp-memory-ranges-uninitialized-fill-n:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-ranges-uninitialized-fill-n:0014 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3870} standard="C++20">
    <PublishedBehavior>this algorithm might create objects on a `const` storage</PublishedBehavior>
    <CorrectedBehavior>kept disallowed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-memory-ranges-uninitialized-fill-n:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::uninitialized_fill`](/docs/cpp/library/memory/ranges/uninitialized_fill) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>copies an object to an uninitialized area of memory, defined by a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`uninitialized_fill_n`](/docs/cpp/library/memory/uninitialized_fill_n)</DescriptionTerm>
    <DescriptionBody>copies an object to an uninitialized area of memory, defined by a start and a count (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
