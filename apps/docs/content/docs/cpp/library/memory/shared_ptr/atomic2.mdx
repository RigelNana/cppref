---
title: "std::atomic<std::shared_ptr>"
description: "The partial template specialization of std::atomic for std::shared_ptr<T> allows users to manipulate shared_ptr objects atomically."
source_url: "https://en.cppreference.com/w/cpp/memory/shared_ptr/atomic2"
language: "C++"
---

{/* source:cpp-memory-shared-ptr-atomic2:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

  <Declaration language="cpp" since="C++20" code={`template< class T >
struct std::atomic<std::shared_ptr<T>>;`} />
</DeclarationDoc>

{/* source:cpp-memory-shared-ptr-atomic2:0001 */}
The partial template specialization of [`std::atomic`](/docs/cpp/library/atomic/atomic) for `std::shared_ptr<T>` allows users to manipulate `shared_ptr` objects atomically.

{/* source:cpp-memory-shared-ptr-atomic2:0002 */}
If multiple threads of execution access the same [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr) object without synchronization and any of those accesses uses a non-const member function of `shared_ptr` then a data race will occur unless all such access is performed through an instance of `std::atomic<std::shared_ptr>` (or, deprecated as of C++20, through the [standalone functions](/docs/cpp/library/memory/shared_ptr/atomic) for atomic access to [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr)).

{/* source:cpp-memory-shared-ptr-atomic2:0003 */}
Associated `use_count` increments are guaranteed to be part of the atomic operation. Associated `use_count` decrements are sequenced after the atomic operation, but are not required to be part of it, except for the `use_count` change when overriding `expected` in a failed CAS. Any associated deletion and deallocation are sequenced after the atomic update step and are not part of the atomic operation.

{/* source:cpp-memory-shared-ptr-atomic2:0004 */}
Note that the control block of a `shared_ptr` is thread-safe: different non-atomic [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr) objects can be accessed using mutable operations, such as `operator=` or `reset`, simultaneously by multiple threads, even when these instances are copies, and share the same control block internally.

{/* source:cpp-memory-shared-ptr-atomic2:0005 */}
The type `T` may be an incomplete type.

### Member types

{/* source:cpp-memory-shared-ptr-atomic2:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`value_type`</DescriptionTerm>
    <DescriptionBody>`std::shared_ptr<T>`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-memory-shared-ptr-atomic2:0007 */}
All non-specialized [`std::atomic`](/docs/cpp/library/atomic/atomic) functions are also provided by this specialization, and no additional member functions.


{/* source:cpp-memory-shared-ptr-atomic2:0008 */}
## atomic\<shared_ptr\<T\>\>::atomic

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`constexpr atomic() noexcept = default;`} />
  <Declaration id="2" language="cpp" code={`constexpr atomic( std::nullptr_t ) noexcept : atomic() {}`} />
  <Declaration id="3" language="cpp" code={`atomic( std::shared_ptr<T> desired ) noexcept;`} />
  <Declaration id="4" language="cpp" code={`atomic( const atomic& ) = delete;`} />
</DeclarationDoc>

1,2) Initializes the underlying `shared_ptr<T>` to the null value.

3) Initializes the underlying `shared_ptr<T>` to a copy of `desired`. As with any [`std::atomic`](/docs/cpp/library/atomic/atomic) type, initialization is not an atomic operation.

4) Atomic types are not copy/move constructible.

{/* source:cpp-memory-shared-ptr-atomic2:0009 */}
## atomic\<shared_ptr\<T\>\>::operator=

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`void operator=( const atomic& ) = delete;`} />
  <Declaration id="2" language="cpp" code={`void operator=( std::shared_ptr<T> desired ) noexcept;`} />
  <Declaration id="3" language="cpp" code={`void operator=( std::nullptr_t ) noexcept;`} />
</DeclarationDoc>

1) Atomic types are not copy/move assignable.

2) Value assignment, equivalent to `store(desired)`.

3) Resets the atomic shared pointer to null pointer value. Equivalent to `store(nullptr);`.

{/* source:cpp-memory-shared-ptr-atomic2:0010 */}
## atomic\<shared_ptr\<T\>\>::is_lock_free

<DeclarationDoc>
  <Declaration language="cpp" code={`bool is_lock_free() const noexcept;`} />
</DeclarationDoc>

Returns `true` if the atomic operations on all objects of this type are lock-free, `false` otherwise.

{/* source:cpp-memory-shared-ptr-atomic2:0011 */}
## atomic\<shared_ptr\<T\>\>::store

<DeclarationDoc>
  <Declaration language="cpp" code={`void store( std::shared_ptr<T> desired,
            std::memory_order order = std::memory_order_seq_cst ) noexcept;`} />
</DeclarationDoc>

Atomically replaces the value of `*this` with the value of `desired` as if by `p.swap(desired)` where `p` is the underlying `std::shared_ptr<T>`. Memory is ordered according to `order`. The behavior is undefined if `order` is [`std::memory_order_consume`](/docs/cpp/library/atomic/memory_order), [`std::memory_order_acquire`](/docs/cpp/library/atomic/memory_order), or [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order).

{/* source:cpp-memory-shared-ptr-atomic2:0012 */}
## atomic\<shared_ptr\<T\>\>::load

<DeclarationDoc>
  <Declaration language="cpp" code={`std::shared_ptr<T> load( std::memory_order order = std::memory_order_seq_cst ) const noexcept;`} />
</DeclarationDoc>

Atomically returns a copy of the underlying shared pointer. Memory is ordered according to `order`. The behavior is undefined if `order` is [`std::memory_order_release`](/docs/cpp/library/atomic/memory_order) or [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order).

{/* source:cpp-memory-shared-ptr-atomic2:0013 */}
## atomic\<shared_ptr\<T\>\>::operator std::shared_ptr\<T\>

<DeclarationDoc>
  <Declaration language="cpp" code={`operator std::shared_ptr<T>() const noexcept;`} />
</DeclarationDoc>

Equivalent to `return load();`.

{/* source:cpp-memory-shared-ptr-atomic2:0014 */}
## atomic\<shared_ptr\<T\>\>::exchange

<DeclarationDoc>
  <Declaration language="cpp" code={`std::shared_ptr<T> exchange( std::shared_ptr<T> desired,
                             std::memory_order order = std::memory_order_seq_cst ) noexcept;`} />
</DeclarationDoc>

Atomically replaces the underlying `std::shared_ptr<T>` with `desired` as if by `p.swap(desired)` where `p` is the underlying `std::shared_ptr<T>` and returns a copy of the value that `p` had immediately before the swap. Memory is ordered according to `order`. This is an atomic read-modify-write operation.

{/* source:cpp-memory-shared-ptr-atomic2:0015 */}
## atomic\<shared_ptr\<T\>\>::compare_exchange_weak, compare_exchange_strong

<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`bool compare_exchange_strong( std::shared_ptr<T>& expected, std::shared_ptr<T> desired,
                              std::memory_order success, std::memory_order failure ) noexcept;`} />
  <Declaration id="2" language="cpp" code={`bool compare_exchange_weak( std::shared_ptr<T>& expected, std::shared_ptr<T> desired,
                            std::memory_order success, std::memory_order failure ) noexcept;`} />
  <Declaration id="3" language="cpp" code={`bool compare_exchange_strong( std::shared_ptr<T>& expected, std::shared_ptr<T> desired,
                              std::memory_order order = std::memory_order_seq_cst ) noexcept;`} />
  <Declaration id="4" language="cpp" code={`bool compare_exchange_weak( std::shared_ptr<T>& expected, std::shared_ptr<T> desired,
                            std::memory_order order = std::memory_order_seq_cst ) noexcept;`} />
</DeclarationDoc>

1) If the underlying `std::shared_ptr<T>` stores the same `T*` as `expected` and shares ownership with it, or if both underlying and `expected` are empty, assigns from `desired` to the underlying `std::shared_ptr<T>`, returns `true`, and orders memory according to `success`, otherwise assigns from the underlying `std::shared_ptr<T>` to `expected`, returns `false`, and orders memory according to `failure`. The behavior is undefined if `failure` is [`std::memory_order_release`](/docs/cpp/library/atomic/memory_order) or [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order). On success, the operation is an atomic read-modify-write operation on `*this` and `expected` is not accessed after the atomic update. On failure, the operation is an atomic load operation on `*this` and `expected` is updated with the existing value read from the atomic object. This update to `expected`'s `use_count` is part of this atomic operation, although the write itself (and any subsequent deallocation/destruction) is not required to be.

2) Same as (1), but may also fail spuriously.

3) Equivalent to: `return compare_exchange_strong(expected, desired, order, fail_order);`, where `fail_order` is the same as `order` except that [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order) is replaced by [`std::memory_order_acquire`](/docs/cpp/library/atomic/memory_order) and [`std::memory_order_release`](/docs/cpp/library/atomic/memory_order) is replaced by [`std::memory_order_relaxed`](/docs/cpp/library/atomic/memory_order).

4) Equivalent to: `return compare_exchange_weak(expected, desired, order, fail_order);`, where `fail_order` is the same as `order` except that [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order) is replaced by [`std::memory_order_acquire`](/docs/cpp/library/atomic/memory_order) and [`std::memory_order_release`](/docs/cpp/library/atomic/memory_order) is replaced by [`std::memory_order_relaxed`](/docs/cpp/library/atomic/memory_order).

{/* source:cpp-memory-shared-ptr-atomic2:0016 */}
## atomic\<shared_ptr\<T\>\>::wait

<DeclarationDoc>
  <Declaration language="cpp" code={`void wait( std::shared_ptr<T> old,
           std::memory_order order = std::memory_order_seq_cst ) const noexcept;`} />
</DeclarationDoc>

Performs an atomic waiting operation.

Compares `load(order)` with `old` and if they are equivalent then blocks until `*this` is notified by `notify_one()` or `notify_all()`. This is repeated until `load(order)` changes. This function is guaranteed to return only if value has changed, even if underlying implementation unblocks spuriously.

Memory is ordered according to `order`. The behavior is undefined if `order` is [`std::memory_order_release`](/docs/cpp/library/atomic/memory_order) or [`std::memory_order_acq_rel`](/docs/cpp/library/atomic/memory_order).

Notes: two `shared_ptrs` are equivalent if they store the same pointer and either share ownership or are both empty.

{/* source:cpp-memory-shared-ptr-atomic2:0017 */}
## atomic\<shared_ptr\<T\>\>::notify_one

<DeclarationDoc>
  <Declaration language="cpp" code={`void notify_one() noexcept;`} />
</DeclarationDoc>

Performs an atomic notifying operation.

If there is a thread blocked in atomic waiting operations (i.e. `wait()`) on `*this`, then unblocks at least one such thread; otherwise does nothing.

{/* source:cpp-memory-shared-ptr-atomic2:0018 */}
## atomic\<shared_ptr\<T\>\>::notify_all

<DeclarationDoc>
  <Declaration language="cpp" code={`void notify_all() noexcept;`} />
</DeclarationDoc>

Performs an atomic notifying operation.

Unblocks all threads blocked in atomic waiting operations (i.e. `wait()`) on `*this`, if there are any; otherwise does nothing.


### Member constants

{/* source:cpp-memory-shared-ptr-atomic2:0019 */}
The only standard [`std::atomic`](/docs/cpp/library/atomic/atomic) member constant `is_always_lock_free` is also provided by this specialization.


{/* source:cpp-memory-shared-ptr-atomic2:0020 */}
## atomic\<shared_ptr\<T\>\>::is_always_lock_free

<DeclarationDoc>
  <Declaration language="cpp" code={`static constexpr bool is_always_lock_free = /*implementation-defined*/;`} />
</DeclarationDoc>


### Notes

{/* source:cpp-memory-shared-ptr-atomic2:0021 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_atomic_shared_ptr`](/docs/cpp/library/feature_test#cpp_lib_atomic_shared_ptr) | [`201711L`](/docs/cpp/library/compiler_support/20#cpp_lib_atomic_shared_ptr_201711L) | (C++20) | [`std::atomic<std::shared_ptr>`](/docs/cpp/library/memory/shared_ptr/atomic2#top) |

### Example

{/* source:cpp-memory-shared-ptr-atomic2:0022 */}
This section is incomplete

Reason: no example

### Defect reports

{/* source:cpp-memory-shared-ptr-atomic2:0023 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-shared-ptr-atomic2:0024 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3661} standard="C++20">
    <PublishedBehavior>`atomic<shared_ptr<T>>` was not constant-initializable from `nullptr`</PublishedBehavior>
    <CorrectedBehavior>made constant-initializable</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3893} standard="C++20">
    <PublishedBehavior>[LWG3661](https://cplusplus.github.io/LWG/issue3661) made `atomic<shared_ptr<T>>` not assignable from `nullptr_t`</PublishedBehavior>
    <CorrectedBehavior>assignability restored</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-memory-shared-ptr-atomic2:0025 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`atomic`](/docs/cpp/library/atomic/atomic) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>atomic class template and specializations for bool, integral,<InlineRevision since="C++20"> floating-point,</InlineRevision> and pointer types (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
