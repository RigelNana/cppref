---
title: "std::shared_ptr<T>::operator="
description: "Replaces the managed object with the one managed by r."
source_url: "https://en.cppreference.com/w/cpp/memory/shared_ptr/operator_assignment"
language: "C++"
---

{/* source:cpp-memory-shared-ptr-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`shared_ptr& operator=( const shared_ptr& r ) noexcept;`} />
  <Declaration id="2" language="cpp" code={`template< class Y > 
shared_ptr& operator=( const shared_ptr<Y>& r ) noexcept;`} />
  <Declaration id="3" language="cpp" code={`shared_ptr& operator=( shared_ptr&& r ) noexcept;`} />
  <Declaration id="4" language="cpp" code={`template< class Y > 
shared_ptr& operator=( shared_ptr<Y>&& r ) noexcept;`} />
  <Declaration id="5" language="cpp" until="C++17" code={`template< class Y > 
shared_ptr& operator=( std::auto_ptr<Y>&& r );`} /> (deprecated in C++11)
  <Declaration id="6" language="cpp" code={`template< class Y, class Deleter > 
shared_ptr& operator=( std::unique_ptr<Y, Deleter>&& r );`} />
</DeclarationDoc>

{/* source:cpp-memory-shared-ptr-operator:0001 */}
Replaces the managed object with the one managed by `r`.

{/* source:cpp-memory-shared-ptr-operator:0002 */}
If `*this` already owns an object and it is the last `shared_ptr` owning it, and `r` is not the same as `*this`, the object is destroyed through the owned deleter.

{/* source:cpp-memory-shared-ptr-operator:0003 */}
1. 1,2) Shares ownership of the object managed by `r`. If `r` manages no object, `*this` manages no object too. Equivalent to `shared_ptr<T>(r).swap(*this)`.

{/* source:cpp-memory-shared-ptr-operator:0004 */}
2. 3,4) Move-assigns a `shared_ptr` from `r`. After the assignment, `*this` contains a copy of the previous state of `r`, and `r` is empty. Equivalent to `shared_ptr<T>(std::move(r)).swap(*this)`.

{/* source:cpp-memory-shared-ptr-operator:0005 */}
3. 5) Transfers the ownership of the object managed by `r` to `*this`. If `r` manages no object, `*this` manages no object too. After the assignment, `*this` contains the pointer previously held by `r`, and `use_count() == 1`; also `r` is empty. Equivalent to `shared_ptr<T>(r).swap(*this)`.

{/* source:cpp-memory-shared-ptr-operator:0006 */}
4. 6) Transfers the ownership of the object managed by `r` to `*this`. The deleter associated to `r` is stored for future deletion of the managed object. `r` manages no object after the call. Equivalent to `shared_ptr<T>(std::move(r)).swap(*this)`.

### Parameters

{/* source:cpp-memory-shared-ptr-operator:0007 */}
<ParameterList>
  <Parameter name="r">another smart pointer to share the ownership to or acquire the ownership from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-shared-ptr-operator:0008 */}
`*this`

### Notes

{/* source:cpp-memory-shared-ptr-operator:0009 */}
The implementation may meet the requirements without creating a temporary `shared_ptr` object.

### Exceptions

{/* source:cpp-memory-shared-ptr-operator:0010 */}
5,6) May throw implementation-defined exceptions.

### Example

{/* source:cpp-memory-shared-ptr-operator:0011 */}
This section is incomplete Reason: no example

### See also

{/* source:cpp-memory-shared-ptr-operator:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`reset`](/docs/cpp/library/memory/shared_ptr/reset)</DescriptionTerm>
    <DescriptionBody>replaces the managed object (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
