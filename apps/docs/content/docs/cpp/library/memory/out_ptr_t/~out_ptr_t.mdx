---
title: "std::out_ptr_t<Smart,Pointer,Args...>::~out_ptr_t"
description: "Resets the adapted Smart object by the value of modified Pointer object (or the void* object if operator void**() has been called) and the captured arguments."
source_url: "https://en.cppreference.com/w/cpp/memory/out_ptr_t/~out_ptr_t"
language: "C++"
---
{/* source:cpp-memory-out-ptr-t-out-ptr-t:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++23" code={`~out_ptr_t();`} />
</DeclarationDoc>

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0001 */}
Resets the adapted `Smart` object by the value of modified `Pointer` object (or the `void*` object if `operator void**()` has been called) and the captured arguments.

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0002 */}
Let

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0003 */}
- `s` denotes the adapted `Smart` object,
- `args...` denotes the captured arguments,
- `p` denotes the value of stored `Pointer`, or `static_cast<Pointer>(*operator void**())` if `operator void**` has been called,
- `SP` be
  - `Smart::pointer`, if it is valid and denotes a type, otherwise,
  - `Smart::element_type*`, if `Smart::element_type` is valid and denotes a type, otherwise,
  - `std::pointer_traits<Smart>::element_type*`, if `std::pointer_traits<Smart>::element_type` is valid and denotes a type, otherwise,
  - `Pointer`.

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0004 */}
If `s.reset(static_cast<SP>(p), std::forward<Args>(args)...)` is well-formed, the destructor performs

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0005 */}
`if (p) s.reset(static_cast<SP>(p), std::forward<Args>(args)...);`,

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0006 */}
otherwise, if `std::is_constructible_v<Smart, SP, Args...>` is `true`, the destructor performs

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0007 */}
`if (p) s = Smart(static_cast<SP>(p), std::forward<Args>(args)...);`,

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0008 */}
otherwise, the program is ill-formed.

### Notes

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0009 */}
If `Smart` is a [`std::shared_ptr`](/docs/cpp/library/memory/shared_ptr) specialization, the implementation may allocate the storage for the new control block on construction, in order to leave non-throwing works to the destructor.

{/* source:cpp-memory-out-ptr-t-out-ptr-t:0010 */}
Arguments captured by value are destroyed after resetting.
