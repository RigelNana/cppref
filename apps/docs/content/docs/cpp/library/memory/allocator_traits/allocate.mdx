---
title: "std::allocator_traits<Alloc>::allocate"
description: "Uses the allocator a to allocate n * sizeof(Alloc::value_type) bytes of uninitialized storage."
source_url: "https://en.cppreference.com/w/cpp/memory/allocator_traits/allocate"
language: "C++"
---

{/* source:cpp-memory-allocator-traits-allocate:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

  <Declaration id="1" language="cpp" since="C++11" code={`static pointer allocate( Alloc& a, size_type n );`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
  <Declaration id="2" language="cpp" since="C++11" code={`static pointer allocate( Alloc& a, size_type n, const_void_pointer hint );`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-memory-allocator-traits-allocate:0001 */}
Uses the allocator `a` to allocate `n * sizeof(Alloc::value_type)` bytes of uninitialized storage. An array of type `Alloc::value_type[n]` is created in the storage, but none of its elements are constructed.

{/* source:cpp-memory-allocator-traits-allocate:0002 */}
1. 1) Calls `a.allocate(n)`.

{/* source:cpp-memory-allocator-traits-allocate:0003 */}
2. 2) Additionally passes memory locality hint `hint`. Calls `a.allocate(n, hint)` if possible. If not possible (e.g. `a` has no two-argument member function `allocate`), calls `a.allocate(n)`.

### Parameters

{/* source:cpp-memory-allocator-traits-allocate:0004 */}
<ParameterList>
  <Parameter name="a">allocator to use</Parameter>
  <Parameter name="n">the number of objects to allocate storage for</Parameter>
  <Parameter name="hint">pointer to a nearby memory location</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-allocator-traits-allocate:0005 */}
The pointer returned by the call to `a.allocate(n)`.

### Notes

{/* source:cpp-memory-allocator-traits-allocate:0006 */}
`Alloc::allocate` was not required to create array object until [P0593R6](https://wg21.link/P0593R6), which made using non-default allocator for [`std::vector`](/docs/cpp/library/container/vector) and some other containers not well-defined according to a strict reading of the core language specification.

{/* source:cpp-memory-allocator-traits-allocate:0007 */}
After calling `allocate` and before construction of elements, pointer arithmetic of `Alloc::value_type*` is well-defined within the allocated array, but the behavior is undefined if elements are accessed.

### Example

{/* source:cpp-memory-allocator-traits-allocate:0008 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-memory-allocator-traits-allocate:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`allocate`](/docs/cpp/library/memory/allocator/allocate)</DescriptionTerm>
    <DescriptionBody>allocates uninitialized storage (public member function of `std::allocator<T>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
