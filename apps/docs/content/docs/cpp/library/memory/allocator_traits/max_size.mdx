---
title: "std::allocator_traits<Alloc>::max_size"
description: "If possible, obtains the maximum theoretically possible allocation size from the allocator a, by calling a.max_size()."
source_url: "https://en.cppreference.com/w/cpp/memory/allocator_traits/max_size"
language: "C++"
---

{/* source:cpp-memory-allocator-traits-max-size:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

  <Declaration language="cpp" since="C++11" code={`static size_type max_size( const Alloc& a ) noexcept;`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-memory-allocator-traits-max-size:0001 */}
If possible, obtains the maximum theoretically possible allocation size from the allocator `a`, by calling `a.max_size()`.

{/* source:cpp-memory-allocator-traits-max-size:0002 */}
If the above is not possible (e.g., `Alloc` does not have the member function `max_size()`), then returns `std::numeric_limits<size_type>::max() / sizeof(value_type)`.

### Parameters

{/* source:cpp-memory-allocator-traits-max-size:0003 */}
<ParameterList>
  <Parameter name="a">allocator to detect</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-allocator-traits-max-size:0004 */}
Theoretical maximum allocation size.

### Defect reports

{/* source:cpp-memory-allocator-traits-max-size:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-memory-allocator-traits-max-size:0006 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2162} standard="C++11">
    <PublishedBehavior>`max_size` was not required to be noexcept</PublishedBehavior>
    <CorrectedBehavior>required</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={2466} standard="C++11">
    <PublishedBehavior>theoretical maximum allocation size in bytes was returned as fallback</PublishedBehavior>
    <CorrectedBehavior>size in elements is returned</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### Example

{/* source:cpp-memory-allocator-traits-max-size:0007 */}
```cpp
#include <iostream>
#include <memory>
#include <locale>
 
int main()
{
    std::allocator<short> b;
    std::allocator<int> d;
 
    const auto p = std::allocator_traits<decltype(b)>::max_size(b);
    const auto q = std::allocator_traits<decltype(d)>::max_size(d);
 
    std::cout.imbue(std::locale("en_US.UTF-8"));
    std::cout << std::uppercase
              << "p = " << std::dec << p << " = 0x" << std::hex << p << '\n'
              << "q = " << std::dec << q << " = 0x" << std::hex << q << '\n';
}
```

Possible output:

```text
p = 9,223,372,036,854,775,807 = 0x7,FFF,FFF,FFF,FFF,FFF
q = 4,611,686,018,427,387,903 = 0x3,FFF,FFF,FFF,FFF,FFF
```

### See also

{/* source:cpp-memory-allocator-traits-max-size:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`max_size`](/docs/cpp/library/memory/allocator/max_size) <InlineRevision until="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the largest supported allocation size (public member function of `std::allocator<T>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
