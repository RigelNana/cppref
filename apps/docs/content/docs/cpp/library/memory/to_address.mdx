---
title: "std::to_address"
description: "Obtain the address represented by p without forming a reference to the object pointed to by p."
source_url: "https://en.cppreference.com/w/cpp/memory/to_address"
language: "C++"
---

{/* source:cpp-memory-to-address:0000 */}
<DeclarationDoc>
Defined in header [`<memory>`](/docs/cpp/library/headers/memory)

  <Declaration id="1" language="cpp" since="C++20" code={`template< class Ptr >
constexpr auto to_address( const Ptr& p ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< class T >
constexpr T* to_address( T* p ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-memory-to-address:0001 */}
Obtain the address represented by `p` without forming a reference to the object pointed to by `p`.

{/* source:cpp-memory-to-address:0002 */}
1. 1) [Fancy pointer](/docs/cpp/library/named_req/Allocator#Fancy_pointers) overload: If the expression `std::pointer_traits<Ptr>::to_address(p)` is well-formed, returns the result of that expression. Otherwise, returns `std::to_address(p.operator->())`.

{/* source:cpp-memory-to-address:0003 */}
2. 2) Raw pointer overload: If `T` is a function type, the program is ill-formed. Otherwise, returns `p` unmodified.

### Parameters

{/* source:cpp-memory-to-address:0004 */}
<ParameterList>
  <Parameter name="p">fancy or raw pointer</Parameter>
</ParameterList>

### Return value

{/* source:cpp-memory-to-address:0005 */}
Raw pointer that represents the same address as `p` does.

### Possible implementation

{/* source:cpp-memory-to-address:0006 */}
```cpp
template<class T>
constexpr T* to_address(T* p) noexcept
{
    static_assert(!std::is_function_v<T>);
    return p;
}
 
template<class T>
constexpr auto to_address(const T& p) noexcept
{
    if constexpr (requires{ std::pointer_traits<T>::to_address(p); })
        return std::pointer_traits<T>::to_address(p);
    else
        return std::to_address(p.operator->());
}```

### Notes

{/* source:cpp-memory-to-address:0007 */}
`std::to_address` can be used even when `p` does not reference storage that has an object constructed in it, in which case `std::addressof(*p)` cannot be used because there is no valid object for the parameter of [`std::addressof`](/docs/cpp/library/memory/addressof) to bind to.

{/* source:cpp-memory-to-address:0008 */}
The fancy pointer overload of `std::to_address` inspects the `std::pointer_traits<Ptr>` specialization. If instantiating that specialization is itself ill-formed (typically because `element_type` cannot be defined), that results in a hard error outside the immediate context and renders the program ill-formed.

{/* source:cpp-memory-to-address:0009 */}
`std::to_address` may additionally be used on iterators that satisfy [`std::contiguous_iterator`](/docs/cpp/library/iterator/contiguous_iterator).

{/* source:cpp-memory-to-address:0010 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_to_address`](/docs/cpp/library/feature_test#cpp_lib_to_address) | [`201711L`](/docs/cpp/library/compiler_support/20#cpp_lib_to_address_201711L) | (C++20) | Utility to convert a pointer to a raw pointer (`std::to_address`) |

### Example

{/* source:cpp-memory-to-address:0011 */}
```cpp
#include <memory>
 
template<class A>
auto allocator_new(A& a)
{
    auto p = a.allocate(1);
    try
    {
        std::allocator_traits<A>::construct(a, std::to_address(p));
    }
    catch (...)
    {
        a.deallocate(p, 1);
        throw;
    }
    return p;
}
 
template<class A>
void allocator_delete(A& a, typename std::allocator_traits<A>::pointer p)
{
    std::allocator_traits<A>::destroy(a, std::to_address(p));
    a.deallocate(p, 1);
}
 
int main()
{
    std::allocator<int> a;
    auto p = allocator_new(a);
    allocator_delete(a, p);
}```

### See also

{/* source:cpp-memory-to-address:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`pointer_traits`](/docs/cpp/library/memory/pointer_traits) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>provides information about pointer-like types (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_address`](/docs/cpp/library/memory/pointer_traits/to_address) [static] <InlineRevision since="C++20" /> (optional)</DescriptionTerm>
    <DescriptionBody>obtains a raw pointer from a fancy pointer (inverse of `pointer_to`) (public static member function of `std::pointer_traits<Ptr>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
