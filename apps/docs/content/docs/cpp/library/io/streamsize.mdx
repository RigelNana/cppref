---
title: "std::streamsize"
description: "The type std::streamsize is a signed integral type used to represent the number of characters transferred in an I/O operation or the size of an I/O buffer."
source_url: "https://en.cppreference.com/w/cpp/io/streamsize"
language: "C++"
---

{/* source:cpp-io-streamsize:0000 */}
<DeclarationDoc>
Defined in header [`<ios>`](/docs/cpp/library/headers/ios)

  <Declaration language="cpp" code={`typedef /*implementation-defined*/ streamsize;`} />
</DeclarationDoc>

{/* source:cpp-io-streamsize:0001 */}
The type `std::streamsize` is a signed integral type used to represent the number of characters transferred in an I/O operation or the size of an I/O buffer. It is used as a signed counterpart of [`std::size_t`](/docs/cpp/library/utility/types/size_t), similar to the POSIX type `ssize_t`.

### Notes

{/* source:cpp-io-streamsize:0002 */}
Except in the constructors of [`std::strstreambuf`](/docs/cpp/library/io/strstreambuf), negative values of `std::streamsize` are never used.

### Example

{/* source:cpp-io-streamsize:0003 */}
```cpp
#include <iostream>
#include <type_traits>
 
static_assert(std::is_signed_v<std::streamsize>);
 
int main()
{
    std::cout << sizeof(std::streamsize) << '\n';
}
```

Possible output:

```text
8
```

### See also

{/* source:cpp-io-streamsize:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`gcount`](/docs/cpp/library/io/basic_istream/gcount)</DescriptionTerm>
    <DescriptionBody>returns number of characters extracted by last unformatted input operation (public member function of `std::basic_istream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ignore`](/docs/cpp/library/io/basic_istream/ignore)</DescriptionTerm>
    <DescriptionBody>extracts and discards characters until the given character is found (public member function of `std::basic_istream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`read`](/docs/cpp/library/io/basic_istream/read)</DescriptionTerm>
    <DescriptionBody>extracts blocks of characters (public member function of `std::basic_istream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`write`](/docs/cpp/library/io/basic_ostream/write)</DescriptionTerm>
    <DescriptionBody>inserts blocks of characters (public member function of `std::basic_ostream<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
