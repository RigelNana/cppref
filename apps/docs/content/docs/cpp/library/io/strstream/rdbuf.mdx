---
title: "std::strstream::rdbuf"
description: "Returns a pointer to the associated std::strstreambuf, casting away its constness (despite the const qualifier on the member function)."
source_url: "https://en.cppreference.com/w/cpp/io/strstream/rdbuf"
language: "C++"
---

{/* source:cpp-io-strstream-rdbuf:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" until="C++26" code={`strstreambuf* rdbuf() const;`} /> (deprecated in C++98)
</DeclarationDoc>

{/* source:cpp-io-strstream-rdbuf:0001 */}

Returns a pointer to the associated `std::strstreambuf`, casting away its constness (despite the const qualifier on the member function).

### Parameters

{/* source:cpp-io-strstream-rdbuf:0002 */}

(none)

### Return value

{/* source:cpp-io-strstream-rdbuf:0003 */}

Pointer to the associated `std::strstreambuf`, with constness cast away.

### Example

{/* source:cpp-io-strstream-rdbuf:0004 */}

```cpp
#include <strstream>
 
int main()
{
    const std::strstream buf;
    std::strstreambuf* ptr = buf.rdbuf();
}
```
