---
title: "std::basic_stringstream<CharT,Traits,Allocator>::view"
description: "Obtains a std::basic_string_view over the underlying string object. Equivalent to return rdbuf( ) - >view( ) ;."
source_url: "https://en.cppreference.com/w/cpp/io/basic_stringstream/view"
language: "C++"
---


{/* source:cpp-io-basic-stringstream-view:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`std::basic_string_view<CharT, Traits> view() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-io-basic-stringstream-view:0001 */}
Obtains a [`std::basic_string_view`](/docs/cpp/library/string/basic_string_view) over the underlying string object. Equivalent to `return rdbuf()->view();`.

### Parameters

{/* source:cpp-io-basic-stringstream-view:0002 */}
(none)

### Return value

{/* source:cpp-io-basic-stringstream-view:0003 */}
A [`std::basic_string_view`](/docs/cpp/library/string/basic_string_view) over the underlying string object.

### Example

{/* source:cpp-io-basic-stringstream-view:0004 */}
```cpp
#include <iostream>
#include <sstream>
 
int main()
{
    // input/output stream
    std::stringstream buf1;
    buf1 << 69;
    int n = 0;
    buf1 >> n;
    std::cout << "1) buf1 = [" << buf1.view() << "], n = " << n << '\n';
 
    // output stream in append mode
    std::ostringstream buf2("test", std::ios_base::ate);
    buf2 << '1';
    std::cout << "2) buf2 = [" << buf2.view() << "]\n";
 
    // input stream
    std::istringstream inbuf("-42");
    inbuf >> n;
    std::cout << "3) inbuf = [" << inbuf.view() << "], n = " << n << '\n';
}
```

Output:

```text
1) buf1 = [69], n = 69
2) buf2 = [test1]
3) inbuf = [-42], n = -42
```

### See also

{/* source:cpp-io-basic-stringstream-view:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`view`](/docs/cpp/library/io/basic_stringbuf/view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>obtains a view over the underlying character sequence (public member function of `std::basic_stringbuf<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
