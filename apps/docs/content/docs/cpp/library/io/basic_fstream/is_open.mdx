---
title: "std::basic_fstream<CharT,Traits>::is_open"
description: "Checks if the file stream has an associated file."
source_url: "https://en.cppreference.com/w/cpp/io/basic_fstream/is_open"
language: "C++"
---

{/* source:cpp-io-basic-fstream-is-open:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`bool is_open() const;`} />
</DeclarationDoc>

{/* source:cpp-io-basic-fstream-is-open:0001 */}
Checks if the file stream has an associated file.

{/* source:cpp-io-basic-fstream-is-open:0002 */}
Effectively calls [`rdbuf()->is_open()`](/docs/cpp/library/io/basic_filebuf/is_open).

### Parameters

{/* source:cpp-io-basic-fstream-is-open:0003 */}
(none)

### Return value

{/* source:cpp-io-basic-fstream-is-open:0004 */}
`true` if the file stream has an associated file, `false` otherwise.

### Example

{/* source:cpp-io-basic-fstream-is-open:0005 */}
```cpp
#include <fstream>
#include <iostream>
#include <string>
 
int main()
{
    std::string filename = "some_file";
 
    std::fstream fs(filename, std::ios::in);
 
    std::cout << std::boolalpha;
    std::cout << "fs.is_open() = " << fs.is_open() << '\n';
 
    if (!fs.is_open())
    {
        fs.clear();
        fs.open(filename, std::ios::out);
        std::cout << "fs.is_open() = " << fs.is_open() << '\n';
    }
}
```

Possible output:

```text
fs.is_open() = false
fs.is_open() = true
```

### Defect reports

{/* source:cpp-io-basic-fstream-is-open:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-fstream-is-open:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={365} standard="C++98">
    <PublishedBehavior>`is_open` was not declared with `const` qualifier</PublishedBehavior>
    <CorrectedBehavior>declared with `const` qualifier</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-fstream-is-open:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`open`](/docs/cpp/library/io/basic_fstream/open)</DescriptionTerm>
    <DescriptionBody>opens a file and associates it with the stream (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`close`](/docs/cpp/library/io/basic_fstream/close)</DescriptionTerm>
    <DescriptionBody>closes the associated file (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
