---
title: "std::resetiosflags"
description: "When used in an expression out << resetiosflags(mask) or in >> resetiosflags(mask), clears all format flags of the stream out or in as specified by the mask."
source_url: "https://en.cppreference.com/w/cpp/io/manip/resetiosflags"
language: "C++"
---

{/* source:cpp-io-manip-resetiosflags:0000 */}
<DeclarationDoc>
Defined in header [`<iomanip>`](/docs/cpp/library/headers/iomanip)

  <Declaration language="cpp" code={`/*unspecified*/ resetiosflags( std::ios_base::fmtflags mask );`} />
</DeclarationDoc>

{/* source:cpp-io-manip-resetiosflags:0001 */}
When used in an expression `out << resetiosflags(mask)` or `in >> resetiosflags(mask)`, clears all format flags of the stream `out` or `in` as specified by the `mask`.

### Parameters

{/* source:cpp-io-manip-resetiosflags:0002 */}
<ParameterList>
  <Parameter name="mask">bitmask of the flags to clear</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-manip-resetiosflags:0003 */}
An object of unspecified type such that

{/* source:cpp-io-manip-resetiosflags:0004 */}
- if `out` is an object of type `std::basic_ostream<CharT, Traits>`, the expression `out << resetiosflags(mask)`
  - has type `std::basic_ostream<CharT, Traits>&`
  - has value `out`
  - behaves as if it called `f(out, mask)`
- if `in` is an object of type `std::basic_istream<CharT, Traits>`, the expression `in >> resetiosflags(mask)`
  - has type `std::basic_istream<CharT, Traits>&`
  - has value `in`
  - behaves as if it called `f(in, mask)`
{/* source:cpp-io-manip-resetiosflags:0005 */}
where the function `f` is defined as:

{/* source:cpp-io-manip-resetiosflags:0006 */}
```cpp
void f(std::ios_base& str, std::ios_base::fmtflags mask)
{
    // reset specified flags
    str.setf(ios_base::fmtflags(0), mask);
}
```

### Example

{/* source:cpp-io-manip-resetiosflags:0007 */}
```cpp
#include <iomanip>
#include <iostream>
#include <sstream>

int main()
{
    std::istringstream in("10 010 10 010 10 010");
    int n1, n2;

    in >> std::oct >> n1 >> n2;
    std::cout << "Parsing \"10 010\" with std::oct gives: " << n1 << ' ' << n2 << '\n';

    in >> std::dec >> n1 >> n2;
    std::cout << "Parsing \"10 010\" with std::dec gives: " << n1 << ' ' << n2 << '\n';

    in >> std::resetiosflags(std::ios_base::basefield) >> n1 >> n2;
    std::cout << "Parsing \"10 010\" with autodetect gives: " << n1 << ' ' << n2 << '\n';
}
```

Output:

```text
Parsing "10 010" with std::oct gives: 8 8
Parsing "10 010" with std::dec gives: 10 10
Parsing "10 010" with autodetect gives: 10 8
```

### Defect reports

{/* source:cpp-io-manip-resetiosflags:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-manip-resetiosflags:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={183} standard="C++98">
    <PublishedBehavior>`resetiosflags` could only be used with  streams of type [`std::ostream`](/docs/cpp/library/io/basic_ostream) or [`std::istream`](/docs/cpp/library/io/basic_istream)</PublishedBehavior>
    <CorrectedBehavior>usable with any  character stream</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-manip-resetiosflags:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`setf`](/docs/cpp/library/io/ios_base/setf)</DescriptionTerm>
    <DescriptionBody>sets specific format flag <br /> (public member function of `std::ios_base`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`setiosflags`](/docs/cpp/library/io/manip/setiosflags)</DescriptionTerm>
    <DescriptionBody>sets the specified `ios_base` flags <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
