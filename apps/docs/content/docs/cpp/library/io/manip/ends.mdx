---
title: "std::ends"
description: "Inserts a null character into the output sequence os as if by calling os.put(CharT())."
source_url: "https://en.cppreference.com/w/cpp/io/manip/ends"
language: "C++"
---

{/* source:cpp-io-manip-ends:0000 */}
<DeclarationDoc>
Defined in header [`<ostream>`](/docs/cpp/library/headers/ostream)

  <Declaration language="cpp" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>& ends( std::basic_ostream<CharT, Traits>& os );`} />
</DeclarationDoc>

{/* source:cpp-io-manip-ends:0001 */}
Inserts a null character into the output sequence `os` as if by calling `os.put(CharT())`.

{/* source:cpp-io-manip-ends:0002 */}
This is an output-only I/O manipulator, it may be called with an expression such as `out << std::ends` for any `out` of type [`std::basic_ostream`](/docs/cpp/library/io/basic_ostream).

### Notes

{/* source:cpp-io-manip-ends:0003 */}
This manipulator is typically used with `std::ostrstream`, when the associated output buffer needs to be null-terminated to be processed as a C string.

{/* source:cpp-io-manip-ends:0004 */}
Unlike [`std::endl`](/docs/cpp/library/io/manip/endl), this manipulator does not flush the stream.

### Parameters

{/* source:cpp-io-manip-ends:0005 */}
<ParameterList>
  <Parameter name="os">reference to output stream</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-manip-ends:0006 */}
`os` (reference to the stream after insertion of the null character).

### Example

{/* source:cpp-io-manip-ends:0007 */}
```cpp
#include <cstdio>
#include <strstream>

int main()
{
    std::ostrstream oss;
    oss << "Sample text: " << 42 << std::ends;
    std::printf("%s\n", oss.str());
    oss.freeze(false); // enable memory deallocation
}
```

Output:

```text
Sample text: 42
```

### See also

{/* source:cpp-io-manip-ends:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ostrstream`](/docs/cpp/library/io/ostrstream) (deprecated in C++98) <InlineRevision until="C++26" /></DescriptionTerm>
    <DescriptionBody>implements character array output operations <br /> (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
