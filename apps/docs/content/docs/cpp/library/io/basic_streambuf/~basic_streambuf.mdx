---
title: "std::basic_streambuf<CharT,Traits>::~basic_streambuf"
description: "This destructor has no effect: the members of this basic_streambuf (the pointers and the locale) are destructed in accordance with the usual object destruction sequence after this destructor returns."
source_url: "https://en.cppreference.com/w/cpp/io/basic_streambuf/~basic_streambuf"
language: "C++"
---

{/* source:cpp-io-basic-streambuf-basic-streambuf:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`virtual ~basic_streambuf();`} />
</DeclarationDoc>

{/* source:cpp-io-basic-streambuf-basic-streambuf:0001 */}
This destructor has no effect: the members of this `basic_streambuf` (the pointers and the locale) are destructed in accordance with the usual object destruction sequence after this destructor returns. However, since it is declared public virtual, it allows the objects that are derived from `std::basic_streambuf` to be deleted through a pointer to base class.

### Parameters

{/* source:cpp-io-basic-streambuf-basic-streambuf:0002 */}
(none)

### Example

{/* source:cpp-io-basic-streambuf-basic-streambuf:0003 */}
```cpp
#include <fstream>
#include <iostream>

int main()
{
    std::filebuf* fbp = new std::filebuf;
    fbp->open("test.txt", std::ios_base::out);
    fbp->sputn("Hello\n", 6);

    std::streambuf* sbp = fbp;
    delete sbp; // the file is closed, output flushed and written

    std::ifstream f("test.txt");
    std::cout << f.rdbuf(); // proof
}
```

Output:

```text
Hello
```

### Defect reports

{/* source:cpp-io-basic-streambuf-basic-streambuf:0004 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-streambuf-basic-streambuf:0005 */}
<DefectReportList>
  <DefectReport kind="lwg" id={54} standard="C++98">
    <PublishedBehavior>the effect of the destructor was not specified</PublishedBehavior>
    <CorrectedBehavior>specified as no effect</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-streambuf-basic-streambuf:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/io/basic_streambuf/basic_streambuf)</DescriptionTerm>
    <DescriptionBody>constructs a `basic_streambuf` object (protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
