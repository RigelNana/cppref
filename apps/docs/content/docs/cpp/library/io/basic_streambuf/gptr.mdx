---
title: "std::basic_streambuf<CharT,Traits>::eback, gptr, egptr"
description: "Returns pointers defining the get area."
source_url: "https://en.cppreference.com/w/cpp/io/basic_streambuf/gptr"
language: "C++"
---

{/* source:cpp-io-basic-streambuf-gptr:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`char_type* eback() const;`} />
  <Declaration id="2" language="cpp" code={`char_type* gptr() const;`} />
  <Declaration id="3" language="cpp" code={`char_type* egptr() const;`} />

  <DeclarationDescription>
    {/* source:cpp-io-basic-streambuf-gptr:0001 */}
    Returns pointers defining the get area.

    {/* source:cpp-io-basic-streambuf-gptr:0002 */}
    1. Returns the pointer to the beginning of the get area.

    {/* source:cpp-io-basic-streambuf-gptr:0003 */}
    2. Returns the pointer to the current character (get pointer) in the get area.

    {/* source:cpp-io-basic-streambuf-gptr:0004 */}
    3. Returns the pointer one past the end of the get area.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-io-basic-streambuf-gptr:0005 */}
(none)

### Return value

{/* source:cpp-io-basic-streambuf-gptr:0006 */}
1. The pointer to the beginning of the get area.

{/* source:cpp-io-basic-streambuf-gptr:0007 */}
2. The pointer to the current character (get pointer) in the get area.

{/* source:cpp-io-basic-streambuf-gptr:0008 */}
3. The pointer one past the end of the get area.

### Notes

{/* source:cpp-io-basic-streambuf-gptr:0009 */}
While the names "gptr" and "egptr" refer to the get area, the name "eback" refers to the end of the putback area: stepping backwards from `gptr`, characters can be put back until `eback`.

### Example

{/* source:cpp-io-basic-streambuf-gptr:0010 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-io-basic-streambuf-gptr:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`pbase` `pptr` `epptr`](/docs/cpp/library/io/basic_streambuf/pptr)</DescriptionTerm>
    <DescriptionBody>returns a pointer to the beginning, current character and the end of the put area (protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
