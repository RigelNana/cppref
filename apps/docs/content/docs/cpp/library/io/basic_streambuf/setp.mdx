---
title: "std::basic_streambuf<CharT,Traits>::setp"
description: "Sets the values of the pointers defining the put area."
source_url: "https://en.cppreference.com/w/cpp/io/basic_streambuf/setp"
language: "C++"
---

{/* source:cpp-io-basic-streambuf-setp:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`protected:
void setp( char_type* pbeg, char_type* pend );`} />
</DeclarationDoc>

{/* source:cpp-io-basic-streambuf-setp:0001 */}
Sets the values of the pointers defining the put area.

{/* source:cpp-io-basic-streambuf-setp:0002 */}
After the call, `pbase() == pbeg`, `pptr() == pbeg` and `epptr() == pend` are all `true`.

{/* source:cpp-io-basic-streambuf-setp:0003 */}
If any of [ `pbeg`, `pend` ) is not a [valid range](/docs/cpp/library/iterator#Ranges), the behavior is undefined.

### Parameters

{/* source:cpp-io-basic-streambuf-setp:0004 */}
<ParameterList>
  <Parameter name="pbeg">pointer to the new beginning of the put area</Parameter>
  <Parameter name="pend">pointer to the new end of the put area</Parameter>
</ParameterList>

### Example

{/* source:cpp-io-basic-streambuf-setp:0005 */}
```cpp
#include <array>
#include <cstddef>
#include <iostream>

// Buffer for std::ostream implemented by std::array
template<std::size_t size, class CharT = char>
struct ArrayedStreamBuffer : std::basic_streambuf<CharT>
{
    using Base = std::basic_streambuf<CharT>;
    using char_type = typename Base::char_type;

    ArrayedStreamBuffer()
    {
        // put area pointers to work with “buffer”
        Base::setp(buffer.data(), buffer.data() + size);
    }

    void print_buffer()
    {
        for (char_type i : buffer)
        {
            if (i == 0)
                std::cout << "\\0";
            else
                std::cout << i;
            std::cout << ' ';
        }
        std::cout << '\n';
    }

private:
    std::array<char_type, size> buffer{}; // value-initialize “buffer”
};

int main()
{
    ArrayedStreamBuffer<10> streambuf;
    std::ostream stream(&streambuf);

    stream << "hello";
    stream << ",";

    streambuf.print_buffer();
}
```

Output:

```text
h e l l o , \0 \0 \0 \0
```

### Defect reports

{/* source:cpp-io-basic-streambuf-setp:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-streambuf-setp:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={4023} standard="C++98">
    <PublishedBehavior>`setp` did not require the output sequence to be a valid range</PublishedBehavior>
    <CorrectedBehavior>requires</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-streambuf-setp:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`setg`](/docs/cpp/library/io/basic_streambuf/setg)</DescriptionTerm>
    <DescriptionBody>repositions the beginning, next, and end pointers of the input sequence (protected member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
