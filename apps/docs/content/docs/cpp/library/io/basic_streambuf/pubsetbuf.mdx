---
title: "std::basic_streambuf<CharT,Traits>::pubsetbuf, std::basic_streambuf<CharT,Traits>::setbuf"
description: "1) Calls setbuf(s, n) of the most derived class."
source_url: "https://en.cppreference.com/w/cpp/io/basic_streambuf/pubsetbuf"
language: "C++"
---

{/* source:cpp-io-basic-streambuf-pubsetbuf:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`public:
basic_streambuf<CharT, Traits>* pubsetbuf( char_type* s, std::streamsize n )`} />
  <Declaration id="2" language="cpp" code={`protected:
virtual basic_streambuf<CharT, Traits>* setbuf( char_type* s, std::streamsize n )`} />

  <DeclarationDescription>
    {/* source:cpp-io-basic-streambuf-pubsetbuf:0001 */}
    1. Calls `setbuf(s, n)` of the most derived class.

    {/* source:cpp-io-basic-streambuf-pubsetbuf:0002 */}
    2. The base class version of this function has no effect. The derived classes may override this function to allow removal or replacement of the controlled character sequence (the buffer) with a user-provided array, or for any other implementation-specific purpose.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-io-basic-streambuf-pubsetbuf:0003 */}
<ParameterList>
  <Parameter name="s">pointer to the first `CharT` in the user-provided buffer</Parameter>
  <Parameter name="n">the number of `CharT` elements in the user-provided buffer</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-basic-streambuf-pubsetbuf:0004 */}
1. The return value of `setbuf(s, n)`.

{/* source:cpp-io-basic-streambuf-pubsetbuf:0005 */}
2. `this`

### Example

{/* source:cpp-io-basic-streambuf-pubsetbuf:0006 */}
Provides a 10k buffer for reading. On linux, the `strace` utility may be used to observe the actual number of bytes read.

```cpp
#include <fstream>
#include <iostream>
#include <string>

int main()
{
    int cnt = 0;
    std::ifstream file;
    char buf[1024 * 10 + 1];

    file.rdbuf()->pubsetbuf(buf, sizeof buf);

    file.open("/usr/share/dict/words");

    for (std::string line; getline(file, line);)
        ++cnt;

    std::cout << cnt << '\n';
}
```

Possible output:

```text
356010
```

### Defect reports

{/* source:cpp-io-basic-streambuf-pubsetbuf:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-streambuf-pubsetbuf:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={158} standard="C++98">
    <PublishedBehavior>the default behavior of `setbuf` was only specified if [`gptr()`](/docs/cpp/library/io/basic_streambuf/gptr) is not null and not equal to [`egptr()`](/docs/cpp/library/io/basic_streambuf/gptr)</PublishedBehavior>
    <CorrectedBehavior>specified as no-op for all cases</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-streambuf-pubsetbuf:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`setbuf`](/docs/cpp/library/io/basic_stringbuf/setbuf)</DescriptionTerm>
    <DescriptionBody>attempts to replace the controlled character sequence with an array (virtual protected member function of `std::basic_stringbuf<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`setbuf`](/docs/cpp/library/io/basic_filebuf/setbuf)</DescriptionTerm>
    <DescriptionBody>provides user-supplied buffer or turns this `filebuf` unbuffered (virtual protected member function of `std::basic_filebuf<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`setbuf`](/docs/cpp/library/io/strstreambuf/setbuf)</DescriptionTerm>
    <DescriptionBody>attempts to replace the controlled character sequence with an array (virtual protected member function of `std::strstreambuf`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`setbuf`](/docs/cpp/library/io/c/setbuf)</DescriptionTerm>
    <DescriptionBody>sets the buffer for a file stream (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
