---
title: "std::basic_ios<CharT,Traits>::eof"
description: "Returns true if the associated stream has reached end-of-file. Specifically, returns true if eofbit is set in rdstate()."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ios/eof"
language: "C++"
---

{/* source:cpp-io-basic-ios-eof:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`bool eof() const;`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-io-basic-ios-eof:0001 */}
Returns true if the associated stream has reached end-of-file. Specifically, returns true if eofbit is set in [`rdstate()`](/docs/cpp/library/io/basic_ios/rdstate).

{/* source:cpp-io-basic-ios-eof:0002 */}
See [`ios_base::iostate`](/docs/cpp/library/io/ios_base/iostate) for the list of conditions that set eofbit.

{/* source:cpp-io-basic-ios-eof:0003 */}
(none)

{/* source:cpp-io-basic-ios-eof:0004 */}
true if an end-of-file has occurred, false otherwise.

{/* source:cpp-io-basic-ios-eof:0005 */}
This function only reports the stream state as set by the most recent I/O operation; it does not examine the associated data source. For example, if the most recent I/O was a [`get()`](/docs/cpp/library/io/basic_istream/get) which returned the last byte of a file, eof() returns false. The next [`get()`](/docs/cpp/library/io/basic_istream/get) fails to read anything and sets the eofbit. Only then does eof() return true.

{/* source:cpp-io-basic-ios-eof:0006 */}
In typical usage, input stream processing stops on any error. eof() and [`fail()`](/docs/cpp/library/io/basic_ios/fail) can then be used to distinguish between different error conditions.

{/* source:cpp-io-basic-ios-eof:0007 */}
```cpp
#include <cstdlib>
#include <fstream>
#include <iostream>
 
int main()
{
    std::ifstream file("test.txt");
    if (!file) // operator! is used here
    {  
        std::cout << "File opening failed\n";
        return EXIT_FAILURE;
    }
 
    // typical C++ I/O loop uses the return value of the I/O function
    // as the loop controlling condition, operator bool() is used here
    for (int n; file >> n;)
       std::cout << n << ' ';
    std::cout << '\n';
 
    if (file.bad())
        std::cout << "I/O error while reading\n";
    else if (file.eof())
        std::cout << "End of file reached successfully\n";
    else if (file.fail())
        std::cout << "Non-integer data encountered\n";
}
```

{/* source:cpp-io-basic-ios-eof:0008 */}
The following table shows the value of [`basic_ios`](/docs/cpp/library/io/basic_ios) accessors ([`good()`](/docs/cpp/library/io/basic_ios/good), [`fail()`](/docs/cpp/library/io/basic_ios/fail), etc.) for all possible combinations of [`ios_base::iostate`](/docs/cpp/library/io/ios_base/iostate) flags:

{/* source:cpp-io-basic-ios-eof:0009 */}
| [`ios_base::iostate`](/docs/cpp/library/io/ios_base/iostate) flags |  |  | `basic_ios` accessors |  |  |  |  |  |
| `eofbit` | `failbit` | `badbit` | [`good()`](/docs/cpp/library/io/basic_ios/good) | [`fail()`](/docs/cpp/library/io/basic_ios/fail) | [`bad()`](/docs/cpp/library/io/basic_ios/bad) | `eof()` | [`operator bool`](/docs/cpp/library/io/basic_ios/operator_bool) | [`operator!`](/docs/cpp/library/io/basic_ios/operator!) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| false | false | false | true | false | false | false | true | false |
| false | false | true | false | true | true | false | false | true |
| false | true | false | false | true | false | false | false | true |
| false | true | true | false | true | true | false | false | true |
| true | false | false | false | false | false | true | true | false |
| true | false | true | false | true | true | true | false | true |
| true | true | false | false | true | false | true | false | true |
| true | true | true | false | true | true | true | false | true |

{/* source:cpp-io-basic-ios-eof:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`feof`](/docs/cpp/library/io/c/feof)</DescriptionTerm>
    <DescriptionBody>checks for the end-of-file (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
