---
title: "std::basic_ios<CharT,Traits>::imbue"
description: "Replaces the current locale. Effectively calls ios_base:: imbue (loc) and if there is an associated stream buffer (rdbuf( ) ! = 0), then calls rdbuf( ) - >pubimbue(loc)."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ios/imbue"
language: "C++"
---

{/* source:cpp-io-basic-ios-imbue:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`std::locale imbue( const std::locale& loc );`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-io-basic-ios-imbue:0001 */}
Replaces the current locale. Effectively calls ios_base:: imbue (loc) and if there is an associated stream buffer (rdbuf( ) ! = 0), then calls rdbuf( ) - \>pubimbue(loc).

{/* source:cpp-io-basic-ios-imbue:0002 */}
<ParameterList>
<Parameter name="loc">the new locale</Parameter>
</ParameterList>

{/* source:cpp-io-basic-ios-imbue:0003 */}
The previous locale, as returned by ios_base:: imbue (loc).

{/* source:cpp-io-basic-ios-imbue:0004 */}
May throw implementation-defined exceptions.

{/* source:cpp-io-basic-ios-imbue:0005 */}
```cpp
#include <iostream>
#include <locale>
#include <sstream>
 
int main()
{
    std::istringstream iss;
    iss.imbue(std::locale("en_US.UTF8"));
 
    std::cout << "Current locale: " << iss.getloc().name() << '\n';
 
    iss.imbue(std::locale());
    std::cout << "Global locale : " << iss.getloc().name() << '\n';
}
```

Output:

```text
Current locale: en_US.UTF8
Global locale : C
```
