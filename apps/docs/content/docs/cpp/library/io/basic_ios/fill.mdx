---
title: "std::basic_ios<CharT,Traits>::fill"
description: "Manages the fill character used to pad the output conversions to the specified field width."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ios/fill"
language: "C++"
---

{/* source:cpp-io-basic-ios-fill:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`CharT fill() const;`} />
  <Declaration id="2" language="cpp" code={`CharT fill( CharT ch );`} />

  <DeclarationDescription>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-io-basic-ios-fill:0001 */}
Manages the fill character used to pad the output conversions to the specified field width.

{/* source:cpp-io-basic-ios-fill:0002 */}
1. Returns the current fill character.

{/* source:cpp-io-basic-ios-fill:0003 */}
2. Sets the fill character to `ch`, returns previous value of the fill character.

{/* source:cpp-io-basic-ios-fill:0004 */}
<ParameterList>
<Parameter name="ch">the character to use as fill character</Parameter>
</ParameterList>

{/* source:cpp-io-basic-ios-fill:0005 */}
The fill character before the call to the function.

{/* source:cpp-io-basic-ios-fill:0006 */}
```cpp
#include <iomanip>
#include <iostream>
 
int main ()
{
    std::cout << "With default setting : [" << std::setw(10) << 40 << "]\n";
    char prev = std::cout.fill('x');
    std::cout << "Replaced '" << prev << "' with '"
              << std::cout.fill() << "': [" << std::setw(10) << 40 << "]\n";
}
```

Output:

```text
With default setting : [        40]
Replaced ' ' with 'x': [xxxxxxxx40]
```

{/* source:cpp-io-basic-ios-fill:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`setfill`](/docs/cpp/library/io/manip/setfill)</DescriptionTerm>
    <DescriptionBody>changes the fill character (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
