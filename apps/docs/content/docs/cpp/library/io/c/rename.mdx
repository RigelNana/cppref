---
title: "std::rename"
description: "Changes the filename of a file. The file is identified by character string pointed to by old_filename. The new filename is identified by character string pointed to by new_filename."
source_url: "https://en.cppreference.com/w/cpp/io/c/rename"
language: "C++"
---

{/* source:cpp-io-c-rename:0000 */}
<DeclarationDoc>
Defined in header [`<cstdio>`](/docs/cpp/library/headers/cstdio)

  <Declaration language="cpp" code={`int rename( const char* old_filename, const char* new_filename );`} />
</DeclarationDoc>

{/* source:cpp-io-c-rename:0001 */}
Changes the filename of a file. The file is identified by character string pointed to by `old_filename`. The new filename is identified by character string pointed to by `new_filename`.

{/* source:cpp-io-c-rename:0002 */}
If `new_filename` exists, the behavior is implementation-defined.

### Parameters

{/* source:cpp-io-c-rename:0003 */}
<ParameterList>
  <Parameter name="old_filename">pointer to a null-terminated string containing the path identifying the file to rename</Parameter>
  <Parameter name="new_filename">pointer to a null-terminated string containing the new path of the file</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-c-rename:0004 */}
`0` upon success or non-zero value on error.

### Notes

{/* source:cpp-io-c-rename:0005 */}
[POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/functions/rename.html) specifies many additional details on the semantics of this function, which are reproduced in C++ by [`std::filesystem::rename`](/docs/cpp/library/filesystem/rename).

### Example

{/* source:cpp-io-c-rename:0006 */}
```cpp
#include <cstdio>
#include <cstdlib>
#include <fstream>
#include <iostream>

int main()
{
    if (!std::ofstream("from.txt").put('a')) // create and write to file
    {
        std::perror("Error creating from.txt");
        return EXIT_FAILURE;
    }

    if (std::rename("from.txt", "to.txt"))
    {
        std::perror("Error renaming");
        return EXIT_FAILURE;
    }

    std::cout << std::ifstream("to.txt").rdbuf() << '\n'; // print file
    return EXIT_SUCCESS;
}
```

Output:

```text
a
```

### See also

{/* source:cpp-io-c-rename:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rename`](/docs/cpp/library/filesystem/rename) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>moves or renames a file or directory (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`remove`](/docs/cpp/library/io/c/remove)</DescriptionTerm>
    <DescriptionBody>erases a file (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/io/rename) for `rename`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
