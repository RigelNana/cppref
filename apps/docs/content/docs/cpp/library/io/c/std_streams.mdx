---
title: "stdin, stdout, stderr"
description: "Three text streams are predefined. These streams are implicitly opened and unoriented at program startup."
source_url: "https://en.cppreference.com/w/cpp/io/c/std_streams"
language: "C++"
---

{/* source:cpp-io-c-std-streams:0000 */}
<DeclarationDoc>
Defined in header [`<cstdio>`](/docs/cpp/library/headers/cstdio)

  <Declaration id="1" language="cpp" code={`#define stdin  /* implementation-defined */`} />
  <Declaration id="2" language="cpp" code={`#define stdout /* implementation-defined */`} />
  <Declaration id="3" language="cpp" code={`#define stderr /* implementation-defined */`} />

  <DeclarationDescription>
    {/* source:cpp-io-c-std-streams:0001 */}
    Three text streams are predefined. These streams are implicitly opened and unoriented at program startup.

    {/* source:cpp-io-c-std-streams:0002 */}
    1. Associated with the standard input stream, used for reading conventional input. At program startup, the stream is fully buffered if and only if the stream can be determined not to refer to an interactive device.

    {/* source:cpp-io-c-std-streams:0003 */}
    2. Associated with the standard output stream, used for writing conventional output. At program startup, the stream is fully buffered if and only if the stream can be determined not to refer to an interactive device.

    {/* source:cpp-io-c-std-streams:0004 */}
    3. Associated with the standard error stream, used for writing diagnostic output. At program startup, the stream is not fully buffered.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-io-c-std-streams:0005 */}
What constitutes an interactive device is implementation-defined.

{/* source:cpp-io-c-std-streams:0006 */}
These macros are expanded to expressions of type `std::FILE*`.

### Notes

{/* source:cpp-io-c-std-streams:0007 */}
Although not mandated by POSIX, the UNIX convention is that `stdin` and `stdout` are line-buffered if associated with a terminal and `stderr` is unbuffered.

{/* source:cpp-io-c-std-streams:0008 */}
These macros may be expanded to modifiable lvalues. If any of these `std::FILE*` lvalue is modified, subsequent operations on the corresponding stream result in unspecified or undefined behavior.

### Example

{/* source:cpp-io-c-std-streams:0009 */}
This example shows a function similar to [`std::printf`](/docs/cpp/library/io/c/printf).

```cpp
#include <concepts>
#include <cstdio>
#include <type_traits>

template<typename T>
concept IsPrintable = std::integral<T> or std::floating_point<T> or std::is_pointer_v<T>;

int my_printf(char const* const format, IsPrintable auto const ... arguments)
{
    return std::fprintf(stdout, format, arguments...);
}

int main(int argv, char*[])
{
    my_printf("Strings and chars:\t%s %c\n", "hello", 'x');
    my_printf("Rounding:\t\t%f %.0f %.32f\n", 1.5, 1.5, 1.3);
    my_printf("Padding:\t\t%05.2f %.2f %5.2f\n", 1.5, 1.5, 1.5);
    my_printf("Scientific:\t\t%E %e\n", 1.5, 1.5);
    my_printf("Hexadecimal:\t\t%a %A 0x%X\n", 1.5, 1.5, &argv);
}
```

Possible output:

```text
Strings and chars:  hello x
Rounding:           1.500000 2 1.30000000000000004440892098500626
Padding:            01.50 1.50  1.50
Scientific:         1.500000E+00 1.500000e+00
Hexadecimal:        0x1.8p+0 0X1.8P+0 0x2CFB41BC
```

### See also

{/* source:cpp-io-c-std-streams:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`cin`, `wcin`](/docs/cpp/library/io/basic_istream/cin)</DescriptionTerm>
    <DescriptionBody>reads from the standard C input stream `stdin` (global object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cout`, `wcout`](/docs/cpp/library/io/basic_ostream/cout)</DescriptionTerm>
    <DescriptionBody>writes to the standard C output stream `stdout` (global object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cerr`, `wcerr`](/docs/cpp/library/io/basic_ostream/cerr)</DescriptionTerm>
    <DescriptionBody>writes to the standard C error stream `stderr`, unbuffered (global object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`clog`, `wclog`](/docs/cpp/library/io/basic_ostream/clog)</DescriptionTerm>
    <DescriptionBody>writes to the standard C error stream `stderr` (global object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`printf`, `fprintf`, `sprintf`, `snprintf`](/docs/cpp/library/io/c/printf) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>prints formatted output to `stdout`, a file stream or a buffer (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`FILE`](/docs/cpp/library/io/c/FILE)</DescriptionTerm>
    <DescriptionBody>object type, capable of holding all information needed to control a C I/O stream (typedef)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/io/std_streams) for `stdin`, `stdout`, `stderr`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
