---
title: "std::fwrite"
description: "Writes up to count binary objects from the given array buffer to the output stream stream. The objects are written as if by reinterpreting each object as an array of unsigned char and calling std::fputc size times for each object to write those unsigned chars into stream, in order. The file position indicator for the stream is advanced by the number of characters written."
source_url: "https://en.cppreference.com/w/cpp/io/c/fwrite"
language: "C++"
---

{/* source:cpp-io-c-fwrite:0000 */}
<DeclarationDoc>
Defined in header [`<cstdio>`](/docs/cpp/library/headers/cstdio)

  <Declaration language="cpp" code={`std::size_t fwrite( const void* buffer, std::size_t size, std::size_t count, std::FILE* stream );`} />
</DeclarationDoc>

{/* source:cpp-io-c-fwrite:0001 */}
Writes up to `count` binary objects from the given array `buffer` to the output stream `stream`. The objects are written as if by reinterpreting each object as an array of `unsigned char` and calling [`std::fputc`](/docs/cpp/library/io/c/fputc) `size` times for each object to write those `unsigned char`s into `stream`, in order. The file position indicator for the stream is advanced by the number of characters written.

{/* source:cpp-io-c-fwrite:0002 */}
If the objects are not [TriviallyCopyable](/docs/cpp/library/named_req/TriviallyCopyable), the behavior is undefined.

{/* source:cpp-io-c-fwrite:0003 */}
If an error occurs, the resulting value of the file position indicator for the stream is indeterminate.

### Parameters

{/* source:cpp-io-c-fwrite:0004 */}
<ParameterList>
  <Parameter name="buffer">pointer to the first object in the array to be written</Parameter>
  <Parameter name="size">size of each object</Parameter>
  <Parameter name="count">the number of the objects to be written</Parameter>
  <Parameter name="stream">output file stream to write to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-c-fwrite:0005 */}
Number of objects written successfully, which may be less than `count` if an error occurred.

{/* source:cpp-io-c-fwrite:0006 */}
If `size` or `count` is zero, `fwrite` returns zero and performs no other action.

### Example

{/* source:cpp-io-c-fwrite:0007 */}
```cpp
#include <array>
#include <cstdio>
#include <vector>
 
int main ()
{
    // write buffer to file
    if (std::FILE* f1 = std::fopen("file.bin", "wb"))
    {
        std::array<int, 3> v = {42, -1, 7}; // underlying storage of std::array is an array
        std::fwrite(v.data(), sizeof v[0], v.size(), f1);
        std::fclose(f1);
    }
 
    // read the same data and print it to the standard output
    if (std::FILE* f2 = std::fopen("file.bin", "rb"))
    {
        std::vector<int> rbuf(10); // underlying storage of std::vector is also an array
        std::size_t sz = std::fread(rbuf.data(), sizeof rbuf[0], rbuf.size(), f2);
        std::fclose(f2);
        for (std::size_t n = 0; n < sz; ++n)
            std::printf("%d\n", rbuf[n]);
    }
}
```

Output:

```text
42
-1
7
```

### See also

{/* source:cpp-io-c-fwrite:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`printf`, `fprintf`, `sprintf`, `snprintf`](/docs/cpp/library/io/c/printf) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>prints formatted output to [`stdout`](/docs/cpp/library/io/c/std_streams), a file stream or a buffer <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`fputs`](/docs/cpp/library/io/c/fputs)</DescriptionTerm>
    <DescriptionBody>writes a character string to a file stream <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`fread`](/docs/cpp/library/io/c/fread)</DescriptionTerm>
    <DescriptionBody>reads from a file <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/io/fwrite) for `fwrite`</DescriptionTerm>
    <DescriptionBody />
  </DescriptionItem>
</DescriptionList>
