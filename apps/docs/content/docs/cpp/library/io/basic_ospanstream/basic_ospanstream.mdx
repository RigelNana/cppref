---
title: "std::basic_ospanstream<CharT,Traits>::basic_ospanstream"
description: "Constructs a new basic_ospanstream."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ospanstream/basic_ospanstream"
language: "C++"
---

{/* source:cpp-io-basic-ospanstream-basic-ospanstream:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`explicit basic_ospanstream( std::span<CharT> s, std::ios_base::openmode mode =
                                                         std::ios_base::out );`} />
  <Declaration id="2" language="cpp" since="C++23" code={`basic_ospanstream( basic_ospanstream&& rhs );`} />
  <Declaration id="3" language="cpp" since="C++23" code={`basic_ospanstream( const basic_ospanstream& ) = delete;`} />

  <DeclarationDescription>
    {/* source:cpp-io-basic-ospanstream-basic-ospanstream:0001 */}
    Constructs a new `basic_ospanstream`.

    {/* source:cpp-io-basic-ospanstream-basic-ospanstream:0002 */}
    1. Uses the storage referenced by `s` as initial underlying buffer of the wrapped `std::basic_spanbuf` device. The wrapped `std::basic_spanbuf` object is constructed as `basic_spanbuf<Char, Traits>(s, mode | std::ios_base::out)`.

    {/* source:cpp-io-basic-ospanstream-basic-ospanstream:0003 */}
    2. Move constructor. Move constructs the [`std::basic_ostream`](/docs/cpp/library/io/basic_ostream) base subobject and the wrapped `std::basic_spanbuf` from those of `rhs`, and then calls [`set_rdbuf`](/docs/cpp/library/io/basic_ios/set_rdbuf) with the address of the wrapped `std::basic_spanbuf` in `*this` to install it.

    {/* source:cpp-io-basic-ospanstream-basic-ospanstream:0004 */}
    3. Copy constructor is deleted. `basic_ospanstream` is not copyable.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-io-basic-ospanstream-basic-ospanstream:0005 */}
<ParameterList>
  <Parameter name="s">`std::span` referencing the storage to be use as initial underlying buffer of stream</Parameter>
  <Parameter name="mode">specifies stream open mode. Following constants and bit-wise OR between them may be used:</Parameter>
</ParameterList>

| Constant | Explanation |
| --- | --- |
| [`app`](/docs/cpp/library/io/ios_base/openmode) | seek to the end of stream before each write |
| [`binary`](/docs/cpp/library/io/ios_base/openmode) | open in [binary mode](/docs/cpp/library/io/c/FILE#Binary_and_text_modes) |
| [`in`](/docs/cpp/library/io/ios_base/openmode) | open for reading |
| [`out`](/docs/cpp/library/io/ios_base/openmode) | open for writing |
| [`trunc`](/docs/cpp/library/io/ios_base/openmode) | discard the contents of the stream when opening |
| [`ate`](/docs/cpp/library/io/ios_base/openmode) | seek to the end of stream immediately after open |
| [`noreplace`](/docs/cpp/library/io/ios_base/openmode) <InlineRevision since="C++23" /> | open in exclusive mode |

<ParameterList>
  <Parameter name="other">another `basic_ospanstream` to be moved from</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-io-basic-ospanstream-basic-ospanstream:0006 */}
May throw implementation-defined exceptions.

### Example

{/* source:cpp-io-basic-ospanstream-basic-ospanstream:0007 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-io-basic-ospanstream-basic-ospanstream:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/io/basic_spanbuf/basic_spanbuf)</DescriptionTerm>
    <DescriptionBody>constructs a `basic_spanbuf` object (public member function of `std::basic_spanbuf<CharT,Traits>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
