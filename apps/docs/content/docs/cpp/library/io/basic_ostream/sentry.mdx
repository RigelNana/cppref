---
title: "std::basic_ostream<CharT,Traits>::sentry"
description: "An object of class basic_ostream::sentry is constructed in local scope at the beginning of each member function of std::basic_ostream that performs output (both formatted and unformatted)."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ostream/sentry"
language: "C++"
---

{/* source:cpp-io-basic-ostream-sentry:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`class sentry;`} />
</DeclarationDoc>

{/* source:cpp-io-basic-ostream-sentry:0001 */}
An object of class `basic_ostream::sentry` is constructed in local scope at the beginning of each member function of [`std::basic_ostream`](/docs/cpp/library/io/basic_ostream) that performs output (both formatted and unformatted). Its constructor prepares the output stream: checks if the stream is already in a failed state, flushes the `tie()`'d output streams, and performs other implementation-defined tasks if necessary. Implementation-defined cleanup, as well as flushing of the output stream if necessary, is performed in the destructor, so that it is guaranteed to happen if exceptions are thrown during output.

### Member functions

{/* source:cpp-io-basic-ostream-sentry:0002 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>(constructor)</DescriptionTerm>
    <DescriptionBody>constructs the sentry object. All the preparation tasks are done here<br />(public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>(destructor)</DescriptionTerm>
    <DescriptionBody>finalizes the stream object after formatted output or after exception, if necessary<br />(public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>operator=</DescriptionTerm>
    <DescriptionBody>the assignment operator is deleted<br />(public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>operator bool</DescriptionTerm>
    <DescriptionBody>checks if the preparation of the stream object was successful<br />(public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-io-basic-ostream-sentry:0003 */}
#### std::basic_ostream::sentry::sentry

<DeclarationDoc>
  <Declaration language="cpp" code={`explicit sentry( std::basic_ostream<CharT, Traits>& os );`} />
</DeclarationDoc>

Prepares the stream for formatted output.

If `os.good()` is `false`, returns. Otherwise, if `os.tie()` is not a null pointer, calls `os.tie()->flush()` to synchronize the output sequence with external streams. During preparation, the constructor may call `setstate(failbit)` (which may throw [`std::ios_base::failure`](/docs/cpp/library/io/ios_base/failure)).

If after preparation is completed, `os.good() == true`, then any subsequent calls to `operator bool` will return `true`.

##### Parameters

<ParameterList>
  <Parameter name="os">output stream to prepare</Parameter>
</ParameterList>

##### Exceptions

[`std::ios_base::failure`](/docs/cpp/library/io/ios_base/failure) if the end of file condition occurs.

{/* source:cpp-io-basic-ostream-sentry:0004 */}
#### std::basic_ostream::sentry::~sentry

<DeclarationDoc>
  <Declaration language="cpp" code={`~sentry();`} />
</DeclarationDoc>

If `(os.flags() & std::ios_base::unitbuf) && !std::uncaught_exception() && os.good())` is `true`, calls `os.rdbuf()->pubsync()`. If that function returns `-1`, sets `badbit` in `os.rdstate()` without propagating an exception.

{/* source:cpp-io-basic-ostream-sentry:0005 */}
#### std::basic_ostream::sentry::operator bool

<DeclarationDoc>
  <Declaration language="cpp" code={`explicit operator bool() const;`} />
</DeclarationDoc>

Checks whether the preparation of the output stream was successful.

##### Parameters

(none)

##### Return value

`true` if the preparation of the output stream was successful, `false` otherwise.

### Example

{/* source:cpp-io-basic-ostream-sentry:0006 */}
```cpp
#include <iostream>
#include <sstream>
 
struct Foo
{
    char n[6];
};
 
std::ostream& operator<<(std::ostream& os, Foo& f)
{
    std::ostream::sentry s(os);
    if (s)
        os.write(f.n, 5);
    return os;
}
 
int main()
{
    Foo f = {"abcde"};
    std::cout << f << '\n';
}
```

Output:

```text
abcde
```

### Defect reports

{/* source:cpp-io-basic-ostream-sentry:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-ostream-sentry:0008 */}
<DefectReportList>
  <DefectReport kind="lwg" id={397} standard="C++98">
    <PublishedBehavior>the destructor might call `os.flush()`, which may throw exceptions</PublishedBehavior>
    <CorrectedBehavior>the exception is not propagated</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={442} standard="C++98">
    <PublishedBehavior>`operator bool` was not declared `const` (it is `const` in the [synopsis](/docs/cpp/library/headers/ostream))</PublishedBehavior>
    <CorrectedBehavior>added `const`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={835} standard="C++98">
    <PublishedBehavior>if `os` sets `unitbuf`, the destructor would call `os.flush()`, which is an [*UnformattedOutputFunction*](/docs/cpp/library/named_req/UnformattedOutputFunction) and creates another sentry object (whose destructor then creates another sentry object and so on)</PublishedBehavior>
    <CorrectedBehavior>calls `os.rdbuf()->pubsync()` in this case instead</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-ostream-sentry:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<`](/docs/cpp/library/io/basic_ostream/operator_ltlt)</DescriptionTerm>
    <DescriptionBody>inserts formatted data (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
