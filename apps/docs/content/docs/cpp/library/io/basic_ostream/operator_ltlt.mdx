---
title: "std::basic_ostream<CharT,Traits>::operator<<"
description: "Inserts data into the stream."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ostream/operator_ltlt"
language: "C++"
---

{/* source:cpp-io-basic-ostream-operator-ltlt:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`basic_ostream& operator<<( bool value );`} />
  <Declaration id="2" language="cpp" code={`basic_ostream& operator<<( long value );`} />
  <Declaration id="3" language="cpp" code={`basic_ostream& operator<<( unsigned long value );`} />
  <Declaration id="4" language="cpp" since="C++11" code={`basic_ostream& operator<<( long long value );`} />
  <Declaration id="5" language="cpp" since="C++11" code={`basic_ostream& operator<<( unsigned long long value );`} />
  <Declaration id="6" language="cpp" code={`basic_ostream& operator<<( double value );`} />
  <Declaration id="7" language="cpp" code={`basic_ostream& operator<<( long double value );`} />
  <Declaration id="8" language="cpp" code={`basic_ostream& operator<<( const void* value );`} />
  <Declaration id="9" language="cpp" since="C++23" code={`basic_ostream& operator<<( const volatile void* value );`} />
  <Declaration id="10" language="cpp" since="C++17" code={`basic_ostream& operator<<( std::nullptr_t );`} />
  <Declaration id="11" language="cpp" code={`basic_ostream& operator<<( short value );`} />
  <Declaration id="12" language="cpp" code={`basic_ostream& operator<<( int value );`} />
  <Declaration id="13" language="cpp" code={`basic_ostream& operator<<( unsigned short value );`} />
  <Declaration id="14" language="cpp" code={`basic_ostream& operator<<( unsigned int value );`} />
  <Declaration id="15" language="cpp" code={`basic_ostream& operator<<( float value );`} />
  <Declaration id="16" language="cpp" since="C++23" code={`basic_ostream& operator<<( /* extended-floating-point-type */ value );`} />
  <Declaration id="17" language="cpp" code={`basic_ostream& operator<<( std::basic_streambuf<CharT, Traits>* sb );`} />
  <Declaration id="18" language="cpp" code={`basic_ostream& operator<<(
    std::ios_base& (*func)(std::ios_base&) );`} />
  <Declaration id="19" language="cpp" code={`basic_ostream& operator<<(
    std::basic_ios<CharT, Traits>& (*func)(std::basic_ios<CharT, Traits>&) );`} />
  <Declaration id="20" language="cpp" code={`basic_ostream& operator<<(
    std::basic_ostream<CharT, Traits>& (*func)
        (std::basic_ostream<CharT, Traits>&) );`} />

  <DeclarationDescription>
    {/* source:cpp-io-basic-ostream-operator-ltlt:0001 */}
    Inserts data into the stream.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0002 */}
    1. 1-8) Inserts `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0003 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, inserts a value by calling [`std::num_put::put()`](/docs/cpp/library/text/locale/num_put/put). If the end of file condition was encountered during output (`put().failed() == true`), sets `badbit`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0004 */}
    2. 9) Equivalent to `return operator<<(const_cast<const void*>(p));`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0005 */}
    3. 10) Equivalent to `return *this << s;`, where `s` is an implementation-defined null-terminated character type string.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0006 */}
    4. 11) Inserts a value from `short` `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0007 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, inserts a `long` value `lval` as in (2), where `lval` is `static_cast<long>(static_cast<unsigned short>(value))`, if `flags() & std::ios_base::basefield` is [`std::ios_base::oct`](/docs/cpp/library/io/ios_base/fmtflags) or [`std::ios_base::hex`](/docs/cpp/library/io/ios_base/fmtflags), or `static_cast<long>(value)` otherwise.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0008 */}
    5. 12) Inserts a value from `int` `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0009 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, inserts a `long` value `lval` as in (2), where `lval` is `static_cast<long>(static_cast<unsigned int>(value))`, if `flags() & std::ios_base::basefield` is [`std::ios_base::oct`](/docs/cpp/library/io/ios_base/fmtflags) or [`std::ios_base::hex`](/docs/cpp/library/io/ios_base/fmtflags), or `static_cast<long>(value)` otherwise.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0010 */}
    6. 13,14) Inserts a value from `unsigned short` or `unsigned int` `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0011 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, inserts `static_cast<unsigned long>(value)` as in (3).

    {/* source:cpp-io-basic-ostream-operator-ltlt:0012 */}
    7. 15) Inserts a value from `float` `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0013 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, inserts `static_cast<double>(value)` as in (6).

    {/* source:cpp-io-basic-ostream-operator-ltlt:0014 */}
    8. 16) Inserts a value from `value`. The library provides overloads for all cv-unqualified [extended floating-point types](/docs/cpp/language/basic_concepts/types#Extended_floating-point_types) as the type of the parameter `value`.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0015 */}
    This function behaves as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction). After constructing and checking the sentry object, checks the [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) of `/* extended-floating-point-type */`: If the rank is less than or equal to that of `double`, inserts `static_cast<double>(value)` as in (6). Otherwise, if the rank is less than or equal to that of `long double`, inserts `static_cast<long double>(value)` as in (7). Otherwise, an invocation of this overload is conditionally supported with implementation-defined semantics.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0016 */}
    9. 17) This function behaves as an [*UnformattedOutputFunction*](/docs/cpp/library/named_req/UnformattedOutputFunction). After constructing and checking the sentry object, checks if `sb` is a null pointer. If it is, executes `setstate(badbit)` and exits. Otherwise, extracts characters from the input sequence controlled by `sb` and inserts them into `*this` until one of the following conditions are met: end-of-file occurs on the input sequence; inserting in the output sequence fails (in which case the character to be inserted is not extracted); an exception occurs (in which case the exception is caught). If no characters were inserted, executes `setstate(failbit)`. If an exception was thrown while extracting, sets `failbit` and, if `failbit` is set in [`exceptions()`](/docs/cpp/library/io/basic_ios/exceptions), rethrows the exception.

    {/* source:cpp-io-basic-ostream-operator-ltlt:0017 */}
    10. 18-20) Calls `func(*this)`. These overloads are used to implement output I/O manipulators such as [`std::endl`](/docs/cpp/library/io/manip/endl).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-io-basic-ostream-operator-ltlt:0018 */}
<ParameterList>
  <Parameter name="value">integer, floating-point, boolean, or pointer value to insert</Parameter>
  <Parameter name="func">function to call</Parameter>
  <Parameter name="sb">pointer to the stream buffer to read the data from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-basic-ostream-operator-ltlt:0019 */}
1. 1-19) `*this`

{/* source:cpp-io-basic-ostream-operator-ltlt:0020 */}
2. 20) `func(*this)`

### Notes

{/* source:cpp-io-basic-ostream-operator-ltlt:0021 */}
There are no overloads for pointers to non-static members, <InlineRevision until="C++23">, pointers to volatiles,</InlineRevision> or function pointers (other than the ones with signatures accepted by the ([18-20](/docs/cpp/library/io/basic_ostream/operator_ltlt#Version_18)) overloads).

{/* source:cpp-io-basic-ostream-operator-ltlt:0022 */}
- Attempting to output such objects invokes implicit conversion to `bool`, and, for any non-null pointer value, the value `1` is printed (unless `boolalpha` was set, in which case `true` is printed).

{/* source:cpp-io-basic-ostream-operator-ltlt:0023 */}
Character and character string arguments (e.g., of type `char` or `const char*`) are handled by the [non-member overloads](/docs/cpp/library/io/basic_ostream/operator_ltlt2) of `operator<<`.

{/* source:cpp-io-basic-ostream-operator-ltlt:0024 */}
- Attempting to output a character using the member function call syntax (e.g., `std::cout.operator<<('c');`) will call one of the overloads in ([2-5](/docs/cpp/library/io/basic_ostream/operator_ltlt#Version_2)) or ([11-14](/docs/cpp/library/io/basic_ostream/operator_ltlt#Version_11)) and output the numerical value. Attempting to output a character string using the member function call syntax will call overload (8) and print the pointer value instead.

{/* source:cpp-io-basic-ostream-operator-ltlt:0025 */}
Overload (10) was added by the resolution of [LWG issue 2221](https://cplusplus.github.io/LWG/issue2221), but it is never implemented in any standard library implementation under C++11/14 modes.

### Example

{/* source:cpp-io-basic-ostream-operator-ltlt:0026 */}
```cpp
#include <iomanip>
#include <iostream>
#include <sstream>
 
int fun() { return 42; }
 
int main()
{
    std::istringstream input(" \"Some text.\" ");
    double f = 3.14;
    bool b = true;
 
    std::cout
        << fun()          // int overload (12)
        << ' '            // non-member overload
        << std::boolalpha // function overload (18)
        << b              // bool overload (1)
        << " "            // non-member overload
        << std::fixed     // function overload (18) again
        << f              // double overload (6)
        << input.rdbuf()  // streambuf overload
        << fun            // bool overload (1): there's no overload for int(*)()
        << std::endl;     // function overload (18) again
 
    int x = 0;
    int* p1 = &x;
    volatile int* p2 = &x;
    std::cout
        << "p1: " << p1 << '\n'  // `const void*` overload, prints address
        << "p2: " << p2 << '\n'; // before C++23 (P1147): bool overload :), because
            // operator<<(const void*) is not a match, as it discards the `volatile`
            // qualifier. To fix this, C++23 adds `const volatile void*` overload (9),
            // that prints the address as expected.
}
```

Possible output:

```text
42 true 3.140000 "Some text." true
p1: 0x7ffcea766600
p2: 0x7ffcea766600
```

### Defect reports

{/* source:cpp-io-basic-ostream-operator-ltlt:0027 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-io-basic-ostream-operator-ltlt:0028 */}
<DefectReportList>
  <DefectReport kind="lwg" id={117} standard="C++98">
    <PublishedBehavior>overloads (1-8,11-15) delegated the insertion to [`num_put::put`](/docs/cpp/library/text/locale/num_put/put), but it does not have overloads for `short`, `unsigned short`, `int`, `unsigned int`, and `float`</PublishedBehavior>
    <CorrectedBehavior>they are converted before being passed to `num_put::put`</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={567} standard="C++98">
    <PublishedBehavior>overload (17) behaved as a [*FormattedOutputFunction*](/docs/cpp/library/named_req/FormattedOutputFunction) because of the resolution of [LWG issue 60](https://cplusplus.github.io/LWG/issue60)</PublishedBehavior>
    <CorrectedBehavior>it behaves as an [*UnformattedOutputFunction*](/docs/cpp/library/named_req/UnformattedOutputFunction)</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-io-basic-ostream-operator-ltlt:0029 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<(std::basic_ostream)`](/docs/cpp/library/io/basic_ostream/operator_ltlt2)</DescriptionTerm>
    <DescriptionBody>inserts character data or insert into rvalue stream (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<< operator>>`](/docs/cpp/library/string/basic_string/operator_ltltgtgt)</DescriptionTerm>
    <DescriptionBody>performs stream input and output on strings (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<`](/docs/cpp/library/string/basic_string_view/operator_ltlt) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>performs stream output on string views (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<< operator>>`](/docs/cpp/library/utility/bitset/operator_ltltgtgt2)</DescriptionTerm>
    <DescriptionBody>performs stream input and output of bitsets (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<< operator>>`](/docs/cpp/library/numeric/complex/operator_ltltgtgt)</DescriptionTerm>
    <DescriptionBody>serializes and deserializes a complex number (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<< operator>>`](/docs/cpp/library/numeric/random/linear_congruential_engine/operator_ltltgtgt) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>performs stream input and output on pseudo-random number engine (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<< operator>>`](/docs/cpp/library/numeric/random/uniform_int_distribution/operator_ltltgtgt) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>performs stream input and output on pseudo-random number distribution (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`put`](/docs/cpp/library/io/basic_ostream/put)</DescriptionTerm>
    <DescriptionBody>inserts a character (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`write`](/docs/cpp/library/io/basic_ostream/write)</DescriptionTerm>
    <DescriptionBody>inserts blocks of characters (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_chars`](/docs/cpp/library/text/to_chars) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts an integer or floating-point value to a character sequence (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
