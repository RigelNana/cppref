---
title: "std::basic_ostream<CharT,Traits>::write"
description: "Behaves as an UnformattedOutputFunction. After constructing and checking the sentry object, outputs the characters from successive locations in the character array whose first element is pointed to by s."
source_url: "https://en.cppreference.com/w/cpp/io/basic_ostream/write"
language: "C++"
---

{/* source:cpp-io-basic-ostream-write:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`basic_ostream& write( const char_type* s, std::streamsize count );`} />
</DeclarationDoc>

{/* source:cpp-io-basic-ostream-write:0001 */}
Behaves as an [*UnformattedOutputFunction*](/docs/cpp/library/named_req/UnformattedOutputFunction). After constructing and checking the sentry object, outputs the characters from successive locations in the character array whose first element is pointed to by `s`. Characters are inserted into the output sequence until one of the following occurs:

{/* source:cpp-io-basic-ostream-write:0002 */}
- exactly `count` characters are inserted
- inserting into the output sequence fails (in which case `setstate(badbit)` is called).

### Parameters

{/* source:cpp-io-basic-ostream-write:0003 */}
<ParameterList>
  <Parameter name="s">pointer to the character string to write</Parameter>
  <Parameter name="count">number of characters to write</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-basic-ostream-write:0004 */}
`*this`

### Exceptions

{/* source:cpp-io-basic-ostream-write:0005 */}
If an internal operation throws an exception, it is caught and [`badbit`](/docs/cpp/library/io/ios_base/iostate) is set. If [`exceptions()`](/docs/cpp/library/io/basic_ios/exceptions) is set for `badbit`, the exception is rethrown.

### Notes

{/* source:cpp-io-basic-ostream-write:0006 */}
This function is not overloaded for the types `signed char` or `unsigned char`, unlike the formatted [`operator<<`](/docs/cpp/library/io/basic_ostream/operator_ltlt2).

{/* source:cpp-io-basic-ostream-write:0007 */}
Also, unlike the formatted output functions, this function does not set the [`failbit`](/docs/cpp/library/io/ios_base/iostate) on failure.

{/* source:cpp-io-basic-ostream-write:0008 */}
When using a non-converting locale (the default locale is non-converting), the overrider of this function in [`std::basic_ofstream`](/docs/cpp/library/io/basic_ofstream) may be optimized for zero-copy bulk I/O (by means of overriding [`std::streambuf::xsputn`](/docs/cpp/library/io/basic_streambuf/sputn)).

### Example

{/* source:cpp-io-basic-ostream-write:0009 */}
This function may be used to output object representations, i.e. binary output

```cpp
#include <iostream>
 
int main()
{
    int n = 0x41424344;
    std::cout.write(reinterpret_cast<char*>(&n), sizeof n) << '\n';
 
    char c[] = "This is sample text.";
    std::cout.write(c, 4).write("!\n", 2);
}
```

Possible output:

```text
DCBA
This!
```

### See also

{/* source:cpp-io-basic-ostream-write:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<(std::basic_ostream)`](/docs/cpp/library/io/basic_ostream/operator_ltlt2)</DescriptionTerm>
    <DescriptionBody>inserts character data or insert into rvalue stream (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`put`](/docs/cpp/library/io/basic_ostream/put)</DescriptionTerm>
    <DescriptionBody>inserts a character (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
