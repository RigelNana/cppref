---
title: "std::cin, std::wcin"
description: "The global objects `std::cin` and `std::wcin` control input from a stream buffer of implementation-defined type (derived from `std::streambuf`), associated with the standard C input stream `stdin`."
source_url: "https://en.cppreference.com/w/cpp/io/cin"
language: "C++"
---

{/* source:cpp-io-cin:0000 */}
<DeclarationDoc>
Defined in header [`<iostream>`](/docs/cpp/library/headers/iostream)

  <Declaration id="1" language="cpp" code={`extern std::istream cin;`} />
  <Declaration id="2" language="cpp" code={`extern std::wistream wcin;`} />

  <DeclarationDescription>
    {/* source:cpp-io-cin:0001 */}
    The global objects `std::cin` and `std::wcin` control input from a stream buffer of implementation-defined type (derived from [`std::streambuf`](/docs/cpp/library/io/basic_streambuf)), associated with the standard C input stream [`stdin`](/docs/cpp/library/io/c/std_streams).

    {/* source:cpp-io-cin:0002 */}
    These objects are guaranteed to be initialized during or before the first time an object of type [`std::ios_base::Init`](/docs/cpp/library/io/ios_base/Init) is constructed and are available for use in the constructors and destructors of static objects with [ordered initialization](/docs/cpp/language/initialization#Non-local_variables) (as long as `<iostream>` is included before the object is defined).

    {/* source:cpp-io-cin:0003 */}
    Unless `sync_with_stdio(false)` has been issued, it is safe to concurrently access these objects from multiple threads for both formatted and unformatted input.

    {/* source:cpp-io-cin:0004 */}
    Once `std::cin` is constructed, `std::cin.tie()` returns `&std::cout`, and likewise, `std::wcin.tie()` returns `&std::wcout`. This means that any formatted input operation on `std::cin` forces a call to `std::cout.flush()` if any characters are pending for output.
  </DeclarationDescription>
</DeclarationDoc>

### Notes

{/* source:cpp-io-cin:0005 */}
The 'c' in the name refers to "character" ([stroustrup.com FAQ](https://www.stroustrup.com/bs_faq2.html#cout)); `cin` means "character input" and `wcin` means "wide character input".

### Example

{/* source:cpp-io-cin:0006 */}
```cpp
#include <iostream>
 
struct Foo
{
    int n;
    Foo()
    {
        std::cout << "Enter n: "; // no flush needed
        std::cin >> n;
    }
};
 
Foo f; // static object
 
int main()
{
    std::cout << "f.n is " << f.n << '\n';
}
```

Possible output:

```text
Enter n: 10
f.n is 10
```

### See also

{/* source:cpp-io-cin:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`Init`](/docs/cpp/library/io/ios_base/Init)</DescriptionTerm>
    <DescriptionBody>initializes standard stream objects (public member class of `std::ios_base`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cout wcout`](/docs/cpp/library/io/basic_ostream/cout)</DescriptionTerm>
    <DescriptionBody>writes to the standard C output stream [`stdout`](/docs/cpp/library/io/c/std_streams) (global object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`stdin stdout stderr`](/docs/cpp/library/io/c/std_streams)</DescriptionTerm>
    <DescriptionBody>expression of type `FILE*` associated with the input stream<br />expression of type `FILE*` associated with the output stream<br />expression of type `FILE*` associated with the error output stream (macro constant)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
