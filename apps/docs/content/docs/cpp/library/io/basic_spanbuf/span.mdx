---
title: "std::basic_spanbuf<CharT,Traits>::span"
description: "1) Gets a span referencing the written area if std::ios_base::out is set in the open mode, or a span referencing the underlying buffer otherwise."
source_url: "https://en.cppreference.com/w/cpp/io/basic_spanbuf/span"
language: "C++"
---

{/* source:cpp-io-basic-spanbuf-span:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++23" code={`std::span<CharT> span() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++23" code={`void span( std::span<CharT> s ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-io-basic-spanbuf-span:0001 */}
1. Gets a `span` referencing the written area if [`std::ios_base::out`](/docs/cpp/library/io/ios_base/openmode) is set in the open mode, or a `span` referencing the underlying buffer otherwise.

{/* source:cpp-io-basic-spanbuf-span:0002 */}
2. Makes the `basic_spanbuf` perform I/O on the buffer referenced by `s`. Sets pointers to get area, put area, or both.

| Set bits in open mode (affecting pointers to get area) | Return value after setting [`eback()`](/docs/cpp/library/io/basic_streambuf/gptr) | Return value after setting [`gptr()`](/docs/cpp/library/io/basic_streambuf/gptr) | Return value after setting [`egptr()`](/docs/cpp/library/io/basic_streambuf/gptr) |
| --- | --- | --- | --- |
| `std::ios_base::in` | `s.data()` | `s.data()` | `s.data() + s.size()` |

| Set bits in open mode (affecting pointers to put area) | Return value after setting [`pbase()`](/docs/cpp/library/io/basic_streambuf/pptr) | Return value after setting [`pptr()`](/docs/cpp/library/io/basic_streambuf/pptr) | Return value after setting [`epptr()`](/docs/cpp/library/io/basic_streambuf/pptr) |
| --- | --- | --- | --- |
| `std::ios_base::out && !std::ios_base::ate` | `s.data()` | `s.data()` | `s.data() + s.size()` |
| `std::ios_base::out && std::ios_base::ate` | `s.data()` | `s.data() + s.size()` | `s.data() + s.size()` |

### Parameters

{/* source:cpp-io-basic-spanbuf-span:0003 */}
<ParameterList>
  <Parameter name="s">a [`std::span`](/docs/cpp/library/container/span) that references the user-provided buffer</Parameter>
</ParameterList>

### Return value

{/* source:cpp-io-basic-spanbuf-span:0004 */}
1. `std::span<CharT>(pbase(), pptr())` if [`std::ios_base::out`](/docs/cpp/library/io/ios_base/openmode) is set in the open mode, or a `std::span<CharT>` that references the whole underlying buffer otherwise.

{/* source:cpp-io-basic-spanbuf-span:0005 */}
2. (none)

### Example

{/* source:cpp-io-basic-spanbuf-span:0006 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-io-basic-spanbuf-span:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`str`](/docs/cpp/library/io/basic_stringbuf/str)</DescriptionTerm>
    <DescriptionBody>replaces or obtains a copy of the associated character string (public member function of `std::basic_stringbuf<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`view`](/docs/cpp/library/io/basic_stringbuf/view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>obtains a view over the underlying character sequence (public member function of `std::basic_stringbuf<CharT,Traits,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`str`](/docs/cpp/library/io/strstreambuf/str)</DescriptionTerm>
    <DescriptionBody>marks the buffer frozen and returns the beginning pointer of the input sequence (public member function of `std::strstreambuf`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
