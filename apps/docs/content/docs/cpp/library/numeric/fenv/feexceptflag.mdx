---
title: "std::fegetexceptflag, std::fesetexceptflag"
description: "1) Attempts to obtain the full contents of the floating-point exception flags that are listed in the bitmask argument excepts, which is a bitwise OR of the floating point exception macros."
source_url: "https://en.cppreference.com/w/cpp/numeric/fenv/feexceptflag"
language: "C++"
---

{/* source:cpp-numeric-fenv-feexceptflag:0000 */}
<DeclarationDoc>
Defined in header [`<cfenv>`](/docs/cpp/library/headers/cfenv)

  <Declaration id="1" language="cpp" since="C++11" code={`int fegetexceptflag( std::fexcept_t* flagp, int excepts );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`int fesetexceptflag( const std::fexcept_t* flagp, int excepts );`} />
</DeclarationDoc>

{/* source:cpp-numeric-fenv-feexceptflag:0001 */}
1. Attempts to obtain the full contents of the floating-point exception flags that are listed in the bitmask argument `excepts`, which is a bitwise OR of the [floating point exception macros](/docs/cpp/library/numeric/fenv/FE_exceptions).

{/* source:cpp-numeric-fenv-feexceptflag:0002 */}
2. Attempts to copy the full contents of the floating-point exception flags that are listed in `excepts` from `flagp` into the floating-point environment. Does not raise any exceptions, only modifies the flags.

{/* source:cpp-numeric-fenv-feexceptflag:0003 */}
The full contents of a floating-point exception flag is not necessarily a boolean value indicating whether the exception is raised or cleared. For example, it may be a struct which includes the boolean status and the address of the code that triggered the exception. These functions obtain all such content and obtain/store it in `flagp` in implementation-defined format.

### Parameters

{/* source:cpp-numeric-fenv-feexceptflag:0004 */}
<ParameterList>
  <Parameter name="flagp">pointer to an `std::fexcept_t` object where the flags will be stored or read from</Parameter>
  <Parameter name="excepts">bitmask listing the exception flags to get/set</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-fenv-feexceptflag:0005 */}
`0` on success, non-zero otherwise.

### See also

{/* source:cpp-numeric-fenv-feexceptflag:0006 */}
[C documentation](/docs/c/library/numeric/fenv/feexceptflag) for `fegetexceptflag`, `fesetexceptflag`
