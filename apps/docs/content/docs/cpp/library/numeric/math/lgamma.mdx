---
title: "std::lgamma, std::lgammaf, std::lgammal"
description: "Computes the natural logarithm of the absolute value of the gamma function of num."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/lgamma"
language: "C++"
---

{/* source:cpp-numeric-math-lgamma:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       lgamma ( float num );
double      lgamma ( double num );

long double lgamma ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/*floating-point-type*/
            lgamma ( /*floating-point-type*/ num );`} /> (constexpr since C++26)
  <Declaration id="2" language="cpp" since="C++11" code={`float       lgammaf( float num );`} /> (constexpr since C++26)
  <Declaration id="3" language="cpp" since="C++11" code={`long double lgammal( long double num );`} /> (constexpr since C++26)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            lgamma ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/lgamma#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      lgamma ( Integer num );`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-lgamma:0001 */}
    1-3) Computes the natural logarithm of the absolute value of the [gamma function](https://en.wikipedia.org/wiki/gamma_function) of `num`.<InlineRevision since="C++23"> The library provides overloads of `std::lgamma` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-lgamma:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::lgamma` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-lgamma:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-lgamma:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-lgamma:0005 */}
If no errors occur, the value of the logarithm of the gamma function of `num`, that is `loge|∫∞ 0tnum-1 e-t dt|`, is returned.

{/* source:cpp-numeric-math-lgamma:0006 */}
If a pole error occurs, [+HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `+HUGE_VALF`, or `+HUGE_VALL` is returned.

{/* source:cpp-numeric-math-lgamma:0007 */}
If a range error due to overflow occurs, [±HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `±HUGE_VALF`, or `±HUGE_VALL` is returned.

### Error handling

{/* source:cpp-numeric-math-lgamma:0008 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-lgamma:0009 */}
If `num` is zero or is an integer less than zero, a pole error may occur.

{/* source:cpp-numeric-math-lgamma:0010 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-lgamma:0011 */}
- If the argument is 1, +0 is returned.
- If the argument is 2, +0 is returned.
- If the argument is ±0, +∞ is returned and [FE_DIVBYZERO](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If the argument is a negative integer, +∞ is returned and [FE_DIVBYZERO](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If the argument is ±∞, +∞ is returned.
- If the argument is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-lgamma:0012 */}
If `num` is a natural number, `std::lgamma(num)` is the logarithm of the factorial of `num - 1`.

{/* source:cpp-numeric-math-lgamma:0013 */}
The [POSIX version of `lgamma`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/lgamma.html) is not thread-safe: each execution of the function stores the sign of the gamma function of `num` in the static external variable `signgam`. Some implementations provide `lgamma_r`, which takes a pointer to user-provided storage for `singgam` as the second parameter, and is thread-safe.

{/* source:cpp-numeric-math-lgamma:0014 */}
There is a non-standard function named `gamma` in various implementations, but its definition is inconsistent. For example, glibc and 4.2BSD version of `gamma` executes `lgamma`, but 4.4BSD version of `gamma` executes `tgamma`.

{/* source:cpp-numeric-math-lgamma:0015 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::lgamma(num)` has the same effect as `std::lgamma(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-lgamma:0016 */}
```cpp
#include <cerrno>
#include <cfenv>
#include <cmath>
#include <cstring>
#include <iostream>
 
// #pragma STDC FENV_ACCESS ON
 
const double pi = std::acos(-1); // or std::numbers::pi since C++20
 
int main()
{
    std::cout << "lgamma(10) = " << std::lgamma(10)
              << ", log(9!) = " << std::log(std::tgamma(10))
              << ", exp(lgamma(10)) = " << std::exp(std::lgamma(10)) << '\n'
              << "lgamma(0.5) = " << std::lgamma(0.5)
              << ", log(sqrt(pi)) = " << std::log(std::sqrt(pi)) << '\n';
 
    // special values
    std::cout << "lgamma(1) = " << std::lgamma(1) << '\n'
              << "lgamma(+Inf) = " << std::lgamma(INFINITY) << '\n';
 
    // error handling
    errno = 0;
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "lgamma(0) = " << std::lgamma(0) << '\n';
 
    if (errno == ERANGE)
        std::cout << "    errno == ERANGE: " << std::strerror(errno) << '\n';
    if (std::fetestexcept(FE_DIVBYZERO))
        std::cout << "    FE_DIVBYZERO raised\n";
}
```

Output:

```text
lgamma(10) = 12.8018, log(9!) = 12.8018, exp(lgamma(10)) = 362880
lgamma(0.5) = 0.572365, log(sqrt(pi)) = 0.572365
lgamma(1) = 0
lgamma(+Inf) = inf
lgamma(0) = inf
    errno == ERANGE: Numerical result out of range
    FE_DIVBYZERO raised
```

### See also

{/* source:cpp-numeric-math-lgamma:0017 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`tgamma`, `tgammaf`, `tgammal`](/docs/cpp/library/numeric/math/tgamma) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>gamma function <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/lgamma) for lgamma

### External links

{/* source:cpp-numeric-math-lgamma:0018 */}
[Weisstein, Eric W. "Log Gamma Function."](https://mathworld.wolfram.com/LogGammaFunction.html) From MathWorld — A Wolfram Web Resource.
