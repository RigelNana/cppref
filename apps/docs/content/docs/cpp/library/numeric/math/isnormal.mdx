---
title: "std::isnormal"
description: "Determines if the given floating point number num is normal, i.e."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/isnormal"
language: "C++"
---

{/* source:cpp-numeric-math-isnormal:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`bool isnormal( float num );
bool isnormal( double num );

bool isnormal( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr bool isnormal( /*floating-point-type*/ num );`} />
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr typename /*deduced-simd-t*/<V>::mask_type

  isnormal ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/isnormal#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Integer >
bool isnormal( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-isnormal:0001 */}
    1) Determines if the given floating point number `num` is normal, i.e. is neither zero, subnormal, infinite, nor NaN.<InlineRevision since="C++23"> The library provides overloads for all cv-unqualified floating-point types as the type of the parameter `num`.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-isnormal:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::isnormal` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-isnormal:0003 */}
A) Additional overloads are provided for all integer types, which are treated as `double`.

### Parameters

{/* source:cpp-numeric-math-isnormal:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
  <Parameter name="v_num">a data-parallel object of std::basic_simd specialization where its element type is a floating-point type</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-isnormal:0005 */}
1) `true` if `num` is normal, `false` otherwise.

{/* source:cpp-numeric-math-isnormal:0006 */}
S) A data-parallel mask object where the ith element equals `true` if `v_num[i]` is normal or `false` otherwise for all `i` in the range `[​0​, v_num.size())`.

### Notes

{/* source:cpp-numeric-math-isnormal:0007 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::isnormal(num)` has the same effect as `std::isnormal(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-isnormal:0008 */}
```cpp
#include <cfloat>
#include <cmath>
#include <iostream>
 
int main()
{
    std::cout << std::boolalpha
              << "isnormal(NaN) = " << std::isnormal(NAN) << '\n'
              << "isnormal(Inf) = " << std::isnormal(INFINITY) << '\n'
              << "isnormal(0.0) = " << std::isnormal(0.0) << '\n'
              << "isnormal(DBL_MIN/2.0) = " << std::isnormal(DBL_MIN / 2.0) << '\n'
              << "isnormal(1.0) = " << std::isnormal(1.0) << '\n';
}
```

Output:

```text
isnormal(NaN) = false
isnormal(Inf) = false
isnormal(0.0) = false
isnormal(DBL_MIN/2.0) = false
isnormal(1.0) = true
```

### See also

{/* source:cpp-numeric-math-isnormal:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`fpclassify`](/docs/cpp/library/numeric/math/fpclassify) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>categorizes the given floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isfinite`](/docs/cpp/library/numeric/math/isfinite) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number has finite value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isinf`](/docs/cpp/library/numeric/math/isinf) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is infinite <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isnan`](/docs/cpp/library/numeric/math/isnan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is NaN <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/isnormal) for isnormal
