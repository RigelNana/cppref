---
title: "std::isinf"
description: "Determines if the given floating-point number num is a positive or negative infinity."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/isinf"
language: "C++"
---

{/* source:cpp-numeric-math-isinf:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`bool isinf( float num );
bool isinf( double num );

bool isinf( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr bool isinf( /*floating-point-type*/ num );`} />
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr typename /*deduced-simd-t*/<V>::mask_type

  isinf ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/isinf#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Integer >
bool isinf( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-isinf:0001 */}
    1) Determines if the given floating-point number `num` is a positive or negative infinity.<InlineRevision since="C++23"> The library provides overloads for all cv-unqualified floating-point types as the type of the parameter `num`.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-isinf:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::isinf` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-isinf:0003 */}
A) Additional overloads are provided for all integer types, which are treated as `double`.

### Parameters

{/* source:cpp-numeric-math-isinf:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
  <Parameter name="v_num">a data-parallel object of std::basic_simd specialization where its element type is a floating-point type</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-isinf:0005 */}
1) `true` if `num` is infinite, `false` otherwise.

{/* source:cpp-numeric-math-isinf:0006 */}
S) A data-parallel mask object where the ith element equals `true` if `v_num[i]` is infinite or `false` otherwise for all `i` in the range `[​0​, v_num.size())`.

### Notes

{/* source:cpp-numeric-math-isinf:0007 */}
[GCC](https://gcc.gnu.org/wiki/FloatingPointMath) and [Clang](https://clang.llvm.org/docs/UsersManual.html#controlling-floating-point-behavior) support a `-ffinite-math` option (additionally implied by `-ffast-math`), which allows the respective compiler to assume the nonexistence of special IEEE-754 floating point values such as NaN, infinity, or negative zero. In other words, `std::isinf` is assumed to always return `false` under this option.

{/* source:cpp-numeric-math-isinf:0008 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::isinf(num)` has the same effect as `std::isinf(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-isinf:0009 */}
```cpp
#include <cfloat>
#include <cmath>
#include <iostream>
#include <limits>
 
int main()
{
    const double max = std::numeric_limits<double>::max();
    const double inf = std::numeric_limits<double>::infinity();
 
    std::cout << std::boolalpha
              << "isinf(NaN) = " << std::isinf(NAN) << '\n'
              << "isinf(Inf) = " << std::isinf(INFINITY) << '\n'
              << "isinf(max) = " << std::isinf(max) << '\n'
              << "isinf(inf) = " << std::isinf(inf) << '\n'
              << "isinf(0.0) = " << std::isinf(0.0) << '\n'
              << "isinf(exp(800)) = " << std::isinf(std::exp(800)) << '\n'
              << "isinf(DBL_MIN/2.0) = " << std::isinf(DBL_MIN / 2.0) << '\n';
}
```

Output:

```text
isinf(NaN) = false
isinf(Inf) = true
isinf(max) = false
isinf(inf) = true
isinf(0.0) = false
isinf(exp(800)) = true
isinf(DBL_MIN/2.0) = false
```

### See also

{/* source:cpp-numeric-math-isinf:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`fpclassify`](/docs/cpp/library/numeric/math/fpclassify) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>categorizes the given floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isfinite`](/docs/cpp/library/numeric/math/isfinite) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number has finite value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isnan`](/docs/cpp/library/numeric/math/isnan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is NaN <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isnormal`](/docs/cpp/library/numeric/math/isnormal) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is normal <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/isinf) for isinf
