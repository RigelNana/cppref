---
title: "std::isunordered"
description: "Determines if the floating point numbers x and y are unordered, that is, one or both are NaN and thus cannot be meaningfully compared with each other."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/isunordered"
language: "C++"
---

{/* source:cpp-numeric-math-isunordered:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`bool isunordered( float x, float y );
bool isunordered( double x, double y );

bool isunordered( long double x, long double y );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr bool isunordered( /* floating-point-type */ x,
                            /* floating-point-type */ y );`} />
[Additional overloads](/docs/cpp/library/numeric/math/isunordered#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Arithmetic1, class Arithmetic2 >
bool isunordered( Arithmetic1 x, Arithmetic2 y );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-isunordered:0001 */}
    1) Determines if the floating point numbers `x` and `y` are unordered, that is, one or both are NaN and thus cannot be meaningfully compared with each other.<InlineRevision since="C++23"> The library provides overloads for all cv-unqualified floating-point types as the type of the parameters `x` and `y`.</InlineRevision>

    {/* source:cpp-numeric-math-isunordered:0002 */}
    A) Additional overloads are provided for all other combinations of arithmetic types.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-math-isunordered:0003 */}
<ParameterList>
  <Parameter name="x, y">floating-point or integer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-isunordered:0004 */}
`true` if either `x` or `y` is NaN, `false` otherwise.

### Notes

{/* source:cpp-numeric-math-isunordered:0005 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-math-isunordered:0006 */}
<Revision until="C++23">
If `num1` or `num2` has type `long double`, then `std::isunordered(num1, num2)` has the same effect as `std::isunordered(static_cast<long double>(num1), static_cast<long double>(num2))`. Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::isunordered(num1, num2)` has the same effect as `std::isunordered(static_cast<double>(num1), static_cast<double>(num2))`. Otherwise, if `num1` or `num2` has type `float`, then `std::isunordered(num1, num2)` has the same effect as `std::isunordered(static_cast<float>(num1), static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::isunordered(num1, num2)` has the same effect as `std::isunordered(static_cast</*common-floating-point-type*/>(num1), static_cast</*common-floating-point-type*/>(num2))`, where `/*common-floating-point-type*/` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-math-isunordered:0007 */}
```cpp
#include <cmath>
#include <iostream>
 
#define SHOW_UNORDERED(x, y) \
    std::cout << std::boolalpha << "isunordered(" \
              << #x << ", " << #y << "): " \
              << std::isunordered(x, y) << '\n'
 
int main()
{
    SHOW_UNORDERED(10, 01);
    SHOW_UNORDERED(INFINITY, NAN);
    SHOW_UNORDERED(INFINITY, INFINITY);
    SHOW_UNORDERED(NAN, NAN);
}
```

Output:

```text
isunordered(10, 01): false
isunordered(INFINITY, NAN): true
isunordered(INFINITY, INFINITY): false
isunordered(NAN, NAN): true
```

### See also

{/* source:cpp-numeric-math-isunordered:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`fpclassify`](/docs/cpp/library/numeric/math/fpclassify) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>categorizes the given floating-point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isnan`](/docs/cpp/library/numeric/math/isnan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is NaN <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/isunordered) for isunordered
