---
title: "std::sin, std::sinf, std::sinl"
description: "Computes the sine of num (measured in radians)."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/sin"
language: "C++"
---

{/* source:cpp-numeric-math-sin:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       sin ( float num );
double      sin ( double num );

long double sin ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/*floating-point-type*/
            sin ( /*floating-point-type*/ num );`} /> (constexpr since C++26)
  <Declaration id="2" language="cpp" since="C++11" code={`float       sinf( float num );`} /> (constexpr since C++26)
  <Declaration id="3" language="cpp" since="C++11" code={`long double sinl( long double num );`} /> (constexpr since C++26)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            sin ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/sin#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      sin ( Integer num );`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-sin:0001 */}
    1-3) Computes the sine of `num` (measured in radians).<InlineRevision since="C++23"> The library provides overloads of `std::sin` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-sin:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::sin` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-sin:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-sin:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value representing angle in radians</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-sin:0005 */}
If no errors occur, the sine of `num` (`sin(num)`) in the range `[-1, +1]`, is returned.

{/* source:cpp-numeric-math-sin:0006 */}
<Revision until="C++11">
The result may have little or no significance if the magnitude of `num` is large.
</Revision>

{/* source:cpp-numeric-math-sin:0007 */}
If a domain error occurs, an implementation-defined value is returned (NaN where supported).

{/* source:cpp-numeric-math-sin:0008 */}
If a range error occurs due to underflow, the correct result (after rounding) is returned.

### Error handling

{/* source:cpp-numeric-math-sin:0009 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-sin:0010 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-sin:0011 */}
- if the argument is ±0, it is returned unmodified.
- if the argument is ±∞, NaN is returned and [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- if the argument is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-sin:0012 */}
The case where the argument is infinite is not specified to be a domain error in C (to which C++ defers), but it is defined as a [domain error in POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/functions/sin.html).

{/* source:cpp-numeric-math-sin:0013 */}
POSIX also specifies that in case of underflow, `num` is returned unmodified, and if that is not supported, an implementation-defined value no greater than [DBL_MIN](/docs/cpp/library/utility/types/climits), [FLT_MIN](/docs/cpp/library/utility/types/climits), and [LDBL_MIN](/docs/cpp/library/utility/types/climits) is returned.

{/* source:cpp-numeric-math-sin:0014 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::sin(num)` has the same effect as `std::sin(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-sin:0015 */}
```cpp
#include <cerrno>
#include <cfenv>
#include <cmath>
#include <iomanip>
#include <iostream>
 
// #pragma STDC FENV_ACCESS ON
 
const double pi = std::acos(-1); // or std::numbers::pi since C++20
 
constexpr double your_sin(double x)
{
    double sin{0}, pow{x};
    for (auto fac{1LLU}, n{1ULL}; n != 20; fac *= ++n, pow *= x)
        if (n & 1)
            sin += (n & 2 ? -pow : pow) / fac;
    return sin;
}
 
int main()
{
    std::cout << std::setprecision(10) << std::showpos
              << "Typical usage:\n"
              << "std::sin(pi/6) = " << std::sin(pi / 6) << '\n'
              << "your sin(pi/6) = " << your_sin(pi / 6) << '\n'
              << "std::sin(pi/2) = " << std::sin(pi / 2) << '\n'
              << "your sin(pi/2) = " << your_sin(pi / 2) << '\n'
              << "std::sin(-3*pi/4) = " << std::sin(-3 * pi / 4) << '\n'
              << "your sin(-3*pi/4) = " << your_sin(-3 * pi / 4) << '\n'
              << "Special values:\n"
              << "std::sin(+0) = " << std::sin(0.0) << '\n'
              << "std::sin(-0) = " << std::sin(-0.0) << '\n';
 
    // error handling
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "std::sin(INFINITY) = " << std::sin(INFINITY) << '\n';
    if (std::fetestexcept(FE_INVALID))
        std::cout << "    FE_INVALID raised\n";
}
```

Possible output:

```text
Typical usage:
std::sin(pi/6) = +0.5
your sin(pi/6) = +0.5
std::sin(pi/2) = +1
your sin(pi/2) = +1
std::sin(-3*pi/4) = -0.7071067812
your sin(-3*pi/4) = -0.7071067812
Special values:
std::sin(+0) = +0
std::sin(-0) = -0
std::sin(INFINITY) = -nan
    FE_INVALID raised
```

### See also

{/* source:cpp-numeric-math-sin:0016 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`cos`, `cosf`, `cosl`](/docs/cpp/library/numeric/math/cos) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes cosine (`cos(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tan`, `tanf`, `tanl`](/docs/cpp/library/numeric/math/tan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes tangent (`tan(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`asin`, `asinf`, `asinl`](/docs/cpp/library/numeric/math/asin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc sine (`arcsin(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`sin(std::complex)`](/docs/cpp/library/numeric/complex/sin)</DescriptionTerm>
    <DescriptionBody>computes sine of a complex number (`sin(z)`) <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`sin(std::valarray)`](/docs/cpp/library/numeric/valarray/sin)</DescriptionTerm>
    <DescriptionBody>applies the function std::sin to each element of valarray <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/sin) for sin
