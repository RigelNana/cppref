---
title: "std::signbit"
description: "Determines if the given floating point number num is negative."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/signbit"
language: "C++"
---

{/* source:cpp-numeric-math-signbit:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`bool signbit( float num );
bool signbit( double num );

bool signbit( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr bool signbit( /*floating-point-type*/ num );`} />
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr typename /*deduced-simd-t*/<V>::mask_type

  signbit ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/signbit#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Integer >
bool signbit( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-signbit:0001 */}
    1) Determines if the given floating point number `num` is negative.<InlineRevision since="C++23"> The library provides overloads for all cv-unqualified floating-point types as the type of the parameter `num`.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-signbit:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::signbit` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-signbit:0003 */}
A) Additional overloads are provided for all integer types, which are treated as `double`.

### Parameters

{/* source:cpp-numeric-math-signbit:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
  <Parameter name="v_num">a data-parallel object of std::basic_simd specialization where its element type is a floating-point type</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-signbit:0005 */}
1) `true` if `num` is negative, `false` otherwise.

{/* source:cpp-numeric-math-signbit:0006 */}
S) A data-parallel mask object where the ith element equals `true` if `v_num[i]` is negative or `false` otherwise for all `i` in the range `[​0​, v_num.size())`.

### Notes

{/* source:cpp-numeric-math-signbit:0007 */}
This function detects the sign bit of zeroes, infinities, and NaNs. Along with [std::copysign](/docs/cpp/library/numeric/math/copysign), `std::signbit` is one of the only two portable ways to examine the sign of a NaN.

{/* source:cpp-numeric-math-signbit:0008 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::signbit(num)` has the same effect as `std::signbit(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-signbit:0009 */}
```cpp
#include <cmath>
#include <iostream>
 
int main()
{
    std::cout << std::boolalpha
              << "signbit(+0.0) = " << std::signbit(+0.0) << '\n'
              << "signbit(-0.0) = " << std::signbit(-0.0) << '\n'
              << "signbit(+nan) = " << std::signbit(+NAN) << '\n'
              << "signbit(-nan) = " << std::signbit(-NAN) << '\n'
              << "signbit(+inf) = " << std::signbit(+INFINITY) << '\n'
              << "signbit(-inf) = " << std::signbit(-INFINITY) << '\n';
}
```

Output:

```text
signbit(+0.0) = false
signbit(-0.0) = true
signbit(+nan) = false
signbit(-nan) = true
signbit(+inf) = false
signbit(-inf) = true
```

### See also

{/* source:cpp-numeric-math-signbit:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`abs(float)`, `fabs`, `fabsf`, `fabsl`](/docs/cpp/library/numeric/math/fabs) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>absolute value of a floating point value (`|x|`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`copysign`, `copysignf`, `copysignl`](/docs/cpp/library/numeric/math/copysign) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>copies the sign of a floating point value <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/signbit) for signbit
