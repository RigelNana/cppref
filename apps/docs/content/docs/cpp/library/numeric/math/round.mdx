---
title: "std::round, std::roundf, std::roundl, std::lround, std::lroundf, std::lroundl, std::llround, std::llroundf"
description: "Computes the nearest integer value to num (in floating-point format), rounding halfway cases away from zero, regardless of the current rounding mode."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/round"
language: "C++"
---

{/* source:cpp-numeric-math-round:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
Rounding to floating-point types
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`float       round ( float num );
double      round ( double num );

long double round ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /* floating-point-type */
            round ( /* floating-point-type */ num );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`float       roundf( float num );`} /> (constexpr since C++23)
  <Declaration id="3" language="cpp" since="C++11" code={`long double roundl( long double num );`} /> (constexpr since C++23)
Rounding to `long`
  <Declaration id="4" language="cpp" since="C++11" until="C++23" code={`long lround ( float num );
long lround ( double num );

long lround ( long double num );`} />
  <Declaration id="4" language="cpp" since="C++23" code={`constexpr long lround( /* floating-point-type */ num );`} />
  <Declaration id="5" language="cpp" since="C++11" code={`long lroundf( float num );`} /> (constexpr since C++23)
  <Declaration id="6" language="cpp" since="C++11" code={`long lroundl( long double num );`} /> (constexpr since C++23)
Rounding to `long long`
  <Declaration id="7" language="cpp" since="C++11" until="C++23" code={`long long llround ( float num );
long long llround ( double num );

long long llround ( long double num );`} />
  <Declaration id="7" language="cpp" since="C++23" code={`constexpr long long llround( /* floating-point-type */ num );`} />
  <Declaration id="8" language="cpp" since="C++11" code={`long long llroundf( float num );`} /> (constexpr since C++23)
  <Declaration id="9" language="cpp" since="C++11" code={`long long llroundl( long double num );`} /> (constexpr since C++23)
[Additional overloads](/docs/cpp/library/numeric/math/round#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Integer >
double round( Integer num );`} /> (constexpr since C++23)
  <Declaration id="B" language="cpp" since="C++11" code={`template< class Integer >
long lround( Integer num );`} /> (constexpr since C++23)
  <Declaration id="C" language="cpp" since="C++11" code={`template< class Integer >
long long llround( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-round:0001 */}
    1-3) Computes the nearest integer value to `num` (in floating-point format), rounding halfway cases away from zero, regardless of the current rounding mode.<InlineRevision since="C++23"> The library provides overloads of `std::round` for all cv-unqualified floating-point types as the type of the parameter `num`.</InlineRevision>

    {/* source:cpp-numeric-math-round:0002 */}
    4-9) Computes the nearest integer value to `num` (in integer format), rounding halfway cases away from zero, regardless of the current rounding mode.<InlineRevision since="C++23"> The library provides overloads of `std::lround` and `std::llround` for all cv-unqualified floating-point types as the type of the parameter `num`.</InlineRevision>

    {/* source:cpp-numeric-math-round:0003 */}
    A-C) Additional overloads are provided for all integer types, which are treated as `double`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-math-round:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-round:0005 */}
If no errors occur, the nearest integer value to `num`, rounding halfway cases away from zero, is returned.

{/* source:cpp-numeric-math-round:0006 */}
Return value · `num`

{/* source:cpp-numeric-math-round:0007 */}
If a domain error occurs, an implementation-defined value is returned.

### Error handling

{/* source:cpp-numeric-math-round:0008 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-round:0009 */}
If the result of `std::lround` or `std::llround` is outside the range representable by the return type, a domain error or a range error may occur.

{/* source:cpp-numeric-math-round:0010 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-round:0011 */}
For the
`std::round`
function:
- The current [rounding mode](/docs/cpp/library/numeric/fenv/FE_round) has no effect.
- If `num` is ±∞, it is returned, unmodified.
- If `num` is ±0, it is returned, unmodified.
- If `num` is NaN, NaN is returned.

For
`std::lround`
and
`std::llround`
functions:
- [FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) is never raised.
- The current [rounding mode](/docs/cpp/library/numeric/fenv/FE_round) has no effect.
- If `num` is ±∞, [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised and an implementation-defined value is returned.
- If the result of the rounding is outside the range of the return type, [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised and an implementation-defined value is returned.
- If `num` is NaN, [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised and an implementation-defined value is returned.

### Notes

{/* source:cpp-numeric-math-round:0012 */}
[FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) may be (but is not required to be) raised by `std::round` when rounding a non-integer finite value.

{/* source:cpp-numeric-math-round:0013 */}
The largest representable floating-point values are exact integers in all standard floating-point formats, so `std::round` never overflows on its own; however the result may overflow any integer type (including [std::intmax_t](/docs/cpp/library/utility/types/integer)), when stored in an integer variable.

{/* source:cpp-numeric-math-round:0014 */}
[POSIX specifies](https://pubs.opengroup.org/onlinepubs/9699919799/functions/lround.html) that all cases where `std::lround` or `std::llround` raise [FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) are domain errors.

{/* source:cpp-numeric-math-round:0015 */}
The `double` version of `std::round` behaves as if implemented as follows:

{/* source:cpp-numeric-math-round:0016 */}
```cpp
#include <cfenv>
#include <cmath>
 
#pragma STDC FENV_ACCESS ON
 
double round(double x)
{
    const int save_round = std::fegetround();
    std::fesetround(FE_TOWARDZERO);
    const double result = std::rint(std::copysign(0.5 + std::fabs(x), x));
    std::fesetround(save_round);
    return result;
}
```

{/* source:cpp-numeric-math-round:0017 */}
The additional overloads are not required to be provided exactly as (A-C). They only need to be sufficient to ensure that for their argument `num` of integer type:

{/* source:cpp-numeric-math-round:0018 */}
- `std::round(num)` has the same effect as `std::round(static_cast<double>(num))`.
- `std::lround(num)` has the same effect as `std::lround(static_cast<double>(num))`.
- `std::llround(num)` has the same effect as `std::llround(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-round:0019 */}
```cpp
#include <cassert>
#include <cfenv>
#include <cfloat>
#include <climits>
#include <cmath>
#include <iostream>
 
// #pragma STDC FENV_ACCESS ON
 
double custom_round(double x)
{
    const int save_round = std::fegetround();
    std::fesetround(FE_TOWARDZERO);
    const double result = std::rint(std::copysign(0.5 + std::fabs(x), x));
    std::fesetround(save_round);
    return result;
}
 
void test_custom_round()
{
    for (const double x :
        {
            0.0, 0.3,
            0.5 - DBL_EPSILON / 2,
            0.5,
            0.5 + DBL_EPSILON / 2,
            0.7, 1.0, 2.3, 2.5, 2.7, 3.0,
            static_cast<double>(INFINITY)
        })
        assert(round(+x) == custom_round(+x) && round(-x) == custom_round(-x));
}
 
int main()
{
    test_custom_round();
 
    std::cout << std::showpos;
 
    // round
    std::cout << "round(+2.3) = " << std::round(2.3)
              << "  round(+2.5) = " << std::round(2.5)
              << "  round(+2.7) = " << std::round(2.7) << '\n'
              << "round(-2.3) = " << std::round(-2.3)
              << "  round(-2.5) = " << std::round(-2.5)
              << "  round(-2.7) = " << std::round(-2.7) << '\n';
 
    std::cout << "round(-0.0) = " << std::round(-0.0)  << '\n'
              << "round(-Inf) = " << std::round(-INFINITY) << '\n';
 
    // lround
    std::cout << "lround(+2.3) = " << std::lround(2.3)
              << "  lround(+2.5) = " << std::lround(2.5)
              << "  lround(+2.7) = " << std::lround(2.7) << '\n'
              << "lround(-2.3) = " << std::lround(-2.3)
              << "  lround(-2.5) = " << std::lround(-2.5)
              << "  lround(-2.7) = " << std::lround(-2.7) << '\n';
 
    std::cout << "lround(-0.0) = " << std::lround(-0.0)  << '\n'
              << "lround(-Inf) = " << std::lround(-INFINITY) << '\n';
 
    // error handling
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "std::lround(LONG_MAX+1.5) = "
              << std::lround(LONG_MAX + 1.5) << '\n';
    if (std::fetestexcept(FE_INVALID))
        std::cout << "    FE_INVALID was raised\n";
}
```

Possible output:

```text
round(+2.3) = +2  round(+2.5) = +3  round(+2.7) = +3
round(-2.3) = -2  round(-2.5) = -3  round(-2.7) = -3
round(-0.0) = -0
round(-Inf) = -inf
lround(+2.3) = +2  lround(+2.5) = +3  lround(+2.7) = +3
lround(-2.3) = -2  lround(-2.5) = -3  lround(-2.7) = -3
lround(-0.0) = +0
lround(-Inf) = -9223372036854775808
std::lround(LONG_MAX+1.5) = -9223372036854775808
    FE_INVALID was raised
```

### See also

{/* source:cpp-numeric-math-round:0020 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`floor`, `floorf`, `floorl`](/docs/cpp/library/numeric/math/floor) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer not greater than the given value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ceil`, `ceilf`, `ceill`](/docs/cpp/library/numeric/math/ceil) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer not less than the given value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`trunc`, `truncf`, `truncl`](/docs/cpp/library/numeric/math/trunc) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer not greater in magnitude than the given value <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/round) for round
