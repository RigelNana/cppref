---
title: "std::logb, std::logbf, std::logbl"
description: "Extracts the value of the unbiased radix-independent exponent from the floating-point argument num, and returns it as a floating-point value."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/logb"
language: "C++"
---

{/* source:cpp-numeric-math-logb:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       logb ( float num );
double      logb ( double num );

long double logb ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /*floating-point-type*/
            logb ( /*floating-point-type*/ num );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`float       logbf( float num );`} /> (constexpr since C++23)
  <Declaration id="3" language="cpp" since="C++11" code={`long double logbl( long double num );`} /> (constexpr since C++23)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            logb ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/logb#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      logb ( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-logb:0001 */}
    1-3) Extracts the value of the unbiased radix-independent exponent from the floating-point argument `num`, and returns it as a floating-point value.<InlineRevision since="C++23"> The library provides overloads of `std::logb` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-logb:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::logb` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-logb:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

{/* source:cpp-numeric-math-logb:0004 */}
Formally, the unbiased exponent is the signed integral part of `logr|num|` (returned by this function as a floating-point value), for non-zero `num`, where `r` is `std::numeric_limits<T>::radix` and `T` is the floating-point type of `num`. If `num` is subnormal, it is treated as though it was normalized.

### Parameters

{/* source:cpp-numeric-math-logb:0005 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-logb:0006 */}
If no errors occur, the unbiased exponent of `num` is returned as a signed floating-point value.

{/* source:cpp-numeric-math-logb:0007 */}
If a domain error occurs, an implementation-defined value is returned.

{/* source:cpp-numeric-math-logb:0008 */}
If a pole error occurs, [-HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `-HUGE_VALF`, or `-HUGE_VALL` is returned.

### Error handling

{/* source:cpp-numeric-math-logb:0009 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-logb:0010 */}
Domain or range error may occur if `num` is zero.

{/* source:cpp-numeric-math-logb:0011 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-logb:0012 */}
- If `num` is ±0, -∞ is returned and [FE_DIVBYZERO](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If `num` is ±∞, +∞ is returned.
- If `num` is NaN, NaN is returned.
- In all other cases, the result is exact ([FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) is never raised) and [the current rounding mode](/docs/cpp/library/numeric/fenv/FE_round) is ignored.

### Notes

{/* source:cpp-numeric-math-logb:0013 */}
[POSIX requires](https://pubs.opengroup.org/onlinepubs/9699919799/functions/logb.html) that a pole error occurs if `num` is ±0.

{/* source:cpp-numeric-math-logb:0014 */}
The value of the exponent returned by `std::logb` is always 1 less than the exponent returned by [std::frexp](/docs/cpp/library/numeric/math/frexp) because of the different normalization requirements: for the exponent `e` returned by `std::logb`, `|num*r-e |` is between `1` and `r` (typically between `1` and `2`), but for the exponent `e` returned by [std::frexp](/docs/cpp/library/numeric/math/frexp), `|num*2-e |` is between `0.5` and `1`.

{/* source:cpp-numeric-math-logb:0015 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::logb(num)` has the same effect as `std::logb(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-logb:0016 */}
Compares different floating-point decomposition functions:

```cpp
#include <cfenv>
#include <cmath>
#include <iostream>
#include <limits>
// #pragma STDC FENV_ACCESS ON
 
int main()
{
    double f = 123.45;
    std::cout << "Given the number " << f << " or " << std::hexfloat
              << f << std::defaultfloat << " in hex,\n";
 
    double f3;
    double f2 = std::modf(f, &f3);
    std::cout << "modf() makes " << f3 << " + " << f2 << '\n';
 
    int i;
    f2 = std::frexp(f, &i);
    std::cout << "frexp() makes " << f2 << " * 2^" << i << '\n';
 
    i = std::ilogb(f);
    std::cout << "logb()/ilogb() make " << f / std::scalbn(1.0, i) << " * "
              << std::numeric_limits<double>::radix
              << "^" << std::ilogb(f) << '\n';
 
    // error handling
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "logb(0) = " << std::logb(0) << '\n';
    if (std::fetestexcept(FE_DIVBYZERO))
        std::cout << "    FE_DIVBYZERO raised\n";
}
```

Possible output:

```text
Given the number 123.45 or 0x1.edccccccccccdp+6 in hex,
modf() makes 123 + 0.45
frexp() makes 0.964453 * 2^7
logb()/ilogb() make 1.92891 * 2^6
logb(0) = -Inf
    FE_DIVBYZERO raised
```

### See also

{/* source:cpp-numeric-math-logb:0017 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`frexp`, `frexpf`, `frexpl`](/docs/cpp/library/numeric/math/frexp) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>decomposes a number into significand and base-`2` exponent <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ilogb`, `ilogbf`, `ilogbl`](/docs/cpp/library/numeric/math/ilogb) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>extracts exponent of the number <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`scalbn`, `scalbnf`, `scalbnl`, `scalbln`, `scalblnf`, `scalblnl`](/docs/cpp/library/numeric/math/scalbn) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>multiplies a number by [FLT_RADIX](/docs/cpp/library/utility/types/climits) raised to a power <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/logb) for logb
