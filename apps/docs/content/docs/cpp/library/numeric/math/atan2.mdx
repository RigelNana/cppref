---
title: "std::atan2, std::atan2f, std::atan2l"
description: "Computes the arc tangent of y / x using the signs of arguments to determine the correct quadrant."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/atan2"
language: "C++"
---

{/* source:cpp-numeric-math-atan2:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       atan2 ( float y, float x );
double      atan2 ( double y, double x );
long double atan2 ( long double y, long double x );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/*floating-point-type*/
            atan2 ( /*floating-point-type*/ y,
                    /*floating-point-type*/ x );`} /> (constexpr since C++26)
  <Declaration id="2" language="cpp" since="C++11" code={`float       atan2f( float y, float x );`} /> (constexpr since C++26)
  <Declaration id="3" language="cpp" since="C++11" code={`long double atan2l( long double y, long double x );`} /> (constexpr since C++26)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< class V0, class V1 >
constexpr /*math-common-simd-t*/<V0, V1>
            atan2 ( const V0& v_y, const V1& v_x );`} />
[Additional overloads](/docs/cpp/library/numeric/math/atan2#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      atan2 ( Integer y, Integer x );`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-atan2:0001 */}
    1-3) Computes the arc tangent of `y / x` using the signs of arguments to determine the correct quadrant.<InlineRevision since="C++23"> The library provides overloads of `std::atan2` for all cv-unqualified floating-point types as the type of the parameters.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-atan2:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::atan2` on `v_yand` `v_x`.
(See [`math-common-simd-t`](/docs/cpp/library/numeric/simd#math-common-simd-t) for its definition.)
</Revision>

{/* source:cpp-numeric-math-atan2:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-atan2:0004 */}
<ParameterList>
  <Parameter name="y, x">floating-point or integer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-atan2:0005 */}
y x

{/* source:cpp-numeric-math-atan2:0006 */}
`y` argument · `x` argument

{/* source:cpp-numeric-math-atan2:0007 */}
If a domain error occurs, an implementation-defined value is returned (NaN where supported).

{/* source:cpp-numeric-math-atan2:0008 */}
If a range error occurs due to underflow, the correct result (after rounding) is returned.

### Error handling

{/* source:cpp-numeric-math-atan2:0009 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-atan2:0010 */}
Domain error may occur if `x` and `y` are both zero.

{/* source:cpp-numeric-math-atan2:0011 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-atan2:0012 */}
- If `x` and `y` are both zero, domain error *does not* occur.
- If `x` and `y` are both zero, range error does not occur either.
- If `y` is zero, pole error does not occur.
- If `y` is ±0 and `x` is negative or -0, ±π is returned.
- If `y` is ±0 and `x` is positive or +0, ±0 is returned.
- If `y` is ±∞ and `x` is finite, ±π/2 is returned.
- If `y` is ±∞ and `x` is -∞, ±3π/4 is returned.
- If `y` is ±∞ and `x` is +∞, ±π/4 is returned.
- If `x` is ±0 and `y` is negative, -π/2 is returned.
- If `x` is ±0 and `y` is positive, +π/2 is returned.
- If `x` is -∞ and `y` is finite and positive, +π is returned.
- If `x` is -∞ and `y` is finite and negative, -π is returned.
- If `x` is +∞ and `y` is finite and positive, +0 is returned.
- If `x` is +∞ and `y` is finite and negative, -0 is returned.
- If either `x` is NaN or `y` is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-atan2:0013 */}
`std::atan2(y, x)` is equivalent to `std::arg(std::complex<std::common_type_t<decltype(x), decltype(y)>>(x, y))`.

{/* source:cpp-numeric-math-atan2:0014 */}
[POSIX specifies](https://pubs.opengroup.org/onlinepubs/9699919799/functions/atan2.html) that in case of underflow, the value `y / x` is returned, and if that is not supported, an implementation-defined value no greater than [DBL_MIN](/docs/cpp/library/utility/types/climits), [FLT_MIN](/docs/cpp/library/utility/types/climits), and [LDBL_MIN](/docs/cpp/library/utility/types/climits) is returned.

{/* source:cpp-numeric-math-atan2:0015 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-math-atan2:0016 */}
<Revision until="C++23">
If `num1` or `num2` has type `long double`, then `std::atan2(num1, num2)` has the same effect as `std::atan2(static_cast<long double>(num1), static_cast<long double>(num2))`. Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::atan2(num1, num2)` has the same effect as `std::atan2(static_cast<double>(num1), static_cast<double>(num2))`. Otherwise, if `num1` or `num2` has type `float`, then `std::atan2(num1, num2)` has the same effect as `std::atan2(static_cast<float>(num1), static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::atan2(num1, num2)` has the same effect as `std::atan2(static_cast</*common-floating-point-type*/>(num1), static_cast</*common-floating-point-type*/>(num2))`, where `/*common-floating-point-type*/` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-math-atan2:0017 */}
```cpp
#include <cmath>
#include <iostream>
 
void print_coordinates(int x, int y)
{
    std::cout << std::showpos
              << "(x:" << x << ", y:" << y << ") cartesian is "
              << "(r:" << std::hypot(x, y)
              << ", phi:" << std::atan2(y, x) << ") polar\n";
}
 
int main()
{
    // normal usage: the signs of the two arguments determine the quadrant
    print_coordinates(+1, +1); // atan2( 1,  1) =  +pi/4, Quad I
    print_coordinates(-1, +1); // atan2( 1, -1) = +3pi/4, Quad II
    print_coordinates(-1, -1); // atan2(-1, -1) = -3pi/4, Quad III
    print_coordinates(+1, -1); // atan2(-1,  1) =  -pi/4, Quad IV
 
    // special values
    std::cout << std::noshowpos
              << "atan2(0, 0) = " << atan2(0, 0) << '\n'
              << "atan2(0,-0) = " << atan2(0, -0.0) << '\n'
              << "atan2(7, 0) = " << atan2(7, 0) << '\n'
              << "atan2(7,-0) = " << atan2(7, -0.0) << '\n';
}
```

Output:

```text
(x:+1, y:+1) cartesian is (r:1.41421, phi:0.785398) polar
(x:-1, y:+1) cartesian is (r:1.41421, phi:2.35619) polar
(x:-1, y:-1) cartesian is (r:1.41421, phi:-2.35619) polar
(x:+1, y:-1) cartesian is (r:1.41421, phi:-0.785398) polar
atan2(0, 0) = 0
atan2(0,-0) = 3.14159
atan2(7, 0) = 1.5708
atan2(7,-0) = 1.5708
```

### See also

{/* source:cpp-numeric-math-atan2:0018 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`asin`, `asinf`, `asinl`](/docs/cpp/library/numeric/math/asin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc sine (`arcsin(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acos`, `acosf`, `acosl`](/docs/cpp/library/numeric/math/acos) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc cosine (`arccos(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan`, `atanf`, `atanl`](/docs/cpp/library/numeric/math/atan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc tangent (`arctan(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`arg`](/docs/cpp/library/numeric/complex/arg)</DescriptionTerm>
    <DescriptionBody>returns the phase angle <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan2(std::valarray)`](/docs/cpp/library/numeric/valarray/atan2)</DescriptionTerm>
    <DescriptionBody>applies the function std::atan2 to a valarray and a value <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/atan2) for atan2
