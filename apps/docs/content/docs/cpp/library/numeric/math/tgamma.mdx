---
title: "std::tgamma, std::tgammaf, std::tgammal"
description: "Computes the gamma function of num."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/tgamma"
language: "C++"
---

{/* source:cpp-numeric-math-tgamma:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       tgamma ( float num );
double      tgamma ( double num );

long double tgamma ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/*floating-point-type*/
            tgamma ( /*floating-point-type*/ num );`} /> (constexpr since C++26)
  <Declaration id="2" language="cpp" since="C++11" code={`float       tgammaf( float num );`} /> (constexpr since C++26)
  <Declaration id="3" language="cpp" since="C++11" code={`long double tgammal( long double num );`} /> (constexpr since C++26)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            tgamma ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/tgamma#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      tgamma ( Integer num );`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-tgamma:0001 */}
    1-3) Computes the [gamma function](https://en.wikipedia.org/wiki/Gamma_function) of `num`.<InlineRevision since="C++23"> The library provides overloads of `std::tgamma` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-tgamma:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::tgamma` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-tgamma:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-tgamma:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-tgamma:0005 */}
If no errors occur, the value of the gamma function of `num`, that is `∫∞ 0tnum-1 e-t dt`, is returned.

{/* source:cpp-numeric-math-tgamma:0006 */}
If a domain error occurs, an implementation-defined value (NaN where supported) is returned.

{/* source:cpp-numeric-math-tgamma:0007 */}
If a pole error occurs, [±HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `±HUGE_VALF`, or `±HUGE_VALL` is returned.

{/* source:cpp-numeric-math-tgamma:0008 */}
If a range error due to overflow occurs, [±HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `±HUGE_VALF`, or `±HUGE_VALL` is returned.

{/* source:cpp-numeric-math-tgamma:0009 */}
If a range error due to underflow occurs, the correct value (after rounding) is returned.

### Error handling

{/* source:cpp-numeric-math-tgamma:0010 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-tgamma:0011 */}
If `num` is zero or is an integer less than zero, a pole error or a domain error may occur.

{/* source:cpp-numeric-math-tgamma:0012 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-tgamma:0013 */}
- If the argument is ±0, ±∞ is returned and [FE_DIVBYZERO](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If the argument is a negative integer, NaN is returned and [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If the argument is -∞, NaN is returned and [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) is raised.
- If the argument is +∞, +∞ is returned.
- If the argument is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-tgamma:0014 */}
If `num` is a natural number, `std::tgamma(num)` is the factorial of `num - 1`. Many implementations calculate the exact integer-domain factorial if the argument is a sufficiently small integer.

{/* source:cpp-numeric-math-tgamma:0015 */}
For IEEE-compatible type `double`, overflow happens if `0 < num && num < 1 / DBL_MAX` or if `num > 171.7`.

{/* source:cpp-numeric-math-tgamma:0016 */}
[POSIX requires](https://pubs.opengroup.org/onlinepubs/9699919799/functions/tgamma.html) that a pole error occurs if the argument is zero, but a domain error occurs when the argument is a negative integer. It also specifies that in future, domain errors may be replaced by pole errors for negative integer arguments (in which case the return value in those cases would change from NaN to ±∞).

{/* source:cpp-numeric-math-tgamma:0017 */}
There is a non-standard function named `gamma` in various implementations, but its definition is inconsistent. For example, glibc and 4.2BSD version of `gamma` executes `lgamma`, but 4.4BSD version of `gamma` executes `tgamma`.

{/* source:cpp-numeric-math-tgamma:0018 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::tgamma(num)` has the same effect as `std::tgamma(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-tgamma:0019 */}
```cpp
#include <cerrno>
#include <cfenv>
#include <cmath>
#include <cstring>
#include <iostream>
// #pragma STDC FENV_ACCESS ON
 
int main()
{
    std::cout << "tgamma(10) = " << std::tgamma(10)
              << ", 9! = " << 2 * 3 * 4 * 5 * 6 * 7 * 8 * 9 << '\n'
              << "tgamma(0.5) = " << std::tgamma(0.5)
              << ", sqrt(pi) = " << std::sqrt(std::acos(-1)) << '\n';
 
    // special values
    std::cout << "tgamma(1) = " << std::tgamma(1) << '\n'
              << "tgamma(+Inf) = " << std::tgamma(INFINITY) << '\n';
 
    // error handling
    errno = 0;
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "tgamma(-1) = " << std::tgamma(-1) << '\n';
 
    if (errno == EDOM)
        std::cout << "    errno == EDOM: " << std::strerror(errno) << '\n';
    if (std::fetestexcept(FE_INVALID))
        std::cout << "    FE_INVALID raised\n";
}
```

Possible output:

```text
tgamma(10) = 362880, 9! = 362880
tgamma(0.5) = 1.77245, sqrt(pi) = 1.77245
tgamma(1) = 1
tgamma(+Inf) = inf
tgamma(-1) = nan
    errno == EDOM: Numerical argument out of domain
    FE_INVALID raised
```

### See also

{/* source:cpp-numeric-math-tgamma:0020 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`lgamma`, `lgammaf`, `lgammal`](/docs/cpp/library/numeric/math/lgamma) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>natural logarithm of the gamma function <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`beta`, `betaf`, `betal`](/docs/cpp/library/numeric/special_functions/beta) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>beta function <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/tgamma) for tgamma

### External links

{/* source:cpp-numeric-math-tgamma:0021 */}
[Weisstein, Eric W. "Gamma Function."](https://mathworld.wolfram.com/GammaFunction.html) From MathWorld — A Wolfram Web Resource.
