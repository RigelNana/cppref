---
title: "std::copysign, std::copysignf, std::copysignl"
description: "Composes a floating point value with the magnitude of mag and the sign of sgn."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/copysign"
language: "C++"
---

{/* source:cpp-numeric-math-copysign:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       copysign ( float mag, float sgn );
double      copysign ( double mag, double sgn );

long double copysign ( long double mag, long double sgn );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /*floating-point-type*/
            copysign ( /*floating-point-type*/ mag,

                       /*floating-point-type*/ sgn );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`float       copysignf( float mag, float sgn );`} /> (constexpr since C++23)
  <Declaration id="3" language="cpp" since="C++11" code={`long double copysignl( long double mag, long double sgn );`} /> (constexpr since C++23)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< class V0, class V1 >
constexpr /*math-common-simd-t*/<V0, V1>

            copysign ( const V0& v_mag, const V1& v_sgn );`} />
[Additional overloads](/docs/cpp/library/numeric/math/copysign#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      copysign ( Integer mag, Integer sgn );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-copysign:0001 */}
    1-3) Composes a floating point value with the magnitude of `mag` and the sign of `sgn`.<InlineRevision since="C++23"> The library provides overloads of `std::copysign` for all cv-unqualified floating-point types as the type of the parameters.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-copysign:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::copysign` on `v_magand` `v_sgn`.
(See [`math-common-simd-t`](/docs/cpp/library/numeric/simd#math-common-simd-t) for its definition.)
</Revision>

{/* source:cpp-numeric-math-copysign:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-copysign:0004 */}
<ParameterList>
  <Parameter name="mag, sgn">floating-point or integer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-copysign:0005 */}
If no errors occur, the floating point value with the magnitude of `mag` and the sign of `sgn` is returned.

{/* source:cpp-numeric-math-copysign:0006 */}
If `mag` is NaN, then NaN with the sign of `sgn` is returned.

{/* source:cpp-numeric-math-copysign:0007 */}
If `sgn` is -0, the result is only negative if the implementation supports the signed zero consistently in arithmetic operations.

### Error handling

{/* source:cpp-numeric-math-copysign:0008 */}
This function is not subject to any errors specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-copysign:0009 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-copysign:0010 */}
- The returned value is exact ([FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) is never raised) and independent of the current [rounding mode](/docs/cpp/library/numeric/fenv/FE_round).

### Notes

{/* source:cpp-numeric-math-copysign:0011 */}
`std::copysign` is the only portable way to manipulate the sign of a NaN value (to examine the sign of a NaN, [std::signbit](/docs/cpp/library/numeric/math/signbit) may also be used).

{/* source:cpp-numeric-math-copysign:0012 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-math-copysign:0013 */}
<Revision until="C++23">
If `num1` or `num2` has type `long double`, then `std::copysign(num1, num2)` has the same effect as `std::copysign(static_cast<long double>(num1), static_cast<long double>(num2))`. Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::copysign(num1, num2)` has the same effect as `std::copysign(static_cast<double>(num1), static_cast<double>(num2))`. Otherwise, if `num1` or `num2` has type `float`, then `std::copysign(num1, num2)` has the same effect as `std::copysign(static_cast<float>(num1), static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::copysign(num1, num2)` has the same effect as `std::copysign(static_cast</*common-floating-point-type*/>(num1), static_cast</*common-floating-point-type*/>(num2))`, where `/*common-floating-point-type*/` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-math-copysign:0014 */}
```cpp
#include <cmath>
#include <iostream>
 
int main()
{
    std::cout << std::showpos
              << "copysign(1.0,+2.0) = " << std::copysign(1.0, +2.0) << '\n'
              << "copysign(1.0,-2.0) = " << std::copysign(1.0, -2.0) << '\n'
              << "copysign(inf,-2.0) = " << std::copysign(INFINITY, -2.0) << '\n'
              << "copysign(NaN,-2.0) = " << std::copysign(NAN, -2.0) << '\n';
}
```

Output:

```text
copysign(1.0,+2.0) = +1
copysign(1.0,-2.0) = -1
copysign(inf,-2.0) = -inf
copysign(NaN,-2.0) = -nan
```

### See also

{/* source:cpp-numeric-math-copysign:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`abs(float)`, `fabs`, `fabsf`, `fabsl`](/docs/cpp/library/numeric/math/fabs) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>absolute value of a floating point value (`|x|`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`signbit`](/docs/cpp/library/numeric/math/signbit) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the given number is negative <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/copysign) for copysign
