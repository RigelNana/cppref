---
title: "std::fmax, std::fmaxf, std::fmaxl"
description: "Returns the larger of two floating point arguments, treating NaNs as missing data (between a NaN and a numeric value, the numeric value is chosen)."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/fmax"
language: "C++"
---

{/* source:cpp-numeric-math-fmax:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       fmax ( float x, float y );
double      fmax ( double x, double y );

long double fmax ( long double x, long double y );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /*floating-point-type*/
            fmax ( /*floating-point-type*/ x,

                   /*floating-point-type*/ y );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`float       fmaxf( float x, float y );`} /> (constexpr since C++23)
  <Declaration id="3" language="cpp" since="C++11" code={`long double fmaxl( long double x, long double y );`} /> (constexpr since C++23)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< class V0, class V1 >
constexpr /*math-common-simd-t*/<V0, V1>

            fmax ( const V0& v_x, const V1& v_y );`} />
[Additional overloads](/docs/cpp/library/numeric/math/fmax#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      fmax ( Integer x, Integer y );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-fmax:0001 */}
    1-3) Returns the larger of two floating point arguments, treating NaNs as missing data (between a NaN and a numeric value, the numeric value is chosen).<InlineRevision since="C++23"> The library provides overloads of `std::fmax` for all cv-unqualified floating-point types as the type of the parameters.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-fmax:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::fmax` on `v_xand` `v_y`.
(See [`math-common-simd-t`](/docs/cpp/library/numeric/simd#math-common-simd-t) for its definition.)
</Revision>

{/* source:cpp-numeric-math-fmax:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-fmax:0004 */}
<ParameterList>
  <Parameter name="x, y">floating-point or integer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-fmax:0005 */}
If successful, returns the larger of two floating point values. The value returned is exact and does not depend on any rounding modes.

### Error handling

{/* source:cpp-numeric-math-fmax:0006 */}
This function is not subject to any of the error conditions specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-fmax:0007 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-fmax:0008 */}
- If one of the two arguments is NaN, the value of the other argument is returned.
- Only if both arguments are NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-fmax:0009 */}
This function is not required to be sensitive to the sign of zero, although some implementations additionally enforce that if one argument is `+0` and the other is `-0`, then `+0` is returned.

{/* source:cpp-numeric-math-fmax:0010 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-math-fmax:0011 */}
<Revision until="C++23">
If `num1` or `num2` has type `long double`, then `std::fmax(num1, num2)` has the same effect as `std::fmax(static_cast<long double>(num1), static_cast<long double>(num2))`. Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::fmax(num1, num2)` has the same effect as `std::fmax(static_cast<double>(num1), static_cast<double>(num2))`. Otherwise, if `num1` or `num2` has type `float`, then `std::fmax(num1, num2)` has the same effect as `std::fmax(static_cast<float>(num1), static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::fmax(num1, num2)` has the same effect as `std::fmax(static_cast</*common-floating-point-type*/>(num1), static_cast</*common-floating-point-type*/>(num2))`, where `/*common-floating-point-type*/` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-math-fmax:0012 */}
```cpp
#include <cmath>
#include <iostream>
 
int main()
{
    std::cout << "fmax(2,1)    = " << std::fmax(2, 1) << '\n'
              << "fmax(-Inf,0) = " << std::fmax(-INFINITY, 0) << '\n'
              << "fmax(NaN,-1) = " << std::fmax(NAN, -1) << '\n';
}
```

Output:

```text
fmax(2,1)    = 2
fmax(-Inf,0) = 0
fmax(NaN,-1) = -1
```

### See also

{/* source:cpp-numeric-math-fmax:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`isgreater`](/docs/cpp/library/numeric/math/isgreater) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the first floating-point argument is greater than the second <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`fmin`, `fminf`, `fminl`](/docs/cpp/library/numeric/math/fmin) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>smaller of two floating point values <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max`](/docs/cpp/library/algorithm/max)</DescriptionTerm>
    <DescriptionBody>returns the greater of the given values <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max_element`](/docs/cpp/library/algorithm/max_element)</DescriptionTerm>
    <DescriptionBody>returns the largest element in a range <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`minmax`](/docs/cpp/library/algorithm/minmax) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns the smaller and larger of two elements <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`minmax_element`](/docs/cpp/library/algorithm/minmax_element) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>returns the smallest and the largest elements in a range <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/fmax) for fmax
