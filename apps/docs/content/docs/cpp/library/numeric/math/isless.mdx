---
title: "std::isless"
description: "Determines if the floating point number x is less than the floating-point number y, without setting floating-point exceptions."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/isless"
language: "C++"
---

{/* source:cpp-numeric-math-isless:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" since="C++11" until="C++23" code={`bool isless( float x, float y );
bool isless( double x, double y );

bool isless( long double x, long double y );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr bool isless( /* floating-point-type */ x,
                       /* floating-point-type */ y );`} />
[Additional overloads](/docs/cpp/library/numeric/math/isless#Notes)
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" since="C++11" code={`template< class Arithmetic1, class Arithmetic2 >
bool isless( Arithmetic1 x, Arithmetic2 y );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-isless:0001 */}
    1) Determines if the floating point number `x` is less than the floating-point number `y`, without setting floating-point exceptions.<InlineRevision since="C++23"> The library provides overloads for all cv-unqualified floating-point types as the type of the parameters `x` and `y`.</InlineRevision>

    {/* source:cpp-numeric-math-isless:0002 */}
    A) Additional overloads are provided for all other combinations of arithmetic types.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-math-isless:0003 */}
<ParameterList>
  <Parameter name="x, y">floating-point or integer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-isless:0004 */}
`true` if `x < y`, `false` otherwise.

### Notes

{/* source:cpp-numeric-math-isless:0005 */}
The built-in `operator<` for floating-point numbers may raise [FE_INVALID](/docs/cpp/library/numeric/fenv/FE_exceptions) if one or both of the arguments is NaN. This function is a "quiet" version of `operator<`.

{/* source:cpp-numeric-math-isless:0006 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-math-isless:0007 */}
<Revision until="C++23">
If `num1` or `num2` has type `long double`, then `std::isless(num1, num2)` has the same effect as `std::isless(static_cast<long double>(num1), static_cast<long double>(num2))`. Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::isless(num1, num2)` has the same effect as `std::isless(static_cast<double>(num1), static_cast<double>(num2))`. Otherwise, if `num1` or `num2` has type `float`, then `std::isless(num1, num2)` has the same effect as `std::isless(static_cast<float>(num1), static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::isless(num1, num2)` has the same effect as `std::isless(static_cast</*common-floating-point-type*/>(num1), static_cast</*common-floating-point-type*/>(num2))`, where `/*common-floating-point-type*/` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### See also

{/* source:cpp-numeric-math-isless:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`less`](/docs/cpp/library/utility/functional/less)</DescriptionTerm>
    <DescriptionBody>function object implementing `x < y` <br /> (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`isgreater`](/docs/cpp/library/numeric/math/isgreater) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks if the first floating-point argument is greater than the second <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/isless) for isless
