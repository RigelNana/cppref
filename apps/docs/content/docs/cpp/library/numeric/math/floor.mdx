---
title: "std::floor, std::floorf, std::floorl"
description: "Computes the largest integer value not greater than num."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/floor"
language: "C++"
---

{/* source:cpp-numeric-math-floor:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       floor ( float num );
double      floor ( double num );

long double floor ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`constexpr /*floating-point-type*/
            floor ( /*floating-point-type*/ num );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`float       floorf( float num );`} /> (constexpr since C++23)
  <Declaration id="3" language="cpp" since="C++11" code={`long double floorl( long double num );`} /> (constexpr since C++23)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            floor ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/floor#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      floor ( Integer num );`} /> (constexpr since C++23)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-floor:0001 */}
    1-3) Computes the largest integer value not greater than `num`.<InlineRevision since="C++23"> The library provides overloads of `std::floor` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-floor:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::floor` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-floor:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-floor:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-floor:0005 */}
If no errors occur, the largest integer value not greater than `num`, that is `⌊num⌋`, is returned.

{/* source:cpp-numeric-math-floor:0006 */}
Return value · `num`

### Error handling

{/* source:cpp-numeric-math-floor:0007 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-floor:0008 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-floor:0009 */}
- The current [rounding mode](/docs/cpp/library/numeric/fenv/FE_round) has no effect.
- If `num` is ±∞, it is returned, unmodified.
- If `num` is ±0, it is returned, unmodified.
- If `num` is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-floor:0010 */}
[FE_INEXACT](/docs/cpp/library/numeric/fenv/FE_exceptions) may be (but isn't required to be) raised when rounding a non-integer finite value.

{/* source:cpp-numeric-math-floor:0011 */}
The largest representable floating-point values are exact integers in all standard floating-point formats, so this function never overflows on its own; however the result may overflow any integer type (including [std::intmax_t](/docs/cpp/library/utility/types/integer)), when stored in an integer variable.

{/* source:cpp-numeric-math-floor:0012 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::floor(num)` has the same effect as `std::floor(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-floor:0013 */}
```cpp
#include <cmath>
#include <iostream>
 
int main()
{
    std::cout << std::fixed
              << "floor(+2.7) = " << std::floor(+2.7) << '\n'
              << "floor(-2.7) = " << std::floor(-2.7) << '\n'
              << "floor(-0.0) = " << std::floor(-0.0) << '\n'
              << "floor(-Inf) = " << std::floor(-INFINITY) << '\n';
}
```

Output:

```text
floor(+2.7) = 2.000000
floor(-2.7) = -3.000000
floor(-0.0) = -0.000000
floor(-Inf) = -inf
```

### See also

{/* source:cpp-numeric-math-floor:0014 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ceil`, `ceilf`, `ceill`](/docs/cpp/library/numeric/math/ceil) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer not less than the given value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`trunc`, `truncf`, `truncl`](/docs/cpp/library/numeric/math/trunc) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer not greater in magnitude than the given value <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`round`, `roundf`, `roundl`, `lround`, `lroundf`, `lroundl`, `llround`, `llroundf`, `llroundl`](/docs/cpp/library/numeric/math/round) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer, rounding away from zero in halfway cases <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/floor) for floor
