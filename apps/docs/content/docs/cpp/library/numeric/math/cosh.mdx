---
title: "std::cosh, std::coshf, std::coshl"
description: "Computes the hyperbolic cosine of num."
source_url: "https://en.cppreference.com/w/cpp/numeric/math/cosh"
language: "C++"
---

{/* source:cpp-numeric-math-cosh:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="1" language="cpp" until="C++23" code={`float       cosh ( float num );
double      cosh ( double num );

long double cosh ( long double num );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/*floating-point-type*/
            cosh ( /*floating-point-type*/ num );`} /> (constexpr since C++26)
  <Declaration id="2" language="cpp" since="C++11" code={`float       coshf( float num );`} /> (constexpr since C++26)
  <Declaration id="3" language="cpp" since="C++11" code={`long double coshl( long double num );`} /> (constexpr since C++26)
[SIMD overload](/docs/cpp/library/numeric/simd#Math_functions) <InlineRevision since="C++26" />
Defined in header [`<simd>`](/docs/cpp/library/headers/simd)
  <Declaration id="S" language="cpp" since="C++26" code={`template< /*math-floating-point*/ V >
constexpr /*deduced-simd-t*/<V> 

            cosh ( const V& v_num );`} />
[Additional overloads](/docs/cpp/library/numeric/math/cosh#Notes) <InlineRevision since="C++11" />
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)
  <Declaration id="A" language="cpp" code={`template< class Integer >
double      cosh ( Integer num );`} /> (constexpr since C++26)

  <DeclarationDescription>
    {/* source:cpp-numeric-math-cosh:0001 */}
    1-3) Computes the hyperbolic cosine of `num`.<InlineRevision since="C++23"> The library provides overloads of `std::cosh` for all cv-unqualified floating-point types as the type of the parameter.</InlineRevision>
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-math-cosh:0002 */}
<Revision since="C++26">
S) The SIMD overload performs an element-wise `std::cosh` on `v_num`.
(See [`math-floating-point`](/docs/cpp/library/numeric/simd#math-floating-point) and [`deduced-simd-t`](/docs/cpp/library/numeric/simd#deduced-simd-t) for their definitions.)
</Revision>

{/* source:cpp-numeric-math-cosh:0003 */}
<Revision since="C++11">
A) Additional overloads are provided for all integer types, which are treated as `double`.
</Revision>

### Parameters

{/* source:cpp-numeric-math-cosh:0004 */}
<ParameterList>
  <Parameter name="num">floating-point or integer value</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-math-cosh:0005 */}
enum +e-num 2

{/* source:cpp-numeric-math-cosh:0006 */}
If a range error due to overflow occurs, [+HUGE_VAL](/docs/cpp/library/numeric/math/HUGE_VAL), `+HUGE_VALF`, or `+HUGE_VALL` is returned.

### Error handling

{/* source:cpp-numeric-math-cosh:0007 */}
Errors are reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-math-cosh:0008 */}
If the implementation supports IEEE floating-point arithmetic (IEC 60559),

{/* source:cpp-numeric-math-cosh:0009 */}
- if the argument is ±0, 1 is returned.
- If the argument is ±∞, +∞ is returned.
- if the argument is NaN, NaN is returned.

### Notes

{/* source:cpp-numeric-math-cosh:0010 */}
For the IEEE-compatible type `double`, if `|num| > 710.5`, then `std::cosh(num)` overflows.

{/* source:cpp-numeric-math-cosh:0011 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their argument `num` of integer type, `std::cosh(num)` has the same effect as `std::cosh(static_cast<double>(num))`.

### Example

{/* source:cpp-numeric-math-cosh:0012 */}
```cpp
#include <cerrno>
#include <cfenv>
#include <cmath>
#include <cstring>
#include <iostream>
// #pragma STDC FENV_ACCESS ON
 
int main()
{
    const double x = 42;
 
    std::cout << "cosh(1) = " << std::cosh(1) << '\n'
              << "cosh(-1) = " << std::cosh(-1) << '\n'
              << "log(sinh(" << x << ")+cosh(" << x << ")) = "
              << std::log(std::sinh(x) + std::cosh(x)) << '\n';
 
    // special values
    std::cout << "cosh(+0) = " << std::cosh(0.0) << '\n'
              << "cosh(-0) = " << std::cosh(-0.0) << '\n';
 
    // error handling
    errno=0;
    std::feclearexcept(FE_ALL_EXCEPT);
 
    std::cout << "cosh(710.5) = " << std::cosh(710.5) << '\n';
 
    if (errno == ERANGE)
        std::cout << "    errno == ERANGE: " << std::strerror(errno) << '\n';
    if (std::fetestexcept(FE_OVERFLOW))
        std::cout << "    FE_OVERFLOW raised\n";
}
```

Possible output:

```text
cosh(1) = 1.54308
cosh(-1) = 1.54308
log(sinh(42)+cosh(42)) = 42
cosh(+0) = 1
cosh(-0) = 1
cosh(710.5) = inf
    errno == ERANGE: Numerical result out of range
    FE_OVERFLOW raised
```

### See also

{/* source:cpp-numeric-math-cosh:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`sinh`, `sinhf`, `sinhl`](/docs/cpp/library/numeric/math/sinh) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes hyperbolic sine (`sinh(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tanh`, `tanhf`, `tanhl`](/docs/cpp/library/numeric/math/tanh) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes hyperbolic tangent (`tanh(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acosh`, `acoshf`, `acoshl`](/docs/cpp/library/numeric/math/acosh) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes the inverse hyperbolic cosine (`arcosh(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cosh(std::complex)`](/docs/cpp/library/numeric/complex/cosh)</DescriptionTerm>
    <DescriptionBody>computes hyperbolic cosine of a complex number (`cosh(z)`) <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cosh(std::valarray)`](/docs/cpp/library/numeric/valarray/cosh)</DescriptionTerm>
    <DescriptionBody>applies the function std::cosh to each element of valarray <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/numeric/math/cosh) for cosh
