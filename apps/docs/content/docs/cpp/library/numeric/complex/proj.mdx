---
title: "std::proj(std::complex)"
description: "Returns the projection of the complex number z onto the Riemann sphere."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/proj"
language: "C++"
---

{/* source:cpp-numeric-complex-proj:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="1" language="cpp" since="C++11" code={`template< class T > 
std::complex<T> proj( const std::complex<T>& z );`} />

[Additional overloads](/docs/cpp/library/numeric/complex/proj#Notes) <InlineRevision since="C++11" />

Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="A" language="cpp" until="C++23" code={`std::complex<float>       proj( float f );
std::complex<double>      proj( double f );

std::complex<long double> proj( long double f );`} />
  <Declaration id="A" language="cpp" since="C++23" code={`template< class FloatingPoint >
std::complex<FloatingPoint> proj( FloatingPoint f );`} />
  <Declaration id="B" language="cpp" code={`template< class Integer >
std::complex<double> proj( Integer i );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-complex-proj:0001 */}
    1. Returns the [projection](https://en.wikipedia.org/wiki/Riemann_sphere) of the complex number `z` onto the Riemann sphere.

    {/* source:cpp-numeric-complex-proj:0002 */}
    For most `z`, `std::proj(z) == z`, but all complex infinities, even the numbers where one component is infinite and the other is NaN, become positive real infinity, `(INFINITY, 0.0)` or `(INFINITY, -0.0)`. The sign of the imaginary (zero) component is the sign of `std::imag(z)`.

    {/* source:cpp-numeric-complex-proj:0003 */}
    A,B) Additional overloads are provided for all integer and floating-point types, which are treated as complex numbers with positive zero imaginary component.
  </DeclarationDescription>
</DeclarationDoc>

## Parameters

{/* source:cpp-numeric-complex-proj:0004 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
  <Parameter name="f">floating-point value</Parameter>
  <Parameter name="i">integer value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-proj:0005 */}
1. The projection of `z` onto the Riemann sphere.

{/* source:cpp-numeric-complex-proj:0006 */}
A) The projection of `std::complex(f)` onto the Riemann sphere.

{/* source:cpp-numeric-complex-proj:0007 */}
B) The projection of `std::complex<double>(i)` onto the Riemann sphere.

## Notes

{/* source:cpp-numeric-complex-proj:0008 */}
The `proj` function helps model the Riemann sphere by mapping all infinities to one (give or take the sign of the imaginary zero), and should be used just before any operation, especially comparisons, that might give spurious results for any of the other infinities.

{/* source:cpp-numeric-complex-proj:0009 */}
The additional overloads are not required to be provided exactly as (A,B). They only need to be sufficient to ensure that for their argument `num`:

{/* source:cpp-numeric-complex-proj:0010 */}
- If `num` has a <InlineRevision until="C++23">standard</InlineRevision> floating-point type `T`, then `std::proj(num)` has the same effect as `std::proj(std::complex<T>(num))`. Otherwise, if `num` has an integer type, then `std::proj(num)` has the same effect as `std::proj(std::complex<double>(num))`.

## Example

{/* source:cpp-numeric-complex-proj:0011 */}
```cpp
#include <complex>
#include <iostream>
 
int main()
{
    std::complex<double> c1(1, 2);
    std::cout << "proj" << c1 << " = " << std::proj(c1) << '\n';
 
    std::complex<double> c2(INFINITY, -1);
    std::cout << "proj" << c2 << " = " << std::proj(c2) << '\n';
 
    std::complex<double> c3(0, -INFINITY);
    std::cout << "proj" << c3 << " = " << std::proj(c3) << '\n';
}
```

Output:

```text
proj(1,2) = (1,2)
proj(inf,-1) = (inf,-0)
proj(0,-inf) = (inf,-0)
```

## See also

{/* source:cpp-numeric-complex-proj:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`abs(std::complex)`](/docs/cpp/library/numeric/complex/abs)</DescriptionTerm>
    <DescriptionBody>returns the magnitude of a complex number (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`norm`](/docs/cpp/library/numeric/complex/norm)</DescriptionTerm>
    <DescriptionBody>returns the squared magnitude (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`polar`](/docs/cpp/library/numeric/complex/polar)</DescriptionTerm>
    <DescriptionBody>constructs a complex number from magnitude and phase angle (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/cproj)</DescriptionTerm>
    <DescriptionBody>for `cproj`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
