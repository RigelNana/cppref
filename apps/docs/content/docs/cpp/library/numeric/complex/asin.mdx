---
title: "std::asin(std::complex)"
description: "Computes complex arc sine of a complex value z. Branch cut exists outside the interval [−1, +1] along the real axis."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/asin"
language: "C++"
---

{/* source:cpp-numeric-complex-asin:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration language="cpp" since="C++11" code={`template< class T >
std::complex<T> asin( const std::complex<T>& z );`} />
</DeclarationDoc>

{/* source:cpp-numeric-complex-asin:0001 */}
Computes complex arc sine of a complex value `z`. Branch cut exists outside the interval [−1, +1] along the real axis.

## Parameters

{/* source:cpp-numeric-complex-asin:0002 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-asin:0003 */}
If no errors occur, complex arc sine of `z` is returned, in the range of a strip unbounded along the imaginary axis and in the interval [−π/2, +π/2] along the real axis.

{/* source:cpp-numeric-complex-asin:0004 */}
Errors and special cases are handled as if the operation is implemented by `-i * `[`std::asinh`](/docs/cpp/library/numeric/complex/asinh)`(i * z)`, where `i` is the imaginary unit.

## Notes

{/* source:cpp-numeric-complex-asin:0005 */}
Inverse sine (or arc sine) is a multivalued function and requires a branch cut on the complex plane. The branch cut is conventionally placed at the line segments (-∞,-1) and (1,∞) of the real axis.

{/* source:cpp-numeric-complex-asin:0006 */}
The mathematical definition of the principal value of arc sine is `arcsin z = -iln(iz + √(1-z2))`.

{/* source:cpp-numeric-complex-asin:0007 */}
For any `z`, `asin(z) = acos(-z) - π/2`.

## Example

{/* source:cpp-numeric-complex-asin:0008 */}
```cpp
#include <cmath>
#include <complex>
#include <iostream>
 
int main()
{
    std::cout << std::fixed;
    std::complex<double> z1(-2.0, 0.0);
    std::cout << "asin" << z1 << " = " << std::asin(z1) << '\n';
 
    std::complex<double> z2(-2.0, -0.0);
    std::cout << "asin" << z2 << " (the other side of the cut) = "
              << std::asin(z2) << '\n';
 
    // for any z, asin(z) = acos(−z) − pi / 2
    const double pi = std::acos(-1);
    std::complex<double> z3 = std::acos(z2) - pi / 2;
    std::cout << "sin(acos" << z2 << " - pi / 2) = " << std::sin(z3) << '\n';
}
```

Output:

```text
asin(-2.000000,0.000000) = (-1.570796,1.316958)
asin(-2.000000,-0.000000) (the other side of the cut) = (-1.570796,-1.316958)
sin(acos(-2.000000,-0.000000) - pi / 2) = (2.000000,0.000000)
```

## See also

{/* source:cpp-numeric-complex-asin:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`acos(std::complex)`](/docs/cpp/library/numeric/complex/acos)</DescriptionTerm>
    <DescriptionBody>computes arc cosine of a complex number (`arccos(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan(std::complex)`](/docs/cpp/library/numeric/complex/atan)</DescriptionTerm>
    <DescriptionBody>computes arc tangent of a complex number (`arctan(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`sin(std::complex)`](/docs/cpp/library/numeric/complex/sin)</DescriptionTerm>
    <DescriptionBody>computes sine of a complex number (`sin(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`asin`, `asinf`, `asinl`](/docs/cpp/library/numeric/math/asin)</DescriptionTerm>
    <DescriptionBody>computes arc sine (`arcsin(x)`) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`asin(std::valarray)`](/docs/cpp/library/numeric/valarray/asin)</DescriptionTerm>
    <DescriptionBody>applies the function `std::asin` to each element of valarray (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/casin)</DescriptionTerm>
    <DescriptionBody>for `casin`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
