---
title: "std::atan(std::complex)"
description: "Computes complex arc tangent of a complex value z. Branch cut exists outside the interval [−i, +i] along the imaginary axis."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/atan"
language: "C++"
---

{/* source:cpp-numeric-complex-atan:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration language="cpp" since="C++11" code={`template< class T > 
complex<T> atan( const complex<T>& z );`} />
</DeclarationDoc>

{/* source:cpp-numeric-complex-atan:0001 */}
Computes complex arc tangent of a complex value `z`. Branch cut exists outside the interval [−i, +i] along the imaginary axis.

## Parameters

{/* source:cpp-numeric-complex-atan:0002 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-atan:0003 */}
If no errors occur, complex arc tangent of `z` is returned, in the range of a strip unbounded along the imaginary axis and in the interval [−π/2, +π/2] along the real axis.

{/* source:cpp-numeric-complex-atan:0004 */}
Errors and special cases are handled as if the operation is implemented by `-i *` [`std::atanh`](/docs/cpp/library/numeric/complex/atanh)`(i * z)`, where `i` is the imaginary unit.

## Notes

{/* source:cpp-numeric-complex-atan:0005 */}
Inverse tangent (or arc tangent) is a multivalued function and requires a branch cut on the complex plane. The branch cut is conventionally placed at the line segments (-∞i,-i) and (+i,+∞i) of the imaginary axis.

{/* source:cpp-numeric-complex-atan:0006 */}
The mathematical definition of the principal value of inverse tangent is `atan z = -1/2 i [ln(1 - iz) - ln(1 + iz)]`.

## Example

{/* source:cpp-numeric-complex-atan:0007 */}
```cpp
#include <cmath>
#include <complex>
#include <iostream>
 
int main()
{
    std::cout << std::fixed;
    std::complex<double> z1(0.0, 2.0);
    std::cout << "atan" << z1 << " = " << std::atan(z1) << '\n';
 
    std::complex<double> z2(-0.0, 2.0);
    std::cout << "atan" << z2 << " (the other side of the cut) = "
              << std::atan(z2) << '\n';
 
    std::complex<double> z3(0.0, INFINITY);
    std::cout << "2 * atan" << z3 << " = " << 2.0 * std::atan(z3) << '\n';
}
```

Output:

```text
atan(0.000000,2.000000) = (1.570796,0.549306)
atan(-0.000000,2.000000) (the other side of the cut) = (-1.570796,0.549306)
2 * atan(0.000000,inf) = (3.141593,0.000000)
```

## See also

{/* source:cpp-numeric-complex-atan:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`asin(std::complex)`](/docs/cpp/library/numeric/complex/asin)</DescriptionTerm>
    <DescriptionBody>computes arc sine of a complex number (`arcsin(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acos(std::complex)`](/docs/cpp/library/numeric/complex/acos)</DescriptionTerm>
    <DescriptionBody>computes arc cosine of a complex number (`arccos(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tan(std::complex)`](/docs/cpp/library/numeric/complex/tan)</DescriptionTerm>
    <DescriptionBody>computes tangent of a complex number (`tan(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan`, `atanf`, `atanl`](/docs/cpp/library/numeric/math/atan)</DescriptionTerm>
    <DescriptionBody>computes arc tangent (`arctan(x)`) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan(std::valarray)`](/docs/cpp/library/numeric/valarray/atan)</DescriptionTerm>
    <DescriptionBody>applies the function `std::atan` to each element of valarray (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/catan)</DescriptionTerm>
    <DescriptionBody>for `catan`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
