---
title: "std::tan(std::complex)"
description: "Computes complex tangent of a complex value z."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/tan"
language: "C++"
---

{/* source:cpp-numeric-complex-tan:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration language="cpp" code={`template< class T > 
complex<T> tan( const complex<T>& z );`} />
</DeclarationDoc>

{/* source:cpp-numeric-complex-tan:0001 */}
Computes complex tangent of a complex value `z`.

## Parameters

{/* source:cpp-numeric-complex-tan:0002 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-tan:0003 */}
If no errors occur, the complex tangent of `z` is returned.

{/* source:cpp-numeric-complex-tan:0004 */}
Errors and special cases are handled as if the operation is implemented by `-i *` [`std::tanh`](/docs/cpp/library/numeric/complex/tanh)`(i * z)`, where `i` is the imaginary unit.

## Notes

{/* source:cpp-numeric-complex-tan:0005 */}
Tangent is an analytical function on the complex plain and has no branch cuts. It is periodic with respect to the real component, with period πi, and has poles of the first order along the real line, at coordinates (π(1/2 + n), 0). However no common floating-point representation is able to represent π/2 exactly, thus there is no value of the argument for which a pole error occurs.

{/* source:cpp-numeric-complex-tan:0006 */}
Mathematical definition of the tangent is `tan z = i(e-iz - eiz) / (e-iz + eiz)`.

## Example

{/* source:cpp-numeric-complex-tan:0007 */}
```cpp
#include <cmath>
#include <complex>
#include <iostream>
 
int main()
{
    std::cout << std::fixed;
    std::complex<double> z(1.0, 0.0); // behaves like real tangent along the real line
    std::cout << "tan" << z << " = " << std::tan(z)
              << " ( tan(1) = " << std::tan(1) << ")\n";
 
    std::complex<double> z2(0.0, 1.0); // behaves like tanh along the imaginary line
    std::cout << "tan" << z2 << " = " << std::tan(z2)
              << " (tanh(1) = " << std::tanh(1) << ")\n";
}
```

Output:

```text
tan(1.000000,0.000000) = (1.557408,0.000000) ( tan(1) = 1.557408)
tan(0.000000,1.000000) = (0.000000,0.761594) (tanh(1) = 0.761594)
```

## See also

{/* source:cpp-numeric-complex-tan:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`sin(std::complex)`](/docs/cpp/library/numeric/complex/sin)</DescriptionTerm>
    <DescriptionBody>computes sine of a complex number (`sin(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cos(std::complex)`](/docs/cpp/library/numeric/complex/cos)</DescriptionTerm>
    <DescriptionBody>computes cosine of a complex number (`cos(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan(std::complex)`](/docs/cpp/library/numeric/complex/atan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc tangent of a complex number (`arctan(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tan`, `tanf`, `tanl`](/docs/cpp/library/numeric/math/tan) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes tangent (`tan(x)`) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tan(std::valarray)`](/docs/cpp/library/numeric/valarray/tan)</DescriptionTerm>
    <DescriptionBody>applies the function `std::tan` to each element of valarray (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/ctan)</DescriptionTerm>
    <DescriptionBody>for `ctan`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
