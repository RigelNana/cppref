---
title: "std::literals::complex_literals::operator\"\"i, operator\"\"if, operator\"\"il"
description: "Forms a std::complex literal representing an imaginary number."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/operator_q__q_i"
language: "C++"
---

{/* source:cpp-numeric-complex-operator-q-q-i:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="1" language="cpp" since="C++14" code={`constexpr complex<double> operator""i( long double arg );
constexpr complex<double> operator""i( unsigned long long arg );`} />
  <Declaration id="2" language="cpp" since="C++14" code={`constexpr complex<float> operator""if( long double arg );
constexpr complex<float> operator""if( unsigned long long arg );`} />
  <Declaration id="3" language="cpp" since="C++14" code={`constexpr complex<long double> operator""il( long double arg );
constexpr complex<long double> operator""il( unsigned long long arg );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-complex-operator-q-q-i:0001 */}
    Forms a [`std::complex`](/docs/cpp/library/numeric/complex) literal representing an imaginary number.

    {/* source:cpp-numeric-complex-operator-q-q-i:0002 */}
    1) Forms a literal `std::complex<double>` with the real part zero and imaginary part `arg`.

    {/* source:cpp-numeric-complex-operator-q-q-i:0003 */}
    2) Forms a literal `std::complex<float>` with the real part zero and imaginary part `arg`.

    {/* source:cpp-numeric-complex-operator-q-q-i:0004 */}
    3) Forms a literal `std::complex<long double>` with the real part zero and imaginary part `arg`.
  </DeclarationDescription>
</DeclarationDoc>

## Parameters

{/* source:cpp-numeric-complex-operator-q-q-i:0005 */}
<ParameterList>
  <Parameter name="arg">the value of the imaginary number</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-operator-q-q-i:0006 */}
The [`std::complex`](/docs/cpp/library/numeric/complex) literal with the real part zero and imaginary part `arg`.

## Notes

{/* source:cpp-numeric-complex-operator-q-q-i:0007 */}
These operators are declared in the namespace `std::literals::complex_literals`, where both `literals` and `complex_literals` are inline namespaces. Access to these operators can be gained with either:

{/* source:cpp-numeric-complex-operator-q-q-i:0008 */}
- `using namespace std::literals`,
- `using namespace std::complex_literals`, or
- `using namespace std::literals::complex_literals`.

{/* source:cpp-numeric-complex-operator-q-q-i:0009 */}
Even though `if` is a [keyword](/docs/cpp/language/keywords/if) in C++, it is a ud-suffix of the [literal operator](/docs/cpp/language/expressions/user_literal) of the form `operator ""if` and in the literal expressions such as `1if` or `1.0if` because it is not separated by whitespace and is not a standalone token.

{/* source:cpp-numeric-complex-operator-q-q-i:0010 */}
| [`Feature-test`](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_complex_udls`](/docs/cpp/library/feature_test#cpp_lib_complex_udls) | [`201309L`](/docs/cpp/library/compiler_support/14#cpp_lib_complex_udls_201309L) | (C++14) | User-Defined Literals for [`std::complex`](/docs/cpp/library/numeric/complex) |

## Possible implementation

{/* source:cpp-numeric-complex-operator-q-q-i:0011 */}
[`operator""i`](/docs/cpp/library/numeric/complex/operator_q__q_i#Version_1)

```cpp
constexpr std::complex<double> operator""i(unsigned long long d)
{
    return std::complex<double> {0.0, static_cast<double>(d)};
}
 
constexpr std::complex<double> operator""i(long double d)
{
    return std::complex<double> {0.0, static_cast<double>(d)};
}
```

[`operator""if`](/docs/cpp/library/numeric/complex/operator_q__q_i#Version_2)

```cpp
constexpr std::complex<float> operator""if(unsigned long long d)
{
    return std::complex<float> {0.0f, static_cast<float>(d)};
}
 
constexpr std::complex<float> operator""if(long double d)
{
    return std::complex<float> {0.0f, static_cast<float>(d)};
}
```

[`operator""il`](/docs/cpp/library/numeric/complex/operator_q__q_i#Version_3)

```cpp
constexpr std::complex<long double> operator""il(unsigned long long d)
{
    return std::complex<long double> {0.0L, static_cast<long double>(d)};
}
 
constexpr std::complex<long double> operator""il(long double d)
{
    return std::complex<long double> {0.0L, d};
}
```

## Example

{/* source:cpp-numeric-complex-operator-q-q-i:0012 */}
```cpp
#include <complex>
#include <iostream>
 
int main()
{
    using namespace std::complex_literals;
 
    std::complex<double> c = 1.0 + 1i;
    std::cout << "abs" << c << " = " << std::abs(c) << '\n';
 
    std::complex<float> z = 3.0f + 4.0if;
    std::cout << "abs" << z << " = " << std::abs(z) << '\n';
}
```

Output:

```text
abs(1,1) = 1.41421
abs(3,4) = 5
```

## See also

{/* source:cpp-numeric-complex-operator-q-q-i:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/numeric/complex/complex)</DescriptionTerm>
    <DescriptionBody>constructs a complex number (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/numeric/complex/operator_assignment)</DescriptionTerm>
    <DescriptionBody>assigns the contents (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/I)</DescriptionTerm>
    <DescriptionBody>for `I`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
