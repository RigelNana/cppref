---
title: "std::imag(std::complex)"
description: "Returns the imaginary part of the complex number z, i.e. z.imag()."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/imag2"
language: "C++"
---

{/* source:cpp-numeric-complex-imag2:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="1" language="cpp" until="C++14" code={`template< class T > 
T imag( const std::complex<T>& z );`} />
  <Declaration id="1" language="cpp" since="C++14" code={`template< class T > 
constexpr T imag( const std::complex<T>& z );`} />

[Additional overloads](/docs/cpp/library/numeric/complex/imag2#Notes) <InlineRevision since="C++11" />

Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="A" language="cpp" until="C++14" code={`float       imag( float f );
double      imag( double f );

long double imag( long double f );`} />
  <Declaration id="A" language="cpp" since="C++14" until="C++23" code={`constexpr float       imag( float f );
constexpr double      imag( double f );

constexpr long double imag( long double f );`} />
  <Declaration id="A" language="cpp" since="C++23" code={`template< class FloatingPoint >
FloatingPoint imag( FloatingPoint f );`} />
  <Declaration id="B" language="cpp" until="C++14" code={`template< class Integer > 
double imag( Integer i );`} />
  <Declaration id="B" language="cpp" since="C++14" code={`template< class Integer > 
constexpr double imag( Integer i );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-complex-imag2:0001 */}
    1. Returns the imaginary part of the complex number `z`, i.e. `z.imag()`.

    {/* source:cpp-numeric-complex-imag2:0002 */}
    <Revision since="C++11">
    A,B) Additional overloads are provided for all integer and floating-point types, which are treated as complex numbers with zero imaginary part.
    </Revision>
  </DeclarationDescription>
</DeclarationDoc>

## Parameters

{/* source:cpp-numeric-complex-imag2:0003 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
  <Parameter name="f">floating-point value</Parameter>
  <Parameter name="i">integer value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-imag2:0004 */}
1. The imaginary part of `z`.

{/* source:cpp-numeric-complex-imag2:0005 */}
A) `decltype(f){}` (zero).

{/* source:cpp-numeric-complex-imag2:0006 */}
B) `0.0`.

## Notes

{/* source:cpp-numeric-complex-imag2:0007 */}
The additional overloads are not required to be provided exactly as (A,B). They only need to be sufficient to ensure that for their argument `num`:

{/* source:cpp-numeric-complex-imag2:0008 */}
- If `num` has a <InlineRevision until="C++23">standard</InlineRevision> floating-point type `T`, then `std::imag(num)` has the same effect as `std::imag(std::complex<T>(num))`. Otherwise, if `num` has an integer type, then `std::imag(num)` has the same effect as `std::imag(std::complex<double>(num))`.

## See also

{/* source:cpp-numeric-complex-imag2:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`imag`](/docs/cpp/library/numeric/complex/imag)</DescriptionTerm>
    <DescriptionBody>accesses the imaginary part of the complex number (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`real`](/docs/cpp/library/numeric/complex/real2)</DescriptionTerm>
    <DescriptionBody>returns the real part (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/cimag)</DescriptionTerm>
    <DescriptionBody>for `cimag`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
