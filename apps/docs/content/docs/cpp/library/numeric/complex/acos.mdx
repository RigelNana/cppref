---
title: "std::acos(std::complex)"
description: "Computes complex arc cosine of a complex value z. Branch cuts exist outside the interval [−1, +1] along the real axis."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/acos"
language: "C++"
---

{/* source:cpp-numeric-complex-acos:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration language="cpp" since="C++11" code={`template< class T > 
complex<T> acos( const complex<T>& z );`} />
</DeclarationDoc>

{/* source:cpp-numeric-complex-acos:0001 */}
Computes complex arc cosine of a complex value `z`. Branch cuts exist outside the interval [−1, +1] along the real axis.

## Parameters

{/* source:cpp-numeric-complex-acos:0002 */}
<ParameterList>
  <Parameter name="z">complex value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-acos:0003 */}
If no errors occur, complex arc cosine of `z` is returned, in the range of a strip unbounded along the imaginary axis and in the interval [0, +π] along the real axis.

## Error handling and special values

{/* source:cpp-numeric-complex-acos:0004 */}
Errors are reported consistent with [`math_errhandling`](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-complex-acos:0005 */}
If the implementation supports IEEE floating-point arithmetic,

{/* source:cpp-numeric-complex-acos:0006 */}
- `std::acos(std::conj(z)) == std::conj(std::acos(z))`
- If `z` is (±0,+0), the result is (π/2,-0)
- If `z` is (±0,NaN), the result is (π/2,NaN)
- If `z` is (x,+∞) (for any finite x), the result is (π/2,-∞)
- If `z` is (x,NaN) (for any nonzero finite x), the result is (NaN,NaN) and [`FE_INVALID`](/docs/cpp/library/numeric/fenv/FE_exceptions) may be raised.
- If `z` is (-∞,y) (for any positive finite y), the result is (π,-∞)
- If `z` is (+∞,y) (for any positive finite y), the result is (+0,-∞)
- If `z` is (-∞,+∞), the result is (3π/4,-∞)
- If `z` is (+∞,+∞), the result is (π/4,-∞)
- If `z` is (±∞,NaN), the result is (NaN,±∞) (the sign of the imaginary part is unspecified)
- If `z` is (NaN,y) (for any finite y), the result is (NaN,NaN) and [`FE_INVALID`](/docs/cpp/library/numeric/fenv/FE_exceptions) may be raised
- If `z` is (NaN,+∞), the result is (NaN,-∞)
- If `z` is (NaN,NaN), the result is (NaN,NaN)

## Notes

{/* source:cpp-numeric-complex-acos:0007 */}
Inverse cosine (or arc cosine) is a multivalued function and requires a branch cut on the complex plane. The branch cut is conventionally placed at the line segments (-∞,-1) and (1,∞) of the real axis.

{/* source:cpp-numeric-complex-acos:0008 */}
The mathematical definition of the principal value of arc cosine is `acos z = 1/2 π + i ln(iz + √(1-z2))`.

{/* source:cpp-numeric-complex-acos:0009 */}
For any `z`, `acos(z) = π - acos(-z)`.

## Example

{/* source:cpp-numeric-complex-acos:0010 */}
```cpp
#include <cmath>
#include <complex>
#include <iostream>
 
int main()
{
    std::cout << std::fixed;
    std::complex<double> z1(-2.0, 0.0);
    std::cout << "acos" << z1 << " = " << std::acos(z1) << '\n';
 
    std::complex<double> z2(-2.0, -0.0);
    std::cout << "acos" << z2 << " (the other side of the cut) = "
              << std::acos(z2) << '\n';
 
    // for any z, acos(z) = pi - acos(-z)
    const double pi = std::acos(-1);
    std::complex<double> z3 = pi - std::acos(z2);
    std::cout << "cos(pi - acos" << z2 << ") = " << std::cos(z3) << '\n';
}
```

Output:

```text
acos(-2.000000,0.000000) = (3.141593,-1.316958)
acos(-2.000000,-0.000000) (the other side of the cut) = (3.141593,1.316958)
cos(pi - acos(-2.000000,-0.000000)) = (2.000000,0.000000)
```

## See also

{/* source:cpp-numeric-complex-acos:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`asin(std::complex)`](/docs/cpp/library/numeric/complex/asin)</DescriptionTerm>
    <DescriptionBody>computes arc sine of a complex number (`arcsin(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan(std::complex)`](/docs/cpp/library/numeric/complex/atan)</DescriptionTerm>
    <DescriptionBody>computes arc tangent of a complex number (`arctan(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cos(std::complex)`](/docs/cpp/library/numeric/complex/cos)</DescriptionTerm>
    <DescriptionBody>computes cosine of a complex number (`cos(z)`) (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acos`, `acosf`, `acosl`](/docs/cpp/library/numeric/math/acos)</DescriptionTerm>
    <DescriptionBody>computes arc cosine (`arccos(x)`) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acos(std::valarray)`](/docs/cpp/library/numeric/valarray/acos)</DescriptionTerm>
    <DescriptionBody>applies the function `std::acos` to each element of valarray (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/cacos)</DescriptionTerm>
    <DescriptionBody>for `cacos`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
