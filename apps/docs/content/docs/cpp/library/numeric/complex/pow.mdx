---
title: "std::pow(std::complex)"
description: "Computes complex x raised to a complex power y with a branch cut along the negative real axis for the first argument."
source_url: "https://en.cppreference.com/w/cpp/numeric/complex/pow"
language: "C++"
---

{/* source:cpp-numeric-complex-pow:0000 */}
<DeclarationDoc>
Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="1" language="cpp" until="C++11" code={`template< class T >
std::complex<T> pow( const std::complex<T>& x, int y );`} />
  <Declaration id="2" language="cpp" code={`template< class T >
std::complex<T> pow( const std::complex<T>& x, const std::complex<T>& y );`} />
  <Declaration id="3" language="cpp" code={`template< class T >
std::complex<T> pow( const std::complex<T>& x, const T& y );`} />
  <Declaration id="4" language="cpp" code={`template< class T >
std::complex<T> pow( const T& x, const std::complex<T>& y );`} />

[Additional overloads](/docs/cpp/library/numeric/complex/pow#Notes) <InlineRevision since="C++11" />

Defined in header [`<complex>`](/docs/cpp/library/headers/complex)

  <Declaration id="A" language="cpp" until="C++23" code={`template< class T1, class T2 >
std::complex</* common-type */>

    pow( const std::complex<T1>& x, const std::complex<T2>& y );`} />
  <Declaration id="A" language="cpp" since="C++23" code={`template< class T1, class T2 >
std::complex<std::common_type_t<T1, T2>>

    pow( const std::complex<T1>& x, const std::complex<T2>& y );`} />
  <Declaration id="B" language="cpp" until="C++23" code={`template< class T, class NonComplex >
std::complex</* common-type */>

    pow( const std::complex<T>& x, const NonComplex& y );`} />
  <Declaration id="B" language="cpp" since="C++23" code={`template< class T, class NonComplex >
std::complex<std::common_type_t<T, NonComplex>>

    pow( const std::complex<T>& x, const NonComplex& y );`} />
  <Declaration id="C" language="cpp" until="C++23" code={`template< class T, class NonComplex >
std::complex</* common-type */>

    pow( const NonComplex& x, const std::complex<T>& y );`} />
  <Declaration id="C" language="cpp" since="C++23" code={`template< class T, class NonComplex >
std::complex<std::common_type_t<T, NonComplex>>

    pow( const NonComplex& x, const std::complex<T>& y );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-complex-pow:0001 */}
    1-4. Computes complex `x` raised to a complex power `y` with a branch cut along the negative real axis for the first argument. Non-complex arguments are treated as complex numbers with positive zero imaginary component.

    {/* source:cpp-numeric-complex-pow:0002 */}
    <Revision since="C++11">
    A-C) Additional overloads are provided. `NonComplex` is not a specialization of [`std::complex`](/docs/cpp/library/numeric/complex).
    </Revision>
  </DeclarationDescription>
</DeclarationDoc>

## Parameters

{/* source:cpp-numeric-complex-pow:0003 */}
<ParameterList>
  <Parameter name="x">base</Parameter>
  <Parameter name="y">exponent</Parameter>
</ParameterList>

## Return value

{/* source:cpp-numeric-complex-pow:0004 */}
1-4. If no errors occur, the complex power `xy`, is returned.

{/* source:cpp-numeric-complex-pow:0005 */}
Errors and special cases are handled as if the operation is implemented by `std::exp(y * std::log(x))`.

{/* source:cpp-numeric-complex-pow:0006 */}
The result of `std::pow(0, 0)` is implementation-defined.

{/* source:cpp-numeric-complex-pow:0007 */}
A-C) Same as (2-4).

## Notes

{/* source:cpp-numeric-complex-pow:0008 */}
Overload (1) was provided in C++98 to match the extra overloads (2) of [`std::pow`](/docs/cpp/library/numeric/math/pow). Those overloads were removed by the resolution of [LWG issue 550](https://cplusplus.github.io/LWG/issue550), and overload (1) was removed by the resolution of [LWG issue 844](https://cplusplus.github.io/LWG/issue844).

{/* source:cpp-numeric-complex-pow:0009 */}
The additional overloads are not required to be provided exactly as (A-C). They only need to be sufficient to ensure that for their first argument `base` and second argument `exponent`:

{/* source:cpp-numeric-complex-pow:0010 */}
<Revision until="C++23">
If `base` and/or `exponent` has type `std::complex<T>`:

- If `base` and/or `exponent` has type `std::complex<long double>` or `long double`, then `std::pow(base, exponent)` has the same effect as `std::pow(std::complex<long double>(base), std::complex<long double>(exponent))`.
- Otherwise, if `base` and/or `exponent` has type `std::complex<double>`, `double`, or an integer type, then `std::pow(base, exponent)` has the same effect as `std::pow(std::complex<double>(base), std::complex<double>(exponent))`.
- Otherwise, if `base` and/or `exponent` has type `std::complex<float>` or `float`, then `std::pow(base, exponent)` has the same effect as `std::pow(std::complex<float>(base), std::complex<float>(exponent))`.
</Revision>
<Revision since="C++23">
If one argument has type `std::complex<T1>` and the other argument has type `T2` or `std::complex<T2>`, then `std::pow(base, exponent)` has the same effect as `std::pow(std::complex<std::common_type_t<T1, T2>>(base), std::complex<std::common_type_t<T1, T2>>(exponent))`. If `std::common_type_t<T1, T2>` is not well-formed, then the program is ill-formed.
</Revision>

## Example

{/* source:cpp-numeric-complex-pow:0011 */}
```cpp
#include <complex>
#include <iostream>
 
int main()
{
    std::cout << std::fixed;
 
    std::complex<double> z(1.0, 2.0);
    std::cout << "(1,2)^2 = " << std::pow(z, 2) << '\n';
 
    std::complex<double> z2(-1.0, 0.0); // square root of -1
    std::cout << "-1^0.5 = " << std::pow(z2, 0.5) << '\n';
 
    std::complex<double> z3(-1.0, -0.0); // other side of the cut
    std::cout << "(-1,-0)^0.5 = " << std::pow(z3, 0.5) << '\n';
 
    std::complex<double> i(0.0, 1.0); // i^i = exp(-pi / 2)
    std::cout << "i^i = " << std::pow(i, i) << '\n';
}
```

Output:

```text
(1,2)^2 = (-3.000000,4.000000)
-1^0.5 = (0.000000,1.000000)
(-1,-0)^0.5 = (0.000000,-1.000000)
i^i = (0.207880,0.000000)
```

## See also

{/* source:cpp-numeric-complex-pow:0012 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`sqrt(std::complex)`](/docs/cpp/library/numeric/complex/sqrt)</DescriptionTerm>
    <DescriptionBody>complex square root in the range of the right half-plane (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`pow`, `powf`, `powl`](/docs/cpp/library/numeric/math/pow) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>raises a number to the given power (`xy`) (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`pow(std::valarray)`](/docs/cpp/library/numeric/valarray/pow)</DescriptionTerm>
    <DescriptionBody>applies the function `std::pow` to two valarrays or a valarray and a value (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`C documentation`](/docs/c/library/numeric/complex/cpow)</DescriptionTerm>
    <DescriptionBody>for `cpow`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
