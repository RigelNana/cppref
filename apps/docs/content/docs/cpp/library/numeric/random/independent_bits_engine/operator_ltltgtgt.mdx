---
title: "operator<<,>>(std::independent_bits_engine)"
description: "These function templates are not visible to ordinary unqualified or qualified lookup, and can only be found by argument-dependent lookup when std::independent_bits_engine<Engine, w, UIntType> is an associated class of the arguments."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/independent_bits_engine/operator_ltltgtgt"
language: "C++"
---

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" id="1" since="C++11" code={`template< class CharT, class Traits >
friend std::basic_ostream<CharT,Traits>& 
    operator<<( std::basic_ostream<CharT,Traits>& ost, 

                independent_bits_engine<>& e );`} />
  <Declaration language="cpp" id="2" since="C++11" code={`template< class CharT, class Traits >
friend std::basic_istream<CharT,Traits>& 
    operator>>( std::basic_istream<CharT,Traits>& ist, 

                independent_bits_engine& e );`} />

  <DeclarationDescription>
{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0001 */}
1. Serializes the internal state of the pseudo-random number engine adaptor as a sequence of decimal numbers separated by one or more spaces, and inserts it to the stream `ost`. The fill character and the formatting flags of the stream are ignored and unaffected.

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0002 */}
2. Restores the internal state of the pseudo-random number engine adaptor `e` from the serialized representation, which was created by an earlier call to `operator<<` using a stream with the same imbued locale and the same `CharT` and `Traits`. If the input cannot be deserialized, `e` is left unchanged and `failbit` is raised on `ist`.

  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0003 */}
These function templates are not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when std::independent_bits_engine\<Engine, w, UIntType\> is an associated class of the arguments.

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0004 */}
If a textual representation is written using `os << x` and that representation is restored into the same or a different object `y` of the same type using `is >> y`, then `x == y`.

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0005 */}
This section is incomplete Reason: standard also defines what the textual representation consists of

### Parameters

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0006 */}
<ParameterList>
  <Parameter name="ost">output stream to insert the data to</Parameter>
  <Parameter name="ist">input stream to extract the data from</Parameter>
  <Parameter name="e">engine adaptor to serialize or restore</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0007 */}
1. `ost`

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0008 */}
2. `ist`

### Complexity

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0009 */}
This section is incomplete

### Exceptions

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0010 */}
1. May throw implementation-defined exceptions.

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0011 */}
2. May throw [`std::ios_base::failure`](/docs/cpp/library/io/ios_base/failure) when setting `failbit`.

### Defect reports

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0012 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-numeric-random-independent-bits-engine-operator-ltltgtgt:0013 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3519} standard="C++11">
    <PublishedBehavior>the form of insertion and extraction operators were unspecified</PublishedBehavior>
    <CorrectedBehavior>specified to be hidden friends</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
