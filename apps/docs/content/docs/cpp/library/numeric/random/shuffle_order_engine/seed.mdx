---
title: "std::shuffle_order_engine<Engine,K>::seed"
description: "Reinitializes the internal state of the underlying engine using a new seed value."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/shuffle_order_engine/seed"
language: "C++"
---

{/* source:cpp-numeric-random-shuffle-order-engine-seed:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`void seed();`} />
  <Declaration id="2" language="cpp" since="C++11" code={`void seed( result_type value );`} />
  <Declaration id="3" language="cpp" since="C++11" code={`template< class SeedSeq > 
void seed( SeedSeq& seq );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-random-shuffle-order-engine-seed:0001 */}
    Reinitializes the internal state of the underlying engine using a new seed value.

    {/* source:cpp-numeric-random-shuffle-order-engine-seed:0002 */}
    1. Seeds the underlying engine with the default seed value. Effectively calls `e.seed()`, where `e` is the underlying engine.

    {/* source:cpp-numeric-random-shuffle-order-engine-seed:0003 */}
    2. Seeds the underlying engine with the seed value `value`. Effectively calls `e.seed(value)`, where `e` is the underlying engine.

    {/* source:cpp-numeric-random-shuffle-order-engine-seed:0004 */}
    3. Seeds the underlying engine with the seed sequence `seq`. Effectively calls `e.seed(seq)`, where `e` is the underlying engine.

    {/* source:cpp-numeric-random-shuffle-order-engine-seed:0005 */}
    This overload participates in overload resolution only if `SeedSeq` satisfies the requirements of [*SeedSequence*](/docs/cpp/library/named_req/SeedSequence).

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-random-shuffle-order-engine-seed:0006 */}
<ParameterList>
  <Parameter name="value">seed value to use in the initialization of the internal state of the underlying engine</Parameter>
  <Parameter name="seq">seed sequence to use in the initialization of the internal state of the underlying engine</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-numeric-random-shuffle-order-engine-seed:0007 */}
3. If `SeedSeq` is not [`std::seed_seq`](/docs/cpp/library/numeric/random/seed_seq), throws the exceptions thrown by the `seq.generate` call.

### Defect reports

{/* source:cpp-numeric-random-shuffle-order-engine-seed:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-numeric-random-shuffle-order-engine-seed:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2181} standard="C++11">
    <PublishedBehavior>overload ([3](/docs/cpp/library/numeric/random/shuffle_order_engine/seed#Version_3)) would not throw even if the `seq.generate` call throws</PublishedBehavior>
    <CorrectedBehavior>propagates the exception</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
