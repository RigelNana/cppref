---
title: "std::uniform_real_distribution<RealType>::operator()"
description: "Generates random numbers that are distributed according to the associated probability function. The entropy is acquired by calling g.operator ( )."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/uniform_real_distribution/operator()"
language: "C++"
---

{/* source:cpp-numeric-random-uniform-real-distribution-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`template< class Generator >
result_type operator()( Generator& g );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`template< class Generator >
result_type operator()( Generator& g, const param_type& params );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-random-uniform-real-distribution-operator:0001 */}
    Generates random numbers that are distributed according to the associated probability function. The entropy is acquired by calling `g.operator()`.

    {/* source:cpp-numeric-random-uniform-real-distribution-operator:0002 */}
    The first version uses the associated parameter set, the second version uses `params`. The associated parameter set is not modified.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-random-uniform-real-distribution-operator:0003 */}
<ParameterList>
  <Parameter name="g">a uniform random bit generator object</Parameter>
  <Parameter name="params">distribution parameter set to use instead of the associated one</Parameter>
</ParameterList>

Type requirements

- `Generator` must meet the requirements of [*UniformRandomBitGenerator*](/docs/cpp/library/named_req/UniformRandomBitGenerator).

### Return value

{/* source:cpp-numeric-random-uniform-real-distribution-operator:0004 */}
The generated random number.

### Complexity

{/* source:cpp-numeric-random-uniform-real-distribution-operator:0005 */}
Amortized constant number of invocations of `g.operator()`.
