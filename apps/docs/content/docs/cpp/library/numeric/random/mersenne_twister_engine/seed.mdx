---
title: "std::mersenne_twister_engine<UIntType,w,n,m,r,a,u,d,s,b,t,c,l,f>::seed"
description: "Sets the state of the random-number engine."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/mersenne_twister_engine/seed"
language: "C++"
---

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" id="1" since="C++11" code={`void seed( result_type value = default_seed );`} />
  <Declaration language="cpp" id="2" since="C++11" code={`template< class SeedSeq > 
void seed( SeedSeq& seq );`} />

  <DeclarationDescription>
{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0001 */}
Sets the [state](/docs/cpp/library/numeric/random/mersenne_twister_engine#Generator_properties) of the random-number engine.

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0002 */}
1. Right after the state is set, *this == std::mersenne_twister_engine(value) is `true`.

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0003 */}
2. Right after the state is set, *this == std::mersenne_twister_engine(seq) is `true`.

  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0004 */}
<ParameterList>
  <Parameter name="value">seed value to use to set the state</Parameter>
  <Parameter name="seq">seed sequence to use to set the state</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0005 */}
2. If `SeedSeq` is not [`std::seed_seq`](/docs/cpp/library/numeric/random/seed_seq), throws the exceptions thrown by the `seq.generate` call.

### Complexity

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0006 */}
1. Same as std::mersenne_twister_engine(value).

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0007 */}
2. Same as std::mersenne_twister_engine(seq).

### Defect reports

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2181} standard="C++11">
    <PublishedBehavior>overload ( [2](/docs/cpp/library/numeric/random/mersenne_twister_engine/seed#Version_2)) would not throw even if the `seq.generate` call throws</PublishedBehavior>
    <CorrectedBehavior>propagates the exception</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/numeric/random/mersenne_twister_engine/mersenne_twister_engine)</DescriptionTerm>
    <DescriptionBody>constructs the engine <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Example

{/* source:cpp-numeric-random-mersenne-twister-engine-seed:0011 */}
```cpp
#include <iostream>
#include <random>
 
int main()
{
    std::mt19937 gen;
 
    // Seed the engine with an unsigned int
    gen.seed(1);
    std::cout << "after seed by 1: " << gen() << '\n';
 
    // Seed the engine with two unsigned ints
    std::seed_seq sseq{1, 2};
    gen.seed(sseq);
    std::cout << "after seed by {1,2}: " << gen() << '\n';
}
```

Possible output:

```text
after seed by 1: 1791095845
after seed by {1,2}: 3127717181
```
