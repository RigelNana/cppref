---
title: "operator<<,>>(std::philox_engine)"
description: "These function templates are not visible to ordinary unqualified or qualified lookup, and can only be found by argument-dependent lookup when decltype(e) is an associated class of the arguments."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/philox_engine/operator_ltltgtgt"
language: "C++"
---

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" id="1" since="C++26" code={`template< class CharT, class Traits >
friend std::basic_ostream<CharT, Traits>& 
    operator<<( std::basic_ostream<CharT, Traits>& os, 

                const philox_engine& e );`} />
  <Declaration language="cpp" id="2" since="C++26" code={`template< class CharT, class Traits >
friend std::basic_istream<CharT, Traits>& 
    operator>>( std::basic_istream<CharT, Traits>& is, 

                philox_engine& e );`} />

  <DeclarationDescription>
{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0001 */}
1. Writes the textual representation of the current state of `e` to `os` with fmtflags set to std::ios_base::dec | std::ios_base::left and the fill character set to the space character.

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0002 */}
After writing, `os`'s original fmtflags and fill character are restored.

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0003 */}
2. Reads a textual representation of engine state from `is` (denoted as `text`) with fmtflags set to std::ios_base::dec, and sets the state of `e` to that state.

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0004 */}
After reading, `is`'s original fmtflags is restored. If `text` was not previously written using an output stream `pr`, the behavior is undefined. Otherwise, if any of the following values is `false`, the behavior is undefined: `is.getloc() == pr.getloc()` std::is_same\<decltype(is)::char_type, decltype(pr)::char_type\>::value std::is_same\<decltype(is)::traits_type, decltype(pr)::traits_type\>::value Otherwise, if `text` is not a valid textual representation of any state of `decltype(e)`, the state of `e` is unchanged and is.setstate(std::ios_base::failbit) is called. Otherwise, given another engine `eng` of the same type as `e`. If `text` was previously written by `pr << eng` and there is no intervening invocations of `e` or of `eng` between `pr << eng` and `is >> e`, `e == eng` is `true`.

  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0005 */}
These function templates are not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `decltype(e)` is an associated class of the arguments.

### Parameters

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0006 */}
<ParameterList>
  <Parameter name="os">output stream to insert the data to</Parameter>
  <Parameter name="is">input stream to extract the data from</Parameter>
  <Parameter name="e">pseudo-random number engine</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0007 */}
1. `os`

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0008 */}
2. `is`

### Complexity

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0009 */}
O(n).

### Exceptions

{/* source:cpp-numeric-random-philox-engine-operator-ltltgtgt:0010 */}
2. May throw [`std::ios_base::failure`](/docs/cpp/library/io/ios_base/failure) when setting std::ios_base::failbit.
