---
title: "RAND_MAX"
description: "Expands to an integer constant expression equal to the maximum value returned by the function std::rand. This value is implementation dependent. It's guaranteed that this value is at least 32767."
source_url: "https://en.cppreference.com/w/cpp/numeric/random/RAND_MAX"
language: "C++"
---

{/* source:cpp-numeric-random-rand-max:0000 */}
<DeclarationDoc>
 Defined in header [`<cstdlib>`](/docs/cpp/library/headers/cstdlib)
 

  <Declaration language="cpp" code={`#define RAND_MAX /*implementation defined*/`} />
</DeclarationDoc>
{/* source:cpp-numeric-random-rand-max:0001 */}
Expands to an integer constant expression equal to the maximum value returned by the function [`std::rand`](/docs/cpp/library/numeric/random/rand). This value is implementation dependent. It's guaranteed that this value is at least `32767`.

### Example

{/* source:cpp-numeric-random-rand-max:0002 */}
```cpp
#include <climits>
#include <cstdlib>
#include <ctime>
#include <iostream>
 
int main()
{
    // use current time as seed for random generator
    std::srand(std::time(NULL));
 
    std::cout << "RAND_MAX: " << RAND_MAX << '\n'
              << "INT_MAX: " << INT_MAX << '\n'
              << "Random value on [0,1]: "
              << static_cast<double>(std::rand()) / RAND_MAX << '\n';
}
```

Possible output:

```text
RAND_MAX: 2147483647
INT_MAX: 2147483647
Random value on [0,1]: 0.618608
```

### See also

{/* source:cpp-numeric-random-rand-max:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`rand`](/docs/cpp/library/numeric/random/rand)</DescriptionTerm>
    <DescriptionBody>generates a pseudo-random number <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`srand`](/docs/cpp/library/numeric/random/srand)</DescriptionTerm>
    <DescriptionBody>seeds pseudo-random number generator <br /> (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
 [C documentation](/docs/c/library/numeric/random/RAND_MAX) for RAND_MAX
