---
title: "std::midpoint"
description: "Computes the midpoint of the integers, floating-points, or pointers a and b."
source_url: "https://en.cppreference.com/w/cpp/numeric/midpoint"
language: "C++"
---

{/* source:cpp-numeric-midpoint:0000 */}
<DeclarationDoc>
Defined in header [`<numeric>`](/docs/cpp/library/headers/numeric)

  <Declaration id="1" language="cpp" since="C++20" code={`template< class T >
constexpr T midpoint( T a, T b ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< class T >
constexpr T* midpoint( T* a, T* b );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-midpoint:0001 */}
    Computes the midpoint of the integers, floating-points, or pointers `a` and `b`.

    {/* source:cpp-numeric-midpoint:0002 */}
    1) This overload participates in overload resolution only if `T` is an arithmetic type other than `bool`.

    {/* source:cpp-numeric-midpoint:0003 */}
    2) This overload participates in overload resolution only if `T` is an object type. Use of this overload is ill-formed if `T` is an [incomplete type](/docs/cpp/language/basic_concepts/type-id#Incomplete_type).
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-midpoint:0004 */}
<ParameterList>
  <Parameter name="a">integers, floating-points, or pointer values</Parameter>
  <Parameter name="b">integers, floating-points, or pointer values</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-midpoint:0005 */}
1) Half the sum of `a` and `b`. No overflow occurs. If `a` and `b` have integer type and the sum is odd, the result is rounded towards `a`. If `a` and `b` have floating-point type, at most one inexact operation occurs.

{/* source:cpp-numeric-midpoint:0006 */}
2) If `a` and `b` point to, respectively, `x[i]` and `x[j]` of the same array object `x` (for the purpose of [pointer arithmetic](/docs/cpp/language/expressions/operator_arithmetic#Additive_operators)), returns a pointer to `x[i + (j - i) / 2]` (or, equivalently `x[std::midpoint(i, j)]`) where the division rounds towards zero. If `a` and `b` do not point to elements of the same array object, the behavior is undefined.

### Exceptions

{/* source:cpp-numeric-midpoint:0007 */}
Throws no exceptions.

### Notes

{/* source:cpp-numeric-midpoint:0008 */}
Overload (2) can be simply implemented as `return a + (b - a) / 2;` on common platforms. However, such implementation is not guaranteed to be portable, because there may be some platforms where creating an array with number of elements greater than [`PTRDIFF_MAX`](/docs/cpp/library/utility/types/climits) is possible, and `b - a` may result in undefined behavior even if both `b` and `a` point to elements in the same array.

{/* source:cpp-numeric-midpoint:0009 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_interpolate`](/docs/cpp/library/feature_test#cpp_lib_interpolate) | [`201902L`](/docs/cpp/library/compiler_support/20#cpp_lib_interpolate_201902L) | (C++20) | [`std::lerp`](/docs/cpp/library/numeric/lerp), `std::midpoint` |

### Example

{/* source:cpp-numeric-midpoint:0010 */}
```cpp
#include <cstdint>
#include <iostream>
#include <limits>
#include <numeric>
 
int main()
{
    std::uint32_t a = std::numeric_limits<std::uint32_t>::max();
    std::uint32_t b = std::numeric_limits<std::uint32_t>::max() - 2;
 
    std::cout << "a: " << a << '\n'
              << "b: " << b << '\n'
              << "Incorrect (overflow and wrapping): " << (a + b) / 2 << '\n'
              << "Correct: " << std::midpoint(a, b) << "\n\n";
 
    auto on_pointers = [](int i, int j)
    {
        char const* text = "0123456789";
        char const* p = text + i;
        char const* q = text + j;
        std::cout << "std::midpoint('" << *p << "', '" << *q << "'): '"
                  << *std::midpoint(p, q) << "'\n";
    };
 
    on_pointers(2, 4);
    on_pointers(2, 5);
    on_pointers(5, 2);
    on_pointers(2, 6);
}
```

Output:

```text
a: 4294967295
b: 4294967293
Incorrect (overflow and wrapping): 2147483646
Correct: 4294967294
 
std::midpoint('2', '4'): '3'
std::midpoint('2', '5'): '3'
std::midpoint('5', '2'): '4'
std::midpoint('2', '6'): '4'
```

### References

{/* source:cpp-numeric-midpoint:0011 */}
C++23 standard (ISO/IEC 14882:2024): 27.10.16 Midpoint [numeric.ops.midpoint]

{/* source:cpp-numeric-midpoint:0012 */}
C++20 standard (ISO/IEC 14882:2020): 25.10.15 Midpoint [numeric.ops.midpoint]

### See also

{/* source:cpp-numeric-midpoint:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`lerp`](/docs/cpp/library/numeric/lerp) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>linear interpolation function (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
