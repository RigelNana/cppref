---
title: "std::ranges::iota, std::ranges::iota_result"
description: "Fills the range [first, last) with sequentially increasing values, starting with value and repetitively evaluating ++value."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/iota"
language: "C++"
---

{/* source:cpp-algorithm-ranges-iota:0000 */}
<DeclarationDoc>
Defined in header [`<numeric>`](/docs/cpp/library/headers/numeric)

Call signature

  <Declaration id="1" language="cpp" since="C++23" code={`template< std::input_or_output_iterator O, std::sentinel_for<O> S,
          std::weakly_incrementable T >
requires std::indirectly_writable<O, const T&>
constexpr iota_result<O, T>

    iota( O first, S last, T value );`} />
  <Declaration id="2" language="cpp" since="C++23" code={`template< std::weakly_incrementable T, ranges::output_range<const T&> R >
constexpr iota_result<ranges::borrowed_iterator_t<R>, T>

    iota( R&& r, T value );`} />

Helper types

  <Declaration id="3" language="cpp" since="C++23" code={`template< class O, class T >
using iota_result = ranges::out_value_result<O, T>;`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-iota:0001 */}
Fills the range [`first`, `last`) with sequentially increasing values, starting with `value` and repetitively evaluating `++value`.

{/* source:cpp-algorithm-ranges-iota:0002 */}
Equivalent operation:

{/* source:cpp-algorithm-ranges-iota:0003 */}
```cpp
*(first)     = value;
*(first + 1) = ++value;
*(first + 2) = ++value;
*(first + 3) = ++value;
...
```

### Parameters

{/* source:cpp-algorithm-ranges-iota:0004 */}
<ParameterList>
  <Parameter name="first, last">the range of elements to fill with sequentially increasing values starting with `value`</Parameter>
  <Parameter name="value">initial value to store; the expression `++value` must be well-formed</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-iota:0005 */}
[`{last, value + `ranges::distance`(first, last)}`](/docs/cpp/library/iterator/ranges/distance)

### Complexity

{/* source:cpp-algorithm-ranges-iota:0006 */}
Exactly `last - first` increments and assignments.

### Possible implementation

{/* source:cpp-algorithm-ranges-iota:0007 */}
```cpp
struct iota_fn
{
    template<std::input_or_output_iterator O, std::sentinel_for<O> S,
            std::weakly_incrementable T>
    requires std::indirectly_writable<O, const T&>
    constexpr iota_result<O, T> operator()(O first, S last, T value) const
    {
        while (first != last)
        {
            *first = as_const(value);
            ++first;
            ++value;
        }
        return {std::move(first), std::move(value)};
    }
 
    template<std::weakly_incrementable T, std::ranges::output_range<const T&> R>
    constexpr iota_result<std::ranges::borrowed_iterator_t<R>, T>
    operator()(R&& r, T value) const
    {
        return (*this)(std::ranges::begin(r), std::ranges::end(r), std::move(value));
    }
};
 
inline constexpr iota_fn iota;
```

### Notes

{/* source:cpp-algorithm-ranges-iota:0008 */}
The function is named after the integer function ⍳ from the programming language [APL](https://en.wikipedia.org/wiki/APL_(programming_language)).

{/* source:cpp-algorithm-ranges-iota:0009 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_ranges_iota`](/docs/cpp/library/feature_test#cpp_lib_ranges_iota) | [`202202L`](/docs/cpp/library/compiler_support/23#cpp_lib_ranges_iota_202202L) | (C++23) | `std::ranges::iota` |

### Example

{/* source:cpp-algorithm-ranges-iota:0010 */}
Uses the [`vector`](/docs/cpp/library/container/vector) of iterators (`std::vector<std::list<T>::iterator>`) as a proxy to shuffle the elements of the [`std::list`](/docs/cpp/library/container/list), because [`ranges::shuffle`](/docs/cpp/library/algorithm/ranges/shuffle) cannot be applied to the [`std::list`](/docs/cpp/library/container/list) directly.

```cpp
#include <algorithm>
#include <functional>
#include <iostream>
#include <list>
#include <numeric>
#include <random>
#include <vector>
 
template <typename Proj = std::identity>
void println(auto comment, std::ranges::input_range auto&& range, Proj proj = {})
{
    for (std::cout << comment; auto const &element : range)
        std::cout << proj(element) << ' ';
    std::cout << '\n';
}
 
int main()
{
    std::list<int> list(8);
 
    // Fill the list with ascending values: 0, 1, 2, ..., 7
    std::ranges::iota(list, 0);
    println("List: ", list);
 
    // A vector of iterators (see the comment to Example)
    std::vector<std::list<int>::iterator> vec(list.size());
 
    // Fill with iterators to consecutive list's elements
    std::ranges::iota(vec.begin(), vec.end(), list.begin());
 
    std::ranges::shuffle(vec, std::mt19937 {std::random_device {}()});
    println("List viewed via vector: ", vec, [](auto it) { return *it; });
}
```

Possible output:

```text
List: 0 1 2 3 4 5 6 7
List viewed via vector: 5 7 6 0 1 3 4 2
```

### See also

{/* source:cpp-algorithm-ranges-iota:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`fill`](/docs/cpp/library/algorithm/fill)</DescriptionTerm>
    <DescriptionBody>copy-assigns the given value to every element in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::fill`](/docs/cpp/library/algorithm/ranges/fill) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>assigns a range of elements a certain value (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`generate`](/docs/cpp/library/algorithm/generate)</DescriptionTerm>
    <DescriptionBody>assigns the results of successive function calls to every element in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::generate`](/docs/cpp/library/algorithm/ranges/generate) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>saves the result of a function in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::iota_view` `views::iota`](/docs/cpp/library/ranges/iota_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a [`view`](/docs/cpp/library/ranges/view) consisting of a sequence generated by repeatedly incrementing an initial value (class template) (customization point object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iota`](/docs/cpp/library/numeric/iota) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>fills a range with successive increments of the starting value (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
