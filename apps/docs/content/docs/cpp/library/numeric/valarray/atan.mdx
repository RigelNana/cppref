---

title: "std::atan(std::valarray)"

description: "For each element in va computes arc tangent of the value of the element."

source_url: "https://en.cppreference.com/w/cpp/numeric/valarray/atan"

language: "C++"

---

{/* source:cpp-numeric-valarray-atan:0000 */}
<DeclarationDoc>
Defined in header [`<valarray>`](/docs/cpp/library/headers/valarray)
  <Declaration language="cpp" code={`template< class T >
valarray<T> atan( const valarray<T>& va );`} />
</DeclarationDoc>

{/* source:cpp-numeric-valarray-atan:0001 */}
For each element in `va` computes arc tangent of the value of the element.

### Parameters

{/* source:cpp-numeric-valarray-atan:0002 */}
<ParameterList>
  <Parameter name="va">value array to apply the operation to</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-valarray-atan:0003 */}
Value array containing arc tangents of the values in `va`.

### Notes

{/* source:cpp-numeric-valarray-atan:0004 */}
Unqualified function (`atan`) is used to perform the computation. If such function is not available, [`std::atan`](/docs/cpp/library/numeric/math/atan) is used due to [argument-dependent lookup](/docs/cpp/language/functions/adl).

{/* source:cpp-numeric-valarray-atan:0005 */}
The function can be implemented with the return type different from [`std::valarray`](/docs/cpp/library/numeric/valarray). In this case, the replacement type has the following properties:

{/* source:cpp-numeric-valarray-atan:0006 */}
- All const member functions of [`std::valarray`](/docs/cpp/library/numeric/valarray) are provided.
- [`std::valarray`](/docs/cpp/library/numeric/valarray), [`std::slice_array`](/docs/cpp/library/numeric/valarray/slice_array), [`std::gslice_array`](/docs/cpp/library/numeric/valarray/gslice_array), [`std::mask_array`](/docs/cpp/library/numeric/valarray/mask_array) and [`std::indirect_array`](/docs/cpp/library/numeric/valarray/indirect_array) can be constructed from the replacement type.
- For every function taking a const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) <InlineRevision since="C++11">except [`begin()`](/docs/cpp/library/numeric/valarray/begin2) and [`end()`](/docs/cpp/library/numeric/valarray/end2)</InlineRevision>, identical functions taking the replacement types shall be added;
- For every function taking two const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) arguments, identical functions taking every combination of const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) and replacement types shall be added.
- The return type does not add more than two levels of template nesting over the most deeply-nested argument type.

### Possible implementation

{/* source:cpp-numeric-valarray-atan:0007 */}
```cpp
template<class T>
valarray<T> atan(const valarray<T>& va)
{
    valarray<T> other = va;
    for (T& i : other)
        i = atan(i);
 
    return other; // proxy object may be returned
}
```

### Example

{/* source:cpp-numeric-valarray-atan:0008 */}
Output:
```cpp
#include <algorithm>
#include <cmath>
#include <iostream>
#include <valarray>
 
auto show = [](char const* title, const std::valarray<float>& va)
{
    std::cout << title << " :";
    std::for_each(std::begin(va), std::end(va), 
        [](const float x) { std::cout << "  " << std::fixed << x; });
    std::cout << '\n';
};
 
int main()
{
    const std::valarray<float> x = {.1f, .3f, .6f, .9f};
    const std::valarray<float> f = std::atan(x);
    const std::valarray<float> g = std::tan(f);
 
    show("x          ", x);
    show("f = atan(x)", f);
    show("g = tan(f) ", g);
}
```
Output:
```text
x           :  0.100000  0.300000  0.600000  0.900000
f = atan(x) :  0.099669  0.291457  0.540420  0.732815
g = tan(f)  :  0.100000  0.300000  0.600000  0.900000
```

### See also

{/* source:cpp-numeric-valarray-atan:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`asin(std::valarray)`](/docs/cpp/library/numeric/valarray/asin)</DescriptionTerm>
    <DescriptionBody>applies the function [`std::asin`](/docs/cpp/library/numeric/math/asin) to each element of valarray <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acos(std::valarray)`](/docs/cpp/library/numeric/valarray/acos)</DescriptionTerm>
    <DescriptionBody>applies the function [`std::acos`](/docs/cpp/library/numeric/math/acos) to each element of valarray <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan2(std::valarray)`](/docs/cpp/library/numeric/valarray/atan2)</DescriptionTerm>
    <DescriptionBody>applies the function [`std::atan2`](/docs/cpp/library/numeric/math/atan2) to a valarray and a value <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tan(std::valarray)`](/docs/cpp/library/numeric/valarray/tan)</DescriptionTerm>
    <DescriptionBody>applies the function [`std::tan`](/docs/cpp/library/numeric/math/tan) to each element of valarray <br /> (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan`, `atanf`, `atanl`](/docs/cpp/library/numeric/math/atan) <InlineRevision since="C++11" /> <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc tangent (`arctan(x)`) <br /> (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`atan(std::complex)`](/docs/cpp/library/numeric/complex/atan)<InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>computes arc tangent of a complex number (`arctan(z)`) <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
