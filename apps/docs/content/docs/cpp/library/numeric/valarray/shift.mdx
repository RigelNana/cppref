---

title: "std::valarray<T>::shift"

description: "Returns a new valarray of the same size with elements whose positions are shifted by count elements. The new position of each element is i\u2212count where i is the previous position. The value of shifted in elements is T( )."

source_url: "https://en.cppreference.com/w/cpp/numeric/valarray/shift"

language: "C++"

---

{/* source:cpp-numeric-valarray-shift:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" code={`valarray<T> shift( int count ) const;`} />
</DeclarationDoc>

{/* source:cpp-numeric-valarray-shift:0001 */}
Returns a new valarray of the same size with elements whose positions are shifted by `count` elements. The new position of each element is `i−count` where `i` is the previous position. The value of shifted in elements is `T()`.

### Parameters

{/* source:cpp-numeric-valarray-shift:0002 */}
<ParameterList>
  <Parameter name="count">number of positions to shift the elements by</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-valarray-shift:0003 */}
The resulting valarray with shifted elements.

### Notes

{/* source:cpp-numeric-valarray-shift:0004 */}
The function can be implemented with the return type different from [`std::valarray`](/docs/cpp/library/numeric/valarray). In this case, the replacement type has the following properties:

{/* source:cpp-numeric-valarray-shift:0005 */}
- All const member functions of [`std::valarray`](/docs/cpp/library/numeric/valarray) are provided.
- [`std::valarray`](/docs/cpp/library/numeric/valarray), [`std::slice_array`](/docs/cpp/library/numeric/valarray/slice_array), [`std::gslice_array`](/docs/cpp/library/numeric/valarray/gslice_array), [`std::mask_array`](/docs/cpp/library/numeric/valarray/mask_array) and [`std::indirect_array`](/docs/cpp/library/numeric/valarray/indirect_array) can be constructed from the replacement type.
- For every function taking a const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) <InlineRevision since="C++11">except [`begin()`](/docs/cpp/library/numeric/valarray/begin2) and [`end()`](/docs/cpp/library/numeric/valarray/end2)</InlineRevision>, identical functions taking the replacement types shall be added;
- For every function taking two const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) arguments, identical functions taking every combination of const [`std::valarray<T>&`](/docs/cpp/library/numeric/valarray) and replacement types shall be added.
- The return type does not add more than two levels of template nesting over the most deeply-nested argument type.

### Example

{/* source:cpp-numeric-valarray-shift:0006 */}
Output:
```cpp
#include <iostream>
#include <valarray>
 
int main()
{
    std::valarray<int> v{1, 2, 3, 4, 5, 6, 7, 8};
 
    for (auto const& val : v)
        std::cout << val << ' ';
    std::cout << '\n';
 
    std::valarray<int> v2 = v.shift(2);
 
    for (auto const& val : v2)
        std::cout << val << ' ';
    std::cout << '\n';
}
```
Output:
```text
1 2 3 4 5 6 7 8 
3 4 5 6 7 8 0 0
```

### See also

{/* source:cpp-numeric-valarray-shift:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`cshift`](/docs/cpp/library/numeric/valarray/cshift)</DescriptionTerm>
    <DescriptionBody>circular shift of the elements of the valarray <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
