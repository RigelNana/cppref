---
title: "std::cyl_bessel_k, std::cyl_bessel_kf, std::cyl_bessel_kl"
description: "Computes the irregular modified cylindrical Bessel function (also known as modified Bessel function of the second kind) of nu and x."
source_url: "https://en.cppreference.com/w/cpp/numeric/special_functions/cyl_bessel_k"
language: "C++"
---

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)

  <Declaration id="1" language="cpp" since="C++17" until="C++23" code={`float       cyl_bessel_k ( float nu, float x );
double      cyl_bessel_k ( double nu, double x );

long double cyl_bessel_k ( long double nu, long double x );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/* floating-point-type */ cyl_bessel_k( /* floating-point-type */ nu,
                                        /* floating-point-type */ x );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`float       cyl_bessel_kf( float nu, float x );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`long double cyl_bessel_kl( long double nu, long double x );`} />

[Additional overloads](/docs/cpp/library/numeric/special_functions/cyl_bessel_k#Notes)

Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)

  <Declaration id="A" language="cpp" since="C++17" code={`template< class Arithmetic1, class Arithmetic2 >
/* common-floating-point-type */

    cyl_bessel_k( Arithmetic1 nu, Arithmetic2 x );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-special-functions-cyl-bessel-k:0001 */}
        1-3) Computes the [irregular modified cylindrical Bessel function](https://en.wikipedia.org/wiki/Bessel_function#Modified_Bessel_functions:_I.CE.B1_.2C_K.CE.B1) (also known as modified Bessel function of the second kind) of `nu` and `x`. <InlineRevision since="C++23">The library provides overloads of `std::cyl_bessel_k` for all cv-unqualified floating-point types as the type of the parameters `nu` and `x`.</InlineRevision>

    {/* source:cpp-numeric-special-functions-cyl-bessel-k:0002 */}
        A) Additional overloads are provided for all other combinations of arithmetic types.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0003 */}
<ParameterList>
  <Parameter name="nu">the order of the function</Parameter>
  <Parameter name="x">the argument of the function</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0004 */}
| π |
| --- |
| 2 |

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0005 */}
| I-nu(x)-Inu(x) |
| --- |
| sin(nuπ) |

### Error handling

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0006 */}
Errors may be reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling):

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0007 */}
- If the argument is NaN, NaN is returned and domain error is not reported.
- If nu≥128, the behavior is implementation-defined.

### Notes

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0008 */}
Implementations that do not support C++17, but support [ISO 29124:2010](/docs/cpp/library/experimental/special_math), provide this function if `__STDCPP_MATH_SPEC_FUNCS__` is defined by the implementation to a value at least `201003L` and if the user defines `__STDCPP_WANT_MATH_SPEC_FUNCS__` before including any standard library headers.

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0009 */}
Implementations that do not support ISO 29124:2010 but support TR 19768:2007 (TR1), provide this function in the header `tr1/cmath` and namespace `std::tr1`.

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0010 */}
An implementation of this function is also [available in boost.math](https://www.boost.org/doc/libs/release/libs/math/doc/html/math_toolkit/bessel/mbessel.html).

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0011 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0012 */}
<Revision until="C++23">
- If `num1` or `num2` has type `long double`, then `std::cyl_bessel_k(num1, num2)` has the same effect as `std::cyl_bessel_k(static_cast<long double>(num1),` `                  static_cast<long double>(num2))`.
- Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::cyl_bessel_k(num1, num2)` has the same effect as `std::cyl_bessel_k(static_cast<double>(num1),` `                  static_cast<double>(num2))`.
- Otherwise, if `num1` or `num2` has type `float`, then `std::cyl_bessel_k(num1, num2)` has the same effect as `std::cyl_bessel_k(static_cast<float>(num1),` `                  static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::cyl_bessel_k(num1, num2)` has the same effect as `std::cyl_bessel_k(static_cast</* common-floating-point-type */>(num1),` `                  static_cast</* common-floating-point-type */>(num2))`, where `/* common-floating-point-type */` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0013 */}
```cpp
#include <cmath>
#include <iostream>
#include <numbers>
 
int main()
{
    double pi = std::numbers::pi;
    const double x = 1.2345;
 
    // spot check for nu == 0.5
    std::cout << "K_.5(" << x << ") = " << std::cyl_bessel_k(.5, x) << '\n'
              << "calculated via I = "
              << (pi / 2) * (std::cyl_bessel_i(-.5, x)
                 - std::cyl_bessel_i(.5, x)) / std::sin(.5 * pi) << '\n';
}
```

Output:

```text
K_.5(1.2345) = 0.32823
calculated via I = 0.32823
```

### See also

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0014 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`cyl_bessel_i`, `cyl_bessel_if`, `cyl_bessel_il`](/docs/cpp/library/numeric/special_functions/cyl_bessel_i) <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>regular modified cylindrical Bessel functions (function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`cyl_bessel_j`, `cyl_bessel_jf`, `cyl_bessel_jl`](/docs/cpp/library/numeric/special_functions/cyl_bessel_j) <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>cylindrical Bessel functions (of the first kind) (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### External links

{/* source:cpp-numeric-special-functions-cyl-bessel-k:0015 */}
[Weisstein, Eric W. "Modified Bessel Function of the Second Kind."](https://mathworld.wolfram.com/ModifiedBesselFunctionoftheSecondKind.html) From MathWorld — A Wolfram Web Resource.
