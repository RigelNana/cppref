---
title: "std::cyl_bessel_i, std::cyl_bessel_if, std::cyl_bessel_il"
description: "Computes the regular modified cylindrical Bessel function of nu and x."
source_url: "https://en.cppreference.com/w/cpp/numeric/special_functions/cyl_bessel_i"
language: "C++"
---

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0000 */}
<DeclarationDoc>
Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)

  <Declaration id="1" language="cpp" since="C++17" until="C++23" code={`float       cyl_bessel_i ( float nu, float x );
double      cyl_bessel_i ( double nu, double x );

long double cyl_bessel_i ( long double nu, long double x );`} />
  <Declaration id="1" language="cpp" since="C++23" code={`/* floating-point-type */ cyl_bessel_i( /* floating-point-type */ nu,
                                        /* floating-point-type */ x );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`float       cyl_bessel_if( float nu, float x );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`long double cyl_bessel_il( long double nu, long double x );`} />

[Additional overloads](/docs/cpp/library/numeric/special_functions/cyl_bessel_i#Notes)

Defined in header [`<cmath>`](/docs/cpp/library/headers/cmath)

  <Declaration id="A" language="cpp" since="C++17" code={`template< class Arithmetic1, class Arithmetic2 >
/* common-floating-point-type */

    cyl_bessel_i( Arithmetic1 nu, Arithmetic2 x );`} />

  <DeclarationDescription>
    {/* source:cpp-numeric-special-functions-cyl-bessel-i:0001 */}
        1-3) Computes the [regular modified cylindrical Bessel function](https://en.wikipedia.org/wiki/Bessel_function#Modified_Bessel_functions:_I.CE.B1_.2C_K.CE.B1) of `nu` and `x`. <InlineRevision since="C++23">The library provides overloads of `std::cyl_bessel_i` for all cv-unqualified floating-point types as the type of the parameters `nu` and `x`.</InlineRevision>

    {/* source:cpp-numeric-special-functions-cyl-bessel-i:0002 */}
        A) Additional overloads are provided for all other combinations of arithmetic types.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0003 */}
<ParameterList>
  <Parameter name="nu">the order of the function</Parameter>
  <Parameter name="x">the argument of the function</Parameter>
</ParameterList>

### Return value

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0004 */}
| (x/2)nu+2k |
| --- |
| k!Γ(nu+k+1) |

### Error handling

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0005 */}
Errors may be reported as specified in [math_errhandling](/docs/cpp/library/numeric/math/math_errhandling).

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0006 */}
- If the argument is NaN, NaN is returned and domain error is not reported.
- If nu≥128, the behavior is implementation-defined.

### Notes

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0007 */}
Implementations that do not support C++17, but support [ISO 29124:2010](/docs/cpp/library/experimental/special_math), provide this function if `__STDCPP_MATH_SPEC_FUNCS__` is defined by the implementation to a value at least `201003L` and if the user defines `__STDCPP_WANT_MATH_SPEC_FUNCS__` before including any standard library headers.

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0008 */}
Implementations that do not support ISO 29124:2010 but support TR 19768:2007 (TR1), provide this function in the header `tr1/cmath` and namespace `std::tr1`.

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0009 */}
An implementation of this function is also [available in boost.math](https://www.boost.org/doc/libs/release/libs/math/doc/html/math_toolkit/bessel/mbessel.html).

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0010 */}
The additional overloads are not required to be provided exactly as (A). They only need to be sufficient to ensure that for their first argument `num1` and second argument `num2`:

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0011 */}
<Revision until="C++23">
- If `num1` or `num2` has type `long double`, then `std::cyl_bessel_i(num1, num2)` has the same effect as `std::cyl_bessel_i(static_cast<long double>(num1),` `                  static_cast<long double>(num2))`.
- Otherwise, if `num1` and/or `num2` has type `double` or an integer type, then `std::cyl_bessel_i(num1, num2)` has the same effect as `std::cyl_bessel_i(static_cast<double>(num1),` `                  static_cast<double>(num2))`.
- Otherwise, if `num1` or `num2` has type `float`, then `std::cyl_bessel_i(num1, num2)` has the same effect as `std::cyl_bessel_i(static_cast<float>(num1),` `                  static_cast<float>(num2))`.
</Revision>
<Revision since="C++23">
If `num1` and `num2` have arithmetic types, then `std::cyl_bessel_i(num1, num2)` has the same effect as `std::cyl_bessel_i(static_cast</* common-floating-point-type */>(num1),` `                  static_cast</* common-floating-point-type */>(num2))`, where `/* common-floating-point-type */` is the floating-point type with the greatest [floating-point conversion rank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_rank) and greatest [floating-point conversion subrank](/docs/cpp/language/expressions/usual_arithmetic_conversions#Floating-point_conversion_subrank) between the types of `num1` and `num2`, arguments of integer type are considered to have the same floating-point conversion rank as `double`.
If no such floating-point type with the greatest rank and subrank exists, then [overload resolution](/docs/cpp/language/functions/overload_resolution) does not result in a usable candidate from the overloads provided.
</Revision>

### Example

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0012 */}
```cpp
#include <cmath>
#include <iostream>
 
int main()
{
    // spot check for nu == 0
    const double x = 1.2345;
    std::cout << "I_0(" << x << ") = " << std::cyl_bessel_i(0, x) << '\n';
 
    // series expansion for I_0
    double fct = 1;
    double sum = 0;
    for (int k = 0; k < 5; fct *= ++k)
    {
        sum += std::pow(x / 2, 2 * k) / std::pow(fct, 2);
        std::cout << "sum = " << sum << '\n';
    }
}
```

Output:

```text
I_0(1.2345) = 1.41886
sum = 1
sum = 1.381
sum = 1.41729
sum = 1.41882
sum = 1.41886
```

### See also

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`cyl_bessel_j`, `cyl_bessel_jf`, `cyl_bessel_jl`](/docs/cpp/library/numeric/special_functions/cyl_bessel_j) <InlineRevision since="C++17" /> <InlineRevision since="C++17" /> <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>cylindrical Bessel functions (of the first kind) (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### External links

{/* source:cpp-numeric-special-functions-cyl-bessel-i:0014 */}
[Weisstein, Eric W. "Modified Bessel Function of the First Kind."](https://mathworld.wolfram.com/ModifiedBesselFunctionoftheFirstKind.html) From MathWorld — A Wolfram Web Resource.
