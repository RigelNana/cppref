---
title: "std::chrono::operator==(std::chrono::month_weekday_last)"
description: "Compares the two month_weekday_last values x and y."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_weekday_last/operator_cmp"
language: "C++"
---

{/* source:cpp-chrono-month-weekday-last-operator-cmp:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`constexpr bool operator==( const std::chrono::month_weekday_last& x,
                           const std::chrono::month_weekday_last& y ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-weekday-last-operator-cmp:0001 */}
Compares the two `month_weekday_last` values `x` and `y`.

{/* source:cpp-chrono-month-weekday-last-operator-cmp:0002 */}
The `!=` operator is [synthesized](/docs/cpp/language/expressions/default_comparisons#Other_defaulted_comparison_operators) from `operator==`.

## Return value

{/* source:cpp-chrono-month-weekday-last-operator-cmp:0003 */}
`x.month() == y.month() && x.weekday_last() == y.weekday_last()`

## Example

{/* source:cpp-chrono-month-weekday-last-operator-cmp:0004 */}
```cpp
#include <chrono>

int main()
{
    constexpr std::chrono::month_weekday_last mwdl1
    {
        std::chrono::March/std::chrono::Friday[std::chrono::last]
    };

    constexpr std::chrono::month_weekday_last mwdl2
    {
        std::chrono::March, std::chrono::Friday[std::chrono::last]
    };

    static_assert(mwdl1 == mwdl2);
}
```
