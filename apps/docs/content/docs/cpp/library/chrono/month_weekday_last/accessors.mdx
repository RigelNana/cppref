---
title: "std::chrono::month_weekday_last::month, std::chrono::month_weekday_last::weekday_last"
description: "Retrieves a copy of the month and weekday_last objects stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_weekday_last/accessors"
language: "C++"
---

{/* source:cpp-chrono-month-weekday-last-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::weekday_last weekday_last() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-weekday-last-accessors:0001 */}
Retrieves a copy of the [`month`](/docs/cpp/library/chrono/month) and [`weekday_last`](/docs/cpp/library/chrono/weekday_last) objects stored in `*this`.

## Return value

{/* source:cpp-chrono-month-weekday-last-accessors:0002 */}
1. A copy of the `std::chrono::month` object stored in `*this`.

{/* source:cpp-chrono-month-weekday-last-accessors:0003 */}
2. A copy of the `std::chrono::weekday_last` object stored in `*this`.

## Example

{/* source:cpp-chrono-month-weekday-last-accessors:0004 */}
```cpp
#include <chrono>
#include <iostream>
using namespace std::chrono;

int main()
{
    std::cout << std::boolalpha;

    auto mwdl{March/Friday[last]}; // Last Friday in a March
    auto ywdl{year(2024)/mwdl};
    std::cout << (year_month_day{ywdl} ==
                  year_month_day{March/29/2024}) << ' ';
    // Last Friday of the next month, in 2024
    mwdl = {(mwdl.month() + months(1))/mwdl.weekday_last()};
    ywdl = {year(2024)/mwdl}; 
    std::cout << (year_month_day{ywdl} ==
                  year_month_day{April/26/2024}) << '\n';
}
```

Output:

```text
true true
```
