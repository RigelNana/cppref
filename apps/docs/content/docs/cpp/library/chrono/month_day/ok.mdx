---
title: "std::chrono::month_day::ok"
description: "Determines whether this month_day stores a valid month-day combination."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_day/ok"
language: "C++"
---

{/* source:cpp-chrono-month-day-ok:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr bool ok() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-day-ok:0001 */}
Determines whether this `month_day` stores a valid month-day combination.

{/* source:cpp-chrono-month-day-ok:0002 */}
The combination is valid if `month()` represents a valid month (`month().ok() == true`), `unsigned{day()} >= 1`, and `unsigned{day()} <= D`, where D is the number of days in the month represented by `month()`. The number of days in February is considered to be 29.

## Return value

{/* source:cpp-chrono-month-day-ok:0003 */}
`true` if the month and day combination is valid, otherwise false.

## Example

{/* source:cpp-chrono-month-day-ok:0004 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << std::boolalpha;

    constexpr auto md1{std::chrono::July/15};
    std::cout << (md1.ok()) << ' ';
    constexpr std::chrono::month_day md2{std::chrono::month(14), std::chrono::day(42)};
    std::cout << (md2.ok()) << ' ';
    constexpr auto md3{std::chrono::February/29};
    std::cout << (md3.ok()) << '\n';
}
```

Output:

```text
true false true
```
