---
title: "std::chrono::month_day::month_day"
description: "Constructs a month_day."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_day/month_day"
language: "C++"
---

{/* source:cpp-chrono-month-day-month-day:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`month_day() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr month_day( const std::chrono::month& m,
             const std::chrono::day& d ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-day-month-day:0001 */}
Constructs a `month_day`.

{/* source:cpp-chrono-month-day-month-day:0002 */}
1) Default constructor leaves the stored month and day values uninitialized.

{/* source:cpp-chrono-month-day-month-day:0003 */}
2) Constructs a `month_day` that stores the month `m` and the day `d`.

## Notes

{/* source:cpp-chrono-month-day-month-day:0004 */}
A more convenient way to construct a `month_day` is with `operator/`, e.g., `std::chrono::April/1`.

## Example

{/* source:cpp-chrono-month-day-month-day:0005 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    using namespace std::chrono_literals;
    constexpr auto mo_da = std::chrono::month_day(std::chrono::February, 29d);
    std::cout << static_cast<unsigned>(mo_da.day()) << '/' 
              << static_cast<unsigned>(mo_da.month()) << '\n';
}
```

Output:

```text
29/2
```

## See also

{/* source:cpp-chrono-month-day-month-day:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator/`](/docs/cpp/library/chrono/operator_slash) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>conventional syntax for Gregorian calendar date creation (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
