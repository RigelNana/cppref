---
title: "std::chrono::month_day::month, std::chrono::month_day::day"
description: "Retrieves the month and day values stored in this month_day object."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_day/accessors"
language: "C++"
---

{/* source:cpp-chrono-month-day-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::day day() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-day-accessors:0001 */}
Retrieves the month and day values stored in this `month_day` object.

## Return value

{/* source:cpp-chrono-month-day-accessors:0002 */}
1. Returns the stored `std::chrono::month` value.
{/* source:cpp-chrono-month-day-accessors:0003 */}
2. Returns the stored `std::chrono::day` value.

## Example

{/* source:cpp-chrono-month-day-accessors:0004 */}
```cpp
#include <chrono>

int main()
{
    constexpr auto md{std::chrono::July/15};  
    static_assert(md.month() == std::chrono::month(7));
    static_assert(md.day() == std::chrono::day(15));
}
```
