---
title: "std::chrono::year_month_day_last::year, std::chrono::year_month_day_last::month, std::chrono::year_month_day_last::day, std::chrono::year_month_day_last::month_day_last"
description: "Retrieves the field values stored in this year_month_day_last object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_day_last/accessors"
language: "C++"
---

{/* source:cpp-chrono-year-month-day-last-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year year() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::day day() const noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::month_day_last month_day_last() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-day-last-accessors:0001 */}
Retrieves the field values stored in this `year_month_day_last` object.

## Return value

{/* source:cpp-chrono-year-month-day-last-accessors:0002 */}
1. Returns the stored `std::chrono::year` value.

{/* source:cpp-chrono-year-month-day-last-accessors:0003 */}
2. Returns the stored `std::chrono::month` value.

{/* source:cpp-chrono-year-month-day-last-accessors:0004 */}
3. A `std::chrono::day` value corresponding to the last day of the stored year and month. If `this->ok()` is false, the return value is unspecified.

{/* source:cpp-chrono-year-month-day-last-accessors:0005 */}
4. A `std::chrono::month_day_last` constructed from `month()`.

## Example

{/* source:cpp-chrono-year-month-day-last-accessors:0006 */}
```cpp
#include <chrono>

int main()
{
    constexpr auto ymdl{std::chrono::last/11/2020};
    static_assert
    (
        ymdl.day() == std::chrono::day(30) &&
        ymdl.month() == std::chrono::November &&
        ymdl.year() == std::chrono::year(2020) &&
        ymdl.month_day_last() == std::chrono::November/std::chrono::last
    );
}
```
