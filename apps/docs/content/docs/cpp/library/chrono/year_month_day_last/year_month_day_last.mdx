---
title: "std::chrono::year_month_day_last::year_month_day_last"
description: "Constructs a year_month_day_last object storing the year y and the month mdl.month(). The constructed object represents the last day of that year and month."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_day_last/year_month_day_last"
language: "C++"
---

{/* source:cpp-chrono-year-month-day-last-year-month-day-last:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr year_month_day_last( const std::chrono::year& y,
    const std::chrono::month_day_last& mdl ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-day-last-year-month-day-last:0001 */}
Constructs a `year_month_day_last` object storing the year `y` and the month `mdl.month()`. The constructed object represents the last day of that year and month.

## Notes

{/* source:cpp-chrono-year-month-day-last-year-month-day-last:0002 */}
A `year_month_day_last` can also be created by combining a `std::chrono::year_month` with `std::chrono::last` using `operator/`.

## Example

{/* source:cpp-chrono-year-month-day-last-year-month-day-last:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << "All last days of months of a given year:\n";

    constexpr std::chrono::year year{2021};

    for (int i{1}; i <= 12; ++i)
    {
        const std::chrono::month_day_last mdl{std::chrono::month(i) / std::chrono::last};
        const std::chrono::year_month_day_last ymdl{year, mdl};
        std::cout << ymdl << " day: " << ymdl.day() << '\n';
    }
}
```

Possible output:

```text
All last days of months of a given year: 
2021/Jan/last day: 31
2021/Feb/last day: 28
2021/Mar/last day: 31
2021/Apr/last day: 30
2021/May/last day: 31
2021/Jun/last day: 30
2021/Jul/last day: 31
2021/Aug/last day: 31
2021/Sep/last day: 30
2021/Oct/last day: 31
2021/Nov/last day: 30
2021/Dec/last day: 31
```

## See also

{/* source:cpp-chrono-year-month-day-last-year-month-day-last:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator/`](/docs/cpp/library/chrono/operator_slash) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>conventional syntax for Gregorian calendar date creation (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
