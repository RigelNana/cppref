---
title: "std::chrono::choose"
description: "The scoped enumeration choose can be passed to certain member functions of std::chrono::time_zone and std::chrono::zoned_time to control how ambiguous or nonexistent local times should be resolved."
source_url: "https://en.cppreference.com/w/cpp/chrono/choose"
language: "C++"
---

{/* source:cpp-chrono-choose:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`enum class choose {
    earliest,
    latest

};`} />
</DeclarationDoc>

{/* source:cpp-chrono-choose:0001 */}
The scoped enumeration `choose` can be passed to certain member functions of `std::chrono::time_zone` and `std::chrono::zoned_time` to control how ambiguous or nonexistent local times should be resolved. Passing `choose::earliest` causes the earlier time point to be returned, while passing `choose::latest` causes the later time point to be returned. (For nonexistent local times, these two time points are identical.)

{/* source:cpp-chrono-choose:0002 */}
If a `choose` is not passed and an ambiguous or nonexistent local time is encountered, a `std::chrono::ambiguous_local_time` or `std::chrono::nonexistent_local_time` exception (as applicable) will be thrown.

## See also

{/* source:cpp-chrono-choose:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/chrono/zoned_time/zoned_time)</DescriptionTerm>
    <DescriptionBody>constructs a `zoned_time` (public member function of `std::chrono::zoned_time<Duration,TimeZonePtr>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`to_sys`](/docs/cpp/library/chrono/time_zone/to_sys)</DescriptionTerm>
    <DescriptionBody>converts a `local_time` in this time zone to a `sys_time` (public member function of `std::chrono::time_zone`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
