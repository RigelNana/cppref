---
title: "std::chrono::operator<<(std::chrono::weekday_last)"
description: "Outputs a textual representation of wdl into the stream os, as if by"
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday_last/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-weekday-last-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>&

    operator<<( std::basic_ostream<CharT, Traits>& os, const std::chrono::weekday_last& wdl );`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-last-operator-ltlt:0001 */}
Outputs a textual representation of `wdl` into the stream `os`, as if by

{/* source:cpp-chrono-weekday-last-operator-ltlt:0002 */}
`os << std::format(os.getloc(), STATICALLY_WIDEN<CharT>("{:L}[last]"), wdl.weekday());`

{/* source:cpp-chrono-weekday-last-operator-ltlt:0003 */}
where `STATICALLY_WIDEN<CharT>("{:L}[last]")` is `"{:L}[last]"` if `CharT` is `char`, and `L"{:L}[last]"` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-weekday-last-operator-ltlt:0004 */}
`os`

## Example

{/* source:cpp-chrono-weekday-last-operator-ltlt:0005 */}
This section is incomplete
Reason: no example

## Defect reports

{/* source:cpp-chrono-weekday-last-operator-ltlt:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-chrono-weekday-last-operator-ltlt:0007 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2372R3" standard="C++20">
    <PublishedBehavior>the given locale was used by default</PublishedBehavior>
    <CorrectedBehavior>`L` is needed to use the given locale</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

## See also

{/* source:cpp-chrono-weekday-last-operator-ltlt:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::formatter<std::chrono::weekday>`](/docs/cpp/library/chrono/weekday/formatter) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>formatting support for weekday (class template specialization)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
