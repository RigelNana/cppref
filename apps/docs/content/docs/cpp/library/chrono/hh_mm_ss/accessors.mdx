---
title: "std::chrono::hh_mm_ss<Duration>::is_negative, std::chrono::hh_mm_ss<Duration>::hours, std::chrono::hh_mm_ss<Duration>::minutes, std::chrono::hh_mm_ss<Duration>::seconds, std::chrono::hh_mm_ss<Duration>::subseconds"
description: "Obtains the components of the stored \"broken down\" time."
source_url: "https://en.cppreference.com/w/cpp/chrono/hh_mm_ss/accessors"
language: "C++"
---

{/* source:cpp-chrono-hh-mm-ss-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" code={`constexpr bool is_negative() const noexcept;`} />
  <Declaration id="2" language="cpp" code={`constexpr std::chrono::hours hours() const noexcept;`} />
  <Declaration id="3" language="cpp" code={`constexpr std::chrono::minutes minutes() const noexcept;`} />
  <Declaration id="4" language="cpp" code={`constexpr std::chrono::seconds seconds() const noexcept;`} />
  <Declaration id="5" language="cpp" code={`constexpr precision subseconds() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-hh-mm-ss-accessors:0001 */}
Obtains the components of the stored "broken down" time.

## Return value

{/* source:cpp-chrono-hh-mm-ss-accessors:0002 */}
Let `d` be the represented duration:

{/* source:cpp-chrono-hh-mm-ss-accessors:0003 */}
1) `true` if `d` is negative, `false` otherwise.

{/* source:cpp-chrono-hh-mm-ss-accessors:0004 */}
2) `std::chrono::duration_cast<std::chrono::hours>(abs(d))`

{/* source:cpp-chrono-hh-mm-ss-accessors:0005 */}
3) `std::chrono::duration_cast<std::chrono::minutes>(abs(d) - hours())`

{/* source:cpp-chrono-hh-mm-ss-accessors:0006 */}
4) `std::chrono::duration_cast<std::chrono::seconds>(abs(d) - hours() - minutes())`

{/* source:cpp-chrono-hh-mm-ss-accessors:0007 */}
5) `abs(d) - hours() - minutes() - seconds()` if `std::chrono::treat_as_floating_point_v<precision::rep>` is `true`; otherwise `std::chrono::duration_cast<precision>(abs(d) - hours() - minutes() - seconds())`.

## Example

{/* source:cpp-chrono-hh-mm-ss-accessors:0008 */}
This section is incomplete

Reason: no example
