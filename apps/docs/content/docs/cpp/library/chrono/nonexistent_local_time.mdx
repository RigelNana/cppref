---
title: "std::chrono::nonexistent_local_time"
description: "Defines a type of object to be thrown as exception to report that an attempt was made to convert a nonexistent std::chrono::local_time to a std::chrono::sys_time without specifying a std::chrono::choose (such as choose::earliest or choose::latest)."
source_url: "https://en.cppreference.com/w/cpp/chrono/nonexistent_local_time"
language: "C++"
---

{/* source:cpp-chrono-nonexistent-local-time:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`class nonexistent_local_time;`} />
</DeclarationDoc>

{/* source:cpp-chrono-nonexistent-local-time:0001 */}
Defines a type of object to be thrown as exception to report that an attempt was made to convert a nonexistent `std::chrono::local_time` to a `std::chrono::sys_time` without specifying a `std::chrono::choose` (such as `choose::earliest` or `choose::latest`).

{/* source:cpp-chrono-nonexistent-local-time:0002 */}
This exception is thrown by `std::chrono::time_zone::to_sys` and functions that call it (such as the constructors of `std::chrono::zoned_time` that take a `std::chrono::local_time`).

{/* source:cpp-chrono-nonexistent-local-time:0003 */}
Inheritance diagram

## Member functions

{/* source:cpp-chrono-nonexistent-local-time:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`(constructor)`</DescriptionTerm>
    <DescriptionBody>constructs the exception object (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator=`</DescriptionTerm>
    <DescriptionBody>replaces the exception object (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`what`</DescriptionTerm>
    <DescriptionBody>returns the explanatory string (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-chrono-nonexistent-local-time:0005 */}
#### `std::chrono::nonexistent_local_time::nonexistent_local_time`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`template< class Duration >
nonexistent_local_time( const std::chrono::local_time<Duration>& tp,

                        const std::chrono::local_info& i );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`nonexistent_local_time( const nonexistent_local_time& other ) noexcept;`} />
</DeclarationDoc>

Constructs the exception object.

1. The explanatory string returned by `what()` is equivalent to that produced by `os.str()` after the following code:

```cpp
std::ostringstream os;
os << tp << " is in a gap between\n"
   << std::chrono::local_seconds(i.first.end.time_since_epoch()) + i.first.offset
   << ' ' << i.first.abbrev << " and\n"
   << std::chrono::local_seconds(i.second.begin.time_since_epoch()) + i.second.offset
   << ' ' << i.second.abbrev
   << " which are both equivalent to\n"
   << i.first.end << " UTC";
```

The behavior is undefined if `i.result != std::chrono::local_info::nonexistent`.

2. Copy constructor. If `*this` and `other` both have dynamic type `std::chrono::nonexistent_local_time` then `std::strcmp(what(), other.what()) == 0`.

##### Parameters

<ParameterList>
  <Parameter name="tp">the time point for which conversion was attempted</Parameter>
  <Parameter name="i">a `std::chrono::local_info` describing the result of the conversion attempt</Parameter>
  <Parameter name="other">another `nonexistent_local_time` to copy</Parameter>
</ParameterList>

##### Exceptions

May throw [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc)

##### Notes

Because copying a standard library class derived from `std::exception` is not permitted to throw exceptions, this message is typically stored internally as a separately-allocated reference-counted string.

{/* source:cpp-chrono-nonexistent-local-time:0006 */}
#### `std::chrono::nonexistent_locale_time::operator=`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`nonexistent_locale_time& operator=( const nonexistent_locale_time& other ) noexcept;`} />
</DeclarationDoc>

Assigns the contents with those of `other`.If `*this` and `other` both have dynamic type `std::chrono::nonexistent_locale_time` then `std::strcmp(what(), other.what()) == 0` after assignment.

##### Parameters

<ParameterList>
  <Parameter name="other">another exception object to assign with</Parameter>
</ParameterList>

##### Return value

`*this`

{/* source:cpp-chrono-nonexistent-local-time:0007 */}
#### `std::chrono::nonexistent_locale_time::what`

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`virtual const char* what() const noexcept;`} />
</DeclarationDoc>

Returns the explanatory string.

##### Return value

Pointer to an implementation-defined null-terminated string with explanatory information. The string is suitable for conversion and display as a [`std::wstring`](/docs/cpp/library/string/basic_string). The pointer is guaranteed to be valid at least until the exception object from which it is obtained is destroyed, or until a non-const member function (e.g. copy assignment operator) on the exception object is called.

<Revision since="C++26">
The returned string is encoded with the ordinary literal encoding during constant evaluation.
</Revision>

##### Notes

Implementations are allowed but not required to override `what()`.

{/* source:cpp-chrono-nonexistent-local-time:0008 */}
#### Inherited from [`std::runtime_error`](/docs/cpp/library/error/runtime_error)

{/* source:cpp-chrono-nonexistent-local-time:0009 */}
#### Inherited from [`std::exception`](/docs/cpp/library/error/exception)

##### Member functions

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(destructor)`](/docs/cpp/library/error/exception/~exception) [virtual]</DescriptionTerm>
    <DescriptionBody>destroys the exception object (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`what`](/docs/cpp/library/error/exception/what) [virtual]</DescriptionTerm>
    <DescriptionBody>returns an explanatory string (virtual public member function of `std::exception`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

## See also

{/* source:cpp-chrono-nonexistent-local-time:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ambiguous_local_time`](/docs/cpp/library/chrono/ambiguous_local_time) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>exception thrown to report that a local time is ambiguous (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
