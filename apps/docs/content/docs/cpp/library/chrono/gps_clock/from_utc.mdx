---
title: "std::chrono::gps_clock::from_utc"
description: "Converts the utc_time t to a gps_time representing the same point in time."
source_url: "https://en.cppreference.com/w/cpp/chrono/gps_clock/from_utc"
language: "C++"
---

{/* source:cpp-chrono-gps-clock-from-utc:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class Duration >
static std::chrono::gps_time<std::common_type_t<Duration, std::chrono::seconds>>

    from_utc( const std::chrono::utc_time<Duration>& ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-gps-clock-from-utc:0001 */}
Converts the `utc_time` `t` to a `gps_time` representing the same point in time.

## Return value

{/* source:cpp-chrono-gps-clock-from-utc:0002 */}
A `std::chrono::gps_time` representing the same point in time as `t`, computed as if by constructing a value of the return type from `t.time_since_epoch()` and subtracting `315964809s` (315964809 is the number of seconds between the epochs of the two clocks: 1980-01-06 00:00:00 UTC for `gps_clock` and 1970-01-01 00:00:00 UTC for `utc_clock`).

## Example

{/* source:cpp-chrono-gps-clock-from-utc:0003 */}
This section is incomplete Reason: no example

## See also

{/* source:cpp-chrono-gps-clock-from-utc:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`to_utc`](/docs/cpp/library/chrono/gps_clock/to_utc)</DescriptionTerm>
    <DescriptionBody>converts a `gps_time` to `utc_time` (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`clock_cast`](/docs/cpp/library/chrono/clock_cast) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>convert time points of one clock to another (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
