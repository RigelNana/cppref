---
title: "std::chrono::year::max"
description: "Returns the largest possible year, that is, std::chrono::year(32767)."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/max"
language: "C++"
---

{/* source:cpp-chrono-year-max:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`static constexpr year max() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-max:0001 */}
Returns the largest possible `year`, that is, `std::chrono::year(32767)`.

## Return value

{/* source:cpp-chrono-year-max:0002 */}
`std::chrono::year(32767)`

## Example

{/* source:cpp-chrono-year-max:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << "The maximum year is: " << (int)std::chrono::year::max() << '\n';
}
```

Output:

```text
The maximum year is: 32767
```
