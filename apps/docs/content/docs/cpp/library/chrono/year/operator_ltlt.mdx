---
title: "std::chrono::operator<<(std::chrono::year)"
description: "Forms a std::basic_string<CharT> s consisting of the year value stored in y formatted as a decimal number, left-padded with 0 to four digits if the result would otherwise be less than four digits."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-year-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>&

    operator<<( std::basic_ostream<CharT, Traits>& os, const std::chrono::year& y );`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-operator-ltlt:0001 */}
Forms a `std::basic_string<CharT>` `s` consisting of the year value stored in `y` formatted as a decimal number, left-padded with `0` to four digits if the result would otherwise be less than four digits. Then, if `!y.ok()`, append `" is not a valid year"` to the formatted string. Inserts that string into `os`.

{/* source:cpp-chrono-year-operator-ltlt:0002 */}
Equivalent to

{/* source:cpp-chrono-year-operator-ltlt:0003 */}
```cpp
return os << (y.ok() ?
    std::format(STATICALLY_WIDEN<CharT>("{:%Y}"), y) :
    std::format(STATICALLY_WIDEN<CharT>("{:%Y} is not a valid year"), y));
```

{/* source:cpp-chrono-year-operator-ltlt:0004 */}
where `STATICALLY_WIDEN<CharT>("...")` is `"..."` if `CharT` is `char`, and `L"..."` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-year-operator-ltlt:0005 */}
`os`

## Example

{/* source:cpp-chrono-year-operator-ltlt:0006 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    constexpr std::chrono::year y1{2020}, y2{-020}, y3{98304};
    std::cout << y1 << '\n'
              << y2 << '\n'
              << y3 << '\n';
}
```

Possible output:

```text
2020
-0016
-32768 is not a valid year
```

## See also

{/* source:cpp-chrono-year-operator-ltlt:0007 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm><DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`std::formatter<std::chrono::year>`](/docs/cpp/library/chrono/year/formatter) <InlineRevision since="C++20" /></DescriptionTerm><DescriptionBody>formatting support for `year` (class template specialization)</DescriptionBody></DescriptionItem>
</DescriptionList>
