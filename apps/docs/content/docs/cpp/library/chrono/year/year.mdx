---
title: "std::chrono::year::year"
description: "Constructs a year object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/year"
language: "C++"
---

{/* source:cpp-chrono-year-year:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`year() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr explicit year( int y ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-year:0001 */}
Constructs a `year` object.

{/* source:cpp-chrono-year-year:0002 */}
1) Default constructor leaves the `year` value uninitialized.

{/* source:cpp-chrono-year-year:0003 */}
2) If `y` is in the range `[-32767, 32767]`, constructs a `year` object holding the year value `y`. Otherwise the value held is unspecified.

## Example

{/* source:cpp-chrono-year-year:0004 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    using namespace std::chrono;

    constexpr int leap_years = []
    {
        int count{};
        for (int i{year::min()}; i <= int{year::max()}; ++i)
            if (year{i}.is_leap()) // uses constructor (2)
                ++count;
        return count;
    } ();

    static_assert(15891 == leap_years);

    std::cout << "There are " << leap_years << " leap years in the range ["
              << int(year::min()) << ", " << int(year::max()) << "].\n";
}
```

Output:

```text
There are 15891 leap years in the range [-32767, 32767].
```
