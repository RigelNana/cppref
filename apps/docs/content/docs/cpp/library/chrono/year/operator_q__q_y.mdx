---
title: "std::literals::chrono_literals::operator\"\"y"
description: "Forms a std::chrono::year literal representing a year in the proleptic Gregorian calendar."
source_url: "https://en.cppreference.com/w/cpp/chrono/operator_q__q_y"
language: "C++"
---

{/* source:cpp-chrono-operator-q-q-y:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year operator""y( unsigned long long y ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-operator-q-q-y:0001 */}
Forms a `std::chrono::year` literal representing a year in the [proleptic Gregorian calendar](https://en.wikipedia.org/wiki/proleptic_Gregorian_calendar).

## Parameters

{/* source:cpp-chrono-operator-q-q-y:0002 */}
<ParameterList>
  <Parameter name="y">the year value</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-operator-q-q-y:0003 */}
A `std::chrono::year` initialized from `int(y)`. If `y` is not in the range [`-32767`, `32767`], the stored value is unspecified.

## Possible implementation

{/* source:cpp-chrono-operator-q-q-y:0004 */}
```cpp
constexpr std::chrono::year operator""y(unsigned long long y) noexcept
{
    return std::chrono::year(static_cast<int>(y));
}
```

## Notes

{/* source:cpp-chrono-operator-q-q-y:0005 */}
This operator is declared in the namespace `std::literals::chrono_literals`, where both `literals` and `chrono_literals` are [inline namespaces](/docs/cpp/language/declarations/namespace#Inline_namespaces). Access to this operator can be gained with:

{/* source:cpp-chrono-operator-q-q-y:0006 */}
- `using namespace std::literals`,
- `using namespace std::chrono_literals`, or
- `using namespace std::literals::chrono_literals`.

{/* source:cpp-chrono-operator-q-q-y:0007 */}
In addition, within the namespace `std::chrono`, the directive `using namespace literals::chrono_literals;` is provided by the [standard library](/docs/cpp/library/standard_library), so that if a programmer uses `using namespace std::chrono;` to gain access to the classes in the [chrono library](/docs/cpp/library/chrono), the corresponding literal operators become visible as well.

## Example

{/* source:cpp-chrono-operator-q-q-y:0008 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    using namespace std::literals;

    std::cout << int(2020y)  << '\t' << 2020y  << '\n'
              << int(-220y)  << '\t' << -220y  << '\n'
              << int(3000y)  << '\t' << 3000y  << '\n'
              << int(32768y) << '\t' << 32768y << '\n'  // unspecified
              << int(65578y) << '\t' << 65578y << '\n'; // unspecified
}
```

Possible output:

```text
2020	2020
-220	-0220
3000	3000
-32768	-32768 is not a valid year
42	0042
```

## See also

{/* source:cpp-chrono-operator-q-q-y:0009 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`(constructor)`](/docs/cpp/library/chrono/year/year)</DescriptionTerm><DescriptionBody>constructs a year (public member function of std::chrono::year)</DescriptionBody></DescriptionItem>
</DescriptionList>
