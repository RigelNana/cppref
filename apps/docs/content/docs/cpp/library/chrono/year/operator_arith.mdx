---
title: "std::chrono::year::operator+=, std::chrono::year::operator-="
description: "Adds or subtracts y.count() years from the year value."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/operator_arith"
language: "C++"
---

{/* source:cpp-chrono-year-operator-arith:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year& operator+=( const std::chrono::years& y ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::year& operator-=( const std::chrono::years& y ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-operator-arith:0001 */}
Adds or subtracts `y.count()` years from the year value.

{/* source:cpp-chrono-year-operator-arith:0002 */}
1. Equivalent to `*this = *this + y;`.

{/* source:cpp-chrono-year-operator-arith:0003 */}
2. Equivalent to `*this = *this - y;`.

## Return value

{/* source:cpp-chrono-year-operator-arith:0004 */}
A reference to this year after modification.

## Notes

{/* source:cpp-chrono-year-operator-arith:0005 */}
If the result would be outside the range `[-32767, 32767]`, the actual stored value is unspecified.

## Example

{/* source:cpp-chrono-year-operator-arith:0006 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    using namespace std::literals::chrono_literals;
    std::cout << std::boolalpha;

    std::chrono::year y{2020};

    y += std::chrono::years(12);
    std::cout << (y == 2032y) << ' ';

    y -= std::chrono::years(33);
    std::cout << (y == 1999y) << '\n';
}
```

Output:

```text
true true
```

## See also

{/* source:cpp-chrono-year-operator-arith:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++`, `operator++(int)`, `operator--`, `operator--(int)`](/docs/cpp/library/chrono/year/operator_inc_dec)</DescriptionTerm>
    <DescriptionBody>increments or decrements the year (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`, `operator-`](/docs/cpp/library/chrono/year/operator_arith_2) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs arithmetic on years (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
