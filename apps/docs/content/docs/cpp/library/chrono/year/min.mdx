---
title: "std::chrono::year::min"
description: "Returns the smallest possible year, that is, std::chrono::year(-32767)."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/min"
language: "C++"
---

{/* source:cpp-chrono-year-min:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`static constexpr std::chrono::year min() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-min:0001 */}
Returns the smallest possible `year`, that is, `std::chrono::year(-32767)`.

## Return value

{/* source:cpp-chrono-year-min:0002 */}
`std::chrono::year(-32767)`

## Example

{/* source:cpp-chrono-year-min:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << "The minimum year is: " << (int)std::chrono::year::min() << '\n';
}
```

Output:

```text
The minimum year is: -32767
```
