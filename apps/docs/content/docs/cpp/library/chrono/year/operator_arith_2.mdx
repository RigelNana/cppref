---
title: "std::chrono::operator+, std::chrono::operator- (std::chrono::year)"
description: "Implements addition and subtraction of years with std::chrono::year."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/operator_arith_2"
language: "C++"
---

{/* source:cpp-chrono-year-operator-arith-2:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year operator+( const std::chrono::year& y,
                               const std::chrono::years& ys ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::year operator+( const std::chrono::years& ys,
                               const std::chrono::year& y ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::year operator-( const std::chrono::year& y,
                               const std::chrono::years& ys ) noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::years operator-( const std::chrono::year& y1,
                                const std::chrono::year& y2 ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-operator-arith-2:0001 */}
1,2) Adds `ys.count()` years to `y`.

{/* source:cpp-chrono-year-operator-arith-2:0002 */}
3) Subtracts `ys.count()` years from `y`.

{/* source:cpp-chrono-year-operator-arith-2:0003 */}
4) Returns the difference in years between `y1` and `y2`.

## Return value

{/* source:cpp-chrono-year-operator-arith-2:0004 */}
1,2) `std::chrono::year(int(y) + ys.count())`

{/* source:cpp-chrono-year-operator-arith-2:0005 */}
3) `std::chrono::year(int(y) - ys.count())`

{/* source:cpp-chrono-year-operator-arith-2:0006 */}
4) `std::chrono::years(int(y1) - int(y2))`

## Notes

{/* source:cpp-chrono-year-operator-arith-2:0007 */}
If the resulting year value for (1-3) is outside the range [-32767, 32767], the actual value stored is unspecified.

{/* source:cpp-chrono-year-operator-arith-2:0008 */}
The result of subtracting two `year` values is a duration of type `std::chrono::years`. This duration unit represents the length of the average Gregorian year, and the resulting duration bears no relationship to the number of days in the particular years represented by the operands. For example, the result of `2018y - 2017y` is `std::chrono::years(1)`, which represents 365.2425 days, not 365 days.

## Example

{/* source:cpp-chrono-year-operator-arith-2:0009 */}
```cpp
#include <cassert>
#include <chrono>

int main()
{
    std::chrono::year y{2020};

    y = std::chrono::years(12) + y; // overload (2): duration + time point
    assert(y == std::chrono::year(2032));

    y = y - std::chrono::years(33); // overload (3): time point - duration
    assert(y == std::chrono::year(1999));

    // y = std::chrono::years(33) - y; // not supported: duration - time point

    using namespace std::chrono;
    constexpr std::chrono::years ys = 2025y - 2020y; // overload (4)
    static_assert(ys == std::chrono::years(5));
}
```

## See also

{/* source:cpp-chrono-year-operator-arith-2:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++`, `operator++(int)`, `operator--`, `operator--(int)`](/docs/cpp/library/chrono/month/operator_inc_dec)</DescriptionTerm>
    <DescriptionBody>increments or decrements the month (public member function of `std::chrono::month`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+=`, `operator-=`](/docs/cpp/library/chrono/month/operator_arith)</DescriptionTerm>
    <DescriptionBody>adds or subtracts a number of months (public member function of `std::chrono::month`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
