---
title: "std::chrono::year::operator int"
description: "Returns the year value stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/year/operator_int"
language: "C++"
---

{/* source:cpp-chrono-year-operator-int:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr explicit operator int() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-operator-int:0001 */}
Returns the year value stored in `*this`.

## Return value

{/* source:cpp-chrono-year-operator-int:0002 */}
The year value stored in `*this`.

## Example

{/* source:cpp-chrono-year-operator-int:0003 */}
```cpp
#include <chrono>
#include <iostream>
using namespace std::chrono;

int main()
{
    constexpr std::chrono::year y{2020};
    std::cout << "The year is: " << static_cast<int>(y) << '\n';

    const year_month_day ymd{floor<days>(system_clock::now())};
    const std::chrono::year this_year{ymd.year()};
    std::cout << "This year is: " << int(this_year) << '\n';
}
```

Possible output:

```text
The year is: 2020
This year is: 2023
```
