---
title: "std::chrono::year_month_weekday::year_month_weekday"
description: "Constructs a year_month_weekday object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_weekday/year_month_weekday"
language: "C++"
---

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`year_month_weekday() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr year_month_weekday( const std::chrono::year& y,
                              const std::chrono::month& m,

                              const std::chrono::weekday_indexed& wdi ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr year_month_weekday( const std::chrono::sys_days& dp ) noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr explicit year_month_weekday( const std::chrono::local_days& dp ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0001 */}
Constructs a `year_month_weekday` object.

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0002 */}
1) Default constructor leaves the fields uninitialized.

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0003 */}
2) Constructs a `year_month_weekday` object storing the year `y`, the month `m`, the weekday `wdi.weekday()` and the weekday index `wdi.index()`.

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0004 */}
3) Constructs a `year_month_weekday` object corresponding to the date represented by `dp`. For any `year_month_weekday` object storing a valid date, converting it to `sys_days` and back yields the same value. This constructor defines an implicit conversion from `sys_days` to `year_month_weekday`.

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0005 */}
4) Constructs a `year_month_weekday` object corresponding to the date represented by `dp`. Equivalent to `year_month_weekday(sys_days(dp.time_since_epoch()))`.

## Notes

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0006 */}
A `year_month_weekday` can also be created by combining one of the partial-date types `std::chrono::year_month` and `std::chrono::month_weekday` with the missing component (indexed weekday and year, respectively) using `operator/`.

## See also

{/* source:cpp-chrono-year-month-weekday-year-month-weekday:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator/`](/docs/cpp/library/chrono/operator_slash) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>conventional syntax for Gregorian calendar date creation (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
