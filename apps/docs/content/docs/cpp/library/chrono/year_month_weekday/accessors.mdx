---
title: "std::chrono::year_month_weekday::year, std::chrono::year_month_weekday::month, std::chrono::year_month_weekday::weekday, std::chrono::year_month_weekday::index, std::chrono::year_month_weekday::weekday_indexed"
description: "Retrieves the field values stored in this year_month_weekday object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_weekday/accessors"
language: "C++"
---

{/* source:cpp-chrono-year-month-weekday-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year year() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::weekday weekday() const noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr unsigned index() const noexcept;`} />
  <Declaration id="5" language="cpp" since="C++20" code={`constexpr std::chrono::weekday_indexed weekday_indexed() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-weekday-accessors:0001 */}
Retrieves the field values stored in this `year_month_weekday` object.

## Return value

{/* source:cpp-chrono-year-month-weekday-accessors:0002 */}
1. Returns the stored `std::chrono::year` value.

{/* source:cpp-chrono-year-month-weekday-accessors:0003 */}
2. Returns the stored `std::chrono::month` value.

{/* source:cpp-chrono-year-month-weekday-accessors:0004 */}
3. Returns the stored `std::chrono::weekday` value.

{/* source:cpp-chrono-year-month-weekday-accessors:0005 */}
4. Returns the stored weekday index.

{/* source:cpp-chrono-year-month-weekday-accessors:0006 */}
5. `weekday()[index()]`

## Example

{/* source:cpp-chrono-year-month-weekday-accessors:0007 */}
```cpp
#include <cassert>
#include <chrono>

int main()
{
    constexpr auto ym{std::chrono::year(2021)/std::chrono::January};
    constexpr auto wdi{std::chrono::Wednesday[1]};
    auto ymwdi{ym/wdi};
    const auto index{ymwdi.index() + 1};
    auto weekday{ymwdi.weekday() + std::chrono::days(1)};

    ymwdi = {ymwdi.year()/ymwdi.month()/weekday[index]};
    // Second Thursday in January, 2021
    assert(std::chrono::year_month_day{ymwdi} == std::chrono::January/14/2021);
}
```
