---
title: "std::chrono::month_day_last::month"
description: "Retrieves a copy of the std::chrono::month object stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/month_day_last/month"
language: "C++"
---

{/* source:cpp-chrono-month-day-last-month:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-day-last-month:0001 */}
Retrieves a copy of the `std::chrono::month` object stored in `*this`.

## Return value

{/* source:cpp-chrono-month-day-last-month:0002 */}
A copy of the `std::chrono::month` object stored in `*this`.

## Example

{/* source:cpp-chrono-month-day-last-month:0003 */}
```cpp
#include <chrono>
#include <iostream>

using namespace std::chrono;

int main()
{
    std::cout << std::boolalpha;

    auto mdl{February/last}; // Last day of a February
    auto ymdl{year(2020) / mdl};
    std::cout << (year_month_day{ymdl} == year_month_day{February/29/2020}) << ' ';

    mdl = (mdl.month() + months(1)) / last; // Last day of the next month, in 2020
    ymdl = year(2020) / mdl;
    std::cout << (year_month_day{ymdl} == year_month_day{March/31/2020}) << '\n';
}
```

Output:

```text
true true
```
