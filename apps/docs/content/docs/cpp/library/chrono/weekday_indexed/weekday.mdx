---
title: "std::chrono::weekday_indexed::weekday"
description: "Retrieves a copy of the std::chrono::weekday object stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday_indexed/weekday"
language: "C++"
---

{/* source:cpp-chrono-weekday-indexed-weekday:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::weekday weekday() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-indexed-weekday:0001 */}
Retrieves a copy of the `std::chrono::weekday` object stored in `*this`.

## Return value

{/* source:cpp-chrono-weekday-indexed-weekday:0002 */}
A copy of the `std::chrono::weekday` object stored in `*this`.

## Example

{/* source:cpp-chrono-weekday-indexed-weekday:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << std::boolalpha;

    // 2nd Tuesday of a month
    std::chrono::weekday_indexed wdi{std::chrono::Tuesday[2]};
    std::cout << (std::chrono::year_month_day{wdi/10/2019} == 
                  std::chrono::year_month_day{std::chrono::October/8/2019}) << ' ';

    // 2nd Tuesday => 2nd Thursday
    wdi = {wdi.weekday() + std::chrono::days(2), wdi.index()};
    std::cout << (std::chrono::year_month_day{wdi/10/2019} == 
                  std::chrono::year_month_day{std::chrono::October/10/2019}) << '\n';
}
```

Output:

```text
true true
```

## See also

{/* source:cpp-chrono-weekday-indexed-weekday:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`index`](/docs/cpp/library/chrono/weekday_indexed)</DescriptionTerm>
    <DescriptionBody>access the stored index (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
