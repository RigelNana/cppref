---
title: "std::chrono::weekday_indexed::weekday_indexed"
description: "Constructs a weekday_indexed."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday_indexed/weekday_indexed"
language: "C++"
---

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`weekday_indexed() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr weekday_indexed( const std::chrono::weekday& wd, unsigned index ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0001 */}
Constructs a `weekday_indexed`.

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0002 */}
1. Default constructor leaves both the `std::chrono::weekday` and the index value uninitialized.

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0003 */}
2. Constructs a `weekday_indexed` storing the weekday `wd` and the index `index`. The values held are unspecified if `!wd.ok() || index > 7`.

## Notes

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0004 */}
A more convenient way to construct a `weekday_indexed` is with `weekday`'s `operator[]`, i.e., `wd[index]`.

## Example

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0005 */}
```cpp
#include <chrono>
#include <iostream>
using namespace std::chrono;

int main()
{
    constexpr auto third_friday = weekday_indexed(Friday, 3); // uses constructor (2)
    static_assert(third_friday == Friday[3]);

    weekday_indexed wdi = Tuesday[2]; // represents the 2nd Tuesday
    std::cout << year_month_day{ wdi / October / 2019y } << '\n';
}
```

Possible output:

```text
2019-10-08
```

## See also

{/* source:cpp-chrono-weekday-indexed-weekday-indexed:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator[]`](/docs/cpp/library/chrono/weekday/operator_at)</DescriptionTerm>
    <DescriptionBody>convenience syntax for constructing a [`weekday_indexed`](/docs/cpp/library/chrono/weekday_indexed) or [`weekday_last`](/docs/cpp/library/chrono/weekday_last) from this `weekday` (public member function of `std::chrono::weekday`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
