---
title: "std::chrono::weekday_indexed::ok"
description: "Checks if the weekday object and the index stored in *this are both valid."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday_indexed/ok"
language: "C++"
---

{/* source:cpp-chrono-weekday-indexed-ok:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr bool ok() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-indexed-ok:0001 */}
Checks if the weekday object and the index stored in `*this` are both valid.

## Return value

{/* source:cpp-chrono-weekday-indexed-ok:0002 */}
`true` if `weekday().ok() == true` and `index()` is in the range `[1, 5]`. Otherwise `false`.

## Example

{/* source:cpp-chrono-weekday-indexed-ok:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << std::boolalpha;

    std::chrono::weekday_indexed wdi{std::chrono::Tuesday[2]};
    std::cout << (wdi.ok()) << ' ';
    wdi = {std::chrono::weekday(42)[2]}; 
    std::cout << (wdi.ok()) << ' ';
    wdi = {std::chrono::Tuesday[-4]}; 
    std::cout << (wdi.ok()) << '\n';
}
```

Output:

```text
true false false
```
