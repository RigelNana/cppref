---
title: "std::chrono::operator<<(std::chrono::weekday_indexed)"
description: "Outputs a textual representation of wdi into the stream os, as if by:"
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday_indexed/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>&
    operator<<( std::basic_ostream<CharT, Traits>& os,

                const std::chrono::weekday_indexed& wdi );`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0001 */}
Outputs a textual representation of `wdi` into the stream `os`, as if by:

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0002 */}
```cpp
if (wdi.index() >=1 && wdi.index() <= 5)
    os << std::format(os.getloc(), STATICALLY_WIDEN<CharT>("{:L}[{}]"),
                      wdi.weekday(), wdi.index());
else
    os << std::format(os.getloc(), STATICALLY_WIDEN<CharT>("{:L}[{} is not a valid index]"),
                      wdi.weekday(), wdi.index());
```

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0003 */}
where `STATICALLY_WIDEN<CharT>("...")` is `"..."` if `CharT` is `char`, and `L"..."` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0004 */}
`os`

## Defect reports

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0006 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2372R3" standard="C++20">
    <PublishedBehavior>the given locale was used by default</PublishedBehavior>
    <CorrectedBehavior>`L` is needed to use the given locale</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

## See also

{/* source:cpp-chrono-weekday-indexed-operator-ltlt:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::formatter<std::chrono::weekday>`](/docs/cpp/library/chrono/weekday/formatter)</DescriptionTerm>
    <DescriptionBody>formatting support for weekday (class template specialization)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
