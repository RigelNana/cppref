---
title: "std::chrono::operator<<(std::chrono::sys_time)"
description: "Outputs tp into the stream os."
source_url: "https://en.cppreference.com/w/cpp/chrono/system_clock/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-system-clock-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

<Declaration id="1" language="cpp" since="C++20" code={`template< class CharT, class Traits, class Duration >
std::basic_ostream<CharT, Traits>&
    operator<<( std::basic_ostream<CharT, Traits>& os,

                const std::chrono::sys_time<Duration>& tp );`} />
<Declaration id="2" language="cpp" since="C++20" code={`template< class CharT, class Traits, class Duration >
std::basic_ostream<CharT, Traits>&
    operator<<( std::basic_ostream<CharT, Traits>& os,

                const std::chrono::sys_days& tp );`} />
</DeclarationDoc>

{/* source:cpp-chrono-system-clock-operator-ltlt:0001 */}
Outputs `tp` into the stream `os`.

{/* source:cpp-chrono-system-clock-operator-ltlt:0002 */}
1. Equivalent to:

```cpp
return os << std::format(os.getloc(), STATICALLY-WIDEN<CharT>("{:L%F %T}"), tp);
```

where `STATICALLY_WIDEN<CharT>("{:L%F %T}")` is `"{:L%F %T}"` if `CharT` is `char`, and `L"{:L%F %T}"` if `CharT` is `wchar_t`.

This overload participates in overload resolution only if `std::chrono::treat_as_floating_point_v<typename Duration::rep>` is `false` and `Duration(1) < std::chrono::days(1)`.

{/* source:cpp-chrono-system-clock-operator-ltlt:0003 */}
2. Equivalent to `os << std::chrono::year_month_day(tp);`.

## Return value

{/* source:cpp-chrono-system-clock-operator-ltlt:0004 */}
`os`

## Defect reports

{/* source:cpp-chrono-system-clock-operator-ltlt:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-chrono-system-clock-operator-ltlt:0006 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2372R3" standard="C++20"><PublishedBehavior>the given locale was used by default</PublishedBehavior><CorrectedBehavior>`L` is needed to use the given locale</CorrectedBehavior></DefectReport>
</DefectReportList>

## See also

{/* source:cpp-chrono-system-clock-operator-ltlt:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::formatter<std::chrono::sys_time>`](/docs/cpp/library/chrono/system_clock/formatter) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>formatting support for `sys_time` (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator<<`](/docs/cpp/library/chrono/year_month_day/operator_ltlt) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>outputs a `year_month_day` into a stream (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
