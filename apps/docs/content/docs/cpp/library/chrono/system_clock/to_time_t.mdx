---
title: "std::chrono::system_clock::to_time_t"
description: "Converts t to a std::time_t type."
source_url: "https://en.cppreference.com/w/cpp/chrono/system_clock/to_time_t"
language: "C++"
---

{/* source:cpp-chrono-system-clock-to-time-t:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`static std::time_t to_time_t( const time_point& t ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-system-clock-to-time-t:0001 */}
Converts `t` to a [`std::time_t`](/docs/cpp/library/chrono/c/time_t) type.

{/* source:cpp-chrono-system-clock-to-time-t:0002 */}
If [`std::time_t`](/docs/cpp/library/chrono/c/time_t) has lower precision, it is implementation-defined whether the value is rounded or truncated.

## Parameters

{/* source:cpp-chrono-system-clock-to-time-t:0003 */}
<ParameterList>
  <Parameter name="t">system clock time point to convert</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-system-clock-to-time-t:0004 */}
A [`std::time_t`](/docs/cpp/library/chrono/c/time_t) value representing `t`.

## Example

{/* source:cpp-chrono-system-clock-to-time-t:0005 */}
Get the current time as a [`std::time_t`](/docs/cpp/library/chrono/c/time_t) two ways.

```cpp
#include <chrono>
#include <ctime>
#include <iostream>
#include <thread>
using namespace std::chrono_literals;

int main()
{
    // The old way
    std::time_t oldt = std::time({});

    std::this_thread::sleep_for(2700ms);

    // The new way
    auto const now = std::chrono::system_clock::now();
    std::time_t newt = std::chrono::system_clock::to_time_t(now);

    std::cout << "newt - oldt == " << newt - oldt << " s\n";
}
```

Possible output:

```text
newt - oldt == 3 s
```

## See also

{/* source:cpp-chrono-system-clock-to-time-t:0006 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`from_time_t`](/docs/cpp/library/chrono/system_clock/from_time_t)</DescriptionTerm><DescriptionBody>converts [`std::time_t`](/docs/cpp/library/chrono/c/time_t) to a system clock time point (public static member function)</DescriptionBody></DescriptionItem>
</DescriptionList>
