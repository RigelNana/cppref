---
title: "std::chrono::system_clock::now"
description: "Returns a time point representing the current point in time."
source_url: "https://en.cppreference.com/w/cpp/chrono/system_clock/now"
language: "C++"
---

{/* source:cpp-chrono-system-clock-now:0000 */}
<DeclarationDoc>
<Declaration language="cpp" since="C++11" code={`static std::chrono::time_point<std::chrono::system_clock> now() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-system-clock-now:0001 */}
Returns a time point representing the current point in time.

## Return value

{/* source:cpp-chrono-system-clock-now:0002 */}
A time point representing the current time.

## Example

{/* source:cpp-chrono-system-clock-now:0003 */}
```cpp
#include <chrono>
#include <iostream>
 
int main()
{
    const auto now = std::chrono::system_clock::now();
    const std::time_t t_c = std::chrono::system_clock::to_time_t(now);
    std::cout << "The system clock is currently at " << std::ctime(&t_c);
}
```

Possible output:

```text
The system clock is currently at Thu Mar 30 13:28:27 2023
```
