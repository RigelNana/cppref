---
title: "std::chrono::zoned_time<Duration,TimeZonePtr>::operator local_time<duration>, std::chrono::zoned_time<Duration,TimeZonePtr>::get_local_time"
description: "Obtains a std::chrono::local_time<duration> representing the local time in the time zone corresponding to the time point *this represents."
source_url: "https://en.cppreference.com/w/cpp/chrono/zoned_time/get_local_time"
language: "C++"
---

{/* source:cpp-chrono-zoned-time-get-local-time:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`std::chrono::local_time<duration> get_local_time() const;`} />
  <Declaration language="cpp" since="C++20" code={`explicit operator std::chrono::local_time<duration>() const;`} />
</DeclarationDoc>

{/* source:cpp-chrono-zoned-time-get-local-time:0001 */}
Obtains a `std::chrono::local_time<duration>` representing the local time in the time zone corresponding to the time point `*this` represents.

## Return value

{/* source:cpp-chrono-zoned-time-get-local-time:0002 */}
A `std::chrono::local_time<duration>` computed as if by `zone->to_local(get_sys_time())`, where `zone` is the non-static data member holding the stored time zone pointer.
