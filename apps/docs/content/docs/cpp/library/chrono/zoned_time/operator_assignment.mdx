---
title: "std::chrono::zoned_time<Duration,TimeZonePtr>::operator="
description: "Assign the value of other to *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/zoned_time/operator="
language: "C++"
---

{/* source:cpp-chrono-zoned-time-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`zoned_time& operator=( const zoned_time& other ) = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`zoned_time& operator=( const std::chrono::sys_time<Duration>& other );`} />
  <Declaration id="3" language="cpp" since="C++20" code={`zoned_time& operator=( const std::chrono::local_time<Duration>& other );`} />
</DeclarationDoc>

{/* source:cpp-chrono-zoned-time-operator:0001 */}
Assign the value of `other` to `*this`.

{/* source:cpp-chrono-zoned-time-operator:0002 */}
1. Defaulted copy assignment operator. Copy-assigns both the stored time point and the stored time zone pointer. `zoned_time` has no move assignment operator; a move is a copy.

{/* source:cpp-chrono-zoned-time-operator:0003 */}
2. Assigns `other` to the stored time point. The time zone pointer is unchanged. After this call, `get_sys_time() == other`.

{/* source:cpp-chrono-zoned-time-operator:0004 */}
3. Converts `other` to a `std::chrono::sys_time` as if by `zone->to_sys(other)` (where `zone` is the non-static data member holding the stored time zone pointer) and assigns the result to the stored time point. The time zone pointer is unchanged. After this call, `get_local_time() == other`.

## Return value

{/* source:cpp-chrono-zoned-time-operator:0005 */}
`*this`
