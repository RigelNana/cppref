---
title: "std::chrono::operator==(std::chrono::zoned_time)"
description: "Compares the two zoned_time values x and y."
source_url: "https://en.cppreference.com/w/cpp/chrono/zoned_time/operator_cmp"
language: "C++"
---

{/* source:cpp-chrono-zoned-time-operator-cmp:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class Duration1, class Duration2, class TimeZonePtr >
bool operator==( const std::chrono::zoned_time<Duration1, TimeZonePtr>& x,

                 const std::chrono::zoned_time<Duration2, TimeZonePtr>& y );`} />
</DeclarationDoc>

{/* source:cpp-chrono-zoned-time-operator-cmp:0001 */}
Compares the two `zoned_time` values `x` and `y`. Two `zoned_time` objects compare equal if their time points and time zone pointers both compare equal according to `operator==`.

{/* source:cpp-chrono-zoned-time-operator-cmp:0002 */}
The `!=` operator is [synthesized](/docs/cpp/language/expressions/default_comparisons#Other_defaulted_comparison_operators) from `operator==`.

## Return value

{/* source:cpp-chrono-zoned-time-operator-cmp:0003 */}
`x.get_time_zone() == y.get_time_zone() && x.get_sys_time() == y.get_sys_time()`, except that the comparisons are performed on the nonstatic data members of `x` and `y` directly and no copying is performed.
