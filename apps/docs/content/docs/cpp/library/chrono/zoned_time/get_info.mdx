---
title: "std::chrono::zoned_time<Duration,TimeZonePtr>::get_info"
description: "Obtains the std::chrono::sys_info containing information about the time zone at the time point stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/zoned_time/get_info"
language: "C++"
---

{/* source:cpp-chrono-zoned-time-get-info:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`std::chrono::sys_info get_info() const;`} />
</DeclarationDoc>

{/* source:cpp-chrono-zoned-time-get-info:0001 */}
Obtains the `std::chrono::sys_info` containing information about the time zone at the time point stored in `*this`.

## Return value

{/* source:cpp-chrono-zoned-time-get-info:0002 */}
`zone->get_info(tp)`, where `zone` is the non-static data member holding the time zone pointer, and `tp` is the non-static data member holding the stored time point (as a `std::chrono::sys_time<duration>`).
