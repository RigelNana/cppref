---
title: "std::chrono::month::operator++, std::chrono::month::operator--"
description: "Adds or subtracts 1 from the month value, reducing the result modulo 12 to an integer in the range [1, 12]."
source_url: "https://en.cppreference.com/w/cpp/chrono/month/operator_inc_dec"
language: "C++"
---

{/* source:cpp-chrono-month-operator-inc-dec:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::month& operator++() noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month operator++( int ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::month& operator--() noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::month operator--( int ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-operator-inc-dec:0001 */}
Adds or subtracts `1` from the month value, reducing the result modulo `12` to an integer in the range `[1, 12]`.

{/* source:cpp-chrono-month-operator-inc-dec:0002 */}
1,2) Performs `*this += std::chrono::months{1};`.

{/* source:cpp-chrono-month-operator-inc-dec:0003 */}
3,4) Performs `*this -= std::chrono::months{1};`.

## Parameters

{/* source:cpp-chrono-month-operator-inc-dec:0004 */}
(none)

## Return value

{/* source:cpp-chrono-month-operator-inc-dec:0005 */}
1,3) A reference to this month after modification.

{/* source:cpp-chrono-month-operator-inc-dec:0006 */}
2,4) A copy of the month made before modification.

## Notes

{/* source:cpp-chrono-month-operator-inc-dec:0007 */}
After a call to one of these functions, `ok()` is always `true`.

## Example

{/* source:cpp-chrono-month-operator-inc-dec:0008 */}
```cpp
#include <cassert>
#include <chrono>
#include <iostream>

int main()
{
    std::chrono::month m{6};

    ++m;
    assert(m == std::chrono::month(7));

    --m;
    assert(m == std::chrono::month(6));

    m = std::chrono::December;
    m++; // rounds up to January
    assert(m.ok());
    std::cout << unsigned(m) << '\n';

    m = std::chrono::January;
    m--; // rounds down to December
    assert(m.ok());
    std::cout << unsigned(m) << '\n';
}
```

Output:

```text
1
12
```

## See also

{/* source:cpp-chrono-month-operator-inc-dec:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator+=`, `operator-=`](/docs/cpp/library/chrono/month/operator_arith)</DescriptionTerm>
    <DescriptionBody>adds or subtracts a number of months (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`, `operator-`](/docs/cpp/library/chrono/month/operator_arith_2) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs arithmetic on `month`s (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
