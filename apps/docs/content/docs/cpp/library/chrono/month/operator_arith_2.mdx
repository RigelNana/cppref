---
title: "std::chrono::operator+, std::chrono::operator- (std::chrono::month)"
description: "Adds or subtracts a number of months to or from a month."
source_url: "https://en.cppreference.com/w/cpp/chrono/month/operator_arith_2"
language: "C++"
---

{/* source:cpp-chrono-month-operator-arith-2:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::month operator+( const std::chrono::month& m,
                                        const std::chrono::months& ms ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month operator+( const std::chrono::months& ms,
                                        const std::chrono::month& m ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::month operator-( const std::chrono::month& m, 
                                        const std::chrono::months& ms ) noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::months operator-( const std::chrono::month& m1, 
                                         const std::chrono::month& m2 ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-month-operator-arith-2:0001 */}
1,2) Adds `ms.count()` months to `m`. The month value held in the result is computed by first evaluating `static_cast<long long>(unsigned(m)) + (ms.count() - 1)`, reducing it modulo 12 to an integer in the range [0, 11], and then adding 1.

{/* source:cpp-chrono-month-operator-arith-2:0002 */}
3) Subtracts `ms.count()` months from `m` and returns the result. Equivalent to `return m + -ms;`.

{/* source:cpp-chrono-month-operator-arith-2:0003 */}
4) If `m1.ok()` and `m2.ok()` are both `true`, returns a `std::chrono::months` value `m` such that `m.count()` is in the range [0, 11] and `m2 + m == m1`. Otherwise the returned value is unspecified.

## Return value

{/* source:cpp-chrono-month-operator-arith-2:0004 */}
1-3) A `std::chrono::month` holding a month value calculated as described above.

{/* source:cpp-chrono-month-operator-arith-2:0005 */}
4) A `std::chrono::months` representing the distance between `m1` and `m2`.

## Notes

{/* source:cpp-chrono-month-operator-arith-2:0006 */}
As long as the computation doesn't overflow, (1-3) always return a valid month even if `m.ok()` is `false`.

{/* source:cpp-chrono-month-operator-arith-2:0007 */}
The result of subtracting two month values is a duration of type `std::chrono::months`. That duration unit represents the length of the average Gregorian month, and the resulting duration bears no relationship to the number of days in the particular months represented by the operands. For example, `std::chrono::seconds(std::chrono::April - std::chrono::March)` is not the number of seconds in March (`2678400s`), but `2629746s` (30.436875 days).

## Example

{/* source:cpp-chrono-month-operator-arith-2:0008 */}
```cpp
#include <cassert>
#include <chrono>

int main()
{
    std::chrono::month m{6};

    m = m + std::chrono::months(2);
    assert(m == std::chrono::month(8));

    m = m - std::chrono::months(3);
    assert(m == std::chrono::month(5));

    constexpr std::chrono::months ms = std::chrono::month(8) - std::chrono::month(6);
    static_assert(ms == std::chrono::months(2));
}
```

## See also

{/* source:cpp-chrono-month-operator-arith-2:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++`, `operator++(int)`, `operator--`, `operator--(int)`](/docs/cpp/library/chrono/month/operator_inc_dec)</DescriptionTerm>
    <DescriptionBody>increments or decrements the month (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+=`, `operator-=`](/docs/cpp/library/chrono/month/operator_arith)</DescriptionTerm>
    <DescriptionBody>adds or subtracts a number of months (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
