---
title: "std::chrono::operator+, std::chrono::operator- (std::chrono::year_month_day)"
description: "Adds dm.count() months to the date represented by ymd."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_day/operator_arith_2"
language: "C++"
---

{/* source:cpp-chrono-year-month-day-operator-arith-2:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator+( const std::chrono::year_month_day& ymd,
                                                 const std::chrono::months& dm

                                               ) noexcept;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator+( const std::chrono::months& dm,
                                                 const std::chrono::year_month_day& ymd

                                               ) noexcept;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator+( const std::chrono::year_month_day& ymd,
                                                 const std::chrono::years& dy

                                               ) noexcept;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator+( const std::chrono::years& dy,
                                                 const std::chrono::year_month_day& ymd

                                               ) noexcept;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator-( const std::chrono::year_month_day& ymd,
                                                 const std::chrono::months& dm

                                               ) noexcept;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr std::chrono::year_month_day operator-( const std::chrono::year_month_day& ymd,
                                                 const std::chrono::years& dy

                                               ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-day-operator-arith-2:0001 */}
1,2) Adds `dm.count()` months to the date represented by `ymd`. The result has the same `day()` as `ymd` and the same `year()` and `month()` as `std::chrono::year_month(ymd.year(), ymd.month()) + dm`.

{/* source:cpp-chrono-year-month-day-operator-arith-2:0002 */}
3,4) Adds `dy.count()` years to the date represented by `ymd`. The result is equivalent to `std::chrono::year_month_day(ymd.year() + dy, ymd.month(), ymd.day()`.

{/* source:cpp-chrono-year-month-day-operator-arith-2:0003 */}
5) Subtracts `dm.count()` months from the date represented by `ymd`. Equivalent to `ymd + -dm`.

{/* source:cpp-chrono-year-month-day-operator-arith-2:0004 */}
6) Subtracts `dy.count()` years from the date represented by `ymd`. Equivalent to `ymd + -dy`.

{/* source:cpp-chrono-year-month-day-operator-arith-2:0005 */}
For durations that are convertible to both `std::chrono::years` and `std::chrono::months`, the `years` overloads (3,4,6) are preferred if the call would otherwise be ambiguous.

## Notes

{/* source:cpp-chrono-year-month-day-operator-arith-2:0006 */}
Even if `ymd.ok()` is `true`, the resulting `year_month_day` may not represent a valid date if `ymd.day()` is 29, 30, or 31.

## Example

{/* source:cpp-chrono-year-month-day-operator-arith-2:0007 */}
```cpp
#include <chrono>
#include <iostream>
 
int main()
{
    std::cout << std::boolalpha;
 
    auto ymd{std::chrono::day(1)/std::chrono::July/2021};
 
    ymd = ymd + std::chrono::months(4);
    std::cout << (ymd.month() == std::chrono::November) << ' '
              << (ymd.year() == std::chrono::year(2021)) << ' ';
 
    ymd = ymd - std::chrono::years(10);
    std::cout << (ymd.month() == std::chrono::month(11)) << ' '
              << (ymd.year() == std::chrono::year(2011)) << '\n';
}
```

Output:

```text
true true true true
```
