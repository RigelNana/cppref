---
title: "std::chrono::year_month_day::year, std::chrono::year_month_day::month, std::chrono::year_month_day::day"
description: "Retrieves the year, month and day values stored in this year_month_day object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_day/accessors"
language: "C++"
---

{/* source:cpp-chrono-year-month-day-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year year() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::day day() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-day-accessors:0001 */}
Retrieves the year, month and day values stored in this `year_month_day` object.

## Return value

{/* source:cpp-chrono-year-month-day-accessors:0002 */}
1. Returns the stored `std::chrono::year` value.

{/* source:cpp-chrono-year-month-day-accessors:0003 */}
2. Returns the stored `std::chrono::month` value.

{/* source:cpp-chrono-year-month-day-accessors:0004 */}
3. Returns the stored `std::chrono::day` value.

## Example

{/* source:cpp-chrono-year-month-day-accessors:0005 */}
```cpp
#include <chrono>

int main()
{
    constexpr std::chrono::year_month_day ymd{std::chrono::July/1/2021};

    static_assert(ymd.year() == std::chrono::year(2021));
    static_assert(ymd.month() == std::chrono::month(7));
    static_assert(ymd.day() == std::chrono::day(1));
}
```
