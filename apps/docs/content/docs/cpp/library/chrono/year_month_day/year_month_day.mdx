---
title: "std::chrono::year_month_day::year_month_day"
description: "Constructs a year_month_day object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_day/year_month_day"
language: "C++"
---

{/* source:cpp-chrono-year-month-day-year-month-day:0000 */}
<DeclarationDoc since="C++20">
  <Declaration id="1" language="cpp" code={`year_month_day() = default;`} />
  <Declaration id="2" language="cpp" code={`constexpr year_month_day( const std::chrono::year& y,
                          const std::chrono::month& m,

                          const std::chrono::day& d ) noexcept;`} />
  <Declaration id="3" language="cpp" code={`constexpr year_month_day( const std::chrono::year_month_day_last& ymdl ) noexcept;`} />
  <Declaration id="4" language="cpp" code={`constexpr year_month_day( const std::chrono::sys_days& dp ) noexcept;`} />
  <Declaration id="5" language="cpp" code={`constexpr explicit year_month_day( const std::chrono::local_days& dp ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-day-year-month-day:0001 */}
Constructs a `year_month_day` object.

{/* source:cpp-chrono-year-month-day-year-month-day:0002 */}
1. Default constructor leaves the date uninitialized.

{/* source:cpp-chrono-year-month-day-year-month-day:0003 */}
2. Constructs a `year_month_day` object that stores the year `y`, month `m` and day `d`.

{/* source:cpp-chrono-year-month-day-year-month-day:0004 */}
3. Constructs a `year_month_day` object that stores the year `ymdl.year()`, month `ymdl.month()` and day `ymdl.day()`.

{/* source:cpp-chrono-year-month-day-year-month-day:0005 */}
4. Constructs a `year_month_day` object that represent the same date as the one represented by `dp`.

{/* source:cpp-chrono-year-month-day-year-month-day:0006 */}
5. Constructs a `year_month_day` object that represent the same date as the one represented by `dp`, as if by `year_month_day(sys_days(dp.time_since_epoch()))`.

{/* source:cpp-chrono-year-month-day-year-month-day:0007 */}
Constructors `(3,4)` define implicit conversions from `std::chrono::year_month_day_last` and `std::chrono::sys_days`, respectively.

{/* source:cpp-chrono-year-month-day-year-month-day:0008 */}
For any `year_month_day` object `ymd` representing a valid date (`ymd.ok() == true`), converting `ymd` to `sys_days` and back yields the same value.

## Notes

{/* source:cpp-chrono-year-month-day-year-month-day:0009 */}
A `year_month_day` can also be created by combining one of the partial-date types `std::chrono::year_month` and `std::chrono::month_day` with the missing component (day and year, respectively) using `operator/`.

## Example

{/* source:cpp-chrono-year-month-day-year-month-day:0010 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    using namespace std::chrono;

    constexpr auto ymd2 = std::chrono::year_month_day(
        2020y, std::chrono::January, 31d // overload (2)
    );
    std::cout << "ymd2: " << ymd2 << '\n';

    constexpr auto ymd3 = std::chrono::year_month_day(
        std::chrono::April / std::chrono::Monday[last] / 2023y // overload (3)
    );
    std::cout << "ymd3: " << ymd3 << '\n';

    const auto now = std::chrono::system_clock::now();
    const auto ymd4 = std::chrono::year_month_day(
        std::chrono::floor<std::chrono::days>(now) // overload (4)
    );
    std::cout << "ymd4: " << ymd4 << '\n';
}
```

Possible output:

```text
ymd2: 2020-01-31
ymd3: 2023-04-24
ymd4: 2023-08-30
```

## See also

{/* source:cpp-chrono-year-month-day-year-month-day:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator/`](/docs/cpp/library/chrono/operator_slash) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>conventional syntax for Gregorian calendar date creation (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
