---
title: "std::chrono::weekday::weekday"
description: "Constructs a weekday object."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday/weekday"
language: "C++"
---

{/* source:cpp-chrono-weekday-weekday:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`weekday() = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr explicit weekday( unsigned wd ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr weekday( const std::chrono::sys_days& sd ) noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr explicit weekday( const std::chrono::local_days& ld ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-weekday:0001 */}
Constructs a `weekday` object.

{/* source:cpp-chrono-weekday-weekday:0002 */}
1. Default constructor leaves the `weekday` value uninitialized.

{/* source:cpp-chrono-weekday-weekday:0003 */}
2. Constructs a `weekday` object holding the weekday value `wd`. If `wd == 7`, the value held is `0`. If `wd > 255`, the value held is unspecified.

{/* source:cpp-chrono-weekday-weekday:0004 */}
3. Constructs a `weekday` object representing the day of the week `sd` corresponds to. This constructor defines an implicit conversion from `std::chrono::sys_days` to `weekday`.

{/* source:cpp-chrono-weekday-weekday:0005 */}
4. Constructs a `weekday` object representing the day of the week `ld` corresponds to, as if by `weekday(std::chrono::sys_days(ld.time_since_epoch()))`.

## Parameters

{/* source:cpp-chrono-weekday-weekday:0006 */}
<ParameterList>
  <Parameter name="wd">a weekday value</Parameter>
  <Parameter name="sd">a system days object</Parameter>
  <Parameter name="ld">a local days object</Parameter>
</ParameterList>

## Example

{/* source:cpp-chrono-weekday-weekday:0007 */}
```cpp
#include <chrono>
#include <iomanip>
#include <iostream>

int main()
{
    constexpr std::chrono::weekday friday{5}; // uses overload (2)
    static_assert(friday == std::chrono::Friday);

    for (int y{2020}; y <= 2024; ++y)
    {
        const std::chrono::year cur_year{y};
        for (int cur_month{1}; cur_month != 13; ++cur_month)
        {
            const std::chrono::year_month_day ymd{cur_year/cur_month/13};
            const std::chrono::weekday cur_weekday{std::chrono::sys_days(ymd)}; // (3)
            if (cur_weekday == friday)
                std::cout << ymd << " is " << friday << '\n';
        }
    }
}
```

Output:

```text
2020-03-13 is Fri
2020-11-13 is Fri
2021-08-13 is Fri
2022-05-13 is Fri
2023-01-13 is Fri
2023-10-13 is Fri
2024-09-13 is Fri
2024-12-13 is Fri
```
