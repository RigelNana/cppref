---
title: "std::chrono::operator<<(std::chrono::weekday)"
description: "Inserts the abbreviated weekday name for the weekday represented by wd into the stream os."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-weekday-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

<Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>&

    operator<<( std::basic_ostream<CharT, Traits>& os, const std::chrono::weekday& wd );`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-operator-ltlt:0001 */}
If `!wd.ok()`, inserts `wd.c_encoding()` followed by `" is not a valid weekday"` into `os`. Otherwise, forms a `std::basic_string<CharT>` `s` consisting of the abbreviated weekday name for the weekday represented by `wd`, determined using the locale associated with `os`, and inserts `s` into `os`.

{/* source:cpp-chrono-weekday-operator-ltlt:0002 */}
Equivalent to

{/* source:cpp-chrono-weekday-operator-ltlt:0003 */}
```cpp
return os << (wd.ok() ?
    std::format(os.getloc(), STATICALLY_WIDEN<CharT>("{:L%a}"), wd) :
    std::format(os.getloc(), STATICALLY_WIDEN<CharT>("{} is not a valid weekday"),
                wd.c_encoding()));
```

{/* source:cpp-chrono-weekday-operator-ltlt:0004 */}
where `STATICALLY_WIDEN<CharT>("...")` is `"..."` if `CharT` is `char`, and `L"..."` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-weekday-operator-ltlt:0005 */}
`os`

## Example

{/* source:cpp-chrono-weekday-operator-ltlt:0006 */}
This section is incomplete Reason: no example

## Defect reports

{/* source:cpp-chrono-weekday-operator-ltlt:0007 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-chrono-weekday-operator-ltlt:0008 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2372R3" standard="C++20"><PublishedBehavior>the given locale was used by default</PublishedBehavior><CorrectedBehavior>`L` is needed to use the given locale</CorrectedBehavior></DefectReport>
</DefectReportList>

## See also

{/* source:cpp-chrono-weekday-operator-ltlt:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`std::formatter<std::chrono::weekday>`](/docs/cpp/library/chrono/weekday/formatter) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>formatting support for `weekday` (class template specialization)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
