---
title: "std::chrono::weekday::operator+=, std::chrono::weekday::operator-="
description: "Adds or subtracts d.count() from the weekday value, reducing the result modulo 7 to an integer in the range [0, 6]."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday/operator_arith"
language: "C++"
---

{/* source:cpp-chrono-weekday-operator-arith:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::weekday& operator+=( const std::chrono::days& d ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::weekday& operator-=( const std::chrono::days& d ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-operator-arith:0001 */}
Adds or subtracts `d.count()` from the weekday value, reducing the result modulo 7 to an integer in the range `[0, 6]`.

{/* source:cpp-chrono-weekday-operator-arith:0002 */}
1) Performs `*this = *this + d;`.

{/* source:cpp-chrono-weekday-operator-arith:0003 */}
2) Performs `*this = *this - d;`.

## Return value

{/* source:cpp-chrono-weekday-operator-arith:0004 */}
A reference to this `weekday` after modification.

## Notes

{/* source:cpp-chrono-weekday-operator-arith:0005 */}
After a call to one of these functions, `ok()` is always true if no overflow occurred during the operation.

## Example

{/* source:cpp-chrono-weekday-operator-arith:0006 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << std::boolalpha;

    std::chrono::weekday wd{5}; // Friday is 5
    wd += std::chrono::days(2);
    std::cout << (wd == std::chrono::weekday(0)) << ' '
              << (wd == std::chrono::Sunday) << ' ';

    wd -= std::chrono::days(3);
    std::cout << (wd == std::chrono::weekday(4)) << ' '
              << (wd == std::chrono::Thursday) << ' ';

    wd = std::chrono::Wednesday;
    wd += std::chrono::days{8}; // ((3 += 8 == 11) % 7) == 4;
    std::cout << (wd == std::chrono::Thursday) << ' ';

    wd -= (std::chrono::Sunday - std::chrono::Tuesday); // -= -2
    // (4 -= -2) == 6
    std::cout << (wd == std::chrono::Saturday) << '\n';
}
```

Output:

```text
true true true true true true
```

## See also

{/* source:cpp-chrono-weekday-operator-arith:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++`, `operator++(int)`, `operator--`, `operator--(int)`](/docs/cpp/library/chrono/weekday/operator_inc_dec)</DescriptionTerm>
    <DescriptionBody>increments or decrements the weekday (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`, `operator-`](/docs/cpp/library/chrono/weekday/operator_arith_2) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>performs arithmetic on weekdays (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
