---
title: "std::chrono::weekday::ok"
description: "Checks if the weekday value stored in *this is in the valid range, i.e., [0, 6]."
source_url: "https://en.cppreference.com/w/cpp/chrono/weekday/ok"
language: "C++"
---

{/* source:cpp-chrono-weekday-ok:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr bool ok() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-weekday-ok:0001 */}
Checks if the weekday value stored in `*this` is in the valid range, i.e., [`0`, 6].

## Return value

{/* source:cpp-chrono-weekday-ok:0002 */}
`true` if the weekday value stored in `*this` is in the range [`0`, 6]. Otherwise `false`.

## Example

{/* source:cpp-chrono-weekday-ok:0003 */}
```cpp
#include <chrono>
#include <iomanip>
#include <iostream>
#include <locale>
#include <string>

struct weekday_ok : std::numpunct<char>
{
    std::string do_truename()  const override { return " (is valid weekday)"; }
    std::string do_falsename() const override { return " (is not valid weekday)"; }
};

int main()
{
    std::cout.imbue(std::locale(std::cout.getloc(), new weekday_ok));
    std::cout << std::boolalpha;

    for (const unsigned u : {0 /* Sun */, 1 /* Mon */, 6, 7 /* Sun */, 8, 9})
    {
        const std::chrono::weekday wd{u};
        std::cout << "u: " << u << "; wd: " << wd.c_encoding() << wd.ok() << '\n';
    }
}
```

Output:

```text
u: 0; wd: 0 (is valid weekday)
u: 1; wd: 1 (is valid weekday)
u: 6; wd: 6 (is valid weekday)
u: 7; wd: 0 (is valid weekday)
u: 8; wd: 8 (is not valid weekday)
u: 9; wd: 9 (is not valid weekday)
```

## See also

{/* source:cpp-chrono-weekday-ok:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`c_encoding`, `iso_encoding`](/docs/cpp/library/chrono/weekday/encoding)</DescriptionTerm>
    <DescriptionBody>retrieves the stored weekday value\
retrieves ISO 8601 weekday value (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
