---
title: "std::chrono::clock_cast"
description: "Converts the time point t of a clock Source to an equivalent time point of the clock Dest, using std::chrono::system_clock and/or std::chrono::utc_clock as intermediaries if necessary."
source_url: "https://en.cppreference.com/w/cpp/chrono/clock_cast"
language: "C++"
---

{/* source:cpp-chrono-clock-cast:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class Dest, class Source, class Duration >
auto clock_cast( const std::chrono::time_point<Source, Duration>& t );`} />
</DeclarationDoc>

{/* source:cpp-chrono-clock-cast:0001 */}
Converts the time point `t` of a clock `Source` to an equivalent time point of the clock `Dest`, using [`std::chrono::system_clock`](/docs/cpp/library/chrono/system_clock) and/or `std::chrono::utc_clock` as intermediaries if necessary.

{/* source:cpp-chrono-clock-cast:0002 */}
- If the expression `std::chrono::clock_time_conversion<Dest, Source>{}(t)` is well-formed, returns the result of that expression.
- Otherwise, if at least one of the following two expressions are well-formed, then

{/* source:cpp-chrono-clock-cast:0003 */}
If both expressions are well-formed, the conversion is ambiguous, and the program is ill-formed. Otherwise, exactly one of the two expressions is well-formed; the result of that expression is returned.

{/* source:cpp-chrono-clock-cast:0004 */}
1) `std::chrono::clock_time_conversion<Dest, std::chrono::system_clock>{}(
    std::chrono::clock_time_conversion<std::chrono::system_clock, Source>{}(t))`

{/* source:cpp-chrono-clock-cast:0005 */}
2) `std::chrono::clock_time_conversion<Dest, std::chrono::utc_clock>{}(
    std::chrono::clock_time_conversion<std::chrono::utc_clock, Source>{}(t))`

{/* source:cpp-chrono-clock-cast:0006 */}
- Otherwise, if at least one of the following two expressions are well-formed, then

{/* source:cpp-chrono-clock-cast:0007 */}
If both expressions are well-formed, the conversion is ambiguous, and the program is ill-formed. Otherwise, exactly one of the two expressions is well-formed; the result of that expression is returned.

{/* source:cpp-chrono-clock-cast:0008 */}
1) `std::chrono::clock_time_conversion<Dest, std::chrono::utc_clock>{}(
    std::chrono::clock_time_conversion<std::chrono::utc_clock, std::chrono::system_clock>{}(
        std::chrono::clock_time_conversion<std::chrono::system_clock, Source>{}(t)))`

{/* source:cpp-chrono-clock-cast:0009 */}
2) `std::chrono::clock_time_conversion<Dest, std::chrono::system_clock>{}(
    std::chrono::clock_time_conversion<std::chrono::system_clock, std::chrono::utc_clock>{}(
        std::chrono::clock_time_conversion<std::chrono::utc_clock, Source>{}(t)))`

{/* source:cpp-chrono-clock-cast:0010 */}
- Otherwise, this function does not participate in overload resolution.

## Return value

{/* source:cpp-chrono-clock-cast:0011 */}
The result of the conversion, determined as described above.

## Example

{/* source:cpp-chrono-clock-cast:0012 */}
This section is incomplete

Reason: no example

## See also

{/* source:cpp-chrono-clock-cast:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`clock_time_conversion`](/docs/cpp/library/chrono/clock_time_conversion) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>traits class defining how to convert time points of one clock to another (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
