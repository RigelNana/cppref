---
title: "std::chrono::operator<<(std::chrono::day)"
description: "Forms a std::basic_string<CharT> s consisting of the day value stored in d formatted as a decimal number, with a leading zero if the result would otherwise be a single decimal digit."
source_url: "https://en.cppreference.com/w/cpp/chrono/day/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-day-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits >
std::basic_ostream<CharT, Traits>&

    operator<<( std::basic_ostream<CharT, Traits>& os, const std::chrono::day& d );`} />
</DeclarationDoc>

{/* source:cpp-chrono-day-operator-ltlt:0001 */}
Forms a `std::basic_string<CharT>` `s` consisting of the day value stored in `d` formatted as a decimal number, with a leading zero if the result would otherwise be a single decimal digit. Then, if `!d.ok()`, append `" is not a valid day"` to the formatted string. Inserts that string into `os`.

{/* source:cpp-chrono-day-operator-ltlt:0002 */}
Equivalent to

{/* source:cpp-chrono-day-operator-ltlt:0003 */}
```cpp
return os << (d.ok() ?
    std::format(STATICALLY_WIDEN<CharT>("{:%d}"), d) :
    std::format(STATICALLY_WIDEN<CharT>("{:%d} is not a valid day"), d));
```

{/* source:cpp-chrono-day-operator-ltlt:0004 */}
where `STATICALLY_WIDEN<CharT>("...")` is `"..."` if `CharT` is `char`, and `L"..."` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-day-operator-ltlt:0005 */}
`os`

## Example

{/* source:cpp-chrono-day-operator-ltlt:0006 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    constexpr std::chrono::day d1{31}, d2{7}, d3{42}, d4{};
    std::cout << d1 << '\n'
              << d2 << '\n'
              << d3 << '\n'
              << d4 << '\n';
}
```

Possible output:

```text
31
07
42 is not a valid day
00 is not a valid day
```

## See also

{/* source:cpp-chrono-day-operator-ltlt:0007 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm><DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`std::formatter<std::chrono::day>`](/docs/cpp/library/chrono/day/formatter) <InlineRevision since="C++20" /></DescriptionTerm><DescriptionBody>formatting support for day (class template specialization)</DescriptionBody></DescriptionItem>
</DescriptionList>
