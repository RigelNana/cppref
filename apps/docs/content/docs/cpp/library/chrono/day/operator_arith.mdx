---
title: "std::chrono::day::operator+=, std::chrono::day::operator-="
description: "Adds or subtracts d.count() days from the day value."
source_url: "https://en.cppreference.com/w/cpp/chrono/day/operator_arith"
language: "C++"
---

{/* source:cpp-chrono-day-operator-arith:0000 */}
<DeclarationDoc>
<Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::day& operator+=( const std::chrono::days& d ) noexcept;`} />
<Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::day& operator-=( const std::chrono::days& d ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-day-operator-arith:0001 */}
Adds or subtracts `d.count()` days from the day value.

{/* source:cpp-chrono-day-operator-arith:0002 */}
1. Equivalent to `*this = *this + d;`.

{/* source:cpp-chrono-day-operator-arith:0003 */}
2. Equivalent to `*this = *this - d;`.

## Return value

{/* source:cpp-chrono-day-operator-arith:0004 */}
A reference to this `day` after modification.

## Notes

{/* source:cpp-chrono-day-operator-arith:0005 */}
If the result would be outside the range `[0, 255]`, the actual stored value is unspecified.

## Example

{/* source:cpp-chrono-day-operator-arith:0006 */}
```cpp
#include <cassert>
#include <chrono>
 
int main()
{
    std::chrono::day d{15};
 
    d += std::chrono::days(2);
    assert(d == std::chrono::day(17));
 
    d -= std::chrono::days{3};
    assert(d == std::chrono::day(14));
}
```

## See also

{/* source:cpp-chrono-day-operator-arith:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++`, `operator++(int)`, `operator--`, `operator--(int)`](/docs/cpp/library/chrono/day/operator_inc_dec)</DescriptionTerm>
    <DescriptionBody>increments or decrements the day (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`, `operator-`](/docs/cpp/library/chrono/day/operator_arith_2) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>adds or subtracts a number of days and a `day`, or find the difference between two `day`s (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
