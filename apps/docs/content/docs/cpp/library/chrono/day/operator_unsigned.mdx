---
title: "std::chrono::day::operator unsigned"
description: "Returns the day value stored in *this."
source_url: "https://en.cppreference.com/w/cpp/chrono/day/operator_unsigned"
language: "C++"
---

{/* source:cpp-chrono-day-operator-unsigned:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr explicit operator unsigned() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-day-operator-unsigned:0001 */}
Returns the day value stored in `*this`.

## Return value

{/* source:cpp-chrono-day-operator-unsigned:0002 */}
The day value stored in `*this`.

## Example

{/* source:cpp-chrono-day-operator-unsigned:0003 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    constexpr std::chrono::day d{15};
    constexpr unsigned day = static_cast<unsigned>(d);
    std::cout << "The day is: " << day << '\n';
}
```

Output:

```text
The day is: 15
```
