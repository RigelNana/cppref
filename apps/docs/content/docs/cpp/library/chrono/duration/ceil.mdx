---
title: "std::chrono::ceil(std::chrono::duration)"
description: "Returns the smallest duration t representable in ToDuration that is greater or equal to d."
source_url: "https://en.cppreference.com/w/cpp/chrono/duration/ceil"
language: "C++"
---

{/* source:cpp-chrono-duration-ceil:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++17" code={`template< class ToDuration, class Rep, class Period >
constexpr ToDuration ceil( const std::chrono::duration<Rep, Period>& d );`} />
</DeclarationDoc>

{/* source:cpp-chrono-duration-ceil:0001 */}
Returns the smallest duration `t` representable in `ToDuration` that is greater or equal to `d`.

{/* source:cpp-chrono-duration-ceil:0002 */}
The function does not participate in the overload resolution unless `ToDuration` is a specialization of [`std::chrono::duration`](/docs/cpp/library/chrono/duration).

## Parameters

{/* source:cpp-chrono-duration-ceil:0003 */}
<ParameterList>
  <Parameter name="d">duration to convert</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-duration-ceil:0004 */}
`d` rounded up to a duration of type `ToDuration`.

## Possible implementation

{/* source:cpp-chrono-duration-ceil:0005 */}
```cpp
namespace detail
{
    template<class> inline constexpr bool is_duration_v = false;
    template<class Rep, class Period> inline constexpr bool is_duration_v<
        std::chrono::duration<Rep, Period>> = true;
}
 
template<class To, class Rep, class Period,
         class = std::enable_if_t<detail::is_duration_v<To>>>
constexpr To ceil(const std::chrono::duration<Rep, Period>& d)
{
    To t = std::chrono::duration_cast<To>(d);
    if (t < d)
        return t + To{1};
    return t;
}
```

## Example

{/* source:cpp-chrono-duration-ceil:0006 */}
```cpp
#include <chrono>
#include <iostream>
 
int main()
{
    using namespace std::chrono_literals;
    using Min = std::chrono::minutes;
 
    std::cout
        << std::showpos
        << "ceil(+2.4min) = " << std::chrono::ceil<Min>(+2.4min).count() << "min\n"
        << "ceil(-2.4min) = " << std::chrono::ceil<Min>(-2.4min).count() << "min\n"
        << "ceil(+0.0min) = " << std::chrono::ceil<Min>(+0.0min).count() << "min\n";
}
```

Output:

```text
ceil(+2.4min) = +3min
ceil(-2.4min) = -2min
ceil(+0.0min) = +0min
```

## See also

{/* source:cpp-chrono-duration-ceil:0007 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`duration_cast`](/docs/cpp/library/chrono/duration/duration_cast) <InlineRevision since="C++11" /></DescriptionTerm><DescriptionBody>converts a duration to another, with a different tick interval (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`floor(std::chrono::duration)`](/docs/cpp/library/chrono/duration/floor) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a duration to another, rounding down (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`round(std::chrono::duration)`](/docs/cpp/library/chrono/duration/round) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a duration to another, rounding to nearest, ties to even (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`ceil(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/ceil) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a time_point to another, rounding up (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`ceil`, `ceilf`, `ceill`](/docs/cpp/library/numeric/math/ceil) <InlineRevision since="C++11" /></DescriptionTerm><DescriptionBody>nearest integer not less than the given value (function)</DescriptionBody></DescriptionItem>
</DescriptionList>
