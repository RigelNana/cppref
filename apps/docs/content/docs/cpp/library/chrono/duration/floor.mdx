---
title: "std::chrono::floor(std::chrono::duration)"
description: "Returns the greatest duration t representable in ToDuration that is less or equal to d."
source_url: "https://en.cppreference.com/w/cpp/chrono/duration/floor"
language: "C++"
---

{/* source:cpp-chrono-duration-floor:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++17" code={`template< class ToDuration, class Rep, class Period >
constexpr ToDuration floor( const std::chrono::duration<Rep, Period>& d );`} />
</DeclarationDoc>

{/* source:cpp-chrono-duration-floor:0001 */}
Returns the greatest duration `t` representable in `ToDuration` that is less or equal to `d`.

{/* source:cpp-chrono-duration-floor:0002 */}
The function does not participate in the overload resolution unless `ToDuration` is a specialization of [`std::chrono::duration`](/docs/cpp/library/chrono/duration).

## Parameters

{/* source:cpp-chrono-duration-floor:0003 */}
<ParameterList>
  <Parameter name="d">
    duration to convert
  </Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-duration-floor:0004 */}
`d` rounded down to a duration of type `ToDuration`.

## Possible implementation

{/* source:cpp-chrono-duration-floor:0005 */}
```cpp
namespace detail
{
    template<class> inline constexpr bool is_duration_v = false;
    template<class Rep, class Period> inline constexpr bool is_duration_v<
        std::chrono::duration<Rep, Period>> = true;
}

template<class To, class Rep, class Period,
         class = std::enable_if_t<detail::is_duration_v<To>>>
constexpr To floor(const duration<Rep, Period>& d)
{
    To t = std::chrono::duration_cast<To>(d);
    if (t > d)
        return t - To{1};
    return t;
}
```

## Example

{/* source:cpp-chrono-duration-floor:0006 */}
```cpp
#include <chrono>
#include <iomanip>
#include <iostream>

int main()
{
    using namespace std::chrono_literals;
    std::cout << "Duration\tFloor\tRound\tCeil\n";
    for (using Sec = std::chrono::seconds;
        auto const d : {+4999ms, +5000ms, +5001ms, +5499ms, +5500ms, +5999ms,
                        -4999ms, -5000ms, -5001ms, -5499ms, -5500ms, -5999ms})
        std::cout << std::showpos << d << "\t\t"
                  << std::chrono::floor<Sec>(d) << '\t'
                  << std::chrono::round<Sec>(d) << '\t'
                  << std::chrono::ceil <Sec>(d) << '\n';
}
```

Output:

```text
Duration	Floor	Round	Ceil
+4999ms		+4s	+5s	+5s
+5000ms		+5s	+5s	+5s
+5001ms		+5s	+5s	+6s
+5499ms		+5s	+5s	+6s
+5500ms		+5s	+6s	+6s
+5999ms		+5s	+6s	+6s
-4999ms		-5s	-5s	-4s
-5000ms		-5s	-5s	-5s
-5001ms		-6s	-5s	-5s
-5499ms		-6s	-5s	-5s
-5500ms		-6s	-6s	-5s
-5999ms		-6s	-6s	-5s
```

## See also

{/* source:cpp-chrono-duration-floor:0007 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`duration_cast`](/docs/cpp/library/chrono/duration/duration_cast) <InlineRevision since="C++11" /></DescriptionTerm><DescriptionBody>converts a duration to another, with a different tick interval (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`ceil(std::chrono::duration)`](/docs/cpp/library/chrono/duration/ceil) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a duration to another, rounding up (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`round(std::chrono::duration)`](/docs/cpp/library/chrono/duration/round) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a duration to another, rounding to nearest, ties to even (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`floor(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/floor) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a time_point to another, rounding down (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`floor`, `floorf`, `floorl`](/docs/cpp/library/numeric/math/floor) <InlineRevision since="C++11" /></DescriptionTerm><DescriptionBody>nearest integer not greater than the given value (function)</DescriptionBody></DescriptionItem>
</DescriptionList>
