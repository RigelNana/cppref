---
title: "std::chrono::year_month::year, std::chrono::year_month::month"
description: "Retrieves the year and month values stored in this year_month object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month/accessors"
language: "C++"
---

{/* source:cpp-chrono-year-month-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year year() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-accessors:0001 */}
Retrieves the year and month values stored in this `year_month` object.

## Return value

{/* source:cpp-chrono-year-month-accessors:0002 */}
1. Returns the stored `std::chrono::year` value.

{/* source:cpp-chrono-year-month-accessors:0003 */}
2. Returns the stored `std::chrono::month` value.

## Example

{/* source:cpp-chrono-year-month-accessors:0004 */}
```cpp
#include <chrono>
#include <iostream>

int main()
{
    std::cout << std::boolalpha;

    constexpr auto ym{std::chrono::year(2021)/std::chrono::July};
    std::cout << (ym.year() == std::chrono::year(2021)) << ' ';
    std::cout << (ym.month() == std::chrono::month(7)) << '\n';
}
```

Output:

```text
true true
```
