---
title: "std::chrono::operator+, std::chrono::operator- (std::chrono::year_month)"
description: "1,2) Adds dy.count() years to ym."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month/operator_arith_2"
language: "C++"
---

{/* source:cpp-chrono-year-month-operator-arith-2:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator+( const std::chrono::year_month& ym,
                       const std::chrono::years& dy ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator+( const std::chrono::years& dy,
                       const std::chrono::year_month& ym ) noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator+( const std::chrono::year_month& ym,
                       const std::chrono::months& dm ) noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator+( const std::chrono::months& dm,
                       const std::chrono::year_month& ym ) noexcept;`} />
  <Declaration id="5" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator-( const std::chrono::year_month& ym,
                       const std::chrono::years& dy ) noexcept;`} />
  <Declaration id="6" language="cpp" since="C++20" code={`constexpr std::chrono::year_month operator-( const std::chrono::year_month& ym,
                       const std::chrono::months& dm ) noexcept;`} />
  <Declaration id="7" language="cpp" since="C++20" code={`constexpr std::chrono::months operator-( const std::chrono::year_month& ym1,
                   const std::chrono::year_month& ym2 ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-operator-arith-2:0001 */}
1,2) Adds `dy.count()` years to `ym`.

{/* source:cpp-chrono-year-month-operator-arith-2:0002 */}
3,4) Adds `dm.count()` months to `ym`.

{/* source:cpp-chrono-year-month-operator-arith-2:0003 */}
5) Subtracts `dy.count()` years from `ym`.

{/* source:cpp-chrono-year-month-operator-arith-2:0004 */}
6) Subtracts `dm.count()` months from `ym`.

{/* source:cpp-chrono-year-month-operator-arith-2:0005 */}
7) Returns the difference in months between the two time points represented by `ym1` and `ym2`.

{/* source:cpp-chrono-year-month-operator-arith-2:0006 */}
For durations that are convertible to both `std::chrono::years` and `std::chrono::months`, the years overloads (1,2,5) are preferred if the call would otherwise be ambiguous.

## Return value

{/* source:cpp-chrono-year-month-operator-arith-2:0007 */}
1,2) `std::chrono::year_month(ym.year() + dy, ym.month())`

{/* source:cpp-chrono-year-month-operator-arith-2:0008 */}
3,4) A `year_month` value `z` such that `z - ym == dm` and `z.ok() == true`.

{/* source:cpp-chrono-year-month-operator-arith-2:0009 */}
5) `ym + -dy`

{/* source:cpp-chrono-year-month-operator-arith-2:0010 */}
6) `ym + -dm`

{/* source:cpp-chrono-year-month-operator-arith-2:0011 */}
7) `ym1.year() - ym2.year() + std::chrono::months(int(unsigned(ym1.month())) - int(unsigned(ym2.month())))`

## Notes

{/* source:cpp-chrono-year-month-operator-arith-2:0012 */}
The result of subtracting two `year_month` values is a duration of type `std::chrono::months`. This duration unit represents the length of the average Gregorian month (30.436875 days), and the resulting duration bears no relationship to the actual number of days in the time period at issue. For example, the result of `2017y/3 - 2017y/2` is `std::chrono::months(1)`, even though February 2017 only contains 28 days.

## Example

{/* source:cpp-chrono-year-month-operator-arith-2:0013 */}
```cpp
#include <cassert>
#include <chrono>

int main()
{
    auto ym{std::chrono::year(2021)/std::chrono::July};

    ym = ym + std::chrono::months(14);
    assert(ym.month() == std::chrono::September);
    assert(ym.year() == std::chrono::year(2022));

    ym = ym - std::chrono::years(3);
    assert(ym.month() == std::chrono::month(9));
    assert(ym.year() == std::chrono::year(2019));

    ym = ym + (std::chrono::September - std::chrono::month(2));
    assert(ym.month() == std::chrono::April);
    assert(ym.year() == std::chrono::year(2020));
}
```

## See also

{/* source:cpp-chrono-year-month-operator-arith-2:0014 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator+=`, `operator-=`](/docs/cpp/library/chrono/year_month/operator_arith)</DescriptionTerm>
    <DescriptionBody>modifies the year_month by some number of months or years (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
