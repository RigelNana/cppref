---
title: "std::chrono::year_month::ok"
description: "Checks if the year and month values stored in this object are valid."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month/ok"
language: "C++"
---

{/* source:cpp-chrono-year-month-ok:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr bool ok() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-ok:0001 */}
Checks if the year and month values stored in this object are valid.

## Return value

{/* source:cpp-chrono-year-month-ok:0002 */}
`year().ok() && month().ok()`

## Example

{/* source:cpp-chrono-year-month-ok:0003 */}
```cpp
#include <chrono>
using namespace std::chrono_literals;

int main()
{
    constexpr std::chrono::year_month ym1{3030y, std::chrono::July};
    static_assert(ym1.ok());

    constexpr std::chrono::year_month ym2{std::chrono::year(2020)/16};
    static_assert(!ym2.ok());
}
```
