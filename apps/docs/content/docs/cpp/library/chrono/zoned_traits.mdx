---
title: "std::chrono::zoned_traits"
description: "The class zoned_traits provides a way to customize the behavior of the constructors of std::chrono::zoned_time with custom time zone pointer types."
source_url: "https://en.cppreference.com/w/cpp/chrono/zoned_traits"
language: "C++"
---

{/* source:cpp-chrono-zoned-traits:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration id="1" language="cpp" since="C++20" code={`template < class TimeZonePtr >
struct zoned_traits { };`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template <>
struct zoned_traits<const std::chrono::time_zone*>;`} />
</DeclarationDoc>

{/* source:cpp-chrono-zoned-traits:0001 */}
The class `zoned_traits` provides a way to customize the behavior of the constructors of `std::chrono::zoned_time` with custom time zone pointer types. In particular, it allows such types to specify the default time zone to use and the mapping of a time zone's name to the corresponding time zone pointer. It is acceptable for custom time zone pointer types to not support either operation, in which case the corresponding constructors of `zoned_time` will not participate in overload resolution.

{/* source:cpp-chrono-zoned-traits:0002 */}
The primary template is empty. A specialization is provided for `const std::chrono::time_zone*`, the default time zone pointer type.

## Member functions

{/* source:cpp-chrono-zoned-traits:0003 */}
## `std::chrono::zoned_traits<const std::chrono::time_zone*>::default_zone`

<DeclarationDoc>
  <Declaration language="cpp" code={`static const std::chrono::time_zone* default_zone();`} />
</DeclarationDoc>

Returns a time zone pointer for the default time zone (UTC).

### Return value

[`std::chrono::locate_zone("UTC")`](/docs/cpp/library/chrono/locate_zone).

{/* source:cpp-chrono-zoned-traits:0004 */}
## `std::chrono::zoned_traits<const std::chrono::time_zone*>::locate_zone`

<DeclarationDoc>
  <Declaration language="cpp" code={`static const std::chrono::time_zone* locate_zone(std::string_view name);`} />
</DeclarationDoc>

Returns the time zone pointer for the time zone designated by `name`.

### Return value

[`std::chrono::locate_zone(name)`](/docs/cpp/library/chrono/locate_zone).
