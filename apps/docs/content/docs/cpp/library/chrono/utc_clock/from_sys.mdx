---
title: "std::chrono::utc_clock::from_sys"
description: "Converts a sys_time t to a utc_time u that represents the same point in time."
source_url: "https://en.cppreference.com/w/cpp/chrono/utc_clock/from_sys"
language: "C++"
---

{/* source:cpp-chrono-utc-clock-from-sys:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class Duration >
static std::chrono::utc_time<std::common_type_t<Duration, std::chrono::seconds>>

    from_sys( const std::chrono::sys_time<Duration>& t );`} />
</DeclarationDoc>

{/* source:cpp-chrono-utc-clock-from-sys:0001 */}
Converts a `sys_time` `t` to a `utc_time` `u` that represents the same point in time.

{/* source:cpp-chrono-utc-clock-from-sys:0002 */}
`u.time_since_epoch() - t.time_since_epoch()` is equal to the number of leap seconds that was inserted between `t` and 1 January 1970. If `t` is the exact date of a leap second insertion, that leap second is counted as inserted.

## Return value

{/* source:cpp-chrono-utc-clock-from-sys:0003 */}
A `utc_time` representing the same point in time as `t`.

## See also

{/* source:cpp-chrono-utc-clock-from-sys:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`to_sys`](/docs/cpp/library/chrono/utc_clock/to_sys) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>converts `utc_time` to `sys_time` (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`clock_cast`](/docs/cpp/library/chrono/clock_cast) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>convert time points of one clock to another (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
