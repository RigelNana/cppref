---
title: "std::chrono::utc_clock::to_sys"
description: "Converts a utc_time t to a sys_time representing the same point in time (if possible)."
source_url: "https://en.cppreference.com/w/cpp/chrono/utc_clock/to_sys"
language: "C++"
---

{/* source:cpp-chrono-utc-clock-to-sys:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class Duration >
static std::chrono::sys_time<std::common_type_t<Duration, std::chrono::seconds>>

      to_sys( const std::chrono::utc_time<Duration>& t );`} />
</DeclarationDoc>

{/* source:cpp-chrono-utc-clock-to-sys:0001 */}
Converts a `utc_time` `t` to a `sys_time` representing the same point in time (if possible).

{/* source:cpp-chrono-utc-clock-to-sys:0002 */}
If `t` represents a time point during a leap second insertion, the last representable value of `sys_time` prior to the insertion of the leap second is returned. In all other cases, `utc_clock::from_sys(utc_clock::to_sys(t)) == t`.

## Return value

{/* source:cpp-chrono-utc-clock-to-sys:0003 */}
A `sys_time` representing the same point in time as `t`, or the last representable value prior to the insertion of the leap second if `t` represents a time point during a leap second insertion.

## See also

{/* source:cpp-chrono-utc-clock-to-sys:0004 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`from_sys`](/docs/cpp/library/chrono/utc_clock/from_sys) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>converts sys_time to utc_time (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`clock_cast`](/docs/cpp/library/chrono/clock_cast) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>convert time points of one clock to another (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
