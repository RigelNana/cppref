---
title: "std::chrono::operator<<(std::chrono::utc_time)"
description: "Outputs tp into the stream os, as if by os << std::format(os.getloc(), STATICALLY-WIDEN(\"{:L%F %T}\"), tp), where STATICALLY_WIDEN<CharT>(\"{:L%F %T}\") is \"{:L%F %T}\" if CharT is char, and L\"{:L%F %T}\" if CharT is wchar_t."
source_url: "https://en.cppreference.com/w/cpp/chrono/utc_clock/operator_ltlt"
language: "C++"
---

{/* source:cpp-chrono-utc-clock-operator-ltlt:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++20" code={`template< class CharT, class Traits, class Duration >
std::basic_ostream<CharT, Traits>&
    operator<<( std::basic_ostream<CharT, Traits>& os,

        const std::chrono::utc_time<Duration>& tp );`} />
</DeclarationDoc>

{/* source:cpp-chrono-utc-clock-operator-ltlt:0001 */}
Outputs `tp` into the stream `os`, as if by `os << std::format(os.getloc(), STATICALLY-WIDEN("{:L%F %T}"), tp)`, where `STATICALLY_WIDEN<CharT>("{:L%F %T}")` is `"{:L%F %T}"` if `CharT` is `char`, and `L"{:L%F %T}"` if `CharT` is `wchar_t`.

## Return value

{/* source:cpp-chrono-utc-clock-operator-ltlt:0002 */}
`os`

## Defect reports

{/* source:cpp-chrono-utc-clock-operator-ltlt:0003 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-chrono-utc-clock-operator-ltlt:0004 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2372R3" standard="C++20"><PublishedBehavior>the given locale was used by default</PublishedBehavior><CorrectedBehavior>`L` is needed to use the given locale</CorrectedBehavior></DefectReport>
</DefectReportList>

## See also

{/* source:cpp-chrono-utc-clock-operator-ltlt:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`std::formatter<std::chrono::utc_time>`](/docs/cpp/library/chrono/utc_clock/formatter) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>formatting support for `utc_time` (class template specialization)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`format`](/docs/cpp/library/text/format/format) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>stores formatted representation of the arguments in a new string (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
