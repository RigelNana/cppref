---
title: "std::chrono::time_point<Clock,Duration>::time_since_epoch"
description: "Returns a duration representing the amount of time between *this and the clock's epoch."
source_url: "https://en.cppreference.com/w/cpp/chrono/time_point/time_since_epoch"
language: "C++"
---

{/* source:cpp-chrono-time-point-time-since-epoch:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`duration time_since_epoch() const;`} />
  <InlineRevision since="C++14">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-chrono-time-point-time-since-epoch:0001 */}
Returns a [`duration`](/docs/cpp/library/chrono/duration) representing the amount of time between `*this` and the clock's epoch.

## Parameters

{/* source:cpp-chrono-time-point-time-since-epoch:0002 */}
(none)

## Return value

{/* source:cpp-chrono-time-point-time-since-epoch:0003 */}
The amount of time between this time_point and the clock's epoch.

## Example

{/* source:cpp-chrono-time-point-time-since-epoch:0004 */}
```cpp
#include <chrono>
#include <ctime>
#include <iostream>

int main()
{
    const auto p0 = std::chrono::time_point<std::chrono::system_clock>{};
    const auto p1 = std::chrono::system_clock::now();
    const auto p2 = p1 - std::chrono::hours(24);

    std::time_t epoch_time = std::chrono::system_clock::to_time_t(p0);
    std::cout << "epoch: " << std::ctime(&epoch_time);
    std::time_t today_time = std::chrono::system_clock::to_time_t(p1);
    std::cout << "today: " << std::ctime(&today_time);

    std::cout << "hours since epoch: "
              << std::chrono::duration_cast<std::chrono::hours>(
                     p1.time_since_epoch()).count() 
              << '\n';
    std::cout << "yesterday, hours since epoch: "
              << std::chrono::duration_cast<std::chrono::hours>(
                     p2.time_since_epoch()).count() 
              << '\n';
}
```

Possible output:

```text
epoch: Thu Jan  1 00:00:00 1970
today: Fri Jun 30 10:44:11 2017
hours since epoch: 416338
yesterday, hours since epoch: 416314
```
