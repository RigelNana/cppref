---
title: "std::chrono::round(std::chrono::time_point)"
description: "Returns the nearest time point to tp representable in ToDuration, rounding to even in halfway cases."
source_url: "https://en.cppreference.com/w/cpp/chrono/time_point/round"
language: "C++"
---

{/* source:cpp-chrono-time-point-round:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration language="cpp" since="C++17" code={`template< class ToDuration, class Clock, class Duration >
constexpr std::chrono::time_point<Clock, ToDuration>

    round( const std::chrono::time_point<Clock, Duration>& tp );`} />
</DeclarationDoc>

{/* source:cpp-chrono-time-point-round:0001 */}
Returns the nearest time point to `tp` representable in `ToDuration`, rounding to even in halfway cases.

{/* source:cpp-chrono-time-point-round:0002 */}
The function does not participate in the overload resolution unless `ToDuration` is a specialization of [`std::chrono::duration`](/docs/cpp/library/chrono/duration) and `std::chrono::treat_as_floating_point_v<typename ToDuration::rep>` is `false`.

## Parameters

{/* source:cpp-chrono-time-point-round:0003 */}
<ParameterList>
  <Parameter name="tp">time point to round to nearest</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-time-point-round:0004 */}
`tp` rounded to nearest time point using duration of type `ToDuration`, rounding to even in halfway cases.

## Possible implementation

{/* source:cpp-chrono-time-point-round:0005 */}
```cpp
namespace detail
{
    template<class> inline constexpr bool is_duration_v = false;
    template<class Rep, class Period> inline constexpr bool is_duration_v<
        std::chrono::duration<Rep, Period>> = true;
}

template<class To, class Clock, class FromDuration,
         class = std::enable_if_t<detail::is_duration_v<To> &&
                !std::chrono::treat_as_floating_point_v<typename To::rep>>>
constexpr std::chrono::time_point<Clock, To> round(
    const std::chrono::time_point<Clock, FromDuration>& tp)
{
    return std::chrono::time_point<Clock, To>{
        std::chrono::round<To>(tp.time_since_epoch())};
}
```

## Example

{/* source:cpp-chrono-time-point-round:0006 */}
```cpp
#include <chrono>
#include <iostream>
#include <string>

template<typename TimePoint>
std::string to_string(const TimePoint& time_point)
{
    return std::to_string(time_point.time_since_epoch().count());
}

int main()
{
    using namespace std::literals::chrono_literals;
    using Sec = std::chrono::seconds;

    std::cout << "Time point\t" "Cast\t" "Floor\t" "Round\t" "Ceil\n";
    std::cout << "(ms)\t\t"     "(s)\t"  "(s)\t"   "(s)\t"   "(s)\n";
    for (const auto value_ms : {5432ms, 5678ms})
    {
        std::chrono::time_point<std::chrono::system_clock, std::chrono::milliseconds>
            time_point_ms(value_ms);

        std::cout
            << to_string(time_point_ms) << "\t\t"
            << to_string(std::chrono::time_point_cast<Sec>(time_point_ms)) << '\t'
            << to_string(std::chrono::floor<Sec>(time_point_ms)) << '\t'
            << to_string(std::chrono::round<Sec>(time_point_ms)) << '\t'
            << to_string(std::chrono::ceil<Sec>(time_point_ms)) << '\n';
    }
}
```

Output:

```text
Time point	Cast	Floor	Round	Ceil
(ms)		(s)	(s)	(s)	(s)
5432		5	5	5	6
5678		5	5	6	6
```

## See also

{/* source:cpp-chrono-time-point-round:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`time_point_cast`](/docs/cpp/library/chrono/time_point/time_point_cast) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts a time point to another time point on the same clock, with a different duration (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ceil(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/ceil) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts a time_point to another, rounding up (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`floor(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/floor) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts a time_point to another, rounding down (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`round(std::chrono::duration)`](/docs/cpp/library/chrono/duration/round) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>converts a duration to another, rounding to nearest, ties to even (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`round`, `roundf`, `roundl`, `lround`, `lroundf`, `lroundl`, `llround`, `llroundf`, `llroundl`](/docs/cpp/library/numeric/math/round) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>nearest integer, rounding away from zero in halfway cases (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
