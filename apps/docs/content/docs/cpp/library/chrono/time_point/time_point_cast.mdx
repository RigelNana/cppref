---
title: "std::chrono::time_point_cast"
description: "Converts a std::chrono::time_point from one duration to another."
source_url: "https://en.cppreference.com/w/cpp/chrono/time_point/time_point_cast"
language: "C++"
---

{/* source:cpp-chrono-time-point-time-point-cast:0000 */}
<DeclarationDoc>
Defined in header [`<chrono>`](/docs/cpp/library/headers/chrono)

  <Declaration id="1" language="cpp" since="C++11" until="C++14" code={`template< class ToDuration, class Clock, class Duration >
std::chrono::time_point<Clock, ToDuration>

  time_point_cast( const std::chrono::time_point<Clock, Duration> &t );`} />
  <Declaration id="2" language="cpp" since="C++14" code={`template< class ToDuration, class Clock, class Duration >
constexpr std::chrono::time_point<Clock, ToDuration>

  time_point_cast( const std::chrono::time_point<Clock, Duration> &t );`} />
</DeclarationDoc>

{/* source:cpp-chrono-time-point-time-point-cast:0001 */}
Converts a [`std::chrono::time_point`](/docs/cpp/library/chrono/time_point) from one duration to another.

{/* source:cpp-chrono-time-point-time-point-cast:0002 */}
`time_point_cast` participates in overload resolution only if `ToDuration` is a specialization of [`std::chrono::duration`](/docs/cpp/library/chrono/duration).

## Parameters

{/* source:cpp-chrono-time-point-time-point-cast:0003 */}
<ParameterList>
  <Parameter name="t">time_point to convert from</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-time-point-time-point-cast:0004 */}
`std::chrono::time_point<Clock, ToDuration>( std::chrono::duration_cast<ToDuration>(t.time_since_epoch()))`

## Example

{/* source:cpp-chrono-time-point-time-point-cast:0005 */}
```cpp
#include <chrono>
#include <iostream>
using namespace std::chrono_literals;

using Clock = std::chrono::high_resolution_clock;
using Ms = std::chrono::milliseconds;
using Sec = std::chrono::seconds;

template<class Duration>
using TimePoint = std::chrono::time_point<Clock, Duration>;

inline void print_ms(const TimePoint<Ms>& time_point)
{
    std::cout << time_point.time_since_epoch().count() << " ms\n";
}

int main()
{
    TimePoint<Sec> time_point_sec{4s};

    // implicit conversion, no precision loss
    TimePoint<Ms> time_point_ms = time_point_sec;
    print_ms(time_point_ms); // 4000 ms

    time_point_ms = TimePoint<Ms>{5756ms};
    print_ms(time_point_ms); // 5756 ms

    // explicit cast, need when precision loss may happen
    // 5756 truncated to 5000
    time_point_sec = std::chrono::time_point_cast<Sec>(time_point_ms);
    print_ms(time_point_sec); // 5000 ms
}
```

Output:

```text
4000 ms
5756 ms
5000 ms
```

## See also

{/* source:cpp-chrono-time-point-time-point-cast:0006 */}
<DescriptionList>
  <DescriptionItem><DescriptionTerm>[`floor(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/floor) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a time_point to another, rounding down (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`ceil(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/ceil) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a time_point to another, rounding up (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`round(std::chrono::time_point)`](/docs/cpp/library/chrono/time_point/round) <InlineRevision since="C++17" /></DescriptionTerm><DescriptionBody>converts a time_point to another, rounding to nearest, ties to even (function template)</DescriptionBody></DescriptionItem>
  <DescriptionItem><DescriptionTerm>[`duration_cast`](/docs/cpp/library/chrono/duration/duration_cast) <InlineRevision since="C++11" /></DescriptionTerm><DescriptionBody>converts a duration to another, with a different tick interval (function template)</DescriptionBody></DescriptionItem>
</DescriptionList>
