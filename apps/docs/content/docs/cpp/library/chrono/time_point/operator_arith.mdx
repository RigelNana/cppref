---
title: "std::chrono::time_point<Clock,Duration>::operator+=, operator-="
description: "Modifies the time point by the given duration."
source_url: "https://en.cppreference.com/w/cpp/chrono/time_point/operator_arith"
language: "C++"
---

{/* source:cpp-chrono-time-point-operator-arith:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`time_point& operator+=( const duration& d );`} />
  <Declaration id="2" language="cpp" since="C++11" code={`time_point& operator-=( const duration& d );`} />

  <InlineRevision since="C++17">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-chrono-time-point-operator-arith:0001 */}
Modifies the time point by the given duration.

{/* source:cpp-chrono-time-point-operator-arith:0002 */}
1. Applies the offset `d` to `pt`. Effectively, `d` is added to the internally stored duration `d_` as `d_ += d`.

{/* source:cpp-chrono-time-point-operator-arith:0003 */}
2. Applies the offset `d` to `pt` in negative direction. Effectively, `d` is subtracted from internally stored duration `d_` as `d_ -= d`.

## Parameters

{/* source:cpp-chrono-time-point-operator-arith:0004 */}
<ParameterList>
  <Parameter name="d">a time offset to apply</Parameter>
</ParameterList>

## Return value

{/* source:cpp-chrono-time-point-operator-arith:0005 */}
`*this`

## Exceptions

{/* source:cpp-chrono-time-point-operator-arith:0006 */}
May throw implementation-defined exceptions.

## Example

{/* source:cpp-chrono-time-point-operator-arith:0007 */}
This section is incomplete

Reason: no example

## See also

{/* source:cpp-chrono-time-point-operator-arith:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator+operator-`](/docs/cpp/library/chrono/time_point/operator_arith2) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>performs add and subtract operations involving a time point (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
