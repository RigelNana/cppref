---
title: "std::chrono::year_month_weekday_last::year, std::chrono::year_month_weekday_last::month, std::chrono::year_month_weekday_last::weekday, std::chrono::year_month_weekday_last::weekday_last"
description: "Retrieves the field values stored in this year_month_weekday_last object."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_weekday_last/accessors"
language: "C++"
---

{/* source:cpp-chrono-year-month-weekday-last-accessors:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr std::chrono::year year() const noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr std::chrono::month month() const noexcept;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr std::chrono::weekday weekday() const noexcept;`} />
  <Declaration id="4" language="cpp" since="C++20" code={`constexpr std::chrono::weekday_last weekday_last() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-weekday-last-accessors:0001 */}
Retrieves the field values stored in this `year_month_weekday_last` object.

## Return value

{/* source:cpp-chrono-year-month-weekday-last-accessors:0002 */}
1. Returns the stored `std::chrono::year` value.

{/* source:cpp-chrono-year-month-weekday-last-accessors:0003 */}
2. Returns the stored `std::chrono::month` value.

{/* source:cpp-chrono-year-month-weekday-last-accessors:0004 */}
3. Returns the stored `std::chrono::weekday` value.

{/* source:cpp-chrono-year-month-weekday-last-accessors:0005 */}
4. `std::chrono::weekday_last(weekday())`

## Example

{/* source:cpp-chrono-year-month-weekday-last-accessors:0006 */}
```cpp
#include <chrono>
#include <iostream>
using namespace std::chrono;

int main()
{
    auto ymwdl{Tuesday[last]/November/2022};
    auto wdl = ymwdl.weekday_last();
    wdl = (wdl.weekday() + days(1))[last];
    ymwdl = {ymwdl.year() + years(1), ymwdl.month() - months(2), wdl};
    std::cout << year_month_day(ymwdl) << '\n';
}
```

Output:

```text
2023-09-27
```
