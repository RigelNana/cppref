---
title: "std::chrono::year_month_weekday_last::ok"
description: "Checks if this object represents a valid date."
source_url: "https://en.cppreference.com/w/cpp/chrono/year_month_weekday_last/ok"
language: "C++"
---

{/* source:cpp-chrono-year-month-weekday-last-ok:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr bool ok() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-chrono-year-month-weekday-last-ok:0001 */}
Checks if this object represents a valid date. Since a `year_month_weekday_last` represents the last weekday of a particular month, it is valid as long as the year, month and weekday are valid.

## Return value

{/* source:cpp-chrono-year-month-weekday-last-ok:0002 */}
`year().ok() && month().ok() && weekday().ok()`

## Example

{/* source:cpp-chrono-year-month-weekday-last-ok:0003 */}
```cpp
#include <cassert>
#include <chrono>
using namespace std::chrono;

int main()
{
    auto ymwdl{Tuesday[last]/11/2020};
    assert(ymwdl.ok());
    ymwdl = Tuesday[last]/-2/2021;
    assert(!ymwdl.ok());
    ymwdl += months(0); // Normalize month
    assert(ymwdl.ok());
}
```
