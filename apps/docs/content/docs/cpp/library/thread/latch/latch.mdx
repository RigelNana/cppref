---
title: "std::latch::latch"
description: "Constructs a latch and initializes its internal counter. The behavior is undefined if expected is negative or greater than max()."
source_url: "https://en.cppreference.com/w/cpp/thread/latch/latch"
language: "C++"
---

{/* source:cpp-thread-latch-latch:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr explicit latch( std::ptrdiff_t expected );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`latch( const latch& ) = delete;`} />

  <DeclarationDescription>
    {/* source:cpp-thread-latch-latch:0001 */}
    1. Constructs a `latch` and initializes its internal counter. The behavior is undefined if `expected` is negative or greater than `max()`.

    {/* source:cpp-thread-latch-latch:0002 */}
    2. Copy constructor is deleted. `latch` is neither copyable nor movable.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-thread-latch-latch:0003 */}
<ParameterList>
  <Parameter name="expected">the initial value of the internal counter</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-thread-latch-latch:0004 */}
Throws nothing.
