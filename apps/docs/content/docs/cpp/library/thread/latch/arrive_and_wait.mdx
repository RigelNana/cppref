---
title: "std::latch::arrive_and_wait"
description: "Atomically decrements the internal counter by n and (if necessary) blocks the calling thread until the counter reaches zero."
source_url: "https://en.cppreference.com/w/cpp/thread/latch/arrive_and_wait"
language: "C++"
---

{/* source:cpp-thread-latch-arrive-and-wait:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void arrive_and_wait( std::ptrdiff_t n = 1 );`} />
</DeclarationDoc>

{/* source:cpp-thread-latch-arrive-and-wait:0001 */}
Atomically decrements the internal counter by `n` and (if necessary) blocks the calling thread until the counter reaches zero. Equivalent to `count_down(n); wait();`.

{/* source:cpp-thread-latch-arrive-and-wait:0002 */}
If `n` is greater than the value of the internal counter or is negative, the behavior is undefined.

### Parameters

{/* source:cpp-thread-latch-arrive-and-wait:0003 */}
<ParameterList>
  <Parameter name="n">the value by which the internal counter is decreased</Parameter>
</ParameterList>

### Return value

{/* source:cpp-thread-latch-arrive-and-wait:0004 */}
(none)

### Exceptions

{/* source:cpp-thread-latch-arrive-and-wait:0005 */}
Throws [`std::system_error`](/docs/cpp/library/error/system_error) with an error code allowed for mutex types on error.
