---
title: "std::barrier<CompletionFunction>::wait"
description: "If arrival is associated with the phase synchronization point for the current phase of *this, blocks at the synchronization point associated with arrival until the phase completion step of the synchronization point's phase is run."
source_url: "https://en.cppreference.com/w/cpp/thread/barrier/wait"
language: "C++"
---

{/* source:cpp-thread-barrier-wait:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void wait( arrival_token&& arrival ) const;`} />
</DeclarationDoc>

{/* source:cpp-thread-barrier-wait:0001 */}
If `arrival` is associated with the phase synchronization point for the current phase of `*this`, blocks at the synchronization point associated with `arrival` until the phase completion step of the synchronization point's phase is run.

{/* source:cpp-thread-barrier-wait:0002 */}
Otherwise, if `arrival` is associated with the phase synchronization point for the immediately preceding phase of `*this`, returns immediately.

{/* source:cpp-thread-barrier-wait:0003 */}
Otherwise, i.e. if `arrival` is associated with the phase synchronization point for an earlier phase of `*this` or any phase of a barrier object other than `*this`, the behavior is undefined.

### Parameters

{/* source:cpp-thread-barrier-wait:0004 */}
<ParameterList>
  <Parameter name="arrival">an `arrival_token` obtained by a previous call to [`arrive`](/docs/cpp/library/thread/barrier/arrive) on the same barrier</Parameter>
</ParameterList>

### Return value

{/* source:cpp-thread-barrier-wait:0005 */}
(none)

### Exceptions

{/* source:cpp-thread-barrier-wait:0006 */}
Throws [`std::system_error`](/docs/cpp/library/error/system_error) with an error code allowed for mutex types on error.

### Example

{/* source:cpp-thread-barrier-wait:0007 */}
This section is incomplete

Reason: no example

### See also

{/* source:cpp-thread-barrier-wait:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`arrive`](/docs/cpp/library/thread/barrier/arrive)</DescriptionTerm>
    <DescriptionBody>arrives at barrier and decrements the expected count (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
