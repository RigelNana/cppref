---
title: "std::barrier<CompletionFunction>::arrive_and_drop"
description: "Decrements the initial expected count for all subsequent phases by one, and then decrements the expected count for the current phase by one."
source_url: "https://en.cppreference.com/w/cpp/thread/barrier/arrive_and_drop"
language: "C++"
---

{/* source:cpp-thread-barrier-arrive-and-drop:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void arrive_and_drop();`} />
</DeclarationDoc>

{/* source:cpp-thread-barrier-arrive-and-drop:0001 */}
Decrements the initial expected count for all subsequent phases by one, and then decrements the expected count for the current phase by one.

{/* source:cpp-thread-barrier-arrive-and-drop:0002 */}
This function is executed atomically. The call to this function [strongly happens-before](/docs/cpp/library/atomic/memory_order#Strongly_happens-before) the start of the phase completion step for the current phase.

{/* source:cpp-thread-barrier-arrive-and-drop:0003 */}
The behavior is undefined if the expected count for the current phase is zero.

### Parameters

{/* source:cpp-thread-barrier-arrive-and-drop:0004 */}
(none)

### Return value

{/* source:cpp-thread-barrier-arrive-and-drop:0005 */}
(none)

### Exceptions

{/* source:cpp-thread-barrier-arrive-and-drop:0006 */}
Throws [`std::system_error`](/docs/cpp/library/error/system_error) with an error code allowed for mutex types on error.

### Notes

{/* source:cpp-thread-barrier-arrive-and-drop:0007 */}
This function can cause the completion step for the current phase to start.

{/* source:cpp-thread-barrier-arrive-and-drop:0008 */}
If the current expected count is zero before calling this function, the initial expected count for all subsequent phases is also zero, which means the barrier cannot be reused.

### Example

{/* source:cpp-thread-barrier-arrive-and-drop:0009 */}
This section is incomplete

Reason: no example
