---
title: "std::barrier<CompletionFunction>::~barrier"
description: "Destroys the barrier."
source_url: "https://en.cppreference.com/w/cpp/thread/barrier/~barrier"
language: "C++"
---

{/* source:cpp-thread-barrier-barrier:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`~barrier();`} />
</DeclarationDoc>

{/* source:cpp-thread-barrier-barrier:0001 */}
Destroys the barrier.

### Notes

{/* source:cpp-thread-barrier-barrier:0002 */}
It is only safe to invoke the destructor if all threads have been notified. The programmer must ensure that no threads attempt to wait on `*this` once the destructor has been started. The destructor does not notify and release any waiting threads.
