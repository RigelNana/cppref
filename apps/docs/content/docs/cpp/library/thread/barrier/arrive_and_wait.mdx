---
title: "std::barrier<CompletionFunction>::arrive_and_wait"
description: "Atomically decrements the expected count by 1, then blocks at the synchronization point for the current phase until the phase completion step of the current phase is run."
source_url: "https://en.cppreference.com/w/cpp/thread/barrier/arrive_and_wait"
language: "C++"
---

{/* source:cpp-thread-barrier-arrive-and-wait:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void arrive_and_wait();`} />
</DeclarationDoc>

{/* source:cpp-thread-barrier-arrive-and-wait:0001 */}
Atomically decrements the expected count by `1`, then blocks at the synchronization point for the current phase until the phase completion step of the current phase is run. Equivalent to `wait(arrive());`.

{/* source:cpp-thread-barrier-arrive-and-wait:0002 */}
The behavior is undefined if the expected count for the current phase is zero.

### Parameters

{/* source:cpp-thread-barrier-arrive-and-wait:0003 */}
(none)

### Return value

{/* source:cpp-thread-barrier-arrive-and-wait:0004 */}
(none)

### Exceptions

{/* source:cpp-thread-barrier-arrive-and-wait:0005 */}
Throws [`std::system_error`](/docs/cpp/library/error/system_error) with an error code allowed for mutex types on error.

### Notes

{/* source:cpp-thread-barrier-arrive-and-wait:0006 */}
If the current expected count is decremented to zero in the call to this function, the phase completion step is run and this function does not block.

{/* source:cpp-thread-barrier-arrive-and-wait:0007 */}
If the current expected count is zero before calling this function, the initial expected count for all subsequent phases is also zero, which means the barrier cannot be reused.

### Example

{/* source:cpp-thread-barrier-arrive-and-wait:0008 */}
This section is incomplete

Reason: no example
