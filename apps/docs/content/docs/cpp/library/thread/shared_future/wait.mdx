---
title: "std::shared_future<T>::wait"
description: "Blocks until the result becomes available."
source_url: "https://en.cppreference.com/w/cpp/thread/shared_future/wait"
language: "C++"
---

{/* source:cpp-thread-shared-future-wait:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void wait() const;`} />
</DeclarationDoc>

{/* source:cpp-thread-shared-future-wait:0001 */}
Blocks until the result becomes available. `valid() == true` after the call.

{/* source:cpp-thread-shared-future-wait:0002 */}
The behavior is undefined if [`valid`](/docs/cpp/library/thread/shared_future/valid)`() == false` before the call to this function.

### Parameters

{/* source:cpp-thread-shared-future-wait:0003 */}
(none)

### Return value

{/* source:cpp-thread-shared-future-wait:0004 */}
(none)

### Exceptions

{/* source:cpp-thread-shared-future-wait:0005 */}
May throw implementation-defined exceptions.

### Notes

{/* source:cpp-thread-shared-future-wait:0006 */}
The implementations are encouraged to detect the case when `valid() == false` before the call and throw a [`std::future_error`](/docs/cpp/library/thread/future_error) with an error condition of [`std::future_errc::no_state`](/docs/cpp/library/thread/future_errc).

{/* source:cpp-thread-shared-future-wait:0007 */}
Calling `wait` on the same `std::shared_future` from multiple threads is not safe; the intended use is for each thread that waits on the same shared state to have a copy of a `std::shared_future`.

### Example

{/* source:cpp-thread-shared-future-wait:0008 */}
```cpp
#include <chrono>
#include <future>
#include <iostream>
#include <thread>
 
int fib(int n)
{
    if (n < 3)
        return 1;
    else
        return fib(n - 1) + fib(n - 2);
}
 
int main()
{
    std::shared_future<int> f1 = std::async(std::launch::async, []() { return fib(40); });
    std::shared_future<int> f2 = std::async(std::launch::async, []() { return fib(43); });
 
    std::cout << "waiting... " << std::flush;
    const auto start = std::chrono::system_clock::now();
 
    f1.wait();
    f2.wait();
 
    const auto diff = std::chrono::system_clock::now() - start;
    std::cout << std::chrono::duration<double>(diff).count() << " seconds\n";
 
    std::cout << "f1: " << f1.get() << '\n';
    std::cout << "f2: " << f2.get() << '\n';
}
```

Possible output:

```text
waiting... 1.61803 seconds
f1: 102334155
f2: 433494437
```

### See also

{/* source:cpp-thread-shared-future-wait:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`wait_for`](/docs/cpp/library/thread/shared_future/wait_for)</DescriptionTerm>
    <DescriptionBody>waits for the result, returns if it is not available for the specified timeout duration (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`wait_until`](/docs/cpp/library/thread/shared_future/wait_until)</DescriptionTerm>
    <DescriptionBody>waits for the result, returns if it is not available until specified time point has been reached (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
