---
title: "std::promise<R>::operator="
description: "Assigns the contents."
source_url: "https://en.cppreference.com/w/cpp/thread/promise/operator="
language: "C++"
---

{/* source:cpp-thread-promise-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`promise& operator=( promise&& other ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`promise& operator=( const promise& rhs ) = delete;`} />
</DeclarationDoc>

{/* source:cpp-thread-promise-operator:0001 */}
Assigns the contents.

{/* source:cpp-thread-promise-operator:0002 */}
1. Move assignment operator. First, abandons the shared state (as in `~promise()`), then assigns the shared state of `other` as if by executing `std::promise(std::move(other)).swap(*this)`.

{/* source:cpp-thread-promise-operator:0003 */}
2. `promise` is not copy-assignable.

### Parameters

{/* source:cpp-thread-promise-operator:0004 */}
<ParameterList>
  <Parameter name="other">another promise to acquire the state from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-thread-promise-operator:0005 */}
`*this`
