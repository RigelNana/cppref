---
title: "std::jthread::detach"
description: "Separates the thread of execution from the jthread object, allowing execution to continue independently."
source_url: "https://en.cppreference.com/w/cpp/thread/jthread/detach"
language: "C++"
---

{/* source:cpp-thread-jthread-detach:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void detach();`} />
</DeclarationDoc>

{/* source:cpp-thread-jthread-detach:0001 */}
Separates the thread of execution from the `jthread` object, allowing execution to continue independently. Any allocated resources will be freed once the thread exits.

{/* source:cpp-thread-jthread-detach:0002 */}
After calling `detach` `*this` no longer owns any thread.

### Parameters

{/* source:cpp-thread-jthread-detach:0003 */}
(none)

### Return value

{/* source:cpp-thread-jthread-detach:0004 */}
(none)

### Postconditions

{/* source:cpp-thread-jthread-detach:0005 */}
`joinable` is `false`.

### Exceptions

{/* source:cpp-thread-jthread-detach:0006 */}
[`std::system_error`](/docs/cpp/library/error/system_error) if `joinable() == false` or an error occurs.

### Example

{/* source:cpp-thread-jthread-detach:0007 */}
```cpp
#include <chrono>
#include <iostream>
#include <thread>

void independentThread()
{
    std::cout << "Starting concurrent thread.\n";
    std::this_thread::sleep_for(std::chrono::seconds(2));
    std::cout << "Exiting concurrent thread.\n";
}

void threadCaller()
{
    std::cout << "Starting thread caller.\n";
    std::jthread t(independentThread);
    t.detach();
    std::this_thread::sleep_for(std::chrono::seconds(1));
    std::cout << "Exiting thread caller.\n";
}

int main()
{
    threadCaller();
    std::this_thread::sleep_for(std::chrono::seconds(5));
}
```

Possible output:

```text
Starting thread caller.
Starting concurrent thread.
Exiting thread caller.
Exiting concurrent thread.
```

### References

{/* source:cpp-thread-jthread-detach:0008 */}
C++23 standard (ISO/IEC 14882:2024): 33.4.4.3 Members [thread.jthread.mem]

{/* source:cpp-thread-jthread-detach:0009 */}
C++20 standard (ISO/IEC 14882:2020): 32.4.3.2 Members [thread.jthread.mem]

### See also

{/* source:cpp-thread-jthread-detach:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`join`](/docs/cpp/library/thread/jthread/join)</DescriptionTerm>
    <DescriptionBody>waits for the thread to finish its execution (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`joinable`](/docs/cpp/library/thread/jthread/joinable)</DescriptionTerm>
    <DescriptionBody>checks whether the thread is joinable, i.e. potentially running in parallel context (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[C documentation](/docs/c/library/thread/thrd_detach)</DescriptionTerm>
    <DescriptionBody>for `thrd_detach`</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
