---
title: "std::jthread::joinable"
description: "Checks if the std::jthread object identifies an active thread of execution."
source_url: "https://en.cppreference.com/w/cpp/thread/jthread/joinable"
language: "C++"
---

{/* source:cpp-thread-jthread-joinable:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`bool joinable() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-thread-jthread-joinable:0001 */}
Checks if the `std::jthread` object identifies an active thread of execution. Specifically, returns `true` if `get_id() != std::jthread::id()`. So a default constructed `jthread` is not joinable.

{/* source:cpp-thread-jthread-joinable:0002 */}
A thread that has finished executing code, but has not yet been joined is still considered an active thread of execution and is therefore joinable.

### Parameters

{/* source:cpp-thread-jthread-joinable:0003 */}
(none)

### Return value

{/* source:cpp-thread-jthread-joinable:0004 */}
`true` if the `std::jthread` object identifies an active thread of execution, `false` otherwise.

### Example

{/* source:cpp-thread-jthread-joinable:0005 */}
```cpp
#include <chrono>
#include <iostream>
#include <thread>
using namespace std::chrono_literals;

void foo()
{
    std::this_thread::sleep_for(500ms);
}

int main()
{
    std::cout << std::boolalpha;

    std::jthread t;
    std::cout << "before starting, joinable: " << t.joinable() << '\n';

    t = std::jthread{foo};
    std::cout << "after starting, joinable: " << t.joinable() << '\n';

    t.join();
    std::cout << "after joining, joinable: " << t.joinable() << '\n';

    t = std::jthread{foo};
    t.detach();
    std::cout << "after detaching, joinable: " << t.joinable() << '\n';

}
```

Output:

```text
before starting, joinable: false
after starting, joinable: true
after joining, joinable: false
after detaching, joinable: false
```

### References

{/* source:cpp-thread-jthread-joinable:0006 */}
C++23 standard (ISO/IEC 14882:2024): 33.4.4.3 Members [thread.jthread.mem]

{/* source:cpp-thread-jthread-joinable:0007 */}
C++20 standard (ISO/IEC 14882:2020): 32.4.3.2 Members [thread.jthread.mem]

### See also

{/* source:cpp-thread-jthread-joinable:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`get_id`](/docs/cpp/library/thread/jthread/get_id)</DescriptionTerm>
    <DescriptionBody>returns the *id* of the thread (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`join`](/docs/cpp/library/thread/jthread/join)</DescriptionTerm>
    <DescriptionBody>waits for the thread to finish its execution (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`detach`](/docs/cpp/library/thread/jthread/detach)</DescriptionTerm>
    <DescriptionBody>permits the thread to execute independently from the thread handle (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
