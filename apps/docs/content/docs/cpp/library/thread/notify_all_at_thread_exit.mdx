---
title: "std::notify_all_at_thread_exit"
description: "notify_all_at_thread_exit provides a mechanism to notify other threads that a given thread has completely finished, including destroying all thread_local objects."
source_url: "https://en.cppreference.com/w/cpp/thread/notify_all_at_thread_exit"
language: "C++"
---

{/* source:cpp-thread-notify-all-at-thread-exit:0000 */}
<DeclarationDoc>
Defined in header [`<condition_variable>`](/docs/cpp/library/headers/condition_variable)

  <Declaration language="cpp" since="C++11" code={`void notify_all_at_thread_exit( std::condition_variable& cond,
                                std::unique_lock<std::mutex> lk );`} />
</DeclarationDoc>

{/* source:cpp-thread-notify-all-at-thread-exit:0001 */}
`notify_all_at_thread_exit` provides a mechanism to notify other threads that a given thread has completely finished, including destroying all [`thread_local`](/docs/cpp/language/keywords/thread_local) objects. It operates as follows:

{/* source:cpp-thread-notify-all-at-thread-exit:0002 */}
- Ownership of the previously acquired lock `lk` is transferred to internal storage.

{/* source:cpp-thread-notify-all-at-thread-exit:0003 */}
- The execution environment is modified such that when the current thread exits, the condition variable `cond` is notified as if by `lk.unlock();` `cond.notify_all();`.

{/* source:cpp-thread-notify-all-at-thread-exit:0004 */}
The implied `lk.unlock()` is [sequenced after](/docs/cpp/library/atomic/memory_order) the destruction of all objects with [thread local storage duration](/docs/cpp/language/keywords/thread_local) associated with the current thread.

{/* source:cpp-thread-notify-all-at-thread-exit:0005 */}
If any of the following conditions is satisfied, the behavior is undefined:

{/* source:cpp-thread-notify-all-at-thread-exit:0006 */}
- `lk` is not locked by the calling thread.
- If some other threads are also waiting on `cond`, `lk.mutex()` is different from the mutex unlocked by the waiting functions (`wait`, `wait_for` and `wait_until`) called on `cond` by those threads.

### Notes

{/* source:cpp-thread-notify-all-at-thread-exit:0007 */}
An equivalent effect may be achieved with the facilities provided by [`std::promise`](/docs/cpp/library/thread/promise) or [`std::packaged_task`](/docs/cpp/library/thread/packaged_task).

{/* source:cpp-thread-notify-all-at-thread-exit:0008 */}
The supplied lock `lk` is held until the thread exits. Once this function has been called, no more threads may acquire the same lock in order to wait on `cond`. If some threads are waiting on this condition variable, ensure that the condition being waited for is satisfied while holding the lock on `lk`, and that this lock is not released and reacquired prior to calling `notify_all_at_thread_exit` to avoid confusion from spurious wakeups in other threads.

{/* source:cpp-thread-notify-all-at-thread-exit:0009 */}
In typical use cases, this function is the last thing called by a detached thread.

### Parameters

{/* source:cpp-thread-notify-all-at-thread-exit:0010 */}
<ParameterList>
  <Parameter name="cond">the condition variable to notify at thread exit</Parameter>
  <Parameter name="lk">the lock associated with the condition variable `cond`</Parameter>
</ParameterList>

### Return value

{/* source:cpp-thread-notify-all-at-thread-exit:0011 */}
(none)

### Example

{/* source:cpp-thread-notify-all-at-thread-exit:0012 */}
This partial code fragment illustrates how `notify_all_at_thread_exit` can be used to avoid accessing data that depends on thread locals while those thread locals are in the process of being destructed:

```cpp
#include <cassert>
#include <condition_variable>
#include <mutex>
#include <string>
#include <thread>
 
std::mutex m;
std::condition_variable cv;
 
bool ready = false;
std::string result; // some arbitrary type
 
void thread_func()
{
    thread_local std::string thread_local_data = "42";
 
    std::unique_lock<std::mutex> lk(m);
 
    // assign a value to result using thread_local data
    result = thread_local_data;
    ready = true;
 
    std::notify_all_at_thread_exit(cv, std::move(lk));
 
}   // 1. destroy thread_locals;
    // 2. unlock mutex;
    // 3. notify cv.
 
int main()
{
    std::thread t(thread_func);
    t.detach();
 
    // do other work
    // ...
 
    // wait for the detached thread
    std::unique_lock<std::mutex> lk(m);
    cv.wait(lk, []{ return ready; });
 
    // result is ready and thread_local destructors have finished, no UB
    assert(result == "42");
}
```

### Defect reports

{/* source:cpp-thread-notify-all-at-thread-exit:0013 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-thread-notify-all-at-thread-exit:0014 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2140} standard="C++11">
    <PublishedBehavior>the call to `notify_all_at_thread_exit`<br />synchronized with calls to functions waiting on `cond`</PublishedBehavior>
    <CorrectedBehavior>updated the synchronization<br />requirement</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-thread-notify-all-at-thread-exit:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`set_value_at_thread_exit`](/docs/cpp/library/thread/promise/set_value_at_thread_exit)</DescriptionTerm>
    <DescriptionBody>sets the result to specific value while delivering the notification only at thread exit (public member function of `std::promise<R>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`make_ready_at_thread_exit`](/docs/cpp/library/thread/packaged_task/make_ready_at_thread_exit)</DescriptionTerm>
    <DescriptionBody>executes the function ensuring that the result is ready only once the current thread exits (public member function of `std::packaged_task<R(Args...)>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
