---
title: "std::counting_semaphore<LeastMaxValue>::max"
description: "Returns the internal counter's maximum possible value, which is greater than or equal to LeastMaxValue."
source_url: "https://en.cppreference.com/w/cpp/thread/counting_semaphore/max"
language: "C++"
---

{/* source:cpp-thread-counting-semaphore-max:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr std::ptrdiff_t max() noexcept;`} />
</DeclarationDoc>

{/* source:cpp-thread-counting-semaphore-max:0001 */}
Returns the internal counter's maximum possible value, which is greater than or equal to `LeastMaxValue`.

### Return value

{/* source:cpp-thread-counting-semaphore-max:0002 */}
The internal counter's maximum possible value, as a [`std::ptrdiff_t`](/docs/cpp/library/utility/types/ptrdiff_t).

### Notes

{/* source:cpp-thread-counting-semaphore-max:0003 */}
For specialization `binary_semaphore`, `LeastMaxValue` is equal to `1`.

{/* source:cpp-thread-counting-semaphore-max:0004 */}
As its name indicates, the `LeastMaxValue` is the *minimum* max value, not the *actual* max value. Thus `max()` can yield a number larger than `LeastMaxValue`.
