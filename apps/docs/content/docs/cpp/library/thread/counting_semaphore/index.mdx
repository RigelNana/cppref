---
title: "std::counting_semaphore, std::binary_semaphore"
description: "A counting_semaphore is a lightweight synchronization primitive that can control access to a shared resource."
source_url: "https://en.cppreference.com/w/cpp/thread/counting_semaphore"
language: "C++"
---

{/* source:cpp-thread-counting-semaphore:0000 */}
<DeclarationDoc>
Defined in header [`<semaphore>`](/docs/cpp/library/headers/semaphore)

  <Declaration id="1" language="cpp" since="C++20" code={`template< std::ptrdiff_t LeastMaxValue = /* implementation-defined */ >
class counting_semaphore;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`using binary_semaphore = std::counting_semaphore<1>;`} />

  <DeclarationDescription>
    {/* source:cpp-thread-counting-semaphore:0001 */}
    1. A `counting_semaphore` is a lightweight synchronization primitive that can control access to a shared resource. Unlike a [`std::mutex`](/docs/cpp/library/thread/mutex), a `counting_semaphore` allows more than one concurrent access to the same resource, for at least `LeastMaxValue` concurrent accessors. The program is ill-formed if `LeastMaxValue` is negative.

    {/* source:cpp-thread-counting-semaphore:0002 */}
    2. `binary_semaphore` is an alias for specialization of `std::counting_semaphore` with `LeastMaxValue` being `1`. Implementations may implement `binary_semaphore` more efficiently than the default implementation of `std::counting_semaphore`.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-thread-counting-semaphore:0003 */}
A `counting_semaphore` contains an internal counter initialized by the constructor. This counter is decremented by calls to `acquire()` and related methods, and is incremented by calls to `release()`. When the counter is zero, `acquire()` blocks until the counter is incremented, but `try_acquire()` does not block; `try_acquire_for()` and `try_acquire_until()` block until the counter is incremented or a timeout is reached.

{/* source:cpp-thread-counting-semaphore:0004 */}
Similar to [`std::condition_variable::wait()`](/docs/cpp/library/thread/condition_variable/wait), `counting_semaphore`'s `try_acquire()` can spuriously fail.

{/* source:cpp-thread-counting-semaphore:0005 */}
Specializations of `std::counting_semaphore` are not [`DefaultConstructible`](/docs/cpp/library/named_req/DefaultConstructible), [`CopyConstructible`](/docs/cpp/library/named_req/CopyConstructible), [`MoveConstructible`](/docs/cpp/library/named_req/MoveConstructible), [`CopyAssignable`](/docs/cpp/library/named_req/CopyAssignable), or [`MoveAssignable`](/docs/cpp/library/named_req/MoveAssignable).

### Data Members

{/* source:cpp-thread-counting-semaphore:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`counter` (private)</DescriptionTerm>
    <DescriptionBody>The internal counter of type [`std::ptrdiff_t`](/docs/cpp/library/utility/types/ptrdiff_t). (exposition-only member object*)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Member functions

{/* source:cpp-thread-counting-semaphore:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/thread/counting_semaphore/counting_semaphore)</DescriptionTerm>
    <DescriptionBody>constructs a counting_semaphore (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`(destructor)`](/docs/cpp/library/thread/counting_semaphore/~counting_semaphore)</DescriptionTerm>
    <DescriptionBody>destructs the counting_semaphore (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`operator=` [deleted]</DescriptionTerm>
    <DescriptionBody>counting_semaphore is not assignable (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

##### Operations

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`release`](/docs/cpp/library/thread/counting_semaphore/release)</DescriptionTerm>
    <DescriptionBody>increments the internal counter and unblocks acquirers (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acquire`](/docs/cpp/library/thread/counting_semaphore/acquire)</DescriptionTerm>
    <DescriptionBody>decrements the internal counter or blocks until it can (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire`](/docs/cpp/library/thread/counting_semaphore/try_acquire)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter without blocking (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire_for`](/docs/cpp/library/thread/counting_semaphore/try_acquire_for)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter, blocking for up to a duration time (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire_until`](/docs/cpp/library/thread/counting_semaphore/try_acquire_until)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter, blocking until a point in time (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

##### Constants

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`max`](/docs/cpp/library/thread/counting_semaphore/max)</DescriptionTerm>
    <DescriptionBody>returns the maximum possible value of the internal counter (public static member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Notes

{/* source:cpp-thread-counting-semaphore:0008 */}
As its name indicates, the `LeastMaxValue` is the minimum max value, not the actual max value. Thus `max()` can yield a number larger than `LeastMaxValue`.

{/* source:cpp-thread-counting-semaphore:0009 */}
Unlike [`std::mutex`](/docs/cpp/library/thread/mutex) a `counting_semaphore` is not tied to threads of execution - acquiring a semaphore can occur on a different thread than releasing the semaphore, for example. All operations on `counting_semaphore` can be performed concurrently and without any relation to specific threads of execution, with the exception of the destructor which cannot be performed concurrently but can be performed on a different thread.

{/* source:cpp-thread-counting-semaphore:0010 */}
Semaphores are also often used for the semantics of signaling/notifying rather than mutual exclusion, by initializing the semaphore with `0` and thus blocking the receiver(s) that try to `acquire()`, until the notifier "signals" by invoking `release(n)`. In this respect semaphores can be considered alternatives to [`std::condition_variables`](/docs/cpp/library/thread/condition_variable), often with better performance.

{/* source:cpp-thread-counting-semaphore:0011 */}
| [Feature-test macro](/docs/cpp/library/utility/feature_test) | Value | Std | Feature |
| --- | ---: | --- | --- |
| [`__cpp_lib_semaphore`](/docs/cpp/library/feature_test#cpp_lib_semaphore) | [`201907L`](/docs/cpp/library/compiler_support/20#cpp_lib_semaphore_201907L) | (C++20) | `std::counting_semaphore`, `std::binary_semaphore` |

### Example

{/* source:cpp-thread-counting-semaphore:0012 */}
```cpp
#include <chrono>
#include <iostream>
#include <semaphore>
#include <thread>

// global binary semaphore instances
// object counts are set to zero
// objects are in non-signaled state
std::binary_semaphore
    smphSignalMainToThread{0},
    smphSignalThreadToMain{0};

void ThreadProc()
{
    // wait for a signal from the main proc
    // by attempting to decrement the semaphore
    smphSignalMainToThread.acquire();

    // this call blocks until the semaphore's count
    // is increased from the main proc

    std::cout << "[thread] Got the signal\n"; // response message

    // wait for 3 seconds to imitate some work
    // being done by the thread
    using namespace std::literals;
    std::this_thread::sleep_for(3s);

    std::cout << "[thread] Send the signal\n"; // message

    // signal the main proc back
    smphSignalThreadToMain.release();
}

int main()
{
    // create some worker thread
    std::thread thrWorker(ThreadProc);

    std::cout << "[main] Send the signal\n"; // message

    // signal the worker thread to start working
    // by increasing the semaphore's count
    smphSignalMainToThread.release();

    // wait until the worker thread is done doing the work
    // by attempting to decrement the semaphore's count
    smphSignalThreadToMain.acquire();

    std::cout << "[main] Got the signal\n"; // response message
    thrWorker.join();
}
```

Output:

```text
[main] Send the signal
[thread] Got the signal
[thread] Send the signal
[main] Got the signal
```
