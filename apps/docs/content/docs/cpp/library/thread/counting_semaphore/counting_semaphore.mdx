---
title: "std::counting_semaphore<LeastMaxValue>::counting_semaphore"
description: "Constructs an object of type std::counting_semaphore with the internal counter initialized to desired."
source_url: "https://en.cppreference.com/w/cpp/thread/counting_semaphore/counting_semaphore"
language: "C++"
---

{/* source:cpp-thread-counting-semaphore-counting-semaphore:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr explicit counting_semaphore( std::ptrdiff_t desired );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`counting_semaphore( const counting_semaphore& ) = delete;`} />

  <DeclarationDescription>
    {/* source:cpp-thread-counting-semaphore-counting-semaphore:0001 */}
    1. Constructs an object of type `std::counting_semaphore` with the internal counter initialized to `desired`.

    {/* source:cpp-thread-counting-semaphore-counting-semaphore:0002 */}
    2. Copy constructor is deleted.
  </DeclarationDescription>
</DeclarationDoc>

### Preconditions

{/* source:cpp-thread-counting-semaphore-counting-semaphore:0003 */}
1) Both `desired >= 0` and `desired <= max()` are `true`.

### Parameters

{/* source:cpp-thread-counting-semaphore-counting-semaphore:0004 */}
<ParameterList>
  <Parameter name="desired">the value to initialize counting_semaphore's counter with</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-thread-counting-semaphore-counting-semaphore:0005 */}
Throws nothing.
