---
title: "std::counting_semaphore<LeastMaxValue>::try_acquire_for"
description: "Tries to atomically decrement the internal counter by 1 if it is greater than 0; otherwise blocks until it is greater than 0 and can successfully decrement the internal counter, or the rel_time duration has been exceeded."
source_url: "https://en.cppreference.com/w/cpp/thread/counting_semaphore/try_acquire_for"
language: "C++"
---

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template< class Rep, class Period >
bool try_acquire_for( const std::chrono::duration<Rep, Period>& rel_time );`} />
</DeclarationDoc>

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0001 */}
Tries to atomically decrement the internal counter by `1` if it is greater than `0`; otherwise blocks until it is greater than `0` and can successfully decrement the internal counter, or the `rel_time` duration has been exceeded.

### Preconditions

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0002 */}
(none)

### Parameters

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0003 */}
<ParameterList>
  <Parameter name="rel_time">the minimum duration the function must wait for it to fail</Parameter>
</ParameterList>

### Return value

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0004 */}
`true` if it decremented the internal counter, otherwise `false`.

### Exceptions

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0005 */}
May throw [`std::system_error`](/docs/cpp/library/error/system_error) or a timeout-related exception.

### Notes

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0006 */}
In practice the function may take longer than `rel_time` to fail.

### See also

{/* source:cpp-thread-counting-semaphore-try-acquire-for:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`release`](/docs/cpp/library/thread/counting_semaphore/release)</DescriptionTerm>
    <DescriptionBody>increments the internal counter and unblocks acquirers (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`acquire`](/docs/cpp/library/thread/counting_semaphore/acquire)</DescriptionTerm>
    <DescriptionBody>decrements the internal counter or blocks until it can (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire`](/docs/cpp/library/thread/counting_semaphore/try_acquire)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter without blocking (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire_until`](/docs/cpp/library/thread/counting_semaphore/try_acquire_until)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter, blocking until a point in time (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
