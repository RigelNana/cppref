---
title: "std::counting_semaphore<LeastMaxValue>::release"
description: "Atomically increments the internal counter by the value of update. Any thread(s) waiting for the counter to be greater than 0, such as due to being blocked in acquire, will subsequently be unblocked."
source_url: "https://en.cppreference.com/w/cpp/thread/counting_semaphore/release"
language: "C++"
---

{/* source:cpp-thread-counting-semaphore-release:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`void release( std::ptrdiff_t update = 1 );`} />
</DeclarationDoc>

{/* source:cpp-thread-counting-semaphore-release:0001 */}
Atomically increments the internal counter by the value of `update`. Any thread(s) waiting for the counter to be greater than `0`, such as due to being blocked in `acquire`, will subsequently be unblocked.

{/* source:cpp-thread-counting-semaphore-release:0002 */}
This operation strongly happens before invocations of `try_acquire` that observe the result of the effects.

### Preconditions

{/* source:cpp-thread-counting-semaphore-release:0003 */}
Both `update >= 0` and `update <= max() - counter` are `true`, where `counter` is the value of the internal counter.

### Parameters

{/* source:cpp-thread-counting-semaphore-release:0004 */}
<ParameterList>
  <Parameter name="update">the amount to increment the internal counter by</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-thread-counting-semaphore-release:0005 */}
May throw [`std::system_error`](/docs/cpp/library/error/system_error).

### See also

{/* source:cpp-thread-counting-semaphore-release:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`acquire`](/docs/cpp/library/thread/counting_semaphore/acquire)</DescriptionTerm>
    <DescriptionBody>decrements the internal counter or blocks until it can (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire`](/docs/cpp/library/thread/counting_semaphore/try_acquire)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter without blocking (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire_for`](/docs/cpp/library/thread/counting_semaphore/try_acquire_for)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter, blocking for up to a duration time (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_acquire_until`](/docs/cpp/library/thread/counting_semaphore/try_acquire_until)</DescriptionTerm>
    <DescriptionBody>tries to decrement the internal counter, blocking until a point in time (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
