---
title: "std::recursive_timed_mutex::lock"
description: "Locks the mutex, blocks if the mutex is not available."
source_url: "https://en.cppreference.com/w/cpp/thread/recursive_timed_mutex/lock"
language: "C++"
---

{/* source:cpp-thread-recursive-timed-mutex-lock:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++11" code={`void lock();`} />
</DeclarationDoc>

{/* source:cpp-thread-recursive-timed-mutex-lock:0001 */}
Locks the mutex. If another thread has already locked the mutex, a call to `lock` will block execution until the lock is acquired.

{/* source:cpp-thread-recursive-timed-mutex-lock:0002 */}
A thread may call `lock` on a recursive mutex repeatedly. Ownership will only be released after the thread makes a matching number of calls to `unlock`.

{/* source:cpp-thread-recursive-timed-mutex-lock:0003 */}
The maximum number of levels of ownership is unspecified. An exception of type [`std::system_error`](/docs/cpp/library/error/system_error) will be thrown if this number is exceeded.

{/* source:cpp-thread-recursive-timed-mutex-lock:0004 */}
Prior [`unlock()`](/docs/cpp/library/thread/recursive_timed_mutex/unlock) operations on the same mutex *synchronize-with* (as defined in [`std::memory_order`](/docs/cpp/library/atomic/memory_order)) this operation.

### Parameters

{/* source:cpp-thread-recursive-timed-mutex-lock:0005 */}
(none)

### Return value

{/* source:cpp-thread-recursive-timed-mutex-lock:0006 */}
(none)

### Exceptions

{/* source:cpp-thread-recursive-timed-mutex-lock:0007 */}
Throws [`std::system_error`](/docs/cpp/library/error/system_error) when errors occur, including errors from the underlying operating system that would prevent `lock` from meeting its specifications. The mutex is not locked in the case of any exception being thrown.

### Notes

{/* source:cpp-thread-recursive-timed-mutex-lock:0008 */}
`lock()` is usually not called directly: [`std::unique_lock`](/docs/cpp/library/thread/unique_lock), [`std::scoped_lock`](/docs/cpp/library/thread/scoped_lock), and [`std::lock_guard`](/docs/cpp/library/thread/lock_guard) are used to manage exclusive locking.

### Example

{/* source:cpp-thread-recursive-timed-mutex-lock:0009 */}
This example shows how `lock` and `unlock` can be used to protect shared data.

```cpp
#include <chrono>
#include <iostream>
#include <mutex>
#include <thread>
 
int g_num = 0; // protected by g_num_mutex
std::mutex g_num_mutex;
 
void slow_increment(int id) 
{
    for (int i = 0; i < 3; ++i)
    {
        g_num_mutex.lock(); 
        ++g_num;
        // note, that the mutex also syncronizes the output
        std::cout << "id: " << id << ", g_num: " << g_num << '\n';
        g_num_mutex.unlock();
 
        std::this_thread::sleep_for(std::chrono::milliseconds(234));
    }
}
 
int main()
{
    std::thread t1{slow_increment, 0};
    std::thread t2{slow_increment, 1};
    t1.join();
    t2.join();
}
```

Possible output:

```text
id: 0, g_num: 1
id: 1, g_num: 2
id: 1, g_num: 3
id: 0, g_num: 4
id: 0, g_num: 5
id: 1, g_num: 6
```

### See also

{/* source:cpp-thread-recursive-timed-mutex-lock:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`try_lock`](/docs/cpp/library/thread/recursive_timed_mutex/try_lock)</DescriptionTerm>
    <DescriptionBody>tries to lock the mutex, returns if the mutex is not available (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_lock_for`](/docs/cpp/library/thread/recursive_timed_mutex/try_lock_for)</DescriptionTerm>
    <DescriptionBody>tries to lock the mutex, returns if the mutex has been unavailable for the specified timeout duration (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`try_lock_until`](/docs/cpp/library/thread/recursive_timed_mutex/try_lock_until)</DescriptionTerm>
    <DescriptionBody>tries to lock the mutex, returns if the mutex has been unavailable until specified time point has been reached (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`unlock`](/docs/cpp/library/thread/recursive_timed_mutex/unlock)</DescriptionTerm>
    <DescriptionBody>unlocks the mutex (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/library/thread/mtx_lock) for `mtx_lock`

