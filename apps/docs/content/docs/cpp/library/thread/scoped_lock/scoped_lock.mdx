---
title: "std::scoped_lock<MutexTypes...>::scoped_lock"
description: "Acquires ownership of the given mutexes m."
source_url: "https://en.cppreference.com/w/cpp/thread/scoped_lock/scoped_lock"
language: "C++"
---

{/* source:cpp-thread-scoped-lock-scoped-lock:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`explicit scoped_lock( MutexTypes&... m );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`scoped_lock( std::adopt_lock_t, MutexTypes&... m );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`scoped_lock( const scoped_lock& ) = delete;`} />

  <DeclarationDescription>
    {/* source:cpp-thread-scoped-lock-scoped-lock:0001 */}
    Acquires ownership of the given mutexes `m`.

    {/* source:cpp-thread-scoped-lock-scoped-lock:0002 */}
    1. If `sizeof...(MutexTypes) == 0`, does nothing. Otherwise, if `sizeof...(MutexTypes) == 1`, effectively calls `m.lock()`. Otherwise, effectively calls `std::lock(m...)`.

    {/* source:cpp-thread-scoped-lock-scoped-lock:0003 */}
    2. Acquires ownership of the mutexes `m...` without attempting to lock any of them. The behavior is undefined unless the current thread holds a non-shared lock (i.e., a lock acquired by `lock`, `try_lock`, `try_lock_for`, or `try_lock_until`) on each object in `m...`.

    {/* source:cpp-thread-scoped-lock-scoped-lock:0004 */}
    3. Copy constructor is deleted.

    {/* source:cpp-thread-scoped-lock-scoped-lock:0005 */}
    The behavior is undefined if `m` is destroyed before the `scoped_lock` object is.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-thread-scoped-lock-scoped-lock:0006 */}
<ParameterList>
  <Parameter name="m">mutexes to acquire ownership of</Parameter>
</ParameterList>

### Exceptions

{/* source:cpp-thread-scoped-lock-scoped-lock:0007 */}
1. Throws any exceptions thrown by `m.lock()`.

{/* source:cpp-thread-scoped-lock-scoped-lock:0008 */}
2. Throws nothing.

### Defect reports

{/* source:cpp-thread-scoped-lock-scoped-lock:0009 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-thread-scoped-lock-scoped-lock:0010 */}
<DefectReportList>
  <DefectReport kind="paper" id="P0739R0" standard="C++17">
    <PublishedBehavior>`adopt_lock_t` parameter was last, impeding class template argument deduction</PublishedBehavior>
    <CorrectedBehavior>moved to first</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
