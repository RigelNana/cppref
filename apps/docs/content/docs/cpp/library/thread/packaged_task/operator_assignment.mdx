---
title: "std::packaged_task<R(Args...)>::operator="
description: "Releases the shared state, if any, destroys the previously-held task, and moves the shared state and the task owned by rhs into *this."
source_url: "https://en.cppreference.com/w/cpp/thread/packaged_task/operator="
language: "C++"
---

{/* source:cpp-thread-packaged-task-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++11" code={`packaged_task& operator=( const packaged_task& ) = delete;`} />
  <Declaration id="2" language="cpp" since="C++11" code={`packaged_task& operator=( packaged_task&& rhs ) noexcept;`} />
</DeclarationDoc>

{/* source:cpp-thread-packaged-task-operator:0001 */}
1. Copy assignment operator is deleted, `std::packaged_task` is move-only.

{/* source:cpp-thread-packaged-task-operator:0002 */}
2. Releases the shared state, if any, destroys the previously-held task, and moves the shared state and the task owned by `rhs` into `*this`. `rhs` is left without a shared state and with a moved-from task.

### Parameters

{/* source:cpp-thread-packaged-task-operator:0003 */}
<ParameterList>
  <Parameter name="rhs">the `std::packaged_task` to move from</Parameter>
</ParameterList>

### Example

{/* source:cpp-thread-packaged-task-operator:0004 */}
This section is incomplete

Reason: no example

### Defect reports

{/* source:cpp-thread-packaged-task-operator:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-thread-packaged-task-operator:0006 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2067} standard="C++11">
    <PublishedBehavior>the parameter type of the copy assignment operator was `packaged_task&`</PublishedBehavior>
    <CorrectedBehavior>added `const`</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
