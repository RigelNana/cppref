---
title: "std::coroutine_handle<Promise>::from_address"
description: "Creates a coroutine_handle from a null pointer value or an underlying address of another coroutine_handle. The underlying address of return value is addr."
source_url: "https://en.cppreference.com/w/cpp/coroutine/coroutine_handle/from_address"
language: "C++"
---

{/* source:cpp-coroutine-coroutine-handle-from-address:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`static constexpr coroutine_handle from_address( void *addr );`} />
</DeclarationDoc>

{/* source:cpp-coroutine-coroutine-handle-from-address:0001 */}
Creates a `coroutine_handle` from a null pointer value or an underlying address of another `coroutine_handle`. The underlying address of return value is `addr`.

{/* source:cpp-coroutine-coroutine-handle-from-address:0002 */}
The behavior is undefined if `addr` is neither a null pointer value nor an underlying address of a `coroutine_handle`. The behavior is also undefined if the `addr` is an underlying address of a `std::coroutine_handle<P1>`, where both `Promise` and `P1` are not `void`, and `P1` is different from `Promise`.

{/* source:cpp-coroutine-coroutine-handle-from-address:0003 */}
This function is not declared for specialization `std::coroutine_handle<std::noop_coroutine_promise>`.

### Parameters

{/* source:cpp-coroutine-coroutine-handle-from-address:0004 */}
<ParameterList>
  <Parameter name="addr">underlying address to import</Parameter>
</ParameterList>

### Return value

{/* source:cpp-coroutine-coroutine-handle-from-address:0005 */}
A `std::coroutine_handle<Promise>` whose underlying address is `addr`.

### Notes

{/* source:cpp-coroutine-coroutine-handle-from-address:0006 */}
If `addr` is not a null pointer value, it must be obtained from a prior call to `address()` on a `coroutine_handle` referring to some coroutine.

### Example

{/* source:cpp-coroutine-coroutine-handle-from-address:0007 */}
This section is incomplete Reason: no example

### See also

{/* source:cpp-coroutine-coroutine-handle-from-address:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/utility/coroutine/coroutine_handle/coroutine_handle)</DescriptionTerm>
    <DescriptionBody>constructs a `coroutine_handle` object (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`from_promise`](/docs/cpp/library/utility/coroutine/coroutine_handle/from_promise)</DescriptionTerm>
    <DescriptionBody>creates a `coroutine_handle` from the promise object of a coroutine (public static member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`noop_coroutine`](/docs/cpp/library/utility/coroutine/noop_coroutine)</DescriptionTerm>
    <DescriptionBody>creates a coroutine handle that has no observable effects when resumed or destroyed (function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
