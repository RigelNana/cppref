---
title: "std::coroutine_handle<Promise>::operator="
description: "Replaces the underlying address."
source_url: "https://en.cppreference.com/w/cpp/coroutine/coroutine_handle/operator="
language: "C++"
---

{/* source:cpp-coroutine-coroutine-handle-operator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`coroutine_handle& operator=( std::nullptr_t ) noexcept;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`coroutine_handle& operator=( const coroutine_handle& other ) = default;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`coroutine_handle& operator=( coroutine_handle&& other ) = default;`} />

  <DeclarationDescription>
    {/* source:cpp-coroutine-coroutine-handle-operator:0001 */}
    Replaces the underlying address.

    {/* source:cpp-coroutine-coroutine-handle-operator:0002 */}
    1) Replaces the underlying address with a null pointer value. After assignment, `*this` does not refer to a coroutine. This assignment operator is not declared for specialization `std::coroutine_handle<std::noop_coroutine_promise>`.

    {/* source:cpp-coroutine-coroutine-handle-operator:0003 */}
    2,3) Replaces the underlying address with the one of `other`. Copy and move assignment operators are equivalent to implicitly declared ones.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-coroutine-coroutine-handle-operator:0004 */}
<ParameterList>
  <Parameter name="other">another `coroutine_handle` to assign from</Parameter>
</ParameterList>

### Return value

{/* source:cpp-coroutine-coroutine-handle-operator:0005 */}
`*this`
