---
title: "std::coroutine_handle<Promise>::operator(), std::coroutine_handle<Promise>::resume"
description: "Resumes the execution of the coroutine to which *this refers, or does nothing if the coroutine is a no-op coroutine."
source_url: "https://en.cppreference.com/w/cpp/coroutine/coroutine_handle/resume"
language: "C++"
---

{/* source:cpp-coroutine-coroutine-handle-resume:0000 */}
<DeclarationDoc>
**Member of other specializations**

  <Declaration id="1" language="cpp" since="C++20" code={`void operator()() const;
void resume() const;`} />

**Member of specialization** `std::coroutine_handle<std::noop_coroutine_promise>`

  <Declaration id="2" language="cpp" since="C++20" code={`constexpr void operator()() const noexcept;
constexpr void resume() const noexcept;`} />

  <DeclarationDescription>
    {/* source:cpp-coroutine-coroutine-handle-resume:0001 */}
    1. Resumes the execution of the coroutine to which `*this` refers, or does nothing if the coroutine is a no-op coroutine.

    {/* source:cpp-coroutine-coroutine-handle-resume:0002 */}
    2. Does nothing.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-coroutine-coroutine-handle-resume:0003 */}
The behavior is undefined if `*this` does not refer to suspended coroutine, or the coroutine is not a no-op coroutine and suspended at its final suspend point. A concurrent resumption of the coroutine may result in a data race.

{/* source:cpp-coroutine-coroutine-handle-resume:0004 */}
Resumption of a coroutine on an execution agent other than the one on which it was suspended has implementation-defined behavior unless each execution agent either is a thread represented by [`std::thread`](/docs/cpp/library/thread/thread) or [`std::jthread`](/docs/cpp/library/thread/jthread), or is the thread executing `main`.

### Parameters

{/* source:cpp-coroutine-coroutine-handle-resume:0005 */}
(none)

### Return value

{/* source:cpp-coroutine-coroutine-handle-resume:0006 */}
(none)

### Exceptions

{/* source:cpp-coroutine-coroutine-handle-resume:0007 */}
If an exception is thrown from the execution of the coroutine, the exception is caught and `unhandled_exception` is called on the coroutine's promise object. If the call to `unhandled_exception` throws or rethrows an exception, that exception is propagated.

### Notes

{/* source:cpp-coroutine-coroutine-handle-resume:0008 */}
A coroutine that is resumed on a different execution agent should avoid relying on consistent thread identity throughout, such as holding a mutex object across a suspend point.

### Example

{/* source:cpp-coroutine-coroutine-handle-resume:0009 */}
This section is incomplete Reason: no example

### See also

{/* source:cpp-coroutine-coroutine-handle-resume:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`destroy`](/docs/cpp/library/utility/coroutine/coroutine_handle/destroy)</DescriptionTerm>
    <DescriptionBody>destroys a coroutine (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
