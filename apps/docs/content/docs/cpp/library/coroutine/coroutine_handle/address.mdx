---
title: "std::coroutine_handle<Promise>::address"
description: "Returns the underlying address of the coroutine_handle. The return value is non-null if and only if the current value of the coroutine_handle is obtained from a promise object of a coroutine."
source_url: "https://en.cppreference.com/w/cpp/coroutine/coroutine_handle/address"
language: "C++"
---

{/* source:cpp-coroutine-coroutine-handle-address:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`constexpr void* address() const noexcept;`} />
</DeclarationDoc>

{/* source:cpp-coroutine-coroutine-handle-address:0001 */}
Returns the underlying address of the `coroutine_handle`. The return value is non-null if and only if the current value of the `coroutine_handle` is obtained from a promise object of a coroutine.

### Parameters

{/* source:cpp-coroutine-coroutine-handle-address:0002 */}
(none)

### Return value

{/* source:cpp-coroutine-coroutine-handle-address:0003 */}
The underlying address.

### Notes

{/* source:cpp-coroutine-coroutine-handle-address:0004 */}
The return value is non-null for specialization `std::noop_coroutine_handle`, because a `std::noop_coroutine_handle` cannot be created without referring to a no-op coroutine.

### See also

{/* source:cpp-coroutine-coroutine-handle-address:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`from_address`](/docs/cpp/library/utility/coroutine/coroutine_handle/from_address)</DescriptionTerm>
    <DescriptionBody>imports a coroutine from a pointer (public static member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
