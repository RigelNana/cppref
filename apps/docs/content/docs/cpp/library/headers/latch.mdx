---
title: "Standard library header <latch> (C++20)"
description: "This header is part of the thread support library."
source_url: "https://en.cppreference.com/w/cpp/header/latch"
language: "C++"
---

{/* source:cpp-header-latch:0000 */}
This header is part of the [`thread support`](/docs/cpp/library/atomic) library.

{/* source:cpp-header-latch:0001 */}
## Classes
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`latch`](/docs/cpp/library/thread/latch) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>single-use thread barrier (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

## Synopsis

{/* source:cpp-header-latch:0002 */}
```cpp
namespace std {
  class latch;
}
```

### Class std::latch

{/* source:cpp-header-latch:0003 */}
```cpp
namespace std {
  class latch {
  public:
    static constexpr ptrdiff_t max() noexcept;
 
    constexpr explicit latch(ptrdiff_t expected);
    ~latch();
 
    latch(const latch&) = delete;
    latch& operator=(const latch&) = delete;
 
    void count_down(ptrdiff_t update = 1);
    bool try_wait() const noexcept;
    void wait() const;
    void arrive_and_wait(ptrdiff_t update = 1);
 
  private:
    ptrdiff_t counter;  // exposition only
  };
}
```
