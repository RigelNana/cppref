---
title: "Standard library header <concepts> (C++20)"
description: "This header is part of the concepts library."
source_url: "https://en.cppreference.com/w/cpp/header/concepts"
language: "C++"
---

{/* source:cpp-header-concepts:0000 */}
This header is part of the [`concepts`](/docs/cpp/library/concepts) library.

{/* source:cpp-header-concepts:0001 */}
## Concepts

#### Core language concepts

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`same_as`](/docs/cpp/library/concepts/same_as)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is the same as another type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`derived_from`](/docs/cpp/library/concepts/derived_from)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is derived from another type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`convertible_to`](/docs/cpp/library/concepts/convertible_to)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is implicitly convertible to another type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`common_reference_with`](/docs/cpp/library/concepts/common_reference_with)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that two types share a common reference type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`common_with`](/docs/cpp/library/concepts/common_with)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that two types share a common type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`integral`](/docs/cpp/library/concepts/integral)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is an integral type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`signed_integral`](/docs/cpp/library/concepts/signed_integral)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is an integral type that is signed (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`unsigned_integral`](/docs/cpp/library/concepts/unsigned_integral)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is an integral type that is unsigned (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`floating_point`](/docs/cpp/library/concepts/floating_point)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is a floating-point type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`assignable_from`](/docs/cpp/library/concepts/assignable_from)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is assignable from another type (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`swappable swappable_with`](/docs/cpp/library/concepts/swappable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type can be swapped or that two types can be swapped with each other (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`destructible`](/docs/cpp/library/concepts/destructible)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of the type can be destroyed (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`constructible_from`](/docs/cpp/library/concepts/constructible_from)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a variable of the type can be constructed from or bound to a set of argument types (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`default_initializable`](/docs/cpp/library/concepts/default_initializable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be default constructed (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`move_constructible`](/docs/cpp/library/concepts/move_constructible)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be move constructed (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`copy_constructible`](/docs/cpp/library/concepts/copy_constructible)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be copy constructed and move constructed (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Comparison concepts

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`equality_comparable equality_comparable_with`](/docs/cpp/library/concepts/equality_comparable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that `operator ==` is an equivalence relation (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`totally_ordered totally_ordered_with`](/docs/cpp/library/concepts/totally_ordered)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that the comparison operators on the type yield a total order (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Object concepts

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`movable`](/docs/cpp/library/concepts/movable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be moved and swapped (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`copyable`](/docs/cpp/library/concepts/copyable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be copied, moved, and swapped (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`semiregular`](/docs/cpp/library/concepts/semiregular)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an object of a type can be copied, moved, swapped, and default constructed (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`regular`](/docs/cpp/library/concepts/regular)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is regular, that is, it is both [`semiregular`](/docs/cpp/library/concepts/semiregular) and [`equality_comparable`](/docs/cpp/library/concepts/equality_comparable) (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Callable concepts

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`invocable regular_invocable`](/docs/cpp/library/concepts/invocable)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a callable type can be invoked with a given set of argument types (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`predicate`](/docs/cpp/library/concepts/predicate)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a callable type is a Boolean predicate (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`relation`](/docs/cpp/library/concepts/relation)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a callable type is a binary relation (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`equivalence_relation`](/docs/cpp/library/concepts/equivalence_relation)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a [`relation`](/docs/cpp/library/concepts/relation) imposes an equivalence relation (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`strict_weak_order`](/docs/cpp/library/concepts/strict_weak_order)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a [`relation`](/docs/cpp/library/concepts/relation) imposes a strict weak ordering (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

#### Customization point objects

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::swap`](/docs/cpp/library/utility/ranges/swap)<InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>swaps the values of two objects (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-header-concepts:0002 */}
## Synopsis

```cpp
```cpp
// all freestanding
namespace std {
  // language-related concepts
  // concept same_as
  template<class T, class U>
  concept same_as = /* see description */;
 
  // concept derived_from
  template<class Derived, class Base>
  concept derived_from = /* see description */;
 
  // concept convertible_to
  template<class From, class To>
  concept convertible_to = /* see description */;
 
  // concept common_reference_with
  template<class T, class U>
  concept common_reference_with = /* see description */;
 
  // concept common_with
  template<class T, class U>
  concept common_with = /* see description */;
 
  // arithmetic concepts
  template<class T>
  concept integral = /* see description */;
  template<class T>
  concept signed_integral = /* see description */;
  template<class T>
  concept unsigned_integral = /* see description */;
  template<class T>
  concept floating_point = /* see description */;
 
  // concept assignable_from
  template<class LHS, class RHS>
  concept assignable_from = /* see description */;
 
  // concept swappable
  namespace ranges {
    inline namespace /* unspecified */ {
      inline constexpr /* unspecified */ swap = /* unspecified */;
    }
  }
  template<class T>
  concept swappable = /* see description */;
  template<class T, class U>
  concept swappable_with = /* see description */;
 
  // concept destructible
  template<class T>
  concept destructible = /* see description */;
 
  // concept constructible_from
  template<class T, class... Args>
  concept constructible_from = /* see description */;
 
  // concept default_initializable
  template<class T>
  concept default_initializable = /* see description */;
 
  // concept move_constructible
  template<class T>
  concept move_constructible = /* see description */;
 
  // concept copy_constructible
  template<class T>
  concept copy_constructible = /* see description */;
 
  // comparison concepts
  // concept equality_comparable
  template<class T>
  concept equality_comparable = /* see description */;
  template<class T, class U>
  concept equality_comparable_with = /* see description */;
 
  // concept totally_ordered
  template<class T>
  concept totally_ordered = /* see description */;
  template<class T, class U>
  concept totally_ordered_with = /* see description */;
 
  // object concepts
  template<class T>
  concept movable = /* see description */;
  template<class T>
  concept copyable = /* see description */;
  template<class T>
  concept semiregular = /* see description */;
  template<class T>
  concept regular = /* see description */;
 
  // callable concepts
  // concept invocable
  template<class F, class... Args>
  concept invocable = /* see description */;
 
  // concept regular_invocable
  template<class F, class... Args>
  concept regular_invocable = /* see description */;
 
  // concept predicate
  template<class F, class... Args>
  concept predicate = /* see description */;
 
  // concept relation
  template<class R, class T, class U>
  concept relation = /* see description */;
 
  // concept equivalence_relation
  template<class R, class T, class U>
  concept equivalence_relation = /* see description */;
 
  // concept strict_weak_order
  template<class R, class T, class U>
  concept strict_weak_order = /* see description */;
}
```
```

{/* source:cpp-header-concepts:0003 */}
### Helper concept boolean-testable

```cpp
```cpp
template<class T>
concept /*boolean-testable-impl*/ = convertible_to<T, bool>; // exposition only;
 
template<class T>
concept boolean-testable = // exposition only
  /*boolean-testable-impl*/<T> && requires(T&& t) {
    {
      !std::forward<T>(t)
    } -> /*boolean-testable-impl*/;
  };
```
```

{/* source:cpp-header-concepts:0004 */}
### Concept same_as

```cpp
```cpp
template<class T, class U>
concept /*same-as-impl*/ = is_same_v<T, U>; // exposition only
 
template<class T, class U>
concept same_as = /*same-as-impl*/<T, U> && /*same-as-impl*/<U, T>;
```
```

{/* source:cpp-header-concepts:0005 */}
### Concept derived_from

```cpp
```cpp
template<class Derived, class Base>
concept derived_from = is_base_of_v<Base, Derived> &&
                       is_convertible_v<const volatile Derived*, const volatile Base*>;
```
```

{/* source:cpp-header-concepts:0006 */}
### Concept convertible_to

```cpp
```cpp
template<class From, class To>
concept convertible_to =
  is_convertible_v<From, To> && requires { static_cast<To>(declval<From>()); };
```
```

{/* source:cpp-header-concepts:0007 */}
### Concept common_reference_with

```cpp
```cpp
template<class T, class U>
concept common_reference_with =
  same_as<common_reference_t<T, U>, common_reference_t<U, T>> &&
  convertible_to<T, common_reference_t<T, U>> &&
  convertible_to<U, common_reference_t<T, U>>;
```
```

{/* source:cpp-header-concepts:0008 */}
### Concept common_with

```cpp
```cpp
template<class T, class U>
concept common_with =
  same_as<common_type_t<T, U>, common_type_t<U, T>> &&
  requires {
    static_cast<common_type_t<T, U>>(declval<T>());
    static_cast<common_type_t<T, U>>(declval<U>());
  } &&
  common_reference_with<add_lvalue_reference_t<const T>,
                        add_lvalue_reference_t<const U>> &&
  common_reference_with<
    add_lvalue_reference_t<common_type_t<T, U>>,
    common_reference_t<add_lvalue_reference_t<const T>, add_lvalue_reference_t<const U>>>;
```
```

{/* source:cpp-header-concepts:0009 */}
### Concept integral

```cpp
```cpp
template<class T>
concept integral = is_integral_v<T>;
```
```

{/* source:cpp-header-concepts:0010 */}
### Concept signed_integral

```cpp
```cpp
template<class T>
concept signed_integral = integral<T> && is_signed_v<T>;
```
```

{/* source:cpp-header-concepts:0011 */}
### Concept unsigned_integral

```cpp
```cpp
template<class T>
concept unsigned_integral = integral<T> && !signed_integral<T>;
```
```

{/* source:cpp-header-concepts:0012 */}
### Concept floating_point

```cpp
```cpp
template<class T>
concept floating_point = is_floating_point_v<T>;
```
```

{/* source:cpp-header-concepts:0013 */}
### Concept assignable_from

```cpp
```cpp
template<class LHS, class RHS>
concept assignable_from =
  is_lvalue_reference_v<LHS> &&
  common_reference_with<const remove_reference_t<LHS>&, const remove_reference_t<RHS>&> &&
  requires(LHS lhs, RHS&& rhs) {
    {
      lhs = std::forward<RHS>(rhs)
    } -> same_as<LHS>;
  };
```
```

{/* source:cpp-header-concepts:0014 */}
### Concept swappable

```cpp
```cpp
template<class T>
concept swappable = requires(T& a, T& b) { ranges::swap(a, b); };
```
```

{/* source:cpp-header-concepts:0015 */}
### Concept swappable_with

```cpp
```cpp
template<class T, class U>
concept swappable_with = common_reference_with<T, U> && requires(T&& t, U&& u) {
  ranges::swap(std::forward<T>(t), std::forward<T>(t));
  ranges::swap(std::forward<U>(u), std::forward<U>(u));
  ranges::swap(std::forward<T>(t), std::forward<U>(u));
  ranges::swap(std::forward<U>(u), std::forward<T>(t));
};
```
```

{/* source:cpp-header-concepts:0016 */}
### Concept destructible

```cpp
```cpp
template<class T>
concept destructible = is_nothrow_destructible_v<T>;
```
```

{/* source:cpp-header-concepts:0017 */}
### Concept constructible_from

```cpp
```cpp
template<class T, class... Args>
concept constructible_from = destructible<T> && is_constructible_v<T, Args...>;
```
```

{/* source:cpp-header-concepts:0018 */}
### Concept default_initializable

```cpp
```cpp
template<class T>
constexpr bool /*is-default-initializable*/ = /* see description */; // exposition only
 
template<class T>
concept default_initializable =
  constructible_from<T> && requires { T{}; } && /*is-default-initializable*/<T>;
```
```

{/* source:cpp-header-concepts:0019 */}
### Concept move_constructible

```cpp
```cpp
template<class T>
concept move_constructible = constructible_from<T, T> && convertible_to<T, T>;
```
```

{/* source:cpp-header-concepts:0020 */}
### Concept copy_constructible

```cpp
```cpp
template<class T>
concept copy_constructible =
  move_constructible<T> && constructible_from<T, T&> && convertible_to<T&, T> &&
  constructible_from<T, const T&> && convertible_to<const T&, T> &&
  constructible_from<T, const T> && convertible_to<const T, T>;
```
```

{/* source:cpp-header-concepts:0021 */}
### Concept equality_comparable

```cpp
```cpp
template<class T, class U>
concept /*weakly-equality-comparable-with*/ = // exposition only
  requires(const remove_reference_t<T>& t, const remove_reference_t<U>& u) {
    { t == u } -> boolean-testable;
    { t != u } -> boolean-testable;
    { u == t } -> boolean-testable;
    { u != t } -> boolean-testable;
  };
 
template<class T>
concept equality_comparable = /*weakly-equality-comparable-with*/<T, T>;
```
```

{/* source:cpp-header-concepts:0022 */}
### Concept equality_comparable_with

```cpp
```cpp
template<class T, class U, class C = common_reference_t<const T&, const U&>>
concept /*comparison-common-type-with-impl*/ = // exposition only
  same_as<common_reference_t<const T&, const U&>,
          common_reference_t<const U&, const T&>> &&
  requires {
    requires convertible_to<const T&, const C&> || convertible_to<T, const C&>;
    requires convertible_to<const U&, const C&> || convertible_to<U, const C&>;
  };
 
template<class T, class U>
concept /*comparison-common-type-with*/ = // exposition only
  /*comparison-common-type-with-impl*/<remove_cvref_t<T>, remove_cvref_t<U>>;
 
template<class T, class U>
concept equality_comparable_with =
  equality_comparable<T> && equality_comparable<U> &&
  /*comparison-common-type-with*/<T, U> &&
  equality_comparable<
    common_reference_t<const remove_reference_t<T>&, const remove_reference_t<U>&>> &&
  /*weakly-equality-comparable-with*/<T, U>;
```
```

{/* source:cpp-header-concepts:0023 */}
### Helper concept partially-ordered-with

Defined in header [`<compare>`](/docs/cpp/library/headers/compare#Concept_three_way_comparable)

{/* source:cpp-header-concepts:0024 */}
```cpp
```cpp
template<class T, class U>
concept /*partially-ordered-with*/ = // exposition only
  requires(const remove_reference_t<T>& t, const remove_reference_t<U>& u) {
    { t <  u } -> boolean-testable;
    { t >  u } -> boolean-testable;
    { t <= u } -> boolean-testable;
    { t >= u } -> boolean-testable;
    { u <  t } -> boolean-testable;
    { u >  t } -> boolean-testable;
    { u <= t } -> boolean-testable;
    { u >= t } -> boolean-testable;
  };
```
```

{/* source:cpp-header-concepts:0025 */}
### Concept totally_ordered

```cpp
```cpp
template<class T>
concept totally_ordered = equality_comparable<T> && /*partially-ordered-with*/<T, T>;
```
```

{/* source:cpp-header-concepts:0026 */}
### Concept totally_ordered_with

```cpp
```cpp
template<class T, class U>
concept totally_ordered_with =
  totally_ordered<T> && totally_ordered<U> && equality_comparable_with<T, U> &&
  totally_ordered<
    common_reference_t<const remove_reference_t<T>&, const remove_reference_t<U>&>> &&
  /*partially-ordered-with*/<T, U>;
```
```

{/* source:cpp-header-concepts:0027 */}
### Concept movable

```cpp
```cpp
template<class T>
concept movable =
  is_object_v<T> && move_constructible<T> && assignable_from<T&, T> && swappable<T>;
```
```

{/* source:cpp-header-concepts:0028 */}
### Concept copyable

```cpp
```cpp
template<class T>
concept copyable = copy_constructible<T> && movable<T> && assignable_from<T&, T&> &&
                   assignable_from<T&, const T&> && assignable_from<T&, const T>;
```
```

{/* source:cpp-header-concepts:0029 */}
### Concept semiregular

```cpp
```cpp
template<class T>
concept semiregular = copyable<T> && default_initializable<T>;
```
```

{/* source:cpp-header-concepts:0030 */}
### Concept regular

```cpp
```cpp
template<class T>
concept regular = semiregular<T> && equality_comparable<T>;
```
```

{/* source:cpp-header-concepts:0031 */}
### Concept invocable

```cpp
```cpp
template<class F, class... Args>
concept invocable = requires(F&& f, Args&&... args) {
  invoke(std::forward<F>(f),
         std::forward<Args>(args)...); // not required to be equality-preserving
};
```
```

{/* source:cpp-header-concepts:0032 */}
### Concept regular_invocable

```cpp
```cpp
template<class F, class... Args>
  concept regular_invocable = invocable<F, Args...>;
```
```

{/* source:cpp-header-concepts:0033 */}
### Concept predicate

```cpp
```cpp
template<class F, class... Args>
concept predicate =
  regular_invocable<F, Args...> && boolean-testable<invoke_result_t<F, Args...>>;
```
```

{/* source:cpp-header-concepts:0034 */}
### Concept relation

```cpp
```cpp
template<class R, class T, class U>
concept relation =
  predicate<R, T, T> && predicate<R, U, U> && predicate<R, T, U> && predicate<R, U, T>;
```
```

{/* source:cpp-header-concepts:0035 */}
### Concept equivalence_relation

```cpp
```cpp
template<class R, class T, class U>
concept equivalence_relation = relation<R, T, U>;
```
```

{/* source:cpp-header-concepts:0036 */}
### Concept strict_weak_order

```cpp
```cpp
template<class R, class T, class U>
concept strict_weak_order = relation<R, T, U>;
```
```
