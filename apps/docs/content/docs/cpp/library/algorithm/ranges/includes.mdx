---
title: "std::ranges::includes"
description: "Returns true if the projections of the sorted range [first2, last2) is a subsequence of the projections of the sorted range [first1, last1)."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/includes"
language: "C++"
---

{/* source:cpp-algorithm-ranges-includes:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< std::input_iterator I1, std::sentinel_for<I1> S1,
          std::input_iterator I2, std::sentinel_for<I2> S2,
          class Proj1 = std::identity, class Proj2 = std::identity,
          std::indirect_strict_weak_order<
              std::projected<I1, Proj1>,
              std::projected<I2, Proj2>> Comp = ranges::less >
constexpr bool
    includes( I1 first1, S1 last1, I2 first2, S2 last2,

              Comp comp = {}, Proj1 proj1 = {}, Proj2 proj2 = {} )`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< ranges::input_range R1, ranges::input_range R2,
          class Proj1 = std::identity, class Proj2 = std::identity,
          std::indirect_strict_weak_order<
              std::projected<ranges::iterator_t<R1>, Proj1>,
              std::projected<ranges::iterator_t<R2>, Proj2>> Comp = ranges::less >
constexpr bool

    includes( R1&& r1, R2&& r2, Comp comp = {}, Proj1 proj1 = {}, Proj2 proj2 = {} )`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-includes:0001 */}
1. 1) Returns `true` if the projections of the sorted range `[first2, last2)` is a [subsequence](https://en.wikipedia.org/wiki/subsequence) of the projections of the sorted range `[first1, last1)`.

{/* source:cpp-algorithm-ranges-includes:0002 */}
2. 2) Same as (1), but uses `r1` and `r2` as the source ranges, as if by using `ranges::begin(r1)` and `ranges::begin(r2)` as `first1` and `first2` respectively, and `ranges::end(r1)` and `ranges::end(r2)` as `last1` and `last2` respectively.

{/* source:cpp-algorithm-ranges-includes:0003 */}
Both ranges must be sorted with the given comparison function `comp`. A subsequence need not be contiguous.

{/* source:cpp-algorithm-ranges-includes:0004 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-algorithm-ranges-includes:0005 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-algorithm-ranges-includes:0006 */}
<ParameterList>
  <Parameter name="first1, last1">the sorted range of elements to examine</Parameter>
  <Parameter name="r1">the sorted range of elements to examine</Parameter>
  <Parameter name="first2, last2">the sorted range of elements to search for</Parameter>
  <Parameter name="r2">the sorted range of elements to search for</Parameter>
  <Parameter name="comp">comparison function to apply to the projected elements</Parameter>
  <Parameter name="proj1">projection to apply to the elements in the first range</Parameter>
  <Parameter name="proj2">projection to apply to the elements in the second range</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-includes:0007 */}
`true` if `[first2, last2)` is a subsequence of `[first1, last1)`; otherwise `false`.

### Complexity

{/* source:cpp-algorithm-ranges-includes:0008 */}
At most 2·(N1+N2-1) comparisons, where N1 is `ranges::distance(r1)` and N2 is `ranges::distance(r2)`.

### Possible implementation

{/* source:cpp-algorithm-ranges-includes:0009 */}
```cpp
struct includes_fn
{
    template<std::input_iterator I1, std::sentinel_for<I1> S1,
             std::input_iterator I2, std::sentinel_for<I2> S2,
             class Proj1 = std::identity, class Proj2 = std::identity,
             std::indirect_strict_weak_order<
                 std::projected<I1, Proj1>,
                 std::projected<I2, Proj2>> Comp = ranges::less>
    constexpr bool operator()(I1 first1, S1 last1, I2 first2, S2 last2,
                              Comp comp = {}, Proj1 proj1 = {}, Proj2 proj2 = {}) const
    {
        for (; first2 != last2; ++first1)
        {
            if (first1 == last1 || comp(*first2, *first1))
                return false;
            if (!comp(*first1, *first2))
                ++first2;
        }
        return true;
    }
 
    template<ranges::input_range R1, ranges::input_range R2,
             class Proj1 = std::identity, class Proj2 = std::identity,
             std::indirect_strict_weak_order<
                 std::projected<ranges::iterator_t<R1>, Proj1>,
                 std::projected<ranges::iterator_t<R2>, Proj2>> Comp = ranges::less>
    constexpr bool operator()(R1&& r1, R2&& r2, Comp comp = {},
                              Proj1 proj1 = {}, Proj2 proj2 = {}) const
    {
        return (*this)(ranges::begin(r1), ranges::end(r1),
                       ranges::begin(r2), ranges::end(r2),
                       std::ref(comp), std::ref(proj1), std::ref(proj2));
    }
};
 
inline constexpr auto includes = includes_fn {};
```

### Example

{/* source:cpp-algorithm-ranges-includes:0010 */}
```cpp
#include <algorithm>
#include <cctype>
#include <initializer_list>
#include <iomanip>
#include <iostream>
#include <locale>
#include <string>
 
template<class T>
std::ostream& operator<<(std::ostream& os, std::initializer_list<T> const& list)
{
    for (os << "{ "; auto const& elem : list)
        os << elem << ' ';
    return os << "} ";
}
 
struct true_false : std::numpunct<char>
{
    std::string do_truename() const { return "? Yes\n"; }
    std::string do_falsename() const { return "? No\n"; }
};
 
int main()
{
    std::cout.imbue(std::locale(std::cout.getloc(), new true_false));
 
    auto ignore_case = [](char a, char b) { return std::tolower(a) < std::tolower(b); };
 
    const auto
        a = {'a', 'b', 'c'},
        b = {'a', 'c'},
        c = {'a', 'a', 'b'},
        d = {'g'},
        e = {'a', 'c', 'g'},
        f = {'A', 'B', 'C'},
        z = {'a', 'b', 'c', 'f', 'h', 'x'};
 
    std::cout
        << z << "includes\n" << std::boolalpha
        << a << std::ranges::includes(z.begin(), z.end(), a.begin(), a.end())
        << b << std::ranges::includes(z, b)
        << c << std::ranges::includes(z, c)
        << d << std::ranges::includes(z, d)
        << e << std::ranges::includes(z, e)
        << f << std::ranges::includes(z, f, ignore_case);
}
```

Output:

```text
{ a b c f h x } includes
{ a b c } ? Yes
{ a c } ? Yes
{ a a b } ? No
{ g } ? No
{ a c g } ? No
{ A B C } ? Yes
```

### See also

{/* source:cpp-algorithm-ranges-includes:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::set_difference`](/docs/cpp/library/algorithm/ranges/set_difference) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the difference between two sets (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::search`](/docs/cpp/library/algorithm/ranges/search) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>searches for the first occurrence of a range of elements (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::contains`, `ranges::contains_subrange`](/docs/cpp/library/algorithm/ranges/contains) <InlineRevision since="C++23" /></DescriptionTerm>
    <DescriptionBody>checks if the range contains the given element or subrange (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`includes`](/docs/cpp/library/algorithm/includes)</DescriptionTerm>
    <DescriptionBody>returns `true` if one sequence is a subsequence of another (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
