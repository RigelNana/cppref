---
title: "std::ranges::in_out_out_result"
description: "ranges::in_out_out_result is a class template that provides a way to store three iterators as a single unit."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/return_types/in_out_out_result"
language: "C++"
---

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

  <Declaration language="cpp" since="C++20" code={`template< class I, class O1, class O2 >
struct in_out_out_result;`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0001 */}
`ranges::in_out_out_result` is a class template that provides a way to store three iterators as a single unit.

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0002 */}
This class template has no base classes or declared members other than those shown below. Thus it is suitable for use with [structured bindings](/docs/cpp/language/declarations/structured_binding).

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0003 */}
All special member functions of this class template are implicitly declared, which makes specializations be [aggregate classes](/docs/cpp/language/initialization/aggregate_initialization), and propagate triviality, potentially-throwing-ness, and constexpr-ness of corresponding operations on data members.

### Template parameters

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0004 */}
<ParameterList>
  <Parameter name="I, O1, O2">the types of the objects that the `ranges::in_out_out_result` stores.</Parameter>
</ParameterList>

### Data members

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`in`</DescriptionTerm>
    <DescriptionBody>a value (that is supposed to be an iterator) of type `I`. (public member object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`out1`</DescriptionTerm>
    <DescriptionBody>a value (that is supposed to be an iterator) of type `O1`. (public member object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>`out2`</DescriptionTerm>
    <DescriptionBody>a value (that is supposed to be an iterator) of type `O2`. (public member object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0006 */}
All these members are declared with [`[[no_unique_address]]`](/docs/cpp/language/declarations/attributes/no_unique_address) attribute.

### Member functions

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0007 */}
#### `std::ranges::in_out_out_result::operator in_out_out_result<II, OO1, OO2>`

<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`template<class II, class OO1, class OO2>
requires std::convertible_to<const I&, II> &&
         std::convertible_to<const O1&, OO1> &&
         std::convertible_to<const O2&, OO2>

constexpr operator in_out_out_result<II, OO1, OO2>() const &;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template<class II, class OO1, class OO2>
requires std::convertible_to<I, II> &&
         std::convertible_to<O1, OO1> &&
         std::convertible_to<O2, OO2>

constexpr operator in_out_out_result<II, OO1, OO2>() &&;`} />
</DeclarationDoc>

Converts `*this` to the result by constructing every data member of the result from the corresponding member of `*this`.

1. 1) Equivalent to `return {in, out1, out2};`.

2. 2) Equivalent to `return {std::move(in), std::move(out1), std::move(out2)};`.

### Standard library

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0008 */}
The following standard library functions use `ranges::in_out_out_result` as the return type:

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0009 */}
**Algorithm functions**

<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::partition_copy`](/docs/cpp/library/algorithm/ranges/partition_copy) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>copies a range dividing the elements into two groups (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

### Synopsis

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0010 */}
```cpp
namespace std::ranges
{
    template<class I, class O1, class O2>
    struct in_out_out_result
    {
        [[no_unique_address]] I  in;
        [[no_unique_address]] O1 out1;
        [[no_unique_address]] O2 out2;

        template<class II, class OO1, class OO2>
        requires std::convertible_to<const I&, II> &&
                 std::convertible_to<const O1&, OO1> &&
                 std::convertible_to<const O2&, OO2>
        constexpr operator in_out_out_result<II, OO1, OO2>() const &
        {
            return {in, out1, out2};
        }

        template<class II, class OO1, class OO2>
        requires std::convertible_to<I, II> &&
                 std::convertible_to<O1, OO1> &&
                 std::convertible_to<O2, OO2>
        constexpr operator in_out_out_result<II, OO1, OO2>() &&
        {
            return {std::move(in), std::move(out1), std::move(out2)};
        }
    };
}
```

### Notes

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0011 */}
Each standard library algorithm that uses this family of return types declares a new [alias type](/docs/cpp/language/declarations/type_alias), e.g. `using merge_result = in_in_out_result<I1, I2, O>;`.

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0012 */}
The names for such aliases are formed by adding the suffix `_result` to the algorithm's name. So, the return type of `std::ranges::merge` can be named as `std::ranges::merge_result`.

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0013 */}
Unlike [`std::pair`](/docs/cpp/library/utility/pair) and [`std::tuple`](/docs/cpp/library/utility/tuple), this class template has data members of meaningful names.

### Example

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0014 */}
```cpp
#include <algorithm>
#include <array>
#include <cctype>
#include <iostream>
#include <iterator>
#include <ranges>
#include <string_view>

void print(std::string_view rem, auto first, auto last)
{
    for (std::cout << rem << ": { "; first != last; ++first)
        std::cout << *first << ' ';
    std::cout << "}\n";
}

int main()
{
    constexpr std::string_view in {"TvEeNcStOoRr"};
    std::array<char, in.size()> o1, o2;

    const auto result = std::ranges::partition_copy(in, o1.begin(), o2.begin(),
        [](char c) { return std::isupper(c); });

    print("in", in.begin(), result.in);
    print("o1", o1.begin(), result.out1);
    print("o2", o2.begin(), result.out2);
}
```

Output:

```text
in: { T v E e N c S t O o R r }
o1: { T E N S O R }
o2: { v e c t o r }
```

### See also

{/* source:cpp-algorithm-ranges-return-types-in-out-out-result:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`pair`](/docs/cpp/library/utility/pair) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>implements binary tuple, i.e. a pair of values (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`tuple`](/docs/cpp/library/utility/tuple) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>implements fixed size container, which holds elements of possibly different types (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
