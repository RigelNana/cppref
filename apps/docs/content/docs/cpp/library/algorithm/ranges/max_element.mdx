---
title: "std::ranges::max_element"
description: "Finds the greatest element in the range [first, last)."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/max_element"
language: "C++"
---

{/* source:cpp-algorithm-ranges-max-element:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< std::forward_iterator I, std::sentinel_for<I> S, class Proj = std::identity,
          std::indirect_strict_weak_order<std::projected<I, Proj>> Comp = ranges::less >
constexpr I

    max_element( I first, S last, Comp comp = {}, Proj proj = {} );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< ranges::forward_range R, class Proj = std::identity,
          std::indirect_strict_weak_order<
              std::projected<ranges::iterator_t<R>, Proj>> Comp = ranges::less >
constexpr ranges::borrowed_iterator_t<R>

    max_element( R&& r, Comp comp = {}, Proj proj = {} );`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-max-element:0001 */}
1. 1) Finds the greatest element in the range `[first, last)`.

{/* source:cpp-algorithm-ranges-max-element:0002 */}
2. 2) Same as (1), but uses `r` as the source range, as if using `ranges::begin(r)` as `first` and `ranges::end(r)` as `last`.

{/* source:cpp-algorithm-ranges-max-element:0003 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-algorithm-ranges-max-element:0004 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-algorithm-ranges-max-element:0005 */}
<ParameterList>
  <Parameter name="first, last">iterator-sentinel pair denoting the range to examine</Parameter>
  <Parameter name="r">the range to examine</Parameter>
  <Parameter name="comp">comparison to apply to the projected elements</Parameter>
  <Parameter name="proj">projection to apply to the elements</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-max-element:0006 */}
Iterator to the greatest element in the range `[first, last)`. If several elements in the range are equivalent to the greatest element, returns the iterator to the first such element. Returns `last` if the range is empty (i.e. if `first == last`).

### Complexity

{/* source:cpp-algorithm-ranges-max-element:0007 */}
Exactly `max(N - 1, 0)` comparisons, where `N = ranges::distance(first, last)`.

### Possible implementation

{/* source:cpp-algorithm-ranges-max-element:0008 */}
```cpp
struct max_element_fn
{
    template<std::forward_iterator I, std::sentinel_for<I> S, class Proj = std::identity,
             std::indirect_strict_weak_order<std::projected<I, Proj>> Comp = ranges::less>
    constexpr I operator()(I first, S last, Comp comp = {}, Proj proj = {}) const
    {
        if (first == last)
            return last;
 
        auto largest = first;
        while (++first != last)
            if (std::invoke(comp, std::invoke(proj, *largest), std::invoke(proj, *first)))
                largest = first;
        return largest;
    }
 
    template<ranges::forward_range R, class Proj = std::identity,
             std::indirect_strict_weak_order<
                 std::projected<ranges::iterator_t<R>, Proj>> Comp = ranges::less>
    constexpr ranges::borrowed_iterator_t<R>
        operator()(R&& r, Comp comp = {}, Proj proj = {}) const
    {
        return (*this)(ranges::begin(r), ranges::end(r), std::ref(comp), std::ref(proj));
    }
};
 
inline constexpr max_element_fn max_element;
```

### Example

{/* source:cpp-algorithm-ranges-max-element:0009 */}
```cpp
#include <algorithm>
#include <cmath>
#include <iostream>
 
int main()
{
    namespace ranges = std::ranges;
 
    const auto v = {3, 1, -14, 1, 5, 9, -14, 9};
 
    auto result = ranges::max_element(v.begin(), v.end());
    std::cout << "Max element at pos " << ranges::distance(v.begin(), result) << '\n';
 
    auto abs_compare = [](int a, int b) { return std::abs(a) < std::abs(b); };
    result = ranges::max_element(v, abs_compare);
    std::cout << "Absolute max element at pos "
              << ranges::distance(v.begin(), result) << '\n';
}
```

Output:

```text
Max element at pos 5
Absolute max element at pos 2
```

### See also

{/* source:cpp-algorithm-ranges-max-element:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::min_element`](/docs/cpp/library/algorithm/ranges/min_element) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the smallest element in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::minmax_element`](/docs/cpp/library/algorithm/ranges/minmax_element) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the smallest and the largest elements in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::max`](/docs/cpp/library/algorithm/ranges/max) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the greater of the given values (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max_element`](/docs/cpp/library/algorithm/max_element)</DescriptionTerm>
    <DescriptionBody>returns the largest element in a range (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
