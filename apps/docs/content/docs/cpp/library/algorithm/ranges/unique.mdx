---
title: "std::ranges::unique"
description: "Eliminates all except the first element from every consecutive group of equivalent elements from the range [first, last) and returns a subrange [ret, last), where ret is a past-the-end iterator for the new end of the range."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/unique"
language: "C++"
---

{/* source:cpp-algorithm-ranges-unique:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< std::permutable I, std::sentinel_for<I> S, class Proj = std::identity,
          std::indirect_equivalence_relation<std::projected<I, Proj>>
              C = ranges::equal_to >
constexpr ranges::subrange<I>

    unique( I first, S last, C comp = {}, Proj proj = {} );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< ranges::forward_range R, class Proj = std::identity,
          std::indirect_equivalence_relation<std::projected<ranges::iterator_t<R>, Proj>>
              C = ranges::equal_to >
requires std::permutable<ranges::iterator_t<R>>
constexpr ranges::borrowed_subrange_t<R>

    unique( R&& r, C comp = {}, Proj proj = {} );`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-unique:0001 */}
1. 1) Eliminates all except the first element from every consecutive group of equivalent elements from the range `[first, last)` and returns a subrange `[ret, last)`, where `ret` is a past-the-end iterator for the new end of the range.

{/* source:cpp-algorithm-ranges-unique:0002 */}
2. Two consecutive elements `*(i - 1)` and `*i` are considered equivalent if `std::invoke(comp, std::invoke(proj, *(i - 1)), std::invoke(proj, *i)) == true`, where `i` is an iterator in the range `[first + 1, last)`.

{/* source:cpp-algorithm-ranges-unique:0003 */}
3. 2) Same as (1), but uses `r` as the range, as if using `ranges::begin(r)` as `first`, and `ranges::end(r)` as `last`.

{/* source:cpp-algorithm-ranges-unique:0004 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-algorithm-ranges-unique:0005 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-algorithm-ranges-unique:0006 */}
<ParameterList>
  <Parameter name="first, last">the range of elements to process</Parameter>
  <Parameter name="r">the range of elements to process</Parameter>
  <Parameter name="comp">the binary predicate to compare the projected elements</Parameter>
  <Parameter name="proj">the projection to apply to the elements</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-unique:0007 */}
Returns `{ret, last}`, where `ret` is a past-the-end iterator for the new end of the range.

### Complexity

{/* source:cpp-algorithm-ranges-unique:0008 */}
For nonempty ranges, exactly `ranges::distance(first, last) - 1` applications of the corresponding predicate `comp` and no more that twice as many applications of any projection `proj`.

### Notes

{/* source:cpp-algorithm-ranges-unique:0009 */}
Removing is done by shifting (by means of move assignment) the elements in the range in such a way that the elements that are not to be removed appear in the beginning of the range. Relative order of the elements that remain is preserved and the physical size of the container is unchanged. Iterators in `[ret, last)` (if any) are still dereferenceable, but the elements themselves have unspecified values (as per [*MoveAssignable*](/docs/cpp/library/named_req/MoveAssignable) post-condition).

{/* source:cpp-algorithm-ranges-unique:0010 */}
A call to `ranges::unique` is sometimes followed by a call to a container’s `erase` member function, which erases the unspecified values and reduces the physical size of the container to match its new logical size. These two invocations together model the [Erase–remove idiom](https://en.wikipedia.org/wiki/Erase-remove_idiom).

### Possible implementation

{/* source:cpp-algorithm-ranges-unique:0011 */}
```cpp
struct unique_fn
{
    template<std::permutable I, std::sentinel_for<I> S, class Proj = std::identity,
             std::indirect_equivalence_relation<std::projected<I, Proj>>
                 C = ranges::equal_to>
    constexpr ranges::subrange<I>
        operator()(I first, S last, C comp = {}, Proj proj = {}) const
    {
        first = ranges::adjacent_find(first, last, comp, proj);
        if (first == last)
            return {first, first};
        auto i {first};
        ++first;
        while (++first != last)
            if (!std::invoke(comp, std::invoke(proj, *i), std::invoke(proj, *first)))
                *++i = ranges::iter_move(first);
        return {++i, first};
    }
 
    template<ranges::forward_range R, class Proj = std::identity,
             std::indirect_equivalence_relation<std::projected<ranges::iterator_t<R>, Proj>>
                 C = ranges::equal_to>
    requires std::permutable<ranges::iterator_t<R>>
    constexpr ranges::borrowed_subrange_t<R>
        operator()(R&& r, C comp = {}, Proj proj = {}) const
    {
        return (*this)(ranges::begin(r), ranges::end(r),
                       std::move(comp), std::move(proj));
    }
};
 
inline constexpr unique_fn unique {};
```

### Example

{/* source:cpp-algorithm-ranges-unique:0012 */}
```cpp
#include <algorithm>
#include <cmath>
#include <complex>
#include <iostream>
#include <vector>
 
struct id {
    int i;
    explicit id(int i) : i {i} {}
};
 
void print(id i, const auto& v)
{
    std::cout << i.i << ") ";
    std::ranges::for_each(v, [](auto const& e) { std::cout << e << ' '; });
    std::cout << '\n';
}
 
int main()
{
    // a vector containing several duplicated elements
    std::vector<int> v {1, 2, 1, 1, 3, 3, 3, 4, 5, 4};
 
    print(id {1}, v);
 
    // remove consecutive (adjacent) duplicates
    const auto ret = std::ranges::unique(v);
    // v now holds {1 2 1 3 4 5 4 x x x}, where 'x' is indeterminate
    v.erase(ret.begin(), ret.end());
    print(id {2}, v);
 
    // sort followed by unique, to remove all duplicates
    std::ranges::sort(v); // {1 1 2 3 4 4 5}
    print(id {3}, v);
 
    const auto [first, last] = std::ranges::unique(v.begin(), v.end());
    // v now holds {1 2 3 4 5 x x}, where 'x' is indeterminate
    v.erase(first, last);
    print(id {4}, v);
 
    // unique with custom comparison and projection
    std::vector<std::complex<int>> vc { {1, 1}, {-1, 2}, {-2, 3}, {2, 4}, {-3, 5} };
    print(id {5}, vc);
 
    const auto ret2 = std::ranges::unique(vc,
        // consider two complex nums equal if their real parts are equal by module:
        [](int x, int y) { return std::abs(x) == std::abs(y); }, // comp
        [](std::complex<int> z) { return z.real(); }             // proj
    );
    vc.erase(ret2.begin(), ret2.end());
    print(id {6}, vc);
}
```

Output:

```text
1) 1 2 1 1 3 3 3 4 5 4
2) 1 2 1 3 4 5 4
3) 1 1 2 3 4 4 5
4) 1 2 3 4 5
5) (1,1) (-1,2) (-2,3) (2,4) (-3,5)
6) (1,1) (-2,3) (-3,5)
```

### See also

{/* source:cpp-algorithm-ranges-unique:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::unique_copy`](/docs/cpp/library/algorithm/ranges/unique_copy) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>creates a copy of some range of elements that contains no consecutive duplicates (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::adjacent_find`](/docs/cpp/library/algorithm/ranges/adjacent_find) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>finds the first two adjacent items that are equal (or satisfy a given predicate) (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::remove`, `ranges::remove_if`](/docs/cpp/library/algorithm/ranges/remove) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>removes elements satisfying specific criteria (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`unique`](/docs/cpp/library/algorithm/unique)</DescriptionTerm>
    <DescriptionBody>removes consecutive duplicate elements in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`unique`](/docs/cpp/library/container/list/unique)</DescriptionTerm>
    <DescriptionBody>removes consecutive duplicate elements (public member function of `std::list<T,Allocator>`)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`unique`](/docs/cpp/library/container/forward_list/unique)</DescriptionTerm>
    <DescriptionBody>removes consecutive duplicate elements (public member function of `std::forward_list<T,Allocator>`)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
