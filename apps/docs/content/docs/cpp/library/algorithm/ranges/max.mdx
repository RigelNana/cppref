---
title: "std::ranges::max"
description: "Returns the greater of the given projected values."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/max"
language: "C++"
---

{/* source:cpp-algorithm-ranges-max:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< class T, class Proj = std::identity,
          std::indirect_strict_weak_order<
              std::projected<const T*, Proj>> Comp = ranges::less >
constexpr const T&

    max( const T& a, const T& b, Comp comp = {}, Proj proj = {} );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`template< std::copyable T, class Proj = std::identity,
          std::indirect_strict_weak_order<
              std::projected<const T*, Proj>> Comp = ranges::less >
constexpr T

    max( std::initializer_list<T> r, Comp comp = {}, Proj proj = {} );`} />
  <Declaration id="3" language="cpp" since="C++20" code={`template< ranges::input_range R, class Proj = std::identity,
          std::indirect_strict_weak_order<
              std::projected<ranges::iterator_t<R>, Proj>> Comp = ranges::less >
requires std::indirectly_copyable_storable<ranges::iterator_t<R>,
                                           ranges::range_value_t<R>*>
constexpr ranges::range_value_t<R>

    max( R&& r, Comp comp = {}, Proj proj = {} );`} />
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-max:0001 */}
Returns the greater of the given projected values.

{/* source:cpp-algorithm-ranges-max:0002 */}
1. 1) Returns the greater of `a` and `b`.

{/* source:cpp-algorithm-ranges-max:0003 */}
2. 2) Returns the first greatest value in the initializer list `r`.

{/* source:cpp-algorithm-ranges-max:0004 */}
3. 3) Returns the first greatest value in the range `r`.

{/* source:cpp-algorithm-ranges-max:0005 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-algorithm-ranges-max:0006 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-algorithm-ranges-max:0007 */}
<ParameterList>
  <Parameter name="a, b">the values to compare</Parameter>
  <Parameter name="r">the range of values to compare</Parameter>
  <Parameter name="comp">comparison to apply to the projected elements</Parameter>
  <Parameter name="proj">projection to apply to the elements</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-max:0008 */}
1. 1) The greater of `a` and `b`, according to their respective projected values. If they are equivalent, returns `a`.

{/* source:cpp-algorithm-ranges-max:0009 */}
2. 2,3) The greatest value in `r`, according to the projection. If several values are equivalent to the greatest, returns the leftmost one. If the range is empty (as determined by `ranges::distance(r)`), the behavior is undefined.

### Complexity

{/* source:cpp-algorithm-ranges-max:0010 */}
1. 1) Exactly one comparison.

{/* source:cpp-algorithm-ranges-max:0011 */}
2. 2,3) Exactly `ranges::distance(r) - 1` comparisons.

### Possible implementation

{/* source:cpp-algorithm-ranges-max:0012 */}
```cpp
struct max_fn
{
    template<class T, class Proj = std::identity,
             std::indirect_strict_weak_order<
                 std::projected<const T*, Proj>> Comp = ranges::less>
    constexpr
    const T& operator()(const T& a, const T& b, Comp comp = {}, Proj proj = {}) const
    {
        return std::invoke(comp, std::invoke(proj, a), std::invoke(proj, b)) ? b : a;
    }
 
    template<std::copyable T, class Proj = std::identity,
             std::indirect_strict_weak_order<
                 std::projected<const T*, Proj>> Comp = ranges::less>
    constexpr
    T operator()(std::initializer_list<T> r, Comp comp = {}, Proj proj = {}) const
    {
        return *ranges::max_element(r, std::ref(comp), std::ref(proj));
    }
 
    template<ranges::input_range R, class Proj = std::identity,
             std::indirect_strict_weak_order<
                  std::projected<ranges::iterator_t<R>, Proj>> Comp = ranges::less>
    requires std::indirectly_copyable_storable<ranges::iterator_t<R>,
                                               ranges::range_value_t<R>*>
    constexpr
    ranges::range_value_t<R> operator()(R&& r, Comp comp = {}, Proj proj = {}) const
    {
        using V = ranges::range_value_t<R>;
        if constexpr (ranges::forward_range<R>)
            return
                static_cast<V>(*ranges::max_element(r, std::ref(comp), std::ref(proj)));
        else
        {
            auto i = ranges::begin(r);
            auto s = ranges::end(r);
            V m(*i);
            while (++i != s)
                if (std::invoke(comp, std::invoke(proj, m), std::invoke(proj, *i)))
                    m = *i;
            return m;
        }
    }
};
 
inline constexpr max_fn max;
```

### Notes

{/* source:cpp-algorithm-ranges-max:0013 */}
Capturing the result of `std::ranges::max` by reference produces a dangling reference if one of the parameters is a temporary and that parameter is returned:

{/* source:cpp-algorithm-ranges-max:0014 */}
```cpp
int n = -1;
const int& r = std::ranges::max(n + 2, n * 2); // r is dangling
```

### Example

{/* source:cpp-algorithm-ranges-max:0015 */}
```cpp
#include <algorithm>
#include <iostream>
#include <string>
 
static_assert(std::ranges::max({0B10, 0X10, 010, 10}) == 16); // overload (2)
 
int main()
{
    namespace ranges = std::ranges;
    using namespace std::string_view_literals;
 
    std::cout << "larger of 1 and 9999: " << ranges::max(1, 9999) << '\n'
              << "larger of 'a', and 'b': '" << ranges::max('a', 'b') << "'\n"
              << "longest of \"foo\", \"bar\", and \"hello\": \""
              << ranges::max({"foo"sv, "bar"sv, "hello"sv}, {},
                             &std::string_view::size) << "\"\n";
}
```

Output:

```text
larger of 1 and 9999: 9999
larger of 'a', and 'b': 'b'
longest of "foo", "bar", and "hello": "hello"
```

### See also

{/* source:cpp-algorithm-ranges-max:0016 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::min`](/docs/cpp/library/algorithm/ranges/min) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the smaller of the given values (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::minmax`](/docs/cpp/library/algorithm/ranges/minmax) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the smaller and larger of two elements (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::max_element`](/docs/cpp/library/algorithm/ranges/max_element) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the largest element in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::clamp`](/docs/cpp/library/algorithm/ranges/clamp) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>clamps a value between a pair of boundary values (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max`](/docs/cpp/library/algorithm/max)</DescriptionTerm>
    <DescriptionBody>returns the greater of the given values (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
