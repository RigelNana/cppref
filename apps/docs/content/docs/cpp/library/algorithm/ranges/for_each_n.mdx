---
title: "std::ranges::for_each_n, std::ranges::for_each_n_result"
description: "Applies the given function object f to the projected result by proj of dereferencing each iterator in the range [first, first + n), in order."
source_url: "https://en.cppreference.com/w/cpp/algorithm/ranges/for_each_n"
language: "C++"
---


{/* source:cpp-algorithm-ranges-for-each-n:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

Call signature

  <Declaration id="1" language="cpp" since="C++20" code={`template< std::input_iterator I, class Proj = std::identity,
          std::indirectly_unary_invocable<std::projected<I, Proj>> Fun >
constexpr for_each_n_result<I, Fun>

    for_each_n( I first, std::iter_difference_t<I> n, Fun f, Proj proj = {});`} />

Helper types

  <Declaration id="2" language="cpp" since="C++20" code={`template< class I, class F >
using for_each_n_result = ranges::in_fun_result<I, F>;`} />
  (2)
</DeclarationDoc>

{/* source:cpp-algorithm-ranges-for-each-n:0001 */}
1. 1) Applies the given function object `f` to the projected result by `proj` of dereferencing each iterator in the range [`first`, `first + n`), in order.

{/* source:cpp-algorithm-ranges-for-each-n:0002 */}
If the iterator type is mutable, `f` may modify the elements of the range through the dereferenced iterator. If `f` returns a result, the result is ignored. If `n` is less than zero, the behavior is undefined.

{/* source:cpp-algorithm-ranges-for-each-n:0003 */}
The function-like entities described on this page are [*algorithm function objects*](/docs/cpp/library/algorithm/ranges#Algorithm_function_objects) (informally known as *niebloids*), that is:

{/* source:cpp-algorithm-ranges-for-each-n:0004 */}
- Explicit template argument lists cannot be specified when calling any of them.
- None of them are visible to [argument-dependent lookup](/docs/cpp/language/functions/adl).
- When any of them are found by [normal unqualified lookup](/docs/cpp/language/basic_concepts/unqualified_lookup) as the name to the left of the function-call operator, [argument-dependent lookup](/docs/cpp/language/functions/adl) is inhibited.

### Parameters

{/* source:cpp-algorithm-ranges-for-each-n:0005 */}
<ParameterList>
  <Parameter name="first">iterator denoting the begin of the range to apply the function to</Parameter>
  <Parameter name="n">the number of elements to apply the function to</Parameter>
  <Parameter name="f">the function to apply to the projected range [`first`, `first + n`)</Parameter>
  <Parameter name="proj">projection to apply to the elements</Parameter>
</ParameterList>

### Return value

{/* source:cpp-algorithm-ranges-for-each-n:0006 */}
An object `{first + n, std::move(f)}`, where `first + n` may be evaluated as `std::ranges::next(std::move(first), n)` depending on iterator category.

### Complexity

{/* source:cpp-algorithm-ranges-for-each-n:0007 */}
Exactly `n` applications of `f` and `proj`.

### Possible implementation

{/* source:cpp-algorithm-ranges-for-each-n:0008 */}
```cpp
struct for_each_n_fn
{
    template<std::input_iterator I, class Proj = std::identity,
             std::indirectly_unary_invocable<std::projected<I, Proj>> Fun>
    constexpr for_each_n_result<I, Fun>
        operator()(I first, std::iter_difference_t<I> n, Fun fun, Proj proj = Proj{}) const
    {
        for (; n-- > 0; ++first)
            std::invoke(fun, std::invoke(proj, *first));
        return {std::move(first), std::move(fun)};
    }
};
 
inline constexpr for_each_n_fn for_each_n {};
```

### Example

{/* source:cpp-algorithm-ranges-for-each-n:0009 */}
```cpp
#include <algorithm>
#include <array>
#include <iostream>
#include <ranges>
#include <string_view>
 
struct P
{
    int first;
    char second;
    friend std::ostream& operator<<(std::ostream& os, const P& p)
    {
        return os << '{' << p.first << ",'" << p.second << "'}";
    }
};
 
auto print = [](std::string_view name, auto const& v)
{
    std::cout << name << ": ";
    for (auto n = v.size(); const auto& e : v)
        std::cout << e << (--n ? ", " : "\n");
};
 
int main()
{
    std::array a {1, 2, 3, 4, 5};
    print("a", a);
    // Negate first three numbers:
    std::ranges::for_each_n(a.begin(), 3, [](auto& n) { n *= -1; });
    print("a", a);
 
    std::array s { P{1,'a'}, P{2, 'b'}, P{3, 'c'}, P{4, 'd'} };
    print("s", s);
    // Negate data members 'P::first' using projection:
    std::ranges::for_each_n(s.begin(), 2, [](auto& x) { x *= -1; }, &P::first);
    print("s", s);
    // Capitalize data members 'P::second' using projection:
    std::ranges::for_each_n(s.begin(), 3, [](auto& c) { c -= 'a'-'A'; }, &P::second);
    print("s", s);
}
```

Output:

```text
a: 1, 2, 3, 4, 5
a: -1, -2, -3, 4, 5
s: {1,'a'}, {2,'b'}, {3,'c'}, {4,'d'}
s: {-1,'a'}, {-2,'b'}, {3,'c'}, {4,'d'}
s: {-1,'A'}, {-2,'B'}, {3,'C'}, {4,'d'}
```

### See also

{/* source:cpp-algorithm-ranges-for-each-n:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[range-for loop](/docs/cpp/language/statements/range-for) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>executes loop over range</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::for_each`](/docs/cpp/library/algorithm/ranges/for_each) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>applies a function to a range of elements (algorithm function object)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`for_each_n`](/docs/cpp/library/algorithm/for_each_n) <InlineRevision since="C++17" /></DescriptionTerm>
    <DescriptionBody>applies a function object to the first `N` elements of a sequence (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`for_each`](/docs/cpp/library/algorithm/for_each)</DescriptionTerm>
    <DescriptionBody>applies a function to a range of elements (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
