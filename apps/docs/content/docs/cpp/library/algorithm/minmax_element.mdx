---
title: "std::minmax_element"
description: "Finds the smallest and greatest element in the range [first, last)."
source_url: "https://en.cppreference.com/w/cpp/algorithm/minmax_element"
language: "C++"
---

{/* source:cpp-algorithm-minmax-element:0000 */}
<DeclarationDoc since="C++11">
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

  <Declaration id="1" language="cpp" code={`template< class ForwardIt >
std::pair<ForwardIt, ForwardIt>

    minmax_element( ForwardIt first, ForwardIt last );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class ExecutionPolicy, class ForwardIt >
std::pair<ForwardIt, ForwardIt>
    minmax_element( ExecutionPolicy&& policy,

                    ForwardIt first, ForwardIt last );`} />
  <Declaration id="3" language="cpp" code={`template< class ForwardIt, class Compare >
std::pair<ForwardIt, ForwardIt>

    minmax_element( ForwardIt first, ForwardIt last, Compare comp );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class ExecutionPolicy, class ForwardIt, class Compare >
std::pair<ForwardIt, ForwardIt>
    minmax_element( ExecutionPolicy&& policy,

                    ForwardIt first, ForwardIt last, Compare comp );`} />
</DeclarationDoc>

{/* source:cpp-algorithm-minmax-element:0001 */}
Finds the smallest and greatest element in the range `[first, last)`.

{/* source:cpp-algorithm-minmax-element:0002 */}
1. 1) Elements are compared using <InlineRevision until="C++20">`operator<`</InlineRevision> <InlineRevision since="C++20">`std::less{}`</InlineRevision>.

{/* source:cpp-algorithm-minmax-element:0003 */}
2. 3) Elements are compared using the comparison function `comp`.

{/* source:cpp-algorithm-minmax-element:0004 */}
3. 2,4) Same as (1,3), but executed according to `policy`.

{/* source:cpp-algorithm-minmax-element:0005 */}
4. These overloads participate in overload resolution only if all following conditions are satisfied:

   <Revision until="C++20">`std::is_execution_policy_v<std::decay_t<ExecutionPolicy>>` is `true`.</Revision>

   <Revision since="C++20">`std::is_execution_policy_v<std::remove_cvref_t<ExecutionPolicy>>` is `true`.</Revision>

### Parameters

{/* source:cpp-algorithm-minmax-element:0006 */}
<ParameterList>
  <Parameter name="first, last">forward iterators defining the range to examine</Parameter>
  <Parameter name="policy">the execution policy to use</Parameter>
  <Parameter name="cmp">
comparison function object (i.e. an object that satisfies the requirements of [*Compare*](/docs/cpp/library/named_req/Compare)) which returns `true` if the first argument is less than the second.

The signature of the comparison function should be equivalent to the following:

`bool cmp(const Type1& a, const Type2& b);`

While the signature does not need to have `const&`, the function must not modify the objects passed to it and must be able to accept all values of type (possibly const) `Type1` and `Type2` regardless of [value category](/docs/cpp/language/expressions/value_category) (thus, `Type1&` is not allowed<InlineRevision since="C++11">, nor is `Type1` unless for `Type1` a move is equivalent to a copy</InlineRevision>). The types `Type1` and `Type2` must be such that an object of type `ForwardIt` can be dereferenced and then implicitly converted to both of them.
  </Parameter>
</ParameterList>

Type requirements

- `ForwardIt` must meet the requirements of [*LegacyForwardIterator*](/docs/cpp/library/named_req/ForwardIterator).

### Return value

{/* source:cpp-algorithm-minmax-element:0007 */}
a pair consisting of an iterator to the smallest element as the first element and an iterator to the greatest element as the second. Returns `std::make_pair(first, first)` if the range is empty. If several elements are equivalent to the smallest element, the iterator to the first such element is returned. If several elements are equivalent to the largest element, the iterator to the last such element is returned.

### Complexity

{/* source:cpp-algorithm-minmax-element:0008 */}
Given `N` as `std::distance(first, last)`:

{/* source:cpp-algorithm-minmax-element:0009 */}
1. 1,2) At most `max(⌊3/2(N-1)⌋,0)` comparisons using <InlineRevision until="C++20">`operator<`</InlineRevision> <InlineRevision since="C++20">`std::less{}`</InlineRevision>.

{/* source:cpp-algorithm-minmax-element:0010 */}
2. 3,4) At most `max(⌊3/2(N-1)⌋,0)` applications of the comparison function `comp`.

### Exceptions

{/* source:cpp-algorithm-minmax-element:0011 */}
The overloads with a template parameter named `ExecutionPolicy` report errors as follows:

{/* source:cpp-algorithm-minmax-element:0012 */}
- If execution of a function invoked as part of the algorithm throws an exception and `ExecutionPolicy` is one of the [standard policies](/docs/cpp/library/algorithm/execution_policy_tag_t), [`std::terminate`](/docs/cpp/library/error/terminate) is called. For any other `ExecutionPolicy`, the behavior is implementation-defined. If the algorithm fails to allocate memory, [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) is thrown.

### Possible implementation

{/* source:cpp-algorithm-minmax-element:0013 */}
[`minmax_element (1)`](/docs/cpp/library/algorithm/minmax_element#Version_1)

```cpp
template<class ForwardIt>
std::pair<ForwardIt, ForwardIt>
    minmax_element(ForwardIt first, ForwardIt last)
{
    using value_type = typename std::iterator_traits<ForwardIt>::value_type;
    return std::minmax_element(first, last, std::less<value_type>());
}
```

[`minmax_element (3)`](/docs/cpp/library/algorithm/minmax_element#Version_3)

```cpp
template<class ForwardIt, class Compare>
std::pair<ForwardIt, ForwardIt>
    minmax_element(ForwardIt first, ForwardIt last, Compare comp)
{
    auto min = first, max = first;
 
    if (first == last || ++first == last)
        return {min, max};
 
    if (comp(*first, *min))
        min = first;
    else
        max = first;
 
    while (++first != last)
    {
        auto i = first;
        if (++first == last)
        {
            if (comp(*i, *min))
                min = i;
            else if (!(comp(*i, *max)))
                max = i;
            break;
        }
        else
        {
            if (comp(*first, *i))
            {
                if (comp(*first, *min))
                    min = first;
                if (!(comp(*i, *max)))
                    max = i;
            }
            else
            {
                if (comp(*i, *min))
                    min = i;
                if (!(comp(*first, *max)))
                    max = first;
            }
        }
    }
    return {min, max};
}
```

### Notes

{/* source:cpp-algorithm-minmax-element:0014 */}
This algorithm is different from `std::make_pair(std::min_element(), std::max_element())`, not only in efficiency, but also in that this algorithm finds the last biggest element while [`std::max_element`](/docs/cpp/library/algorithm/max_element) finds the first biggest element.

### Example

{/* source:cpp-algorithm-minmax-element:0015 */}
```cpp
#include <algorithm>
#include <iostream>
 
int main()
{
    const auto v = {3, 9, 1, 4, 2, 5, 9};
    const auto [min, max] = std::minmax_element(begin(v), end(v));
 
    std::cout << "min = " << *min << ", max = " << *max << '\n';
}
```

Output:

```text
min = 1, max = 9
```

### See also

{/* source:cpp-algorithm-minmax-element:0016 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`min_element`](/docs/cpp/library/algorithm/min_element)</DescriptionTerm>
    <DescriptionBody>returns the smallest element in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`max_element`](/docs/cpp/library/algorithm/max_element)</DescriptionTerm>
    <DescriptionBody>returns the largest element in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::minmax_element`](/docs/cpp/library/algorithm/ranges/minmax_element) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>returns the smallest and the largest elements in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
