---
title: "std::partition_point"
description: "Examines the partitioned range [first, last) and locates the end of the first partition, that is, the first element that does not satisfy p or last if all elements satisfy p."
source_url: "https://en.cppreference.com/w/cpp/algorithm/partition_point"
language: "C++"
---

{/* source:cpp-algorithm-partition-point:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

  <Declaration language="cpp" since="C++11" code={`template< class ForwardIt, class UnaryPred >
ForwardIt partition_point( ForwardIt first, ForwardIt last, UnaryPred p );`} />
  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-algorithm-partition-point:0001 */}
Examines the partitioned range `[first, last)` and locates the end of the first partition, that is, the first element that does not satisfy `p` or `last` if all elements satisfy `p`.

{/* source:cpp-algorithm-partition-point:0002 */}
If the elements `elem` of `[first, last)` are not [partitioned](/docs/cpp/library/algorithm#Requirements) with respect to the expression `bool(p(elem))`, the behavior is undefined.

### Parameters

{/* source:cpp-algorithm-partition-point:0003 */}
<ParameterList>
  <Parameter name="first, last">the partitioned range of elements to examine</Parameter>
  <Parameter name="p">unary predicate which returns `true` for the elements found in the beginning of the range. The expression `p(v)` must be convertible to `bool` for every argument `v` of type (possibly const) `VT`, where `VT` is the value type of `ForwardIt`, regardless of [value category](/docs/cpp/language/expressions/value_category), and must not modify `v`. Thus, a parameter type of `VT&` is not allowed<InlineRevision since="C++11">, nor is `VT` unless for `VT` a move is equivalent to a copy</InlineRevision>.</Parameter>
</ParameterList>

Type requirements

- `ForwardIt` must meet the requirements of [*LegacyForwardIterator*](/docs/cpp/library/named_req/ForwardIterator).
- `UnaryPred` must meet the requirements of [*Predicate*](/docs/cpp/library/named_req/Predicate).

### Return value

{/* source:cpp-algorithm-partition-point:0004 */}
The iterator past the end of the first partition within `[first, last)` or `last` if all elements satisfy `p`.

### Complexity

{/* source:cpp-algorithm-partition-point:0005 */}
Given `N` as `std::distance(first, last)`, performs `O(log(N))` applications of the predicate `p`.

### Notes

{/* source:cpp-algorithm-partition-point:0006 */}
This algorithm is a more general form of [`std::lower_bound`](/docs/cpp/library/algorithm/lower_bound), which can be expressed in terms of `std::partition_point` with the predicate `[&](const auto& e) { return e < value; });`.

### Possible implementation

{/* source:cpp-algorithm-partition-point:0007 */}
```cpp
template<class ForwardIt, class UnaryPred>
constexpr //< since C++20
ForwardIt partition_point(ForwardIt first, ForwardIt last, UnaryPred p)
{
    for (auto length = std::distance(first, last); 0 < length; )
    {
        auto half = length / 2;
        auto middle = std::next(first, half);
        if (p(*middle))
        {
            first = std::next(middle);
            length -= (half + 1);
        }
        else
            length = half;
    }
 
    return first;
}
```

### Example

{/* source:cpp-algorithm-partition-point:0008 */}
```cpp
#include <algorithm>
#include <array>
#include <iostream>
#include <iterator>
 
auto print_seq = [](auto rem, auto first, auto last)
{
    for (std::cout << rem; first != last; std::cout << *first++ << ' ') {}
    std::cout << '\n';
};
 
int main()
{
    std::array v{1, 2, 3, 4, 5, 6, 7, 8, 9};
 
    auto is_even = [](int i) { return i % 2 == 0; };
 
    std::partition(v.begin(), v.end(), is_even);
    print_seq("After partitioning, v: ", v.cbegin(), v.cend());
 
    const auto pp = std::partition_point(v.cbegin(), v.cend(), is_even);
    const auto i = std::distance(v.cbegin(), pp);
    std::cout << "Partition point is at " << i << "; v[" << i << "] = " << *pp << '\n';
 
    print_seq("First partition (all even elements): ", v.cbegin(), pp);
    print_seq("Second partition (all odd elements): ", pp, v.cend());
}
```

Possible output:

```text
After partitioning, v: 8 2 6 4 5 3 7 1 9
Partition point is at 4; v[4] = 5
First partition (all even elements): 8 2 6 4
Second partition (all odd elements): 5 3 7 1 9
```

### See also

{/* source:cpp-algorithm-partition-point:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`find`, `find_if`, `find_if_not`](/docs/cpp/library/algorithm/find) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>finds the first element satisfying specific criteria (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`is_sorted`](/docs/cpp/library/algorithm/is_sorted) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>checks whether a range is sorted into ascending order (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`lower_bound`](/docs/cpp/library/algorithm/lower_bound)</DescriptionTerm>
    <DescriptionBody>returns an iterator to the first element not less than the given value (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::partition_point`](/docs/cpp/library/algorithm/ranges/partition_point) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>locates the partition point of a partitioned range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
