---
title: "std::set_symmetric_difference"
description: "Computes symmetric difference of two sorted ranges: the elements that are found in either of the ranges, but not in both of them are copied to the range beginning at d_first."
source_url: "https://en.cppreference.com/w/cpp/algorithm/set_symmetric_difference"
language: "C++"
---

{/* source:cpp-algorithm-set-symmetric-difference:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

  <Declaration id="1" language="cpp" since="C++20" code={`template< class InputIt1, class InputIt2, class OutputIt >
OutputIt set_symmetric_difference
    ( InputIt1 first1, InputIt1 last1,
      InputIt2 first2, InputIt2 last2,

      OutputIt d_first );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class ExecutionPolicy,
          class ForwardIt1, class ForwardIt2, class ForwardIt3 >
ForwardIt3 set_symmetric_difference
    ( ExecutionPolicy&& policy,
      ForwardIt1 first1, ForwardIt1 last1,
      ForwardIt2 first2, ForwardIt2 last2,

      ForwardIt3 d_first );`} />
  <Declaration id="3" language="cpp" since="C++20" code={`template< class InputIt1, class InputIt2,
          class OutputIt, class Compare >
OutputIt set_symmetric_difference
    ( InputIt1 first1, InputIt1 last1,
      InputIt2 first2, InputIt2 last2,

      OutputIt d_first, Compare comp );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`template< class ExecutionPolicy,
          class ForwardIt1, class ForwardIt2,
          class ForwardIt3, class Compare >
ForwardIt3 set_symmetric_difference
    ( ExecutionPolicy&& policy,
      ForwardIt1 first1, ForwardIt1 last1,
      ForwardIt2 first2, ForwardIt2 last2,

      ForwardIt3 d_first, Compare comp );`} />
</DeclarationDoc>

{/* source:cpp-algorithm-set-symmetric-difference:0001 */}
Computes symmetric difference of two sorted ranges: the elements that are found in either of the ranges, but not in both of them are copied to the range beginning at `d_first`. The output range is also sorted.

{/* source:cpp-algorithm-set-symmetric-difference:0002 */}
If `[first1, last1)` contains `m` elements that are equivalent to each other and `[first2, last2)` contains `n` elements that are equivalent to them, then `std::abs(m - n)` of those elements will be copied to the output range, preserving order:

{/* source:cpp-algorithm-set-symmetric-difference:0003 */}
- if `m > n`, the final `m - n` of these elements from `[first1, last1)`.
- if `m < n`, the final `n - m` of these elements from `[first2, last2)`.

{/* source:cpp-algorithm-set-symmetric-difference:0004 */}
1. 1) If `[first1, last1)` or `[first2, last2)` is not [sorted](/docs/cpp/library/algorithm#Requirements) with respect to <InlineRevision until="C++20">`operator<`</InlineRevision><InlineRevision since="C++20">`std::less{}`</InlineRevision>, the behavior is undefined.

{/* source:cpp-algorithm-set-symmetric-difference:0005 */}
2. 3) If `[first1, last1)` or `[first2, last2)` is not sorted with respect to `comp`, the behavior is undefined.

{/* source:cpp-algorithm-set-symmetric-difference:0006 */}
3. 2,4) Same as (1,3), but executed according to `policy`.

{/* source:cpp-algorithm-set-symmetric-difference:0007 */}
4. These overloads participate in overload resolution only if all following conditions are satisfied:

   <Revision until="C++20">`std::is_execution_policy_v<std::decay_t<ExecutionPolicy>>` is `true`.</Revision>

   <Revision since="C++20">`std::is_execution_policy_v<std::remove_cvref_t<ExecutionPolicy>>` is `true`.</Revision>

{/* source:cpp-algorithm-set-symmetric-difference:0008 */}
If the output range overlaps with `[first1, last1)` or `[first2, last2)`, the behavior is undefined.

### Parameters

{/* source:cpp-algorithm-set-symmetric-difference:0009 */}
<ParameterList>
  <Parameter name="first1, last1">the first sorted range of elements</Parameter>
  <Parameter name="first2, last2">the second sorted range of elements</Parameter>
  <Parameter name="d_first">the beginning of the output range</Parameter>
  <Parameter name="policy">the [execution policy](/docs/cpp/library/algorithm/execution_policy_tag_t) to use</Parameter>
  <Parameter name="comp">
comparison function object (i.e. an object that satisfies the requirements of [*Compare*](/docs/cpp/library/named_req/Compare)) which returns `true` if the first argument is less than (i.e. is ordered before) the second.

The signature of the comparison function should be equivalent to the following:

`bool cmp(const Type1& a, const Type2& b);`

While the signature does not need to have `const&`, the function must not modify the objects passed to it and must be able to accept all values of type (possibly const) `Type1` and `Type2` regardless of [value category](/docs/cpp/language/expressions/value_category) (thus, `Type1&` is not allowed<InlineRevision since="C++11">, nor is `Type1` unless for `Type1` a move is equivalent to a copy</InlineRevision>). The types `Type1` and `Type2` must be such that objects of types `InputIt1` and `InputIt2` can be dereferenced and then implicitly converted to both `Type1` and `Type2`.
  </Parameter>
</ParameterList>

**Type requirements**

- `InputIt1`, `InputIt2` must meet the requirements of [*LegacyInputIterator*](/docs/cpp/library/named_req/InputIterator).
- `OutputIt` must meet the requirements of [*LegacyOutputIterator*](/docs/cpp/library/named_req/OutputIterator).
- `ForwardIt1`, `ForwardIt2`, `ForwardIt3` must meet the requirements of [*LegacyForwardIterator*](/docs/cpp/library/named_req/ForwardIterator).
- `Compare` must meet the requirements of [*Compare*](/docs/cpp/library/named_req/Compare).

### Return value

{/* source:cpp-algorithm-set-symmetric-difference:0010 */}
Iterator past the end of the constructed range.

### Complexity

{/* source:cpp-algorithm-set-symmetric-difference:0011 */}
Given `N1` as `std::distance(first1, last1)` and `N2` as `std::distance(first2, last2)`:

{/* source:cpp-algorithm-set-symmetric-difference:0012 */}
1. 1,2) At most `2⋅(N1+N2)-1` comparisons using <InlineRevision until="C++20">`operator<`</InlineRevision><InlineRevision since="C++20">`std::less{}`</InlineRevision>.

{/* source:cpp-algorithm-set-symmetric-difference:0013 */}
2. 3,4) At most `2⋅(N1+N2)-1` applications of the comparison function `comp`.

### Exceptions

{/* source:cpp-algorithm-set-symmetric-difference:0014 */}
The overloads with a template parameter named `ExecutionPolicy` report errors as follows:

{/* source:cpp-algorithm-set-symmetric-difference:0015 */}
- If execution of a function invoked as part of the algorithm throws an exception and `ExecutionPolicy` is one of the [standard policies](/docs/cpp/library/algorithm/execution_policy_tag_t), [`std::terminate`](/docs/cpp/library/error/terminate) is called. For any other `ExecutionPolicy`, the behavior is implementation-defined.
- If the algorithm fails to allocate memory, [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) is thrown.

### Possible implementation

{/* source:cpp-algorithm-set-symmetric-difference:0016 */}
[`set_symmetric_difference (1)`](/docs/cpp/library/algorithm/set_symmetric_difference#Version_1)

```cpp
template<class InputIt1, class InputIt2, class OutputIt>
OutputIt set_symmetric_difference(InputIt1 first1, InputIt1 last1,
                                  InputIt2 first2, InputIt2 last2, OutputIt d_first)
{
    while (first1 != last1)
    {
        if (first2 == last2)
            return std::copy(first1, last1, d_first);
 
        if (*first1 < *first2)
            *d_first++ = *first1++;
        else
        {
            if (*first2 < *first1)
                *d_first++ = *first2;
            else
                ++first1;
            ++first2;
        }
    }
    return std::copy(first2, last2, d_first);
}
```

[`set_symmetric_difference (3)`](/docs/cpp/library/algorithm/set_symmetric_difference#Version_3)

```cpp
template<class InputIt1, class InputIt2, class OutputIt, class Compare>
OutputIt set_symmetric_difference(InputIt1 first1, InputIt1 last1,
                                  InputIt2 first2, InputIt2 last2,
                                  OutputIt d_first, Compare comp)
{
    while (first1 != last1)
    {
        if (first2 == last2)
            return std::copy(first1, last1, d_first);
 
        if (comp(*first1, *first2))
            *d_first++ = *first1++;
        else
        {
            if (comp(*first2, *first1))
                *d_first++ = *first2;
            else
                ++first1;
            ++first2;
        }
    }
    return std::copy(first2, last2, d_first);
}
```

### Example

{/* source:cpp-algorithm-set-symmetric-difference:0017 */}
```cpp
#include <algorithm>
#include <iostream>
#include <iterator>
#include <vector>
 
int main()
{
    std::vector<int> v1{1, 2, 3, 4, 5, 6, 7, 8};
    std::vector<int> v2{5, 7, 9, 10};
    std::sort(v1.begin(), v1.end());
    std::sort(v2.begin(), v2.end());
 
    std::vector<int> v_symDifference;
 
    std::set_symmetric_difference(v1.begin(), v1.end(), v2.begin(), v2.end(),
                                  std::back_inserter(v_symDifference));
 
    for (int n : v_symDifference)
        std::cout << n << ' ';
    std::cout << '\n';
}
```

Output:

```text
1 2 3 4 6 8 9 10
```

### Defect reports

{/* source:cpp-algorithm-set-symmetric-difference:0018 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-algorithm-set-symmetric-difference:0019 */}
<DefectReportList>
  <DefectReport kind="lwg" id={291} standard="C++98">
    <PublishedBehavior>it was unspecified how to handle equivalent elements in the input ranges</PublishedBehavior>
    <CorrectedBehavior>specified</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-algorithm-set-symmetric-difference:0020 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`includes`](/docs/cpp/library/algorithm/includes)</DescriptionTerm>
    <DescriptionBody>returns `true` if one sequence is a subsequence of another (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`set_difference`](/docs/cpp/library/algorithm/set_difference)</DescriptionTerm>
    <DescriptionBody>computes the difference between two sets (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`set_union`](/docs/cpp/library/algorithm/set_union)</DescriptionTerm>
    <DescriptionBody>computes the union of two sets (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`set_intersection`](/docs/cpp/library/algorithm/set_intersection)</DescriptionTerm>
    <DescriptionBody>computes the intersection of two sets (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::set_symmetric_difference`](/docs/cpp/library/algorithm/ranges/set_symmetric_difference) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the symmetric difference between two sets (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
