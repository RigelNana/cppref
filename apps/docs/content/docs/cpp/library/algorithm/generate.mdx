---
title: "std::generate"
description: "Assigns each element in range [first, last) a value generated by the given function object g."
source_url: "https://en.cppreference.com/w/cpp/algorithm/generate"
language: "C++"
---

{/* source:cpp-algorithm-generate:0000 */}
<DeclarationDoc>
Defined in header [`<algorithm>`](/docs/cpp/library/headers/algorithm)

  <Declaration id="1" language="cpp" code={`template< class ForwardIt, class Generator >
void generate( ForwardIt first, ForwardIt last, Generator g );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class ExecutionPolicy, class ForwardIt, class Generator >
void generate( ExecutionPolicy&& policy,

               ForwardIt first, ForwardIt last, Generator g );`} />

  <InlineRevision since="C++20">constexpr</InlineRevision>
</DeclarationDoc>

{/* source:cpp-algorithm-generate:0001 */}
1. 1) Assigns each element in range [`first`, `last`) a value generated by the given function object `g`.

{/* source:cpp-algorithm-generate:0002 */}
2. 2) Same as (1), but executed according to `policy`.

{/* source:cpp-algorithm-generate:0003 */}
3. This overload participates in overload resolution only if all following conditions are satisfied:

   <Revision until="C++20">
   `std::is_execution_policy_v<std::decay_t<ExecutionPolicy>>` is `true`.
   </Revision>

   <Revision since="C++20">
   `std::is_execution_policy_v<std::remove_cvref_t<ExecutionPolicy>>` is `true`.
   </Revision>

### Parameters

{/* source:cpp-algorithm-generate:0004 */}
<ParameterList>
  <Parameter name="first, last">the range of elements to generate</Parameter>
  <Parameter name="policy">the [execution policy](/docs/cpp/library/algorithm/execution_policy_tag_t) to use</Parameter>
  <Parameter name="g">
generator function object that will be called. The signature of the function should be equivalent to the following:

`Ret fun();`

The type `Ret` must be such that an object of type `ForwardIt` can be dereferenced and assigned a value of type `Ret`.
  </Parameter>
</ParameterList>

Type requirements

- `ForwardIt` must meet the requirements of [*LegacyForwardIterator*](/docs/cpp/library/named_req/ForwardIterator).

### Complexity

{/* source:cpp-algorithm-generate:0005 */}
Exactly `std::distance(first, last)` invocations of `g()` and assignments.

### Exceptions

{/* source:cpp-algorithm-generate:0006 */}
The overload with a template parameter named `ExecutionPolicy` reports errors as follows:

{/* source:cpp-algorithm-generate:0007 */}
- If execution of a function invoked as part of the algorithm throws an exception and `ExecutionPolicy` is one of the [standard policies](/docs/cpp/library/algorithm/execution_policy_tag_t), [`std::terminate`](/docs/cpp/library/error/terminate) is called. For any other `ExecutionPolicy`, the behavior is implementation-defined.
- If the algorithm fails to allocate memory, [`std::bad_alloc`](/docs/cpp/library/memory/new/bad_alloc) is thrown.

### Possible implementation

{/* source:cpp-algorithm-generate:0008 */}
```cpp
template<class ForwardIt, class Generator>
constexpr //< since C++20
void generate(ForwardIt first, ForwardIt last, Generator g)
{
    for (; first != last; ++first)
        *first = g();
}
```

### Example

{/* source:cpp-algorithm-generate:0009 */}
```cpp
#include <algorithm>
#include <iostream>
#include <vector>
 
void println(std::string_view fmt, const auto& v)
{
    for (std::cout << fmt; const auto& e : v)
        std::cout << e << ' ';
    std::cout << '\n';
};
 
 
int f()
{
    static int i;
    return ++i;
}
 
int main()
{
    std::vector<int> v(5);
 
    std::generate(v.begin(), v.end(), f);
    println("v: ", v);
 
    // Initialize with default values 0,1,2,3,4 from a lambda function
    // Equivalent to std::iota(v.begin(), v.end(), 0);
    std::generate(v.begin(), v.end(), [n = 0] () mutable { return n++; });
    println("v: ", v);
}
```

Output:

```text
v: 1 2 3 4 5
v: 0 1 2 3 4
```

### See also

{/* source:cpp-algorithm-generate:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`fill`](/docs/cpp/library/algorithm/fill)</DescriptionTerm>
    <DescriptionBody>copy-assigns the given value to every element in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`generate_n`](/docs/cpp/library/algorithm/generate_n)</DescriptionTerm>
    <DescriptionBody>assigns the results of successive function calls to `N` elements in a range (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iota`](/docs/cpp/library/numeric/iota) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>fills a range with successive increments of the starting value (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::generate`](/docs/cpp/library/algorithm/ranges/generate) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>saves the result of a function in a range (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
