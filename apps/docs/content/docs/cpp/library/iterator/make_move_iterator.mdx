---
title: "std::make_move_iterator"
description: "make_move_iterator is a convenience function template that constructs a std::move_iterator for the given iterator i with the type deduced from the type of the argument."
source_url: "https://en.cppreference.com/w/cpp/iterator/make_move_iterator"
language: "C++"
---

{/* source:cpp-iterator-make-move-iterator:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" since="C++11" code={`template< class Iter >
std::move_iterator<Iter> make_move_iterator( Iter i );`} />
  (constexpr since C++17)
</DeclarationDoc>

{/* source:cpp-iterator-make-move-iterator:0001 */}
`make_move_iterator` is a convenience function template that constructs a [`std::move_iterator`](/docs/cpp/library/iterator/move_iterator) for the given iterator `i` with the type deduced from the type of the argument.

### Parameters

{/* source:cpp-iterator-make-move-iterator:0002 */}
<ParameterList>
  <Parameter name="i">input iterator to be converted to move iterator</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-make-move-iterator:0003 */}
`std::move_iterator<Iter>(std::move(i))`

### Example

{/* source:cpp-iterator-make-move-iterator:0004 */}
```cpp
#include <iomanip>
#include <iostream>
#include <iterator>
#include <list>
#include <string>
#include <vector>
 
auto print = [](const auto rem, const auto& seq)
{
    for (std::cout << rem; const auto& str : seq)
        std::cout << std::quoted(str) << ' ';
    std::cout << '\n';
};
 
int main()
{
    std::list<std::string> s{"one", "two", "three"};
 
    std::vector<std::string> v1(s.begin(), s.end()); // copy
 
    std::vector<std::string> v2(std::make_move_iterator(s.begin()),
                                std::make_move_iterator(s.end())); // move
 
    print("v1 now holds: ", v1);
    print("v2 now holds: ", v2);
    print("original list now holds: ", s);
}
```

Possible output:

```text
v1 now holds: "one" "two" "three" 
v2 now holds: "one" "two" "three" 
original list now holds: "" "" ""
```

### Defect reports

{/* source:cpp-iterator-make-move-iterator:0005 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-make-move-iterator:0006 */}
<DefectReportList>
  <DefectReport kind="lwg" id={2061} standard="C++11">
    <PublishedBehavior>`make_move_iterator` did not convert array arguments to pointers</PublishedBehavior>
    <CorrectedBehavior>made to convert</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-iterator-make-move-iterator:0007 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`move_iterator`](/docs/cpp/library/iterator/move_iterator) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>iterator adaptor which dereferences to an rvalue (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`move`](/docs/cpp/library/utility/move) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>converts the argument to an xvalue (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
