---
title: "std::input_iterator"
description: "The input_iterator concept is a refinement of input_or_output_iterator, adding the requirement that the referenced values can be read (via indirectly_readable) and the requirement that the iterator concept tag be present."
source_url: "https://en.cppreference.com/w/cpp/iterator/input_iterator"
language: "C++"
---

{/* source:cpp-iterator-input-iterator:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" since="C++20" code={`template< class I >
    concept input_iterator =
        std::input_or_output_iterator<I> &&
        std::indirectly_readable<I> &&
        requires { typename /*ITER_CONCEPT*/<I>; } &&

        std::derived_from</*ITER_CONCEPT*/<I>, std::input_iterator_tag>;
`} />
</DeclarationDoc>

{/* source:cpp-iterator-input-iterator:0001 */}
The `input_iterator` concept is a refinement of [`input_or_output_iterator`](/docs/cpp/library/iterator/input_or_output_iterator), adding the requirement that the referenced values can be read (via [`indirectly_readable`](/docs/cpp/library/iterator/indirectly_readable)) and the requirement that the iterator concept tag be present.

### Iterator concept determination

{/* source:cpp-iterator-input-iterator:0002 */}
Definition of this concept is specified via an exposition-only alias template `/*ITER_CONCEPT*/`.

{/* source:cpp-iterator-input-iterator:0003 */}
In order to determine `/*ITER_CONCEPT*/<I>`, let `ITER_TRAITS<I>` denote `I` if the specialization `std::iterator_traits<I>` is generated from the primary template, or `std::iterator_traits<I>` otherwise:

{/* source:cpp-iterator-input-iterator:0004 */}
- If `ITER_TRAITS<I>::iterator_concept` is valid and names a type, `/*ITER_CONCEPT*/<I>` denotes the type.
- Otherwise, if `ITER_TRAITS<I>::iterator_category` is valid and names a type, `/*ITER_CONCEPT*/<I>` denotes the type.
- Otherwise, if `std::iterator_traits<I>` is generated from the primary template, `/*ITER_CONCEPT*/<I>` denotes [`std::random_access_iterator_tag`](/docs/cpp/library/iterator/iterator_tags).
- Otherwise, `/*ITER_CONCEPT*/<I>` does not denote a type and results in a substitution failure.

### Notes

{/* source:cpp-iterator-input-iterator:0005 */}
Unlike the [*LegacyInputIterator*](/docs/cpp/library/named_req/InputIterator) requirements, the `input_iterator` concept does not require [`equality_comparable`](/docs/cpp/library/concepts/equality_comparable), since input iterators are typically compared with sentinels.

### Example

{/* source:cpp-iterator-input-iterator:0006 */}
A minimum input iterator.

{/* source:cpp-iterator-input-iterator:0007 */}
```cpp
#include <cstddef>
#include <iterator>

struct SimpleInputIterator
{
    using difference_type = std::ptrdiff_t;
    using value_type = int;

    int operator*() const;

    SimpleInputIterator& operator++();

    void operator++(int) { ++*this; }
};

static_assert(std::input_iterator<SimpleInputIterator>);
```

### See also

{/* source:cpp-iterator-input-iterator:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`input_or_output_iterator`](/docs/cpp/library/iterator/input_or_output_iterator) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that objects of a type can be incremented and dereferenced (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`forward_iterator`](/docs/cpp/library/iterator/forward_iterator) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that an `input_iterator` is a forward iterator, supporting equality comparison and multi-pass (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
