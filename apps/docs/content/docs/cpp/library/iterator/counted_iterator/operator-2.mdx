---
title: "operator-(std::counted_iterator<I>, std::default_sentinel_t)"
description: "Computes the signed distance to the end."
source_url: "https://en.cppreference.com/w/cpp/iterator/counted_iterator/operator-2"
language: "C++"
---

{/* source:cpp-iterator-counted-iterator-operator-2:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`friend constexpr std::iter_difference_t<I> operator-(
    const counted_iterator& x, std::default_sentinel_t );`} />
  <Declaration id="2" language="cpp" since="C++20" code={`friend constexpr std::iter_difference_t<I> operator-(
    std::default_sentinel_t, const counted_iterator& y );`} />

  <DeclarationDescription>
    {/* source:cpp-iterator-counted-iterator-operator-2:0001 */}
    1. 1) Returns the negative distance to the end.

    {/* source:cpp-iterator-counted-iterator-operator-2:0002 */}
    2. 2) Returns the positive distance to the end.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-iterator-counted-iterator-operator-2:0003 */}
This function template is not visible to ordinary [`unqualified`](/docs/cpp/language/basic_concepts/unqualified_lookup) or [`qualified lookup`](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [`argument-dependent lookup`](/docs/cpp/language/functions/adl) when `std::counted_iterator<I>` is an associated class of the arguments.

### Parameters

{/* source:cpp-iterator-counted-iterator-operator-2:0004 */}
<ParameterList>
  <Parameter name="x, y">iterator adaptors to compute the difference of</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-counted-iterator-operator-2:0005 */}
1. 1) `-x.count()`

{/* source:cpp-iterator-counted-iterator-operator-2:0006 */}
2. 2) `y.count()`

### Example

{/* source:cpp-iterator-counted-iterator-operator-2:0007 */}
```cpp
#include <initializer_list>
#include <iterator>
 
int main()
{
    constexpr static auto v = {1, 2, 3, 4};
    constexpr std::counted_iterator<std::initializer_list<int>::iterator>
        it{v.begin(), 3};
    constexpr auto d1 = it - std::default_sentinel;
    static_assert(d1 == -3); // (1)
    constexpr auto d2 = std::default_sentinel - it;
    static_assert(d2 == +3); // (2)
}
```

### See also

{/* source:cpp-iterator-counted-iterator-operator-2:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++ operator++(int) operator+= operator+ operator-- operator--(int) operator-= operator-`](/docs/cpp/library/iterator/counted_iterator/operator_arith) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>advances or decrements the iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`](/docs/cpp/library/iterator/counted_iterator/operator_plus) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>advances the iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator-`](/docs/cpp/library/iterator/counted_iterator/operator-) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the distance between two iterator adaptors (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`default_sentinel_t`](/docs/cpp/library/iterator/default_sentinel) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>default sentinel for use with iterators that know the bound of their range (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
