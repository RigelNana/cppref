---
title: "std::move_iterator<Iter>::operator[]"
description: "Returns a reference to the element at specified relative location."
source_url: "https://en.cppreference.com/w/cpp/iterator/move_iterator/operator_at"
language: "C++"
---

{/* source:cpp-iterator-move-iterator-operator-at:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" until="C++20" code={`/* unspecified */ operator[]( difference_type n ) const;`} />
  <Declaration language="cpp" since="C++20" code={`constexpr reference operator[]( difference_type n ) const;`} />
</DeclarationDoc>

{/* source:cpp-iterator-move-iterator-operator-at:0001 */}
Returns a reference to the element at specified relative location.

### Parameters

{/* source:cpp-iterator-move-iterator-operator-at:0002 */}
<ParameterList>
  <Parameter name="n">position relative to current location</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-move-iterator-operator-at:0003 */}
<InlineRevision until="C++20">`std::move(`[`current`](/docs/cpp/library/iterator/move_iterator#current)`[n])`</InlineRevision><InlineRevision since="C++20">[`ranges::iter_move`](/docs/cpp/library/iterator/ranges/iter_move)`(`[`current`](/docs/cpp/library/iterator/move_iterator#current)` + n)`</InlineRevision>

### Notes

{/* source:cpp-iterator-move-iterator-operator-at:0004 */}
<Revision until="C++20">
The return type is unspecified because the return type of the underlying iterator's `operator[]` is also unspecified (see [`LegacyRandomAccessIterator`](/docs/cpp/library/named_req/RandomAccessIterator)).
</Revision>

### Example

{/* source:cpp-iterator-move-iterator-operator-at:0005 */}
```cpp
#include <cstddef>
#include <iomanip>
#include <iostream>
#include <iterator>
#include <list>
#include <string>
#include <vector>

void print(auto rem, const auto& v)
{
    for (std::cout << rem; const auto& e : v)
        std::cout << std::quoted(e) << ' ';
    std::cout << '\n';
}

int main()
{
    std::vector<std::string> p{"alpha", "beta", "gamma", "delta"}, q;
    print("1) p: ", p);

    std::move_iterator it{p.begin()};

    for (std::size_t t{}; t != p.size(); ++t)
        q.emplace_back(it[t]); 

    print("2) p: ", p);
    print("3) q: ", q);

    std::list l{1, 2, 3};
    std::move_iterator it2{l.begin()};
//  it2[1] = 13; // Compilation error: the underlying iterator
                 // does not model the random access iterator
//  *it2 = 999;  // Compilation error: using rvalue as lvalue
}
```

Possible output:

```text
1) p: "alpha" "beta" "gamma" "delta"
2) p: "" "" "" ""
3) q: "alpha" "beta" "gamma" "delta"
```

### See also

{/* source:cpp-iterator-move-iterator-operator-at:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator* operator->`](/docs/cpp/library/iterator/move_iterator/operator_star_) <InlineRevision since="C++11" /> <InlineRevision since="C++11" /> (deprecated in C++20)</DescriptionTerm>
    <DescriptionBody>accesses the pointed-to element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
