---
title: "std::common_iterator<I,S>::common_iterator"
description: "Constructs a new iterator adaptor, effectively initializes the underlying std::variant<I, S> member object var to hold an I (iterator) or S (sentinel) object."
source_url: "https://en.cppreference.com/w/cpp/iterator/common_iterator/common_iterator"
language: "C++"
---

{/* source:cpp-iterator-common-iterator-common-iterator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr common_iterator() requires std::default_initializable<I> = default;`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr common_iterator( I i );`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr common_iterator( S s );`} />
  <Declaration id="4" language="cpp" since="C++20" code={`template< class I2, class S2 >
requires std::convertible_to<const I2&, I> && 
         std::convertible_to<const S2&, S>

constexpr common_iterator( const common_iterator<I2, S2>& x );`} />

  {/* source:cpp-iterator-common-iterator-common-iterator:0001 */}
  Constructs a new iterator adaptor, effectively initializes the underlying [`std::variant<I, S>`](/docs/cpp/library/utility/variant) member object `var` to hold an `I` (iterator) or `S` (sentinel) object.

  <DeclarationDescription>
    {/* source:cpp-iterator-common-iterator-common-iterator:0002 */}
    1. 1) Default constructor. Default-initializes `var`. After construction, `var` holds a value-initialized `I` object. Operations on the resulting iterator adaptor have defined behavior if and only if the corresponding operations on a value-initialized `I` also have defined behavior.

    {/* source:cpp-iterator-common-iterator-common-iterator:0003 */}
    2. 2) After construction, `var` holds an `I` object move-constructed from `i`.

    {/* source:cpp-iterator-common-iterator-common-iterator:0004 */}
    3. 3) After construction, `var` holds an `S` object move-constructed from `s`.

    {/* source:cpp-iterator-common-iterator-common-iterator:0005 */}
    4. 4) After construction, `var` holds an `I` or `S` object initialized from the `I2` or `S2` held by `x.var`, if `x.var` holds that alternative, respectively. The behavior is undefined if `x` is in an invalid state, that is, `x.var.valueless_by_exception()` is equal to `true`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-iterator-common-iterator-common-iterator:0006 */}
<ParameterList>
  <Parameter name="i">iterator to adapt</Parameter>
  <Parameter name="s">sentinel to adapt</Parameter>
  <Parameter name="x">iterator adaptor to copy</Parameter>
</ParameterList>

### Example

{/* source:cpp-iterator-common-iterator-common-iterator:0007 */}
```cpp
#include <algorithm>
#include <iostream>
#include <iterator>
#include <numeric>
#include <vector>
 
int main()
{
    std::vector v{3, 1, 4, 1, 5, 9, 2};
 
    using CI = std::common_iterator<
                   std::counted_iterator<std::vector<int>::iterator>,
                   std::default_sentinel_t>;
    CI unused; // (1)
    CI start{std::counted_iterator{std::next(begin(v)), ssize(v) - 2}}; // (2)
    CI finish{std::default_sentinel}; // (3)
    CI first{start}; // (4)
    CI last{finish}; // (4)
 
    std::copy(first, last, std::ostream_iterator<int>{std::cout, " "});
    std::cout << '\n';
 
    std::common_iterator<
        std::counted_iterator<
            std::ostream_iterator<double>>,
            std::default_sentinel_t>
            beg{std::counted_iterator{std::ostream_iterator<double>{std::cout,";  "}, 5}},
            end{std::default_sentinel};
    std::iota(beg, end, 3.1);
    std::cout << '\n';
}
```

Output:

```text
1 4 1 5 9
3.1;  4.1;  5.1;  6.1;  7.1;
```

### See also

{/* source:cpp-iterator-common-iterator-common-iterator:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/iterator/common_iterator/operator_assignment) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>assigns another iterator adaptor (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
