---
title: "std::common_iterator<I,S>::operator*,->"
description: "Returns pointer or reference to the current element, or a proxy holding it."
source_url: "https://en.cppreference.com/w/cpp/iterator/common_iterator/operator_star_"
language: "C++"
---

{/* source:cpp-iterator-common-iterator-operator-star:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++20" code={`constexpr decltype(auto) operator*();`} />
  <Declaration id="2" language="cpp" since="C++20" code={`constexpr decltype(auto) operator*() const
    requires /*dereferenceable*/<const I>;`} />
  <Declaration id="3" language="cpp" since="C++20" code={`constexpr auto operator->() const
    requires /* see description */;`} />

Helper types

  <Declaration id="4" language="cpp" since="C++20" code={`class /*proxy*/ {
    std::iter_value_t<I> keep_;
    constexpr proxy(std::iter_reference_t<I>&& x)
        : keep_(std::move(x)) {}
public:
    constexpr const std::iter_value_t<I>* operator->() const noexcept {
        return std::addressof(keep_);
    }

};`} />
  (4) (exposition only*)

  {/* source:cpp-iterator-common-iterator-operator-star:0001 */}
  Returns pointer or reference to the current element, or a proxy holding it.

  {/* source:cpp-iterator-common-iterator-operator-star:0002 */}
  The behavior is undefined if the underlying [`std::variant`](/docs/cpp/library/utility/variant) member object `var` does not hold an object of type `I`, i.e. `std::holds_alternative<I>(var)` is equal to `false`.

  {/* source:cpp-iterator-common-iterator-operator-star:0003 */}
  Let `it` denote the iterator of type `I` held by `var`, that is `std::get<I>(var)`.

  <DeclarationDescription>
    {/* source:cpp-iterator-common-iterator-operator-star:0004 */}
    1. 1,2) Returns the result of dereferencing `it`.

    {/* source:cpp-iterator-common-iterator-operator-star:0005 */}
    2. 3) Returns a pointer or underlying iterator to the current element, or a proxy holding it: Equivalent to `return it;`, if `I` is a pointer type or if the expression `it.operator->()` is well-formed. Otherwise, equivalent to `auto&& tmp = *it; return std::addressof(tmp);`, if `std::iter_reference_t<I>` is a reference type. Otherwise, equivalent to `return proxy(*it);`, where `proxy` is an exposition only class (4).

    {/* source:cpp-iterator-common-iterator-operator-star:0006 */}
    3. The expression in the requires-clause is equivalent to `std::indirectly_readable<const I> && ( requires(const I& i) { i.operator->(); } || std::is_reference_v<std::iter_reference_t<I>> || std::constructible_from<std::iter_value_t<I>, std::iter_reference_t<I>> )`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-iterator-common-iterator-operator-star:0007 */}
(none)

### Return value

{/* source:cpp-iterator-common-iterator-operator-star:0008 */}
1. 1,2) Reference to the current element, or prvalue temporary. Equivalent to `*it`.

{/* source:cpp-iterator-common-iterator-operator-star:0009 */}
2. 3) Pointer or iterator to the current element or proxy holding it as described above.

### Example

{/* source:cpp-iterator-common-iterator-operator-star:0010 */}
```cpp
#include <complex>
#include <initializer_list>
#include <iostream>
#include <iterator>
 
using std::complex_literals::operator""i;
 
int main()
{
    const auto il = {1i, 3.14 + 2i, 3i, 4i, 5i};
 
    using CI = std::common_iterator<
        std::counted_iterator<decltype(il)::iterator>,
        std::default_sentinel_t>;
 
    CI ci{std::counted_iterator{std::next(begin(il), 1), std::ssize(il) - 1}};
 
    std::cout << *ci << ' ' << ci->real() << '\n';
}
```

Output:

```text
(3.14,2) 3.14
```

### Defect reports

{/* source:cpp-iterator-common-iterator-operator-star:0011 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-common-iterator-operator-star:0012 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3574} standard="C++20">
    <PublishedBehavior>`variant` was fully constexpr (P2231R1) but `common_iterator` was not</PublishedBehavior>
    <CorrectedBehavior>also made constexpr</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3595} standard="C++20">
    <PublishedBehavior>functions of the proxy type lacked constexpr and noexcept</PublishedBehavior>
    <CorrectedBehavior>added</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3672} standard="C++20">
    <PublishedBehavior>`operator->` might return by reference in usual cases</PublishedBehavior>
    <CorrectedBehavior>always returns by value</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-iterator-common-iterator-operator-star:0013 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`(constructor)`](/docs/cpp/library/iterator/common_iterator/common_iterator) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>constructs a new iterator adaptor (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
