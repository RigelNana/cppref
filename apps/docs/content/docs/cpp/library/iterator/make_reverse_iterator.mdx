---
title: "std::make_reverse_iterator"
description: "make_reverse_iterator is a convenience function template that constructs a std::reverse_iterator for the given iterator i with the type deduced from the type of the argument."
source_url: "https://en.cppreference.com/w/cpp/iterator/make_reverse_iterator"
language: "C++"
---

{/* source:cpp-iterator-make-reverse-iterator:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" since="C++14" code={`template< class Iter >
std::reverse_iterator<Iter> make_reverse_iterator( Iter i );`} />
  (constexpr since C++17)
</DeclarationDoc>

{/* source:cpp-iterator-make-reverse-iterator:0001 */}
`make_reverse_iterator` is a convenience function template that constructs a [`std::reverse_iterator`](/docs/cpp/library/iterator/reverse_iterator) for the given iterator `i` (which must be a [*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator)) with the type deduced from the type of the argument.

### Parameters

{/* source:cpp-iterator-make-reverse-iterator:0002 */}
<ParameterList>
  <Parameter name="i">iterator to be converted to reverse iterator</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-make-reverse-iterator:0003 */}
`std::reverse_iterator<Iter>(i)`

### Notes

{/* source:cpp-iterator-make-reverse-iterator:0004 */}
| [Feature-test](/docs/cpp/library/utility/feature_test) macro | Value | Std | Feature |
| --- | --- | --- | --- |
| [`__cpp_lib_make_reverse_iterator`](/docs/cpp/library/feature_test#cpp_lib_make_reverse_iterator) | [`201402L`](/docs/cpp/library/compiler_support/14#cpp_lib_make_reverse_iterator_201402L) | (C++14) | `std::make_reverse_iterator` |

### Example

{/* source:cpp-iterator-make-reverse-iterator:0005 */}
```cpp
#include <algorithm>
#include <iostream>
#include <iterator>
#include <vector>
 
int main()
{
    std::vector<int> v{1, 3, 10, 8, 22};
 
    std::sort(v.begin(), v.end());
    std::copy(v.begin(), v.end(), std::ostream_iterator<int>(std::cout, ", "));
    std::cout << '\n';
 
    std::copy(std::make_reverse_iterator(v.end()),
              std::make_reverse_iterator(v.begin()),
              std::ostream_iterator<int>(std::cout, ", "));
    std::cout << '\n';
}
```

Output:

```text
1, 3, 8, 10, 22,
22, 10, 8, 3, 1,
```

### See also

{/* source:cpp-iterator-make-reverse-iterator:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`reverse_iterator`](/docs/cpp/library/iterator/reverse_iterator)</DescriptionTerm>
    <DescriptionBody>iterator adaptor for reverse-order traversal (class template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rbegin`, `crbegin`](/docs/cpp/library/iterator/rbegin) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse iterator to the beginning of a container or array (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`rend`, `crend`](/docs/cpp/library/iterator/rend) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>returns a reverse end iterator for a container or array (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
