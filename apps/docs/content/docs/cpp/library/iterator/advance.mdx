---
title: "std::advance"
description: "Increments given iterator it by n elements."
source_url: "https://en.cppreference.com/w/cpp/iterator/advance"
language: "C++"
---

{/* source:cpp-iterator-advance:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration id="1" language="cpp" until="C++17" code={`template< class InputIt, class Distance >
void advance( InputIt& it, Distance n );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class InputIt, class Distance >
constexpr void advance( InputIt& it, Distance n );`} />

  <DeclarationDescription>
    {/* source:cpp-iterator-advance:0001 */}
    1. Increments given iterator `it` by `n` elements.
  </DeclarationDescription>
</DeclarationDoc>

{/* source:cpp-iterator-advance:0002 */}
If `n` is negative, the iterator is decremented. In this case, `InputIt` must meet the requirements of [*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator), otherwise the behavior is undefined.

### Parameters

{/* source:cpp-iterator-advance:0003 */}
<ParameterList>
  <Parameter name="it">iterator to be advanced</Parameter>
  <Parameter name="n">number of elements `it` should be advanced</Parameter>
</ParameterList>

Type requirements

- `InputIt` must meet the requirements of [*LegacyInputIterator*](/docs/cpp/library/named_req/InputIterator).

### Return value

{/* source:cpp-iterator-advance:0004 */}
(none)

### Complexity

{/* source:cpp-iterator-advance:0005 */}
Linear.

{/* source:cpp-iterator-advance:0006 */}
However, if `InputIt` additionally meets the requirements of [*LegacyRandomAccessIterator*](/docs/cpp/library/named_req/RandomAccessIterator), complexity is constant.

### Notes

{/* source:cpp-iterator-advance:0007 */}
The behavior is undefined if the specified sequence of increments or decrements would require that a non-incrementable iterator (such as the past-the-end iterator) is incremented, or that a non-decrementable iterator (such as the front iterator or the [singular](/docs/cpp/library/iterator#Dereferenceability_and_validity) iterator) is decremented.

### Possible implementation

{/* source:cpp-iterator-advance:0008 */}
See also the implementations in [libstdc++](https://github.com/gcc-mirror/gcc/blob/d9375e490072d1aae73a93949aa158fcd2a27018/libstdc%2B%2B-v3/include/bits/stl_iterator_base_funcs.h#L200) and [libc++](https://github.com/llvm-mirror/libcxx/blob/a12cb9d211019d99b5875b6d8034617cbc24c2cc/include/iterator#L582).

{/* source:cpp-iterator-advance:0009 */}
[Non-constexpr version](/docs/cpp/library/iterator/advance#Version_1)

```cpp
namespace detail
{
    template<class It>
    void do_advance(It& it, typename std::iterator_traits<It>::difference_type n,
                    std::input_iterator_tag)
    {
        while (n > 0)
        {
            --n;
            ++it;
        }
    }
 
    template<class It>
    void do_advance(It& it, typename std::iterator_traits<It>::difference_type n,
                    std::bidirectional_iterator_tag)
    {
        while (n > 0)
        {
            --n;
            ++it;
        }
        while (n < 0)
        {
            ++n;
            --it;
        }
    }
 
    template<class It>
    void do_advance(It& it, typename std::iterator_traits<It>::difference_type n,
                    std::random_access_iterator_tag)
    {
        it += n;
    }
} // namespace detail
 
template<class It, class Distance>
void advance(It& it, Distance n)
{
    detail::do_advance(it, typename std::iterator_traits<It>::difference_type(n),
                       typename std::iterator_traits<It>::iterator_category());
}
```

[constexpr version](/docs/cpp/library/iterator/advance#Version_2)

```cpp
template<class It, class Distance>
constexpr void advance(It& it, Distance n)
{
    using category = typename std::iterator_traits<It>::iterator_category;
    static_assert(std::is_base_of_v<std::input_iterator_tag, category>);
 
    auto dist = typename std::iterator_traits<It>::difference_type(n);
    if constexpr (std::is_base_of_v<std::random_access_iterator_tag, category>)
        it += dist;
    else
    {
        while (dist > 0)
        {
            --dist;
            ++it;
        }
        if constexpr (std::is_base_of_v<std::bidirectional_iterator_tag, category>)
            while (dist < 0)
            {
                ++dist;
                --it;
            }
    }
}
```

### Example

{/* source:cpp-iterator-advance:0010 */}
```cpp
#include <iostream>
#include <iterator>
#include <vector>
 
int main() 
{
    std::vector<int> v{3, 1, 4};
 
    auto vi = v.begin();
    std::advance(vi, 2);
    std::cout << *vi << ' ';
 
    vi = v.end();
    std::advance(vi, -2);
    std::cout << *vi << '\n';
}
```

Output:

```text
4 1
```

### See also

{/* source:cpp-iterator-advance:0011 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`next`](/docs/cpp/library/iterator/next) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>increment an iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`prev`](/docs/cpp/library/iterator/prev) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>decrement an iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`distance`](/docs/cpp/library/iterator/distance)</DescriptionTerm>
    <DescriptionBody>returns the distance between two iterators (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::advance`](/docs/cpp/library/iterator/ranges/advance) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>advances an iterator by given distance or to a given bound (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
