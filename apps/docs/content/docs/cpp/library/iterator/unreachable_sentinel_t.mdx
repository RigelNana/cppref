---
title: "std::unreachable_sentinel_t, std::unreachable_sentinel"
description: "unreachable_sentinel_t is an empty class type that can be used to denote the upper bound of an unbounded interval."
source_url: "https://en.cppreference.com/w/cpp/iterator/unreachable_sentinel_t"
language: "C++"
---

{/* source:cpp-iterator-unreachable-sentinel-t:0000 */}
<DeclarationDoc since="C++20">
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration id="1" language="cpp" code={`struct unreachable_sentinel_t;`} />
  <Declaration id="2" language="cpp" code={`inline constexpr unreachable_sentinel_t unreachable_sentinel{};`} />

  <DeclarationDescription>
    {/* source:cpp-iterator-unreachable-sentinel-t:0001 */}
    1. 1) `unreachable_sentinel_t` is an empty class type that can be used to denote the “upper bound” of an unbounded interval.

    {/* source:cpp-iterator-unreachable-sentinel-t:0002 */}
    2. 2) `unreachable_sentinel` is a constant of type `unreachable_sentinel_t`.
  </DeclarationDescription>
</DeclarationDoc>

### Non-member functions

{/* source:cpp-iterator-unreachable-sentinel-t:0003 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>`operator==` <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>compares an `unreachable_sentinel_t` with a value of any [`weakly_incrementable`](/docs/cpp/library/iterator/weakly_incrementable) type (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

{/* source:cpp-iterator-unreachable-sentinel-t:0004 */}
#### operator==(std::unreachable_sentinel_t)

<DeclarationDoc>
  <Declaration language="cpp" since="C++20" code={`template<std::weakly_incrementable I>
friend constexpr bool operator==( unreachable_sentinel_t, const I& ) noexcept

{ return false; }`} />
</DeclarationDoc>

`unreachable_sentinel_t` can be compared with any [`weakly_incrementable`](/docs/cpp/library/iterator/weakly_incrementable) type and the result is always `false`.

This function template is not visible to ordinary [unqualified](/docs/cpp/language/basic_concepts/unqualified_lookup) or [qualified lookup](/docs/cpp/language/basic_concepts/qualified_lookup), and can only be found by [argument-dependent lookup](/docs/cpp/language/functions/adl) when `std::unreachable_sentinel_t` is an associated class of the arguments.

### Example

{/* source:cpp-iterator-unreachable-sentinel-t:0005 */}
```cpp
#include <algorithm>
#include <cstddef>
#include <iostream>
#include <iterator>
 
template<class CharT>
constexpr std::size_t strlen(const CharT* s)
{
    return std::ranges::find(s, std::unreachable_sentinel, CharT{}) - s;
}
 
template<class CharT>
constexpr std::size_t find_first(const CharT* haystack, const CharT* needle)
{
    const char* needle_end = needle + strlen(needle);
    // search(begin, unreachable_sentinel) is usually more efficient than
    // search(begin, end) due to one less comparison per cycle.
    // But "needle" MUST BE PRESENT in the "haystack", otherwise the call
    // is UB (which is a compile-time error in constexpr context).
    auto found = std::ranges::search(haystack, std::unreachable_sentinel,
                                     needle, needle_end);
    return found.begin() - haystack;
}
 
int main()
{
    static_assert(strlen("The quick brown fox jumps over a lazy dog.") == 42);
    static_assert(find_first("unsigned short int", "short") == 9);
//  static_assert(find_first("long int", "float")); // compile-time error
}
```

### See also

{/* source:cpp-iterator-unreachable-sentinel-t:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::iota_view`, `views::iota`](/docs/cpp/library/ranges/iota_view) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a [`view`](/docs/cpp/library/ranges/view) consisting of a sequence generated by repeatedly incrementing an initial value (class template) (customization point object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
