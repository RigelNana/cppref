---
title: "std::prev"
description: "Return the nth predecessor (or -nth successor if n is negative) of iterator it."
source_url: "https://en.cppreference.com/w/cpp/iterator/prev"
language: "C++"
---

{/* source:cpp-iterator-prev:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration id="1" language="cpp" since="C++11" until="C++17" code={`template< class BidirIt >
BidirIt prev( BidirIt it, typename std::iterator_traits<BidirIt>::difference_type n = 1 );`} />
  <Declaration id="2" language="cpp" since="C++17" code={`template< class BidirIt >
constexpr

BidirIt prev( BidirIt it, typename std::iterator_traits<BidirIt>::difference_type n = 1 );`} />

  <DeclarationDescription>
    {/* source:cpp-iterator-prev:0001 */}
    1. Return the `n`th predecessor (or `-n`th successor if `n` is negative) of iterator `it`.
  </DeclarationDescription>
</DeclarationDoc>

### Parameters

{/* source:cpp-iterator-prev:0002 */}
<ParameterList>
  <Parameter name="it">an iterator</Parameter>
  <Parameter name="n">number of elements `it` should be descended</Parameter>
</ParameterList>

Type requirements

- `BidirIt` must meet the requirements of [*LegacyBidirectionalIterator*](/docs/cpp/library/named_req/BidirectionalIterator).

### Return value

{/* source:cpp-iterator-prev:0003 */}
An iterator of type `BidirIt` that holds the `n`th predecessor (or `-n`th successor if `n` is negative) of iterator `it`.

### Complexity

{/* source:cpp-iterator-prev:0004 */}
Linear.

{/* source:cpp-iterator-prev:0005 */}
However, if `BidirIt` additionally meets the requirements of [*LegacyRandomAccessIterator*](/docs/cpp/library/named_req/RandomAccessIterator), complexity is constant.

### Possible implementation

{/* source:cpp-iterator-prev:0006 */}
```cpp
template<class BidirIt>
constexpr // since C++17
BidirIt prev(BidirIt it, typename std::iterator_traits<BidirIt>::difference_type n = 1)
{
    std::advance(it, -n);
    return it;
}
```

### Notes

{/* source:cpp-iterator-prev:0007 */}
Although the expression `--c.end()` often compiles, it is not guaranteed to do so: `c.end()` is an rvalue expression, and there is no iterator requirement that specifies that decrement of an rvalue is guaranteed to work. In particular, when iterators are implemented as pointers or its `operator--` is lvalue-ref-qualified, `--c.end()` does not compile, while `std::prev(c.end())` does.

### Example

{/* source:cpp-iterator-prev:0008 */}
```cpp
#include <iostream>
#include <iterator>
#include <vector>
 
int main()
{
    std::vector<int> v{3, 1, 4};
 
    auto it = v.end();
    auto pv = std::prev(it, 2);
    std::cout << *pv << '\n';
 
    it = v.begin();
    pv = std::prev(it, -2);
    std::cout << *pv << '\n';
}
```

Output:

```text
1
4
```

### See also

{/* source:cpp-iterator-prev:0009 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`next`](/docs/cpp/library/iterator/next) <InlineRevision since="C++11" /></DescriptionTerm>
    <DescriptionBody>increment an iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`advance`](/docs/cpp/library/iterator/advance)</DescriptionTerm>
    <DescriptionBody>advances an iterator by given distance (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`distance`](/docs/cpp/library/iterator/distance)</DescriptionTerm>
    <DescriptionBody>returns the distance between two iterators (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`ranges::prev`](/docs/cpp/library/iterator/ranges/prev) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>decrement an iterator by a given distance or to a bound (algorithm function object)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
