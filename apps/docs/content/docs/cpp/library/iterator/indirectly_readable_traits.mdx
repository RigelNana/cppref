---
title: "std::indirectly_readable_traits"
description: "Computes the associated value type of the template argument."
source_url: "https://en.cppreference.com/w/cpp/iterator/indirectly_readable_traits"
language: "C++"
---

{/* source:cpp-iterator-indirectly-readable-traits:0000 */}
<DeclarationDoc since="C++20">
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration id="1" language="cpp" code={`template< class I >
struct indirectly_readable_traits {};`} />
  <Declaration id="2" language="cpp" code={`template< class T >
struct indirectly_readable_traits<T*> :

    /* cond-value-type */<T> {};`} />
  <Declaration id="3" language="cpp" code={`template< class I >
    requires std::is_array_v<I>
struct indirectly_readable_traits<I>;

{ using value_type = std::remove_cv_t<std::remove_extent_t<I>>; }`} />
  <Declaration id="4" language="cpp" code={`template< class T >
struct indirectly_readable_traits<const T> :

    indirectly_readable_traits<T> {};`} />
  <Declaration id="5" language="cpp" code={`template< /* has-member-value-type */ T >
struct indirectly_readable_traits<T> :

    /* cond-value-type */<typename T::value_type> {};`} />
  <Declaration id="6" language="cpp" code={`template< /* has-member-element-type */ T >
struct indirectly_readable_traits<T> :

    /* cond-value-type */<typename T::element_type> {};`} />
  <Declaration id="7" language="cpp" code={`template< /* has-member-value-type */ T >
    requires /* has-member-element-type */<T>

struct indirectly_readable_traits<T> {};`} />
  <Declaration id="8" language="cpp" code={`template< /* has-member-value-type */ T >
    requires /* has-member-element-type */<T> &&
             std::same_as<std::remove_cv_t<typename T::element_type>,
                          std::remove_cv_t<typename T::value_type>>
struct indirectly_readable_traits<T> :

    /* cond-value-type */<typename T::value_type> {};`} />

#### Helper classes and concepts

  <Declaration id="1" language="cpp" code={`template< class >
struct /* cond-value-type */ {};`} />
  (exposition only*)
  <Declaration id="2" language="cpp" code={`template< class T >
    requires std::is_object_v<T>
struct /* cond-value-type */ <T>

{ using value_type = std::remove_cv_t<T>; };`} />
  (exposition only*)
  <Declaration id="3" language="cpp" code={`template< class T >
concept /* has-member-value-type */ =

    requires { typename T::value_type; };`} />
  (exposition only*)
  <Declaration id="4" language="cpp" code={`template< class T >
concept /* has-member-element-type */ =

    requires { typename T::element_type; };`} />
  (exposition only*)
</DeclarationDoc>

{/* source:cpp-iterator-indirectly-readable-traits:0001 */}
Computes the associated value type of the template argument. If the associated value type exists, it is represented by the nested type `value_type`, otherwise `value_type` is not defined. A program may specialize `indirectly_readable_traits` for a [program-defined type](/docs/cpp/language/basic_concepts/type-id#Program-defined_type).

### Explanation

{/* source:cpp-iterator-indirectly-readable-traits:0002 */}
The specializations above can be informally described as below.

{/* source:cpp-iterator-indirectly-readable-traits:0003 */}
Given a type `T`, its associated value type `V` is determined as follows:

{/* source:cpp-iterator-indirectly-readable-traits:0004 */}
- If `T` is const-qualified, `V` is the associated value type of const-unqualified `T`.
- Otherwise, if `T` is an array type, `V` is the cv-unqualified array element type.
- Otherwise, a conditional value type `C` is determined first:

{/* source:cpp-iterator-indirectly-readable-traits:0005 */}
- If `T` is a pointer type, `C` is the pointed-to type.
- Otherwise, if `T` has nested types `value_type` and `element_type`:
  - If these types are the same (not considering cv-qualification), `C` is `typename T::value_type`.
  - Otherwise, `C` is undefined.
- Otherwise, if `T` has the nested type `value_type` but not `element_type`, `C` is `typename T::value_type`.
- Otherwise, if `T` has the nested type `element_type` but not `value_type`, `C` is `typename T::element_type`.
- Otherwise, `C` is undefined.

Then `V` is determined from `C` as follows:

- If `C` is undefined, or `C` is not an [object type](/docs/cpp/language/basic_concepts/type-id#Object_type), `V` is undefined.
- Otherwise, `V` is cv-unqualified `C`.

### Notes

{/* source:cpp-iterator-indirectly-readable-traits:0006 */}
`value_type` is intended for use with [`indirectly_readable`](/docs/cpp/library/iterator/indirectly_readable) types such as iterators. It is not intended for use with ranges.

### Example

{/* source:cpp-iterator-indirectly-readable-traits:0007 */}
This section is incomplete

Reason: no example

### Defect reports

{/* source:cpp-iterator-indirectly-readable-traits:0008 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-indirectly-readable-traits:0009 */}
<DefectReportList>
  <DefectReport kind="lwg" id={3446} standard="C++20">
    <PublishedBehavior>specializations (5,6) were ambiguous for types having both `value_type` and `element_type` nested types</PublishedBehavior>
    <CorrectedBehavior>added specialization (8)</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3541} standard="C++20">
    <PublishedBehavior>LWG 3446 introduced hard error for ambiguous cases that `value_type` and `element_type` are different</PublishedBehavior>
    <CorrectedBehavior>added specialization (7)</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-iterator-indirectly-readable-traits:0010 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`indirectly_readable`](/docs/cpp/library/iterator/indirectly_readable) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that a type is indirectly readable by applying `operator *` (concept)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iter_value_t`, `iter_reference_t`, `iter_const_reference_t`, `iter_difference_t`, `iter_rvalue_reference_t`, `iter_common_reference_t`](/docs/cpp/library/iterator/iter_t) <InlineRevision since="C++20" /> <InlineRevision since="C++20" /> <InlineRevision since="C++23" /> <InlineRevision since="C++20" /> <InlineRevision since="C++20" /> <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>computes the associated types of an iterator (alias template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`iterator_traits`](/docs/cpp/library/iterator/iterator_traits)</DescriptionTerm>
    <DescriptionBody>provides uniform interface to the properties of an iterator (class template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
