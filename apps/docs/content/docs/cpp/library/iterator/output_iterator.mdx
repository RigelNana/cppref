---
title: "std::output_iterator"
description: "The output_iterator concept is a refinement of input_or_output_iterator, adding the requirement that it can be used to write values of type and value category encoded by T (via indirectly_writable)."
source_url: "https://en.cppreference.com/w/cpp/iterator/output_iterator"
language: "C++"
---

{/* source:cpp-iterator-output-iterator:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" since="C++20" code={`template< class I, class T >
    concept output_iterator =
        std::input_or_output_iterator<I> &&
        std::indirectly_writable<I, T> &&
        requires(I i, T&& t) {
            *i++ = std::forward<T>(t); /* not required to be equality-preserving */

        };
`} />
</DeclarationDoc>

{/* source:cpp-iterator-output-iterator:0001 */}
The `output_iterator` concept is a refinement of [`input_or_output_iterator`](/docs/cpp/library/iterator/input_or_output_iterator), adding the requirement that it can be used to write values of type and value category encoded by `T` (via [`indirectly_writable`](/docs/cpp/library/iterator/indirectly_writable)). [`equality_comparable`](/docs/cpp/library/concepts/equality_comparable) is not required.

### Semantic requirements

{/* source:cpp-iterator-output-iterator:0002 */}
Let `E` be an expression such that `decltype((E))` is `T`, and `i` be a dereferenceable object of type `I`. `std::output_iterator<I, T>` is modeled only if all the concepts it subsumes are modeled, and `*i++ = E;` has effects equivalent to `*i = E; ++i;`.

### Equality preservation

{/* source:cpp-iterator-output-iterator:0003 */}
Expressions declared in [requires expressions](/docs/cpp/language/templates/requires) of the standard library concepts are required to be [equality-preserving](/docs/cpp/library/concepts#Equality_preservation) (except where stated otherwise).

### Notes

{/* source:cpp-iterator-output-iterator:0004 */}
Unlike the [*LegacyOutputIterator*](/docs/cpp/library/named_req/OutputIterator) requirements, the `output_iterator` concept does not require that the iterator category tag be defined.

{/* source:cpp-iterator-output-iterator:0005 */}
Algorithms on output iterators should be single pass.

### See also

{/* source:cpp-iterator-output-iterator:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`input_or_output_iterator`](/docs/cpp/library/iterator/input_or_output_iterator) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>specifies that objects of a type can be incremented and dereferenced (concept)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
