---
title: "std::reverse_iterator<Iter>::reverse_iterator"
description: "Constructs a new reverse_iterator."
source_url: "https://en.cppreference.com/w/cpp/iterator/reverse_iterator/reverse_iterator"
language: "C++"
---

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`reverse_iterator();`} />
  <Declaration id="2" language="cpp" since="C++17" code={`explicit reverse_iterator( iterator_type x );`} />
  <Declaration id="3" language="cpp" since="C++17" code={`template< class U >
reverse_iterator( const reverse_iterator<U>& other );`} />
</DeclarationDoc>

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0001 */}
Constructs a new `reverse_iterator`.

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0002 */}
| Overload | [`current`](/docs/cpp/library/iterator/reverse_iterator#current) |
| --- | --- |
| (1) | [`value-initialized`](/docs/cpp/language/initialization/value_initialization) |
| (2) | initialized with `x` |
| (3) | initialized with `other.current` |

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0003 */}
<Revision since="C++20">

    3. 3) The converting constructor. This overload participates in overload resolution only if `std::is_same_v<U, Iter>` is false and `std::convertible_to<const U&, Iter>` is modeled.

</Revision>

### Parameters

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0004 */}
<ParameterList>
  <Parameter name="x">iterator to adapt</Parameter>
  <Parameter name="other">iterator adaptor to copy</Parameter>
</ParameterList>

### Example

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0005 */}
```cpp
#include <cassert>
#include <concepts>
#include <iterator>
#include <vector>

int main()
{
    std::vector v{0, 1, 2, 3, 4};

    using ReverseIt = std::reverse_iterator<std::vector<int>::const_iterator>;

    ReverseIt i1; // overload (1)
    i1 = v.crbegin();
    assert(*i1 == 4);

    ReverseIt i2(i1); // overload (2)
    assert(i2[0] == 4);

    int x[]{1, 2, 3};
    auto i3 = std::reverse_iterator<int*>(x + std::size(x)); // overload (1)
    i3[0] = -3;
    assert(x[2] == -3);
    std::reverse_iterator<int const*> i4(i3); // overload (3): int => const int
    static_assert(std::convertible_to<decltype(i3)::value_type,
                                      decltype(i4)::value_type>);
    // i4[0] = 5; // Error: assignment of read-only location
}
```

### Defect reports

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={235} standard="C++98">
    <PublishedBehavior>the effect of overload [(1)](/docs/cpp/library/iterator/reverse_iterator/reverse_iterator#Version_1) was not specified</PublishedBehavior>
    <CorrectedBehavior>specified</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={1012} standard="C++98">
    <PublishedBehavior>overload [(1)](/docs/cpp/library/iterator/reverse_iterator/reverse_iterator#Version_1) default-initialized [`current`](/docs/cpp/library/iterator/reverse_iterator#current)</PublishedBehavior>
    <CorrectedBehavior>it is value-initialized</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="lwg" id={3435} standard="C++20">
    <PublishedBehavior>overload [(3)](/docs/cpp/library/iterator/reverse_iterator/reverse_iterator#Version_3) was not constrained</PublishedBehavior>
    <CorrectedBehavior>constrained</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-iterator-reverse-iterator-reverse-iterator:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator=`](/docs/cpp/library/iterator/reverse_iterator/operator_assignment)</DescriptionTerm>
    <DescriptionBody>assigns another iterator adaptor (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`make_reverse_iterator`](/docs/cpp/library/iterator/make_reverse_iterator) <InlineRevision since="C++14" /></DescriptionTerm>
    <DescriptionBody>creates a `std::reverse_iterator` of type inferred from the argument (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
