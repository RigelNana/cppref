---
title: "std::reverse_iterator<Iter>::operator++,+,+=,--,-,-="
description: "Increments or decrements the underlying iterator reversely."
source_url: "https://en.cppreference.com/w/cpp/iterator/reverse_iterator/operator_arith"
language: "C++"
---

{/* source:cpp-iterator-reverse-iterator-operator-arith:0000 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" since="C++17" code={`reverse_iterator& operator++();`} />
  <Declaration id="2" language="cpp" since="C++17" code={`reverse_iterator& operator--();`} />
  <Declaration id="3" language="cpp" since="C++17" code={`reverse_iterator operator++( int );`} />
  <Declaration id="4" language="cpp" since="C++17" code={`reverse_iterator operator--( int );`} />
  <Declaration id="5" language="cpp" since="C++17" code={`reverse_iterator operator+( difference_type n ) const;`} />
  <Declaration id="6" language="cpp" since="C++17" code={`reverse_iterator operator-( difference_type n ) const;`} />
  <Declaration id="7" language="cpp" since="C++17" code={`reverse_iterator& operator+=( difference_type n );`} />
  <Declaration id="8" language="cpp" since="C++17" code={`reverse_iterator& operator-=( difference_type n );`} />
</DeclarationDoc>

{/* source:cpp-iterator-reverse-iterator-operator-arith:0001 */}
Increments or decrements the underlying iterator reversely.

{/* source:cpp-iterator-reverse-iterator-operator-arith:0002 */}
| Overload | Equivalent to |
| --- | --- |
| (1) | `--current; return *this;` |
| (2) | `++current; return *this;` |
| (3) | `reverse_iterator tmp = *this; --current; return tmp;` |
| (4) | `reverse_iterator tmp = *this; ++current; return tmp;` |
| (5) | `return reverse_iterator(current - n);` |
| (6) | `return reverse_iterator(current + n);` |
| (7) | `current -= n; return *this;` |
| (8) | `current += n; return *this;` |

### Parameters

{/* source:cpp-iterator-reverse-iterator-operator-arith:0003 */}
<ParameterList>
  <Parameter name="n">position relative to [`current`](/docs/cpp/library/iterator/reverse_iterator#current) location</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-reverse-iterator-operator-arith:0004 */}
As described above.

### Example

{/* source:cpp-iterator-reverse-iterator-operator-arith:0005 */}
```cpp
#include <iostream>
#include <iterator>
#include <list>
#include <vector>

int main()
{
    std::vector v{0, 1, 2, 3, 4};
    auto rv = std::reverse_iterator{v.rbegin()};
    std::cout << *(++rv) << ' '; // 3
    std::cout << *(--rv) << ' '; // 4
    std::cout << *(rv + 3) << ' '; // 1
    rv += 3;
    std::cout << rv[0] << ' '; // 1
    rv -= 3;
    std::cout << rv[0] << '\n'; // 4

    std::list l{5, 6, 7, 8};
    auto rl = std::reverse_iterator{l.rbegin()};
    std::cout << *(++rl) << ' '; // OK: 3
    std::cout << *(--rl) << '\n'; // OK: 4
    // The following statements raise compilation error because the
    // underlying iterator does not model the random access iterator:
//  *(rl + 3) = 13;
//  rl += 3;
//  rl -= 3;
}
```

Output:

```text
3 4 1 1 4
7 8
```

### See also

{/* source:cpp-iterator-reverse-iterator-operator-arith:0006 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`](/docs/cpp/library/iterator/reverse_iterator/operator_plus)</DescriptionTerm>
    <DescriptionBody>advances the iterator (function template)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator-`](/docs/cpp/library/iterator/reverse_iterator/operator-)</DescriptionTerm>
    <DescriptionBody>computes the distance between two iterator adaptors (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
