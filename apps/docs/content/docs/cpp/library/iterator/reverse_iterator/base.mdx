---
title: "std::reverse_iterator<Iter>::base"
description: "Returns the underlying iterator."
source_url: "https://en.cppreference.com/w/cpp/iterator/reverse_iterator/base"
language: "C++"
---

{/* source:cpp-iterator-reverse-iterator-base:0000 */}
<DeclarationDoc>
  <Declaration language="cpp" since="C++17" code={`iterator_type base() const;`} />
</DeclarationDoc>

{/* source:cpp-iterator-reverse-iterator-base:0001 */}
Returns the underlying iterator.

### Return value

{/* source:cpp-iterator-reverse-iterator-base:0002 */}
[`current`](/docs/cpp/library/iterator/reverse_iterator#current)

### Notes

{/* source:cpp-iterator-reverse-iterator-base:0003 */}
The base iterator refers to the element that is next (from the `iterator_type` perspective) to the element the `reverse_iterator` is currently pointing to. That is `&*(this->base() - 1) == &*(*this)`.

### Example

{/* source:cpp-iterator-reverse-iterator-base:0004 */}
```cpp
#include <iostream>
#include <iterator>
#include <vector>

int main()
{
    std::vector<int> v = {0, 1, 2, 3, 4, 5};

    using RevIt = std::reverse_iterator<std::vector<int>::iterator>;

    const auto it = v.begin() + 3;
    RevIt r_it{it};

    std::cout << "*it == " << *it << '\n'
              << "*r_it == " << *r_it << '\n'
              << "*r_it.base() == " << *r_it.base() << '\n'
              << "*(r_it.base() - 1) == " << *(r_it.base() - 1) << '\n';

    RevIt r_end{v.begin()};
    RevIt r_begin{v.end()};

    for (auto it = r_end.base(); it != r_begin.base(); ++it)
        std::cout << *it << ' ';
    std::cout << '\n';

    for (auto it = r_begin; it != r_end; ++it)
        std::cout << *it << ' ';
    std::cout << '\n';
}
```

Output:

```text
*it == 3
*r_it == 2
*r_it.base() == 3
*(r_it.base() - 1) == 2
0 1 2 3 4 5
5 4 3 2 1 0
```

### See also

{/* source:cpp-iterator-reverse-iterator-base:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator* operator->`](/docs/cpp/library/iterator/reverse_iterator/operator_star_)</DescriptionTerm>
    <DescriptionBody>accesses the pointed-to element (public member function)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
