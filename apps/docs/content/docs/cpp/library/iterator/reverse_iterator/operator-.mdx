---
title: "operator-(std::reverse_iterator)"
description: "Returns the distance between two iterator adaptors."
source_url: "https://en.cppreference.com/w/cpp/iterator/reverse_iterator/operator-"
language: "C++"
---

{/* source:cpp-iterator-reverse-iterator-operator:0000 */}
<DeclarationDoc>
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" until="C++11" code={`template< class Iter1, class Iter2 >
typename reverse_iterator<Iter>::difference_type
    operator-( const reverse_iterator<Iter1>& lhs,
               const reverse_iterator<Iter2>& rhs );`} />
  <Declaration language="cpp" since="C++11" code={`template< class Iter1, class Iter2 >
auto operator-( const reverse_iterator<Iter1>& lhs,
                const reverse_iterator<Iter2>& rhs )

    -> decltype(rhs.base() - lhs.base());`} />
  (constexpr since C++17)
</DeclarationDoc>

{/* source:cpp-iterator-reverse-iterator-operator:0001 */}
Returns the distance between two iterator adaptors.

### Parameters

{/* source:cpp-iterator-reverse-iterator-operator:0002 */}
<ParameterList>
  <Parameter name="lhs, rhs">iterator adaptors to compute the difference of</Parameter>
</ParameterList>

### Return value

{/* source:cpp-iterator-reverse-iterator-operator:0003 */}
`rhs.base() - lhs.base()`

### Notes

{/* source:cpp-iterator-reverse-iterator-operator:0004 */}
`operator-` returns `rhs.base() - lhs.base()` rather than `lhs.base() - rhs.base()` because this is a reverse iterator.

### Example

{/* source:cpp-iterator-reverse-iterator-operator:0005 */}
```cpp
#include <iostream>
#include <iterator>
#include <list>
#include <vector>

int main()
{
    std::vector vec{0, 1, 2, 3};
    std::reverse_iterator<std::vector<int>::iterator>
        vec_ri1{std::reverse_iterator{vec.rbegin()}},
        vec_ri2{std::reverse_iterator{vec.rend()}};
    std::cout << (vec_ri2 - vec_ri1) << ' '; // 4
    std::cout << (vec_ri1 - vec_ri2) << '\n'; // -4

    std::list lst{5, 6, 7, 8};
    std::reverse_iterator<std::list<int>::iterator>
        lst_ri1{std::reverse_iterator{lst.rbegin()}},
        lst_ri2{std::reverse_iterator{lst.rend()}};
//  auto n = (lst_ri1 - lst_ri2); // Error: the underlying iterators do not
                                  //        model random access iterator
}
```

Output:

```text
4 -4
```

### Defect reports

{/* source:cpp-iterator-reverse-iterator-operator:0006 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-reverse-iterator-operator:0007 */}
<DefectReportList>
  <DefectReport kind="lwg" id={280} standard="C++98">
    <PublishedBehavior>heterogeneous subtraction was not allowed</PublishedBehavior>
    <CorrectedBehavior>allowed</CorrectedBehavior>
  </DefectReport>
</DefectReportList>

### See also

{/* source:cpp-iterator-reverse-iterator-operator:0008 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`operator++ operator++(int) operator+= operator+ operator-- operator--(int) operator-= operator-`](/docs/cpp/library/iterator/reverse_iterator/operator_arith)</DescriptionTerm>
    <DescriptionBody>advances or decrements the iterator (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[`operator+`](/docs/cpp/library/iterator/reverse_iterator/operator_plus)</DescriptionTerm>
    <DescriptionBody>advances the iterator (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
