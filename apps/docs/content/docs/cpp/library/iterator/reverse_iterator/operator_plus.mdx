---
title: "operator+(std::reverse_iterator)"
description: "Defined in header <iterator> template < class Iter > reverse_iterator<Iter> operator+ ( typename reverse_iterator<Iter> :: difference_type n, const reverse_iterator<Iter> & it ) ; template < class Iter > constexpr reverse_iterator<Iter> operator+ ( std::iter_difference_t <Iter> n, const reverse_iterator<Iter> & it ) ;"
source_url: "https://en.cppreference.com/w/cpp/iterator/reverse_iterator/operator+"
language: "C++"
---

{/* source:cpp-iterator-reverse-iterator-operator:0000 */}
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

<DeclarationDoc>
  <Declaration language="cpp" until="C++23" code={`template< class Iter > 
reverse_iterator<Iter> operator+
    ( typename reverse_iterator<Iter>::difference_type n,

      const reverse_iterator<Iter>& it );`} />
  (constexpr since C++17) (until C++23)
  <Declaration language="cpp" since="C++23" code={`template< class Iter > 
constexpr reverse_iterator<Iter> operator+

    ( std::iter_difference_t<Iter> n, const reverse_iterator<Iter>& it );`} />
</DeclarationDoc>

{/* source:cpp-iterator-reverse-iterator-operator:0001 */}
Returns the iterator `it` incremented by `n`. In fact, the underlying iterator is decremented by `n`.

{/* source:cpp-iterator-reverse-iterator-operator:0002 */}
| n | - | the number of positions to increment the iterator |
| --- | --- | --- |
| it | - | the iterator adaptor to increment |

{/* source:cpp-iterator-reverse-iterator-operator:0003 */}
`reverse_iterator<Iter>(it.base() - n)`

{/* source:cpp-iterator-reverse-iterator-operator:0004 */}
Output:

```cpp
#include <iostream>
#include <iterator>
#include <list>
#include <vector>
 
int main()
{
    {
        std::vector v{0, 1, 2, 3};
        std::reverse_iterator<std::vector<int>::iterator>
            ri1{std::reverse_iterator{v.rbegin()}};
        std::cout << *ri1 << ' '; // 3
        std::reverse_iterator<std::vector<int>::iterator> ri2{2 + ri1};
        std::cout << *ri2 << ' '; // 1
    }
 
    {
        std::list l{5, 6, 7, 8};
        std::reverse_iterator<std::list<int>::iterator>
            ri1{std::reverse_iterator{l.rbegin()}};
        std::cout << *ri1 << '\n'; // 8
    //  auto ri2{2 + ri1}; // Error: the underlying iterator does not
                           //        model random access iterator
    }
}
```

Possible output:

```text
3 1 8
```

{/* source:cpp-iterator-reverse-iterator-operator:0005 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[operator++, operator++(int), operator+=, operator+, operator--, operator--(int), operator-=, operator-](/docs/cpp/library/iterator/reverse_iterator/operator_arith)</DescriptionTerm>
    <DescriptionBody>advances or decrements the iterator <br /> (public member function)</DescriptionBody>
  </DescriptionItem>
  <DescriptionItem>
    <DescriptionTerm>[operator-](/docs/cpp/library/iterator/reverse_iterator/operator-)</DescriptionTerm>
    <DescriptionBody>computes the distance between two iterator adaptors <br /> (function template)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>
