---
title: "std::indirect_strict_weak_order"
description: "The concept indirect_strict_weak_order specifies requirements for algorithms that call strict weak orders as their arguments."
source_url: "https://en.cppreference.com/w/cpp/iterator/indirect_strict_weak_order"
language: "C++"
---

{/* source:cpp-iterator-indirect-strict-weak-order:0000 */}
<DeclarationDoc since="C++20">
Defined in header [`<iterator>`](/docs/cpp/library/headers/iterator)

  <Declaration language="cpp" code={`template< class F, class I1, class I2 = I1 >
concept indirect_strict_weak_order =
    std::indirectly_readable<I1> &&
    std::indirectly_readable<I2> &&
    std::copy_constructible<F> &&
    std::strict_weak_order
        <F&, /*indirect-value-t*/<I1>, /*indirect-value-t*/<I2>> &&
    std::strict_weak_order
        <F&, /*indirect-value-t*/<I1>, std::iter_reference_t<I2>> &&
    std::strict_weak_order
        <F&, std::iter_reference_t<I1>, /*indirect-value-t*/<I2>> &&
    std::strict_weak_order

        <F&, std::iter_reference_t<I1>, std::iter_reference_t<I2>>;`} />
</DeclarationDoc>

{/* source:cpp-iterator-indirect-strict-weak-order:0001 */}
The concept `indirect_strict_weak_order` specifies requirements for algorithms that call strict weak orders as their arguments. The key difference between this concept and [`std::strict_weak_order`](/docs/cpp/library/concepts/strict_weak_order) is that it is applied to the types that `I1` and `I2` references, rather than `I1` and `I2` themselves.

### Defect reports

{/* source:cpp-iterator-indirect-strict-weak-order:0002 */}
The following behavior-changing defect reports were applied retroactively to previously published C++ standards.

{/* source:cpp-iterator-indirect-strict-weak-order:0003 */}
<DefectReportList>
  <DefectReport kind="paper" id="P2609R3" standard="C++20">
    <PublishedBehavior>some requirements were defined in terms of `std::iter_value_t<I>&` which mishandled projections resulting in incompatibility with strict weak order `F&`</PublishedBehavior>
    <CorrectedBehavior>defined in terms of `/*indirect-value-t*/<I>` to correctly handle such projections</CorrectedBehavior>
  </DefectReport>
  <DefectReport kind="paper" id="P2997R1" standard="C++20">
    <PublishedBehavior>`indirect_strict_weak_order` required `F&` to satisfy [`strict_weak_order`](/docs/cpp/library/concepts/strict_weak_order) with `std::iter_common_reference_t<I>`</PublishedBehavior>
    <CorrectedBehavior>does not require</CorrectedBehavior>
  </DefectReport>
</DefectReportList>
