---
title: "Filename and line information"
description: "Changes the source code's line number and, optionally, the current file name, in the preprocessor."
source_url: "https://en.cppreference.com/w/cpp/preprocessor/line"
language: "C++"
---

{/* source:cpp-preprocessor-line:0000 */}
Changes the source code's line number and, optionally, the current file name, in the preprocessor.

## Syntax

{/* source:cpp-preprocessor-line:0001 */}
<DeclarationDoc>
  <Declaration id="1" language="cpp" grammar code={`#line lineno`} />
  <Declaration id="2" language="cpp" grammar code={`#line lineno "filename"`} />
</DeclarationDoc>

{/* source:cpp-preprocessor-line:0002 */}
1. Changes the current preprocessor line number to `lineno`. Expansions of the macro `__LINE__` beyond this point will expand to `lineno` plus the number of actual source code lines encountered since.

{/* source:cpp-preprocessor-line:0003 */}
2. Also changes the current preprocessor file name to `filename`. Expansions of the macro `__FILE__` from this point will produce `filename`.

{/* source:cpp-preprocessor-line:0004 */}
Any preprocessing tokens (macro constants or expressions) are permitted as arguments to `#line` as long as they expand to a valid decimal integer optionally following a valid character string.

{/* source:cpp-preprocessor-line:0005 */}
`lineno` must be a sequence of at least one decimal digit (the program is ill-formed, otherwise) and is always interpreted as decimal (even if it starts with `0`).

{/* source:cpp-preprocessor-line:0006 */}
If `lineno` is `0` or greater than <InlineRevision until="C++11">`32767`</InlineRevision><InlineRevision since="C++11">`2147483647`</InlineRevision>, the behavior is undefined.

## Notes

{/* source:cpp-preprocessor-line:0007 */}
This directive is used by some automatic code generation tools which produce C++ source files from a file written in another language. In that case, `#line` directives may be inserted in the generated C++ file referencing line numbers and the file name of the original (human-editable) source file.

## Example

{/* source:cpp-preprocessor-line:0008 */}
```cpp
#include <cassert>
#define FNAME "test.cc"
int main()
{
#line 777 FNAME
        assert(2+2 == 5);
}
```

Possible output:

```text
test: test.cc:777: int main(): Assertion `2+2 == 5' failed.
```

## References

{/* source:cpp-preprocessor-line:0009 */}
C++23 standard (ISO/IEC 14882:2024): 15.7 Line control [cpp.line]

{/* source:cpp-preprocessor-line:0010 */}
C++20 standard (ISO/IEC 14882:2020): 15.7 Line control [cpp.line]

{/* source:cpp-preprocessor-line:0011 */}
C++17 standard (ISO/IEC 14882:2017): 19.4 Line control [cpp.line]

{/* source:cpp-preprocessor-line:0012 */}
C++14 standard (ISO/IEC 14882:2014): 16.4 Line control [cpp.line]

{/* source:cpp-preprocessor-line:0013 */}
C++11 standard (ISO/IEC 14882:2011): 16.4 Line control [cpp.line]

{/* source:cpp-preprocessor-line:0014 */}
C++98 standard (ISO/IEC 14882:1998): 16.4 Line control [cpp.line]

## See also

{/* source:cpp-preprocessor-line:0015 */}
<DescriptionList>
  <DescriptionItem>
    <DescriptionTerm>[`source_location`](/docs/cpp/library/utility/source_location) <InlineRevision since="C++20" /></DescriptionTerm>
    <DescriptionBody>a class representing information about the source code, such as file names, line numbers, and function names (class)</DescriptionBody>
  </DescriptionItem>
</DescriptionList>

[C documentation](/docs/c/language/preprocessor/line) for Filename and line information
