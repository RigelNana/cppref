---
title: "C++ keyword: not_eq"
description: "alternative operators: as an alternative for !="
source_url: "https://en.cppreference.com/w/cpp/keyword/not_eq"
language: "C++"
---

### Usage

{/* source:cpp-keyword-not-eq:0000 */}
- [alternative operators](/docs/cpp/language/expressions/operator_alternative): as an alternative for `!=`

### Example

{/* source:cpp-keyword-not-eq:0001 */}
```cpp
#include <iostream>

void show(bool z, const char* s, int n)
{
    const char* r { z ? " true  " : " false " };
    if (n == 0) std::cout << "┌────────────────────┬─────────┐\n";
    if (n <= 2) std::cout << "│ "      <<s<<     " │ "<<r<<" │\n";
    if (n == 2) std::cout << "└────────────────────┴─────────┘\n";
}

int main()
{
    show(false not_eq false, "false not_eq false", 0);
    show(false not_eq true , "false not_eq true ", 1);
    show(true  not_eq false, "true  not_eq false", 1);
    show(true  not_eq true , "true  not_eq true ", 2);
}
```

Output:

```text
┌────────────────────┬─────────┐
│ false not_eq false │  false  │
│ false not_eq true  │  true   │
│ true  not_eq false │  true   │
│ true  not_eq true  │  false  │
└────────────────────┴─────────┘
```

### See also

{/* source:cpp-keyword-not-eq:0002 */}
- [`and`](/docs/cpp/language/keywords/and), [`and_eq`](/docs/cpp/language/keywords/and_eq)
- [`bitand`](/docs/cpp/language/keywords/bitand), [`bitor`](/docs/cpp/language/keywords/bitor)
- [`not`](/docs/cpp/language/keywords/not)
- [`or`](/docs/cpp/language/keywords/or), [`or_eq`](/docs/cpp/language/keywords/or_eq)
- [`xor`](/docs/cpp/language/keywords/xor), [`xor_eq`](/docs/cpp/language/keywords/xor_eq)
