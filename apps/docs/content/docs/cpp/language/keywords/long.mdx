---
title: "C++ keyword: long"
description: "long type modifier"
source_url: "https://en.cppreference.com/w/cpp/keyword/long"
language: "C++"
---

### Usage

{/* source:cpp-keyword-long:0000 */}
- [`long` type modifier](/docs/cpp/language/basic_concepts/types#Modifiers)

### Example

{/* source:cpp-keyword-long:0001 */}
```cpp
#include <climits>
#include <concepts>
#include <iostream>
#include <limits>
#include <locale>

static_assert(
    sizeof(long) >= 32 / CHAR_BIT &&
    sizeof(unsigned long) >= 32 / CHAR_BIT &&
    std::numeric_limits<long>::min() <= -2'147'483'647l - 1 && //'
    std::numeric_limits<long>::max() >= 2'147'483'647l && //'
    std::numeric_limits<unsigned long>::max() >= 4'294'967'295ul); //'

// 'long long' and 'unsigned long long' are available since C++11
static_assert(
    sizeof(long long) >= 64 / CHAR_BIT &&
    sizeof(unsigned long long) >= 64 / CHAR_BIT &&
    std::numeric_limits<long long>::min() <= -9'223'372'036'854'775'807ll - 1 &&
    std::numeric_limits<long long>::max() >= 9'223'372'036'854'775'807ll &&
    std::numeric_limits<unsigned long long>::max() >= 18'446'744'073'709'551'615ull);

// concepts are available since C++20
static_assert(
    std::integral<long> &&
    std::integral<long long> &&
    std::integral<unsigned long> &&
    std::integral<unsigned long long> &&
    std::floating_point<long double>);
// Note that 'long' cannot modify 'float'

static_assert(
    std::same_as<decltype(0l), signed long int> &&
    std::same_as<decltype(0ll), signed long long int> &&
    std::same_as<decltype(0ul), unsigned long int> &&
    std::same_as<decltype(0ull), unsigned long long int> &&
    std::same_as<decltype(0.l), long double>);

template <typename T, typename... Ts>
concept all_same = (... and std::same_as <T, Ts>);

static_assert(
    all_same<long, long int, signed long, signed long int> &&
    all_same<unsigned long, unsigned long int> &&
    all_same<long long, long long int, signed long long, signed long long int> &&
    all_same<unsigned long long, unsigned long long int>);

#define OUT(...) std::cout << #__VA_ARGS__ << " = " << __VA_ARGS__ << '\n'

int main()
{
    std::cout.imbue(std::locale("en_US.UTF-8"));

    OUT(sizeof(long));
    OUT(std::numeric_limits<long>::min());
    OUT(std::numeric_limits<long>::max());
    OUT(std::numeric_limits<unsigned long>::max());

    OUT(sizeof(long long));
    OUT(std::numeric_limits<long long>::min());
    OUT(std::numeric_limits<long long>::max());
    OUT(std::numeric_limits<unsigned long long>::max());
}

#undef OUT
```

Possible output:

```text
sizeof(long) = 8
std::numeric_limits<long>::min() = -9,223,372,036,854,775,808
std::numeric_limits<long>::max() = 9,223,372,036,854,775,807
std::numeric_limits<unsigned long>::max() = 18,446,744,073,709,551,615
sizeof(long long) = 8
std::numeric_limits<long long>::min() = -9,223,372,036,854,775,808
std::numeric_limits<long long>::max() = 9,223,372,036,854,775,807
std::numeric_limits<unsigned long long>::max() = 18,446,744,073,709,551,615
```

### See also

{/* source:cpp-keyword-long:0002 */}
- [Fundamental types](/docs/cpp/language/basic_concepts/types): [`void`](/docs/cpp/language/keywords/void)<InlineRevision since="C++11">, [`std::nullptr_t`](/docs/cpp/library/utility/types/nullptr_t)</InlineRevision>. [Integral types](/docs/cpp/language/basic_concepts/types#Integral_types): [`int`](/docs/cpp/language/keywords/int). [Modifiers](/docs/cpp/language/basic_concepts/types#Modifiers): [`signed`](/docs/cpp/language/keywords/signed), [`unsigned`](/docs/cpp/language/keywords/unsigned), [`short`](/docs/cpp/language/keywords/short), [`long`](/docs/cpp/language/keywords/long). [Boolean type](/docs/cpp/language/basic_concepts/types#Boolean_type): [`bool`](/docs/cpp/language/keywords/bool). [Boolean literals](/docs/cpp/language/expressions/bool_literal): [`false`](/docs/cpp/language/keywords/false), [`true`](/docs/cpp/language/keywords/true).
- [Character types](/docs/cpp/language/basic_concepts/types#Character_types): [`char`](/docs/cpp/language/keywords/char)<InlineRevision since="C++20">, [`char8_t`](/docs/cpp/language/keywords/char8_t)</InlineRevision><InlineRevision since="C++11">, [`char16_t`](/docs/cpp/language/keywords/char16_t), [`char32_t`](/docs/cpp/language/keywords/char32_t)</InlineRevision>, [`wchar_t`](/docs/cpp/language/keywords/wchar_t). [Floating-point types](/docs/cpp/language/basic_concepts/types#Floating-point_types): [`float`](/docs/cpp/language/keywords/float), [`double`](/docs/cpp/language/keywords/double).
