---
title: "C++ keyword: while"
description: "while loop: as the declaration of the loop"
source_url: "https://en.cppreference.com/w/cpp/keyword/while"
language: "C++"
---

### Usage

{/* source:cpp-keyword-while:0000 */}
- [`while`](/docs/cpp/language/statements/while) loop: as the declaration of the loop
- [`do-while`](/docs/cpp/language/statements/do) loop: as the declaration of the terminating condition of the loop

### Example

{/* source:cpp-keyword-while:0001 */}
```cpp
#include <iostream>
 
int main() noexcept
{
    int i{3};
 
    // The following 'while' loop statement:
    // 1. (condition) Checks if value of the variable 'i' is greater than zero
    //                and if not, ends the loop execution with end of this point.
    //                Post-decrements the variable 'i' (decreases its value by 1).
    // 2. (statement) Writes out a current value of the variable 'i' to the stdout.
    // 3.             Returns to the point 1 (condition).
 
    while (i --> 0)     // condition: i-- > 0
        std::cout << i; // statement: std::cout << i;
}
```

Output:

```text
210
```

### See also

{/* source:cpp-keyword-while:0002 */}
- [`if`](/docs/cpp/language/statements/if) statement: [`if`](/docs/cpp/language/keywords/if), [`else`](/docs/cpp/language/keywords/else)

{/* source:cpp-keyword-while:0003 */}
<Revision since="C++17">
- [`constexpr` `if` statement](/docs/cpp/language/statements/if#Constexpr_if): [`constexpr`](/docs/cpp/language/keywords/constexpr) (`constexpr` `if` statement)
</Revision>

<Revision since="C++23">
- [`consteval` `if` statement](/docs/cpp/language/statements/if#Consteval_if): [`consteval`](/docs/cpp/language/keywords/consteval) (`consteval` `if` statement)
</Revision>

{/* source:cpp-keyword-while:0004 */}
- [`switch`](/docs/cpp/language/statements/switch) statement: [`switch`](/docs/cpp/language/keywords/switch), [`case`](/docs/cpp/language/keywords/case)
- [`default`](/docs/cpp/language/statements/switch) (as case label declaration) etc: [`default`](/docs/cpp/language/keywords/default)
- [`goto`](/docs/cpp/language/statements/goto) statement: [`goto`](/docs/cpp/language/keywords/goto)
- [`continue`](/docs/cpp/language/statements/continue) statement: [`continue`](/docs/cpp/language/keywords/continue)
- [`break`](/docs/cpp/language/statements/break) statement: [`break`](/docs/cpp/language/keywords/break)
- [`return`](/docs/cpp/language/statements/return) statement: [`return`](/docs/cpp/language/keywords/return)

{/* source:cpp-keyword-while:0005 */}
<Revision since="C++20">
- [`co_return`](/docs/cpp/language/statements/return) statement (return from a coroutine): [`co_return`](/docs/cpp/language/keywords/co_return)
</Revision>

{/* source:cpp-keyword-while:0006 */}
- [`do-while`](/docs/cpp/language/statements/do) loop and [`while` loop](/docs/cpp/language/statements/while): [`do`](/docs/cpp/language/keywords/do)
- [`for`](/docs/cpp/language/statements/for) loop and [range-based `for` loop](/docs/cpp/language/statements/range-for): [`for`](/docs/cpp/language/keywords/for)

