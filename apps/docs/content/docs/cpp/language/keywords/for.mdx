---
title: "C++ keyword: for"
description: "for loop: as the declaration of the loop"
source_url: "https://en.cppreference.com/w/cpp/keyword/for"
language: "C++"
---
### Usage

{/* source:cpp-keyword-for:0000 */}
- [`for`](/docs/cpp/language/statements/for) loop: as the declaration of the loop

{/* source:cpp-keyword-for:0001 */}
<Revision since="C++11">
- range-based [`for`](/docs/cpp/language/statements/range-for) loop: as the declaration of the loop
</Revision>

### Example

{/* source:cpp-keyword-for:0002 */}
```cpp
#include <iostream>
 
int main() noexcept
{
    // The following 'for' loop statement:
    // 1. (init-statement) Declares an integer named 'i' and initializes it with value '0'.
    // 2. (condition)      Checks if i is less than 3 and if not, ends the loop execution.
    // 3. (statement)      Writes out a current value of the integer 'i' to the stdout.
    // 4. (expression)     Pre-increments the integer 'i' (increases its value by 1).
    // 5.                  Returns to the point 2 (condition).
 
                                // init-statement: int i{0};
                                // condition:      i < 3
    for (int i{0}; i < 3; ++i)  // expression:     ++i
        std::cout << i;         // statement:      std::cout << i;
}
```

Output:

```text
012
```

### See also

{/* source:cpp-keyword-for:0003 */}
- [`if`](/docs/cpp/language/statements/if) statement: [`if`](/docs/cpp/language/keywords/if), [`else`](/docs/cpp/language/keywords/else)

{/* source:cpp-keyword-for:0004 */}
<Revision since="C++17">
- [`constexpr` `if` statement](/docs/cpp/language/statements/if#Constexpr_if): [`constexpr`](/docs/cpp/language/keywords/constexpr) (`constexpr` `if` statement)
</Revision>

<Revision since="C++23">
- [`consteval` `if` statement](/docs/cpp/language/statements/if#Consteval_if): [`consteval`](/docs/cpp/language/keywords/consteval) (`consteval` `if` statement)
</Revision>

{/* source:cpp-keyword-for:0005 */}
- [`switch`](/docs/cpp/language/statements/switch) statement: [`switch`](/docs/cpp/language/keywords/switch), [`case`](/docs/cpp/language/keywords/case)
- [`default`](/docs/cpp/language/statements/switch) (as case label declaration) etc: [`default`](/docs/cpp/language/keywords/default)
- [`goto`](/docs/cpp/language/statements/goto) statement: [`goto`](/docs/cpp/language/keywords/goto)
- [`continue`](/docs/cpp/language/statements/continue) statement: [`continue`](/docs/cpp/language/keywords/continue)
- [`break`](/docs/cpp/language/statements/break) statement: [`break`](/docs/cpp/language/keywords/break)
- [`return`](/docs/cpp/language/statements/return) statement: [`return`](/docs/cpp/language/keywords/return)

{/* source:cpp-keyword-for:0006 */}
<Revision since="C++20">
- [`co_return`](/docs/cpp/language/statements/return) statement (return from a coroutine): [`co_return`](/docs/cpp/language/keywords/co_return)
</Revision>

{/* source:cpp-keyword-for:0007 */}
- [`do-while`](/docs/cpp/language/statements/do) loop and [`while` loop](/docs/cpp/language/statements/while): [`do`](/docs/cpp/language/keywords/do), [`while`](/docs/cpp/language/keywords/while)
