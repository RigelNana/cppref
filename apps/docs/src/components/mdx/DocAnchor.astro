---
import type { HTMLAttributes } from "astro/types";
import { withBase } from "@/lib/urls";

interface Props extends HTMLAttributes<"a"> {
  href?: string;
}

const { href = "", ...attributes } = Astro.props;
---

<a {...attributes} href={withBase(href)}><slot /></a>
